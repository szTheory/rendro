---
phase: 48
plan: 01
type: execute
wave: 1
depends_on: ["47-03"]
files_modified:
  - lib/rendro/embedded_file_registry.ex
  - lib/rendro/document.ex
  - lib/rendro.ex
  - lib/rendro/rules/check_embedded_files.ex
  - lib/rendro/pipeline/validate.ex
  - test/rendro/embedded_file_registry_test.exs
  - test/rendro/document_test.exs
  - test/rendro_builders_test.exs
  - test/rendro/rules/check_embedded_files_test.exs
  - test/rendro/pipeline/validate_test.exs
autonomous: true
requirements:
  - EMBED-01
  - EMBED-02
  - EMBED-03
must_haves:
  truths:
    - Developers can register document-level embedded files through explicit Rendro-authored inputs.
    - Embedded-file metadata is explicit, deterministic, and stored on the document as pure data.
    - Invalid or ambiguous embedded-file state fails during `Rendro.Pipeline.Validate` before PDF serialization begins.
  artifacts:
    - path: lib/rendro/embedded_file_registry.ex
      provides: "Owned embedded-file registration and normalization contract"
    - path: lib/rendro/rules/check_embedded_files.ex
      provides: "Author-boundary validation for embedded-file semantics"
  key_links:
    - from: lib/rendro.ex
      to: lib/rendro/document.ex
      via: "`Rendro.register_embedded_file/4` remains a thin public wrapper"
    - from: lib/rendro/document.ex
      to: lib/rendro/embedded_file_registry.ex
      via: "documents own embedded-file registrations as pure data"
    - from: lib/rendro/pipeline/validate.ex
      to: lib/rendro/rules/check_embedded_files.ex
      via: "embedded-file invariants join the default validation pipeline"
---

<objective>
Add the authored embedded-file contract to core before any PDF writer changes land.

Purpose: Make embedded files a first-class, document-owned, deterministic input surface instead of an ad hoc metadata escape hatch.
Output: Registration API, owned registry data, and validate-stage rejection of malformed or duplicate embedded-file state.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.9-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-embedded-file-core-surface/48-RESEARCH.md
@.planning/phases/48-embedded-file-core-surface/48-PATTERNS.md
@.planning/phases/48-embedded-file-core-surface/48-VALIDATION.md
</execution_context>

<interfaces>
From `lib/rendro/document.ex`:
```elixir
defstruct ...,
          font_registry: Rendro.FontRegistry.new(),
          asset_registry: Rendro.AssetRegistry.new(),
          ...
```

From `lib/rendro/asset_registry.ex`:
```elixir
@spec register_image(t(), atom(), {:binary, binary()} | {:path, Path.t()}) :: t()
def register_image(%__MODULE__{} = registry, logical_name, source) when is_atom(logical_name) do
  ...
end
```

From `lib/rendro/pipeline/validate.ex`:
```elixir
@default_rules [CheckReferences, CheckBounds, CheckRequiredKeys, CheckFormFields]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a document-owned embedded-file registration surface</name>
  <files>lib/rendro/embedded_file_registry.ex, lib/rendro/document.ex, lib/rendro.ex, test/rendro/embedded_file_registry_test.exs, test/rendro/document_test.exs, test/rendro_builders_test.exs</files>
  <behavior>
    - Documents can register one or more embedded files through explicit API calls.
    - Registration eagerly normalizes `{:path, path}` and `{:binary, bytes}` sources into owned bytes.
    - Each entry stores explicit authored metadata such as filename, MIME type, optional description, and optional authored timestamps.
  </behavior>
  <action>
    Create `lib/rendro/embedded_file_registry.ex` following the existing registry pattern from `48-PATTERNS.md`, not a writer-owned side table.
    Extend `%Rendro.Document{}` with an embedded-file registry field and add `Document.register_embedded_file/4`.
    Add a top-level `Rendro.register_embedded_file/4` wrapper so the public surface stays parallel with image/font registration.
    Keep metadata explicit: require filename and MIME type at registration time, store description/timestamps only when provided, and avoid any automatic timestamp defaults.
    Add focused registry, document, and builder tests proving purity, eager path normalization, and explicit metadata storage.
  </action>
  <verify>
    <automated>mix test test/rendro/embedded_file_registry_test.exs test/rendro/document_test.exs test/rendro_builders_test.exs</automated>
  </verify>
  <done>Embedded files are first-class document-owned inputs with a narrow deterministic builder surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject ambiguous embedded-file state in the validate stage</name>
  <files>lib/rendro/rules/check_embedded_files.ex, lib/rendro/pipeline/validate.ex, test/rendro/rules/check_embedded_files_test.exs, test/rendro/pipeline/validate_test.exs</files>
  <behavior>
    - Duplicate embedded filenames fail before render.
    - Missing or malformed filename, MIME type, description, or timestamp inputs fail with typed validation tuples.
    - Validate-stage aggregation preserves the new tuples under `%Rendro.Error{details: %{errors: errors}}`.
  </behavior>
  <action>
    Add `lib/rendro/rules/check_embedded_files.ex` as a document-level validation rule following the tuple-based style from Phase 47, rather than pushing these checks into registration exceptions or writer guards.
    Register the new rule in `lib/rendro/pipeline/validate.ex` without changing traversal structure.
    Use explicit tuple errors for duplicate names and malformed metadata, and keep timestamps narrow: only authored `DateTime` values are accepted when present.
    Add raw rule tests plus pipeline aggregation tests so the authored-boundary proof exists independently of writer serialization.
  </action>
  <verify>
    <automated>mix test test/rendro/rules/check_embedded_files_test.exs test/rendro/pipeline/validate_test.exs</automated>
  </verify>
  <done>Embedded-file metadata ambiguity is trapped in core validation before serialization starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User API -> Core | Developer-authored embedded-file bytes and metadata enter the trusted contract boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | `Rendro.EmbeddedFileRegistry` | mitigate | Normalize sources into owned bytes immediately so later render steps do not depend on mutable external paths. |
| T-48-02 | Integrity | `Rendro.Rules.CheckEmbeddedFiles` | mitigate | Reject duplicate filenames and malformed metadata before writer code can guess or silently normalize unsupported state. |
</threat_model>

<verification>
mix test test/rendro/embedded_file_registry_test.exs test/rendro/document_test.exs test/rendro_builders_test.exs test/rendro/rules/check_embedded_files_test.exs test/rendro/pipeline/validate_test.exs
</verification>

<success_criteria>
- Developers can register document-level embedded files through an explicit document-owned API.
- Embedded-file bytes and metadata are stored as pure data on the document.
- Validation rejects duplicate or malformed embedded-file state before writer serialization begins.
</success_criteria>

<output>
After completion, create `.planning/phases/48-embedded-file-core-surface/48-01-SUMMARY.md`
</output>
