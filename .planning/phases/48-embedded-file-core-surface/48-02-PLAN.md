---
phase: 48
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
  - test/rendro/deterministic_test.exs
autonomous: true
requirements:
  - EMBED-01
  - EMBED-02
  - EMBED-03
must_haves:
  truths:
    - Validated embedded files serialize into the PDF catalog/object graph through a deterministic names-tree and file-spec surface.
    - Rendro emits document-level embedded files only; no page-level attachment annotations or generic annotation escape hatch are introduced.
    - Catalog wiring appears only when embedded files exist and is stable under deterministic output.
  artifacts:
    - path: lib/rendro/pdf/writer.ex
      provides: "Embedded-file object allocation, file-spec serialization, and catalog names-tree wiring"
    - path: test/rendro/pdf/writer_test.exs
      provides: "Structural proof for `/EmbeddedFile`, `/Filespec`, `/Names`, and `/AF` output"
  key_links:
    - from: lib/rendro/embedded_file_registry.ex
      to: lib/rendro/pdf/writer.ex
      via: "validated, normalized embedded-file descriptors feed deterministic object allocation"
    - from: lib/rendro/pdf/writer.ex
      to: test/rendro/pdf/writer_test.exs
      via: "substring assertions lock the PDF object graph as contract"
---

<objective>
Serialize the Phase 48 embedded-file surface into the PDF catalog and object graph without widening Rendro into review-style attachment features.

Purpose: Turn the authored embedded-file registry into deterministic PDF `/EmbeddedFile` and `/Filespec` objects referenced from the catalog.
Output: Stable writer allocation and proof tests for names-tree, file-spec, and `/AF` wiring.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.9-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-embedded-file-core-surface/48-RESEARCH.md
@.planning/phases/48-embedded-file-core-surface/48-PATTERNS.md
@.planning/phases/48-embedded-file-core-surface/48-VALIDATION.md
</execution_context>

<context>
@lib/rendro/pdf/writer.ex
@lib/rendro/pdf/object.ex
@test/rendro/pdf/writer_test.exs
@test/rendro/deterministic_test.exs

<interfaces>
From `lib/rendro/pdf/writer.ex`:
```elixir
{font_objects, next_num} = allocate_font_nums(fonts, next_num)
{image_objects, next_num} = allocate_image_nums(images, next_num)
{form_field_objects, next_num} = allocate_form_field_nums(form_fields, next_num)
```

From `lib/rendro/pdf/writer.ex`:
```elixir
catalog_dict =
  {:dict, maybe_add_acro_form_entry(pages_num, form_field_objects)}
```

From `lib/rendro/pdf/writer.ex`:
```elixir
defp encode_pdf_name(name) do
  ...
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic embedded-file object allocation and serialization</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <behavior>
    - Each embedded file emits one embedded-file stream object and one file-spec object.
    - File specs reference the stream through `/EF` and expose explicit metadata through `/F`, `/UF`, and optional `/Desc`.
    - The writer does not emit any embedded-file catalog entries when the document has no embedded files.
  </behavior>
  <action>
    Extend `lib/rendro/pdf/writer.ex` with embedded-file allocation/build helpers following the existing `allocate_*_nums` and `build_*_objects` pattern from `48-PATTERNS.md`, not a one-off inline serializer.
    Serialize embedded files through document-level objects only: `/Type /EmbeddedFile` streams and `/Type /Filespec` dictionaries. Keep page annotation logic untouched.
    If MIME subtype emission needs PDF-name escaping, reuse the existing `encode_pdf_name/1` helper instead of adding a second encoding implementation.
    Add writer tests that assert the presence of `/Type /EmbeddedFile`, `/Type /Filespec`, `/EF`, `/F`, `/UF`, and `/Desc`, and that no embedded-file catalog surface appears in a control document without attachments.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Writer output contains deterministic embedded-file stream and file-spec objects without widening into new annotation surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire catalog `/Names` and `/AF` deterministically</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs, test/rendro/deterministic_test.exs</files>
  <behavior>
    - Catalog `/Names << /EmbeddedFiles ... >>` and `/AF [...]` are present when embedded files exist.
    - Embedded-file ordering is stable in deterministic mode.
    - Page-level `/Annots` behavior remains limited to form widgets and later link work.
  </behavior>
  <action>
    Add catalog helper logic parallel to the existing AcroForm injection seam so embedded-file catalog entries are opt-in and isolated.
    Sort embedded-file allocations by a stable key before building the names array and `/AF` references.
    Include `/Params` metadata such as size and checksum, plus authored dates when present, only from validated inputs.
    Add writer and deterministic tests proving names-tree ordering and confirming the phase does not mutate page-level annotation behavior.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs test/rendro/deterministic_test.exs</automated>
  </verify>
  <done>Catalog embedded-file surfaces are deterministic, isolated, and structurally proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated registry -> PDF writer | Only prevalidated embedded-file descriptors should cross into object allocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Integrity | `Rendro.PDF.Writer` | mitigate | Use stable allocation/order helpers so identical inputs always yield identical embedded-file catalog wiring. |
| T-48-04 | Scope creep | `Rendro.PDF.Writer` | mitigate | Limit Phase 48 to catalog-level file specs and streams; do not touch page-level attachment annotations or generic annotation support. |
</threat_model>

<verification>
mix test test/rendro/pdf/writer_test.exs test/rendro/deterministic_test.exs
</verification>

<success_criteria>
- Writer emits deterministic `/EmbeddedFile` and `/Filespec` objects for registered embedded files.
- Catalog `/Names` and `/AF` are present only when embedded files exist and remain stably ordered.
- No page-level file-attachment annotation or generic annotation surface is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/48-embedded-file-core-surface/48-02-SUMMARY.md`
</output>
