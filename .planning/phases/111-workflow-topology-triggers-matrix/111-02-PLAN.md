---
phase: 111-workflow-topology-triggers-matrix
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified: [".github/workflows/ci.yml"]
autonomous: true
requirements: ["FLOW-03", "FLOW-04", "FLOW-05"]
must_haves:
  truths:
    - Pull requests run a fast test suite on a single Elixir version
    - Scheduled runs execute a broad version matrix
    - A single job represents the overall success of the pipeline
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Matrix and Gate config
      contains: "ci-success"
  key_links:
    - from: "ci-success"
      to: "branch protection"
      via: "GitHub required checks"
---

<objective>
Implement version matrix policy and summary gate job.

Purpose: Optimize PR execution speed by running a single target, whilst covering broad compatibility on nightly schedules. Stabilize branch protection using a single required-check name (`ci-success`).
Output: Finalized `ci.yml` with proper triggers, explicit matrix strategy for the minimum and latest supported versions, and the `ci-success` gate.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-workflow-topology-triggers-matrix/111-CONTEXT.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Triggers and Version Matrix</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Update `on:` triggers to include a `schedule` (e.g., `- cron: '0 2 * * *'`) for nightly runs.
    Refactor the `test` job to define a version matrix using `strategy.matrix.include` with both the latest supported version (`otp: '28'`, `elixir: '1.19.5'`, `primary: true`) and the minimum supported version (`otp: '25'`, `elixir: '1.19.0'`, `primary: false`) per D-09 and `mix.exs`.
    Explicitly implement the `pull_request` vs `schedule` execution logic by adding an `if: github.event_name != 'pull_request' || matrix.primary == true` condition to the `test` job so PRs only run the fast gate, while schedule/push runs the full broad matrix.
    Replace hardcoded Elixir/OTP versions in `erlef/setup-beam` with `${{ matrix.otp }}` and `${{ matrix.elixir }}`.
    Wrap linting/formatting steps (e.g., `mix format --check-formatted`, `mix credo`) with `if: matrix.primary == true` to ensure they only run once, avoiding redundant linting on older versions.
  </action>
  <verify>
    <automated>grep -A 10 "matrix:" .github/workflows/ci.yml | grep -q "1.19.0" && grep -q "github.event_name != 'pull_request'" .github/workflows/ci.yml</automated>
  </verify>
  <done>Triggers support Schedule, the matrix defines minimum/latest, and PRs correctly run only the primary version.</done>
</task>

<task type="auto">
  <name>Task 2: Add Summary Gate Job</name>
  <files>.github/workflows/ci.yml</files>
  <action>Append a `ci-success` job at the bottom of the file (per D-04 and D-05). It must have `if: always()` and `needs: [test, example-phoenix, integration-proofs, advisory-checks]`. Note: `advisory-checks` has `continue-on-error: true` so it won't fail the pipeline natively, but we should depend on it here to ensure it finishes before CI is marked complete. It should evaluate `needs.*.result` and exit with `1` if any strictly-required job failed or was unexpectedly cancelled (e.g., `if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')`, then `run: exit 1`, else `run: exit 0`). This becomes the stable required-check name for branch protection.</action>
  <verify>
    <automated>grep -A 5 "ci-success:" .github/workflows/ci.yml | grep -q "if: always()"</automated>
  </verify>
  <done>The `ci-success` summary gate exists and accurately rolls up the results of needed jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Branch Protection -> CI Gate | Single source of truth for repository PR merges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-02 | Tampering | GitHub Branch Protection | mitigate | Single `ci-success` gate cannot be bypassed by path skips |
</threat_model>

<verification>
Ensure `ci.yml` is valid and the `ci-success` job captures the `needs` correctly.
</verification>

<success_criteria>
- `schedule` trigger is active.
- `matrix` strategy is used in `test` encompassing latest and minimum supported versions.
- `ci-success` evaluates `needs.*.result` correctly.
</success_criteria>

<output>
Create `.planning/phases/111-workflow-topology-triggers-matrix/111-02-SUMMARY.md` when done
</output>