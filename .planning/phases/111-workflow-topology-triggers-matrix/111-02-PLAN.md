---
phase: 111-workflow-topology-triggers-matrix
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified: [".github/workflows/ci.yml"]
autonomous: true
requirements: ["FLOW-03", "FLOW-04", "FLOW-05"]
must_haves:
  truths:
    - Pull requests run a fast test suite on a single Elixir version
    - Scheduled runs execute a broad version matrix
    - A single job represents the overall success of the pipeline
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Matrix and Gate config
      contains: "ci-success"
  key_links:
    - from: "ci-success"
      to: "branch protection"
      via: "GitHub required checks"
---

<objective>
Implement version matrix policy and summary gate job.

Purpose: Optimize PR execution speed by running a single target, whilst covering broad compatibility on nightly schedules. Stabilize branch protection using a single required-check name (`ci-success`).
Output: Finalized `ci.yml` with proper triggers, matrix strategy on the `test` job, and the `ci-success` gate.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-workflow-topology-triggers-matrix/111-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Triggers and Version Matrix</name>
  <files>.github/workflows/ci.yml</files>
  <action>Update `on:` to include a `schedule` trigger (e.g., `- cron: '0 2 * * *'`). Refactor the `test` job (and appropriately passed variables) to use `strategy: matrix: otp: ['28'], elixir: ['1.19.5']` (a single target currently, establishing the policy for future). Ensure that `pull_request` execution is decoupled from the broad matrix either by conditionals on the job or matrix (though with a single element, no behavior change is needed immediately, setup the structure). Crucially, ensure linting/formatting (`mix ci`) runs strictly once on the primary version if the matrix is expanded. Replace hardcoded version strings in `erlef/setup-beam` with `${{ matrix.otp }}` and `${{ matrix.elixir }}`.</action>
  <verify>
    <automated>grep -A 5 "strategy:" .github/workflows/ci.yml | grep -q "matrix:"</automated>
  </verify>
  <done>Triggers support PR/Push/Schedule and a strategy matrix structure is in place for `test`.</done>
</task>

<task type="auto">
  <name>Task 2: Add Summary Gate Job</name>
  <files>.github/workflows/ci.yml</files>
  <action>Append a `ci-success` job at the bottom of the file (per D-04 and D-05). It must have `if: always()` and `needs: [test, example-phoenix, integration-proofs]`. It should evaluate `needs.*.result` and exit with `1` if any required job failed or was unexpectedly cancelled (e.g., `if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')`, then `run: exit 1`, else `run: exit 0`). This becomes the stable required-check name for branch protection.</action>
  <verify>
    <automated>grep -A 5 "ci-success:" .github/workflows/ci.yml | grep -q "if: always()"</automated>
  </verify>
  <done>The `ci-success` summary gate exists and accurately rolls up the results of needed jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Branch Protection -> CI Gate | Single source of truth for repository PR merges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-02 | Tampering | GitHub Branch Protection | mitigate | Single `ci-success` gate cannot be bypassed by path skips |
</threat_model>

<verification>
Ensure `ci.yml` is valid and the `ci-success` job captures the `needs` correctly without depending on `advisory-checks`.
</verification>

<success_criteria>
- `schedule` trigger is active.
- `matrix` strategy is used in `test`.
- `ci-success` evaluates `needs.*.result` correctly.
</success_criteria>

<output>
Create `.planning/phases/111-workflow-topology-triggers-matrix/111-02-SUMMARY.md` when done
</output>
