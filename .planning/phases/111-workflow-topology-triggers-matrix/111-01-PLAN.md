---
phase: 111-workflow-topology-triggers-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".github/workflows/ci.yml"]
autonomous: true
requirements: ["FLOW-01", "FLOW-02"]
must_haves:
  truths:
    - Superseded PR runs are cancelled automatically
    - Main branch and release runs are never cancelled
    - Advisory jobs are merged into a single disconnected job
    - Live-proof jobs are merged into a single dependent job
  artifacts:
    - path: .github/workflows/ci.yml
      provides: CI configuration
      contains: "cancel-in-progress"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions"
      via: "concurrency group"
---

<objective>
Apply concurrency rules and merge disjointed integration and advisory jobs into two sequential jobs to minimize duplicated setup overhead.

Purpose: Save runner minutes, minimize the critical path, and cancel superseded PR runs while protecting main branch builds.
Output: Refactored `ci.yml` with concurrency block and condensed job topology.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-workflow-topology-triggers-matrix/111-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Concurrency Logic</name>
  <files>.github/workflows/ci.yml</files>
  <action>Add a `concurrency` block at the top level of `.github/workflows/ci.yml` (per D-06 and D-07). Set `group: ${{ github.workflow }}-${{ github.ref }}` and `cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}`.</action>
  <verify>
    <automated>grep -A 2 "concurrency:" .github/workflows/ci.yml | grep -q "cancel-in-progress"</automated>
  </verify>
  <done>Concurrency block is present and properly restricts cancellation to non-main branches.</done>
</task>

<task type="auto">
  <name>Task 2: Merge Advisory Jobs</name>
  <files>.github/workflows/ci.yml</files>
  <action>Merge `raster-advisory`, `comparison-advisory`, `livebook-advisory`, and `pdfjs-advisory` into a single `advisory-checks` job (per D-02). Condense the setup steps (checkout, beam setup, node setup, system dependencies like pdfium-cli) so they only run once. Then sequence the actual test runs (`mix test --include raster_snapshot...`, `mix rendro.comparison.check`, etc.) one after the other. Keep `continue-on-error: true` and DO NOT add a `needs:` block (it must remain graph-disconnected).</action>
  <verify>
    <automated>grep -q "advisory-checks:" .github/workflows/ci.yml && ! grep -q "raster-advisory:" .github/workflows/ci.yml</automated>
  </verify>
  <done>Advisory jobs are combined into `advisory-checks` and redundant setup is eliminated.</done>
</task>

<task type="auto">
  <name>Task 3: Merge Integration Jobs</name>
  <files>.github/workflows/ci.yml</files>
  <action>Merge `viewer-evidence-live-proof`, `signing-live-proof`, `long-lived-live-proof`, and `release-proof` into a single `integration-proofs` job (per D-01). Condense the setup steps (checkout, beam, python, poppler-utils/qpdf/pdfsig, pyhanko/certomancer). Run the elixir dependencies install once, then run the four proof scripts sequentially. Retain `needs: test` to ensure it only runs after unit tests pass.</action>
  <verify>
    <automated>grep -q "integration-proofs:" .github/workflows/ci.yml && ! grep -q "signing-live-proof:" .github/workflows/ci.yml</automated>
  </verify>
  <done>Live-proof jobs are combined into `integration-proofs` with shared setup dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> GitHub | Workflow runner actions and API tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Denial of Service | GitHub Actions Runner | mitigate | Concurrency rules cancel redundant runs |
</threat_model>

<verification>
Check `ci.yml` is valid YAML and the new jobs `advisory-checks` and `integration-proofs` exist, while the 8 old individual jobs are gone.
</verification>

<success_criteria>
- Concurrency block is present.
- 8 jobs condensed into 2.
- No `needs` on `advisory-checks`.
</success_criteria>

<output>
Create `.planning/phases/111-workflow-topology-triggers-matrix/111-01-SUMMARY.md` when done
</output>
