---
phase: 111-workflow-topology-triggers-matrix
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: ["priv/guardrails/required_status_checks.json", "test/guardrails/required_checks_contract_test.exs"]
autonomous: true
requirements: ["FLOW-01", "FLOW-05"]
must_haves:
  truths:
    - Guardrail baseline uses new topology job names
    - Guardrail tests assert the new topology
  artifacts:
    - path: "priv/guardrails/required_status_checks.json"
      provides: "New baseline topology"
      contains: "integration-proofs"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "New contract assertions"
      contains: "advisory-checks"
  key_links: []
---

<objective>
Update guardrail tests and baseline JSON to reflect the new condensed CI topology before `ci.yml` is modified.

Purpose: Ensure guardrail test contracts strictly expect the new pipeline topology (merging 8 jobs into 2, introducing `ci-success`) so they are ready when `ci.yml` is updated in subsequent waves.
Output: Updated `required_status_checks.json` and `required_checks_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-workflow-topology-triggers-matrix/111-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Baseline JSON</name>
  <files>priv/guardrails/required_status_checks.json</files>
  <action>Change `required_contexts` to only `["ci-success"]`. Replace the specific live-proof contexts with a single `integration-proofs` context, and `test`, and `ci-success`. Replace the multiple advisory contexts with a single `advisory-checks` context, keeping `example-phoenix` separate.</action>
  <verify>
    <automated>grep -q "ci-success" priv/guardrails/required_status_checks.json</automated>
  </verify>
  <done>Baseline JSON reflects the new merged topology.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Contract Tests</name>
  <files>test/guardrails/required_checks_contract_test.exs</files>
  <action>Update `@required_contexts` to `~w(ci-success)`. Rewrite tests that look for old job names (like `raster-advisory` or `signing-live-proof`) to instead look within the new `advisory-checks` or `integration-proofs` job blocks. Ensure the test logic correctly asserts the expected commands within the merged job definitions.</action>
  <verify>
    <automated>grep -q "integration-proofs" test/guardrails/required_checks_contract_test.exs</automated>
  </verify>
  <done>Tests are updated to assert the new CI job topology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> GitHub | Workflow runner actions and API tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-00 | Tampering | Contract Tests | mitigate | Ensure guardrail tests enforce branch protection on the correct gate job |
</threat_model>

<verification>
Check that both files reference the new jobs (`advisory-checks`, `integration-proofs`, `ci-success`) instead of the old separate jobs.
</verification>

<success_criteria>
- `required_status_checks.json` uses `ci-success` as the sole required context.
- `required_checks_contract_test.exs` is fully updated to expect the new structure.
</success_criteria>

<output>
Create `.planning/phases/111-workflow-topology-triggers-matrix/111-00-SUMMARY.md` when done
</output>
