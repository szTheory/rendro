---
phase: 117-edge-case-stress-matrix
plan: 06
type: execute
wave: 2
depends_on: ["117-01", "117-02"]
files_modified:
  - test/rendro/adapters/pdfium_raster_snapshot_test.exs
autonomous: true
requirements: [EDGE-01]
must_haves:
  truths:
    - "Exactly 6 curated raster fixtures exist, respecting D-10's ceiling (<=8 fixtures / <=16 page refs): 3 combined pagination+60plus+odd-even fixtures (Invoice/Statement/Payslip), 1 A4/Letter pair on Certificate (2 fixtures), and 1 extreme-wrap fixture (Invoice)"
    - "The new raster tests live inside the EXISTING test/rendro/adapters/pdfium_raster_snapshot_test.exs file (not a new file) so the CI job hardcoding that exact file path (Landmine 1) discovers them automatically -- no .github/workflows/ci.yml edit, per the locked in-fence decision"
    - "Every new raster test reuses the file's existing private assert_or_bless/2 helper -- no forked/duplicated bless logic"
    - "Every new raster fixture's PDF is proven deterministic (two-run byte-identical) before rasterization, via Rendro.Test.Golden.assert_deterministic!/1"
  artifacts:
    - test/rendro/adapters/pdfium_raster_snapshot_test.exs (extended)
  key_links:
    - "Reuses Rendro.Test.EdgeFixtures.document/2 (117-02) for 5 of the 6 fixtures (odd_even_running_content x3, text_wrap x1) and EdgeFixtures.build/2 + opts/2 directly for the Certificate A4/Letter pair (opts/2's page_size_a4_letter clause always returns :us_letter, so the A4 half of the pair calls document/2 with no opts on the same underlying data)"
    - "Reuses Rendro.Test.Golden.assert_deterministic!/1 (117-01) as a pre-rasterization determinism guard"
---

<objective>
Extend the EXISTING `test/rendro/adapters/pdfium_raster_snapshot_test.exs` in place with the 6 curated pdfium raster fixtures D-10 specifies -- a hard ceiling deliberately far short of exhaustive per-cell rasterization, reserved for the sub-grid where the correctness claim is placement GEOMETRY (page breaks, A4-vs-Letter dimensions, odd/even running-content parity, extreme text wrap) that a byte-content-stream diff cannot humanly verify.

Purpose: This is the locked in-fence resolution to RESEARCH.md's "Landmine 1" (the CI raster job hardcodes `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs`, a single explicit file path, not a tag-wide sweep) -- extending this exact file, rather than creating a new one, means the existing CI wiring discovers the new fixtures with zero `.github/workflows/ci.yml` edit, keeping the phase's edit surface fenced to `test/`, `test/support/`, and non-`lib/` `priv/`.
Output: 6 new `@tag raster_snapshot: true` tests inside `Rendro.Adapters.PdfiumRasterSnapshotTest`, reusing its existing private `assert_or_bless/2`/`assert_golden_hashes/2`/`bless_refs/2` helpers verbatim, plus `priv/raster_refs/<fixture>/page_N.sha256` refs (blessed separately in a pinned CI container per D-11 -- not authored by this plan).
</objective>

<artifacts_produced>
## Artifacts this phase produces

- 6 new `@tag raster_snapshot: true` tests added to the EXISTING `test/rendro/adapters/pdfium_raster_snapshot_test.exs` (no new test file, no new module):
  1. `edge_invoice_pagination_60plus_odd_even` -- Invoice, 3+ pages
  2. `edge_statement_pagination_60plus_odd_even` -- Statement, 3+ pages
  3. `edge_payslip_pagination_60plus_odd_even` -- Payslip, 2+ pages
  4. `edge_certificate_a4` -- Certificate at default A4 geometry
  5. `edge_certificate_us_letter` -- Certificate at US Letter geometry (same underlying data as #4)
  6. `edge_invoice_extreme_wrap` -- Invoice, narrow item-name column
- `priv/raster_refs/<fixture_name>/page_N.sha256` for all 6 fixtures -- created by a one-time `MIX_RASTER_BLESS=true GITHUB_ACTIONS=true mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs` run in the pinned CI container (D-11 -- not authored by this plan; the tests are written assert-only and will fail until that CI bless run happens, exactly like the existing `forms_support_fixture` case behaves today).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-edge-case-stress-matrix/117-CONTEXT.md
@.planning/phases/117-edge-case-stress-matrix/117-RESEARCH.md
@.planning/phases/117-edge-case-stress-matrix/117-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 3 combined pagination+60plus+odd-even raster fixtures (Invoice, Statement, Payslip)</name>
  <files>test/rendro/adapters/pdfium_raster_snapshot_test.exs</files>
  <read_first>
    test/rendro/adapters/pdfium_raster_snapshot_test.exs (full file, 96 lines -- extend this EXACT file; reuse `assert_or_bless/2` at lines 52-65, `assert_golden_hashes/2` at 72-81, `bless_refs/2` at 88-95 verbatim; the existing `@tag raster_snapshot: true` test shape at lines 28-36 is the template for each new test)
    test/support/edge_fixtures.ex (as written by 117-02 -- `Rendro.Test.EdgeFixtures.document/2`, specifically the `:odd_even_running_content` clause for `:invoice`/`:statement`/`:payslip`)
    test/support/golden.ex (as written by 117-01 -- `Rendro.Test.Golden.assert_deterministic!/1`)
    lib/rendro/adapters/pdfium.ex (lines 58-105 -- `Pdfium.render/2`: `dpi:` default 150, `pages:` default `nil` = ALL pages, sorted list of PNG binaries)
  </read_first>
  <behavior>
    - Each of the 3 new tests renders its `EdgeFixtures.document/2` output, proves two-run determinism via `Rendro.Test.Golden.assert_deterministic!/1`, rasterizes ALL pages via `Pdfium.render(pdf, dpi: 150)` (no `:pages` opt), and calls the file's existing private `assert_or_bless/2` with a unique `fixture_name` string and the resulting PNG list.
    - Each fixture's rendered page count is >= 2 (multi-page, proving the pagination claim) -- assert `length(pngs) >= 2` before calling `assert_or_bless/2` (a structural sanity check independent of the hash comparison).
  </behavior>
  <action>
    Add `alias Rendro.Test.EdgeFixtures` and `alias Rendro.Test.Golden` near the top of `Rendro.Adapters.PdfiumRasterSnapshotTest` (below the existing `alias Rendro.Adapters.Pdfium`).

    Add 3 new `@tag raster_snapshot: true` tests, each following the EXACT shape of the existing "forms support fixture" test (lines 28-36) but building the PDF via Rendro instead of reading a static fixture file: `doc = EdgeFixtures.document(:invoice, :odd_even_running_content)`, `pdf = Golden.assert_deterministic!(doc)`, `assert {:ok, pngs} = Pdfium.render(pdf, dpi: 150)`, `assert length(pngs) >= 2`, `assert_or_bless("edge_invoice_pagination_60plus_odd_even", pngs)`. Repeat identically for `:statement` (fixture name `"edge_statement_pagination_60plus_odd_even"`) and `:payslip` (fixture name `"edge_payslip_pagination_60plus_odd_even"`).

    Use the EXACT fixture-name strings given above -- they become the `priv/raster_refs/<fixture_name>/` directory names, which downstream CI blessing and any future maintainer reading the ref tree will rely on verbatim.
  </action>
  <acceptance_criteria>
    - 3 new `@tag raster_snapshot: true` tests exist inside the existing module, each reusing `assert_or_bless/2` unmodified
    - Each fixture renders >= 2 pages
    - `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs` (run locally with `pdfium-cli` available, unblessed) fails cleanly at the missing-ref stage for these 3 new fixtures (proving the assert-by-default path is reached, not skipped) -- OR, if `pdfium-cli` is unavailable in this environment, verify via `mix test --include raster_snapshot --exclude raster_snapshot` is not a valid combination; instead confirm compilation succeeds and the test names appear via `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs --trace --seed 0 2>&1 | grep -c "edge_"` equals 3 (proves the tests exist and attempt to run, independent of whether the pdfium-cli binary is installed in this sandbox)
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs --trace --seed 0 2>&amp;1 | grep -c "edge_"</automated>
  </verify>
  <done>3 new multi-page raster fixtures (Invoice/Statement/Payslip) exist in the existing raster test file, each simultaneously proving pagination + 60+ line items + odd/even running-content parity per D-10a.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Certificate A4/Letter pair + Invoice extreme-wrap raster fixture</name>
  <files>test/rendro/adapters/pdfium_raster_snapshot_test.exs</files>
  <read_first>
    test/support/edge_fixtures.ex (as written by 117-02 -- `Rendro.Test.EdgeFixtures.build/2` and `opts/2` for `:certificate, :page_size_a4_letter` -- `opts/2` for this pair always returns `[page_size: :us_letter]`, so the A4 half of the pair must call `Certificate.document(data)` directly with NO opts, reusing the SAME `build/2` data for both renders)
    lib/rendro/page_size.ex (full file -- A4 portrait 595.28x841.89 vs US Letter portrait 612x792; Certificate's default orientation is :landscape per its own moduledoc, so both renders in this pair stay landscape, only the page dimensions swap)
  </read_first>
  <behavior>
    - `edge_certificate_a4` and `edge_certificate_us_letter` render the SAME underlying `Certificate` data (`EdgeFixtures.build(:certificate, :page_size_a4_letter)`) at two different page geometries, producing two DIFFERENT PNG byte sequences (different page pixel dimensions at 150 DPI).
    - `edge_invoice_extreme_wrap` reuses `EdgeFixtures.document(:invoice, :text_wrap)` (the SAME data as the byte-golden `text_wrap` cell for Invoice, per RESEARCH's A3 recommendation -- Invoice's narrow `{:share, 1}` item-name column).
  </behavior>
  <action>
    Add a test for `edge_certificate_a4`: `data = EdgeFixtures.build(:certificate, :page_size_a4_letter)`, `doc = Rendro.Recipes.Certificate.document(data)` (no opts -- default A4), `pdf = Golden.assert_deterministic!(doc)`, `assert {:ok, pngs} = Pdfium.render(pdf, dpi: 150)`, `assert_or_bless("edge_certificate_a4", pngs)`.

    Add a test for `edge_certificate_us_letter`: same `data` (reuse `EdgeFixtures.build(:certificate, :page_size_a4_letter)`), `doc = Rendro.Recipes.Certificate.document(data, EdgeFixtures.opts(:certificate, :page_size_a4_letter))` (opts includes `page_size: :us_letter`), same render/bless pattern with fixture name `"edge_certificate_us_letter"`.

    Add a test for `edge_invoice_extreme_wrap`: `doc = EdgeFixtures.document(:invoice, :text_wrap)`, same render/bless pattern with fixture name `"edge_invoice_extreme_wrap"`.

    All 3 tests are `@tag raster_snapshot: true`, follow the existing file's exact structural shape, and reuse the SAME private `assert_or_bless/2`/`assert_golden_hashes/2`/`bless_refs/2` helpers -- do not fork or duplicate them.
  </action>
  <acceptance_criteria>
    - 3 new `@tag raster_snapshot: true` tests exist (6 total across both tasks of this plan)
    - `edge_certificate_a4` and `edge_certificate_us_letter` derive from the identical `data` map, varying only `document/2`'s `opts`
    - `edge_invoice_extreme_wrap` reuses the exact same `EdgeFixtures.document(:invoice, :text_wrap)` call the byte-golden matrix (117-04) uses for that cell
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs --trace --seed 0 2>&amp;1 | grep -c "edge_"</automated>
  </verify>
  <done>All 6 curated raster fixtures exist inside the existing pdfium_raster_snapshot_test.exs, respecting D-10's ceiling; no new file, no .github/workflows/ci.yml edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (test-only, no runtime surface) | Consumes only already-shipped `lib/` recipe output rendered through the existing, already-vendored `pdfium-cli` adapter. No new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-117-06-01 | Tampering | CI raster-job discoverability (Landmine 1) | medium | mitigate | Extending the EXISTING `pdfium_raster_snapshot_test.exs` file in place (not a new file) means the CI step's hardcoded single-file `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs` invocation discovers all 6 new fixtures automatically -- avoiding the silent-never-runs-in-CI gap RESEARCH.md flagged, with zero `.github/workflows/ci.yml` edit. |
| T-117-06-02 | Repudiation | Raster ref platform-portability | low | accept | Raster hashes are pinned to a specific `pdfium-cli` version (`priv/pdfium_pin.json`) and CI container, per D-11's existing bless-guard (`MIX_RASTER_BLESS=true` outside `GITHUB_ACTIONS` raises) -- an already-shipped mitigation this plan does not modify. |
</threat_model>

<verification>
`mix compile --warnings-as-errors`. `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs` -- full pinned-container run (blessing + verification) happens once in CI per D-11; local runs without a pinned `pdfium-cli` may not be meaningful for hash comparison but MUST compile and discover all 6 new test names.
</verification>

<success_criteria>
- Exactly 6 new raster fixtures exist inside the existing `pdfium_raster_snapshot_test.exs` (no new file).
- Each reuses the file's existing private bless/assert helpers unmodified.
- D-10's ceiling (<=8 fixtures) is respected; the raster lane stays advisory (not required-check-gating), as already configured.
</success_criteria>

<output>
Create `.planning/phases/117-edge-case-stress-matrix/117-06-SUMMARY.md` when done
</output>
