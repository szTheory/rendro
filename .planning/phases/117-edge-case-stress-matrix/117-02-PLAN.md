---
phase: 117-edge-case-stress-matrix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/edge_fixtures.ex
  - test/support/edge_fixtures_test.exs
autonomous: true
requirements: [EDGE-01, EDGE-02]
must_haves:
  truths:
    - "Every {family, dimension} pair EdgeFixtures dispatches to renders successfully through that family's real Rendro.Recipes.* recipe (no lib/ change required)"
    - "EdgeFixtures.document/2 raises loudly (ArgumentError) for any {family, dimension} pair that is not a recognized :applies cell -- never silently returns a nonsense document"
    - "The four EDGE-02 error fixtures (overflow, tall-row, RTL default-font, RTL shaping-required) are built from public Rendro structs/functions only, with zero lib/ edits (RTL uses the fake-font-registry clone technique, not a vendored font)"
    - "Money-dimension fixtures always pass each recipe's own Decimal.equal?/2 caller-assertion (Invoice totals, Payslip reconciliation) so document/2 never raises on a money-edge fixture"
  artifacts:
    - test/support/edge_fixtures.ex
    - test/support/edge_fixtures_test.exs
  key_links:
    - "test/rendro/edge_matrix_test.exs (117-04, Wave 2) calls Rendro.Test.EdgeFixtures.document/2 for every :applies matrix cell"
    - "test/rendro/edge_error_matrix_test.exs (117-05, Wave 2) calls the four EdgeFixtures error-fixture functions"
    - "test/rendro/adapters/pdfium_raster_snapshot_test.exs (117-06, Wave 2) reuses EdgeFixtures.document/2 for the 6 curated raster fixtures (the odd_even_running_content cells for invoice/statement/payslip, page_size_a4_letter for certificate, text_wrap for invoice)"
---

<objective>
Build `Rendro.Test.EdgeFixtures`, the single fixture-construction module every downstream Wave-2 test file consumes: a `{family, dimension} -> recipe-shaped document` dispatch table covering all 62 `:applies` cells across the 6 families (Invoice, Statement, Receipt, Certificate, Payslip, Ticket) x 17 dimensions, plus 4 standalone document builders for the EDGE-02 error cases (overflow, tall-row, RTL x2). This is pure test-support composition of already-shipped `lib/` recipes and public primitives -- zero `lib/` edits.

Purpose: Centralize fixture construction in one tested module so the golden-matrix (117-04), error-matrix (117-05), and raster (117-06) plans each consume a stable, self-verified contract instead of duplicating per-family data-shape knowledge three times.
Output: `test/support/edge_fixtures.ex` (`Rendro.Test.EdgeFixtures`, `@moduledoc false`) exposing `recipe_module/1`, `build/2`, `opts/2`, `document/2`, `overflow_document/0`, `tall_row_document/0`, `rtl_default_font_document/0`, `rtl_shaping_required_document/0`; `test/support/edge_fixtures_test.exs` spot-checking representative cells per family plus the raise-on-unrecognized-pair guard.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `Rendro.Test.EdgeFixtures.recipe_module/1` -- `@spec recipe_module(atom()) :: module()`. Maps `:invoice -> Rendro.Recipes.Invoice`, etc.
- `Rendro.Test.EdgeFixtures.build/2` -- `@spec build(atom(), atom()) :: map()`. Returns the recipe-shaped `data` map for one `{family, dimension}` `:applies` cell. Raises `ArgumentError` for any unrecognized pair.
- `Rendro.Test.EdgeFixtures.opts/2` -- `@spec opts(atom(), atom()) :: keyword()`. Returns the `document/2` opts (e.g. `:formatters`, `:page_size`) for a `{family, dimension}` pair; `[]` when no special opts apply.
- `Rendro.Test.EdgeFixtures.document/2` -- `@spec document(atom(), atom()) :: Rendro.Document.t()`. The single entry point Wave-2 test files call: for most `{family, dimension}` pairs, `recipe_module(family).document(build(family, dimension), opts(family, dimension))`; for `:odd_even_running_content` specifically, an escape-hatch composition (recipe's own `page_template/1` + `sections/2` plus two extra odd/even footer sections) since that dimension is not a `document/2`-opts concern.
- `Rendro.Test.EdgeFixtures.overflow_document/0`, `.tall_row_document/0`, `.rtl_default_font_document/0`, `.rtl_shaping_required_document/0` -- `@spec ... :: Rendro.Document.t()` each. The four EDGE-02 error fixtures (D-05/D-06), reused by 117-05.
- `test/support/edge_fixtures_test.exs` (`Rendro.Test.EdgeFixturesTest`, `async: true` -- pure functions, no shared/env state).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-edge-case-stress-matrix/117-CONTEXT.md
@.planning/phases/117-edge-case-stress-matrix/117-RESEARCH.md
@.planning/phases/117-edge-case-stress-matrix/117-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build/2, opts/2, document/2 dispatch skeleton + content-substitution dimensions</name>
  <files>test/support/edge_fixtures.ex, test/support/edge_fixtures_test.exs</files>
  <read_first>
    lib/rendro/recipes/invoice.ex (lines 1-61 module attributes and doc; lines 403-431 validate_required_keys!/validate_data!; lines 640-694 maybe_validate_totals! -- :totals shape is %{subtotal:, tax:, discount:, total:}, subtotal MUST Decimal.equal? the sum of items qty*price, total MUST Decimal.equal? subtotal+tax-discount when those keys are present)
    lib/rendro/recipes/statement.ex (lines 460-494 validate_required_keys!; lines 521-534 :period is %{from: Date, to: Date}, :account is %{name: String}; lines 547-600ish :lines entries are %{date:, description:, amount: Decimal} signed, never include a caller :balance key)
    lib/rendro/recipes/receipt.ex (lines 1-20 moduledoc -- Receipt genuinely paginates, same Pagination.chunk_rows_into_pages/2 mechanism as Invoice; lines 374-401 validate_required_keys!/validate_customer! -- :customer is %{name: String})
    lib/rendro/recipes/certificate.ex (lines 1-52 moduledoc -- required :title/:recipient/:date, optional :body/:seal_line/:brand; lines 56-100 page_size/orientation opts via Rendro.PageSize.resolve/2, default orientation :landscape)
    lib/rendro/recipes/payslip.ex (lines 655-748 validate_data!/validate_required_keys! -- :employer/:employee are %{name: String}, :period is %{from:,to:}, :pay_date is a %Date{}; lines 672-673 earnings MUST be non-empty, deductions MAY be empty; lines 679-731 validate_reconciliation!/maybe_validate_totals! -- net_pay MUST Decimal.equal? derive_totals(data).net (gross earnings minus total deductions, computed via exact Decimal arithmetic -- never hand-type net_pay, always derive it as Decimal.sub(gross_sum, deductions_sum))
    lib/rendro/recipes/ticket.ex (lines 478-504 validate_required_keys!; lines 506-528 :issuer is %{name: String}; lines 594-690 :placement is a list of 1-4 %{label:, value:} maps, each field <=40 bytes; lines 692+ :code is %{reference: String, label?:, image?:} -- reference required, label/image optional)
    lib/rendro/format.ex (lines 62-71 money/1 -- rounds half, groups thousands, wraps negatives in parens; do not hand-roll this logic in fixtures, only supply raw Decimal inputs)
    lib/rendro/recipes/pagination.ex (lines 55-60 formatter/3 -- opts[:formatters][key] override consumed by each recipe's own section builder; lines 76-116 label_resolver/2, validate_labels!/2)
    test/support/pdfium_cli.ex (module shape: @moduledoc false, @spec-annotated pure functions -- mirror this shape, not a GenServer)
  </read_first>
  <behavior>
    - `recipe_module(:invoice)` returns `Rendro.Recipes.Invoice`; all 6 atoms map to their `Rendro.Recipes.*` module.
    - `build(:invoice, :missing_optional_fields)` returns a map containing exactly `:id, :date, :items` (no optional keys) and `Rendro.Recipes.Invoice.document(build(:invoice, :missing_optional_fields))` succeeds without raising.
    - `build(:invoice, :text_wrap)` returns items with an item name longer than 200 characters; `document(:invoice, :text_wrap)` renders successfully (proves the narrow `{:share, 1}` item-name column wraps rather than overflows).
    - `build(:statement, :money_negative_parens)` returns a line with `amount: Decimal.new("-200.00")`; `document(:statement, :money_negative_parens)` renders successfully.
    - `build(:invoice, :money_large)` returns `:totals` whose `:subtotal`/`:total` Decimal-equal the sum of `items` qty*price (verified via the SAME arithmetic Invoice's own `maybe_validate_totals!/1` performs); `document(:invoice, :money_large)` does not raise.
    - `build(:payslip, :money_cents_rounding)` returns `:net_pay` computed as the exact `Decimal.sub/2` of derived gross minus derived deductions (never a manually-typed literal that could drift from the reconciliation check); `document(:payslip, :money_cents_rounding)` does not raise.
    - `opts(:invoice, :currency_format)` returns a `[formatters: [amount: fun]]` keyword list where `fun` is an arity-1 function; `opts(:invoice, :text_wrap)` returns `[]`.
    - `document(:certificate, :qty_zero)` raises `ArgumentError` (Certificate/qty_zero is a genuine N/A cell per the resolved matrix -- `build/2`'s catch-all clause must raise loudly, never silently return a nonsense map).
  </behavior>
  <action>
    Create `test/support/edge_fixtures.ex` as `defmodule Rendro.Test.EdgeFixtures do @moduledoc false`, mirroring `test/support/pdfium_cli.ex`'s pure-function, `@spec`-annotated shape (NOT a GenServer/Agent).

    Implement `recipe_module/1` as a simple 6-clause dispatch: `:invoice -> Rendro.Recipes.Invoice`, `:statement -> Rendro.Recipes.Statement`, `:receipt -> Rendro.Recipes.Receipt`, `:certificate -> Rendro.Recipes.Certificate`, `:payslip -> Rendro.Recipes.Payslip`, `:ticket -> Rendro.Recipes.Ticket`.

    Implement a private `base_data/1` per family returning each family's minimal happy-path map using its exact required-key shape (verified above): Invoice `%{id: "INV-1001", date: ~D[2026-01-15], items: [%{name: "Consulting Services", qty: 2, price: 150}]}`; Statement `%{period: %{from: ~D[2026-01-01], to: ~D[2026-01-31]}, account: %{name: "Acme Corp"}, opening_balance: Decimal.new("1000.00"), lines: [%{date: ~D[2026-01-05], description: "Invoice #1001", amount: Decimal.new("250.00")}]}`; Receipt `%{title: "Receipt", date: ~D[2026-01-15], customer: %{name: "Jane Doe"}, lines: [%{description: "Widget", amount: Decimal.new("10.00")}]}`; Certificate `%{title: "Certificate of Completion", recipient: "Jane Smith", date: ~D[2026-01-15]}`; Payslip `%{employer: %{name: "Acme Corp"}, employee: %{name: "Jordan Rivera"}, period: %{from: ~D[2026-01-01], to: ~D[2026-01-31]}, pay_date: ~D[2026-02-01], earnings: [%{description: "Base Salary", amount: Decimal.new("4000.00"), ytd: Decimal.new("4000.00")}], deductions: [%{description: "Federal Income Tax", amount: Decimal.new("450.00"), ytd: Decimal.new("450.00")}], net_pay: Decimal.new("3550.00")}`; Ticket `%{issuer: %{name: "Aurora Live"}, title: "Concert Pass", placement: [%{label: "Seat", value: "24"}], code: %{reference: "AB123456"}}`.

    `build(family, :missing_optional_fields)` returns `base_data(family)` verbatim for all 6 families (it is already minimal).

    `build(family, :text_wrap)` mutates the free-text field per family with a long string: Invoice `items[0].name` (>=200 chars, exercises the narrow `{:share, 1}` column); Statement/Receipt `lines[0].description`; Certificate adds a `:body` key with a long paragraph; Payslip `earnings[0].description`; Ticket sets `:terms` to a long string capped under 600 bytes (Ticket's `validate_optional_text!(terms, :terms, 600)` hard-rejects anything longer -- stay comfortably under, e.g. ~400 bytes). Certificate/Ticket also apply per the resolved matrix (all 6 families are `:applies` for `text_wrap`).

    `build(family, :line_items_zero | :line_items_one | :line_items_few)` for Invoice/Statement/Receipt varies `items`/`lines` count (0, 1, 3 entries respectively, reusing `base_data/1`'s line shape); for Payslip, vary `:deductions` (0, 1, 3 entries) while keeping `:earnings` non-empty (required) and re-deriving `:net_pay` as the exact `Decimal.sub/2` of the earnings sum minus the deductions sum for each variant. Certificate/Ticket have no `build/2` clause for these three dimensions (genuinely N/A per the resolved matrix -- do not add one).

    `build(family, :money_zero | :money_large | :money_cents_rounding)` for Invoice: keep `items` at `qty: 1` with a plain-integer `price` (the legacy slot never routes through `Format.money` per INV-02 and is irrelevant to these dimensions), and vary `:totals` instead -- `:money_zero` -> `%{subtotal: Decimal.new("0.00"), total: Decimal.new("0.00")}` (with a $0 item, e.g. `price: 0`); `:money_large` -> item `price: 1_250_000`, `totals: %{subtotal: Decimal.new("1250000.00"), total: Decimal.new("1250000.00")}`; `:money_cents_rounding` -> keep item `price: 300`, `totals: %{subtotal: Decimal.new("300.00"), tax: Decimal.new("19.995"), total: Decimal.new("319.995")}` (subtotal/total both Decimal-equal the exact arithmetic Invoice's own `maybe_validate_totals!/1` performs -- the cents-rounding proof surfaces when `tax`/`total` render through `Format.money`, which rounds for display without affecting the exact reconciliation check). For Statement/Receipt: vary a single line's `:amount` directly (`Decimal.new("0.00")`, `Decimal.new("1250000.00")`, `Decimal.new("19.995")`). For Payslip: vary a `:deductions` (or `:earnings` for `:money_large`) entry's `:amount` the same way, and ALWAYS re-derive `:net_pay` as `Decimal.sub(gross_sum, deductions_sum)` computed from the actual entries used (never a hand-typed literal) so `validate_reconciliation!/1` never raises. Certificate/Ticket have no clauses (N/A -- carries no money).

    `build(:statement, :money_negative_parens)` -> one line with `amount: Decimal.new("-200.00")`, `description: "Refund"` (Statement's `:lines.amount` is explicitly signed per its own moduledoc). No other family gets this clause (N/A elsewhere per the resolved matrix).

    `build(:invoice, :qty_zero)` -> `items: [%{name: "Free Sample", qty: 0, price: 25}]` (only Invoice has a `:qty` concept). No other family gets this clause.

    `build(family, :currency_format)` for Invoice/Statement/Receipt/Payslip returns `base_data(family)` unmodified (the variation is entirely in `opts/2`, below) EXCEPT Invoice additionally needs a `:totals` block present for the formatter override to have any visible effect (per INV-02, legacy `:price` never routes through `Format.money`) -- use `totals: %{subtotal: Decimal.new("300.00"), total: Decimal.new("300.00")}` alongside `items: [%{name: "Consulting Services", qty: 2, price: 150}]`.

    `build(:payslip, :tax_label)` -> `deductions: [%{description: "PAYE Income Tax", amount: Decimal.new("450.00"), ytd: Decimal.new("450.00")}]` (a distinctly non-US label, proving the label is caller DATA not hardcoded), `earnings`/`net_pay` from `base_data/1` (450 deduction already reconciles against the 4000/3550 base). No other family gets this clause (N/A per the resolved matrix -- Invoice/Receipt's tax label is a hardcoded string literal with no override, Statement/Certificate/Ticket carry no tax concept).

    Any `build/2` clause NOT explicitly listed above (e.g. `build(:certificate, :qty_zero)`, `build(:ticket, :money_zero)`) must fall through to a catch-all `build(family, dimension)` clause that raises `ArgumentError` with a message naming the unrecognized `{family, dimension}` pair and pointing at `test/rendro/edge_matrix_test.exs`'s `@matrix` as the source of truth for which cells are `:applies`.

    Implement `opts(family, dimension)`: `opts(_, :currency_format)` for Invoice/Statement/Receipt/Payslip returns `[formatters: [amount: fn d -> "£" <> String.trim_leading(Rendro.Format.money(d), "$") end]]` (GBP-pound-sign override, reusing `Rendro.Format.money/1` -- never reimplement grouping/rounding). All other `{family, dimension}` pairs handled in THIS task return `[]` via a catch-all clause (do not raise here -- `[]` is always valid for `document/2`'s opts argument).

    Implement `document(family, dimension)` for THIS task's scope as `recipe_module(family).document(build(family, dimension), opts(family, dimension))`. Task 2 will extend this function with the `:odd_even_running_content` escape-hatch branch.

    Create `test/support/edge_fixtures_test.exs` (`Rendro.Test.EdgeFixturesTest`, `async: true`) covering all `<behavior>` cases above, plus a full sweep asserting `document/2` does not raise for every `{family, dimension}` combination this task implements (iterate the explicit clause list, not a blind cross-product).
  </action>
  <acceptance_criteria>
    - `recipe_module/1`, `build/2`, `opts/2`, `document/2` exist with the `@spec`s stated in `<artifacts_produced>`
    - Every `<behavior>` case above passes
    - `document/2` never raises for any `{family, dimension}` pair this task implements
    - `document/2` (via `build/2`'s catch-all) raises `ArgumentError` for at least one genuinely-N/A pair (`:certificate, :qty_zero` per the acceptance test above)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/support/edge_fixtures_test.exs</automated>
  </verify>
  <done>EdgeFixtures.document/2 successfully renders every content-substitution dimension (text_wrap, missing_optional_fields, line_items_zero/one/few, money_zero/negative_parens/large/cents_rounding, qty_zero, currency_format, tax_label) for every applicable family, and raises loudly on unrecognized pairs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: structural dimensions (pagination, page_size, odd/even) + EDGE-02 error fixtures</name>
  <files>test/support/edge_fixtures.ex, test/support/edge_fixtures_test.exs</files>
  <read_first>
    test/rendro/recipes/statement_test.exs (lines 58-74 -- `rows_per_page/0`'s exact `Rendro.measure_rows/4`-based capacity formula for Statement: `content_width = 595.28 - 2*72`; `table_opts = [header: [...], columns: [{:fixed,72},{:share,1},{:fixed,72},{:fixed,72}]]`; `capacity = body_height - header_height - footer_height`; reuse verbatim for Statement fixtures)
    test/rendro/recipes/receipt_test.exs (lines 42-57 -- Receipt's own `rows_per_page/0`, a distinct formula/table_opts from Statement's -- Receipt's own moduledoc confirms it genuinely paginates via the identical `Rendro.Recipes.Pagination.chunk_rows_into_pages/2` mechanism as Invoice; do NOT mark Receipt's pagination cells N/A)
    test/rendro/recipes/payslip_test.exs (lines 280-299 -- the existing `for i <- 1..80` 60+-earnings multi-page test; reuse this exact row-count/shape convention for Payslip's `:line_items_60_plus`)
    lib/rendro/pipeline/pagination.ex or lib/rendro.ex (`Rendro.measure_rows/4` -- public function used by the two rows_per_page/0 analogs above; use the same technique against Invoice's own `@table_columns`/`@body_height`/`@header_height`/`@footer_height`/`@row_epsilon` module attributes (invoice.ex lines 34-61, already read in Task 1) to derive Invoice's row capacity precisely, rather than guessing)
    lib/rendro/page_size.ex (full file, 19 lines -- `resolve/2`; A4 portrait 595.28x841.89, US Letter portrait 612x792)
    test/rendro/pipeline/paginate_test.exs (lines 923-961 -- `only_on: :odd`/`:even` + `%Rendro.RunningContent{fun: fn {pn, tp} -> ... end}` escape-hatch composition pattern; lines 963-1020 the `%Rendro.Block{content: %Rendro.RunningContent{...}, height: 14.4}` shape)
    lib/rendro/section.ex (lines 8-26 -- `%Rendro.Section{}` struct fields including `only_on: nil | :odd | :even`)
    test/rendro/pipeline/measure_test.exs (lines 617-672, already read in full during research -- clone `arabic_capable_fake_font/0` and `doc_with_arabic_text/0` verbatim for the RTL shaping-required fixture)
    lib/rendro/pipeline/paginate.ex (lines 292-311 -- the table-row-overflow branch: when `current_h == 0` and `block.content` is a `%Rendro.Table{}` whose first row doesn't fit, throws `{:error, :content_overflow, details}` where `details` has `:row_index/:row_height/:header_height/:column_widths` -- NO `:block` key, distinct from the generic block-overflow path's `check_overflow!/4` at lines 863-877 which DOES include `details.block`)
    lib/rendro/error.ex (lines 273-275 -- `next_step(:paginate, :content_overflow)` is the SAME string regardless of which throw site produced the error)
  </read_first>
  <behavior>
    - `build(:statement, :line_items_page_boundary)` returns exactly `rows_per_page() + 1` lines (2-page transition); `document(:statement, :line_items_page_boundary)` renders successfully as a 2-page document.
    - `build(:receipt, :pagination_boundary)` returns exactly `2 * rows_per_page() + 1` lines (3-page transition, mirroring `statement_test.exs`'s own "2*capacity+1 lines produces 3 pages" test technique); `document/2` renders successfully as a 3-page document.
    - `build(:payslip, :line_items_60_plus)` returns 65+ earnings entries (mirrors the existing `payslip_test.exs:280-299` 80-entry pattern) and `document/2` renders as a multi-page document.
    - `document(:certificate, :page_size_a4_letter)` renders at US Letter geometry (`opts(:certificate, :page_size_a4_letter)` includes `page_size: :us_letter`); `document(:certificate, :missing_optional_fields)` (default, no page_size opt) renders at A4 geometry -- the two are byte-different.
    - `document(:invoice, :odd_even_running_content)` returns a `%Rendro.Document{}` spanning 2+ pages whose odd-page footer content differs from its even-page footer content (verified structurally, e.g. by checking distinct RunningContent closures are wired to distinct `only_on:` sections -- full behavioral proof happens at render-time in 117-04/117-06).
    - `overflow_document/0` renders via `Rendro.render/1` to `{:error, %Rendro.Error{stage: :paginate, reason: :content_overflow}}` whose `details.block` is a map.
    - `tall_row_document/0` renders to `{:error, %Rendro.Error{stage: :paginate, reason: :content_overflow}}` whose `details` has a `:row_height` key (NOT a `:block` key -- the table-row-overflow throw site never merges in a `:block` entry).
    - `rtl_default_font_document/0` renders to `{:error, %Rendro.Error{stage: :measure, reason: {:unsupported_glyph, _char}}}`.
    - `rtl_shaping_required_document/0` renders to `{:error, %Rendro.Error{stage: :measure, reason: {:shaping_required, :arab, _hint}}}`.
  </behavior>
  <action>
    Add a private `rows_per_page(family)` helper (or per-family private functions) computing exact per-page row capacity via `Rendro.measure_rows/4`, mirroring `test/rendro/recipes/statement_test.exs:58-74` for Statement and `test/rendro/recipes/receipt_test.exs:42-57` for Receipt verbatim (their formulas differ slightly -- read both, do not assume they're identical). Derive Invoice's and Payslip's capacity the same way, using each recipe's own `@table_columns`/geometry module attributes (Invoice: lines 34-61 already read; Payslip: derive analogously from its own module attributes and `PageSize.resolve/2`-based body height, reading the relevant lines directly before implementing).

    Add a private `big_line_items(family, count)` helper generating `count` synthetic line-item/earnings entries per family's line shape (reusing `base_data/1`'s field names), each with a distinct sequential description (e.g. `"Line item #{i}"`) and a modest fixed amount.

    `build(family, :line_items_page_boundary)` for Invoice/Statement/Receipt/Payslip -> `big_line_items(family, rows_per_page(family) + 1)` substituted into the relevant list key (`:items`/`:lines`/`:earnings`+`:deductions` as appropriate; for Payslip, add the extra rows to `:deductions` and re-derive `:net_pay`).

    `build(family, :pagination_boundary)` for Invoice/Statement/Receipt/Payslip -> `big_line_items(family, 2 * rows_per_page(family) + 1)` (a distinctly larger, 3-page dataset -- deliberately different from `:line_items_page_boundary`'s 2-page dataset so the two matrix cells are not byte-identical).

    `build(family, :line_items_60_plus)` for Invoice/Statement/Receipt/Payslip -> `big_line_items(family, 65)` (mirrors the existing `payslip_test.exs` 80-row convention; 65 safely exceeds the "60+" threshold for every family's typical row height without needing per-family precision).

    `build(family, :page_size_a4_letter)` for Certificate/Payslip/Ticket -> `base_data(family)` unmodified; `opts(family, :page_size_a4_letter)` -> `[page_size: :us_letter]` (the byte-golden cell proves the override threads through; 117-06's raster task separately renders Certificate at BOTH `:a4` and `:us_letter`).

    `document(family, :odd_even_running_content)` for Invoice/Statement/Receipt/Payslip (Certificate/Ticket have no clause -- N/A, single-page layouts): build via the escape-hatch composition pattern from `paginate_test.exs:923-961`, NOT via `build/2`+`document/2`. IMPORTANT: each recipe's `:footer`-region height is a hardcoded module attribute (`@footer_height`, confirmed identical in Invoice/Receipt at 24pt) computed into an absolute region `height:` at struct-construction time -- passing `margin_bottom:`/similar opts to `page_template/1` does NOT resize the footer region (those opts only set the resulting `%Rendro.PageTemplate{}.margin_bottom` field, which region placement ignores; verified by reading each recipe's `page_template/1` body directly). Do NOT attempt a margin-override trick. Instead: call `template = recipe_module(family).page_template()` (unmodified) and `all_sections = recipe_module(family).sections(build(family, :line_items_60_plus))` (reusing the 65-row dataset so this SAME fixture simultaneously proves pagination + 60+ rows + odd/even parity, per D-10a's combined-fixture design), then `base_sections = Enum.reject(all_sections, &(&1.region == :footer))` (drop the recipe's OWN footer section -- its `%Rendro.Section{}.region` field is `:footer` per `lib/rendro/section.ex:8-15` -- so only ONE footer section is ever active per page, avoiding any footer-region height-budget conflict); then build two replacement `Rendro.section/1` calls (`region: :footer, only_on: :odd, content: [...]` and `region: :footer, only_on: :even, content: [...]`), each wrapping a `%Rendro.Block{content: %Rendro.RunningContent{fun: fn {pn, _tp} -> [Rendro.block(Rendro.text("Page #{pn} (odd)"))] end}, height: 14.4}` (odd) / `"Page #{pn} (even)"` (even) -- 14.4pt comfortably fits every family's footer region height (24pt or larger); assemble via `Rendro.Document.new() |> Rendro.Document.add_template(template) |> Rendro.Document.set_template(template.name) |> then(&Enum.reduce(base_sections ++ [odd_footer, even_footer], &1, fn s, d -> Rendro.Document.add_section(d, s) end))`. Render this document once during implementation to confirm it does not raise `:content_overflow`.

    Implement the four EDGE-02 error fixtures:
    - `overflow_document/0`: `Rendro.flow([Rendro.block(Rendro.text("overflow probe"), height: 5000)])` (or equivalent) -- a single block with an explicit `:height` far exceeding any page's body capacity, guaranteeing the generic `check_overflow!/4` throw path (paginate.ex ~863-877), NOT the table-row-specific path.
    - `tall_row_document/0`: a `Rendro.table/2` call with exactly ONE row and ONE narrow `{:fixed, N}` column (small N, e.g. 20) whose cell text is very long (forcing wrapped row height to exceed body capacity), wrapped as the FIRST and ONLY block via `Rendro.flow([Rendro.block(that_table)])` (guaranteeing `current_h == 0` at the row-overflow check site, paginate.ex ~292-311).
    - `rtl_default_font_document/0`: `Rendro.flow([Rendro.block(Rendro.text("שלום עולם"))])` (Hebrew "shalom olam", no custom font/shaper config -- reuses this session's live-probe-verified construction).
    - `rtl_shaping_required_document/0`: clone `arabic_capable_fake_font/0` and `doc_with_arabic_text/0` from `test/rendro/pipeline/measure_test.exs:617-672` verbatim as private functions inside `EdgeFixtures`, exposed via the public `rtl_shaping_required_document/0` wrapper.

    Extend `test/support/edge_fixtures_test.exs` with all remaining `<behavior>` cases: the 5 structural dimensions (spot-checked across at least Statement + Receipt + one of Invoice/Payslip) and all 4 error-fixture functions, asserting the exact `stage`/`reason`/`details` shapes stated above (this is a light structural sanity check -- the deep EDGE-02 assertion coverage belongs to 117-05, not duplicated here).
  </action>
  <acceptance_criteria>
    - All 9 `<behavior>` cases pass
    - `EdgeFixtures.document/2` covers all 62 `:applies` cells from the resolved N/A matrix (RESEARCH.md `## Resolved N/A Matrix`) across both tasks of this plan -- spot-verify by rendering at least one representative cell per family without raising
    - The 4 error-fixture functions produce the EXACT `stage`/`reason` shapes documented above when rendered via `Rendro.render/1`
    - `tall_row_document/0`'s error `details` map does NOT contain a `:block` key (distinguishing it from `overflow_document/0`'s `details.block`)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/support/edge_fixtures_test.exs</automated>
  </verify>
  <done>EdgeFixtures.document/2 covers all 62 :applies cells (both tasks combined); the 4 EDGE-02 error fixtures reproduce the exact live-probe-verified error shapes with zero lib/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (test-only, no runtime surface) | `test/support/edge_fixtures.ex` is test-support code, never shipped. It consumes only already-public `lib/` recipe/primitive APIs (`Rendro.Recipes.*`, `Rendro.section/1`, `%Rendro.RunningContent{}`) exactly as any external caller could -- no reflection, no private-API access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-117-02-01 | Tampering | RTL fake-font-registry construction (`rtl_shaping_required_document/0`) | low | accept | Builds a synthetic `%Rendro.PDF.Font{}` directly on a hand-built `%Rendro.Document{}` -- a technique that already ships in `measure_test.exs`, uses only public structs, and never touches `lib/`; the resulting document is used solely to prove an error path, never rendered to a shipped artifact. |
| T-117-02-02 | Denial of Service | `big_line_items/2` unbounded row generation | low | accept | Row counts are hardcoded, small (max ~2x a single family's page capacity, well under a few hundred), and test-only -- no caller-controlled input reaches this function. |
</threat_model>

<verification>
`mix test test/support/edge_fixtures_test.exs` -- all behaviors from both tasks green. `mix compile --warnings-as-errors` stays clean.
</verification>

<success_criteria>
- `Rendro.Test.EdgeFixtures` exposes `recipe_module/1`, `build/2`, `opts/2`, `document/2`, and the 4 error-fixture functions, all `@spec`-annotated.
- `document/2` succeeds for every one of the 62 `:applies` cells in the resolved N/A matrix and raises for unrecognized pairs.
- The 4 error fixtures reproduce the exact live-probe-verified `%Rendro.Error{}` shapes (stage/reason) with zero `lib/` edits.
</success_criteria>

<output>
Create `.planning/phases/117-edge-case-stress-matrix/117-02-SUMMARY.md` when done
</output>
