---
phase: 117-edge-case-stress-matrix
plan: 07
type: execute
wave: 3
depends_on: ["117-04"]
files_modified:
  - priv/quality/rubric_scores.json
  - priv/schemas/rubric_scores.schema.json
  - test/docs_contract/rubric_manifest_contract_test.exs
autonomous: true
requirements: [EDGE-03]
must_haves:
  truths:
    - "The rubric manifest carries exactly ONE top-level stress_exemption block (exempt: true, non-empty reason) -- not 62 per-fixture noise entries -- making the exemption a single reviewer-visible, reasoned statement (D-13)"
    - "The schema's root required array includes stress_exemption, so the manifest cannot validate if the block is ever deleted (hard anti-silent-loss, D-14)"
    - "No entry in the scores array uses the existing per-entry stress_exempt field to dodge the beauty gate -- that field is now a loophole tripwire, not a real exemption path (D-15ii)"
    - "The stress-matrix fixture ID set (imported from edge_matrix_test.exs's @matrix, the SAME source of truth 117-04 authored) is provably disjoint from -- and non-empty relative to -- the scores array's demo_ids (D-15iii/iv)"
  artifacts:
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - test/docs_contract/rubric_manifest_contract_test.exs
  key_links:
    - "Imports Rendro.EdgeMatrixTest.stress_fixture_ids/0 (117-04) as the single source of truth for which fixture IDs count as stress-exempt -- via Code.require_file/2 at the top of rubric_manifest_contract_test.exs, mirroring the existing test/scripts/release_preflight_proof_test.exs:1 precedent for cross-test-file dependency in this codebase"
---

<objective>
Record EDGE-03's rubric beauty-gate exemption: add ONE explicit, schema-enforced `stress_exemption` block to `priv/quality/rubric_scores.json`, extend `priv/schemas/rubric_scores.schema.json` so the manifest cannot validate without it, and extend `test/docs_contract/rubric_manifest_contract_test.exs` with 4 fail-loud-in-both-directions guards (D-15) proving the exemption is present, that no real demo entry can hijack it, and that the stress-fixture ID set is genuinely disjoint from (and non-vacuous relative to) Phase 118's future `scores` entries.

Note on scope: `priv/schemas/rubric_scores.schema.json` is a **JSON Schema** file describing this repo's own quality-manifest structure -- there is no database/ORM schema and no live database anywhere in this project. This plan's "schema" edit is a pure-JSON structural contract change, not a DB migration; no schema-push task applies.

Purpose: Make the "stress fixtures prove robustness, not aesthetics" exemption a machine-checked, explicit, reviewer-visible fact rather than an implicit gap -- so Phase 118's real family x domain demonstration scores can never accidentally collide with, or be confused for, a stress-matrix fixture.
Output: `stress_exemption` present in both the manifest and schema (schema-required), plus 4 new contract-test guards.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `priv/quality/rubric_scores.json` -- new top-level `"stress_exemption"` key: `{"exempt": true, "reason": "<non-empty rationale>", "fixture_source": "test/rendro/edge_matrix_test.exs", "gate_scope": "scores"}`.
- `priv/schemas/rubric_scores.schema.json` -- new `properties.stress_exemption` definition (`required: ["exempt", "reason"]`, `exempt` const `true`, `reason` non-empty string); `"stress_exemption"` appended to the ROOT `required` array (`["schema_version", "dimensions", "gates", "thresholds", "scores", "stress_exemption"]`). The existing per-entry `properties.$defs.score_entry.stress_exempt` field (schema line ~126) is left unchanged, now explicitly repurposed as the D-15(ii) loophole tripwire.
- `test/docs_contract/rubric_manifest_contract_test.exs` -- 4 new tests (D-15 i-iv) plus a `Code.require_file("test/rendro/edge_matrix_test.exs", File.cwd!())` at the top of the file.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-edge-case-stress-matrix/117-CONTEXT.md
@.planning/phases/117-edge-case-stress-matrix/117-RESEARCH.md
@.planning/phases/117-edge-case-stress-matrix/117-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: stress_exemption manifest + schema edits (D-13/D-14)</name>
  <files>priv/quality/rubric_scores.json, priv/schemas/rubric_scores.schema.json</files>
  <read_first>
    priv/quality/rubric_scores.json (full file, 83 lines -- top-level keys `schema_version`/`dimensions`/`gates`/`thresholds`/`scores`; `thresholds` block at lines 77-81 is the sibling-key placement template; `scores` is currently `[]`)
    priv/schemas/rubric_scores.schema.json (full file, 131 lines -- root `required` array at line 7; `properties.thresholds` at lines 63-72 is the shape template for the new `stress_exemption` def; `$defs.score_entry.stress_exempt` at line 126 -- the existing, currently-unused per-entry field this plan repurposes as a tripwire, left untouched)
  </read_first>
  <action>
    Edit `priv/quality/rubric_scores.json`: add a new top-level `"stress_exemption"` key as a sibling of `"thresholds"` (same nesting level), value `{"exempt": true, "reason": "Stress-matrix fixtures (test/rendro/edge_matrix_test.exs) exist to prove pipeline robustness and byte-determinism under adversarial inputs -- overflow, extreme text length, empty/60+ line-item counts, numeric edge cases, currency and page-size variance -- not reader-facing aesthetic quality. They are deliberately excluded from the reader-quality rubric's beauty gate; the gate applies only to curated family x domain demonstration documents.", "fixture_source": "test/rendro/edge_matrix_test.exs", "gate_scope": "scores"}`.

    Edit `priv/schemas/rubric_scores.schema.json`: (1) append `"stress_exemption"` to the ROOT `required` array (line 7), making the full array `["schema_version", "dimensions", "gates", "thresholds", "scores", "stress_exemption"]`; (2) add a new `properties.stress_exemption` definition (sibling of `properties.thresholds`): `{"type": "object", "required": ["exempt", "reason"], "properties": {"exempt": {"const": true}, "reason": {"type": "string", "minLength": 1}, "fixture_source": {"type": "string"}, "gate_scope": {"type": "string"}}, "additionalProperties": true}`. Do NOT add an `if/then` conditional and do NOT make `dimension_scores` optional anywhere -- no exempt entry ever lives in `scores` (D-14's explicit "minimal schema delta" scope). Leave the existing `$defs.score_entry.stress_exempt` boolean field exactly as-is.
  </action>
  <acceptance_criteria>
    - `priv/quality/rubric_scores.json` has a top-level `stress_exemption` object with `exempt: true` and a non-empty `reason` string
    - `priv/schemas/rubric_scores.schema.json`'s root `required` array includes `"stress_exemption"`
    - `priv/schemas/rubric_scores.schema.json`'s `properties.stress_exemption` requires `exempt` (const `true`) and non-empty `reason`
    - The manifest validates against the updated schema
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs</automated>
  </verify>
  <done>The manifest now validates against a schema that HARD-REQUIRES the stress_exemption block -- deleting it from the manifest would fail schema validation, closing the silent-loss gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-15 fail-loud contract-test guards (both directions)</name>
  <files>test/docs_contract/rubric_manifest_contract_test.exs</files>
  <read_first>
    test/docs_contract/rubric_manifest_contract_test.exs (full file, 84 lines -- `manifest/0` and `rubric_schema/0` private helpers at lines 8-19, reuse verbatim; the existing flat (no `describe` blocks) test style; existing `test "schema validation..."` at lines 41-44 already re-runs against the updated schema from Task 1)
    test/scripts/release_preflight_proof_test.exs (line 1 -- `Code.require_file("scripts/release_preflight_proof.exs", File.cwd!())`, the existing precedent in this codebase for a test file depending on another file's compiled module; mirror this exact pattern, NOT a different cross-file technique)
    test/rendro/edge_matrix_test.exs (as written by 117-04 -- the public `stress_fixture_ids/0` function this task imports; NOTE this file `use ExUnit.Case`, so `Code.require_file`-ing it also registers its own ~65 tests for whatever `mix test` invocation triggers the require -- this is expected, not a bug, and mirrors how the existing precedent file behaves)
  </read_first>
  <behavior>
    - `manifest()["stress_exemption"]["exempt"] == true` and `manifest()["stress_exemption"]["reason"]` is a non-empty binary (D-15i).
    - `Enum.any?(manifest()["scores"], &Map.get(&1, "stress_exempt", false))` is `false` -- no real demo entry (today, `scores` is `[]`, so this passes vacuously; the guard exists for Phase 118's future entries) (D-15ii).
    - `MapSet.disjoint?(Rendro.EdgeMatrixTest.stress_fixture_ids(), MapSet.new(Enum.map(manifest()["scores"], & &1["demo_id"])))` is `true` (D-15iii).
    - `MapSet.size(Rendro.EdgeMatrixTest.stress_fixture_ids()) > 0` -- the "teeth guard" proving disjointness isn't trivially passing against an empty stress-fixture set; expect exactly 62 (D-15iv).
  </behavior>
  <action>
    At the very top of `test/docs_contract/rubric_manifest_contract_test.exs`, ABOVE the `defmodule` line, add `Code.require_file("test/rendro/edge_matrix_test.exs", File.cwd!())` -- mirroring `test/scripts/release_preflight_proof_test.exs:1`'s exact precedent. This guarantees `Rendro.EdgeMatrixTest` (and its `stress_fixture_ids/0` function) is compiled and available even when this file is run in isolation (e.g. `mix test test/docs_contract/rubric_manifest_contract_test.exs` alone, which would NOT otherwise compile `test/rendro/edge_matrix_test.exs` since `.exs` test files outside `test/support/` are not part of this project's `elixirc_paths(:test) = ["lib", "test/support"]`).

    Add 4 new tests implementing the `<behavior>` cases above, in this file's existing flat (no `describe` block) style, reusing the existing `manifest/0` private helper -- do not reimplement JSON decoding.
  </action>
  <acceptance_criteria>
    - `Code.require_file/2` is present at the top of the file, before `defmodule`
    - All 4 `<behavior>` cases pass as new tests
    - Running `mix test test/docs_contract/rubric_manifest_contract_test.exs` alone (no other file args) succeeds -- proving the cross-file dependency resolves correctly in isolation, not just when run alongside the full suite
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs</automated>
  </verify>
  <done>All 4 D-15 guards pass; the exemption is provably present, no scores entry can hijack it, and the stress-fixture ID set (62 entries) is disjoint from -- and non-vacuous relative to -- the current (empty) scores array, ready for Phase 118 to append real demo entries without ever colliding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (test-only + repo-local JSON config, no runtime surface) | `priv/quality/rubric_scores.json` and `priv/schemas/rubric_scores.schema.json` are repo-local, non-`lib/`, non-shipped-to-consumers configuration/schema files (confirmed excluded from the Hex tarball via `test "built tarball excludes operator-only priv paths"`, `refute contents =~ "priv/quality/"` and `refute contents =~ "priv/schemas/rubric_scores.schema.json"`). No untrusted input reaches either file at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-117-07-01 | Repudiation | `stress_exemption` silent deletion | medium | mitigate | Two independent enforcement layers per D-15: the JSON Schema's root `required` array (structural, fails `JSV.validate/2`) AND a dedicated contract-test assertion (semantic, fails `mix test`) -- either layer alone could be silently deleted by a future edit, but both together cannot be removed without a reviewable, red-CI diff. |
| T-117-07-02 | Elevation of Privilege (quality-gate bypass) | `scores[].stress_exempt` loophole field | medium | mitigate | D-15(ii)'s guard explicitly forbids any real `scores` entry from setting `stress_exempt: true` to dodge the beauty gate -- the field exists in the schema only as a tripwire target, never a legitimate escape hatch for a real demo. |
</threat_model>

<verification>
`mix test test/docs_contract/rubric_manifest_contract_test.exs` -- all 7 tests (3 existing + 4 new) green, including the isolated single-file run (proving the `Code.require_file/2` cross-file dependency resolves correctly without the rest of the suite).
</verification>

<success_criteria>
- Manifest carries exactly one explicit `stress_exemption` block; schema hard-requires it.
- No `scores` entry can silently claim `stress_exempt: true`.
- The stress-fixture ID set (62 entries, imported from `Rendro.EdgeMatrixTest.stress_fixture_ids/0`) is disjoint from `scores` demo_ids and non-empty.
</success_criteria>

<output>
Create `.planning/phases/117-edge-case-stress-matrix/117-07-SUMMARY.md` when done
</output>
