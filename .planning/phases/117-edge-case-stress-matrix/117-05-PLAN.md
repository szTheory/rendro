---
phase: 117-edge-case-stress-matrix
plan: 05
type: execute
wave: 2
depends_on: ["117-02"]
files_modified:
  - test/rendro/edge_error_matrix_test.exs
autonomous: true
requirements: [EDGE-02]
must_haves:
  truths:
    - "Overflow (a plain block exceeding body region height) raises %Rendro.Error{stage: :paginate, reason: :content_overflow} whose next text matches the exact frozen 'does not auto-fit' string and whose details.block is a map"
    - "A single table row taller than the body raises the SAME typed stage/reason via a distinct code path (paginate.ex's current_h==0 table branch) whose details identify the offending row (row_height/column_widths), NOT a details.block key"
    - "RTL text under the default shaper raises %Rendro.Error{stage: :measure, reason: {:unsupported_glyph, char}}, never silently rendering LTR-reordered garbage"
    - "RTL text with a glyph-capable (but shaping-incapable) font raises %Rendro.Error{stage: :measure, reason: {:shaping_required, :arab, hint}} -- Rendro has no UAX #9 reordering and refuses rather than mis-render"
    - "Rendro.render/2 never returns {:ok, _} for either RTL fixture under the default shaper (locked as a regression guard, D-06)"
  artifacts:
    - test/rendro/edge_error_matrix_test.exs
  key_links:
    - "Consumes the 4 error-fixture functions from test/support/edge_fixtures.ex (117-02); produces no golden artifact and no raster ref (D-08 -- error cells have no output to hash)"
---

<objective>
Implement EDGE-02's typed-error assertion matrix: overflow, tall-row, and both RTL refusal modes each raise an instructive, typed `%Rendro.Error{}` -- never silent truncation, never a leaked internal tuple, never a silently-wrong LTR render of RTL text. Engine-level granularity, one representative fixture per input, NOT per-family (D-08) -- these are pipeline (paginate/measure) concerns identical across all six recipes.

Purpose: Prove Rendro's "errors-as-product, never silent" DNA holds for the three hardest edge inputs in the family x stress-dimension universe, using assertions verified live against the current pipeline this session (zero `lib/` change needed -- all three paths already produce correct, public, typed errors).
Output: `test/rendro/edge_error_matrix_test.exs` (`Rendro.EdgeErrorMatrixTest`, `async: true`) with 4 primary error-shape tests plus the D-06 negative regression guard.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `Rendro.EdgeErrorMatrixTest` (`test/rendro/edge_error_matrix_test.exs`, `async: true`) -- the EDGE-02 sibling to `edge_matrix_test.exs` per D-08. No `@matrix`/golden generation here (error cells produce no artifact); a small `@error_cases` list (4 named entries) plus a lightweight coverage check mirrors D-02's exhaustiveness discipline at this module's smaller scale, honoring D-08's "reusing the same fixture builder + exhaustiveness discipline" without overclaiming per-family coverage.
- No new `priv/` files (error cells never produce a golden or raster ref).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-edge-case-stress-matrix/117-CONTEXT.md
@.planning/phases/117-edge-case-stress-matrix/117-RESEARCH.md
@.planning/phases/117-edge-case-stress-matrix/117-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: overflow + tall-row assertions (paginate stage, D-05)</name>
  <files>test/rendro/edge_error_matrix_test.exs</files>
  <read_first>
    test/support/edge_fixtures.ex (as written by 117-02 -- `overflow_document/0`, `tall_row_document/0`)
    lib/rendro/pipeline/paginate.ex (lines 292-311 -- table-row-overflow branch, details has :row_index/:row_height/:header_height/:column_widths, NO :block key; lines 863-877 -- generic check_overflow!/4, details.block = block_rect(block))
    lib/rendro/error.ex (lines 273-275 -- `next_step(:paginate, :content_overflow)` returns the exact string `"Reduce content size or expand the declared page/region bounds; Rendro does not auto-fit overflowing content."`, identical for BOTH overflow variants since next_step/2 pattern-matches only on {stage, reason})
    test/rendro/pipeline/measure_test.exs (lines 685-696 -- the struct + stage/reason + next-substring assertion idiom to mirror: match the typed struct, assert stage/reason precisely, assert next =~ substring -- never assert on a raw internal tuple escaping to the caller, per D-07)
  </read_first>
  <behavior>
    - `Rendro.render(EdgeFixtures.overflow_document())` returns `{:error, %Rendro.Error{stage: :paginate, reason: :content_overflow} = error}`; `error.next == "Reduce content size or expand the declared page/region bounds; Rendro does not auto-fit overflowing content."`; `is_map(error.details.block)`.
    - `Rendro.render(EdgeFixtures.tall_row_document())` returns `{:error, %Rendro.Error{stage: :paginate, reason: :content_overflow} = error}`; `error.next` is the SAME exact string as above; `is_number(error.details.row_height)` and `error.details.row_height > 0`; `refute Map.has_key?(error.details, :block)` (this path's details map never merges in a `:block` key -- distinguishing it structurally from the generic overflow fixture above, proving both entry points into `:content_overflow` are genuinely exercised, per D-05).
  </behavior>
  <action>
    Create `test/rendro/edge_error_matrix_test.exs` as `defmodule Rendro.EdgeErrorMatrixTest do use ExUnit.Case, async: true` (render is pure -- safe to run concurrently). `alias Rendro.Test.EdgeFixtures`.

    Add `describe "EDGE-02: overflow and tall-row (D-05)"` with two tests implementing the `<behavior>` cases above exactly, pattern-matching the typed struct first (`assert {:error, %Rendro.Error{stage: :paginate, reason: :content_overflow} = error} = Rendro.render(EdgeFixtures.overflow_document())`), then asserting individual fields -- never asserting against a raw `{:error, :content_overflow, _}` tuple escaping to the caller (D-07's "never a raw internal tuple" rule; the pipeline's internal `throw`/`catch` shape is wrapped by `Rendro.Error.from_stage/3` before `Rendro.render/2` returns it, and only the WRAPPED struct is a legitimate assertion target).
  </action>
  <acceptance_criteria>Both `<behavior>` cases pass; the tall-row assertion explicitly proves `details` has no `:block` key (structural distinction from the generic overflow case).</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/edge_error_matrix_test.exs</automated>
  </verify>
  <done>Overflow and tall-row each raise the correctly-shaped, distinct %Rendro.Error{stage: :paginate, reason: :content_overflow} with the exact next text and family-appropriate details map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RTL default-font + RTL shaping-required assertions (measure stage, D-06) + coverage guard</name>
  <files>test/rendro/edge_error_matrix_test.exs</files>
  <read_first>
    test/support/edge_fixtures.ex (as written by 117-02 -- `rtl_default_font_document/0`, `rtl_shaping_required_document/0`)
    lib/rendro/pipeline/measure.ex (lines 648, 873 -- both `{:halt, {:error, {:unsupported_glyph, grapheme}}}`)
    lib/rendro/text/shaper/simple.ex (lines 8-38 `@requires_shaping` includes `:hebr`/`:arab`; lines 47-59 `shape/3` returns `{:error, {:shaping_required, script, hint}}` when `opts[:script]` is in that set)
    test/rendro/pipeline/measure_test.exs (lines 674-696 -- both the raw `Measure.run/1` assertion AND the full `Rendro.render/1` assertion for the shaping_required case; note the full-render assertion additionally checks `error.render_id`/`error.details.document_type`/`error.why =~ ":arab"` -- reuse the SAME `why`/`next` substring idiom, not full-string equality, since these interpolate the script atom)
  </read_first>
  <behavior>
    - `Rendro.render(EdgeFixtures.rtl_default_font_document())` returns `{:error, %Rendro.Error{stage: :measure, reason: {:unsupported_glyph, char}} = error}` where `char` is a single Hebrew grapheme (e.g. `"ש"`); `error.next =~ "fallback font"`.
    - `Rendro.render(EdgeFixtures.rtl_shaping_required_document())` returns `{:error, %Rendro.Error{stage: :measure, reason: {:shaping_required, :arab, hint}} = error}` where `hint` is a binary; `error.next =~ "shaping adapter"`; `error.why =~ ":arab"`.
    - `refute match?({:ok, _}, Rendro.render(EdgeFixtures.rtl_default_font_document()))` and the same `refute` for `rtl_shaping_required_document/0` -- locking the "never silently render RTL as LTR" guarantee against regression (D-06's explicit negative-outcome lock; Hebrew/Arabic rendered LTR would be silently-wrong output, the worst outcome for an auditable-docs library).
    - A small `@error_cases` list names all 4 representative inputs (`:overflow, :tall_row, :rtl_default_font, :rtl_shaping_required`) and a lightweight test asserts every name has a corresponding `describe`/`test` in this file (grep-style self-check via `File.read!(__ENV__.file)` counting occurrences, OR a simpler explicit comment-documented list -- pick whichever keeps the check honest without being brittle; do not over-engineer this into a second full `@matrix`).
  </behavior>
  <action>
    Add `describe "EDGE-02: RTL refusal, both paths (D-06)"` with the two positive-shape tests and the two `refute` regression-lock tests from `<behavior>` above, matching the typed struct first exactly as Task 1's idiom (D-07).

    Add a brief module-level comment (or a minimal `@error_cases [:overflow, :tall_row, :rtl_default_font, :rtl_shaping_required]` list with one matching assertion, e.g. `test "all 4 representative EDGE-02 inputs are covered"` asserting the list has exactly 4 entries) documenting that this module intentionally covers ENGINE-LEVEL representative cases, not per-family cases (D-08) -- per-family `validate_data!/1` `ArgumentError` coverage already lives in each recipe's own existing test file and is out of scope here.
  </action>
  <acceptance_criteria>
    - Both positive-shape RTL cases pass with the exact `stage`/`reason` shapes
    - Both `refute match?({:ok, _}, ...)` regression locks pass
    - The file documents (via comment or list-count assertion) that it covers exactly the 4 D-08 representative inputs, engine-level, not per-family
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/edge_error_matrix_test.exs</automated>
  </verify>
  <done>Both RTL refusal modes raise the correctly-shaped %Rendro.Error{stage: :measure, ...}; render/2 never silently succeeds for RTL under the default shaper; the file's engine-level-not-per-family scope is explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (test-only, no runtime surface) | Consumes only the already-shipped `lib/` pipeline's public error contract (`%Rendro.Error{}`, `Rendro.render/2`) exactly as any external caller could. No `lib/` edit, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-117-05-01 | Information Disclosure | RTL/silent-mis-render regression | high | mitigate | The `refute match?({:ok, _}, ...)` locks are the actual mitigation artifact: they turn "Rendro could someday silently render Hebrew/Arabic text left-to-right (semantically wrong, looks plausible, easy to miss in review)" into a hard-failing regression test. This is the single highest-value assertion in this plan for an auditable-documents library. |
| T-117-05-02 | Repudiation | Error `stage`/`reason` contract drift | medium | mitigate | Asserting on the typed struct + `stage`/`reason` + `next`/`why` substrings (never raw internal tuples, never full-string prose equality, per D-07) means a future refactor that changes error WORDING doesn't spuriously break this suite, while a refactor that changes the error TYPE/STAGE/REASON contract correctly does. |
</threat_model>

<verification>
`mix test test/rendro/edge_error_matrix_test.exs` -- all tests green, no pdfium dependency, `async: true`.
</verification>

<success_criteria>
- All 4 EDGE-02 representative inputs (overflow, tall-row, RTL default-font, RTL shaping-required) raise the exact typed, live-probe-verified `%Rendro.Error{}` shapes.
- The negative regression lock (`render/2` never returns `{:ok, _}` for RTL under the default shaper) is in place.
- Zero `lib/` edits.
</success_criteria>

<output>
Create `.planning/phases/117-edge-case-stress-matrix/117-05-SUMMARY.md` when done
</output>
