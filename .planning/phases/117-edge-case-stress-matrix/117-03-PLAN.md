---
phase: 117-edge-case-stress-matrix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/docs_contract/branding_claims_test.exs
autonomous: true
requirements: [EDGE-01]
must_haves:
  truths:
    - "The built Hex tarball never contains priv/goldens/ or priv/raster_refs/ paths (D-12's explicit tripwire, backed by mix.exs's existing files: allowlist which already omits both by omission)"
    - "The tarball still contains lib/ and priv/branded/ (a positive companion assertion so an over-aggressive future exclusion also fails loudly, not just an under-exclusion)"
  artifacts:
    - test/docs_contract/branding_claims_test.exs
  key_links:
    - "mix.exs:110-130 defp package's files: allowlist is the actual enforcement mechanism this test tripwires; this plan adds no packaging change, only a guard test"
---

<objective>
Add the D-12 tarball-exclusion tripwire: extend the existing `test/docs_contract/branding_claims_test.exs` "hex tarball contents" describe block with explicit `refute` assertions that `priv/goldens/` and `priv/raster_refs/` never ship in the built Hex tarball, plus a positive companion proving the exclusion isn't over-aggressive (`lib/` still ships).

Purpose: `mix.exs`'s package `files:` allowlist already excludes `priv/goldens`/`priv/raster_refs` by omission (verified this session -- neither path is in the allowlist), but EDGE-01 requires this to be an explicit, tested tripwire rather than an implicit, unenforced gap -- so a future accidental addition of either path to the allowlist fails loudly instead of silently shipping ~40-60 goldens + raster PNGs to every Hex consumer.
Output: Two new `refute` lines inside the existing "built tarball excludes operator-only priv paths" test, plus one new assertion (or a small new test) proving `lib/rendro` still ships.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- No new files. Extends `test/docs_contract/branding_claims_test.exs`'s existing "hex tarball contents" describe block with 2 `refute` lines + 1 `assert` line (or one small new test).
- This is the EDGE-01 tarball-exclusion guard; it does not touch `mix.exs` (the allowlist is already correct -- this plan is a test-only tripwire).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-edge-case-stress-matrix/117-CONTEXT.md
@.planning/phases/117-edge-case-stress-matrix/117-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-12 tarball-exclusion tripwire + positive companion</name>
  <files>test/docs_contract/branding_claims_test.exs</files>
  <read_first>
    test/docs_contract/branding_claims_test.exs (full file, 95 lines -- the "hex tarball contents" describe block at lines 41-73; the exact existing exclude-test "built tarball excludes operator-only priv paths" at lines 57-72; the exact existing include-test "built tarball includes branded assets and NOTICE" at lines 42-55)
    mix.exs (lines 110-128 -- `defp package` `files:` allowlist: `lib`, `assets/rendro`, `priv/branded`, `priv/examples`, `bench/results`, `guides`, `.formatter.exs`, `mix.exs`, `README.md`, `ADOPTION.md`, `LICENSE`, `NOTICE`, `CHANGELOG.md` -- confirms `priv/goldens`/`priv/raster_refs` and `test/` are NOT present, so this test's `refute` assertions pass against the current, unmodified allowlist)
    test/support/hex_build_cache.ex (full file -- `Rendro.Test.HexBuildCache.get_build_output/0`, the shared Agent-cached `mix hex.build` runner both existing tarball tests already call)
  </read_first>
  <action>
    In the existing `test "built tarball excludes operator-only priv paths"` (lines 57-72), immediately after the existing 5 `refute` lines (`priv/viewer_evidence/`, `priv/support_matrix.json`, `priv/schemas/examples.schema.json`, `priv/schemas/rubric_scores.schema.json`, `priv/quality/`), add two more: `refute contents =~ "priv/goldens/"` and `refute contents =~ "priv/raster_refs/"` -- reusing the SAME `contents` variable already computed by that test's existing `tar -xOf ... | tar -tzf -` pipeline (do not duplicate the tarball-build/untar logic).

    Add the positive companion (D-12's "an over-aggressive exclusion also fails loudly" requirement) by extending the existing `test "built tarball includes branded assets and NOTICE"` (lines 42-55) with one additional line: `assert contents =~ "lib/rendro"` (proving the core `lib/` tree still ships -- placed alongside the existing `priv/branded/fonts/...`/`priv/branded/images/...`/`NOTICE` assertions in that SAME test, not a new test, since it already has the tarball built and untarred).
  </action>
  <acceptance_criteria>
    - `test/docs_contract/branding_claims_test.exs`'s exclude-test asserts `refute contents =~ "priv/goldens/"` and `refute contents =~ "priv/raster_refs/"`
    - Its include-test asserts `assert contents =~ "lib/rendro"` alongside the existing branded-asset assertions
    - Both tests pass against the CURRENT, unmodified `mix.exs` allowlist (no `mix.exs` edit in this plan)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/branding_claims_test.exs</automated>
  </verify>
  <done>The built Hex tarball is proven, via an automated test, to exclude priv/goldens/ and priv/raster_refs/ while still shipping lib/rendro and priv/branded -- both directions of D-12's fail-loud guard are covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package build -> Hex tarball -> consumer install | The only trust boundary this plan touches: what ships to every Hex consumer's `deps` directory. `priv/goldens`/`priv/raster_refs` are test-only artifacts (hashes only, per D-03 -- never PDF bytes) that must never cross this boundary, purely to keep the shipped package small and free of internal test infrastructure, not for any secrecy reason. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-117-03-01 | Information Disclosure | Hex package contents (`mix.exs` `files:` allowlist) | low | mitigate | This plan's `refute` assertions turn an implicit, unenforced exclusion (correct today only by omission) into an explicit, tested tripwire -- a future accidental `files:` addition of `priv/goldens`/`priv/raster_refs` now fails `mix test` instead of silently shipping test infrastructure to every consumer. |
</threat_model>

<verification>
`mix test test/docs_contract/branding_claims_test.exs` -- both the exclude and include tests green.
</verification>

<success_criteria>
- `priv/goldens/` and `priv/raster_refs/` are proven excluded from the built Hex tarball by an automated, fail-loud test.
- `lib/rendro` and existing branded assets are proven to still ship (positive companion, guards against over-exclusion).
</success_criteria>

<output>
Create `.planning/phases/117-edge-case-stress-matrix/117-03-SUMMARY.md` when done
</output>
