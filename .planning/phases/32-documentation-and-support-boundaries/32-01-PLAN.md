---
phase: 32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/api_stability.md
  - mix.exs
  - README.md
autonomous: true
requirements: [REL-03, REL-04, REL-05]

must_haves:
  truths:
    - User can read explicit support and API stability boundaries
    - Documentation sidebar categorizes extra pages rationally
    - README prominently displays build and package status
  artifacts:
    - path: "guides/api_stability.md"
      provides: "API stability policy"
      min_lines: 10
  key_links:
    - from: "mix.exs"
      to: "guides/api_stability.md"
      via: "groups_for_extras"
      pattern: "groups_for_extras:"
---

<objective>
Polish the public-facing documentation surfaces and explicitly declare the project's API stability policy before the first public Hex release.

Purpose: Elixir ecosystem adoption requires clear discoverability (HexDocs, README badges) and explicit API guarantees (stability policy, SemVer expectations).
Output: A new `guides/api_stability.md` policy document, organized HexDocs sidebar via `groups_for_extras`, and standard status badges on `README.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mix.exs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define API Stability and Support Boundaries Policy</name>
  <files>guides/api_stability.md</files>
  <action>Create `guides/api_stability.md`. This document must explicitly define the project's public API stability policy and release support boundaries for the `0.1.x` era. Include expectations around semantic versioning (SemVer) for `0.x.x` vs `1.x.x`, and the deprecation policy. Focus on how the core API vs adapters are treated. Do not include executable code snippets unless absolutely necessary (if included, they must follow the `# docs-contract` pattern).</action>
  <verify>
    <automated>test -f guides/api_stability.md</automated>
  </verify>
  <done>guides/api_stability.md exists and contains the support boundaries and API stability policy.</done>
</task>

<task type="auto">
  <name>Task 2: Organize HexDocs Extras</name>
  <files>mix.exs</files>
  <action>Update `mix.exs` `docs/0` function. 
1. Add `"guides/api_stability.md"` to the `extras` list.
2. Add `groups_for_extras:` to rationally organize the guides. 
Use `Guides: ["guides/branding.md", "guides/integrations.md"]` and `Policies: ["guides/api_stability.md"]` or similar logical groupings. Ensure `mix.exs` remains formatted properly.</action>
  <verify>
    <automated>mix test test/mix/tasks/docs_contract_task_test.exs</automated>
  </verify>
  <done>mix.exs contains groups_for_extras categorizing guides and policies.</done>
</task>

<task type="auto">
  <name>Task 3: Add README Status Badges</name>
  <files>README.md</files>
  <action>Update `README.md`. Insert standard status badges for CI, Hex.pm, and HexDocs immediately below the `# Rendro` H1 header and above the first summary paragraph. Use standard Markdown image link formats pointing to `https://github.com/szTheory/rendro/actions/workflows/ci.yml`, `https://hex.pm/packages/rendro`, and `https://hexdocs.pm/rendro` respectively.</action>
  <verify>
    <automated>mix test test/docs_contract/readme_doctest_test.exs</automated>
  </verify>
  <done>README.md contains CI, Hex.pm, and HexDocs badges at the top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Package | Documentation does not execute code, but sets expectations for API stability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | Documentation | accept | No sensitive information, docs are public |
| T-32-02 | Repudiation | API Policy | mitigate | Clear statement of what is and is not supported in the policy doc prevents false expectations |
</threat_model>

<verification>
mix test
</verification>

<success_criteria>
- `guides/api_stability.md` is present and outlines SemVer and support expectations.
- `mix.exs` successfully configures ExDoc's `groups_for_extras`.
- `README.md` displays standard status badges.
</success_criteria>

<output>
After completion, create `.planning/phases/32-documentation-and-support-boundaries/32-01-SUMMARY.md`
</output>