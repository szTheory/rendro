---
phase: 08-bounded-async-timeout-telemetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/pipeline.ex
  - test/rendro/pipeline_test.exs
  - test/rendro/adapters/threadline_test.exs
  - lib/rendro/adapters/oban/render_worker.ex
  - test/rendro/adapters/oban/render_worker_test.exs
autonomous: true
requirements: [ADPT-04, ADPT-05, OBS-02, OBS-04]
must_haves:
  truths:
    - Oban worker fails renders exceeding injected policy bounds
    - Pipeline timeout emits a telemetry exception event
    - Threadline records timeouts as failed renders
  artifacts:
    - path: lib/rendro/adapters/oban/render_worker.ex
      provides: Oban job arg parsing for policies
    - path: test/rendro/adapters/oban/render_worker_test.exs
      provides: Test coverage for bounds injection
  key_links:
    - from: lib/rendro/pipeline.ex
      to: telemetry
      via: execute in timeout case
---

<objective>
Inject render policy bounds into the Oban RenderWorker and make the Pipeline timeout path emit a telemetry exception.

Purpose: Guarantee background jobs abide by memory/page limits and timeouts are properly audited in Threadline.
Output: Tested Oban worker bounded by policies, tested telemetry timeout path, and Threadline observing timeouts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/rendro/adapters/threadline.ex:
```elixir
    @events [
      [:rendro, :render, :stop],
      [:rendro, :render, :exception]
    ]
```

From lib/rendro/pipeline.ex:
```elixir
    case Task.yield(task, timeout) || Task.shutdown(task) do
      {:ok, result} -> result
      nil -> {:error, Error.from_stage(:render, :timeout, base_meta)}
    end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit telemetry exception on render timeout</name>
  <files>lib/rendro/pipeline.ex, test/rendro/pipeline_test.exs, test/rendro/adapters/threadline_test.exs</files>
  <behavior>
    - Pipeline.run/1 times out -> emits `[:rendro, :render, :exception]` with `status: :error`
    - Threadline integration test observes the timeout exception and records as `render_failed`
  </behavior>
  <action>Intercept the `nil` timeout case in `Pipeline.run/1`. When the task times out (via Task.yield returning nil), explicitly emit a `[:rendro, :render, :exception]` telemetry event with `duration: timeout` and appropriate metadata (merge `%{status: :error, kind: :timeout, reason: :timeout}` with `base_meta`, plus zero `page_count` and `byte_size`) before returning the `{:error, ...}` tuple. This enables audit handlers to see crashed/timed-out renders. Update the pipeline and threadline tests to assert this event is fired and captured.</action>
  <verify>
    <automated>mix test test/rendro/pipeline_test.exs test/rendro/adapters/threadline_test.exs</automated>
  </verify>
  <done>Timeout returns error AND fires telemetry exception observed by Threadline</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject policy bounds in Oban worker</name>
  <files>lib/rendro/adapters/oban/render_worker.ex, test/rendro/adapters/oban/render_worker_test.exs</files>
  <behavior>
    - Oban job args with `max_pages` or `max_bytes` sets document policies
    - Worker returns error when bounds are exceeded
  </behavior>
  <action>Update `Rendro.Adapters.Oban.RenderWorker.perform/1` to accept `max_pages`, `max_bytes`, and `timeout` in the job arguments. Before passing the built document to `Rendro.render/2`, inject these limits into `doc.options[:policies]` (append them to existing policies or create the `:policies` key). Create a new test file `test/rendro/adapters/oban/render_worker_test.exs` to verify that when bounds are exceeded (e.g. `max_pages`), the worker correctly returns `{:error, reason}` and does not succeed.</action>
  <verify>
    <automated>mix test test/rendro/adapters/oban/render_worker_test.exs</automated>
  </verify>
  <done>Worker parses bound limits from job args and correctly fails jobs exceeding them</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job Args → Worker | Background jobs are assumed internally trusted, but resource exhaustion is mitigated by bounds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | D | RenderWorker | mitigate | Explicit injection of max_pages/max_bytes bounds from job args into document policies to prevent single background job from OOMing the node |
| T-08-02 | D | Pipeline | mitigate | Explicit Task.yield timeout bounded execution |
</threat_model>

<verification>
mix test test/rendro/pipeline_test.exs test/rendro/adapters/threadline_test.exs test/rendro/adapters/oban/render_worker_test.exs
</verification>

<success_criteria>
1. Oban RenderWorker applies max_pages, max_bytes, and timeout limits.
2. Pipeline timeout emits [:rendro, :render, :exception] which Threadline handles as render_failed.
3. Tests for pipeline, threadline, and render_worker all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bounded-async-timeout-telemetry/08-01-SUMMARY.md`
</output>