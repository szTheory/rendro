---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/table.ex
  - lib/rendro.ex
  - lib/rendro/pipeline/paginate.ex
  - test/rendro_builders_test.exs
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/flow_test.exs
autonomous: true
requirements: [LAY-10]

must_haves:
  truths:
    - "Authored table split policy is consumed at runtime instead of remaining a dead public field."
    - "The supported meaning is explicit row-atomic continuation, with no new speculative table modes."
    - "Regression tests prove the public builder contract and runtime pagination path stay aligned."
  artifacts:
    - path: "lib/rendro/table.ex"
      provides: "Explicit public split-policy contract and type surface"
      min_lines: 20
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Runtime branch that consumes authored split policy"
      min_lines: 60
    - path: "test/rendro/pipeline/paginate_test.exs"
      provides: "Direct runtime proof that split policy is no longer dead"
      min_lines: 30
  key_links:
    - from: "lib/rendro/table.ex"
      to: "lib/rendro.ex"
      via: "public builder/type contract for split policy"
      pattern: "split_policy|row_atomic|atomic"
    - from: "lib/rendro.ex"
      to: "lib/rendro/pipeline/paginate.ex"
      via: "normalized authored split policy reaches pagination"
      pattern: "split_policy"
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "test/rendro/pipeline/paginate_test.exs"
      via: "runtime row-atomic branch and failure behavior"
      pattern: "split_policy|table_split|content_overflow"
---

<objective>
Close the product gap identified by `INT-TABLE-SPLIT-POLICY` by making `%Rendro.Table{split_policy: ...}` affect runtime pagination truthfully.

Purpose: callers should be able to trust that the public table split-policy field is real contract surface, not dead metadata.
Output: explicit row-atomic split-policy contract, runtime paginator consumption of that contract, and regression proof at both builder and end-to-end flow levels.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-table-split-policy-runtime-wiring/23-CONTEXT.md
@.planning/phases/23-table-split-policy-runtime-wiring/23-RESEARCH.md
@.planning/phases/23-table-split-policy-runtime-wiring/23-PATTERNS.md
@.planning/phases/20-table-layout-maturity/20-CONTEXT.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the public split-policy contract around explicit row-atomic semantics</name>
  <files>lib/rendro/table.ex, lib/rendro.ex, test/rendro_builders_test.exs</files>
  <read_first>
    - lib/rendro/table.ex
    - lib/rendro.ex
    - test/rendro_builders_test.exs
    - .planning/phases/23-table-split-policy-runtime-wiring/23-CONTEXT.md
    - .planning/phases/23-table-split-policy-runtime-wiring/23-RESEARCH.md
  </read_first>
  <action>
    Make the public contract explicit per D-01 through D-04. Update `%Rendro.Table{}` and its `@type` so the canonical supported value is `:row_atomic`, meaning "split only between full rows with repeated headers." If compatibility support is needed, keep `:atomic` as a narrow temporary alias for one cycle, but do not leave the public meaning ambiguous: the code, type, and tests must make `:row_atomic` the truthful name. Do not add `:whole_table`, `:avoid_split_if_possible`, or any other new split modes.

    Update `Rendro.table/2` so the boundary behavior is intentional rather than incidental. If a temporary alias is retained, normalize `split_policy: :atomic` into the canonical runtime value or preserve it with an explicit compatibility path that the tests prove. If unsupported values are passed, reject them explicitly at the builder boundary or fail with a typed, actionable contract error; do not silently coerce unknown values into row-atomic behavior. Extend builder tests so they assert the canonical `:row_atomic` contract directly and cover the exact compatibility behavior for `:atomic` if that alias remains.
  </action>
  <acceptance_criteria>
    - `lib/rendro/table.ex` contains `:row_atomic` as the explicit supported split-policy meaning.
    - `lib/rendro/table.ex` does not introduce speculative split-policy values beyond the Phase 23 scope.
    - `test/rendro_builders_test.exs` contains a builder assertion for `split_policy: :row_atomic`.
    - If `:atomic` remains supported, `test/rendro_builders_test.exs` contains a regression covering its exact compatibility behavior.
    - Unsupported split-policy values are rejected explicitly rather than silently treated as row-atomic.
    - `mix test test/rendro_builders_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>
  <done>The public builder and type surface now describe one explicit supported table continuation policy instead of an ambiguous dead field.</done>
</task>

<task type="auto">
  <name>Task 2: Consume authored split policy in pagination and prove the branch with regression tests</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - test/rendro/pipeline/paginate_test.exs
    - test/rendro/flow_test.exs
    - lib/rendro/table.ex
    - lib/rendro.ex
    - .planning/phases/23-table-split-policy-runtime-wiring/23-CONTEXT.md
    - .planning/phases/23-table-split-policy-runtime-wiring/23-RESEARCH.md
    - .planning/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Keep the existing measured-row split engine in `Paginate`, but make it consume the authored split policy instead of bypassing it. The row-atomic path should remain the supported runtime behavior from D-05 and D-06: fit full rows on the current page when possible, otherwise continue on a fresh page with the repeated header, and fail through the existing typed `:paginate/:content_overflow` contract when a single row plus repeated header cannot fit on an empty page/body region. The change here is not to redesign row splitting; it is to make the authored policy field decide whether the paginator enters that path.

    Implement the branch so the runtime contract is explicit. If `:row_atomic` is authored, use the existing split-table behavior. If a temporary `:atomic` alias is still supported, route it through the same canonical row-atomic branch and prove that parity in tests. If any unsupported split-policy value reaches pagination, return a typed, actionable failure rather than silently defaulting to row-atomic. Extend `test/rendro/pipeline/paginate_test.exs` with direct runtime coverage for canonical row-atomic behavior and compatibility alias handling, and extend `test/rendro/flow_test.exs` so at least one end-to-end multi-page table render uses the canonical split-policy field explicitly. Keep whole-table cohesion on `%Rendro.Block{keep_together: true}`; do not add table-local whole-table behavior.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/paginate.ex` branches on `table.split_policy` before executing the table continuation path.
    - The row-atomic branch preserves repeated headers and typed impossible-row overflow details.
    - `test/rendro/pipeline/paginate_test.exs` contains a test proving authored `:row_atomic` reaches runtime pagination.
    - If `:atomic` remains supported, `test/rendro/pipeline/paginate_test.exs` contains a test proving alias parity with `:row_atomic`.
    - `test/rendro/flow_test.exs` contains an end-to-end multi-page table proof using the canonical split-policy field explicitly.
    - Unsupported split-policy values fail explicitly rather than silently falling back.
    - `mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>The paginator now consumes authored split policy, and regression tests prove the public field cannot drift back into dead metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public table authoring surface | `split_policy` is a user-facing contract and must not be ambiguous or dead. |
| Builder to paginator | Any alias or normalization logic must remain explicit so authored intent reaches runtime predictably. |
| Runtime failure surface | Unsupported values and impossible row fits must fail through typed, actionable paths instead of silent fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `lib/rendro/table.ex` public contract | mitigate | Make `:row_atomic` explicit and block speculative modes so callers do not automate against unsupported semantics. |
| T-23-02 | Repudiation | builder-to-runtime split-policy path | mitigate | Prove with focused tests that the authored field is consumed by pagination and that any temporary alias behavior is deliberate. |
| T-23-03 | Denial of service | table continuation logic | mitigate | Preserve existing typed impossible-row overflow behavior and avoid silent retry/fallback branches. |
| T-23-04 | Elevation of privilege | table-local policy surface | mitigate | Keep whole-table cohesion on block-level keep semantics rather than broadening `split_policy` into overlapping meanings. |
</threat_model>

<verification>
Run `mix test test/rendro_builders_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs` to prove the explicit row-atomic contract, runtime policy consumption, repeated headers, and typed overflow behavior land together.
</verification>

<success_criteria>
- `%Rendro.Table{split_policy}` is a truthful public contract surface, not dead metadata.
- Runtime pagination consumes the authored row-atomic policy and preserves deterministic repeated-header continuation.
- Regression tests prove both boundary semantics and end-to-end behavior for the supported split-policy contract.
</success_criteria>

<output>
After completion, create `.planning/phases/23-table-split-policy-runtime-wiring/23-01-SUMMARY.md`
</output>
