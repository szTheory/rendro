---
phase: "112"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - ".github/dependabot.yml"
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
must_haves:
  truths:
    - "Maintainers receive weekly dependabot PRs grouped by context, reducing fatigue."
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Grouped weekly dependency updates"
      contains: "package-ecosystem"
  key_links: []
---

<objective>
Configure Dependabot to manage dependencies for Hex and GitHub Actions on a grouped, weekly schedule to minimize PR fatigue.

Purpose: Maintainers need an automated but low-noise way to keep libraries and actions up to date. Grouping and weekly schedules concentrate the review burden.
Output: A new `.github/dependabot.yml` file enforcing the grouping strategy.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Configure Dependabot for Hex and GitHub Actions</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml
  </read_first>
  <action>
    Create `.github/dependabot.yml` based on the specified decisions (D-01, D-02, D-03).
    Use configuration `version: 2`.
    
    Define two update configurations:
    1. `package-ecosystem: "mix"` with `directory: "/"`
    2. `package-ecosystem: "github-actions"` with `directory: "/"`
    
    Schedule for both must be `interval: "weekly"`.
    
    Configure `groups` for the `mix` ecosystem:
    - Name: `elixir-dev-tools`
      - `patterns`: `["ex_doc", "credo", "dialyxir", "mix_audit", "bypass"]`
    - Name: `runtime-minor-patch`
      - `patterns`: `["*"]`
      - `update-types`: `["minor", "patch"]`
    
    Configure `groups` for the `github-actions` ecosystem:
    - Name: `github-actions`
      - `patterns`: `["*"]`
  </action>
  <verify>
    <automated>grep -q 'version: 2' .github/dependabot.yml && grep -q 'package-ecosystem: "mix"' .github/dependabot.yml && grep -q 'package-ecosystem: "github-actions"' .github/dependabot.yml && grep -q 'interval: "weekly"' .github/dependabot.yml && grep -A 5 'elixir-dev-tools:' .github/dependabot.yml | grep -q 'ex_doc' && grep -A 5 'runtime-minor-patch:' .github/dependabot.yml | grep -q 'minor'</automated>
  </verify>
  <done>Dependabot configuration properly groups Hex and GitHub Actions updates weekly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI/CD Pipeline -> Dependabot | Dependabot dictates code execution; grouping scopes PRs but does not prevent malicious upstream code. Review remains manual. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | `.github/dependabot.yml` | mitigate | Grouping restricts automatic merges; manual review required for major version bumps. |
</threat_model>

<verification>
- `.github/dependabot.yml` is syntactically valid YAML.
- Groups match the specified phase requirements.
</verification>

<success_criteria>
- Dependabot config file exists with the requested groups and schedule.
</success_criteria>

<output>
Create `.planning/phases/112-security-supply-chain-release-hardening/112-01-SUMMARY.md` when done.
</output>