---
phase: "112"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - ".github/workflows/ci.yml"
  - ".github/workflows/audit.yml"
autonomous: true
requirements:
  - D-04
  - D-05
must_haves:
  truths:
    - "Community PRs get immediate advisory feedback on dependency vulnerabilities."
    - "Nightly audit creates an actionable issue if upstream CVEs appear, without blocking the main branch."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Advisory PR audit lane"
      contains: "security-audit:"
    - path: ".github/workflows/audit.yml"
      provides: "Nightly actionable audit lane"
      contains: "peter-evans/create-issue-from-file"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "ci-success"
      via: "needs array"
      pattern: "security-audit"
---

<objective>
Introduce separated security audit lanes to give immediate, non-blocking feedback on PRs while maintaining a strict, actionable nightly audit for maintainers.

Purpose: Prevent upstream CVEs from instantly breaking community forks/PRs (violates principle of least surprise), while ensuring maintainers track and act on vulnerabilities through automated issues.
Output: Updates to `.github/workflows/ci.yml` and a new `.github/workflows/audit.yml`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Add Advisory Audit Job to CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Add a new discrete job named `security-audit` to `.github/workflows/ci.yml`.
    Set `continue-on-error: true` on the `security-audit` job.
    Run on `ubuntu-latest`.
    Include steps: `actions/checkout@v6.0.3` (or current SHA), `erlef/setup-beam` matching the exact SHA pinning `8251c48667b97e88a0a24ec512f5b72a039fcea7` (otp '28', elixir '1.19.5'), `mix deps.get`, `mix deps.audit`, and `mix hex.audit`.
    Finally, append `security-audit` to the `needs:` array of the existing `ci-success` job so the pipeline waits for it to complete.
  </action>
  <verify>
    <automated>grep 'security-audit:' .github/workflows/ci.yml && grep -A 2 'security-audit:' .github/workflows/ci.yml | grep -q 'continue-on-error: true' && grep 'mix deps.audit' .github/workflows/ci.yml && grep 'mix hex.audit' .github/workflows/ci.yml && grep -E 'needs:.*security-audit' .github/workflows/ci.yml</automated>
  </verify>
  <done>Advisory `security-audit` job is added to CI without blocking `ci-success`.</done>
</task>

<task type="auto">
  <name>Create Nightly Audit Workflow</name>
  <files>.github/workflows/audit.yml</files>
  <read_first>
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Create `.github/workflows/audit.yml` triggered by `schedule` (e.g., `cron: '0 3 * * *'`) and `workflow_dispatch`.
    Define a strict job (no `continue-on-error`) named `audit` running on `ubuntu-latest`.
    Steps must include: checkout, setup-beam (exact SHA pinning `8251c486...`), `mix deps.get`, `mix deps.audit`, and `mix hex.audit`.
    Add a final step with `if: failure()` using exact pinned SHA `peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710`.
    The step should create an issue titled "Security Audit Failure" containing workflow run details. Assign the issue to the maintainers (or leave unassigned if exact handles are unknown).
  </action>
  <verify>
    <automated>grep -q 'schedule:' .github/workflows/audit.yml && grep -q 'mix hex.audit' .github/workflows/audit.yml && grep -q 'peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710' .github/workflows/audit.yml && grep -q 'if: failure()' .github/workflows/audit.yml</automated>
  </verify>
  <done>Nightly audit strictly checks dependencies and opens an issue on failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI/CD Pipeline -> Hex Audit | Audit checks verify downstream packages against known CVEs but do not prevent 0-day exploits. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02 | Information Disclosure | `peter-evans/create-issue-from-file` | mitigate | Action creates an issue visible on GitHub. We do not echo secrets in the workflow. |
</threat_model>

<verification>
- `ci.yml` is syntactically valid YAML and `security-audit` is in `ci-success` needs.
- `audit.yml` correctly targets `peter-evans/create-issue-from-file` on failure.
</verification>

<success_criteria>
- CI Pipeline has discrete advisory checks and nightly tracking issues are wired up.
</success_criteria>

<output>
Create `.planning/phases/112-security-supply-chain-release-hardening/112-02-SUMMARY.md` when done.
</output>