---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/block.ex
  - lib/rendro/text.ex
  - lib/rendro/pipeline/measured_text.ex
  - lib/rendro/pipeline/measure.ex
  - test/rendro/pipeline/measure_test.exs
  - test/rendro_builders_test.exs
autonomous: true
requirements: [LAY-06, LAY-09]

must_haves:
  truths:
    - "Width-constrained flow text measures into the same wrapped lines every time for identical input."
    - "Authored geometry stays on `Rendro.Block` while text-specific vertical styling stays on `Rendro.Text`."
    - "Flow break intent is explicit on `Rendro.Block` before pagination evaluates it."
  artifacts:
    - path: "lib/rendro/pipeline/measured_text.ex"
      provides: "Private measured-text carrier with wrapped line data for downstream pagination and rendering"
      min_lines: 20
    - path: "lib/rendro/pipeline/measure.ex"
      provides: "Deterministic newline-aware and width-aware text measurement"
      min_lines: 40
    - path: "lib/rendro/block.ex"
      provides: "Public block-level keep and break directive fields"
      min_lines: 15
  key_links:
    - from: "lib/rendro/block.ex"
      to: "lib/rendro/pipeline/paginate.ex"
      via: "block-level keep and break directive fields"
      pattern: "keep_together|keep_with_next|break_before|break_after"
    - from: "lib/rendro/pipeline/measure.ex"
      to: "lib/rendro/pdf/writer.ex"
      via: "measured wrapped line payload"
      pattern: "MeasuredText|lines"
---

<objective>
Define the wrapped-text and break-intent contract for Phase 19, then teach `Measure` to produce deterministic wrapped-line geometry that later pagination and writer work can consume without re-measuring strings.

Purpose: lock the public ownership model from CONTEXT.md before behavior fans out across pagination, rendering, and docs.
Output: block/text field updates, a private measured-text carrier, deterministic wrapping logic, and direct measurement proofs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-PATTERNS.md
@.planning/phases/18-layout-contract-and-page-template-model/18-03-SUMMARY.md

<interfaces>
From `lib/rendro/block.ex`:
```elixir
defstruct [
  :content,
  x: 0,
  y: 0,
  width: nil,
  height: nil
]
```

From `lib/rendro/text.ex`:
```elixir
defstruct [
  :content,
  font: "Helvetica",
  size: 12,
  color: {0, 0, 0}
]
```

From `lib/rendro/pipeline/measure.ex`:
```elixir
defp measure_block(%Rendro.Block{content: %Rendro.Text{} = text} = block, font) do
  width = block.width || Font.text_width(font, text.content, text.size)
  height = block.height || text.size * 1.2
  %{block | width: width, height: height}
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the public break-intent fields and private measured-text contract</name>
  <files>lib/rendro/block.ex, lib/rendro/text.ex, lib/rendro/pipeline/measured_text.ex, test/rendro_builders_test.exs</files>
  <read_first>
    - lib/rendro/block.ex
    - lib/rendro/text.ex
    - test/rendro_builders_test.exs
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
  </read_first>
  <action>
    Extend `Rendro.Block` with the four block-level pagination flags from D-06: `keep_together`, `keep_with_next`, `break_before`, and `break_after`, each defaulting to `false` and reflected in the `@type`. Keep width and height on `Block` per D-01 and D-02. Extend `Rendro.Text` with a text-owned vertical styling field named `line_height`, default `1.2`, and update its `@type` so wrapped-text line spacing stays on the leaf struct per D-03. Add a new internal-only module `Rendro.Pipeline.MeasuredText` that stores the original `%Rendro.Text{}` plus deterministic wrapped-line data needed by downstream stages; include fields for `source`, `lines`, `line_height`, `width`, and `height`. Update `test/rendro_builders_test.exs` so `Rendro.text/2` accepts `line_height: 1.2` and `Rendro.block/2` accepts the new break-intent flags while still rejecting unknown keys via `struct!`. Do not add paragraph helpers, block-local line-height fields, or any public `Rendro.Text.width` geometry per D-01, D-03, and D-05.
  </action>
  <acceptance_criteria>
    - `lib/rendro/block.ex` contains `keep_together: false`, `keep_with_next: false`, `break_before: false`, and `break_after: false`.
    - `lib/rendro/text.ex` contains `line_height: 1.2`.
    - `lib/rendro/pipeline/measured_text.ex` exists and contains `defmodule Rendro.Pipeline.MeasuredText`.
    - `test/rendro_builders_test.exs` contains assertions for `line_height` and at least one of `keep_together` or `break_before`.
    - `mix test test/rendro_builders_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>
  <done>The public ownership model for geometry, styling, and break intent is explicit and test-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Measure wrapped text deterministically from block width</name>
  <files>lib/rendro/pipeline/measure.ex, test/rendro/pipeline/measure_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/measure.ex
    - test/rendro/pipeline/measure_test.exs
    - lib/rendro/pipeline/measured_text.ex
    - lib/rendro/block.ex
    - lib/rendro/text.ex
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
  </read_first>
  <action>
    Update `Measure` so `%Rendro.Block{content: %Rendro.Text{}}` uses `block.width` as the wrap constraint per D-02. Preserve explicit newlines first, then wrap each newline-separated segment deterministically on whitespace per D-04. For a single token whose width exceeds the available width, hard-wrap it grapheme-by-grapheme with no hyphen insertion so the fallback is deterministic and fixture-testable. Populate the new `Rendro.Pipeline.MeasuredText` carrier with the original text struct and final `lines`, then store that carrier on the measured block content so `Paginate` and `Writer` can consume it without re-splitting strings. Compute total block height from `text.size * text.line_height * line_count` when `block.height` is nil, and preserve caller-supplied `block.height` overrides exactly. Add `Measure` tests for: identical input yields identical wrapped `lines`; explicit `\n` produces distinct lines; a long token falls back to grapheme wrapping; and constrained wrapping increases measured height while leaving the authored `block.width` unchanged.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/measure.ex` contains handling for `Rendro.Pipeline.MeasuredText`.
    - `test/rendro/pipeline/measure_test.exs` contains a test that asserts wrapped `lines`.
    - `test/rendro/pipeline/measure_test.exs` contains a test that asserts explicit newline preservation.
    - `test/rendro/pipeline/measure_test.exs` contains a test that asserts long-token fallback behavior.
    - `mix test test/rendro/pipeline/measure_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Measured text now carries deterministic wrapped-line geometry derived from the existing block-and-text API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public authoring structs | New block/text fields become product contract and must not blur geometry vs. styling ownership. |
| Measure to downstream stages | Wrapped-line data must stay deterministic and must not depend on render-time string splitting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `Rendro.Block` / `Rendro.Text` public structs | mitigate | Add explicit fields only where locked decisions allow them and cover acceptance through builder tests per D-01, D-03, and D-06. |
| T-19-02 | Denial of service | `Rendro.Pipeline.Measure` wrapping fallback | mitigate | Use deterministic grapheme iteration for oversized tokens instead of unbounded retry logic or render-time reflow. |
| T-19-03 | Repudiation | Wrapped-text measurement | mitigate | Encode measured lines in a private carrier so later stages consume the same line list that the measurement tests prove. |
</threat_model>

<verification>
Run `mix test test/rendro_builders_test.exs test/rendro/pipeline/measure_test.exs` to prove the public contract and deterministic wrapped measurement land together.
</verification>

<success_criteria>
- `Rendro.Block` exposes the Phase 19 keep/break flags and `Rendro.Text` owns line-height styling.
- `Measure` preserves explicit newlines, wraps on whitespace, and uses a deterministic long-token fallback.
- A private measured-text carrier exists so later plans can paginate and render the exact measured lines.
</success_criteria>

<output>
After completion, create `.planning/phases/19-deterministic-text-flow-and-break-semantics/19-01-SUMMARY.md`
</output>
