---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/rendro/pipeline/paginate.ex
  - lib/rendro/error.ex
  - test/rendro/pipeline/paginate_test.exs
autonomous: true
requirements: [LAY-09]

must_haves:
  truths:
    - "Block-level `keep_together`, `keep_with_next`, `break_before`, and `break_after` drive page assignment deterministically."
    - "Impossible keep-group layouts fail truthfully instead of relaxing the authored rule."
    - "Flow-only break directives on fixed-position pages are rejected with a typed paginate boundary error."
  artifacts:
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Deterministic keep-group evaluation and explicit break semantics"
      min_lines: 50
    - path: "lib/rendro/error.ex"
      provides: "Typed paginate guidance for keep-rule and fixed-page directive failures"
      min_lines: 20
  key_links:
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "test/rendro/pipeline/paginate_test.exs"
      via: "keep-group movement and failure diagnostics"
      pattern: "keep_together|keep_with_next|break_before|break_after"
---

<objective>
Teach `Paginate` to honor authored break intent from `Rendro.Block` using the measured heights produced in Plan 01, and keep failure behavior truthful when the requested grouping cannot fit.

Purpose: satisfy the explicit pagination-control requirement without inventing a second pagination model or softening the authored rules.
Output: deterministic keep/break pagination logic, structured error details, and focused stage-level regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-PATTERNS.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-01-PLAN.md
@.planning/phases/18-layout-contract-and-page-template-model/18-03-SUMMARY.md

<interfaces>
From `lib/rendro/pipeline/paginate.ex`:
```elixir
defp paginate_block(block, [current_page | rest] = _pages, template, max_h, overflow_details) do
  block_h = block.height || 0
  current_h = Enum.sum(Enum.map(current_page.blocks, &(&1.height || 0)))
```

From `lib/rendro/error.ex`:
```elixir
def from_stage(stage, reason, context \\ %{}) when is_atom(stage) do
  %__MODULE__{stage: stage, reason: reason, details: Map.get(context, :details, %{})}
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paginate measured blocks with deterministic keep and break groups</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - test/rendro/pipeline/paginate_test.exs
    - lib/rendro/block.ex
    - lib/rendro/pipeline/measured_text.ex
    - lib/rendro/pipeline/measure.ex
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
  </read_first>
  <action>
    Refactor flow pagination so `Paginate` evaluates authored directives after measurement. Implement `break_before` by starting a fresh page before placing the current block, and implement `break_after` by forcing the next block onto a fresh page after the current block is placed. Implement `keep_together` as a hard single-block fit rule: if the measured block does not fit on the current page but does fit on an empty page/body region, move it intact; if it cannot fit even there, fail with `%Rendro.Error{stage: :paginate, reason: :content_overflow}` per D-10 and D-11. Implement `keep_with_next` as a contiguous chain rule per D-09 and research assumption A2: gather the current block plus each immediately following block while `keep_with_next` remains `true`, measure the total kept height, and either place the full chain on the current page, move the chain to a fresh page, or fail truthfully if the chain cannot fit even on a fresh page. Keep table splitting behavior intact for non-kept tables and do not introduce section-level break nodes or best-effort relaxation per D-08 and D-10.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/paginate.ex` contains logic for `break_before` and `break_after`.
    - `lib/rendro/pipeline/paginate.ex` contains logic for `keep_together` and `keep_with_next`.
    - `test/rendro/pipeline/paginate_test.exs` contains a test proving chained `keep_with_next` behavior across three blocks.
    - `test/rendro/pipeline/paginate_test.exs` contains a test proving `break_before` or `break_after` forces a page boundary.
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>`Paginate` now turns authored block directives into deterministic page moves instead of treating all blocks as independent units.</done>
</task>

<task type="auto">
  <name>Task 2: Return typed diagnostics for impossible keep rules and fixed-page directive misuse</name>
  <files>lib/rendro/pipeline/paginate.ex, lib/rendro/error.ex, test/rendro/pipeline/paginate_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - lib/rendro/error.ex
    - test/rendro/pipeline/paginate_test.exs
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
    - .planning/phases/18-layout-contract-and-page-template-model/18-03-SUMMARY.md
  </read_first>
  <action>
    Extend the existing typed paginate error surface instead of creating a new top-level error family. For impossible keep layouts, continue using `reason: :content_overflow` and enrich `error.details` with at least `keep_rule`, `kept_height`, `max_height`, `page_index`, `region`, `overflow_source`, and `block_indexes` per D-12. For flow-only break directives applied to fixed-position pages, return a typed paginate boundary error with `reason: :invalid_flow_directive` and `details` including `directive`, `page_index`, and `block_index`, then add matching `Rendro.Error.next_step/2` guidance that tells users to remove flow directives from fixed-position pages or use `Rendro.flow/2` instead. Add paginate tests for both cases and ensure no path silently clips content, clears the directive, or raises a raw exception.
  </action>
  <acceptance_criteria>
    - `lib/rendro/error.ex` contains `defp next_step(:paginate, :invalid_flow_directive)`.
    - `test/rendro/pipeline/paginate_test.exs` contains a test asserting `error.details.keep_rule`.
    - `test/rendro/pipeline/paginate_test.exs` contains a test asserting `reason == :invalid_flow_directive` for a fixed page.
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Hard keep rules and fixed-page directive misuse now fail through stable typed diagnostics instead of undefined behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measured layout to pagination | Paginate must consume measured heights exactly as produced in Plan 01. |
| Public error surface | Keep-rule failures and boundary misuse are contract-level behavior that callers will automate against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | `Rendro.Pipeline.Paginate` keep-rule evaluation | mitigate | Evaluate all keep and break directives after measurement using explicit chain/group rules and cover the rules with paginate tests. |
| T-19-05 | Repudiation | Keep-rule failure diagnostics | mitigate | Preserve the existing typed paginate error contract and enrich `details` with the exact keep rule, block indexes, and max height per D-11 and D-12. |
| T-19-06 | Elevation of privilege | Fixed-page API misuse | mitigate | Reject flow-only directives on fixed pages with `:invalid_flow_directive` instead of silently applying unsupported behavior. |
</threat_model>

<verification>
Run `mix test test/rendro/pipeline/paginate_test.exs` to prove deterministic keep-group movement, explicit break boundaries, and typed failure diagnostics.
</verification>

<success_criteria>
- `Paginate` honors `keep_together`, `keep_with_next`, `break_before`, and `break_after` on measured blocks.
- Impossible keep-rule layouts return structured typed errors with keep-specific details.
- Fixed-position pages reject flow-only break directives through a stable boundary error.
</success_criteria>

<output>
After completion, create `.planning/phases/19-deterministic-text-flow-and-break-semantics/19-02-SUMMARY.md`
</output>
