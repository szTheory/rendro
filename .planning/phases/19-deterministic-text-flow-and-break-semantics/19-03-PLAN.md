---
phase: 19
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
  - test/rendro/flow_test.exs
  - README.md
  - guides/integrations.md
autonomous: true
requirements: [LAY-06, LAY-09]

must_haves:
  truths:
    - "Rendered PDF output uses the exact wrapped lines measured upstream instead of rendering the original paragraph as one line."
    - "Public `Rendro.render/1` examples prove wrapped text and explicit keep/break directives through the existing flow builders."
    - "Docs state the narrow Phase 19 contract and explicitly reject unsupported paragraph/typography promises."
  artifacts:
    - path: "lib/rendro/pdf/writer.ex"
      provides: "Multi-line text serialization from measured wrapped lines"
      min_lines: 35
    - path: "README.md"
      provides: "User-facing wrapped text and break-semantics guidance"
      min_lines: 20
    - path: "test/rendro/flow_test.exs"
      provides: "End-to-end public render proof for wrapped text and keep/break semantics"
      min_lines: 30
  key_links:
    - from: "lib/rendro/pdf/writer.ex"
      to: "test/rendro/pdf/writer_test.exs"
      via: "multi-line text stream serialization"
      pattern: "BT|Td|Tj"
    - from: "README.md"
      to: "test/docs_contract/readme_doctest_test.exs"
      via: "docs contract verification"
      pattern: "docs-contract"
---

<objective>
Finish Phase 19 by making rendered PDFs honor measured wrapped lines and by publishing truthful public examples for wrapped text and block-level keep/break directives.

Purpose: close the render truth gap and ship a public contract that Phoenix teams can use without reading internal pipeline stages.
Output: writer changes, end-to-end render proofs, and docs/examples aligned to the supported scope.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-PATTERNS.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-01-PLAN.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-02-PLAN.md

<interfaces>
From `lib/rendro/pdf/writer.ex`:
```elixir
defp render_block(%Rendro.Block{content: %Rendro.Text{} = text} = block, page, _font, ox, oy) do
  [
    "BT",
    "/F1 #{format_num(text.size)} Tf",
    "#{format_num(x)} #{format_num(y)} Td",
    "(#{escape_pdf_string(text.content)}) Tj",
    "ET"
  ]
end
```

From `README.md`:
```elixir
doc = Rendro.Recipes.invoice(data)
{:ok, _pdf} = Rendro.render(doc)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialize measured wrapped lines explicitly in the PDF writer</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - lib/rendro/pdf/writer.ex
    - test/rendro/pdf/writer_test.exs
    - test/rendro/flow_test.exs
    - lib/rendro/pipeline/measured_text.ex
    - lib/rendro/pipeline/measure.ex
    - lib/rendro/pipeline/paginate.ex
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
  </read_first>
  <action>
    Update `Rendro.PDF.Writer` so wrapped text renders from the measured line payload produced in Plan 01 instead of writing one `Tj` for the original paragraph string. Keep coordinates deterministic and page-local: emit the first line at the current block origin, then offset each subsequent line by `text.size * text.line_height` so the rendered line positions match the measured block height exactly. Do not re-run wrapping or width measurement in the writer. Extend `test/rendro/pdf/writer_test.exs` with a multi-line assertion that the content stream contains each measured line separately, and extend `test/rendro/flow_test.exs` with an end-to-end render proof that constrained flow text wraps into multiple `Tj` operations while still producing stable page counts across identical input.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pdf/writer.ex` contains code that iterates over measured wrapped lines.
    - `test/rendro/pdf/writer_test.exs` contains a test proving multiple `Tj` operations for one wrapped block.
    - `test/rendro/flow_test.exs` contains a public render test for width-constrained wrapped text.
    - `mix test test/rendro/pdf/writer_test.exs test/rendro/flow_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>The writer and public render boundary now honor the measured wrapped lines exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Publish truthful Phase 19 examples and boundaries</name>
  <files>README.md, guides/integrations.md, test/rendro/flow_test.exs</files>
  <read_first>
    - README.md
    - guides/integrations.md
    - test/rendro/flow_test.exs
    - .planning/PROJECT.md
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-CONTEXT.md
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-RESEARCH.md
  </read_first>
  <action>
    Update the README flow section to show the supported Phase 19 authoring path from D-14 and D-15: `Rendro.flow/2`, `Rendro.block/2`, `Rendro.text/2`, and page templates/regions when width constraints matter. Add one example that constrains a body block width and explicitly documents the D-04 long-token fallback: preserve explicit newlines, wrap on whitespace, and hard-wrap overlong single tokens grapheme-by-grapheme with no hyphen insertion. Add one example that documents the full public break surface from D-14 on `Rendro.Block`: `keep_together`, chained `keep_with_next` behavior per D-09, `break_before`, and `break_after`. State that consecutive `keep_with_next` blocks form one contiguous keep group ending at the first block without the flag. Add explicit boundary language from D-16 that says Phase 19 does not promise widow/orphan control, hyphenation, browser/CSS break parity, or best-effort keep-rule relaxation. Keep `guides/integrations.md` aligned with that same scope boundary and public semantics, but do not widen the adapter surface or add a paragraph DSL. Add or update `test/rendro/flow_test.exs` assertions only where needed to keep the public render examples covered by executable proof. Verify docs through the repo’s supported fallback command `mix run scripts/verify_docs.exs` rather than inventing a new docs task.
  </action>
  <acceptance_criteria>
    - `README.md` contains `keep_together` or `keep_with_next`.
    - `README.md` contains `break_before` and `break_after`.
    - `README.md` contains text describing the long-token fallback with `grapheme` or `no hyphen`.
    - `README.md` contains text describing chained `keep_with_next` behavior.
    - `README.md` contains boundary language mentioning `hyphenation` and `widow/orphan`.
    - `guides/integrations.md` mentions `break_before` or `break_after` and keeps the semantics framed as core-library behavior, not adapter-specific behavior.
    - `mix run scripts/verify_docs.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>The public docs teach the new wrapped-text and break semantics truthfully and stay inside the project’s support boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measure to render | The writer must serialize exactly what measurement decided; any reflow here breaks determinism. |
| Public documentation | README and guides are product contracts and must not claim more paragraph intelligence than Phase 19 actually supports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | `Rendro.PDF.Writer` line serialization | mitigate | Render only from the measured wrapped-line payload and cover multi-line output with writer and flow regression tests. |
| T-19-08 | Repudiation | Public docs/examples | mitigate | Add explicit unsupported-boundary language for hyphenation, widow/orphan control, CSS parity, and keep-rule relaxation per D-16, then verify with the docs contract script. |
| T-19-09 | Information disclosure | Internal pipeline details in docs | mitigate | Keep docs centered on `Rendro.flow/2`, `Rendro.block/2`, `Rendro.text/2`, and page templates/regions without exposing private measured-text implementation details. |
</threat_model>

<verification>
Run `mix test test/rendro/pdf/writer_test.exs test/rendro/flow_test.exs` and `mix run scripts/verify_docs.exs` to prove render truth and public docs contract alignment.
</verification>

<success_criteria>
- Wrapped PDF output now emits measured lines explicitly instead of rendering one raw paragraph string.
- Public render tests prove width-constrained wrapped text and block-level keep/break behavior through the existing builder API.
- README and guides teach the new semantics truthfully and state the Phase 19 exclusions explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/19-deterministic-text-flow-and-break-semantics/19-03-SUMMARY.md`
</output>
