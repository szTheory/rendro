---
phase: 136-catalog-visual-quality
plan: "08"
type: execute
wave: 6
depends_on: ["136-07"]
files_modified:
  - priv/quality/rubric_scores.json
  - priv/quality/SIGN-OFF.md
  - assets/rendro/catalog.json
  - assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png
  - assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png
  - assets/rendro/catalog/payslip/northline-logistics/swiss-light.png
  - assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png
  - assets/rendro/catalog/ticket/aurora-live/brutalist-light.png
  - assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png
autonomous: false
gap_closure: true
requirements: [CATALOG-10, CATALOG-11, CATALOG-12, CATALOG-13]
must_haves:
  truths:
    - "Per D-21, a named human reviews only hash-bound full-size reconciled images in locked Invoice light/dark, Statement light/dark, Payslip light/dark, Ticket light/dark order after Plan 07 validation and fresh Plan 08 intake revalidation."
    - "Per D-22, each of the six exact targets has an independent record containing six frozen integer scores, reading order, unchanged print_safety, concise rationale, reviewer identity/date, exact candidate SHA, PDFium version/executable digest, run/attempt, source-PDF hash, PNG hash, Artifact URL/digest, and superseded evidence reference."
    - "Per D-23, Phase 136 qualification requires content_hierarchy exactly 5, every other scored visual dimension at least 4, and reading_order true for every target; complete manifest passed arithmetic remains separate and every dark record remains print_safety:false."
    - "Per D-24, any absent, incomplete, or sub-threshold record retains its actual result and unpromoted disposition; no rounding, clamping, averaging, family inference, generated score, or historical reassociation is allowed."
    - "Per D-25, canonical materialization is atomic and occurs only after all six reviews qualify and exact six-changed/26-control proof passes; otherwise all seven canonical paths remain byte-identical and the exact next action is recorded."
    - "Catalog membership remains exactly 32 ordered cells, 20 explicitly unscored; only Corporate Classic Invoice dark, Minimal Mono Statement dark, Swiss Payslip light/dark, and Brutalist Ticket light/dark may gain current review/canonical provenance per D-01 and CATALOG-10/CATALOG-12."
    - "Invoice empty/loading/error/populated/partial/zero-one-many behavior remains the already-proven atomic structured-render contract; this evidence closure introduces no empty substitute, fallback PDF, fixture edit, or padding."
    - "Statement empty/loading/error/populated/partial/zero-one-many behavior remains the already-proven atomic structured-render contract; this evidence closure introduces no empty substitute, fallback PDF, fixture edit, or padding."
    - "Payslip empty/loading/error/populated/partial/zero-one-many behavior remains the already-proven sequential measured-ledger contract with owned headers, no fake blank rows, and present reconciliation."
    - "Review bundle empty, loading, error, populated, partial, overflow, zero/one/many, and long-text states remain explicit: no bundle authority before validation, no cropped images, no truncated identity, and no promotion from incomplete evidence."
    - "Canonical-procedure overflow and long text remain ordinary terminal/runbook output that preserves exact six/26, all-six threshold, and dark print-safety predicates without compliance overclaim."
    - "[backstop] Held-out long Invoice facts/items preserve deterministic wrapping, geometry, pagination, caller text, and clipping behavior under the already-completed semantic-ink-only repair."
    - "[backstop] Existing long Invoice labels/facts retain deterministic measured wrapping without clipping or caller-text changes."
    - "[backstop] Held-out long Statement ledger facts preserve deterministic wrapping, geometry, pagination, and caller text under the already-completed semantic-ink-only repair."
    - "[backstop] Existing long Statement descriptions/context retain deterministic measured wrapping without clipping or caller-text changes."
    - "[backstop] Held-out Payslip longest labels, widest money tokens, and continuation pages retain flexible descriptions, atomic right-aligned money, native pagination, repeated own-table headers, and reserved reconciliation space."
    - "[backstop] Held-out verbatim Payslip descriptions wrap only in the flexible description column; headers and money never wrap, clip, abbreviate, or shrink."
    - "[backstop] Ticket assertions retain one equal-share locator row with atomic distinct GA, H, 24, and B values in light/dark and no 24B association."
    - "[backstop] Held-out Ticket subtitle, terms, and reference strings wrap only in existing prose regions; locator labels/values never wrap or truncate."
  artifacts:
    - path: priv/quality/rubric_scores.json
      provides: "Six current reviewer-owned target records only when exact complete named evidence exists"
      contains: "catalog_dispositions"
    - path: priv/quality/SIGN-OFF.md
      provides: "Phase 136 named review provenance, actual misses, or explicit unreviewed continuation"
      contains: "Phase 136"
    - path: assets/rendro/catalog.json
      provides: "Canonical ordered 32-cell projection only after all eligibility predicates pass"
      contains: "catalog_id"
    - path: assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png
      provides: "Eligible exact candidate Invoice target raster"
    - path: assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png
      provides: "Eligible exact candidate Statement target raster"
    - path: assets/rendro/catalog/payslip/northline-logistics/swiss-light.png
      provides: "Eligible exact candidate Swiss Payslip light raster"
    - path: assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png
      provides: "Eligible exact candidate Swiss Payslip dark raster"
    - path: assets/rendro/catalog/ticket/aurora-live/brutalist-light.png
      provides: "Eligible exact candidate Brutalist Ticket light raster"
    - path: assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png
      provides: "Eligible exact candidate Brutalist Ticket dark raster"
  key_links:
    - from: "136-07-SUMMARY.md and the exact recorded review artifact"
      to: priv/quality/rubric_scores.json
      via: "Plan 08 independently re-downloads and validates the exact bundle from a fresh control checkout, then six named records bind only the resulting verified full-size paths and exact ID/SHA/renderer/run/attempt/PDF/PNG/artifact identity"
      pattern: "catalog_dispositions"
    - from: priv/quality/rubric_scores.json
      to: priv/quality/SIGN-OFF.md
      via: "the same reviewer-owned scores, rationale, identities, and superseded references are appended without generator authorship"
      pattern: "Phase 136"
    - from: "six qualifying target records"
      to: assets/rendro/catalog.json
      via: "existing exact eligibility predicate, canonical workflow operation, mix rendro.catalog.gen, and mix rendro.catalog.check"
      pattern: "canonical_eligible"
---

<objective>
Collect six genuine exact-evidence visual reviews and conditionally materialize the canonical catalog only when every frozen threshold and provenance predicate qualifies.

Purpose: Close D-21 through D-26 and the remaining CATALOG-11/CATALOG-13 truths without weakening the already-proven six/26, 32/20, or dark-screen-only boundaries.
Output: Six complete named target records plus atomic canonical provenance when qualified, or exact reviewer misses/absence with an unchanged canonical catalog.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
@.planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-02-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-03-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-04-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-06-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-07-SUMMARY.md
@priv/quality/rubric_scores.json
@priv/schemas/rubric_scores.schema.json
@priv/quality/SIGN-OFF.md
@assets/rendro/catalog.json
@test/docs_contract/rubric_manifest_contract_test.exs
@test/docs_contract/catalog_quality_contract_test.exs
@.github/workflows/CATALOG-EVIDENCE.md
</context>

## Artifacts this phase produces

- No new source symbol, schema field, public API, dependency, workflow, recipe, preset, catalog cell, renderer, compliance field, or product UI is created.
- Existing `catalog_dispositions` entries for the six exact targets may receive current reviewer-owned `dimension_scores`, `gate_results.reading_order`, unchanged `gate_results.print_safety`, `justifications`, `recorded_at`, `review_status`, `signed_off_at`, `signed_off_by`, `source_pdf_sha256`, `png_sha256`, `evidence_ref`, `supersedes_evidence_ref`, and exact Phase 136 source/renderer/run/artifact provenance using the schema-supported homes established by existing contracts.
- `priv/quality/SIGN-OFF.md` appends six named exact reviews or the exact absent/incomplete/missed records and bounded next action; prior Phase 130 evidence stays immutable and explicitly superseded only by qualifying current records.
- Eligible output modifies exactly `assets/rendro/catalog.json` plus the six target PNG paths named in frontmatter. Ineligible output modifies none of those seven canonical paths.
- Canonical provenance records the actual canonical run ID/attempt/artifact URL/digest dynamically; this plan contains no future identifiers.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Perform six named reviews on validated full-size evidence</name>
  <reversibility rating="costly">Named source-bound judgment is append/supersede-only; correcting it later requires a new review record with explicit provenance rather than silent editing.</reversibility>
  <precondition>136-07-SUMMARY.md exists and durably records one successful exact review run with a unique run ID/attempt, candidate SHA, default-branch control ref/SHA, Artifact URL, Archive digest, and hash-bound full-size reconciled image identities/paths; Task 1 reconstructs and revalidates all of these inputs rather than inheriting Plan 07 shell state or temporary directories.</precondition>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-07-SUMMARY.md
    - .github/workflows/CATALOG-EVIDENCE.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - priv/quality/SIGN-OFF.md
  </read_first>
  <files>None during the checkpoint; Task 2 transcribes supplied reviewer-owned evidence.</files>
  <action>Begin with no inherited `BUNDLE_ROOT`, `CONTROL_SHA`, worktree path, download path, or other Plan 07 shell variable. Parse `.planning/phases/136-catalog-visual-quality/136-07-SUMMARY.md` as the sole durable handoff and require exactly one recorded value for each of: successful validator result, `operation=review`, candidate SHA, run ID, positive run attempt, default branch, full `refs/heads/...` control ref, control SHA, Artifact URL, Archive digest, expected artifact name, renderer version/executable digest, the twelve ordered final-image identities, and the eight D-21 family-paired full-size image paths. Stop before download or presentation if any field is absent, ambiguous, malformed, marked unavailable/non-reviewable, or inconsistent with candidate `d547bbfa60760d43f19a15372d88a2d159bfa327`; do not substitute nearby prose, the current checkout, a neighboring run, or a Plan 07 temporary path.

Create new unique intake, archive-download, extraction, and worktree parents with `mktemp -d`. Derive the artifact ID from the recorded Artifact URL, query that exact artifact through `gh api`, and require one unexpired artifact whose API URL/download URL, name `rendro-catalog-evidence--review--{candidate_sha}--run-{run_id}--attempt-{run_attempt}`, workflow run ID, and API-reported digest equal the recorded values. Query the exact run/attempt endpoint and require successful `workflow_dispatch` metadata for the recorded run and attempt. Download only that artifact through its API-provided archive URL into the new archive path, compute the archive SHA-256, normalize only the documented `sha256:` prefix, and require exact equality with both the API and 136-07-SUMMARY.md digests before extraction. Require exactly one extracted absolute `BUNDLE_ROOT` containing `manifest.json`; any expired, zero/many, URL/name, run/attempt, or digest mismatch stops the checkpoint.

Independently establish the control plane without trusting bundle contents: require the recorded control ref to equal `refs/heads/${DEFAULT_BRANCH}`, resolve that exact ref with `git ls-remote --exit-code origin`, require one lowercase 40-character SHA exactly equal to the recorded control SHA, fetch that exact ref, create a fresh detached `CONTROL_WORKTREE` at the resolved object, and immediately require `git -C "$CONTROL_WORKTREE" rev-parse HEAD` to equal it. Run `mix deps.get` and `Rendro.CatalogEvidenceBundle.validate/3` only inside `CONTROL_WORKTREE`, passing the new absolute `BUNDLE_ROOT`, literal `review`, and the independently resolved control SHA; assert detached HEAD again immediately before validation. Stop before opening payloads or presenting images on any ref, fetch, checkout, HEAD, dependency, or validator failure.

Only after `validate/3` returns `:ok`, compare the manifest and payloads to the recorded and independently resolved identities: exact operation, candidate/checked-out HEAD, control SHA, renderer pin/version/executable digest, run ID/attempt, closed role order `candidate/catalog.json`, `final-review/final.json`, `multipage-review/multipage.json`, `preset-review/preset.json`, counts 32/12/4/12, safe paths, root manifest/checksum set, and every declared payload/file checksum. Require the validated final payload to contain exactly twelve identities in its locked order and the candidate payload to retain exactly six changed IDs and 26 byte-identical PDF/PNG controls. Resolve the eight recorded D-21 full-size paths only within the new intake/extraction roots, reject symlinks, missing/non-regular files, thumbnails, paths outside those roots, reordered IDs, or any PNG/source-PDF hash that does not match the validated payload identity. Build the human packet exclusively from those freshly verified absolute full-size paths plus candidate/control/renderer/run/attempt/artifact/PDF/PNG identities; never pass through a path merely copied from the summary. If any manifest, checksum, pin, role, count, classification, image path, or identity check fails, record the exact unavailable reason and stop with all six targets unreviewed and unpromoted.

Present only that reconstructed and freshly validated packet in the exact D-21 sequence: Corporate Classic Invoice light control then dark target; Minimal Mono Statement light control then dark target; Swiss Payslip light then dark; Brutalist Ticket light then dark. For each of the six targets, collect independently: all six integer dimension scores, reading_order, unchanged print_safety, concise dimension rationale, reviewer identity, review date, candidate SHA, PDFium version and executable digest, run ID/attempt, source-PDF hash, PNG hash, Artifact URL, Archive digest, and prior/superseded evidence reference per D-22. Do not prefill judgment from Phase 130, generation success, code/tests, thumbnails, or family siblings. Show the frozen qualification rule—content_hierarchy exactly 5, each other dimension at least 4, reading_order true—and state that dark `print_safety:false` remains unchanged and separate from the Phase threshold per D-23. Allow the reviewer to submit actual misses; do not ask them to raise, average, round, clamp, or reinterpret a score to qualify per D-24.</action>
  <what-built>One independently reconstructed review intake whose exact artifact was freshly downloaded and digest-checked, whose bundle was freshly validated from an asserted detached control checkout, and whose human packet contains only verified full-size paths and identities.</what-built>
  <how-to-verify>
    1. Confirm the fresh intake receipt shows the recorded and independently re-resolved candidate, control ref/SHA, detached checkout HEAD, renderer, run/attempt, Artifact URL/digest, manifest/role/count/checksum result, and image hashes all agree before viewing.
    2. Inspect only the eight freshly verified absolute full-size image paths in the locked family-paired order; do not use thumbnails, Plan 07 temporary paths, or summary prose as review authority.
    3. Supply one complete independent record for each of the six exact targets, including actual misses.
    4. Confirm every dark record remains `print_safety:false` and that no accessibility, PDF/UA, viewer, print, or universal-quality claim is made.
  </how-to-verify>
  <resume-signal>Type `six reviews supplied` and provide all required per-target fields, or `review incomplete` with the exact absent/missed target and reason; Task 2 preserves either result truthfully.</resume-signal>
  <acceptance_criteria>
    - Task 1 initializes its own inputs from unique durable 136-07-SUMMARY.md fields, re-downloads the exact artifact into a new directory, independently re-resolves the recorded control ref/SHA, and validates only from a fresh detached control checkout with asserted HEAD.
    - Missing/mismatched summary fields, artifact URL/name/digest, run/attempt, candidate/control SHA, checkout HEAD, manifest/checksums/pin, roles/counts, six/26 classification, or full-size path/hash binding stops before any image presentation.
    - The reviewer saw only freshly verified hash-bound full-size image paths and reviewed them in locked family-paired order.
    - Exactly six independent target records are supplied for a complete review; every field binds to the same Plan 07 evidence chain.
    - Actual scores and reading-order results are accepted verbatim, including misses; no field is generated or inferred.
    - Missing or incomplete human evidence is explicitly identified and cannot authorize canonical materialization.
  </acceptance_criteria>
  <verify>
    <automated>SUMMARY=.planning/phases/136-catalog-visual-quality/136-07-SUMMARY.md; test -s "$SUMMARY" &amp;&amp; test "$CANDIDATE_SHA" = d547bbfa60760d43f19a15372d88a2d159bfa327 &amp;&amp; test "$OPERATION" = review &amp;&amp; test "$RECORDED_CONTROL_REF" = "refs/heads/$DEFAULT_BRANCH" &amp;&amp; test "$(git ls-remote --exit-code origin "$RECORDED_CONTROL_REF" | awk 'NF == 2 {print $1}' | wc -l | tr -d ' ')" = 1 &amp;&amp; test "$(git ls-remote --exit-code origin "$RECORDED_CONTROL_REF" | awk 'NF == 2 {print $1}')" = "$CONTROL_SHA" &amp;&amp; test "$(git -C "$CONTROL_WORKTREE" rev-parse HEAD)" = "$CONTROL_SHA" &amp;&amp; test "$(shasum -a 256 "$ARTIFACT_ARCHIVE" | awk '{print $1}')" = "${ARCHIVE_DIGEST#sha256:}" &amp;&amp; test "$RUN_ID" = "$RECORDED_RUN_ID" &amp;&amp; test "$RUN_ATTEMPT" = "$RECORDED_RUN_ATTEMPT" &amp;&amp; (cd "$CONTROL_WORKTREE" &amp;&amp; test "$(git rev-parse HEAD)" = "$CONTROL_SHA" &amp;&amp; BUNDLE_ROOT="$BUNDLE_ROOT" OPERATION=review CONTROL_SHA="$CONTROL_SHA" mix run -e 'Code.require_file("dev/rendro/catalog_evidence_bundle.ex"); :ok = Rendro.CatalogEvidenceBundle.validate(System.fetch_env!("BUNDLE_ROOT"), :review, System.fetch_env!("CONTROL_SHA"))') &amp;&amp; test "$VERIFIED_FULL_SIZE_COUNT" = 8</automated>
    <human-check>Confirm the displayed paths and identities come from this fresh successful intake, then supply six complete independent records or identify the exact incomplete/missed target without altering any score.</human-check>
  </verify>
  <done>The exact artifact and trusted control environment have been reconstructed and revalidated immediately before review; six named exact-evidence judgments, including any real misses, are ready for transcription, or the precise intake/review deficiency is explicitly identified and grants no publication authority.</done>
</task>

<task type="tracer">
  <name>Task 2: Bind review truth and conditionally publish the canonical catalog</name>
  <reversibility rating="costly">Repository assets are Git-reversible, but reviewer and canonical provenance are append/supersede-only; correction requires a new exact evidence identity and explicit supersedes chain.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-07-SUMMARY.md
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - priv/quality/SIGN-OFF.md
    - assets/rendro/catalog.json
    - test/docs_contract/rubric_manifest_contract_test.exs
    - test/docs_contract/catalog_quality_contract_test.exs
    - test/rendro/catalog_test.exs
    - .github/workflows/catalog-evidence.yml
    - .github/workflows/CATALOG-EVIDENCE.md
  </read_first>
  <files>priv/quality/rubric_scores.json, priv/quality/SIGN-OFF.md, assets/rendro/catalog.json, assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png, assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png, assets/rendro/catalog/payslip/northline-logistics/swiss-light.png, assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png, assets/rendro/catalog/ticket/aurora-live/brutalist-light.png, assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png</files>
  <action>Set `PROVENANCE_WORKSPACE` to the current repository root and `CANDIDATE_SHA=d547bbfa60760d43f19a15372d88a2d159bfa327`. Transcribe Task 1's reviewer-owned values exactly into the existing schema-supported target record fields in `PROVENANCE_WORKSPACE` and append matching source-bound prose to `SIGN-OFF.md`; do not change generator code or historical Phase 130 values in place. Validate exact ID order, six records, same candidate/control/renderer/run/attempt/artifact chain, complete 40/64-character identities, six integer dimensions, reading order, unchanged print_safety, rationale, reviewer/date, and explicit supersedes references per D-22/D-26. Before touching canonical assets, record pre-task hashes for the seven allowlisted canonical paths and run the existing Phase 136 eligibility contract. If any review is absent, incomplete, mismatched, has content_hierarchy other than 5, another dimension below 4, or reading_order false, append the exact absence/miss and bounded target-only next action, retain that target unpromoted, do not dispatch `canonical`, do not run `mix rendro.catalog.gen`, and prove all seven canonical paths byte-identical to their recorded pre-task hashes per D-24/D-25. Only when all six records qualify and candidate classification proves exactly six changed IDs plus 26 byte-identical PDF/PNG controls, dispatch the existing workflow with `operation=canonical` and the same exact candidate SHA; dynamically capture its new run ID/attempt/control SHA/Artifact URL/Archive digest, download the one closed canonical bundle, and validate it from a fresh detached worktree at its independently resolved exact control SHA using the Plan 07 control-checkout procedure. For eligible local materialization, create a separate fresh unique parent with `mktemp -d`, set `CANDIDATE_WORKTREE` to a new child path, run `git worktree add --detach "$CANDIDATE_WORKTREE" "$CANDIDATE_SHA"`, and require `git -C "$CANDIDATE_WORKTREE" rev-parse HEAD` to equal the reviewed full SHA before any dependency or generation command; a missing object, dirty source/config checkout, or HEAD mismatch stops without modifying the seven canonical paths. Copy only the already-validated reviewer-owned `priv/quality/rubric_scores.json` and `priv/quality/SIGN-OFF.md` from `PROVENANCE_WORKSPACE` into the same paths in `CANDIDATE_WORKTREE` as an uncommitted provenance overlay, record their hashes, and prove every other pre-generation tracked difference in that detached worktree is absent. Run `mix deps.get`, reassert `git rev-parse HEAD = CANDIDATE_SHA` immediately before `mix rendro.catalog.gen`, then run `mix rendro.catalog.gen` and `mix rendro.catalog.check` only inside `CANDIDATE_WORKTREE`. Compare the generated ordered target `source_pdf_sha256` and `png_sha256` identities against both the validated review payload and validated canonical payload, and compare the generated 26 control PDF/PNG identities against the pre-task canonical manifest; any ID/order/hash mismatch stops transfer. Transfer from `CANDIDATE_WORKTREE` to `PROVENANCE_WORKSPACE` only `assets/rendro/catalog.json` and the six explicitly named target PNGs, using seven path-specific copies rather than a directory copy; never copy back the overlaid rubric/sign-off files, source, configuration, dependency, temporary, or other generated files, so the review records in `PROVENANCE_WORKSPACE` remain the authoritative transcriptions. In `PROVENANCE_WORKSPACE`, require the canonical-path diff to equal exactly those seven allowlisted paths, rerun `mix rendro.catalog.check`, and verify 32 ordered cells, 20 explicit unscored dispositions, all 26 control source-PDF/PNG hashes unchanged, all dark `print_safety:false`, and no accessibility/PDF-UA/viewer/print/full-pass overclaim per D-01/D-23/D-25. Record the candidate worktree path, asserted HEAD, overlay hashes, generated evidence comparisons, seven transferred paths, and final diff/control hashes in 136-08-SUMMARY.md.</action>
  <acceptance_criteria>
    - Review records are exact transcriptions bound independently to the validated candidate, renderer, run/attempt, PDF/PNG hashes, artifact URL/digest, reviewer/date/rationale, and superseded evidence.
    - Qualification is true only when all six have content_hierarchy 5, every other dimension integer >=4, and reading_order true; dark print_safety remains boolean false and complete `passed` arithmetic remains separate.
    - Any absent/incomplete/missed/mismatched record remains unpromoted, records the exact next action, and leaves all seven canonical paths byte-identical with no canonical dispatch.
    - Eligible canonical execution uses the same exact candidate plus a dynamically captured validated canonical run; no future run ID, attempt, hash, or reviewer identity is hardcoded.
    - Eligible generation and checking run only in a fresh detached candidate worktree whose HEAD is asserted equal to d547bbfa60760d43f19a15372d88a2d159bfa327 immediately before materialization; mismatch or unexpected tracked input stops before canonical transfer.
    - Generated target PDF/PNG identities equal the validated review and canonical evidence, and generated 26-control identities equal the recorded canonical controls before any file is copied back.
    - Exactly seven path-specific canonical outputs are transferred to the provenance workspace; reviewer records and every non-allowlisted worktree file are preserved rather than copied back.
    - Eligible Git diff contains exactly `assets/rendro/catalog.json` and the six target PNGs; the ordered 26 controls retain both source-PDF and PNG hashes exactly.
    - The catalog remains exactly 32 ordered cells and 20 explicit unscored records; every dark record remains screen-oriented `print_safety:false` and no broader claim appears.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs test/rendro/catalog_evidence_bundle_test.exs test/rendro/catalog_review_payload_contract_test.exs test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs test/docs_contract/catalog_evidence_runbook_test.exs --max-failures 1 &amp;&amp; if test "$ELIGIBILITY" = canonical_eligible; then CANDIDATE_SHA=d547bbfa60760d43f19a15372d88a2d159bfa327; test -n "$CANDIDATE_WORKTREE" &amp;&amp; test -n "$REVIEW_BUNDLE_ROOT" &amp;&amp; test -n "$CANONICAL_BUNDLE_ROOT" &amp;&amp; test "$(git -C "$CANDIDATE_WORKTREE" rev-parse HEAD)" = "$CANDIDATE_SHA" &amp;&amp; diff -u &lt;(jq -S '[.cells[] | {id, source_pdf_sha256, png_sha256}]' "$CANDIDATE_WORKTREE/assets/rendro/catalog.json") &lt;(jq -S '[.cells[] | {id, source_pdf_sha256, png_sha256}]' "$REVIEW_BUNDLE_ROOT/candidate/catalog.json") &amp;&amp; diff -u &lt;(jq -S '[.cells[] | {id, source_pdf_sha256, png_sha256}]' "$CANDIDATE_WORKTREE/assets/rendro/catalog.json") &lt;(jq -S '[.cells[] | {id, source_pdf_sha256, png_sha256}]' "$CANONICAL_BUNDLE_ROOT/canonical/catalog.json") &amp;&amp; (cd "$CANDIDATE_WORKTREE" &amp;&amp; mix rendro.catalog.check) &amp;&amp; mix rendro.catalog.check &amp;&amp; test "$(git diff --name-only -- assets/rendro/catalog.json assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png assets/rendro/catalog/payslip/northline-logistics/swiss-light.png assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png assets/rendro/catalog/ticket/aurora-live/brutalist-light.png assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png | sort | wc -l | tr -d ' ')" = 7; else test "$ELIGIBILITY" = canonical_ineligible &amp;&amp; git diff --exit-code -- assets/rendro/catalog.json assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png assets/rendro/catalog/payslip/northline-logistics/swiss-light.png assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png assets/rendro/catalog/ticket/aurora-live/brutalist-light.png assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png; fi</automated>
    <human-check>Compare the six transcribed records to the supplied review forms and confirm either exact eligible publication provenance or exact unpromoted miss/absence with zero canonical diff.</human-check>
  </verify>
  <done>Six genuine reviews are durably bound to exact evidence and the canonical catalog is materialized only if every frozen predicate qualifies; otherwise the actual misses remain unpromoted and canonical bytes remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Freshly re-downloaded and control-validated artifact -> human reviewer | Subjective judgment is permitted only over exact hash-bound full-size evidence whose paths and identities passed the Plan 08 intake. |
| Reviewer input -> durable rubric/sign-off | Human-owned values cross schema, identity, cardinality, and provenance validation. |
| Eligibility -> canonical workflow/writer | Only the all-six exact predicate may authorize external canonical evidence and repository writes. |
| Generated canonical output -> repository | Exactly six target rasters and one manifest may change; 26 controls are immutable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-80 | Spoofing | reviewer/evidence identity | high | mitigate | Fresh exact-artifact download and digest check, independent control-ref resolution, asserted detached validation checkout, verified full-size paths, then named reviewer plus exact candidate/renderer/run/attempt/PDF/PNG/artifact binding per record. |
| T-136-81 | Repudiation | score transcription | high | mitigate | Independent complete records, rationale/date, exact transcription, append/supersede history, and human cross-check. |
| T-136-82 | Elevation of authority | threshold vs complete passed | high | mitigate | Separate tested predicates; dark `print_safety:false` cannot be synthesized into complete pass or compliance. |
| T-136-83 | Tampering | canonical eligibility | high | mitigate | Exact six records, six/26 classification, 32/20 counts, complete identities, and fail-closed mutations. |
| T-136-84 | Tampering | canonical file set | high | mitigate | Pre/post hashes, exact seven-path diff allowlist, catalog check, and 26 PDF/PNG control identity comparison. |
| T-136-85 | Elevation of authority | incomplete or missed review | high | mitigate | Explicit unpromoted/no-dispatch/no-write branch; no averaging, inference, or historical reassociation. |
| T-136-86 | Denial of service | unavailable human feedback | medium | accept | Preserve exact incomplete state and bounded next action; do not convert absence into success. |
| T-136-SC | Tampering | package installs | low | accept | No new package installation or dependency is introduced. |
</threat_model>

## UI consideration resolution

- All 28 explicit UI considerations remain covered by the scalar truths above and the already-executed Plans 136-01 through 136-06; this gap closure reopens only review-bundle and canonical-procedure behavior.
- All eight UI backstops are lifted as flat scalar `[backstop]` truths in this plan.
- The UI-SPEC reports 36 applicable, 36 resolved, and 0 unresolved considerations; this plan introduces no new UI consideration and no browser/product UI.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Gap Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Six named cells meet the frozen rubric through exact reviewable evidence without scope/dark-claim expansion | 07-08 | COVERED | Remote evidence, named review, and conditional canonical provenance are one dependency chain. |
| REQ | CATALOG-10 | Exact six target cells only | 07-08 | COVERED | Six/26 regression guard and exact seven canonical path allowlist. |
| REQ | CATALOG-11 | Current exact-SHA pinned-renderer evidence and six qualifying human reviews; misses truthful | 07-08 | COVERED | New exact run plus six independent records or exact unpromoted misses. |
| REQ | CATALOG-12 | 32/20 boundary and dark print_safety:false | 07-08 | COVERED | Preserved invariant, exact typed/count checks, no claim expansion. |
| REQ | CATALOG-13 | Source/renderer/artifact/review/canonical provenance | 07-08 | COVERED | Dynamic ref/run/attempt/artifact identities through conditional canonical publication. |
| RESEARCH | Evidence authority | Candidate never authors judgment; validation precedes review | 07-08 | COVERED | No evidence-system redesign or new integration. |
| RESEARCH | Exact identity and scope | Full SHA/digests, exact ordering, six targets/26 controls | 07-08 | COVERED | Prefix/stale/neighbor/partial evidence rejected. |
| CONTEXT | D-01 | Six exact changes and 26 byte-identical controls | 07-08 | COVERED | Regression gate before review and publication. |
| CONTEXT | D-02 | Catalog identity remains dev-only and recipes receive only generic profile data | 01 preserved | COVERED | Already verified; regression guard only. |
| CONTEXT | D-03 | Existing build/compose/measure/paginate/render/validate engine remains the sole path | 01-04 preserved | COVERED | No renderer or layout fork is introduced. |
| CONTEXT | D-04 | No public API, runtime dependency, browser, Phoenix, Oban, or database scope | 01-08 | COVERED | Gap plans create evidence only. |
| CONTEXT | D-05 | Selectable text, caller data, default bytes, and deterministic measurement remain preserved | 01-04 preserved | COVERED | Existing deterministic guards remain required. |
| CONTEXT | D-06 | Invoice/Statement contrast remains target-scoped, never a global dark retune | 02 preserved | COVERED | No rendering changes in gap closure. |
| CONTEXT | D-07 | Primary/secondary semantic ink assignments remain exact | 02 preserved | COVERED | Reviewed as rendered; not altered. |
| CONTEXT | D-08 | Total Due and Closing Balance remain sole focal anchors | 02 preserved | COVERED | Frozen review predicate. |
| CONTEXT | D-09 | Geometry/data/fonts/pagination/accent and dark claim boundary remain preserved | 02, 08 | COVERED | Deterministic guards plus no-overclaim check. |
| CONTEXT | D-10 | Swiss target remains two sequential full-width ledgers | 03 preserved | COVERED | Frozen review input. |
| CONTEXT | D-11 | Payslip caller descriptions remain verbatim | 03 preserved | COVERED | Backstop truth retained. |
| CONTEXT | D-12 | Flexible description and measured atomic money columns remain exact | 03 preserved | COVERED | Backstop truth retained. |
| CONTEXT | D-13 | Payslip light/dark geometry remains identical with semantic dark roles | 03 preserved | COVERED | Reviewed without changing print boundary. |
| CONTEXT | D-14 | Net Pay dominance and final reconciliation adjacency remain preserved | 03 preserved | COVERED | Frozen review predicate. |
| CONTEXT | D-15 | Native measured pagination, Unicode fallback, determinism, and failure semantics remain preserved | 03 preserved | COVERED | Existing tests remain required. |
| CONTEXT | D-16 | Ticket retains source-order Section GA, Row H, Seat 24, Gate B | 04 preserved | COVERED | Frozen review input. |
| CONTEXT | D-17 | Ticket retains one atomic equal-share locator row and dominant placement hierarchy | 04 preserved | COVERED | Backstop truth retained. |
| CONTEXT | D-18 | Gate/fixture/order remain unchanged; no placeholder or two-row capability | 04 preserved | COVERED | No rendering changes in gap closure. |
| CONTEXT | D-19 | Ticket light/dark geometry and dark screen-only semantics remain preserved | 04, 08 | COVERED | Frozen review and claim predicates. |
| CONTEXT | D-20 | One validated immutable review bundle | 07 | COVERED | Remote publication, new dispatch, download, validate. |
| CONTEXT | D-21 | Full-size locked family-paired review order | 07-08 | COVERED | Reviewability checked, then human checkpoint. |
| CONTEXT | D-22 | Independent complete reviewer-owned records | 08 | COVERED | Exact transcription only. |
| CONTEXT | D-23 | Phase threshold differs from passed; dark false retained | 08 | COVERED | Separate predicates and no overclaim. |
| CONTEXT | D-24 | Actual misses retained and bounded | 08 | COVERED | Unpromoted/no-write branch. |
| CONTEXT | D-25 | Atomic canonical publication after all gates | 08 | COVERED | Dynamic canonical run then exact seven-file materialization. |
| CONTEXT | D-26 | Human review advisory; objective checks reproducible | 07-08 | COVERED | Human judgment never invented; deterministic identity/scope/publication gates remain blocking. |
| UI-SPEC | Covered/backstop considerations | Preserve 28 explicit and eight backstop states | 08 | COVERED | Flat scalar backstops; 0 unresolved. |

No GOAL, REQ, RESEARCH, CONTEXT, or UI-SPEC source item is missing. Deferred/out-of-scope catalog expansion, new recipes/presets/APIs, global dark/print/accessibility/viewer claims, Phoenix/Oban work, dependencies, and reviewer-product UI remain excluded.

<verification>
- Six supplied review records are checked against the exact validated bundle and frozen rubric without generated judgment.
- `mix test test/rendro/catalog_test.exs test/rendro/catalog_evidence_bundle_test.exs test/rendro/catalog_review_payload_contract_test.exs test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs test/docs_contract/catalog_evidence_runbook_test.exs --max-failures 1`
- Eligible path: validated canonical run, `mix rendro.catalog.gen`, `mix rendro.catalog.check`, exact seven-path diff, and 26 PDF/PNG control hashes.
- Ineligible path: exact miss/absence in sign-off and zero diff for all seven canonical paths.
- `mix ci.fast`, `mix quality.governance`, and `mix quality.uat 136 --check`.
</verification>

<success_criteria>
- Six independent human records are exact-evidence-bound; actual scores and misses are preserved without synthesis.
- All-six eligibility is distinct from complete rubric passed arithmetic and retains every dark `print_safety:false` boundary.
- Canonical publication occurs only from one qualifying evidence chain and changes exactly the six targets plus the ordered manifest; 26 controls remain byte-identical.
- Any unavailable, incomplete, invalid, or sub-threshold state stays explicitly unpromoted and leaves canonical artifacts unchanged.
- No catalog, recipe, public API, dependency, dark-mode, print, accessibility, viewer, Phoenix, Oban, or external API scope is added.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-08-SUMMARY.md` when done.
</output>
