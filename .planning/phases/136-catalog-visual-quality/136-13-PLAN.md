---
phase: 136-catalog-visual-quality
plan: "13"
type: execute
wave: 11
depends_on: ["136-12"]
files_modified:
  - .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
  - priv/quality/rubric_scores.json
  - priv/quality/SIGN-OFF.md
  - assets/rendro/catalog.json
  - assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png
  - assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png
  - assets/rendro/catalog/payslip/northline-logistics/swiss-light.png
  - assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png
  - assets/rendro/catalog/ticket/aurora-live/brutalist-light.png
  - assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png
autonomous: false
gap_closure: true
requirements: [CATALOG-10, CATALOG-11, CATALOG-12, CATALOG-13]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-21, a named human sees only freshly redownloaded and revalidated hash-bound full-size images in locked Invoice light control/dark target, Statement light control/dark target, Payslip light/dark, Ticket light/dark order."
    - "Task 1 validates the complete Plan 12 receipt, both newly downloaded artifact digests, detached-control closed bundle, distinct reviewer packet, and all exact eight role/path/PDF/PNG bindings before presenting an image."
    - "Per D-22, each of the six targets receives an independent verbatim record with six integer scores, reading_order, unchanged print_safety, rationale, reviewer/date, exact candidate/control/renderer/run/attempt/PDF/PNG/evidence-artifact/packet-artifact provenance, and superseded evidence reference."
    - "Per D-23, a target qualifies only when content_hierarchy equals 5, every other scored dimension is at least 4, and reading_order is true; dark print_safety remains false and outside that visual threshold."
    - "Per D-24/D-26, unavailable, incomplete, or sub-threshold evidence is retained truthfully and remains unpromoted; it enters the bounded Revision Gate and cannot satisfy task, plan, requirement, or phase completion."
    - "Per D-25, canonical publication occurs only after one machine review receipt proves all six independent records qualify and deterministic exact-six/26 plus full provenance revalidation still pass."
    - "An eligible canonical result is downloaded by exact run/attempt/artifact digest, semantically validated from a trusted detached checkout, and installed through the proven transaction; any failure restores the previous complete generation."
    - "The catalog remains exactly 32 ordered cells and 20 explicitly unscored non-target records, with no scope, public API, dark-mode, print, accessibility, or universal-quality expansion."
  artifacts:
    - path: .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
      provides: "Machine-readable six-review qualification/provenance record and Revision-Gate iteration chain"
      contains: "review_status"
    - path: priv/quality/rubric_scores.json
      provides: "Six verbatim current reviewer-owned records; misses/incomplete states remain nonqualifying"
      contains: "catalog_dispositions"
    - path: priv/quality/SIGN-OFF.md
      provides: "Named Phase 136 review provenance, outcomes, supersession, Revision-Gate, and publication receipt"
      contains: "Phase 136"
    - path: assets/rendro/catalog.json
      provides: "Exact 32-cell canonical manifest only after all eligibility predicates"
      contains: "source_commit_sha"
    - path: assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png
      provides: "Conditionally promoted Invoice target raster"
    - path: assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png
      provides: "Conditionally promoted Statement target raster"
    - path: assets/rendro/catalog/payslip/northline-logistics/swiss-light.png
      provides: "Conditionally promoted Payslip light target raster"
    - path: assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png
      provides: "Conditionally promoted Payslip dark target raster"
    - path: assets/rendro/catalog/ticket/aurora-live/brutalist-light.png
      provides: "Conditionally promoted Ticket light target raster"
    - path: assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png
      provides: "Conditionally promoted Ticket dark target raster"
  key_links:
    - from: .planning/phases/136-catalog-visual-quality/136-12-RECEIPT.json
      to: fresh reviewer intake
      via: "new API downloads of both exact artifacts reproduce provider/archive digests, then bundle and exact eight packet paths revalidate"
      pattern: "evidence_artifact|reviewer_packet_artifact|review_images"
    - from: six human review records
      to: .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
      via: "verbatim independent transcription plus frozen threshold computation and full dual-artifact provenance"
      pattern: "dimension_scores|reading_order|reviewer|artifact"
    - from: .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
      to: assets/rendro/catalog.json
      via: "only review_status qualified with six qualifying records and exact deterministic provenance gates canonical operation"
      pattern: "qualified|source_commit_sha"
  prohibitions:
    - statement: "No score, rationale, reviewer, approval, or passed value may be generated, inferred from tests/workflow success, copied from a sibling, or reassociated from stale evidence."
      status: unverified
      flagged: true
    - statement: "No unavailable, miss, incomplete review, provenance mismatch, or packet defect may be hidden, rounded into qualification, or treated as gap-chain completion."
      status: unverified
      flagged: true
    - statement: "No canonical update may partially replace assets, overwrite unrelated controls, erase the previous good generation, or broaden dark-mode/print/accessibility claims."
      status: unverified
      flagged: true
---

<objective>
Collect six independent named reviews from a freshly revalidated eight-image packet and publish canonically only if every frozen visual and deterministic predicate qualifies.

Purpose: Close CATALOG-11/CATALOG-13 without turning human judgment into automation; retain negative evidence truthfully while preventing it from completing the gap chain.
Output: A machine-valid six-review receipt with exact dual-artifact provenance and one atomic canonical generation only on full qualification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
@.planning/phases/136-catalog-visual-quality/136-12-RECEIPT.json
@.github/workflows/CATALOG-EVIDENCE.md
@dev/rendro/catalog_evidence_bundle.ex
@dev/rendro/catalog_visual_gallery.ex
@dev/mix/tasks/rendro/catalog/gallery.ex
@priv/quality/rubric_scores.json
@priv/schemas/rubric_scores.schema.json
@priv/quality/SIGN-OFF.md
</context>

## Artifacts this phase produces

- `136-13-REVIEW-RECEIPT.json` records exact intake nonce/download/digest facts, six verbatim review records, mechanical per-target qualification, full evidence/packet provenance, and Revision-Gate iteration/cap.
- `priv/quality/rubric_scores.json` gains exactly six reviewer-owned current records only when supplied; actual misses/incomplete states remain explicit and nonqualifying.
- `priv/quality/SIGN-OFF.md` gains one append/supersede Phase 136 receipt binding review, gate iteration, and publication or rework outcome to exact identities.
- On full eligibility only, `assets/rendro/catalog.json` and exactly the six target PNG paths listed in frontmatter are replaced atomically from the validated canonical artifact; the other 26 assets remain byte-identical.
- On any unavailable, missed, incomplete, or failed path, none of the seven canonical paths changes and the plan returns a Revision-Gate checkpoint instead of completion.
- No new public API, dependency, database/schema ORM, external API integration, catalog cell, preset, visual score generator, or dark/print/accessibility claim is created.

<tasks>

<task type="tracer">
  <name>Task 1: Freshly redownload and validate both artifacts into the exact eight-image intake</name>
  <precondition>`136-12-RECEIPT.json` exists, passes the complete receipt validator, and has `status: complete`; an unavailable receipt remains in Plan 12's Revision Gate.</precondition>
  <reversibility rating="reversible">The intake uses nonce-bearing temporary directories and performs no tracked or remote mutation.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-12-RECEIPT.json
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - .github/workflows/CATALOG-EVIDENCE.md
    - dev/rendro/catalog_evidence_bundle.ex
    - dev/rendro/catalog_visual_gallery.ex
    - dev/mix/tasks/rendro/catalog/gallery.ex
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
  </read_first>
  <files>None; this task creates `tmp/phase136-review-intake.json` plus nonce-bearing temporary archives/extractions.</files>
  <action>Parse the complete machine receipt rather than summary prose and require one candidate SHA, control ref/SHA, run ID/attempt, renderer identity, unique closed-evidence artifact, unique reviewer-packet artifact, exact authoritative roles, and exact eight review-image records. Re-resolve the control ref and re-query the exact run attempt plus both artifact IDs; reject changed, expired, duplicate, ambiguous, or cross-attempt metadata. Download both API URLs again into a new nonce-bearing temporary root distinct from Plan 12, record response artifact IDs/provider digests, compute both archive SHA-256 values, and require equality with the receipt before extraction. Create a new detached worktree at the exact control SHA and assert HEAD. Validate the closed bundle first; then invoke the committed gallery task validation mode against the freshly extracted packet and validated bundle. Require the exact eight review roles/paths in D-21 order, both Invoice-light and Statement-light controls, six targets, unique safe paths, authority-none packet state, and exact PNG/source-PDF hash bindings. Write `tmp/phase136-review-intake.json` with receipt ID, a fresh download nonce/timestamp, both local roots/archive hashes, detached control checkout, validation results, and the eight exact bound paths. Trace Invoice dark end-to-end, then create the six-row blank worksheet with only immutable provenance/target IDs prefilled. Leave scores, reading_order, rationale, reviewer, and date empty. Do not open an image before both validators return `:ok`.</action>
  <acceptance_criteria>
    - Plan 13 performs two new API downloads and independently reproduces both artifact IDs/provider/archive digests from the complete Plan 12 receipt.
    - Fresh detached-control validation returns `:ok` for the closed bundle before packet validation starts.
    - Exactly eight freshly extracted, hash-bound full-size paths include the two named controls and six targets in D-21 order.
    - The Invoice-dark trace binds candidate/control/renderer/run/attempt/evidence-artifact/packet-artifact/PDF/PNG identities exactly.
    - No human-owned field is prefilled or inferred, and no image is presented after any mismatch.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.status == "complete" and (.artifacts.evidence.id != .artifacts.reviewer_packet.id) and ([.review_images[].review_role] == ["invoice_light_control","invoice_dark_target","statement_light_control","statement_dark_target","payslip_light_target","payslip_dark_target","ticket_light_target","ticket_dark_target"])' .planning/phases/136-catalog-visual-quality/136-12-RECEIPT.json &amp;&amp; jq -e '.fresh_download == true and (.download_nonce | length &gt; 0) and (.evidence.archive_sha256 | test("^[0-9a-f]{64}$")) and (.reviewer_packet.archive_sha256 | test("^[0-9a-f]{64}$")) and (.validation.bundle_result == "ok") and (.validation.packet_result == "ok") and ([.review_images[].path] | length == 8 and length == (unique | length))' tmp/phase136-review-intake.json &amp;&amp; mix rendro.catalog.gallery --validate-intake tmp/phase136-review-intake.json</automated>
  </verify>
  <done>A second fresh download and detached-control validation prove the exact eight bound paths are ready for genuine human judgment.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Supply six independent reviews and authorize only their truthful classification</name>
  <reversibility rating="one-way">Named review evidence is append/supersede-only, and selecting qualified publication authorizes provider history plus canonical replacement in Task 3.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-12-RECEIPT.json
    - tmp/phase136-review-intake.json
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - priv/quality/SIGN-OFF.md
  </read_first>
  <files>None during the checkpoint; Task 3 serializes the supplied records.</files>
  <action>Present only Task 1's freshly revalidated full-size packet in D-21 family-paired order. For each exact target independently collect all six integer dimensions, reading_order, unchanged print_safety, concise dimension rationale, reviewer identity, review date, and confirmation of immutable candidate/control/renderer/run/attempt/PDF/PNG/evidence-artifact/packet-artifact provenance plus superseded evidence. Accept actual misses verbatim. Compute eligibility mechanically without altering input: content_hierarchy equals 5, every other dimension is at least 4, and reading_order is true; dark print_safety remains false and is not averaged into the threshold. Show all six qualified/missed/incomplete classifications. Request authorization for exactly one truthful next state: six qualified records plus conditional canonical operation, actual misses followed by target-only Revision Gate, or incomplete/unavailable review followed by evidence/review Revision Gate. Do not suggest raising a score or treating workflow/tests as visual evidence.</action>
  <decision>Whether the supplied verbatim six-record set qualifies for conditional canonical publication or must enter the bounded Revision Gate.</decision>
  <context>Human feedback is advisory per D-26, but the objective CATALOG-11 threshold remains open until six complete records qualify; negative feedback is recorded without becoming a successful gap-chain outcome.</context>
  <options>
    <option id="six-qualified-authorize-canonical">
      <name>Authorize qualifying set</name>
      <pros>Allows exact transcription and canonical publication after deterministic revalidation.</pros>
      <cons>Creates durable named review/provider history and replaces canonical target artifacts if every gate still passes.</cons>
    </option>
    <option id="record-misses-revise">
      <name>Record misses and revise</name>
      <pros>Preserves honest evidence and reworks only the target profiles that actually missed.</pros>
      <cons>Leaves CATALOG-11/CATALOG-13 open until a new candidate and review qualify.</cons>
    </option>
    <option id="review-incomplete-retry">
      <name>Record incomplete review</name>
      <pros>Invents no absent judgment and leaves canonical assets unchanged.</pros>
      <cons>Requires another fresh intake/review attempt and does not complete this plan.</cons>
    </option>
  </options>
  <resume-signal>Provide six records and select the exact matching option. Negative options continue only into the Revision Gate; they do not complete Plan 13.</resume-signal>
  <acceptance_criteria>
    - Each supplied record is independent, complete when classified complete, and provenance-identical to Task 1.
    - Qualification is a direct frozen-predicate calculation; no score is averaged, rounded, clamped, inferred, or rewritten.
    - Miss/incomplete choices explicitly forbid canonical mutation and identify the exact Revision-Gate target/evidence set.
    - Qualified publication has explicit one-way authorization immediately before Task 3.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The supplied records are classified truthfully; only a six-qualified authorization advances to completion, while negative classifications advance to the nonterminal Revision Gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Serialize exact reviews and publish only one fully qualified canonical generation</name>
  <precondition>Task 2 supplies a selected outcome and every field required for that outcome; absent or ambiguous input stops without mutation.</precondition>
  <reversibility rating="costly">Review records require explicit supersession and canonical assets require the tested rollback transaction, while Task 2 separately authorizes any one-way remote run.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-12-RECEIPT.json
    - tmp/phase136-review-intake.json
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .github/workflows/CATALOG-EVIDENCE.md
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - priv/quality/SIGN-OFF.md
    - dev/rendro/catalog.ex
    - dev/rendro/catalog_evidence_bundle.ex
  </read_first>
  <files>.planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json, priv/quality/rubric_scores.json, priv/quality/SIGN-OFF.md, assets/rendro/catalog.json, assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png, assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png, assets/rendro/catalog/payslip/northline-logistics/swiss-light.png, assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png, assets/rendro/catalog/ticket/aurora-live/brutalist-light.png, assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png</files>
  <behavior>
    - "Verbatim supplied review fields round-trip through the machine receipt, closed rubric schema, and sign-off without identity reassociation."
    - "Unavailable, missed, or incomplete input changes no canonical path, records exact nonqualification, and enters the Revision Gate instead of passing this task."
    - "All-six qualifying input triggers one exact canonical operation; any later mismatch/failure restores all prior canonical bytes."
  </behavior>
  <action>Serialize the supplied records into `136-13-REVIEW-RECEIPT.json` before touching canonical output. Include schema version, unique review receipt ID, parent Plan 12 receipt ID, intake download nonce, candidate/control/run/attempt/renderer, distinct evidence and packet artifact identities/digests, exact eight packet roles/paths/hashes, exactly six target records, per-record qualification, aggregate `review_status`, and `revision_gate` iteration/cap/prior receipt IDs. Validate exact target set, integer ranges, frozen predicate arithmetic, provenance equality, dark print-safety false, and schema shape. Update only the six target dispositions and append/supersede one Phase 136 sign-off section with verbatim reviewer content and exact identities; never silently edit prior evidence.

For missed/incomplete/unavailable selection, set `review_status` accordingly, list exact failed/missing targets and fields, preserve before/after hashes proving all seven canonical paths unchanged, and record the matching Revision-Gate next action. Commit the truthful reviewer evidence if supplied, then return `CHECKPOINT REACHED`; do not create `136-13-SUMMARY.md`, do not pass the completion verify, and do not mark CATALOG-11/CATALOG-13 or the phase complete.

For `six-qualified-authorize-canonical` only, require `review_status: qualified`, exactly six complete records, six qualifying predicates, complete Plan 12 provenance, and Task 1's fresh validation. Re-run exact candidate scope and bundle eligibility; dispatch canonical at the same candidate/control identities, capture actual run/attempt/artifact API identity and digest, download uniquely, validate from a fresh detached trusted checkout, and require exact 32 cells, renderer/source identity, six target changes, and 26 controls. Install through Plan 14's transaction, run `mix rendro.catalog.check`, schema/docs contracts, and a complete before/after hash diff proving exactly the manifest plus six target PNGs changed. If any predicate, run, artifact, validation, install, or post-check fails, execute rollback, set a nonqualified Revision-Gate outcome, and stop without completion.</action>
  <acceptance_criteria>
    - Machine receipt, rubric, and sign-off exactly match supplied human records and full dual-artifact provenance; no generated or inferred judgment appears.
    - Negative outcomes preserve all seven canonical hashes, record exact failed/missing targets, and stop at the Revision Gate without a completion summary.
    - Qualified outcome has exactly six threshold-passing records, same complete evidence chain, one successful canonical artifact, and trusted semantic validation.
    - Successful publication changes exactly seven listed canonical paths, retains 32 ordered cells/20 unscored records, and passes `mix rendro.catalog.check`.
    - Any canonical failure restores the complete old generation and cannot pass the final verification.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.schema_version == 1 and .review_status == "qualified" and (.reviews | length == 6) and ([.reviews[].catalog_id] | length == (unique | length)) and (all(.reviews[]; .qualification == "qualified" and .dimension_scores.content_hierarchy == 5 and (all(.dimension_scores | to_entries[]; if .key == "content_hierarchy" then true else .value &gt;= 4 end)) and .reading_order == true)) and (.provenance.evidence_artifact.id != .provenance.reviewer_packet_artifact.id) and ([.review_images[].review_role] | length == 8 and length == (unique | length))' .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json &amp;&amp; mix test test/docs_contract/rubric_manifest_contract_test.exs test/rendro/catalog_test.exs test/rendro/catalog_evidence_bundle_test.exs --max-failures 1 &amp;&amp; mix rendro.catalog.check</automated>
  </verify>
  <done>Six qualifying reviews with complete deterministic provenance are durably recorded and one atomic canonical generation passes; no other outcome completes the gap chain.</done>
</task>

</tasks>

## Bounded Revision Gate

The gate applies to unavailable, incomplete, threshold-miss, provenance-mismatch, packet-mismatch, and canonical-failure outcomes. It is not a successful branch.

1. Start at iteration 1 and cap at 3. Each attempt preserves its Plan 12/13 receipt IDs, exact candidate/control/run/attempt/artifacts, intake nonce, review truth, failed stage, target set, and canonical before/after hashes.
2. For threshold misses, map only failed target IDs to their Plan 09/10 private presentation profiles and directly owned tests. Rework no sibling/unaffected target; rerender all 32 and require the unaffected five targets plus 26 controls to remain byte-stable relative to the failed candidate except where the exact failed targets changed.
3. For deterministic/provenance defects, change only the implicated dev/workflow/runbook contract. For unavailable or incomplete human feedback, do not invent a visual defect or edit visual bytes; retain the phase truth as open and request a fresh review intake.
4. Every scoped correction becomes a new immutable candidate SHA, runs the complete deterministic Plans 09-11/14 validation, then creates a fresh Plan 12 run with two fresh artifacts and a fresh Plan 13 intake/review. A transient provider/reviewer retry uses a new exact run/attempt and remains tied to the same immutable content until an actual scoped correction justifies a new SHA.
5. Increment the iteration only after a complete attempt outcome is recorded. After iteration 3 fails, set `revision_gate.status: escalated`, preserve all receipts and reviewer records, return `CHECKPOINT REACHED` with the exact unresolved targets/stage and required next authority, leave canonical output unchanged/restored, and keep CATALOG-11/CATALOG-13 plus the phase goal open.

Optional human comments can enrich a receipt but never alter deterministic pass/fail. Conversely, missing optional feedback does not erase deterministic completion, yet the objective six-review requirement remains truthfully unachieved and the gap chain remains incomplete.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider artifacts -> fresh intake | A prior receipt is insufficient; both archives must be downloaded and validated again. |
| Validated images -> named human | Trusted bytes cross into non-automatable aesthetic judgment. |
| Human records -> rubric/sign-off | Reviewer-owned fields become durable contractual evidence. |
| Qualified evidence -> canonical workflow/assets | Advisory qualification and deterministic gates jointly authorize durable publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-D0 | Spoofing | reviewer/evidence identity | high | mitigate | Each record binds named reviewer/date and exact candidate/control/renderer/run/two-artifact/PDF/PNG identities. |
| T-136-D1 | Tampering | fresh intake | high | mitigate | Requery/redownload both artifact IDs, compare both digests, then validate bundle and exact eight packet paths. |
| T-136-D2 | Tampering | scores/qualification | high | mitigate | Verbatim inputs plus frozen integer predicate are machine-recorded and schema-validated before publication. |
| T-136-D3 | Repudiation | superseded review | high | mitigate | Append/supersede sign-off and receipt chain preserve prior evidence and gate iterations. |
| T-136-D4 | Information disclosure / misrepresentation | dark/print claims | high | mitigate | Dark print_safety remains false and no accessibility/PDF-UA/viewer claim enters qualification. |
| T-136-D5 | Denial of service | incomplete/missed review | medium | mitigate | Bounded three-attempt Revision Gate preserves truth, prevents false completion, and escalates with exact state. |
| T-136-D6 | Elevation of privilege | canonical authorization | high | mitigate | Six human predicates plus exact deterministic provenance and explicit authorization must all pass. |
| T-136-D7 | Tampering | canonical files | high | mitigate | Exact seven-path diff, closed bundle validation, transaction rollback, and post-install catalog check. |
| T-136-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs. |
</threat_model>

## Spec-less probe resolution

- CATALOG-10 exact boundary/precision stays a deterministic precondition and post-publication seven-path diff.
- CATALOG-11 behavior remains automated by Plan 10; its visual threshold completes only with six machine-recorded qualifying reviews.
- CATALOG-12's human-owned probe is resolved only by exact reviews; absent/missed judgment remains an open gap, never an automated pass.
- CATALOG-13 boundary/adjacency/empty/order/precision is enforced by complete receipt validation, two fresh downloads, exact eight paths, exact six records, one evidence chain, and transactional publication.
- Prohibition recall retains anti-fabrication, anti-concealment, stale-intake, and anti-partial-publication items above; identity/tampering concerns route to STRIDE.

## Multi-source coverage audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Six current visual gaps meet the frozen rubric through exact evidence without catalog/dark-claim expansion | Plans 09-14: real exact scope, held-out behavior, semantic evidence, exact packet, fresh dual-artifact run, independent review, conditional publication, bounded revision |
| REQ | CATALOG-10 | Plans 09, 11, 12, 13, 14: exact 32/six/26 identity and canonical diff |
| REQ | CATALOG-11 | Plans 10-14: deterministic recipe boundaries, exact eight-image review intake, and six qualifying records |
| REQ | CATALOG-12 | Plans 09-14: explicit dark boundary plus human-only six-cell judgment |
| REQ | CATALOG-13 | Plans 11-14: semantic provenance, dual-artifact receipts, fresh validation, and transactional publication |
| RESEARCH | Frozen stack, one pipeline, semantic profile, no new dependency/API, pinned renderer | Plans 09-14 actions/prohibitions and artifacts |
| RESEARCH | Exact bundle identity, truthful baseline, rollback, fresh review | Plans 11-14 |
| CONTEXT | D-01 through D-05 | Plan 09 exact scope/default/pipeline; Plans 11-14 evidence/publish guards |
| CONTEXT | D-06 through D-09 | Plan 10 Invoice/Statement held-out semantic behavior |
| CONTEXT | D-10 through D-15 | Plan 10 Payslip optional/measured/sequential behavior |
| CONTEXT | D-16 through D-19 | Plans 09-10 Ticket real target treatment and atomic behavior |
| CONTEXT | D-20 through D-26 | Plans 11-14 validation, exact packet, dual-artifact receipt, review, threshold, bounded revision, and publication |

Deferred ideas and other-phase items are excluded. No GOAL, REQ, RESEARCH, or non-deferred CONTEXT item is unplanned.

<verification>
- Require Plan 12's complete machine receipt before starting.
- Redownload and validate both exact artifacts, then verify all eight packet roles/paths/hashes before presenting images.
- Require `136-13-REVIEW-RECEIPT.json` to have `review_status: qualified`, exactly six qualifying records, and full dual-artifact provenance.
- Run `mix test test/docs_contract/rubric_manifest_contract_test.exs test/rendro/catalog_test.exs test/rendro/catalog_evidence_bundle_test.exs --max-failures 1` and `mix rendro.catalog.check`.
- Any negative variant may be recorded but must stop at the bounded Revision Gate and cannot produce `136-13-SUMMARY.md`.
</verification>

<success_criteria>
- Fresh intake independently proves two exact artifact downloads and all eight D-21 image bindings.
- Six independent named records preserve actual visual outcomes and complete provenance.
- Only six qualifying records plus every deterministic gate produce one atomic canonical generation and plan completion.
- Any unavailable, miss, incomplete, mismatch, or failure remains unpromoted, enters the bounded gate, and leaves the phase goal open.
- Catalog scope, dark print-safety truth, public API, and dependency boundaries remain frozen.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-13-SUMMARY.md` only after the review receipt is `qualified`, all six records pass, fresh dual-artifact provenance validates, and canonical checks succeed. Preserve negative receipts/sign-off records without creating a completion summary.
</output>
