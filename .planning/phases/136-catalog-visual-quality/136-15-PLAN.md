---
phase: 136-catalog-visual-quality
plan: "15"
type: execute
wave: 13
depends_on: ["136-13"]
files_modified:
  - .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json
  - assets/rendro/catalog.json
  - assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png
  - assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png
  - assets/rendro/catalog/payslip/northline-logistics/swiss-light.png
  - assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png
  - assets/rendro/catalog/ticket/aurora-live/brutalist-light.png
  - assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png
autonomous: false
gap_closure: true
requirements: [CATALOG-10, CATALOG-11, CATALOG-12, CATALOG-13]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-25, canonical publication starts only from Plan 13's separately validated `qualified` receipt with six D-23 passes, empty failed/missing sets, fresh parent receipt/intake/artifact/path validation, and an explicit Plan 15 one-way authorization."
    - "The canonical provider operation uses the exact authorized candidate/control identities and records one actual run/attempt/artifact ID/name/API URL/provider digest/archive SHA-256; no nearby, stale, or ambiguous result is accepted."
    - "The downloaded canonical artifact is semantically validated from a fresh detached trusted-control checkout as exactly 32 ordered cells with truthful source/renderer identity, exact six target changes, and 26 controls before installation."
    - "The Plan 14 transaction installs the canonical manifest and six target PNGs as one generation; any dispatch, download, validation, install, or post-check failure restores/preserves all prior canonical bytes and records an unavailable publication receipt."
    - "Successful publication changes exactly the manifest plus six locked target PNGs, retains the other 26 assets byte-identically, preserves 32 cells/20 unscored entries and dark `print_safety:false`, and passes the catalog/schema/docs contracts."
  artifacts:
    - path: .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json
      provides: "Exact qualified-input, canonical artifact, validation, installation, and rollback provenance"
      contains: "publication_status"
    - path: assets/rendro/catalog.json
      provides: "Qualified exact 32-cell canonical manifest"
      contains: "source_commit_sha"
    - path: assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png
      provides: "Atomically promoted Invoice dark target"
    - path: assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png
      provides: "Atomically promoted Statement dark target"
    - path: assets/rendro/catalog/payslip/northline-logistics/swiss-light.png
      provides: "Atomically promoted Payslip light target"
    - path: assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png
      provides: "Atomically promoted Payslip dark target"
    - path: assets/rendro/catalog/ticket/aurora-live/brutalist-light.png
      provides: "Atomically promoted Ticket light target"
    - path: assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png
      provides: "Atomically promoted Ticket dark target"
  key_links:
    - from: .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
      to: canonical workflow dispatch
      via: "the qualified-only validator reruns parent complete receipt, fresh intake/artifact/path, and six-predicate gates before Plan 15 requests separate publication authorization"
      pattern: "qualified|require-qualified-review-receipt"
    - from: canonical provider artifact
      to: .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json
      via: "exact run/attempt/artifact/provider/archive identities and detached-control semantic validation are recorded before install"
      pattern: "artifact|archive_sha256|validation"
    - from: .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json
      to: assets/rendro/catalog.json
      via: "only publication_status complete plus a validated staging generation may enter Plan 14's rollback-safe transaction"
      pattern: "complete|installed_paths|rollback"
  prohibitions:
    - statement: "No missed, incomplete, unavailable, stale, or provenance-mismatched review receipt may dispatch or install canonical output."
      status: unverified
      flagged: true
    - statement: "No partial generation, changed control, failed post-check, or ambiguous provider artifact may remain installed."
      status: unverified
      flagged: true
    - statement: "No publication may broaden catalog membership, dark print/accessibility claims, public API, or dependency scope."
      status: unverified
      flagged: true
---

<objective>
Publish the qualified Phase 136 candidate as one rollback-safe canonical generation, independently of review receipt/rubric recording.

Purpose: Complete D-25's atomic publication boundary only after Plan 13 has produced the closed qualified review input and Plan 15 has separately obtained explicit publication authorization.
Output: One exact canonical publication receipt and exactly seven qualified canonical path updates, or a truthful unavailable receipt with the previous generation intact.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
@.planning/phases/136-catalog-visual-quality/136-13-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-14-SUMMARY.md
@.github/workflows/catalog-evidence.yml
@.github/workflows/CATALOG-EVIDENCE.md
@dev/rendro/catalog.ex
@dev/rendro/catalog_evidence_bundle.ex
</context>

## Artifacts this phase produces

- `136-15-PUBLICATION-RECEIPT.json` is `publication_status: complete` only after exact qualified-input, provider artifact, detached-control semantic validation, transactional install, exact seven-path diff, and post-check facts all pass. Any failure records `publication_status: unavailable`, the exact failed stage and observed facts, rollback result, and unchanged before/after hashes; unavailable never completes the plan.
- `assets/rendro/catalog.json` plus the exact six target PNGs in frontmatter are installed together from validated canonical staging only on success.
- The 26 control assets, rubric, sign-off, Plan 13 review receipt, public API, dependencies, catalog membership, and claims remain unchanged.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize the exact qualified canonical operation</name>
  <reversibility rating="one-way">Canonical dispatch creates provider-side history and installation replaces durable canonical target bytes, even though the local asset transaction can roll back failures.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
    - .planning/phases/136-catalog-visual-quality/136-13-SUMMARY.md
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .github/workflows/CATALOG-EVIDENCE.md
  </read_first>
  <files>.planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json only if publication is declined.</files>
  <action>Run the general and qualified review-receipt validators, display the exact candidate/control identities, six target IDs and frozen predicate results, parent evidence/reviewer-packet artifact identities, and current seven canonical path hashes. Confirm `failed_target_ids` and `missing_target_ids` are empty and that the review receipt contains no prior publication result. Present the provider-retention and canonical replacement consequences immediately before asking whether to dispatch the exact canonical operation. Do not dispatch, download, stage, or mutate canonical assets at this checkpoint. If publication is declined, write the unavailable publication receipt with the qualified parent receipt ID, exact candidate/control facts, `failure.stage: authorization`, unchanged seven-path/26-control before-after hashes, and `rollback_result: not_needed`; validate that closed unavailable shape and return `CHECKPOINT REACHED` without completing the plan.</action>
  <decision>Whether to authorize one canonical provider operation and rollback-safe installation for the exact qualified Plan 13 receipt.</decision>
  <context>D-25 requires atomic publication after all six qualify, but provider history is not erasable and canonical bytes are durable product evidence.</context>
  <options>
    <option id="authorize-exact-canonical">
      <name>Authorize exact canonical</name>
      <pros>Allows the independently validated qualified generation to become canonical through the tested transaction.</pros>
      <cons>Creates provider-side history and may replace seven durable canonical paths after all gates pass.</cons>
    </option>
    <option id="leave-qualified-unpublished">
      <name>Leave unpublished</name>
      <pros>Creates no new provider or canonical state while preserving the qualified review receipt.</pros>
      <cons>Leaves CATALOG-13 and the phase goal incomplete.</cons>
    </option>
  </options>
  <resume-signal>Select `authorize-exact-canonical` to continue or `leave-qualified-unpublished` to record a truthful unavailable publication receipt and stop without completion.</resume-signal>
  <acceptance_criteria>
    - The exact qualified receipt and all fresh eligibility predicates are shown before the decision.
    - Authorization is scoped to one candidate/control canonical operation; no mutable ref or nearby run is permitted.
    - No external or canonical mutation occurs before explicit selection.
    - Decline is recorded as truthful unavailable publication evidence and cannot complete the plan.
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.catalog.gallery --validate-review-receipt .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json &amp;&amp; mix rendro.catalog.gallery --require-qualified-review-receipt .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json</automated>
  </verify>
  <done>The exact qualified canonical operation is explicitly authorized; decline instead records noncompletion and stops.</done>
</task>

<task type="tracer">
  <name>Task 2: Trace qualified review authority into one exact canonical artifact</name>
  <precondition>Task 1 selected `authorize-exact-canonical`; the qualified receipt and its fresh parent/artifact/path gates still pass.</precondition>
  <reversibility rating="one-way">The authorized canonical dispatch creates provider-side history and retained artifact metadata that local rollback cannot erase.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .github/workflows/CATALOG-EVIDENCE.md
    - .github/workflows/catalog-evidence.yml
    - dev/rendro/catalog_evidence_bundle.ex
    - priv/pdfium_pin.json
  </read_first>
  <files>.planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json</files>
  <action>First run both the general and qualified Plan 13 receipt validators and snapshot complete hashes for the canonical manifest, six target PNGs, and 26 control PNGs. Require the exact candidate/control/run/attempt/renderer/two-review-artifact identities from the qualified receipt and Task 1's explicit publication authorization. Dispatch only the canonical operation for those full candidate/control identities. Capture the actual provider run ID and positive attempt, require success, and resolve exactly one matching canonical artifact by ID/name/API URL/provider digest; ambiguity or a nearby run fails. Download it to a new nonce-bearing directory, compute the lowercase archive SHA-256, compare provider digest, and extract safely. Create a fresh detached worktree at the exact control SHA, assert HEAD, and run `Rendro.CatalogEvidenceBundle.validate/3` for `:canonical`. Require the real ordered `cells` shape with 32 unique canonical IDs, exact source commit and renderer, six target records bound to the qualified candidate, 26 controls equal to the prior canonical generation, safe unique paths, and complete hashes. Write the publication receipt before install with qualified parent receipt ID, exact provider/archive facts, validation root/nonce/timestamp, pre-install hashes, and `publication_status: ready`. On any failure, write the closed unavailable form with failed stage/code/expected/observed/next action and all pre/post hashes unchanged, then return `CHECKPOINT REACHED` without Task 3.</action>
  <acceptance_criteria>
    - The qualified-only validator reruns fresh parent receipt/intake/artifact/path gates and rejects every nonqualified status.
    - Exactly one canonical run/attempt/artifact matches the authorized candidate/control identities and has independent provider plus archive digests.
    - Detached-control semantic validation proves exact 32/six/26 ordered identity, source commit, renderer, paths, and full hashes.
    - Any unavailable path records truthful noncompletion and leaves every canonical/control byte unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.catalog.gallery --validate-review-receipt .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json &amp;&amp; mix rendro.catalog.gallery --require-qualified-review-receipt .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json &amp;&amp; jq -e '.schema_version == 1 and (.publication_status == "ready" or .publication_status == "unavailable") and (.review_receipt_id | type == "string" and length &gt; 0) and (if .publication_status == "ready" then (.canonical_artifact.archive_sha256 | test("^[0-9a-f]{64}$")) and (.validation.bundle_result == "ok") else (.failure.stage | type == "string") and (.rollback_result == "not_needed") end)' .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json</automated>
  </verify>
  <done>One qualified review chain reaches one exact semantically validated canonical artifact; no negative review or provider outcome can reach installation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install and verify one complete canonical generation</name>
  <precondition>`136-15-PUBLICATION-RECEIPT.json` has `publication_status: ready`, its extracted canonical root exists, and its exact archive/payload hashes still validate.</precondition>
  <reversibility rating="costly">The canonical asset set is durable but recoverable through Plan 14's tested whole-generation backup/rollback transaction.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json
    - .planning/phases/136-catalog-visual-quality/136-13-REVIEW-RECEIPT.json
    - dev/rendro/catalog.ex
    - test/rendro/catalog_test.exs
    - assets/rendro/catalog.json
  </read_first>
  <files>.planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json, assets/rendro/catalog.json, assets/rendro/catalog/invoice/cedar-mutual/corporate-classic-dark.png, assets/rendro/catalog/statement/signal-ledger/minimal-mono-dark.png, assets/rendro/catalog/payslip/northline-logistics/swiss-light.png, assets/rendro/catalog/payslip/northline-logistics/swiss-dark.png, assets/rendro/catalog/ticket/aurora-live/brutalist-light.png, assets/rendro/catalog/ticket/aurora-live/brutalist-dark.png</files>
  <behavior>
    - "Ready validated staging installs the manifest and six target PNGs as one generation, then exact catalog/schema/docs checks pass."
    - "Any injected or real failure before, between, or after install restores all prior manifest/target bytes and leaves all 26 controls unchanged."
    - "Successful before/after classification is exactly seven changed paths and 26 byte-stable controls."
  </behavior>
  <action>Immediately revalidate the ready publication receipt's archive, extraction root, canonical bundle, and exact staged file hashes. Invoke only Plan 14's tested whole-generation publication transaction; do not copy target files individually. After install, run `mix rendro.catalog.check`, the catalog/evidence/rubric contract tests, and `mix ci.fast`. Compute complete before/after hashes for the manifest, all 32 PNGs, rubric, sign-off, and review receipt. Require exactly the manifest plus the six D-01 target PNGs to change, all 26 controls plus rubric/sign-off/review receipt to remain byte-identical, the catalog to remain 32 ordered cells with 20 unscored, and every dark record to retain `print_safety:false`. On any install or post-check failure, invoke transaction rollback, prove every prior canonical/control hash restored, set the publication receipt to unavailable with exact failed stage and rollback result, and return `CHECKPOINT REACHED` without completion. On success set `publication_status: complete`, record exact installed paths/hashes/check results and `rollback_result: not_needed`, then rerun the receipt and catalog checks.</action>
  <acceptance_criteria>
    - Publication uses the validated staged generation and the rollback-safe transaction, never per-file replacement.
    - Success changes exactly seven locked paths; all 26 controls and reviewer-owned evidence remain byte-identical.
    - Catalog check, rubric/schema/docs contracts, and `mix ci.fast` pass with 32/20 and dark non-print-safe boundaries intact.
    - Every failure restores the complete prior generation and leaves an unavailable receipt that cannot satisfy completion.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.schema_version == 1 and .publication_status == "complete" and (.installed_paths | length == 7) and ([.installed_paths[]] | length == (unique | length)) and (.validation.bundle_result == "ok") and (.post_checks.catalog == "ok") and (.post_checks.tests == "ok") and (.rollback_result == "not_needed")' .planning/phases/136-catalog-visual-quality/136-15-PUBLICATION-RECEIPT.json &amp;&amp; mix rendro.catalog.check &amp;&amp; mix test test/rendro/catalog_test.exs test/rendro/catalog_evidence_bundle_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>One fully qualified canonical generation is installed and proven exact; every failure path restores the previous generation and remains noncompletion evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Qualified review receipt -> canonical dispatch | Human review evidence authorizes a one-way provider operation only after closed eligibility validation. |
| Provider artifact -> trusted canonical staging | Remote bytes remain untrusted until exact identity, digest, path, and semantic validation. |
| Validated staging -> canonical assets | Seven durable paths cross an all-or-nothing transaction boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-F0 | Elevation of privilege | review eligibility | high | mitigate | Qualified-only validator reruns six predicates, parent complete receipt, and fresh artifact/path binding; Task 1 separately records publication authorization. |
| T-136-F1 | Spoofing | canonical run/artifact | high | mitigate | Require exact full SHAs, run/attempt, unique artifact API identity, provider digest, and archive SHA-256. |
| T-136-F2 | Tampering | canonical payload | high | mitigate | Detached-control closed validation requires exact 32/six/26 identities, source/renderer facts, safe paths, and full hashes. |
| T-136-F3 | Tampering | canonical install | high | mitigate | Whole-generation transaction plus before/after hashes and rollback prevent partial replacement. |
| T-136-F4 | Repudiation | publication outcome | high | mitigate | Machine receipt records qualified parent, provider artifact, validation, installed paths, post-checks, and rollback result. |
| T-136-F5 | Information disclosure / misrepresentation | claims | medium | mitigate | Post-checks retain catalog scope, 20 unscored rows, dark print_safety false, and no broader claims. |
| T-136-F6 | Denial of service | provider/install failure | medium | mitigate | Closed unavailable receipt and complete rollback preserve the prior generation without false completion. |
| T-136-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs. |
</threat_model>

## Scope calibration

The project has no estimate-calibration samples, so factor remains 1 and confidence `low`. Review intake/rubric/sign-off ownership stays in Plan 13. This plan owns only canonical artifact provenance and the indivisible seven-path publication transaction; the seven durable paths cannot be split without violating D-25 atomicity.

## Spec-less probe resolution

- CATALOG-10 boundary/precision is re-proven through exact seven changed paths and 26 byte-stable controls.
- CATALOG-11/CATALOG-12 enter only through Plan 13's six-record qualified gate; publication creates no visual judgment or broader claim.
- CATALOG-13 boundary/identity/ordering/precision is re-proven at provider download, detached validation, transaction, and post-check boundaries.
- D-25 atomic publication is the plan's sole mutation concern; D-20 through D-24/D-26 remain immutable inputs from Plans 11-13.

<verification>
- Require both general and qualified Plan 13 receipt validators before dispatch.
- Require one exact canonical provider artifact and detached-control semantic validation before install.
- Require a complete publication receipt, exact seven-path diff, 26 stable controls, `mix rendro.catalog.check`, focused contracts, and `mix ci.fast` after install.
- Any unavailable or rollback outcome is valid noncompletion evidence but cannot create `136-15-SUMMARY.md`.
</verification>

<success_criteria>
- Only one closed qualified review receipt plus explicit Plan 15 publication authorization reaches canonical publication.
- Provider and canonical payload identities are exact, fresh, unique, and semantically validated.
- One all-or-nothing transaction changes exactly the manifest plus six target PNGs and preserves all controls/evidence.
- Any failure restores the prior complete generation and remains truthfully incomplete.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-15-SUMMARY.md` only after `publication_status: complete`, exact seven-path/26-control proof, and all post-checks pass. Preserve unavailable receipts without creating a completion summary.
</output>
