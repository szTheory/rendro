---
phase: 136-catalog-visual-quality
plan: "04"
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - lib/rendro/recipes/ticket.ex
  - test/rendro/recipes/ticket_test.exs
  - test/rendro/recipes/ticket_byte_identity_test.exs
autonomous: true
requirements: [CATALOG-11, CATALOG-12]
must_haves:
  truths:
    - "The A6 locator remains one left-to-right equal-share row with Section | Row | Seat | Gate labels directly above atomic GA | H | 24 | B values in source order."
    - "GA never splits and Seat 24 remains visually and structurally distinct from Gate B; the placement group is the sole 34pt dominant hierarchy."
    - "Light and dark target profiles have identical locator and page geometry; dark muted labels, reference/stub, perforation/rules, and terms use readable semantic secondary treatment while retaining print_safety:false."
    - "Existing 1-4 placement validation, structured failures, caller data, default/no-profile bytes, subtitle/terms wrapping, and A6 Brutalist geometry remain unchanged."
    - statement: "Ticket render assertions prove the one-row equal-share locator keeps `GA`, `H`, `24`, and `B` atomic and distinct in light and dark with no `24B` association."
      verification: backstop
    - statement: "Held-out Ticket subtitle, terms, and reference strings wrap only in existing flexible prose regions; the four locator labels and values never wrap or truncate."
      verification: backstop
  artifacts:
    - path: lib/rendro/recipes/ticket.ex
      provides: "Generic atomic equal-share locator profile and target-scoped semantic secondary treatment"
      contains: "atomic_equal_share"
    - path: test/rendro/recipes/ticket_test.exs
      provides: "Source-order label/value association, atomicity, dominance, contrast, and held-out wrapping contracts"
      contains: "GA"
    - path: test/rendro/recipes/ticket_byte_identity_test.exs
      provides: "Light/dark geometry parity, default identity, and two-render determinism"
      contains: "byte-identical"
  key_links:
    - from: dev/rendro/catalog.ex
      to: lib/rendro/recipes/ticket.ex
      via: "Plan 01 maps only the two Brutalist IDs to generic `locator_layout: :atomic_equal_share`"
      pattern: 'atomic_equal_share'
    - from: lib/rendro/recipes/ticket.ex
      to: priv/examples/ticket/aurora-live/ticket.json
      via: "existing source-order placement list supplies Section GA, Row H, Seat 24, Gate B"
      pattern: 'data\.placement'
    - from: lib/rendro/recipes/ticket.ex
      to: test/rendro/recipes/ticket_test.exs
      via: "one equal-share table retains label/value cell boundaries and dominance"
      pattern: 'List\.duplicate\(\{:share, 1\}'
  prohibitions: []
---

<objective>
Make the Brutalist Ticket locator unambiguous in light and dark while preserving the one-row A6 archetype, all four supplied fields, equal-share geometry, source order, dominant placement hierarchy, and default behavior.

Purpose: Implement D-16 through D-19 using the generic profile boundary without inventing a responsive locator capability or altering the shared Aurora fixture.
Output: An atomic equal-share locator treatment, readable target-scoped dark secondary roles, and deterministic association/geometry/held-out coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-RESEARCH.md
@.planning/phases/136-catalog-visual-quality/136-PATTERNS.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
@lib/rendro/recipes/ticket.ex
@priv/examples/ticket/aurora-live/ticket.json
@priv/examples/ticket/DOMAIN.md
</context>

## Artifacts this phase produces

- `lib/rendro/recipes/ticket.ex` adds private generic profile predicate `atomic_locator?/1` and private fit/role helper `locator_roles/3` (or equivalently named private helpers) consuming `locator_layout: :atomic_equal_share`.
- The locator retains the existing `Rendro.table([value_cells], header: header_cells, columns: List.duplicate({:share, 1}, length(data.placement)), borders: :none)` structure and generic 1-4 entry contract.
- The target uses only existing 8/10/20/34pt roles, weight 400, and leading 1.2; the fit helper ensures each of GA/H/24/B fits its own equal-share cell without adding a size.
- Existing tests add exact Aurora source-order, direct label/value association, atomic-token, no-24B, sole-dominance, muted-role, held-out prose, geometry parity, default identity, and two-render cases.
- No new file, field, fixture, placeholder, row, layout archetype, point size, theme token, public API, dependency, renderer, or catalog cell is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render the Aurora locator as four atomic, directly associated equal-share cells</name>
  <reversibility rating="reversible">The generic atomic-locator branch and its tests are isolated to Ticket-owned files and revert normally in Git; rollback criterion is restored pre-task locator bytes with the Aurora fixture unchanged.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - .planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
    - lib/rendro/recipes/ticket.ex
    - lib/rendro/recipes/palette.ex
    - lib/rendro/theme/presets.ex
    - test/rendro/recipes/ticket_test.exs
    - priv/examples/ticket/aurora-live/ticket.json
    - priv/examples/ticket/DOMAIN.md
  </read_first>
  <files>lib/rendro/recipes/ticket.ex, test/rendro/recipes/ticket_test.exs</files>
  <behavior>
    - "The real Aurora target emits labels Section/Row/Seat/Gate and values GA/H/24/B in exact source order as four aligned header/value columns."
    - "All four values are atomic one-line blocks; GA does not split and 24 cannot share/merge/associate with B."
    - "The locator is one equal-share row and the only 34pt placement display; title remains 20pt and subtitle/terms remain 10pt."
    - "The shared fixture and generic 1-4 placement validation are unchanged."
  </behavior>
  <action>Write failing structural assertions for the real Aurora fixture before production changes. In `Rendro.Recipes.Ticket.main_section/2`, consume only the generic `locator_layout: :atomic_equal_share` value from Plan 01. Retain one `header_cells` list, one `value_cells` list, one left-to-right table row, `List.duplicate({:share, 1}, length(data.placement))`, and the existing source enumeration. Per D-17, add only generic fit/role data needed to keep atomic `GA`, `H`, `24`, and `B` in their own cells at the existing 34pt placement role, with every label directly above its value and placement remaining the dominant hierarchy. Assert explicit Section/GA, Row/H, Seat/24, Gate/B index pairs and cell boundaries, including a negative association check for `24B`. Preserve title/subtitle, reference/stub, terms, A6 geometry, and validation per D-16 and D-18. Do not inspect Gate specially, reorder data, add a second row, remove a field, or edit the fixture.</action>
  <acceptance_criteria>
    - The locator consists of exactly four equal-share columns and one value row with exact label/value index pairs in source order.
    - GA, H, 24, and B are each one atomic text block confined to its own cell; no combined `24B` content or adjacency ambiguity exists structurally.
    - Placement alone uses the existing 34pt display role; title/subtitle/reference/terms remain subordinate at their existing sizes.
    - The Aurora fixture hash/content and existing generic 1-4 validation are unchanged.
    - Omitted profile retains the pre-phase locator structure and bytes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/ticket_test.exs --max-failures 1</automated>
  </verify>
  <done>The real Aurora locator renders GA | H | 24 | B as four atomic, directly labeled, source-order equal-share cells with the placement group uniquely dominant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove light/dark geometry parity and readable muted dark semantics</name>
  <reversibility rating="reversible">Ticket semantic-role refinements are independently Git-revertible from Plan 01 threading and the locator structure; rollback criterion is restored prior target colors with default/no-profile bytes green.</reversibility>
  <read_first>
    - lib/rendro/recipes/ticket.ex
    - lib/rendro/recipes/palette.ex
    - lib/rendro/theme/presets.ex
    - test/rendro/recipes/ticket_test.exs
    - test/rendro/recipes/ticket_byte_identity_test.exs
    - priv/examples/ticket/aurora-live/ticket.json
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
  </read_first>
  <files>lib/rendro/recipes/ticket.ex, test/rendro/recipes/ticket_test.exs, test/rendro/recipes/ticket_byte_identity_test.exs</files>
  <behavior>
    - "Light and dark target profiles have identical page/locator geometry and differ only in semantic palette treatment."
    - "Dark locator labels, reference/stub, perforation/rules, and terms use secondary semantic roles readable on the warm dark surface; values use primary ink."
    - "Held-out subtitle, terms, and reference strings wrap only in existing flexible prose regions while all locator labels/values remain one line."
    - "Two renders per target mode are byte identical and default/no-profile/unrelated-theme bytes remain frozen."
  </behavior>
  <action>Complete D-19 by routing locator labels, reference/stub, perforation/rules, and terms through existing semantic secondary roles only when the generic locator profile is active; keep primary placement values on `colors.ink`. Do not change geometry values between modes or soften the rectilinear table/rule motif. Add structural equality assertions for light/dark page template, table columns, block order, positions, and role sizes; semantic color inequality/equality assertions where appropriate; held-out long subtitle/terms/reference cases; exact atomic locator assertions in both modes; and two-render byte determinism. Compare default/no-profile and unrelated themes to pre-phase controls and retain `print_safety: false` in catalog evidence per D-19.</action>
  <acceptance_criteria>
    - Light and dark target structure/geometry values compare exactly, including the single equal-share locator table.
    - Dark secondary elements resolve to deliberate semantic secondary/rule roles and placement values resolve to primary ink; no raw-black fallback is present.
    - Held-out flexible prose wraps without clipping while the eight locator label/value strings never wrap or truncate.
    - Both modes render deterministically twice and all omitted/unrelated paths remain byte-identical.
    - Dark remains screen-oriented with typed `print_safety: false`; no accessibility, PDF/UA, viewer, or print claim is added.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/ticket_test.exs test/rendro/recipes/ticket_byte_identity_test.exs --max-failures 1</automated>
  </verify>
  <done>Brutalist Ticket light/dark share exact geometry, retain the blunt motif, and expose readable semantic secondary information without compromising locator association or truthful scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generic locator profile -> Ticket | Layout intent crosses into the recipe without catalog identity. |
| Caller placement list -> table cells | Supplied labels/values must retain exact order and association. |
| Target render -> evidence | Geometry and semantic role changes are allowed only for the two locked Ticket cells. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-40 | Tampering | placement data | high | mitigate | Exact fixture equality and index-paired Section/GA, Row/H, Seat/24, Gate/B assertions. |
| T-136-41 | Repudiation | locator association | high | mitigate | Structural one-row/equal-share/atomic cell assertions and explicit negative `24B` association control. |
| T-136-42 | Tampering | geometry parity | medium | mitigate | Exact light/dark structural geometry equality and double-render tests. |
| T-136-43 | Information disclosure / misrepresentation | muted dark text | medium | mitigate | Semantic role assertions plus retained screen-only/print_safety false boundary. |
| T-136-44 | Denial of service | long prose | low | mitigate | Existing flexible prose wrapping and held-out long-content tests; locator strings remain bounded/atomic. |
| T-136-AUTH | Spoofing / Session | runtime authentication | low | accept | N/A: no request, credential, identity, authentication, or session surface exists. |
| T-136-SC | Tampering | package installs | low | accept | No package/dependency installation occurs. |

## Verification and Residual Risk

- Association and geometry are deterministic; rapid visual scan clarity remains named advisory evidence in Plan 05.
</threat_model>

## Spec-less probe fallback audit

- UI coverage: both Ticket backstops are lifted exactly once into this plan's truths; the four surface-contract truths make the rendered outcome observable without inventing browser states.
- The fixture/field/reordering prohibition recalled for Ticket is retained once at plan-set level in Plan 03 and mechanically enforced here through source/fixture/order acceptance criteria; no duplicate prohibition is minted.

<verification>
- `mix test test/rendro/recipes/ticket_test.exs test/rendro/recipes/ticket_byte_identity_test.exs --max-failures 1`
- `mix format --check-formatted`
- `mix ci.fast`
</verification>

<success_criteria>
- Ticket targets retain one A6 equal-share row with exact Section/GA, Row/H, Seat/24, Gate/B association and atomic values.
- Placement remains uniquely dominant and cannot read as `24B`.
- Light/dark geometry is identical; dark secondary information is readable through semantic roles and remains non-print-safe.
- Held-out prose, default paths, error semantics, and deterministic bytes remain intact.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-04-SUMMARY.md` when done.
</output>
