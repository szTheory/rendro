---
phase: 136-catalog-visual-quality
plan: "11"
type: execute
wave: 8
depends_on: ["136-09", "136-10"]
files_modified:
  - dev/rendro/catalog_evidence_bundle.ex
  - dev/rendro/catalog_visual_gallery.ex
  - dev/mix/tasks/rendro/catalog/gallery.ex
  - test/rendro/catalog_evidence_bundle_test.exs
  - test/rendro/catalog_visual_gallery_test.exs
autonomous: true
gap_closure: true
requirements: [CATALOG-10, CATALOG-11, CATALOG-12, CATALOG-13]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-20, the authoritative review bundle validates semantic identity—not counts alone—across the ordered 32 candidate cells, exact six/26 diff, 12 final images, four multipage checksums, 12 preset images, renderer pin, candidate/control commits, workflow run/attempt, and every PDF/PNG digest before any image is reviewed."
    - "Per D-01/D-26, candidate authority comes from deterministic `changed_targets`; no reviewer status, convenience rendering, or packet metadata can manufacture or weaken that exact scope."
    - "Per D-21, a distinct non-authoritative reviewer packet contains exactly eight full-size images in locked family-paired order: Invoice light control/dark target, Statement light control/dark target, Payslip light/dark, and Ticket light/dark."
    - "Every reviewer-packet image has a unique role and safe relative path, and its PNG/source-PDF hashes bind exactly to the separately validated closed bundle; the packet has its own manifest and digest and is never evidence authority."
    - "The committed candidate exposes a discoverable `mix rendro.catalog.gallery` task that builds or validates the exact eight-image packet rather than a six-target-only gallery."
    - "Malformed or mismatched renderer pins, role payloads, packet paths, hashes, order, candidate/control identity, run, or attempt return structured validation failure instead of raising or accepting a nearby value."
  artifacts:
    - path: dev/rendro/catalog_evidence_bundle.ex
      provides: "Closed semantic review/canonical bundle validator with cross-payload identity binding"
      contains: "validate"
    - path: dev/rendro/catalog_visual_gallery.ex
      provides: "Exact eight-image, non-authoritative reviewer-packet builder and bundle-binding validator"
      contains: "authority"
    - path: dev/mix/tasks/rendro/catalog/gallery.ex
      provides: "Committed Mix command for reviewer-packet build and validation"
      contains: "Mix.Tasks.Rendro.Catalog.Gallery"
    - path: test/rendro/catalog_evidence_bundle_test.exs
      provides: "Boundary mutation matrix for closed semantic bundle roles and identities"
      contains: "semantic"
    - path: test/rendro/catalog_visual_gallery_test.exs
      provides: "Exact eight-role packet, two-control inclusion, hash-binding, and CLI contract coverage"
      contains: "invoice_light_control"
  key_links:
    - from: dev/rendro/catalog_evidence_bundle.ex
      to: dev/rendro/catalog_visual_gallery.ex
      via: "packet validation first requires the closed bundle to validate, then joins each packet role/path/hash to its authoritative payload record"
      pattern: "candidate/catalog.json|final-review/final.json"
    - from: dev/mix/tasks/rendro/catalog/gallery.ex
      to: dev/rendro/catalog_visual_gallery.ex
      via: "Mix task delegates both build and validate modes to the packet module"
      pattern: "CatalogVisualGallery"
    - from: dev/rendro/catalog_visual_gallery.ex
      to: D-21 review order
      via: "literal ordered reviewer roles select the two named light controls plus six targets"
      pattern: "invoice_light_control|statement_light_control|ticket_dark_target"
  prohibitions:
    - statement: "No count-only, stale, partial, convenience packet, prior-phase, or cross-run payload may be accepted as review authority."
      status: unverified
      flagged: true
    - statement: "No baseline, candidate, renderer, workflow, PDF, PNG, role, path, or packet identity may be inferred from filename, prefix, current HEAD, or unchecked metadata."
      status: unverified
      flagged: true
    - statement: "No reviewer packet may omit either named light control, contain only the six changed targets, or be packaged inside the authoritative evidence bundle."
      status: unverified
      flagged: true
---

<objective>
Make Phase 136 review evidence semantically self-proving and give reviewers one exact eight-image packet whose bytes remain subordinate to the validated bundle.

Purpose: Close the count-only and six-image handoff defects while preserving D-20/D-21/D-26 authority separation before any external run is trusted.
Output: A closed semantic bundle validator plus a committed, testable Mix task that creates and validates the separate eight-image reviewer packet.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-RESEARCH.md
@.planning/phases/136-catalog-visual-quality/136-PATTERNS.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
@.planning/phases/136-catalog-visual-quality/136-REVIEW.md
@.planning/phases/136-catalog-visual-quality/136-REVIEW-FIX.md
@.planning/phases/136-catalog-visual-quality/136-09-SUMMARY.md
@.planning/phases/136-catalog-visual-quality/136-10-SUMMARY.md
@dev/rendro/catalog_evidence_bundle.ex
@dev/rendro/catalog_visual_gallery.ex
@dev/mix/tasks/rendro/catalog/gallery.ex
</context>

## Artifacts this phase produces

- `dev/rendro/catalog_evidence_bundle.ex` adds role-specific semantic validators and cross-payload identity joins for review and canonical inputs; exact private symbol names may follow existing module conventions.
- `dev/rendro/catalog_visual_gallery.ex` evolves the user-owned convenience work into a separate exact eight-image reviewer packet: two named unchanged light controls plus six targets, each hash-bound to closed-bundle facts and explicitly non-authoritative.
- `dev/mix/tasks/rendro/catalog/gallery.ex` is an owned, committed implementation artifact. It supports packet build and validation modes and is discoverable from the exact candidate checkout.
- Tests add mutation matrices for semantic counts/order/identity/digests/paths and for eight-role packet membership, uniqueness, authority labeling, and closed-bundle hash binding.
- No baseline ownership, canonical transaction, workflow publication, public Rendro API, dependency, database/schema ORM, catalog scope, visual score, or approval record is changed here; Plan 14 owns the operational publication transaction.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one target identity through the authoritative closed bundle</name>
  <reversibility rating="costly">The closed evidence schema becomes the workflow/reviewer interchange contract; rollback requires coordinated validator, workflow, runbook, and fixture changes.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
    - .planning/phases/136-catalog-visual-quality/136-REVIEW.md
    - dev/rendro/catalog_evidence_bundle.ex
    - test/rendro/catalog_evidence_bundle_test.exs
    - assets/rendro/catalog.json
    - priv/pdfium_pin.json
  </read_first>
  <files>dev/rendro/catalog_evidence_bundle.ex, test/rendro/catalog_evidence_bundle_test.exs</files>
  <behavior>
    - "One named target's candidate PDF/PNG digests, final candidate/control image, preset image, renderer pin, candidate/control commits, run identity, and multipage identity join exactly through one validated review bundle."
    - "The candidate payload is exactly 32 canonical cells with exact ordered six changed_targets and 26 controls; final/preset payloads are the exact ordered 12 identities; multipage is the exact four named checksums; canonical is the exact 32 ordered cells."
    - "Any missing, extra, duplicate, reordered, empty, unsafe-path, malformed-hash, mismatched renderer/run/attempt/commit, or cross-payload ID/digest mutation returns stable structured errors."
  </behavior>
  <action>Begin with a valid end-to-end fixture using real manifest shapes and trace one D-01 target across every authoritative role. Replace generic count acceptance with closed role validators: candidate requires the exact canonical 32 IDs/order, required cell/path/full-SHA/renderer/source fields, exact six `diff.changed_targets`, exact 26 controls, and no candidate-owned quality/reviewer authority; final requires the exact ordered 12 candidate/control image identities and binds each PNG to source cell digest, renderer, candidate SHA, control SHA, run ID, and run attempt; preset requires the exact ordered 12 identities and the same renderer/candidate identity; multipage requires exactly the four locked safe filenames and full digests bound to candidate multipage output; canonical requires the exact 32 `cells` IDs/order, paths/hashes/renderer/source identity. Reject absolute, traversal, backslash, NUL, duplicate, or out-of-root paths and any non-lowercase full SHA-256. Validate each role before cross-role joins and aggregate stable reasons. Make renderer-pin read/decode/key/type mismatch return an error tuple. Add table-driven empty, one-short, one-extra, duplicate, reorder, exact-boundary, and one-character-digest mutations for every role per CATALOG-13.</action>
  <acceptance_criteria>
    - A semantically valid fixture with all authoritative roles passes, and each target/control traces to exact candidate bytes and one run/renderer identity.
    - Count-correct but semantically arbitrary candidate/final/preset/multipage/canonical fixtures fail.
    - Missing, extra, duplicate, reorder, unsafe path, short/mixed-case digest, renderer mismatch, commit mismatch, run mismatch, attempt mismatch, and cross-payload digest mismatch return `{:error, reasons}` without raising.
    - Exact 32/6/26/12/4/12 boundaries pass; one below or above each boundary fails.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_evidence_bundle_test.exs --max-failures 1</automated>
  </verify>
  <done>The authoritative review bundle proves complete semantic identity instead of merely counting files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and validate the distinct eight-image reviewer packet</name>
  <reversibility rating="costly">The packet manifest and Mix CLI become the handoff contract used by workflow, runbook, and review intake.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - dev/rendro/catalog_evidence_bundle.ex
    - dev/rendro/catalog_visual_gallery.ex
    - dev/mix/tasks/rendro/catalog/gallery.ex
    - test/rendro/catalog_visual_gallery_test.exs
    - test/rendro/catalog_raster_review_test.exs
  </read_first>
  <files>dev/rendro/catalog_visual_gallery.ex, dev/mix/tasks/rendro/catalog/gallery.ex, test/rendro/catalog_visual_gallery_test.exs</files>
  <behavior>
    - "Build mode selects exactly Invoice light control/dark target, Statement light control/dark target, Payslip light/dark, and Ticket light/dark in D-21 order from full-size final-review output."
    - "The packet manifest declares `authority: none`, exact candidate/control/run/attempt/renderer identity, and eight unique role/path records with PNG/source-PDF hashes and authoritative bundle role/path bindings."
    - "Validate mode first requires the closed bundle to pass, then compares every packet file hash and bound identity to candidate/final payload facts; a six-image packet, omitted control, duplicate role/path, reorder, stale hash, or cross-run packet fails."
    - "`mix rendro.catalog.gallery` is discoverable and exercises the committed candidate module rather than relying on an untracked file."
  </behavior>
  <action>Preserve the user-owned gallery work but change its contract from the six changed targets to D-21's exact eight full-size review roles. Consume the candidate manifest plus the final-review identity manifest/output so the unchanged Invoice-light and Statement-light controls are present beside the six targets. Emit one safe packet root containing `manifest.json`, `index.html`, and exactly eight PNGs; give each image a unique fixed review role, ordinal, catalog ID, control/target kind, relative path, PNG SHA-256, source-PDF SHA-256, and authoritative bundle role/path reference. Include candidate SHA, control SHA, run ID, run attempt, renderer version/hash, `authority: none`, and a statement that the packet is navigation/review presentation only. Add validation that requires a separately validated D-20 bundle and joins all eight hashes/identities to its candidate/final records before returning `:ok`; the packet's own digest is computed externally and never grants authority. Extend `Mix.Tasks.Rendro.Catalog.Gallery` with three explicit interfaces: build via `--candidate-manifest PATH --final-manifest PATH --output DIR`; validate Plan 12's complete local receipt via `--validate-receipt PATH`; and validate Plan 13's fresh intake manifest via `--validate-intake PATH`. Both validation interfaces must read the referenced extracted bundle/packet roots, rerun closed-bundle then packet binding, and reject any unavailable receipt or missing local archive fact. Keep strict unexpected-argument failure and diagnostics naming the failed role. Keep the module dev-only and preserve every unrelated user edit. Tests must exercise exact order, both named controls, six targets, uniqueness, safe paths, candidate/control/run/attempt/renderer joins, pixel hash mismatch, source-PDF mismatch, wrong bundle digest binding, six/seven/nine counts, duplicate/reorder, unavailable receipt rejection, and `authority: none`.</action>
  <acceptance_criteria>
    - Build output contains exactly eight full-size PNGs in D-21 order, including the two named unchanged light controls.
    - Every packet role/path is unique and every PNG/source-PDF hash joins to the separately validated closed bundle.
    - Packet metadata and HTML state that it has no evidence, review, approval, disposition, or canonical authority.
    - All malformed, stale, cross-run, omitted-control, duplicate, reordered, and hash-mismatched packet cases fail closed.
    - `mix help rendro.catalog.gallery` discovers the committed task, and its build/validate tests execute from the current candidate checkout.
  </acceptance_criteria>
  <verify>
    <automated>mix help rendro.catalog.gallery &gt;/dev/null &amp;&amp; mix test test/rendro/catalog_evidence_bundle_test.exs test/rendro/catalog_visual_gallery_test.exs --max-failures 1</automated>
  </verify>
  <done>The committed Mix task produces and validates one separate exact eight-image reviewer packet whose identities remain subordinate to the closed bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow payloads -> closed validator | Externally produced manifests and metadata are untrusted until role and cross-payload semantic validation succeeds. |
| Closed bundle -> reviewer packet | Non-authoritative presentation bytes must match authoritative hashes without inheriting authority. |
| Mix CLI -> filesystem | User-supplied roots and packet paths must remain bounded, regular, and unique. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-B0 | Spoofing | run/commit/renderer identity | high | mitigate | Full exact identities are cross-bound across every payload and reviewer-packet record. |
| T-136-B1 | Tampering | bundle role payloads | high | mitigate | Closed schemas, safe paths, full digests, exact order/count, and cross-payload joins fail closed. |
| T-136-B2 | Tampering | reviewer packet | high | mitigate | Exact eight roles and file hashes are joined to the separately validated bundle before viewing. |
| T-136-B3 | Information disclosure | artifact paths | medium | mitigate | Reject absolute, traversal, backslash, NUL, duplicate, and out-of-root paths. |
| T-136-B4 | Denial of service | malformed pin/JSON | medium | mitigate | Decode/type failures return bounded structured errors rather than crashes. |
| T-136-B5 | Elevation of privilege | convenience packet | high | mitigate | Packet metadata is authority-none and validation cannot create review or canonical eligibility. |
| T-136-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs. |
</threat_model>

## Scope calibration

The project has zero estimate-calibration samples, so the emitted factor is 1 and confidence remains `low`. The former ten-file Plan 11 is split at the semantic contract boundary: this plan has two tasks over five exclusively owned files; Plan 14 owns Git baseline/canonical transaction plus workflow/runbook publication. No same-wave file overlaps remain.

## Spec-less probe resolution

- CATALOG-10 boundary/precision are enforced through exact six/26 membership and full digests.
- CATALOG-11 empty/ordering/precision are inherited from Plan 10 and bound here into exact payload and packet order.
- CATALOG-12 remains explicitly human-owned; bundle/packet validation proves provenance and invariants, not aesthetic quality.
- CATALOG-13 boundary/adjacency/empty/ordering/precision are explicit in role and packet mutation matrices.
- Prohibition recall retains stale/partial authority, identity inference, and missing-control prohibitions above; generic injection/path threats route to STRIDE.

<verification>
- `mix help rendro.catalog.gallery` succeeds from the committed candidate checkout.
- `mix test test/rendro/catalog_evidence_bundle_test.exs test/rendro/catalog_visual_gallery_test.exs --max-failures 1`.
- `mix format --check-formatted` and `mix ci.fast`.
</verification>

<success_criteria>
- Every authoritative bundle role is semantically validated and identity-bound across one exact run.
- A distinct authority-none packet provides exactly the eight D-21 full-size images, including both controls, with every hash bound to the validated bundle.
- The Mix task implementation is explicitly owned, committed, discoverable, and exercised.
- Packet convenience cannot substitute for evidence authority or human review.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md` when done.
</output>
