---
phase: 136-catalog-visual-quality
plan: "14"
type: execute
wave: 9
depends_on: ["136-11"]
files_modified:
  - dev/rendro/catalog.ex
  - .github/workflows/catalog-evidence.yml
  - .github/workflows/CATALOG-EVIDENCE.md
  - test/rendro/catalog_test.exs
  - test/docs_contract/catalog_evidence_runbook_test.exs
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
gap_closure: true
requirements: [CATALOG-10, CATALOG-11, CATALOG-12, CATALOG-13]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The candidate's baseline_commit_sha truthfully identifies the full Git commit that owns the exact checked-in baseline manifest bytes and fails closed if that identity cannot be proven; it is never copied from the candidate SHA."
    - "Canonical manifests use the real ordered `cells` shape, bind a truthful source commit and renderer, and fail on missing, extra, reordered, duplicate, malformed, path-escaping, wrong-digest, or cross-payload identity data."
    - "Canonical publication is transactional: any staged rename/write failure restores the complete pre-publication asset directory and manifest byte-for-byte and leaves no mixed generation."
    - "The review workflow publishes two separately identified artifacts for one exact run/attempt: the authoritative closed evidence bundle and the non-authoritative eight-image reviewer packet, each with its own provider and archive digest."
    - "The runbook derives the live run attempt and validates candidate/control/run/attempt/renderer/roles/paths plus both artifact identities and digests before any of the eight images is opened."
    - "Workflow and documentation contract tests exercise the committed `mix rendro.catalog.gallery` task and exact eight-image packet, including Invoice-light and Statement-light controls."
  artifacts:
    - path: dev/rendro/catalog.ex
      provides: "Truthful baseline source identity and transactional canonical publication"
      contains: "baseline_commit_sha"
    - path: .github/workflows/catalog-evidence.yml
      provides: "Real `.cells` canonical checks plus separately uploaded closed evidence and eight-image packet artifacts"
      contains: "mix rendro.catalog.gallery"
    - path: .github/workflows/CATALOG-EVIDENCE.md
      provides: "Dynamic run-attempt, dual-artifact validation, authority, and recovery contract"
      contains: "RUN_ATTEMPT"
    - path: test/rendro/catalog_test.exs
      provides: "Baseline provenance and failure-injected transaction coverage"
      contains: "baseline_commit_sha"
    - path: test/docs_contract/catalog_evidence_runbook_test.exs
      provides: "Exact dual-artifact download and validation contract"
      contains: "reviewer packet"
    - path: test/guardrails/required_checks_contract_test.exs
      provides: "Workflow shape, artifact separation, packet command, and canonical cells gates"
      contains: "upload-artifact"
  key_links:
    - from: dev/rendro/catalog.ex
      to: assets/rendro/catalog.json
      via: "Git object identity is accepted only after exact manifest-byte comparison"
      pattern: "baseline_commit_sha|source_commit_sha"
    - from: .github/workflows/catalog-evidence.yml
      to: dev/mix/tasks/rendro/catalog/gallery.ex
      via: "candidate workflow builds the exact packet with the committed Mix task after full-size rasters exist"
      pattern: "mix rendro.catalog.gallery"
    - from: .github/workflows/catalog-evidence.yml
      to: .github/workflows/CATALOG-EVIDENCE.md
      via: "artifact names, run attempt, separate digests, and validation commands agree exactly"
      pattern: "run_attempt|reviewer-packet|catalog-evidence"
  prohibitions:
    - statement: "No baseline, candidate, renderer, workflow, artifact, or canonical identity may be inferred from filename, current HEAD, prefix, or unchecked metadata."
      status: unverified
      flagged: true
    - statement: "No failed canonical publication may delete or partially overwrite the previous complete canonical generation."
      status: unverified
      flagged: true
    - statement: "No reviewer packet may be embedded in or represented as the authoritative closed evidence artifact."
      status: unverified
      flagged: true
---

<objective>
Make baseline/canonical publication recoverable and publish the closed evidence plus exact reviewer packet as two independently digestible artifacts.

Purpose: Complete the operational half split from Plan 11 so D-20/D-21/D-25 evidence can be fetched, validated, reviewed, and rolled back without identity ambiguity.
Output: Truthful Git provenance, failure-injected canonical transaction, dual-artifact workflow, and matching operator/runbook contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-RESEARCH.md
@.planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
@.planning/phases/136-catalog-visual-quality/136-REVIEW.md
@.planning/phases/136-catalog-visual-quality/136-REVIEW-FIX.md
@.planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md
@dev/rendro/catalog.ex
@.github/workflows/catalog-evidence.yml
@.github/workflows/CATALOG-EVIDENCE.md
</context>

## Artifacts this phase produces

- `dev/rendro/catalog.ex` gains a truthful baseline-owner resolver, canonical `source_commit_sha`, and a private/testable transaction seam that proves rollback at each filesystem failure boundary.
- `.github/workflows/catalog-evidence.yml` emits fields/shapes accepted by Plan 11's closed validator, builds the D-21 reviewer packet through the committed Mix task, and uploads evidence and packet under distinct names with the same exact candidate/run/attempt identity.
- `.github/workflows/CATALOG-EVIDENCE.md` derives the live run attempt, documents two independent artifact URL/digest checks, and requires closed-bundle validation before packet binding or image viewing.
- Tests cover baseline provenance, canonical rollback, workflow shapes, separate artifact identities, exact two-control/eight-image packet generation, and attempt 2+ runbook commands.
- No public Rendro API, dependency, database/schema ORM, external API integration, catalog scope, visual score, or approval record is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove baseline ownership and transactional canonical rollback</name>
  <reversibility rating="costly">Baseline provenance and publication transaction semantics become durable evidence contracts, though implementation remains dev-only.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
    - .planning/phases/136-catalog-visual-quality/136-REVIEW.md
    - dev/rendro/catalog.ex
    - test/rendro/catalog_test.exs
    - assets/rendro/catalog.json
  </read_first>
  <files>dev/rendro/catalog.ex, test/rendro/catalog_test.exs</files>
  <behavior>
    - "baseline_commit_sha names a full Git commit whose catalog manifest blob is byte-identical to the checked-in baseline being compared."
    - "A missing, uncommitted, or mismatched baseline identity fails before candidate publication; candidate SHA is never substituted."
    - "Canonical success swaps the complete staged generation, while failure at every rename/write boundary restores old assets and manifest exactly and removes incomplete staging/backup state."
  </behavior>
  <action>Write provenance tests around a temporary Git fixture or injectable command runner. Resolve the full commit that last authored the exact current `assets/rendro/catalog.json`, read that commit's blob, and require byte-for-byte equality with the baseline before emitting `baseline_commit_sha`; fail closed on absent history, dirty mismatch, abbreviated/invalid SHA, or same-as-candidate identity that does not own identical bytes. Add truthful canonical `source_commit_sha` to the built manifest and include it in closed validation. Refactor `publish_canonical_staging/0` into an explicit transaction: validate complete staging, move current asset directory and manifest to sibling backups, install both staged replacements, verify both, and only then remove backups. On every failure restore both old artifacts byte-for-byte and remove partial new outputs. Introduce a private dependency/test seam for deterministic failure injection at each move/write step. Preserve D-25's all-or-nothing boundary and never publish from a test.</action>
  <acceptance_criteria>
    - Candidate baseline_commit_sha is a full SHA whose Git blob exactly equals baseline bytes and differs from candidate identity unless Git evidence proves otherwise.
    - Missing history, byte mismatch, malformed SHA, or unproven identity returns a structured error before candidate evidence publication.
    - Canonical manifests contain exact full source commit and renderer identity and pass the closed validator.
    - Success installs one complete generation; injected failure before, between, or after both installs restores assets and manifest byte-for-byte.
    - No failure leaves a mixed old/new generation, deletes the prior good baseline, or retains ambiguous staging/backup directories.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs --max-failures 1</automated>
  </verify>
  <done>Baseline provenance is truthful and canonical publication is proven atomic under every modeled filesystem failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish and document two separately validated review artifacts</name>
  <reversibility rating="costly">Workflow fields, artifact names, and runbook commands are operational contracts that must remain synchronized with bundle and packet validation.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
    - .planning/phases/136-catalog-visual-quality/136-REVIEW-FIX.md
    - .planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md
    - .github/workflows/catalog-evidence.yml
    - .github/workflows/CATALOG-EVIDENCE.md
    - dev/rendro/catalog_evidence_bundle.ex
    - dev/rendro/catalog_visual_gallery.ex
    - dev/mix/tasks/rendro/catalog/gallery.ex
    - test/docs_contract/catalog_evidence_runbook_test.exs
    - test/guardrails/required_checks_contract_test.exs
  </read_first>
  <files>.github/workflows/catalog-evidence.yml, .github/workflows/CATALOG-EVIDENCE.md, test/docs_contract/catalog_evidence_runbook_test.exs, test/guardrails/required_checks_contract_test.exs</files>
  <behavior>
    - "Review/canonical jobs assemble fields and real shapes accepted by the semantic validator, including canonical `.cells | length == 32`."
    - "One review run uploads an authoritative closed evidence artifact and a distinct authority-none eight-image packet artifact with unique IDs/names/URLs/digests and the same candidate/control/run/attempt/renderer facts."
    - "The runbook derives the actual run attempt, downloads both artifacts freshly, validates the evidence bundle first, then verifies all eight packet roles/paths/hashes against it."
    - "Attempt 2+ and malformed renderer/role/packet input fail with stable diagnostics before image use."
  </behavior>
  <action>Update workflow payload assembly to emit every semantic identity required by Plan 11 using checked-out candidate/control inputs and pinned renderer outputs. Correct candidate/canonical gates to their real ordered `cells` shape and keep final/preset `images` only where those payloads use it. After full-size final-review rasters exist, invoke `mix rendro.catalog.gallery --candidate-manifest PATH --final-manifest PATH --output DIR` from the committed candidate so the packet contains exactly the two named controls plus six targets in D-21 order. Upload the closed evidence bundle and reviewer packet as distinct artifacts with names containing candidate SHA, run ID, and `github.run_attempt`; keep the packet outside the bundle and mark it authority-none. Pass run attempt consistently through both manifests and artifact lookup. Rewrite the runbook to query the actual attempt, resolve exactly one evidence artifact and one packet artifact by API metadata, record each artifact ID/name/API URL/provider digest, download each into a distinct new directory, compute each archive SHA-256, validate the closed bundle from the detached control checkout, then run packet validation against that bundle before opening images. Document the machine-readable complete/unavailable receipt fields consumed by Plans 12-13 and the three-attempt Revision Gate. Update contract tests for attempt 1 and 2+, exact artifact separation, exact eight roles including Invoice-light/Statement-light controls, real canonical cells, malformed renderer pin, and committed Mix task discovery/execution. Preserve all user-owned edits while changing the current six-image convenience contract to the exact eight-image reviewer packet.</action>
  <acceptance_criteria>
    - Workflow syntax parses and its review/canonical sources satisfy the closed semantic contracts.
    - Candidate and canonical use `.cells | length == 32`; final and preset keep their real image collection shape.
    - One review run exposes exactly one closed evidence artifact and one distinct eight-image reviewer packet artifact, each independently named and digested.
    - Runbook tests cover attempt 1 and attempt 2+, both fresh downloads and archive digests, detached-control bundle validation, packet binding, and the exact eight D-21 paths.
    - Bad renderer-pin JSON/key/type/hash or artifact/packet ambiguity produces a controlled failure before any image opens.
  </acceptance_criteria>
  <verify>
    <automated>mix help rendro.catalog.gallery &gt;/dev/null &amp;&amp; mix test test/docs_contract/catalog_evidence_runbook_test.exs test/guardrails/required_checks_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The live workflow and runbook expose two distinct, exact-run artifacts and require evidence-first eight-image packet validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git history -> baseline provenance | A commit label is trusted only after its exact manifest blob is compared. |
| Candidate/control checkout -> provider artifacts | Exact source identities cross into remotely retained archives. |
| Staging -> canonical assets | Publication crosses an all-or-nothing durable-state boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-E0 | Spoofing | run/commit/artifact identity | high | mitigate | Full SHAs, run/attempt, distinct artifact IDs/URLs, provider digests, and archive hashes must agree. |
| T-136-E1 | Repudiation | baseline ownership | high | mitigate | Git blob bytes must equal the checked-in baseline before its owning full SHA is recorded. |
| T-136-E2 | Tampering | reviewer packet publication | high | mitigate | Workflow creates exact eight roles separately; runbook rebinds them to validated evidence before viewing. |
| T-136-E3 | Denial of service | malformed pin/JSON | medium | mitigate | Decode/type failures return structured errors and contract tests pin diagnostics. |
| T-136-E4 | Elevation of privilege | packet authority | high | mitigate | Separate upload, authority-none manifest, and evidence-first validation prevent packet promotion. |
| T-136-E5 | Tampering | canonical transaction | high | mitigate | Failure-injected two-artifact transaction tests prove complete rollback before backups are removed. |
| T-136-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs. |
</threat_model>

## Scope calibration

Estimate calibration reports factor 1 with zero samples and `low` confidence. This two-task operational slice owns six files with no overlap with Plan 11: the tracer covers the two-file Git/transaction path, and the expansion covers the four-file workflow/runbook contract.

<verification>
- `mix test test/rendro/catalog_test.exs test/docs_contract/catalog_evidence_runbook_test.exs test/guardrails/required_checks_contract_test.exs --max-failures 1`.
- `mix help rendro.catalog.gallery` succeeds from the candidate checkout.
- `mix format --check-formatted` and `mix ci.fast`.
</verification>

<success_criteria>
- Baseline and canonical commit identities are provably truthful.
- Canonical publication rollback preserves the prior generation under every injected failure.
- One exact run publishes separately digestible authoritative evidence and authority-none eight-image packet artifacts.
- Workflow/runbook shapes, attempts, identities, and validation order match the Plan 11 contracts.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-14-SUMMARY.md` when done.
</output>
