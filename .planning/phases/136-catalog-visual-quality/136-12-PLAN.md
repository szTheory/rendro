---
phase: 136-catalog-visual-quality
plan: "12"
type: execute
wave: 9
depends_on: ["136-11"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [CATALOG-10, CATALOG-11, CATALOG-12, CATALOG-13]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh candidate is built only from a clean committed exact SHA containing Plans 09-11, with the real exact six/26 partition, pinned renderer, truthful baseline owner, and all deterministic tests green."
    - "Publishing the candidate ref and creating provider-side workflow history requires explicit one-way authorization; no force-push, mutable branch inference, or guessed future identifier is allowed."
    - "Per D-20, the exact successful workflow artifact is downloaded by API identity and archive digest, then semantically validated from a fresh detached trusted control checkout before any image is opened."
    - "The durable 136-12 summary records one unambiguous candidate SHA, control ref/SHA, run ID/attempt, artifact URL/name/digest, renderer version/hash, role identities, and validated full-size paths—or an exact unavailable reason with all targets unreviewed."
    - "Per D-26, generation and validation are deterministic completion lanes; this plan neither requests nor invents human visual scores."
  artifacts:
    - path: .planning/phases/136-catalog-visual-quality/136-12-SUMMARY.md
      provides: "Durable exact-run review handoff or truthful unavailable receipt"
      contains: "candidate_sha"
  key_links:
    - from: .planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md
      to: .github/workflows/catalog-evidence.yml
      via: "the committed repaired implementation is addressed by exact candidate SHA"
      pattern: "commit|candidate"
    - from: GitHub workflow artifact
      to: .planning/phases/136-catalog-visual-quality/136-12-SUMMARY.md
      via: "API URL/digest and detached-control semantic validation are recorded exactly"
      pattern: "run_id|run_attempt|artifact"
  prohibitions:
    - statement: "No dirty worktree snapshot, moving branch name, force-pushed ref, stale Phase 130/136 run, partial bundle, or convenience gallery may become review authority."
      status: unverified
      flagged: true
    - statement: "No image may be opened and no reviewer judgment may be requested before the exact artifact passes trusted semantic validation."
      status: unverified
      flagged: true
---

<objective>
Produce one fresh, exact-SHA, pinned-renderer, semantically validated review artifact for the repaired six targets.

Purpose: Cross the authorized external boundary only after deterministic closure, satisfying D-20/D-26 and creating the sole durable handoff for independent review.
Output: A uniquely identified validated review bundle receipt, or a truthful unavailable outcome that leaves every target unpromoted.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
@.planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md
@.github/workflows/catalog-evidence.yml
@.github/workflows/CATALOG-EVIDENCE.md
@dev/rendro/catalog_evidence_bundle.ex
</context>

## Artifacts this phase produces

- No tracked source, test, workflow, quality, or canonical asset file is created or modified.
- One dedicated non-force remote candidate ref and one provider workflow run/artifact may be created only after Task 1 authorization.
- `136-12-SUMMARY.md` is the only tracked execution artifact. It records a complete validated review receipt or an exact unavailable/declined reason; it contains no score or visual judgment.
- No new symbol, public API, dependency, database/schema ORM, external API integration, catalog cell, rubric score, or canonical publication is created.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize exact candidate publication and workflow history</name>
  <reversibility rating="one-way">A deleted ref cannot guarantee erasure of the published Git object, provider logs, workflow metadata, or artifact retention.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .github/workflows/CATALOG-EVIDENCE.md
    - .github/workflows/catalog-evidence.yml
  </read_first>
  <files>None; this checkpoint authorizes Task 2 remote side effects.</files>
  <action>First establish the narrowly unavoidable prerequisite for the tracer: require a clean tracked worktree for all Phase 136 implementation/evidence paths while preserving unrelated user-owned changes, run the complete deterministic verification from Plans 09-11, and create or identify one immutable local commit containing exactly the repaired implementation. Resolve its full 40-character SHA; show commit metadata and a scoped diff proving the workflow and validator are included. Query the default branch and proposed dedicated `refs/heads/gsd/phase-136-candidate-{short_sha}` with `git ls-remote`; require one full control SHA and require the candidate ref to be absent or already equal to the exact candidate. Present the provider-retention consequence immediately before any push/dispatch. Do not use force, update the default branch, open images, or create workflow history at this checkpoint.</action>
  <decision>Whether to publish this exact repaired candidate at its dedicated non-force ref and create a fresh workflow run/artifact.</decision>
  <context>Remote publication is required for the D-20 evidence path but is not fully reversible; deterministic local completion remains valid if authorization is declined.</context>
  <options>
    <option id="publish-fresh-candidate">
      <name>Publish fresh candidate</name>
      <pros>Creates the exact-SHA review evidence needed for the six independent reviews.</pros>
      <cons>Exposes the Git object and creates durable provider-side history.</cons>
    </option>
    <option id="stop-unpromoted">
      <name>Stop unpromoted</name>
      <pros>Creates no external state and preserves deterministic repair evidence.</pros>
      <cons>Leaves visual review and canonical publication incomplete.</cons>
    </option>
  </options>
  <resume-signal>Select `publish-fresh-candidate` to continue, or `stop-unpromoted` to record a truthful declined outcome.</resume-signal>
  <acceptance_criteria>
    - The exact local candidate resolves as a full commit and contains the repaired workflow/validator plus passing Plan 09-11 verification.
    - The exact default-branch control ref/SHA is recorded independently.
    - The dedicated candidate ref is absent or already equal; any conflicting value stops without force-push.
    - No remote mutation occurs before explicit authorization.
  </acceptance_criteria>
  <verify>
    <automated>mix ci.fast &amp;&amp; git diff --check</automated>
  </verify>
  <done>The developer authorizes the exact one-way publication or declines it with all visual targets remaining unpromoted.</done>
</task>

<task type="tracer">
  <name>Task 2: Trace one fresh target from exact commit to trusted validated artifact</name>
  <precondition>Task 1 selected `publish-fresh-candidate`; otherwise write the declined receipt in the summary and do not execute this task.</precondition>
  <reversibility rating="one-way">The non-force push and workflow dispatch create remotely retained Git/workflow/artifact state.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-11-SUMMARY.md
    - .github/workflows/CATALOG-EVIDENCE.md
    - .github/workflows/catalog-evidence.yml
    - dev/rendro/catalog_evidence_bundle.ex
    - priv/pdfium_pin.json
  </read_first>
  <files>None; remote evidence and the plan summary are the outputs.</files>
  <action>Push only the authorized exact candidate SHA to the preflighted dedicated ref using a non-force full refspec, fetch it back, and require remote equality. Dispatch the review operation with the full candidate SHA and independently resolved default-branch control ref/SHA. Capture the actual run ID and positive attempt from the provider API; wait for that exact attempt and require success. Locate exactly one artifact whose name, workflow run, attempt, candidate identity, and API URL match the contract; record its API digest, download through the API-provided URL into a unique `mktemp -d` path, and require the computed archive digest to match before extraction. Create another unique detached worktree at the independently fetched control SHA, assert its HEAD immediately before validation, install only locked dependencies, and invoke `Rendro.CatalogEvidenceBundle.validate/3` from that control checkout against the extracted review bundle. Require exact operation, candidate/control commits, run/attempt, renderer pin, 32/6/26/12/4/12 semantic roles, paths, complete digests, and all cross-payload identities. Only after `:ok`, identify the exact eight full-size family-paired paths for D-21 and record their bound PDF/PNG identities without opening them. Trace at least one named target end-to-end in the receipt. On auth, timeout, run failure, expiration, ambiguity, mismatch, or validation failure, record the exact reason/observed identity/next action and keep all targets unreviewed; never substitute a nearby run.</action>
  <acceptance_criteria>
    - Remote candidate ref equals the authorized SHA and default control ref resolves independently to one full SHA.
    - Exactly one successful run attempt and exactly one matching artifact are identified by API metadata and archive digest.
    - Validation runs from an asserted detached trusted control checkout and returns `:ok` for the complete semantic/cross-payload contract.
    - The receipt records all exact identities plus eight locked-order, hash-bound full-size paths without scores or visual claims.
    - Any failure yields a precise unavailable receipt and no image is opened or promoted.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/136-catalog-visual-quality/136-12-SUMMARY.md &amp;&amp; rg -q 'candidate_sha|unavailable|declined' .planning/phases/136-catalog-visual-quality/136-12-SUMMARY.md</automated>
  </verify>
  <done>One fresh exact run is durably available for review, or the precise external failure/decline is recorded without overstating completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local commit -> remote ref | An immutable local object becomes externally retained state. |
| Provider API/artifact -> local review intake | Run metadata and archive bytes remain untrusted until independently identified and validated. |
| Candidate bundle -> human viewing | No image crosses into advisory judgment before trusted semantic validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-C0 | Spoofing | candidate/control/run/artifact | high | mitigate | Full SHAs, exact refs, API run/attempt/URL, and archive digest must all agree. |
| T-136-C1 | Tampering | downloaded bundle | high | mitigate | API digest plus detached-control semantic validation precede extraction use/viewing. |
| T-136-C2 | Repudiation | external side effects | high | mitigate | Blocking one-way decision records authorization immediately before non-force push/dispatch. |
| T-136-C3 | Information disclosure | remote publication | medium | mitigate | Publish only the exact scoped commit/ref after explicit authorization; never default branch or force. |
| T-136-C4 | Denial of service | provider/auth/timeout | medium | accept | Record precise unavailable state and next action; deterministic repair stays complete and unpromoted. |
| T-136-C5 | Elevation of privilege | stale/nearby artifact | high | mitigate | Exact run attempt/artifact identity is required; ambiguity stops rather than falls back. |
| T-136-SC | Tampering | package supply chain | low | accept | No new package is installed; detached validation uses the lockfile and existing dependency policy. |
</threat_model>

## Spec-less probe resolution

- CATALOG-10 exact scope and precision are revalidated at the external evidence boundary.
- CATALOG-11 deterministic behavior is a required preflight from Plans 09-11.
- CATALOG-12 is explicitly unresolved until human review; this plan creates no score.
- CATALOG-13 empty/ambiguity/order/identity boundaries fail to an unavailable receipt rather than selecting a nearby artifact.
- Prohibition recall retained dirty/mutable/stale authority and pre-validation viewing prohibitions above; remote spoofing/tampering route to STRIDE.

<verification>
- Before authorization: `mix ci.fast && git diff --check`.
- After a run: re-run the documented detached-control `Rendro.CatalogEvidenceBundle.validate/3` command and require `:ok`.
- Inspect `136-12-SUMMARY.md` for exactly one complete receipt or one explicit unavailable/declined outcome.
</verification>

<success_criteria>
- The evidence source is a clean exact repaired commit, not the dirty working tree.
- One authorized non-force run is identified and validated exactly, or failure remains explicit.
- No visual score, approval, or promotion is inferred from workflow success.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-12-SUMMARY.md` when done.
</output>
