---
phase: 136-catalog-visual-quality
plan: "09"
type: execute
wave: 7
depends_on: ["136-08"]
files_modified:
  - dev/rendro/catalog.ex
  - lib/rendro/recipes/ticket.ex
  - priv/quality/rubric_scores.json
  - priv/schemas/rubric_scores.schema.json
  - test/fixtures/quality/rubric_scores_phase130_non_prose.json
  - test/rendro/catalog_test.exs
  - test/rendro/recipes/ticket_test.exs
  - test/docs_contract/rubric_manifest_contract_test.exs
autonomous: true
gap_closure: true
requirements: [CATALOG-10, CATALOG-12]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01, rendering the real ordered 32-cell catalog against the committed baseline yields exactly the six locked target IDs as changed and the other 26 as byte-stable; five, seven, a reordered set, or either one-cell boundary fails."
    - "Per D-17 and D-19, Brutalist Ticket light and dark receive one intentional profile-gated rendered treatment that preserves the one-row GA | H | 24 | B geometry and makes both target PDFs genuinely differ without synthetic hash mutation."
    - "Exact changed-target membership is derived from PDF/PNG byte identity and the D-01 allowlist, independently of scored/unscored human-review state; descriptive review state cannot block candidate generation per D-26."
    - "CATALOG-10 precision is exact complete-hash equality with stable manifest ordering; no threshold, rounding, perceptual comparison, prefix match, or manufactured drift can classify a change."
    - "The catalog remains exactly 32 ordered cells and exactly 20 explicitly unscored records per D-23."
    - "Every dark disposition explicitly represents print_safety:false: scored records retain it in gate_results, while dark unscored records carry a schema-validated non-review scope-boundary field without acquiring scores, reading-order judgment, passed, or approval."
    - "Assumption (unclassified CATALOG-12 probe): an explicit print-safety boundary on an unscored record is non-visual scope metadata, not a reviewer score or promotion; the schema keeps those authorities separate."
  artifacts:
    - path: lib/rendro/recipes/ticket.ex
      provides: "Profile-gated rendered Ticket locator-header treatment for both locked Ticket targets"
      contains: "locator_table_opts"
    - path: dev/rendro/catalog.ex
      provides: "Reviewer-independent exact changed-target scope validation"
      contains: "changed_targets"
    - path: test/rendro/catalog_test.exs
      provides: "Real 32-cell source-PDF baseline partition and advisory-state independence regressions"
      contains: "real catalog"
    - path: priv/schemas/rubric_scores.schema.json
      provides: "Explicit dark-unscored print-safety scope-boundary contract"
      contains: "print_safety"
    - path: priv/quality/rubric_scores.json
      provides: "Seven formerly implicit dark-unscored print_safety:false values"
      contains: "print_safety"
  key_links:
    - from: dev/rendro/catalog.ex
      to: assets/rendro/catalog.json
      via: "real rendered source-PDF and PNG hashes are compared in canonical order"
      pattern: "changed_targets|byte_stable"
    - from: dev/rendro/catalog.ex
      to: priv/quality/rubric_scores.json
      via: "review_status remains descriptive and does not determine the exact change set"
      pattern: "review_status"
    - from: priv/quality/rubric_scores.json
      to: priv/schemas/rubric_scores.schema.json
      via: "dark unscored scope metadata validates without reviewer-owned score fields"
      pattern: "print_safety"
  prohibitions:
    - statement: "No control cell, catalog membership, recipe family, preset, public API, or global dark token may be changed to make the six-count pass."
      status: unverified
      flagged: true
    - statement: "No dark record may be described as print-safe, accessible, PDF/UA conformant, or universally viewer-safe from this screen-oriented repair."
      status: unverified
      flagged: true
---

<objective>
Close the false-positive catalog scope and dark-boundary gaps with real rendered behavior: both Ticket targets change intentionally, exact target scope no longer depends on reviewer state, and all dark records carry explicit non-print-safe truth.

Purpose: Restore deterministic trust before any new evidence run, implementing D-01, D-17, D-19, D-23, and D-26 without widening the six-cell blast radius.
Output: A real exact-six/26 partition, reviewer-independent candidate scope, and schema-valid explicit dark print-safety representation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
@.planning/phases/136-catalog-visual-quality/136-REVIEW.md
@.planning/phases/136-catalog-visual-quality/136-08-SUMMARY.md
@dev/rendro/catalog.ex
@lib/rendro/recipes/ticket.ex
@assets/rendro/catalog.json
@priv/quality/rubric_scores.json
</context>

## Artifacts this phase produces

- `lib/rendro/recipes/ticket.ex` adds private helper `locator_table_opts/3` (or an equivalently narrow private helper) that emits the existing borders/columns options and adds a semantic header fill only when `locator_layout: :atomic_equal_share` is active; omitted-profile behavior remains byte-identical.
- `dev/rendro/catalog.ex` adds deterministic `diff.changed_targets` in canonical order and changes candidate validation so this exact byte-derived list—not reviewer disposition—must equal the D-01 six-ID allowlist. Existing `changed_scored`/`changed_unscored` values may remain descriptive, but their union/order must equal `changed_targets`.
- `priv/schemas/rubric_scores.schema.json` adds a closed boolean `print_safety` property permitted and required only for unscored dark catalog dispositions; it remains forbidden as a substitute for scored `gate_results`.
- `priv/quality/rubric_scores.json` and `test/fixtures/quality/rubric_scores_phase130_non_prose.json` add `print_safety: false` to the seven dark unscored records that currently omit it.
- Tests add real-render exact-six/26, scored/unscored independence, Ticket treatment, and explicit dark-record schema regressions.
- No new public function, dependency, renderer, recipe, preset, catalog cell, score, review judgment, theme token, database/schema ORM, or external API integration is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the real Ticket light target through exact reviewer-independent candidate scope</name>
  <reversibility rating="reversible">The target-only Ticket table option and candidate diff field are dev/private changes removable by reverting two source files and their focused tests.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
    - .planning/phases/136-catalog-visual-quality/136-REVIEW.md
    - dev/rendro/catalog.ex
    - lib/rendro/recipes/ticket.ex
    - assets/rendro/catalog.json
    - test/rendro/catalog_test.exs
    - test/rendro/recipes/ticket_test.exs
    - .planning/debug/invalid-candidate-scope-136.md
  </read_first>
  <files>dev/rendro/catalog.ex, lib/rendro/recipes/ticket.ex, test/rendro/catalog_test.exs, test/rendro/recipes/ticket_test.exs</files>
  <behavior>
    - "A real render of all 32 catalog specs yields the exact six D-01 target source-PDF changes and 26 exact controls in canonical order."
    - "The Ticket light target changes because the existing atomic profile adds a rendered semantic locator-header treatment, not because a test substitutes hashes or edits the fixture."
    - "Changing one target disposition between scored and unscored changes only descriptive review state; candidate scope remains valid and `changed_targets` remains the exact six."
    - "Removing one real target change or changing one control PDF/PNG hash returns :invalid_candidate_scope."
  </behavior>
  <action>Write the failing real-render regression first. Render every `Rendro.Catalog.catalog_specs/0` source PDF deterministically, compare its SHA-256 with the same ID in committed `assets/rendro/catalog.json`, and assert the exact canonical six/26 partition per D-01; do not mutate hashes in this test. In `Rendro.Recipes.Ticket`, keep the existing single equal-share row and atomic `Rendro.Cell` wrappers, but add a profile-gated rendered header treatment using an existing semantic surface/rule role for both light and dark targets per D-17/D-19. The helper must omit the new table option entirely for absent/unrelated profiles so default bytes remain frozen. In `Rendro.Catalog.candidate_manifest/6`, derive a canonical `changed_targets` list solely from complete PDF/PNG inequality and require it to equal the six allowlisted IDs; retain reviewer status only as descriptive cell/bucket metadata and validate any descriptive buckets by exact ordered union, never as the scope authority per D-26. Add a regression with one target disposition changed to unscored that still generates candidate evidence, plus missing/extra/reordered and one-control boundary cases. Preserve the current dirty gallery/evidence work in unrelated files and do not reformat it.</action>
  <acceptance_criteria>
    - The real 32-source-PDF test reports the six locked IDs, including `ticket--aurora-live--brutalist--light`, and exactly 26 controls in manifest order.
    - Ticket light/dark profile documents keep Section/GA, Row/H, Seat/24, Gate/B order, one row, equal-share columns, atomic cells, existing sizes, and identical geometry; omitted profile matches its prior deterministic bytes.
    - Candidate `diff.changed_targets` equals the six allowlisted IDs regardless of any target's scored/unscored disposition, while candidate records still expose truthful descriptive review status.
    - A five-target partition, a seven-target partition, any reorder, one changed control PDF hash, or one changed control PNG hash returns `{:error, :invalid_candidate_scope}`.
    - Candidate generation contains no reviewer score, approval, `passed`, justification, or inferred visual result.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs test/rendro/recipes/ticket_test.exs --max-failures 1</automated>
  </verify>
  <done>The real Ticket light bytes participate in a truthful exact-six slice, and deterministic candidate scope remains valid even when human review status is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode explicit non-print-safe truth for every dark disposition</name>
  <reversibility rating="costly">The rubric schema is a durable evidence contract read by current tooling; reverting requires coordinated rollback of the manifest, snapshot fixture, and every schema/contract reader.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-VERIFICATION.md
    - priv/schemas/rubric_scores.schema.json
    - priv/quality/rubric_scores.json
    - test/fixtures/quality/rubric_scores_phase130_non_prose.json
    - test/rendro/catalog_test.exs
    - test/docs_contract/rubric_manifest_contract_test.exs
  </read_first>
  <files>priv/schemas/rubric_scores.schema.json, priv/quality/rubric_scores.json, test/fixtures/quality/rubric_scores_phase130_non_prose.json, test/rendro/catalog_test.exs, test/docs_contract/rubric_manifest_contract_test.exs</files>
  <behavior>
    - "Every dark scored disposition has `gate_results.print_safety: false`; every dark unscored disposition has explicit top-level `print_safety: false`."
    - "An unscored dark record missing this value, setting it true, or carrying visual scores/gates/passed fails schema and contract validation."
    - "Light unscored records do not acquire an invented print judgment, and the catalog remains 32/20."
  </behavior>
  <action>Update the catalog-disposition schema and tests before data. For the conjunction `review_status: unscored` and `mode: dark`, require one top-level boolean `print_safety` whose only accepted value is false; keep `dimension_scores`, reviewer `gate_results`, `passed`, signer fields, and justifications prohibited for unscored records. Keep scored records' existing `gate_results.print_safety` contract unchanged. Add the field to exactly the seven affected dark unscored dispositions in both the durable rubric and its historical non-prose fixture, preserving all existing IDs, hashes, reasons, dates, order, and the exact count of 20 unscored records per D-23. Change `dark_print_safety?/1` and catalog tests to require the explicit branch rather than accepting a reason as a default. Add negative schema cases for missing/true/string values and for an unscored record that attempts to add reviewer-owned visual fields. Record the CATALOG-12 probe assumption explicitly: this value is a scope boundary only and does not constitute review or promotion.</action>
  <acceptance_criteria>
    - Schema validation passes for the checked-in rubric and historical non-prose fixture with exact pre-existing values plus the seven false booleans.
    - Every dark disposition has an explicit false value in its authorized location; no reason-only branch passes.
    - Missing, true, string, or null print-safety on a dark unscored record fails, as does adding dimension scores, gate results, passed, or signer evidence to an unscored record.
    - Exactly 32 dispositions remain, exactly 20 remain unscored, and all light records retain their prior values.
    - No print, accessibility, PDF/UA, or viewer-support claim is added.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>Every dark catalog record now states the screen-only non-print-safe boundary explicitly without turning unscored records into reviewed or promoted records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Real catalog render -> candidate diff | Exact PDF/PNG bytes determine the only allowed changed set. |
| Rubric disposition -> candidate metadata | Advisory reviewer state may describe a record but cannot authorize deterministic generation. |
| Rubric JSON -> schema/quality projection | Scope-boundary metadata must not masquerade as human visual judgment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-90 | Tampering | real six-target scope | high | mitigate | Render all 32 real specs, compare complete hashes, and assert exact six/26 ordered membership with one-item boundary mutations. |
| T-136-91 | Elevation of privilege | reviewer status in candidate classifier | high | mitigate | Make byte-derived `changed_targets` authoritative and test scored/unscored mutations without changing eligibility. |
| T-136-92 | Repudiation | Ticket light change | medium | mitigate | Profile-gated rendered table treatment plus real baseline comparison proves the delta is intentional and reviewable. |
| T-136-93 | Information disclosure / misrepresentation | dark print boundary | high | mitigate | Closed schema requires explicit false while prohibiting review-bearing fields on unscored records. |
| T-136-94 | Denial of service | 32-cell render test | low | accept | The catalog is a fixed 32-item bounded dev/test corpus and uses the existing deterministic renderer path. |
| T-136-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs. |
</threat_model>

## Spec-less probe resolution

- CATALOG-10 boundary is explicit in Task 1: exact six and 26, with one fewer, one extra, reorder, and one-control mutations.
- CATALOG-10 precision is explicit in Task 1: complete deterministic PDF/PNG SHA-256 equality, never approximate classification.
- The unclassified CATALOG-12 row remains visible as the scope-metadata assumption in `must_haves.truths` and Task 2; it is not auto-dismissed.
- Prohibition recall retained scope/claim integrity above as descriptor-less flagged items. Generic path/injection/package concerns route to STRIDE and were not minted as project-specific prohibitions.

<verification>
- `mix test test/rendro/catalog_test.exs test/rendro/recipes/ticket_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1`
- `mix format --check-formatted`
- `mix ci.fast`
</verification>

<success_criteria>
- Real catalog bytes prove exactly six changed targets and 26 controls without synthetic mutation.
- Candidate scope is independent of human scored/unscored state.
- The Ticket light target has an intentional bounded visual delta while its geometry and default path stay frozen.
- All dark dispositions explicitly retain `print_safety:false`, 32/20 counts, and no broader claim.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-09-SUMMARY.md` when done.
</output>
