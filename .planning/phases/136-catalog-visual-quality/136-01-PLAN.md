---
phase: 136-catalog-visual-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/rendro/catalog.ex
  - lib/rendro/recipes/invoice.ex
  - test/rendro/catalog_test.exs
  - test/rendro/recipes/invoice_test.exs
  - test/rendro/recipes/invoice_opts_threading_test.exs
autonomous: true
requirements: [CATALOG-10, CATALOG-12]
must_haves:
  truths:
    - "Candidate classification equals the locked six-ID set exactly; extra, missing, duplicated, or reordered target selection fails."
    - "Target scoping uses exact catalog-ID equality; substring, family, preset, brand, or neighboring-record matches are forbidden."
    - "Empty/absent target-profile selection fails because all six targets must be selected; the remaining catalog still renders through the default path."
    - "Changed-ID partition uses manifest order and equals the ordered six-ID list; controls preserve existing order."
    - "Scope is exact set/count arithmetic: six changed and 26 byte-identical controls out of 32, with no tolerance, rounding, or approximate visual-diff classification."
    - "Exactly 32 records, exactly 20 explicitly unscored, and every dark record print_safety:false; one-count deviations fail."
    - "Dark screen-readable contrast and visual-target success remain separate from accessibility, PDF/UA, viewer-support, and print-safety claims."
    - "No record omits scored/unscored disposition or dark print_safety; absence is not a passing default."
    - "Canonical manifest order remains stable; unchanged records remain byte-identical; only six locked records may receive updated evidence."
    - "Counts/booleans are exact typed values: 32 total, 20 unscored, dark print_safety false."
  artifacts:
    - path: dev/rendro/catalog.ex
      provides: "Dev-only literal target map and generic private presentation-profile threading"
      contains: "@visual_target_profiles"
    - path: lib/rendro/recipes/invoice.ex
      provides: "First production recipe consumer of generic semantic presentation data"
      contains: "presentation_profile"
    - path: test/rendro/catalog_test.exs
      provides: "Exact ordered six/26 classification and target-selection negative controls"
      contains: "invoice--cedar-mutual--corporate-classic--dark"
    - path: test/rendro/recipes/invoice_opts_threading_test.exs
      provides: "Profile omission and no-theme byte-stability contract"
      contains: "sections(data) equals sections(data, [])"
  key_links:
    - from: dev/rendro/catalog.ex
      to: lib/rendro/recipes/invoice.ex
      via: "source_document_for/1 appends only generic `presentation_profile:` data before document/2"
      pattern: 'presentation_profile'
    - from: dev/rendro/catalog.ex
      to: test/rendro/catalog_test.exs
      via: "candidate_manifest/6 retains ordered PDF-and-PNG candidate_status/3 partitioning"
      pattern: 'changed_scored|byte_stable'
    - from: lib/rendro/recipes/invoice.ex
      to: test/rendro/recipes/invoice_opts_threading_test.exs
      via: "omitted/empty profile and no-theme paths retain the pre-phase sections and bytes"
      pattern: 'sections\(data.*\[\]'
  prohibitions:
    - statement: "The repair must not add a public API or dependency, and catalog IDs, brands, presets, or phase names must not leak into core recipe logic."
      status: unverified
      flagged: true
    - statement: "The repair must not use raster overlays, PDF post-processing, or a second renderer."
      status: unverified
      flagged: true
---

<objective>
Prove the Phase 136 architecture end to end with an exact dev-only six-ID selector, generic private profile threading, one production Invoice semantic-label path, and fail-closed six/26 classification.

Purpose: Establish D-01 through D-05 as executable compatibility boundaries before the three recipe expansions run in parallel.
Output: A dev-only `@visual_target_profiles` map, generic `:presentation_profile` option flow, one target-only Invoice semantic treatment, and deterministic classification/byte-stability tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-RESEARCH.md
@.planning/phases/136-catalog-visual-quality/136-PATTERNS.md
@.planning/phases/136-catalog-visual-quality/136-VALIDATION.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/135-test-ci-cd-simplification/135-03-SUMMARY.md
@dev/rendro/catalog.ex
@lib/rendro/recipes/invoice.ex
</context>

## Artifacts this phase produces

- `dev/rendro/catalog.ex` adds private module attribute `@visual_target_profiles`, keyed in canonical manifest order by the exact six locked IDs, plus private helper `presentation_profile/1` returning one generic map or `nil` and private helper `recipe_options/2` that preserves `[theme: theme, catalog_layout: true]` while appending `presentation_profile:` only for targets.
- The generic private profile keys are `:semantic_ink`, `:ledger_layout`, and `:locator_layout`; this tracer consumes only `semantic_ink: :primary_secondary` in Invoice. These keys are undocumented implementation data, not public API.
- `test/rendro/catalog_test.exs` adds exact-order target-map, exact six-changed/26-byte-stable, and extra/missing/duplicate/reordered/empty selection controls.
- `test/rendro/recipes/invoice_test.exs` and `invoice_opts_threading_test.exs` add tracer activation, semantic primary label, omitted-profile, empty-profile, unrelated-theme, and byte-identity assertions.
- No new file, dependency, renderer, public function, theme token, fixture, catalog record, score field, database/schema, API coverage artifact, or external integration is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one exact catalog target into a generic Invoice semantic-label render</name>
  <reversibility rating="reversible">The catalog profile thread and the single Invoice consumer are independently removable with a normal Git revert; rollback criterion is restoration of the pre-phase no-profile bytes and removal of `:presentation_profile` from the dev call boundary.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - dev/rendro/catalog.ex
    - lib/rendro/recipes/invoice.ex
    - lib/rendro/recipes/palette.ex
    - lib/rendro/recipes/table_cell.ex
    - test/rendro/catalog_test.exs
    - test/rendro/recipes/invoice_test.exs
    - test/rendro/recipes/invoice_opts_threading_test.exs
  </read_first>
  <files>dev/rendro/catalog.ex, lib/rendro/recipes/invoice.ex, test/rendro/catalog_test.exs, test/rendro/recipes/invoice_test.exs, test/rendro/recipes/invoice_opts_threading_test.exs</files>
  <behavior>
    - "The Corporate Classic Invoice dark spec alone receives `%{semantic_ink: :primary_secondary}` from the dev-only exact-ID map during the tracer; every other spec receives no profile data."
    - "One functional Invoice label or table heading uses primary semantic ink through that generic value, while `Total Due` geometry/accent and caller data are unchanged."
    - "No-profile, empty-profile, no-theme, and unrelated-theme calls remain structurally and byte identical."
    - "The six exact catalog IDs appear in dev/rendro/catalog.ex and catalog tests, never in lib/rendro/recipes/*.ex."
  </behavior>
  <action>Write the failing contracts first. In `Rendro.Catalog`, add private `@visual_target_profiles` containing exactly the six D-01 IDs in canonical manifest order; each value is generic presentation data only. Add `presentation_profile/1` using exact map lookup and `recipe_options/2` that keeps the existing theme and `catalog_layout: true` options, appending `presentation_profile:` only when a target map exists. Update `source_document_for/1` to call the same recipe module `document/2` once with those options per D-02 and D-03. In `Rendro.Recipes.Invoice`, read only `opts[:presentation_profile][:semantic_ink]`; for the `:primary_secondary` value, route one functional label/header through existing `Palette.resolve/2` and `TableCell.content/5` semantic roles. Do not inspect an ID, brand, preset, mode, or phase in the recipe. Preserve every omitted/empty profile path exactly per D-05. Tests must scan core recipe sources for all six literal IDs, exercise the real catalog `source_document_for/1` path, and render twice for deterministic bytes.</action>
  <acceptance_criteria>
    - `@visual_target_profiles` has exactly six unique keys in the locked canonical order and exact equality is the only selection operation.
    - `source_document_for/1` retains one recipe `document/2` call and passes only generic profile data beyond the existing options.
    - Invoice consumes only `semantic_ink: :primary_secondary`; no target ID or catalog descriptor occurs anywhere under `lib/rendro/recipes/`.
    - The tracer changes a semantic text role only for Corporate Classic Invoice dark and retains its existing 8/10/12/18pt sizes, weight 400, leading 1.3, data, geometry, pagination, and blue `Total Due` accent.
    - Existing omitted/empty profile, no-theme, and unrelated-theme render assertions remain byte-identical across two renders.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_opts_threading_test.exs --max-failures 1</automated>
  </verify>
  <done>One real target identity is selected only in dev tooling, reaches Invoice as generic private data, changes one semantic render path, and proves the default paths and core identity isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock exact target selection and ordered six/26 byte classification</name>
  <reversibility rating="reversible">Contract-only classifier changes revert normally in Git; rollback criterion is the prior `candidate_manifest/6` behavior with all pre-existing catalog tests green.</reversibility>
  <read_first>
    - dev/rendro/catalog.ex
    - test/rendro/catalog_test.exs
    - assets/rendro/catalog.json
    - priv/quality/rubric_scores.json
    - test/docs_contract/catalog_quality_contract_test.exs
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
  </read_first>
  <files>dev/rendro/catalog.ex, test/rendro/catalog_test.exs</files>
  <behavior>
    - "The target selector rejects extra, missing, duplicated, reordered, substring, family, preset, brand, neighbor, and empty target definitions."
    - "candidate_manifest/6 classifies both source-PDF and PNG hash equality in manifest order as exactly six changed_scored, zero changed_unscored, and 26 byte_stable controls."
    - "All 32 records retain an explicit scored/unscored disposition, exactly 20 remain unscored, and every dark record retains typed `print_safety: false`."
    - "Candidate generation contains no reviewer score, approval, passed, or quality projection field."
  </behavior>
  <action>Extend `Rendro.CatalogTest` around existing `catalog_specs/0`, `candidate_manifest/6`, and private `candidate_status/3` behavior. Build expected IDs from the current ordered 32-cell manifest, partition them by the literal six target IDs, mutate both source-PDF and PNG hashes for those six only, and assert exact ordered lists: six `changed_scored`, empty `changed_unscored`, and the other 26 `byte_stable`. Add independent negative controls for one extra target, one missing target, a duplicated target, reordered targets, an empty selection, substring/family/preset/brand matches, a neighboring record, one changed control PDF hash, and one changed control PNG hash. Assert exact typed 32/20/false invariants and candidate reviewer-field absence per D-01, D-04, and D-05. Keep the production classifier's exact complete lowercase hashes and equality semantics unchanged; no approximate image comparison is introduced.</action>
  <acceptance_criteria>
    - Exact six/26 positive classification passes in canonical manifest order using both PDF and PNG hashes.
    - Every target-selection and changed-control mutation fails at the intended exact-set/order/hash assertion.
    - Catalog count is integer 32, unscored count is integer 20, and all dark `print_safety` values are boolean false with no missing dispositions.
    - The 26 control PDF and PNG hashes compare byte-for-byte to the pre-phase canonical manifest.
    - Candidate records remain free of reviewer-owned scores, approval, `passed`, and projected quality.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs --max-failures 1</automated>
  </verify>
  <done>The catalog deterministically rejects any target-set drift and proves exactly six changed records plus 26 ordered PDF/PNG byte-identical controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Catalog spec -> dev selector | Catalog IDs are trusted only inside dev tooling and must map by exact equality. |
| Dev selector -> recipe options | Identity-bearing input is reduced to generic private presentation data before core code. |
| Rendered cells -> candidate manifest | Source-PDF and PNG hashes cross exact ordered classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-01 | Tampering | target allowlist | high | mitigate | Literal ordered six-key map plus extra/missing/duplicate/reordered and neighboring-ID negative controls. |
| T-136-02 | Elevation of privilege | core recipe boundary | high | mitigate | Strip catalog identity before document/2; source scans and real render tests reject ID/brand/preset/phase leakage. |
| T-136-03 | Tampering | candidate diff | high | mitigate | Complete lowercase PDF and PNG hashes, exact equality, manifest order, and exact six/26 arithmetic. |
| T-136-04 | Repudiation | candidate authority | medium | mitigate | Candidate records contain generation identity only and no reviewer score, approval, or quality projection. |
| T-136-05 | Denial of service | 32-cell double render | low | accept | The catalog is fixed and bounded at 32 records; focused tests and existing render pipeline retain current limits. |
| T-136-AUTH | Spoofing / Session | runtime authentication | low | accept | N/A: this plan adds no runtime request, identity, authentication, or session surface. |
| T-136-SC | Tampering | package installs | low | accept | No npm, pip, cargo, Hex, or other dependency install is introduced. |

## Verification and Residual Risk

- Exact selector and hash-partition mutations are deterministic ASVS L1 controls.
- Human visual quality is not asserted in this plan; residual aesthetic judgment remains in Plan 05.
</threat_model>

## Spec-less probe fallback audit

- Edge coverage: CATALOG-10 items 1-5 and CATALOG-12 items 1-5 are lifted exactly once into this plan's truths; CATALOG-11 and CATALOG-13 are owned by Plan 05.
- Prohibition Stage 1 recalled public-surface growth, dependency growth, catalog identity leakage, renderer forks, raster/PDF patching, fixture manipulation, global token drift, collateral controls, generated scores, and compliance/full-pass overclaim.
- Stage 2 kept the two plan-relevant values/safety prohibitions descriptor-less and flagged/unverified. Routine null/error/hash mechanics are acceptance criteria; generic injection/path and supply-chain canon route to this STRIDE register and `$gsd-secure-phase`.

<verification>
- `mix test test/rendro/catalog_test.exs test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_opts_threading_test.exs --max-failures 1`
- `mix format --check-formatted`
- `mix ci.fast`
</verification>

<success_criteria>
- The exact six-ID map exists only in dev catalog tooling and recipes see only generic private presentation data.
- One Invoice semantic-label path works end to end while every default/no-theme/unrelated route retains its bytes.
- Candidate classification proves exactly six changed scored IDs, zero changed unscored IDs, and 26 ordered PDF/PNG byte-identical controls.
- The catalog remains exactly 32 records, 20 explicitly unscored, with every dark record `print_safety: false`.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md` when done.
</output>
