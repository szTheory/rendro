---
phase: 136-catalog-visual-quality
plan: "02"
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes/statement.ex
  - test/rendro/recipes/invoice_test.exs
  - test/rendro/recipes/statement_test.exs
autonomous: true
requirements: [CATALOG-11, CATALOG-12]
must_haves:
  truths:
    - "Invoice empty: catalog fixture validation rejects a missing invoice payload; Phase 136 never renders an empty-document substitute."
    - "Invoice loading: PDF generation is atomic and exposes no document loading state; only a completed PDF or an existing structured error reaches catalog tooling."
    - "Invoice error: existing recipe validation/render errors remain structured failures; Phase 136 does not emit a partial fallback PDF."
    - "Invoice populated: the existing Cedar Mutual fixture and exact item rows remain unchanged while only the private dark presentation profile changes semantic ink."
    - "Invoice partial: missing required invoice data remains an upstream recipe validation failure; optional facts retain their existing source behavior."
    - "Invoice zero/one/many: existing row-count behavior remains unchanged; the target profile introduces no collection padding or new empty copy."
    - "Statement empty: catalog fixture validation rejects a missing statement payload; Phase 136 never renders an empty-document substitute."
    - "Statement loading: PDF generation is atomic and exposes no document loading state; only a completed PDF or an existing structured error reaches catalog tooling."
    - "Statement error: existing recipe validation/render errors remain structured failures; Phase 136 does not emit a partial fallback PDF."
    - "Statement populated: the existing Signal Ledger fixture rows remain unchanged while only the private dark presentation profile changes semantic ink."
    - "Statement partial: missing required statement data remains an upstream recipe validation failure; optional facts retain their existing source behavior."
    - "Statement zero/one/many: existing row-count behavior remains unchanged; the target profile introduces no padding or new empty copy."
    - statement: "Held-out existing long Invoice facts and item content retain deterministic wrapping, geometry, and pagination under semantic-ink-only changes."
      verification: backstop
    - statement: "Existing long Invoice labels and facts retain deterministic measured wrapping without clipping or changing caller text."
      verification: backstop
    - statement: "Held-out existing long Statement ledger facts retain deterministic wrapping, geometry, and pagination under semantic-ink-only changes."
      verification: backstop
    - statement: "Existing long Statement descriptions and contextual facts retain deterministic measured wrapping without clipping or caller-text changes."
      verification: backstop
  artifacts:
    - path: lib/rendro/recipes/invoice.ex
      provides: "Complete generic primary/secondary Invoice semantic treatment with preserved Total Due anchor"
      contains: "presentation_profile"
    - path: lib/rendro/recipes/statement.ex
      provides: "Generic primary/secondary Statement ledger treatment with preserved Closing Balance anchor"
      contains: "presentation_profile"
    - path: test/rendro/recipes/invoice_test.exs
      provides: "Invoice palette, anchor, error, cardinality, long-content, and deterministic profile contracts"
      contains: "Total Due"
    - path: test/rendro/recipes/statement_test.exs
      provides: "Statement palette, anchor, error, cardinality, long-content, pagination, and deterministic profile contracts"
      contains: "Closing Balance"
  key_links:
    - from: lib/rendro/recipes/invoice.ex
      to: lib/rendro/recipes/palette.ex
      via: "generic profile resolves `:ink` for functional labels and `:muted` for support facts"
      pattern: 'Palette\.resolve'
    - from: lib/rendro/recipes/statement.ex
      to: lib/rendro/recipes/table_cell.ex
      via: "ledger headers and values use semantic roles without raw-black fallback"
      pattern: 'TableCell'
    - from: lib/rendro/recipes/statement.ex
      to: test/rendro/recipes/statement_test.exs
      via: "measured table/pagination and boxed Closing Balance remain intact under the profile"
      pattern: 'Closing Balance'
  prohibitions:
    - statement: "The repair must not retune global theme or preset tokens or change any of the 26 control cells."
      status: unverified
      flagged: true
---

<objective>
Complete target-scoped dark semantic contrast and hierarchy for Corporate Classic Invoice and Minimal Mono Statement without changing their geometry, data, fonts, pagination, default behavior, or screen-only claim boundary.

Purpose: Implement D-06 through D-09 on the proven generic profile boundary so functional text is readable while `Total Due` and boxed `Closing Balance` remain the sole anchors.
Output: Two recipe-local semantic treatments and deterministic structural/render coverage for all explicit and backstop Invoice/Statement UI considerations.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-RESEARCH.md
@.planning/phases/136-catalog-visual-quality/136-PATTERNS.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
@lib/rendro/recipes/invoice.ex
@lib/rendro/recipes/statement.ex
</context>

## Artifacts this phase produces

- `lib/rendro/recipes/invoice.ex` completes private helpers `semantic_ink_profile/1`, `primary_ink/2`, and `secondary_ink/2` (or equivalently named private helpers) consuming the Plan 01 generic profile; no public function is added.
- `lib/rendro/recipes/statement.ex` adds the same generic profile interpretation locally and no catalog-aware symbol.
- Invoice's existing 8/10/12/18pt roles and Statement's existing 8/9.5/11/16pt roles remain the only four Phase 136 sizes; weight and leading remain 400/1.3 and 400/1.25 respectively.
- Existing test modules receive named semantic-role, anchor, long-content, empty/partial/cardinality, structured-error, pagination, and two-render cases; no new test file is created.
- No raw color literal, new card, accent, font, point size, weight, layout region, theme token, fixture copy, API, dependency, or artifact file is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete Corporate Invoice dark semantic contrast around the preserved Total Due anchor</name>
  <reversibility rating="reversible">Invoice profile-role changes and their tests are independently Git-revertible without removing Plan 01's generic threading; rollback criterion is restored Corporate target bytes plus green default/no-profile controls.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - .planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
    - lib/rendro/recipes/invoice.ex
    - lib/rendro/recipes/palette.ex
    - lib/rendro/recipes/table_cell.ex
    - lib/rendro/theme/presets.ex
    - test/rendro/recipes/invoice_test.exs
    - priv/examples/invoice/cedar-mutual/invoice.json
  </read_first>
  <files>lib/rendro/recipes/invoice.ex, test/rendro/recipes/invoice_test.exs</files>
  <behavior>
    - "Functional labels and Item/Qty/Price headings use primary semantic ink; addresses, invoice/date/due/terms, subtotal, tax, and footer use secondary semantic ink."
    - "Total Due remains the only 18pt display element and retains its existing Corporate blue accent meaning."
    - "Missing/invalid payloads retain structured failures; zero, one, and many items retain existing collection behavior without padding or substitute copy."
    - "Long existing and held-out facts wrap through existing measurement without clipping, text edits, geometry change, or pagination change."
  </behavior>
  <action>Extend tests first around the Plan 01 `semantic_ink: :primary_secondary` tracer. In `Rendro.Recipes.Invoice`, finish D-06 and D-07 by routing functional labels and `Item`, `Qty`, `Price` through `colors.ink`, and support facts through a deliberate `colors.muted`/semantic secondary value supplied by the profile. Keep the existing table measurement, `@table_columns`, page regions, font registrations, and totals construction. Assert exactly one 18pt `Total Due` display path and existing accent; do not promote headings or support facts. Add structural palette assertions plus two-render tests for the real Cedar Mutual dark profile, missing/partial/zero/one/many input behavior, and long existing/held-out facts. Compare default, no-profile, and unrelated-theme source PDFs to their Plan 01/pre-phase controls per D-08 and D-09.</action>
  <acceptance_criteria>
    - Every named functional Invoice label/header has primary semantic ink and every named support fact has secondary semantic ink under the generic profile.
    - The only display-size target remains 18pt `Total Due`; blue accent semantics, page geometry, font roles, data, pagination, and selectable text are unchanged.
    - Missing and invalid inputs raise the existing structured errors; no partial PDF or empty substitute is produced.
    - Zero, one, many, existing-long, and held-out-long cases retain caller text and measured wrapping with no clipping or collection padding.
    - Two identical profile renders match byte-for-byte; omitted profile, no-theme, and unrelated theme match their frozen bytes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_opts_threading_test.exs --max-failures 1</automated>
  </verify>
  <done>Corporate Classic Invoice dark has readable semantic labels/support facts and an unchanged, uniquely dominant Total Due anchor with all non-target behavior preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the Statement ledger in semantic ink without flattening Closing Balance</name>
  <reversibility rating="reversible">Statement profile-role changes own separate files and revert independently in Git; rollback criterion is restored Statement target bytes plus green default/no-profile controls.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - lib/rendro/recipes/statement.ex
    - lib/rendro/recipes/palette.ex
    - lib/rendro/recipes/table_cell.ex
    - lib/rendro/theme/presets.ex
    - test/rendro/recipes/statement_test.exs
    - test/rendro/recipes/statement_opts_threading_test.exs
    - priv/examples/statement/signal-ledger/statement.json
  </read_first>
  <files>lib/rendro/recipes/statement.ex, test/rendro/recipes/statement_test.exs</files>
  <behavior>
    - "Date/Description/Amount/Balance headers and row values use primary semantic ink; date range, opening balance, footer, and context use secondary semantic ink."
    - "Boxed Closing Balance remains the only 16pt display anchor and the ledger retains mono restraint without accent or new surfaces."
    - "Measured rows, carried/brought-forward behavior, native pagination, and default/no-profile bytes remain unchanged."
    - "Empty, partial, zero/one/many, invalid, existing-long, and held-out-long statement states retain current behavior and text."
  </behavior>
  <action>Write failing profile-role and anchor tests first. Update `Rendro.Recipes.Statement` to consume only the generic `semantic_ink: :primary_secondary` value selected by dev tooling. Build the existing four-cell `table_header` and ledger rows with semantic primary ink through the established cell path; use semantic secondary for period/date range, opening/context balance, page footer, and supporting facts. Preserve `@table_columns`, `measure_rows/4`, carried/brought-forward rows, `Pagination.chunk_rows_into_pages`, exact Decimal balance semantics, page geometry, and the boxed Closing Balance path per D-06 through D-09. Add structural and deterministic render tests for the real Signal Ledger dark profile, empty/partial/zero/one/many and structured-error states, and long existing/held-out content. No decorative accent, new card, global token, or raw-black fallback is allowed.</action>
  <acceptance_criteria>
    - All four ledger headings and every active ledger value resolve to primary semantic ink; all listed contextual facts resolve to secondary semantic ink.
    - Boxed Closing Balance is the only 16pt display element; existing box geometry, fonts, data, pagination, and mono palette restraint remain unchanged.
    - Empty/missing/invalid cases retain existing structured behavior and do not emit substitute/partial PDFs.
    - Zero, one, many, existing-long, and held-out-long cases preserve rows and caller text through measured wrapping and native pagination.
    - Profile renders are deterministic twice; default/no-profile/no-theme/unrelated-theme outputs remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/statement_test.exs test/rendro/recipes/statement_opts_threading_test.exs --max-failures 1</automated>
  </verify>
  <done>Minimal Mono Statement dark has a readable reconciliation ledger with a uniquely dominant boxed Closing Balance and no collateral geometry or byte drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generic profile -> Invoice/Statement | Private data may select semantic roles but cannot carry catalog identity or widen public contracts. |
| Caller document data -> measured layout | Untrusted/missing/long content must retain validation, wrapping, and error semantics. |
| Recipe output -> catalog evidence | Only the two locked dark target bytes may change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-20 | Tampering | semantic profile | high | mitigate | Match only generic `semantic_ink` data and assert all omitted/unrelated paths remain byte-identical. |
| T-136-21 | Tampering | global palette/theme | high | mitigate | Recipe-local Palette/TableCell roles only; no Theme.dark/1 or preset changes; Plan 01 six/26 classifier detects collateral output. |
| T-136-22 | Information disclosure / misrepresentation | dark claims | medium | mitigate | Tests and later docs keep screen readability distinct from accessibility, PDF/UA, viewer, and print safety. |
| T-136-23 | Denial of service | long/many rows | medium | mitigate | Existing measurement/pagination paths plus boundary and held-out long-content tests; no guessed capacity. |
| T-136-AUTH | Spoofing / Session | runtime authentication | low | accept | N/A: pure recipe rendering has no authentication, session, or authorization boundary. |
| T-136-SC | Tampering | package installs | low | accept | No package install or dependency modification occurs. |

## Verification and Residual Risk

- Structural role checks and exact bytes cover deterministic safety; readable visual hierarchy remains named advisory evidence in Plan 05.
</threat_model>

## Spec-less probe fallback audit

- UI coverage: all twelve explicit Invoice/Statement considerations and their four backstops are lifted exactly once into this plan's truths. Payslip, Ticket, review, and canonical considerations are owned by Plans 03-05.
- Prohibition Stage 1 recalled global palette drift, raw-dark fallback, decorative hierarchy inflation, fixture edits, and compliance overclaim. Stage 2 kept global/collateral drift as this plan's descriptor-less flagged prohibition; routine validation/pagination cases are truths and acceptance criteria, while compliance overclaim is retained once in Plan 05.

<verification>
- `mix test test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_opts_threading_test.exs test/rendro/recipes/statement_test.exs test/rendro/recipes/statement_opts_threading_test.exs --max-failures 1`
- `mix format --check-formatted`
- `mix ci.fast`
</verification>

<success_criteria>
- Corporate Invoice and Minimal Statement target dark profiles use deliberate primary/secondary semantic ink with no raw-black fallback.
- `Total Due` and boxed `Closing Balance` remain their respective sole display anchors.
- Geometry, caller data, fonts, pagination, error semantics, default/no-profile behavior, and all unrelated bytes remain unchanged.
- Every explicit/backstop Invoice and Statement UI state has deterministic coverage or the declared backstop route.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-02-SUMMARY.md` when done.
</output>
