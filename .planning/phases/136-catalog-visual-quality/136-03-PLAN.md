---
phase: 136-catalog-visual-quality
plan: "03"
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - lib/rendro/recipes/payslip.ex
  - test/rendro/recipes/payslip_test.exs
  - test/rendro/recipes/payslip_byte_identity_test.exs
autonomous: true
requirements: [CATALOG-11, CATALOG-12]
must_haves:
  truths:
    - "Payslip empty: each sequential ledger owns its header and existing zero-row behavior; no paired-table blank-padding grid is generated."
    - "Payslip loading: PDF generation is atomic and exposes no document loading state; only a completed PDF or an existing structured error reaches catalog tooling."
    - "Payslip error: invalid data or impossible measured layout remains a structured render/validation error, never a clipped or partially approved PDF."
    - "Payslip populated: the Northline fixture renders full-width Earnings then Deductions tables, followed by contiguous reconciliation and uniquely dominant Net Pay."
    - "Payslip partial: Earnings and Deductions are independent sequential sections; one may have fewer rows without fake blank rows, while required reconciliation stays present."
    - "Payslip zero/one/many: focused tests cover zero, one, and many rows per sequential table without paired-grid padding and with deterministic section order."
    - statement: "Held-out Payslip longest labels, widest money tokens, and continuation pages prove flexible descriptions, atomic right-aligned money, native pagination, repeated own-table headers, and reserved reconciliation space."
      verification: backstop
    - statement: "Held-out verbatim Payslip descriptions wrap only in the flexible description column; headers and monetary tokens never wrap, clip, abbreviate, or shrink."
      verification: backstop
  artifacts:
    - path: lib/rendro/recipes/payslip.ex
      provides: "Generic sequential measured-ledger profile integrated with native pagination and reconciliation"
      contains: "sequential_measured"
    - path: test/rendro/recipes/payslip_test.exs
      provides: "Sequential section, measured width, atomic money, continuation, reconciliation, and state coverage"
      contains: "Earnings"
    - path: test/rendro/recipes/payslip_byte_identity_test.exs
      provides: "Light/dark geometry parity, default-path identity, and two-render determinism"
      contains: "byte-identical"
  key_links:
    - from: dev/rendro/catalog.ex
      to: lib/rendro/recipes/payslip.ex
      via: "Plan 01 maps only Swiss light/dark IDs to generic `ledger_layout: :sequential_measured`"
      pattern: 'sequential_measured'
    - from: lib/rendro/recipes/payslip.ex
      to: Rendro.measure_rows/4
      via: "each full-width table measures its own header and rows using registered fonts"
      pattern: 'measure_rows'
    - from: lib/rendro/recipes/payslip.ex
      to: Rendro.Recipes.Pagination
      via: "measured blocks paginate natively and reserve final reconciliation height"
      pattern: 'chunk_rows_into_pages'
  prohibitions:
    - statement: "The repair must not alter fixture text, delete supplied fields, insert blank placeholders, or reorder caller data as a layout workaround."
      status: unverified
      flagged: true
---

<objective>
Replace the Swiss target's paired ledger with two deterministic full-width measured ledgers that preserve every description and money token, paginate natively, reconcile contiguously, and render with identical light/dark geometry.

Purpose: Implement D-10 through D-15 without changing the default Payslip path, caller data, font roles, Unicode fallback, or dark screen-only boundary.
Output: A generic `:sequential_measured` ledger profile plus structural, pagination, geometry, error, cardinality, held-out, and byte-determinism coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136-catalog-visual-quality/136-CONTEXT.md
@.planning/phases/136-catalog-visual-quality/136-RESEARCH.md
@.planning/phases/136-catalog-visual-quality/136-PATTERNS.md
@.planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
@.planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
@lib/rendro/recipes/payslip.ex
@priv/examples/payslip/northline-logistics/payslip.json
</context>

## Artifacts this phase produces

- `lib/rendro/recipes/payslip.ex` adds private generic profile predicate `sequential_ledger?/1`; private builders `sequential_ledger_blocks/4`, `ledger_table/5`, and `money_column_width/3` (or equivalently named private helpers); and reuses existing `build_reconciliation_blocks/6`, `Rendro.measure_rows/4`, `Pagination.chunk_rows_into_pages`, `cell_text/3`, Unicode measurement document, and `cell_align` paths.
- The profile key/value is `ledger_layout: :sequential_measured`; it carries no catalog ID, brand, preset, mode, or phase name.
- Description remains one `{:share, 1}` column; Current and YTD use two equal `{:fixed, measured_width}` columns computed from the widest header/money token plus measured headroom.
- Existing test modules add zero/one/many, asymmetric sections, long verbatim descriptions, widest-money, atomic/right alignment, repeated-own-header, no-orphan, reserved-reconciliation, Unicode, light/dark geometry, default identity, and two-render cases.
- No fixture, public API, new point size, font weight, dependency, renderer, manual coordinate system, guessed rows-per-page constant, or new file is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render one complete Swiss payslip through sequential measured ledgers</name>
  <reversibility rating="reversible">The generic sequential-ledger branch is isolated from the default paired-ledger path and reverts normally in Git; rollback criterion is removal of the branch with default and frozen target controls green.</reversibility>
  <read_first>
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
    - .planning/phases/136-catalog-visual-quality/136-01-SUMMARY.md
    - lib/rendro/recipes/payslip.ex
    - lib/rendro/recipes/palette.ex
    - lib/rendro/recipes/table_cell.ex
    - lib/rendro/recipes/pagination.ex
    - lib/rendro/theme/presets.ex
    - test/rendro/recipes/payslip_test.exs
    - priv/examples/payslip/northline-logistics/payslip.json
    - priv/examples/payslip/DOMAIN.md
  </read_first>
  <files>lib/rendro/recipes/payslip.ex, test/rendro/recipes/payslip_test.exs</files>
  <behavior>
    - "The real Northline target profile renders full-width Earnings | Current | YTD followed by Deductions | Current | YTD, never the paired seven-column zip_pad grid."
    - "Every fixture description is verbatim; description flexes while Current/YTD widths derive from registered-font measurement of the widest money token plus headroom."
    - "Money cells are indivisible and right aligned; both tables use their own semantic headers and dark has no raw-black header/body/rule fallback."
    - "Net Pay remains the unique 21pt display role and the gross-deductions-net reconciliation follows the final table contiguously."
  </behavior>
  <action>Start with a failing structural test for the real Northline data using Plan 01's generic `ledger_layout: :sequential_measured`. In `Rendro.Recipes.Payslip`, branch only on that generic value and replace the paired `zip_pad/2` body for this profile with independently built Earnings and Deductions blocks in source order per D-10. Compute a shared description column and two equal fixed Current/YTD widths using the registered body font and the widest formatted header/money token plus deterministic headroom per D-12; use the existing atomic text/cell alignment path. Each block owns its `Earnings | Current | YTD` or `Deductions | Current | YTD` header and semantic palette roles per D-13. Preserve every Northline string exactly per D-11 and reuse the existing reconciliation builder so Net Pay remains the only 21pt display anchor per D-14. Keep existing 8/10.5/13/21pt roles, weight 400, leading 1.3, Unicode fallback, data validation, and the default paired-ledger path.</action>
  <acceptance_criteria>
    - The target profile emits exactly two full-width three-column tables in Earnings-then-Deductions order with no `zip_pad/2` blank rows.
    - Fixture descriptions compare exactly to the JSON input; Current/YTD width is derived from measured widest tokens and amounts are one-line/right-aligned.
    - Light and dark structure uses the same columns/rows/spacing; dark header/body/rule/summary colors are semantic palette values rather than raw black.
    - Net Pay alone uses 21pt display size and reconciliation remains immediately after the final ledger.
    - Omitted profile retains the current paired ledger and existing bytes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_test.exs --max-failures 1</automated>
  </verify>
  <done>The real Swiss payslip renders complete sequential measured ledgers, intact payroll content, atomic money, semantic dark treatment, and an unchanged dominant Net Pay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove native continuation, reconciliation reservation, geometry parity, and edge states</name>
  <reversibility rating="reversible">Pagination/reconciliation changes and focused tests revert together in the Payslip-owned files; rollback criterion is restoration of the pre-task target branch without changing the default layout or caller fixture.</reversibility>
  <read_first>
    - lib/rendro/recipes/payslip.ex
    - test/rendro/recipes/payslip_test.exs
    - test/rendro/recipes/payslip_byte_identity_test.exs
    - priv/examples/payslip/northline-logistics/payslip.json
    - priv/examples/payslip/DOMAIN.md
    - .planning/phases/136-catalog-visual-quality/136-CONTEXT.md
    - .planning/phases/136-catalog-visual-quality/136-UI-SPEC.md
  </read_first>
  <files>lib/rendro/recipes/payslip.ex, test/rendro/recipes/payslip_test.exs, test/rendro/recipes/payslip_byte_identity_test.exs</files>
  <behavior>
    - "Each table measures rows with Rendro.measure_rows/4, paginates through Pagination.chunk_rows_into_pages, repeats only its own header, and never orphans a section heading."
    - "The final page reserves measured reconciliation height so Gross Pay - Total Deductions = Net Pay stays contiguous with final ledger rows."
    - "Zero/one/many and asymmetric earnings/deductions have deterministic order and no fake padding; impossible layouts fail structurally."
    - "Held-out longest labels and widest amounts preserve text, headers, atomic money, Unicode fallback, and geometry parity; two renders match bytes."
  </behavior>
  <action>Extend the existing Payslip measurement/chunking path so each sequential table calls `Rendro.measure_rows/4` with its own header/table options and converts measured rows to the existing `{row, height, metadata}` pagination input. Reserve `reconciliation_reserved_height(data)` plus `@row_epsilon` on the final capacity, repeat a table's own header when it continues, and keep a section heading with its first row per D-14 and D-15. Add deterministic tests for zero, one, many, asymmetric, boundary, continuation, and impossible-fit cases; held-out longest verbatim descriptions and widest money tokens; repeated Earnings versus Deductions headers; final reconciliation adjacency; Unicode fallback; and exact light/dark geometry. Render target light and dark twice, and compare omitted-profile/default paths against frozen bytes. Do not add guessed capacity, manual coordinates, abbreviation, normalization, clipping, font shrinkage, or blank padding.</action>
  <acceptance_criteria>
    - Instrumented/structural assertions prove `measure_rows/4` results—not row counts or character estimates—feed native pagination for both tables.
    - Continuations repeat the correct owning header and never leave Earnings/Deductions headings orphaned.
    - Final reconciliation fits with final rows on the same page or the bounded layout returns the existing structured error; it is never clipped/partial.
    - Zero/one/many/asymmetric and held-out long/wide cases retain exact text, atomic right-aligned money, Unicode fallback, and deterministic section order.
    - Light/dark profile geometry is identical, each mode renders byte-identically twice, and the omitted profile matches its pre-phase bytes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_test.exs test/rendro/recipes/payslip_byte_identity_test.exs --max-failures 1</automated>
  </verify>
  <done>Swiss sequential ledgers handle real and held-out boundary content through measured native pagination with correct headers, reconciliation, geometry, errors, and deterministic bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generic ledger profile -> Payslip | The recipe accepts layout intent but no catalog identity. |
| Caller payroll data -> measurement | Long descriptions, amounts, and Unicode are untrusted content that must not be lost or mis-associated. |
| Measured rows -> pagination | Capacity and reconciliation reservation must be deterministic and fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-30 | Tampering | payroll content | high | mitigate | Exact fixture/held-out string equality; no abbreviation, normalization, padding, or deletion. |
| T-136-31 | Tampering | money association | high | mitigate | Measured fixed columns, atomic text, right alignment, and structural cell-order assertions. |
| T-136-32 | Denial of service | long rows/pagination | medium | mitigate | Native measured pagination, reconciliation reservation, impossible-fit structured errors, and boundary tests. |
| T-136-33 | Repudiation | geometry parity | medium | mitigate | Structural light/dark geometry equality and two-render byte identity. |
| T-136-34 | Information disclosure / misrepresentation | dark output | medium | mitigate | Semantic roles and retained print_safety false; no print/accessibility claim. |
| T-136-AUTH | Spoofing / Session | runtime authentication | low | accept | N/A: the pure recipe has no auth/session/request surface. |
| T-136-SC | Tampering | package installs | low | accept | No dependency install or external service occurs. |

## Verification and Residual Risk

- Deterministic layout invariants are hard-gated; final visual rhythm/density scoring remains named human evidence in Plan 05.
</threat_model>

## Spec-less probe fallback audit

- UI coverage: all six explicit Payslip considerations and both Payslip backstops are lifted exactly once into this plan's truths.
- Prohibition Stage 1 recalled fixture shortening, field deletion, blank padding, money splitting, guessed capacity, font shrinkage, and raw-dark fallback. Stage 2 retained the project-specific caller-data/field integrity constraint once as a descriptor-less flagged prohibition; layout mechanics are explicit acceptance criteria.

<verification>
- `mix test test/rendro/recipes/payslip_test.exs test/rendro/recipes/payslip_byte_identity_test.exs --max-failures 1`
- `mix format --check-formatted`
- `mix ci.fast`
</verification>

<success_criteria>
- Swiss targets render sequential Earnings and Deductions ledgers with verbatim flexible descriptions and measured atomic money columns.
- Native pagination repeats owning headers, prevents orphaned headings, and reserves contiguous reconciliation space.
- Light/dark geometry, semantic role usage, Unicode fallback, deterministic bytes, default behavior, and structured errors are proven.
- Every explicit/backstop Payslip UI state has deterministic coverage or the declared backstop route.
</success_criteria>

<output>
Create `.planning/phases/136-catalog-visual-quality/136-03-SUMMARY.md` when done.
</output>
