---
phase: 94-docs-warning-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/viewer_evidence/validator.ex
  - guides/viewer_evidence.md
autonomous: true
requirements:
  - HYG-02

must_haves:
  truths:
    - "staleness_warnings/1 message is self-explaining: contains remediation command, advisory-outside-strict note, and guides/viewer_evidence.md pointer"
    - "@staleness_days 180 is preserved unchanged in validator.ex"
    - "guides/viewer_evidence.md contains a staleness lifecycle section explaining the signal is designed, not a defect"
    - "The staleness warning is not silenced, raised, lowered, or pre-recorded — wording/docs only"
  artifacts:
    - path: "lib/rendro/viewer_evidence/validator.ex"
      provides: "augmented staleness_warnings/1 message string"
      contains: "mix rendro.viewer_evidence record"
    - path: "guides/viewer_evidence.md"
      provides: "staleness lifecycle section for maintainer reference"
  key_links:
    - from: "staleness_warnings/1 message"
      to: "house style in launch_artifacts.ex"
      via: "inline compact remediation, no shell prompt, lowercase 'run'"
      pattern: 'run mix rendro.viewer_evidence record'
    - from: "guides/viewer_evidence.md lifecycle section"
      to: "staleness_warnings/1 guide pointer"
      via: "maintainer reads guide after seeing the warning"
---

<objective>
Make the viewer-evidence staleness signal self-explaining at its source and document the staleness lifecycle in the guide, so a maintainer who first sees the signal fire (~late November 2026) understands it is a designed cadence signal, not a defect.

Purpose: The 180-day staleness warning is latent (will first fire approximately late November 2026). Without explanation, a maintainer seeing the warning for the first time has no context: Is this a bug? Do I silence it? How do I fix it? The fix is wording and docs only — the threshold, cadence, and severity semantics are preserved exactly.

Output: Updated `lib/rendro/viewer_evidence/validator.ex` (staleness_warnings/1 message string augmented) and updated `guides/viewer_evidence.md` (new staleness lifecycle section added).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rendro/.planning/ROADMAP.md
@/Users/jon/projects/rendro/.planning/phases/94-docs-warning-hygiene/94-CONTEXT.md
@/Users/jon/projects/rendro/.planning/phases/94-docs-warning-hygiene/94-RESEARCH.md

<interfaces>
<!-- Key facts the executor needs — extracted from codebase and research. No codebase exploration needed. -->

From lib/rendro/viewer_evidence/validator.ex:
  @staleness_days 180          ← line 11; PRESERVE THIS EXACTLY, do not touch
  staleness_warnings/1         ← lines 91-114; single edit site for the message string

Current message string (lines 103-107):
  [
    "#{cell.matrix_path}: recorded_at #{recorded_at} is older than #{@staleness_days} days"
  ]

Target: Augment the single string to append (a) advisory note that it is non-fatal outside --strict,
(b) remediation command following house style, (c) guide pointer.

House style from lib/rendro/launch_artifacts.ex:
  "source PDF hash drift for ...; run mix rendro.launch_artifacts.gen"
  "README launch artifact block is stale; run mix rendro.launch_artifacts.gen"
  Pattern: "<description>; run mix <task.name>" — no trailing period, lowercase "run",
  no shell prompt prefix, compact inline single string.

Suggested augmented shape (exact wording is Claude's discretion per CONTEXT.md):
  "#{cell.matrix_path}: recorded_at #{recorded_at} is older than #{@staleness_days} days " <>
  "(advisory — non-fatal unless --strict; re-record with mix rendro.viewer_evidence record; " <>
  "see guides/viewer_evidence.md)"

Call chain context (so executor edits the right site):
  run_full/3 → staleness_warnings(matrix) → returns [warning_string]
  The edit is in staleness_warnings/1 only. Do NOT edit run_full/3, warnings/2 aggregator, or @staleness_days.

From guides/viewer_evidence.md:
  Guide already exists, already wired into ExDoc extras and package files — no wiring changes needed.
  Guide does NOT currently contain a staleness lifecycle section.
  A new section is needed explaining the 180-day signal is a designed cadence signal, not a defect.

Key facts for the new guide section:
  - The 180-day signal first fires approximately late November 2026 (180 days after most recent recordings)
  - The appropriate response is to re-record evidence (not suppress the warning)
  - Outside --strict the warning is advisory; with --strict it is fatal
  - The threshold (180 days) is a deliberate cadence, not a regression
  - Re-recording command: mix rendro.viewer_evidence record <surface> <viewer> ...
  - Full workflow is in the same guide

Pitfall (D-06): Do NOT silence, raise, or lower @staleness_days. Do NOT pre-emptively re-record evidence to dodge the warning. This is wording/docs only.
Pitfall (RESEARCH.md Pitfall 4): The edit site is staleness_warnings/1 lines 103-107 — not the warnings/2 aggregator or run_full/3. Edit the string that is returned in the list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Augment staleness_warnings/1 message in validator.ex</name>
  <files>lib/rendro/viewer_evidence/validator.ex</files>
  <read_first>
    - lib/rendro/viewer_evidence/validator.ex (read full file — confirm @staleness_days at line 11, staleness_warnings/1 message at lines 103-107, and call chain at lines 116-130)
    - lib/rendro/launch_artifacts.ex (read for house style pattern — extract the remediation message strings to confirm the exact inline compact format with "run mix <task>" without shell prompt)
    - .planning/phases/94-docs-warning-hygiene/94-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    In lib/rendro/viewer_evidence/validator.ex, edit only the staleness_warnings/1 function. Specifically, augment the single warning string returned in the list at lines 103-107. The original string is:
      "#{cell.matrix_path}: recorded_at #{recorded_at} is older than #{@staleness_days} days"

    Append to this string (via string concatenation or multi-line string) three pieces of information per D-05:
    (a) an advisory note that the warning is non-fatal outside --strict (fatal only when --strict is passed),
    (b) the remediation command for re-recording evidence: mix rendro.viewer_evidence record, following house style (inline, compact, lowercase "run", no shell prompt, no trailing period — match the pattern from launch_artifacts.ex),
    (c) a pointer to guides/viewer_evidence.md for the full workflow.

    The @staleness_days 180 module attribute at line 11 MUST NOT be changed. The staleness threshold, the cadence, and whether the warning fires at all must not change — this is wording only. Do not edit run_full/3 or the warnings/2 aggregator.

    Exact wording is Claude's discretion (per CONTEXT.md). Match the terse, inline house style from launch_artifacts.ex.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rendro && grep "@staleness_days 180" lib/rendro/viewer_evidence/validator.ex</automated>
    Must match — threshold preserved.

    <automated>cd /Users/jon/projects/rendro && grep "mix rendro.viewer_evidence record" lib/rendro/viewer_evidence/validator.ex</automated>
    Must match — remediation command present in message string.

    <automated>cd /Users/jon/projects/rendro && grep "viewer_evidence.md" lib/rendro/viewer_evidence/validator.ex</automated>
    Must match — guide pointer present in message string.

    <automated>cd /Users/jon/projects/rendro && mix test test/docs_contract/public_api_contract_test.exs</automated>
    Must pass — contract test unaffected by validator wording change.
  </verify>
  <acceptance_criteria>
    - grep "@staleness_days 180" lib/rendro/viewer_evidence/validator.ex matches (threshold preserved, per D-06)
    - grep "mix rendro.viewer_evidence record" lib/rendro/viewer_evidence/validator.ex matches (remediation command in message, per D-05a)
    - The message string contains language indicating the warning is advisory / non-fatal outside --strict (per D-05b — verify by reading the modified staleness_warnings/1 function)
    - grep "viewer_evidence.md" lib/rendro/viewer_evidence/validator.ex matches (guide pointer in message, per D-05c)
    - grep "@staleness_days" lib/rendro/viewer_evidence/validator.ex produces exactly one match (at line 11 — confirm no duplicate was added)
    - mix test test/docs_contract/public_api_contract_test.exs exits 0, 6 tests, 0 failures
    - The message follows house style: inline compact single string or string concatenation, no shell prompt (no "$" prefix), lowercase "run" before mix command
  </acceptance_criteria>
  <done>staleness_warnings/1 message augmented with remediation command, advisory note, and guide pointer; @staleness_days 180 preserved; contract test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add staleness lifecycle section to guides/viewer_evidence.md</name>
  <files>guides/viewer_evidence.md</files>
  <read_first>
    - guides/viewer_evidence.md (read full file — understand the existing section structure to choose the right placement for the new section; note what is already covered so the new section does not duplicate existing content)
    - .planning/phases/94-docs-warning-hygiene/94-CONTEXT.md (D-07)
    - .planning/phases/94-docs-warning-hygiene/94-RESEARCH.md (the "guides/viewer_evidence.md — staleness lifecycle section" subsection for what D-07 requires)
  </read_first>
  <action>
    Add a new section to guides/viewer_evidence.md documenting the staleness lifecycle. Place it in a location that makes logical sense given the existing guide structure — likely near the end of the guide or adjacent to the section discussing supported/promoted rows, since staleness is a lifecycle concern for maintained evidence. The heading and exact placement within the guide are Claude's discretion (per CONTEXT.md).

    The section must explain (per D-07):
    1. The 180-day staleness signal is a designed cadence signal, not a defect or regression.
    2. It will first fire approximately late November 2026 (180 days after the most recent viewer-evidence recordings). This makes the "first firing" legible as a planned event, not a surprise.
    3. When the signal fires, the correct response is to re-record evidence using mix rendro.viewer_evidence record (not to suppress the warning, not to raise the threshold).
    4. The signal is advisory (non-fatal) in normal operation; it becomes fatal only when mix rendro.verify --strict is passed.
    5. The guide already contains the full recording workflow — point the reader to the relevant section for the actual commands.

    Do not add any new mix task, new config, or wiring changes. This is a pure docs addition to an existing guide. No changes to ExDoc extras or package files are needed (the guide is already listed).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rendro && grep -c "staleness" guides/viewer_evidence.md</automated>
    Must output a number greater than 0 — confirms staleness content was added.

    <automated>cd /Users/jon/projects/rendro && grep "180" guides/viewer_evidence.md</automated>
    Must match — the 180-day threshold is mentioned in the guide section.

    <automated>cd /Users/jon/projects/rendro && grep -i "cadence\|designed\|lifecycle\|intentional" guides/viewer_evidence.md</automated>
    Must match — the section explains the signal is designed/intentional, not a defect.
  </verify>
  <acceptance_criteria>
    - guides/viewer_evidence.md contains a new section about the staleness lifecycle (verify by reading the file; section heading is Claude's discretion but must be meaningful)
    - The section mentions the 180-day threshold and the approximate first-fire date (~late November 2026)
    - The section explains the signal is a designed cadence signal, not a defect (per D-07)
    - The section states the correct response is re-recording evidence with mix rendro.viewer_evidence record
    - The section clarifies the advisory nature (non-fatal unless --strict)
    - grep "180" guides/viewer_evidence.md matches in the new section
    - grep -i "cadence\|designed\|lifecycle\|intentional" guides/viewer_evidence.md matches
    - The guide does NOT add new wiring (no changes to mix.exs extras list or package files needed)
  </acceptance_criteria>
  <done>guides/viewer_evidence.md contains staleness lifecycle section explaining the designed cadence signal, first-fire date, correct remediation response, and advisory severity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code / docs | All changes are to warning message strings and a markdown guide; no runtime inputs, no auth, no data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02-01 | Information Disclosure | staleness warning message | accept | The message now contains the mix command for re-recording; this is public maintainer information, not sensitive. Viewer evidence paths are already in priv/ and not confidential. |
| T-94-02-02 | Tampering | @staleness_days threshold | mitigate | Task 1 acceptance criteria include a grep gate confirming @staleness_days 180 is preserved. Executor is explicitly instructed not to touch it. |

No new attack surface. This plan modifies a warning message string and adds a markdown guide section. No user input, no network calls, no auth, no runtime code paths, no new dependencies.
</threat_model>

<verification>
After both tasks complete:

1. `grep "@staleness_days 180" lib/rendro/viewer_evidence/validator.ex` matches (threshold preserved)
2. `grep "mix rendro.viewer_evidence record" lib/rendro/viewer_evidence/validator.ex` matches (remediation in message)
3. `grep "viewer_evidence.md" lib/rendro/viewer_evidence/validator.ex` matches (guide pointer in message)
4. `grep -i "cadence\|designed\|lifecycle\|intentional" guides/viewer_evidence.md` matches (lifecycle section present and accurate)
5. `grep "180" guides/viewer_evidence.md` matches (threshold mentioned in guide)
6. `mix test test/docs_contract/public_api_contract_test.exs` passes (6 tests, 0 failures)
</verification>

<success_criteria>
HYG-02 complete when:
- staleness_warnings/1 message contains all three augmentations: remediation command (mix rendro.viewer_evidence record), advisory-outside-strict note, guides/viewer_evidence.md pointer
- @staleness_days 180 is unchanged (threshold preserved, nothing silenced)
- guides/viewer_evidence.md has a staleness lifecycle section explaining the designed cadence signal, ~late November 2026 first-fire date, correct re-recording response, and advisory severity
- The signal still fires on the same cadence — no behavior change, wording/docs only
</success_criteria>

<output>
Create `.planning/phases/94-docs-warning-hygiene/94-02-SUMMARY.md` when done
</output>
