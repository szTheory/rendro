---
phase: 94-docs-warning-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/rendro/pdf/font.ex
autonomous: true
requirements:
  - HYG-01

must_haves:
  truths:
    - "mix docs emits zero ExDoc warnings"
    - "mix docs --warnings-as-errors exits 0"
    - "Rendro.PDF.Font has a real internal-marking @moduledoc (not @moduledoc false)"
    - "mix.exs docs/0 contains skip_code_autolink_to: with all three prose-warning module strings"
    - "The ci: alias runs docs --warnings-as-errors"
    - "priv/public_api.json is unchanged (Rendro.PDF.Font not added)"
    - "mix test test/docs_contract/public_api_contract_test.exs passes (6 tests, 0 failures)"
  artifacts:
    - path: "mix.exs"
      provides: "skip_code_autolink_to: list + --warnings-as-errors ci enforcement"
      contains: "skip_code_autolink_to:"
    - path: "lib/rendro/pdf/font.ex"
      provides: "real @moduledoc that resolves typespec autolink warnings"
  key_links:
    - from: "mix.exs docs/0 skip_code_autolink_to:"
      to: "ExDoc 0.40.1 warning suppression"
      via: "list of exact module name strings"
      pattern: 'skip_code_autolink_to:'
    - from: "mix.exs aliases/0 ci:"
      to: "ExDoc --warnings-as-errors gate"
      via: "space-separated flag on docs step"
      pattern: '"docs --warnings-as-errors"'
    - from: "lib/rendro/pdf/font.ex @moduledoc"
      to: "ExDoc typespec autolink resolution"
      via: "non-false @moduledoc makes @type t visible"
      pattern: '@moduledoc """'
---

<objective>
Resolve all 9 unique ExDoc warnings currently emitted by `mix docs` and enforce the resulting zero-warning policy mechanically in CI.

Purpose: A maintainer building the docs must see a clean warning posture. Unexplained ExDoc warnings erode trust in the build output. The two warning classes (prose autolinks to hidden internals, typespec autolinks through a hidden Font module) have distinct fixes; after both fixes, `--warnings-as-errors` on the ci alias ensures no future drift goes undetected.

Output: Updated `mix.exs` (new `skip_code_autolink_to:` option in `docs/0`; `"docs --warnings-as-errors"` in `ci:` alias) and updated `lib/rendro/pdf/font.ex` (real internal-marking `@moduledoc`).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rendro/.planning/ROADMAP.md
@/Users/jon/projects/rendro/.planning/phases/94-docs-warning-hygiene/94-CONTEXT.md
@/Users/jon/projects/rendro/.planning/phases/94-docs-warning-hygiene/94-RESEARCH.md

<interfaces>
<!-- Key facts the executor needs — extracted from codebase and research. No codebase exploration needed. -->

From mix.exs aliases/0 (lines 63-74) — CURRENT STATE:
  ci: [
    "format --check-formatted",
    "hex.build",
    "compile --warnings-as-errors",
    "test",
    "docs",
    "credo --strict",
    "dialyzer"
  ]
Target: "docs" → "docs --warnings-as-errors"

From mix.exs docs/0 (lines 98-115) — existing option to augment alongside:
  skip_undefined_reference_warnings_on: [
    "CHANGELOG.md", "guides/branding.md", "guides/integrations.md",
    "guides/comparison.md", "guides/livebook/first_invoice.livemd",
    "guides/page_primitive.md", "guides/recipes.md",
    "guides/user_flows_and_jtbd.md", "lib/rendro/document.ex",
    "lib/rendro/font_registry.ex", "lib/rendro.ex"
  ],
New option to insert after this, in the same keyword list:
  skip_code_autolink_to: ["Rendro.PDF.CidFont", "Rendro.PDF.FontSubsetter", "Rendro.Format"],

From lib/rendro/pdf/font.ex line 2 — CURRENT STATE:
  @moduledoc false

From Mix.Tasks.Rendro.Api.Gen (lib/mix/tasks/rendro/api.gen.ex):
  @public_modules is a hardcoded explicit allowlist — Rendro.PDF.Font is NOT in it.
  Giving Font a real @moduledoc will NOT cause mix rendro.api.gen to add it to
  priv/public_api.json. The generator uses an explicit allowlist, not "any non-hidden module."
  This closes RESEARCH.md Open Question 1 — manifest drift risk is ZERO.

Warning class breakdown (empirically verified 2026-06-13):
  Class A — prose autolink (fix: skip_code_autolink_to:):
    "Rendro.PDF.CidFont" referenced in guides/api_stability.md:36
    "Rendro.PDF.FontSubsetter" referenced in guides/api_stability.md:36
    "Rendro.Format" referenced in lib/rendro/recipes/statement.ex:66 and receipt.ex:64
  Class B — typespec autolink (fix: real @moduledoc on Font):
    "Rendro.PDF.Font.t()" referenced in shaper.ex:49, shaper.ex:68, shaper/simple.ex:41, adapters/harfbuzz.ex:24

Pitfall: skip_undefined_reference_warnings_on: takes FILE PATHS; skip_code_autolink_to: takes MODULE NAME STRINGS. These are different options — do not confuse them.
Pitfall: Do NOT add Rendro.PDF.Font to the asserted-hidden list in public_api_contract_test.exs. Leave that test file untouched.
Pitfall: ExDoc generates html + epub in sequence, so each warning appears twice (18 lines = 9 unique). skip_code_autolink_to: needs 3 entries (not 9).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add skip_code_autolink_to: to docs/0 and replace Font @moduledoc false</name>
  <files>mix.exs, lib/rendro/pdf/font.ex</files>
  <read_first>
    - mix.exs (full file — confirm current docs/0 structure and aliases/0 structure before editing)
    - lib/rendro/pdf/font.ex (confirm @moduledoc false at line 2 and @type t definition)
    - .planning/phases/94-docs-warning-hygiene/94-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/94-docs-warning-hygiene/94-RESEARCH.md (Empirical Warning Inventory section, ExDoc Option Verification section, Common Pitfalls section)
  </read_first>
  <action>
    In mix.exs docs/0 keyword list, insert skip_code_autolink_to: immediately after the closing bracket of skip_undefined_reference_warnings_on:. The new option value is the list ["Rendro.PDF.CidFont", "Rendro.PDF.FontSubsetter", "Rendro.Format"]. These three strings are the exact module name targets for ExDoc to suppress globally — one entry covers both Rendro.Format occurrences (statement.ex and receipt.ex). Per D-01, this is a distinct option from the existing skip_undefined_reference_warnings_on: (which takes file paths); do not alter the existing option.

    In lib/rendro/pdf/font.ex, replace the line @moduledoc false with a real @moduledoc. The moduledoc text must: (a) identify Rendro.PDF.Font as an internal implementation detail of Rendro's PDF rendering layer, (b) state it is not a public, stable surface and is not covered by the SemVer API guarantee, (c) be terse (2-4 sentences is appropriate). The mechanical purpose of this change (per D-02) is to make @type t resolvable by ExDoc so the four Class B typespec autolink warnings stop; the text should reflect that intent without being misleading. Do NOT add Rendro.PDF.Font to priv/public_api.json and do NOT touch test/docs_contract/public_api_contract_test.exs.

    After both edits, run the verification commands below before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rendro && mix docs 2>&1 | grep -c "^    warning:"</automated>
    Command must output 0. If output is non-zero, the remaining warnings indicate missing skip_code_autolink_to: entries or Font @moduledoc not yet applied — fix before continuing.

    <automated>cd /Users/jon/projects/rendro && mix test test/docs_contract/public_api_contract_test.exs</automated>
    Must pass: 6 tests, 0 failures. Confirms Font @moduledoc change did not break the public-API contract.
  </verify>
  <acceptance_criteria>
    - grep -n "skip_code_autolink_to:" mix.exs matches (option present in docs/0)
    - grep for '"Rendro.PDF.CidFont"' in mix.exs matches (first list entry)
    - grep for '"Rendro.PDF.FontSubsetter"' in mix.exs matches (second list entry)
    - grep for '"Rendro.Format"' in mix.exs matches within skip_code_autolink_to: block (third entry)
    - grep "@moduledoc false" lib/rendro/pdf/font.ex outputs nothing (false moduledoc removed)
    - grep "@moduledoc" lib/rendro/pdf/font.ex matches (real moduledoc present)
    - mix docs 2>&1 | grep -c "^    warning:" outputs 0
    - mix test test/docs_contract/public_api_contract_test.exs exits 0 with 6 tests, 0 failures
  </acceptance_criteria>
  <done>mix docs emits zero warnings; @moduledoc false removed from Font; skip_code_autolink_to: present in mix.exs; contract test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add --warnings-as-errors to ci: alias docs step</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (confirm aliases/0 ci: list current state — specifically that "docs" is still the plain entry, not already changed)
    - .planning/phases/94-docs-warning-hygiene/94-CONTEXT.md (D-04)
    - .planning/phases/94-docs-warning-hygiene/94-RESEARCH.md (ExDoc Option Verification section — --warnings-as-errors flag verified at deps/ex_doc/lib/mix/tasks/docs.ex:30)
  </read_first>
  <action>
    In mix.exs aliases/0, in the ci: list, change the string "docs" to "docs --warnings-as-errors". This is a space-separated flag in the Mix alias string — standard Mix alias format. Per D-04, this is the mechanical enforcement gate: any future ExDoc warning will cause CI to fail with a non-zero exit. This step MUST come after Task 1 has achieved zero warnings; if warnings remain when this flag is active the ci alias would perpetually fail. Confirm Task 1 acceptance criteria are met before making this change.

    The ci: list order stays otherwise unchanged: ["format --check-formatted", "hex.build", "compile --warnings-as-errors", "test", "docs --warnings-as-errors", "credo --strict", "dialyzer"].
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rendro && mix docs --warnings-as-errors</automated>
    Must exit 0. This is the definitive gate: zero warnings AND the enforcement flag behaves correctly.

    <automated>cd /Users/jon/projects/rendro && grep '"docs --warnings-as-errors"' mix.exs</automated>
    Must match exactly — confirms the ci: alias was updated.
  </verify>
  <acceptance_criteria>
    - grep '"docs --warnings-as-errors"' mix.exs matches (ci alias updated)
    - grep '"docs",' mix.exs within the ci: block does NOT match (plain "docs" entry replaced)
    - mix docs --warnings-as-errors exits 0 (zero warnings, flag satisfied)
    - mix docs 2>&1 | grep -c "^    warning:" outputs 0 (smoke check still clean)
    - mix test test/docs_contract/public_api_contract_test.exs exits 0, 6 tests, 0 failures (contract still green)
  </acceptance_criteria>
  <done>ci: alias enforces --warnings-as-errors on docs; mix docs --warnings-as-errors exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time config | mix.exs and ExDoc options are developer/maintainer controlled; no runtime or user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | mix.exs ci: alias — docs --warnings-as-errors | accept | Intended behavior: future stray ExDoc references will correctly fail CI. This is the designed "fail-on-drift" posture, not a security threat. No sensitive data involved. |
| T-94-02 | Denial of Service | @moduledoc on Rendro.PDF.Font — manifest drift | mitigate | Executor runs mix test test/docs_contract/public_api_contract_test.exs after the change; generator uses explicit @public_modules allowlist so Font cannot appear in manifest. Verified safe pre-plan. |

No new attack surface. This plan modifies build-time documentation configuration and a module-level doc attribute. No user input, no auth, no data handling, no runtime code paths affected.
</threat_model>

<verification>
After both tasks complete:

1. `mix docs 2>&1 | grep -c "^    warning:"` outputs `0`
2. `mix docs --warnings-as-errors` exits 0
3. `mix test test/docs_contract/public_api_contract_test.exs` passes (6 tests, 0 failures)
4. `grep '"docs --warnings-as-errors"' mix.exs` matches
5. `grep -c "skip_code_autolink_to:" mix.exs` outputs at least 1
6. `grep "@moduledoc false" lib/rendro/pdf/font.ex` outputs nothing
</verification>

<success_criteria>
HYG-01 complete when:
- mix docs emits zero warnings (empirical oracle satisfied)
- mix docs --warnings-as-errors exits 0 (enforcement gate satisfied)
- The ci: alias contains "docs --warnings-as-errors" (mechanical enforcement locked in)
- Rendro.PDF.Font has a real internal-marking @moduledoc (not @moduledoc false)
- mix.exs docs/0 contains skip_code_autolink_to: with ["Rendro.PDF.CidFont", "Rendro.PDF.FontSubsetter", "Rendro.Format"]
- Public-API contract test still passes (no manifest drift)
</success_criteria>

<output>
Create `.planning/phases/94-docs-warning-hygiene/94-01-SUMMARY.md` when done
</output>
