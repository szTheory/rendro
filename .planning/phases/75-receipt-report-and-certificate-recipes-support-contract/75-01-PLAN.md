---
phase: 75-receipt-report-and-certificate-recipes-support-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes/pagination.ex
  - lib/rendro/page_size.ex
  - lib/rendro/recipes/statement.ex
autonomous: true
requirements:
  - RCPT-01
  - RCPT-02
  - RCPT-03
  - CERT-01
  - CERT-02
must_haves:
  truths:
    - "Rendro.Recipes.Pagination module exists with chunk_rows_into_pages/2, formatter/3, label_resolver/1, type_name/1 as public functions"
    - "Rendro.PageSize.resolve/2 returns {841.89, 595.28} for :a4/:landscape, {595.28, 841.89} for :a4/:portrait, {792.0, 612.0} for :us_letter/:landscape"
    - "Statement.document/2 produces identical output before and after the D-04 refactor (51 tests, 0 failures)"
    - "statement.ex no longer contains defp chunk_into_pages/5, defp do_chunk_pages/5, defp finalize_page/1, defp formatter/3, defp label_resolver/1, defp type_name/1"
  artifacts:
    - path: "lib/rendro/recipes/pagination.ex"
      provides: "Shared chunking helper: chunk_rows_into_pages/2, formatter/3, label_resolver/1, type_name/1"
      contains: "defmodule Rendro.Recipes.Pagination"
    - path: "lib/rendro/page_size.ex"
      provides: "Named page-size resolution: :a4 and :us_letter atoms to {width, height} tuples with landscape swap"
      contains: "defmodule Rendro.PageSize"
    - path: "lib/rendro/recipes/statement.ex"
      provides: "Statement recipe refactored to delegate to Rendro.Recipes.Pagination"
      contains: "Rendro.Recipes.Pagination.chunk_rows_into_pages"
  key_links:
    - from: "lib/rendro/recipes/statement.ex"
      to: "lib/rendro/recipes/pagination.ex"
      via: "delegation calls to Pagination.chunk_rows_into_pages/2, Pagination.formatter/3, Pagination.label_resolver/1, Pagination.type_name/1"
      pattern: "Rendro\\.Recipes\\.Pagination\\."
---

<objective>
Extract shared recipe pagination/formatting machinery from statement.ex into a private Rendro.Recipes.Pagination helper module, add a pure Rendro.PageSize named-size helper, and refactor Statement to call the shared module — while keeping all 51 Statement tests green verbatim (the D-04 determinism contract per CONTEXT.md).

Purpose: This extraction is the blocking architectural prerequisite for Receipt (Plan 02) and Certificate (Plan 03). Both new recipes call Pagination.chunk_rows_into_pages/2, Pagination.formatter/3, and Pagination.label_resolver/1 rather than duplicating the logic. PageSize.resolve/2 is a direct Certificate dependency (D-07).

Output: lib/rendro/recipes/pagination.ex (private, @moduledoc false), lib/rendro/page_size.ex (@moduledoc false), lib/rendro/recipes/statement.ex (refactored — public API/behavior unchanged, 51 tests green).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-CONTEXT.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-RESEARCH.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Rendro.Recipes.Pagination (private shared helper) and Rendro.PageSize</name>
  <files>lib/rendro/recipes/pagination.ex, lib/rendro/page_size.ex</files>

  <read_first>
    - lib/rendro/recipes/statement.ex — source of verbatim extractions; read lines 375–436 (chunk_into_pages/5, do_chunk_pages/5, finalize_page/1) and lines 758–785 (formatter/3, label_resolver/1, type_name/1) before writing anything
    - lib/rendro/page_template.ex — read lines 1–15 to confirm @default_width 595.28 and @default_height 841.89 (the A4 constants PageSize must match exactly)
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-PATTERNS.md — Pattern 1 (pagination.ex module shape) and the PageSize module shape from Pattern 2 source
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-RESEARCH.md — Shared-Helper Extraction Detail section and Pattern 3 (PageSize module)
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-CONTEXT.md — D-04 (extraction scope, keep private) and D-07 (PageSize helper, landscape swap)
  </read_first>

  <action>
    Create lib/rendro/recipes/pagination.ex as defmodule Rendro.Recipes.Pagination with @moduledoc false.

    Export six public functions (all were defp in statement.ex — change to def):

    1. chunk_rows_into_pages/2 — accepts rows_with_meta (list of {fmt_row, height, opaque_meta} triples) and effective_capacity (float, pre-computed by caller). Returns [{[fmt_row], last_opaque_meta}] page tuples. Delegates to private do_chunk/5 and finalize_page/1. This replaces statement.ex's chunk_into_pages/5 + do_chunk_pages/5 + finalize_page/1. The CRITICAL change: the caller pre-computes effective_capacity (Statement computes "capacity - header_h - 2 * typical_row_h - @row_epsilon"; Receipt will compute "capacity - header_h - @row_epsilon"). The accumulator direction and finalize_page/1 reversal are preserved verbatim.

    2. formatter/3 — verbatim lift from statement.ex lines 760–763, def instead of defp.

    3. label_resolver/1 — verbatim lift from statement.ex lines 767–776, def instead of defp.

    4. type_name/1 — verbatim lift from statement.ex lines 779–785, all seven clauses, def instead of defp.

    Private helpers do_chunk/5 and finalize_page/1 stay private (defp) inside pagination.ex.

    The do_chunk/5 private function signature: (rows_with_meta, cap, current_page_acc, current_h, pages_acc). Pattern-match three clauses: empty-input with empty-acc, empty-input with non-empty-acc, and the main head-cons clause. The head-cons clause checks "new_h <= cap or current_page_acc == []" — if true, prepend {fmt_row, meta} to acc; if false, finalize current page and restart. Preserve this exact condition verbatim to keep Statement determinism unchanged.

    finalize_page/1 reverses the accumulator, extracts rows as the first element of each pair, and returns {rows, last_meta} where last_meta is the second element of the last pair. Verbatim from statement.ex lines 431–436 with "balance" generalized to "meta".

    Create lib/rendro/page_size.ex as defmodule Rendro.PageSize with @moduledoc false.

    Module attributes: @a4_portrait {595.28, 841.89} — must match page_template.ex @default_width and @default_height exactly. @us_letter_portrait {612.0, 792.0} — standard PostScript US Letter.

    Export resolve/2 with clauses: resolve(:a4, :portrait) returns @a4_portrait; resolve(:a4, :landscape) calls private swap/1 on @a4_portrait; resolve(:us_letter, :portrait) returns @us_letter_portrait; resolve(:us_letter, :landscape) calls swap/1 on @us_letter_portrait; resolve({w, h}, :portrait) returns {w, h} as-is (raw passthrough per D-07); resolve({w, h}, :landscape) calls swap/1. Private swap({w, h}) returns {h, w}.

    Default orientation argument is :portrait (via def resolve(size, orientation \\ :portrait)).

    No @moduledoc, no @doc — this module is internal. No new entries in mix.exs groups_for_modules (stays private this phase per D-07 / D-04).
  </action>

  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -v "^$"</automated>
  </verify>

  <done>
    mix compile --warnings-as-errors exits 0. Rendro.Recipes.Pagination and Rendro.PageSize modules exist in the compiled application. Rendro.PageSize.resolve(:a4, :landscape) == {841.89, 595.28}. Rendro.PageSize.resolve(:us_letter, :landscape) == {792.0, 612.0}.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor statement.ex onto Rendro.Recipes.Pagination + regression gate</name>
  <files>lib/rendro/recipes/statement.ex</files>

  <read_first>
    - lib/rendro/recipes/statement.ex — read lines 250–440 (body_section/2 including chunk_into_pages call at line 302, and the six defp functions to be removed at lines 375–436 and 758–785) before making any changes
    - lib/rendro/recipes/pagination.ex — just created in Task 1; confirm chunk_rows_into_pages/2 signature and that formatter/3, label_resolver/1, type_name/1 are all public def
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-PATTERNS.md — "lib/rendro/recipes/statement.ex (MODIFIED)" section showing the exact before/after for body_section/2 call site and the helper delegation pattern
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-RESEARCH.md — Statement Refactoring code example (the rows_with_meta zip + effective_capacity formula + Pagination.chunk_rows_into_pages call)
  </read_first>

  <action>
    Refactor lib/rendro/recipes/statement.ex to delegate to Rendro.Recipes.Pagination. This is a purely mechanical refactor — no logic changes, no behavior changes, no public API changes.

    Step 1 — Replace the chunk_into_pages call in body_section/2 (currently at line ~302). The existing call is:
      pages = chunk_into_pages(rows_with_balance, formatted_rows, row_heights, header_h, capacity)
    Replace with the three-step delegation pattern: (a) build rows_with_meta by zipping [formatted_rows, row_heights, rows_with_balance] and mapping each triple to {fmt_row, height, row_data.balance}; (b) compute typical_row_h (if Enum.empty?(row_heights), do: 14.4, else: Enum.sum(row_heights) / length(row_heights)); (c) compute effective_capacity = capacity - header_h - 2 * typical_row_h - @row_epsilon (preserving Statement's CF/BF overhead exactly — per RESEARCH.md pitfall 5, do NOT change this formula); (d) assign pages = Rendro.Recipes.Pagination.chunk_rows_into_pages(rows_with_meta, effective_capacity). The pages variable continues to hold [{[fmt_row], balance_at_break}] tuples — same shape as before, because Pagination.finalize_page/1 returns {rows, last_meta} where last_meta is the balance for Statement.

    Step 2 — Replace all inline helper call sites. Every occurrence of formatter(opts, key, default_fn) becomes Rendro.Recipes.Pagination.formatter(opts, key, default_fn). Every label_resolver(opts) call becomes Rendro.Recipes.Pagination.label_resolver(opts). Every type_name(value) call (inside validate_data!/1 error messages) becomes Rendro.Recipes.Pagination.type_name(value).

    Step 3 — Delete the six private function definitions from statement.ex: defp chunk_into_pages/5 (lines ~375–398), defp do_chunk_pages/5 (lines ~401–426), defp finalize_page/1 (lines ~431–436), defp formatter/3 (lines ~760–763), defp label_resolver/1 (lines ~767–776), defp type_name/1 all seven clauses (lines ~779–785). Confirm the file compiles after each deletion.

    The public API surface of statement.ex (document/2, page_template/1, sections/2, validate_data!/1) is NOT changed. All module attributes (@page_width, @page_height, @margin, @content_width, @header_height, @footer_height, @body_y, @body_height, @footer_y, @table_columns, @row_epsilon) remain. fold_balance/2 remains. All section builders (header_section/2, body_section/2, footer_section/2) remain — only the call sites inside body_section/2 change. All validate_data! functions remain — only type_name/value call sites inside error message strings change.

    Run mix format after the edit.
  </action>

  <verify>
    <automated>mix test test/rendro/recipes/statement_test.exs --no-deps-check 2>&1 | tail -5</automated>
  </verify>

  <done>
    mix test test/rendro/recipes/statement_test.exs exits 0 with "51 tests, 0 failures". statement.ex contains no defp chunk_into_pages, no defp do_chunk_pages, no defp finalize_page, no defp formatter, no defp label_resolver, no defp type_name. statement.ex contains Rendro.Recipes.Pagination.chunk_rows_into_pages. mix compile --warnings-as-errors exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statement.ex caller-supplied data | Caller-supplied data maps enter validate_data!/1; already mitigated by errors-as-product pattern — no change in this plan |
| Pagination module internals | chunk_rows_into_pages/2 receives pre-validated rows from recipes; no external untrusted input enters this module directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01-01 | Tampering | Mechanical refactor of statement.ex | mitigate | 51-test regression gate (Task 2 verify) proves behavior is byte-identical post-refactor; any deviation is caught immediately |
| T-75-01-02 | Denial of Service | chunk_rows_into_pages with empty rows_with_meta list | accept | Existing finalize_page/1 pattern handles empty input (returns Enum.reverse([]) = []) — same as Statement's current behavior; no new DoS surface |
| T-75-01-03 | Information Disclosure | PageSize module exposes internal dimension constants | accept | @moduledoc false; no public API surface added; dimensions are standard PostScript values with no confidentiality concern |
| T-75-01-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — pure Elixir file creation and refactoring; no slopcheck needed |
</threat_model>

<verification>
After both tasks complete:
- mix test test/rendro/recipes/statement_test.exs exits 0 (51 tests, 0 failures)
- mix compile --warnings-as-errors exits 0
- Rendro.PageSize.resolve(:a4, :landscape) == {841.89, 595.28}
- Rendro.PageSize.resolve(:us_letter, :landscape) == {792.0, 612.0}
- grep -c "Rendro.Recipes.Pagination.chunk_rows_into_pages" lib/rendro/recipes/statement.ex returns >= 1
- grep -v "^#" lib/rendro/recipes/statement.ex | grep -c "defp chunk_into_pages\|defp do_chunk_pages\|defp finalize_page\|defp formatter\|defp label_resolver\|defp type_name" returns 0
</verification>

<success_criteria>
- Rendro.Recipes.Pagination exists with 4 public functions: chunk_rows_into_pages/2, formatter/3, label_resolver/1, type_name/1
- Rendro.PageSize exists with resolve/2 covering :a4/:portrait, :a4/:landscape, :us_letter/:portrait, :us_letter/:landscape, and raw {w,h} passthrough
- Statement's 51 tests all pass with 0 failures after the refactor
- No defp chunk_into_pages, defp do_chunk_pages, defp finalize_page, defp formatter, defp label_resolver, or defp type_name remain in statement.ex
- mix compile --warnings-as-errors exits 0
</success_criteria>

<output>
Create .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-01-SUMMARY.md when done
</output>
