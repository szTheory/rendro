---
phase: 75-receipt-report-and-certificate-recipes-support-contract
plan: 04
type: execute
wave: 3
depends_on: ["75-02", "75-03"]
files_modified:
  - priv/support_matrix.json
  - mix.exs
autonomous: true
requirements:
  - CONTRACT-01
must_haves:
  truths:
    - "priv/support_matrix.json contains top-level keys: page_numbering, statement, receipt_report, certificate — all with status: supported and a resolvable evidence pointer"
    - "No new surface ships as silent unverified (v2.3 discipline preserved)"
    - "mix test test/docs_contract/viewer_evidence_claims_test.exs exits 0 (21 tests, 0 failures) after adding all new rows"
    - "The new rows do NOT have a viewers sub-key (non-viewer-sensitive surfaces per D-09 — bypasses viewer_row $def)"
    - "Rendro.Recipes.Statement and Rendro.Recipes.Receipt and Rendro.Recipes.Certificate appear in mix.exs groups_for_modules under Canonical Recipes"
  artifacts:
    - path: "priv/support_matrix.json"
      provides: "Terminal support-matrix rows for all new surfaces: page_numbering (Phase 73), statement (Phase 74 backfill), receipt_report, certificate"
      contains: "\"page_numbering\""
    - path: "mix.exs"
      provides: "Canonical Recipes module group updated to include Statement, Receipt, Certificate"
      contains: "Rendro.Recipes.Receipt"
  key_links:
    - from: "priv/support_matrix.json"
      to: "test/rendro/recipes/receipt_test.exs"
      via: "evidence pointer in receipt_report row"
      pattern: "receipt_test\\.exs"
    - from: "priv/support_matrix.json"
      to: "test/rendro/recipes/certificate_test.exs"
      via: "evidence pointer in certificate row"
      pattern: "certificate_test\\.exs"
---

<objective>
Close the CONTRACT-01 requirement by adding five terminal support-matrix rows to priv/support_matrix.json and backfilling the Statement row (shipped Phase 74 without one per D-10). All rows are non-viewer-sensitive flat objects (D-09), with status: supported and evidence pointers referencing the recipe test files. Update mix.exs Canonical Recipes group to include Statement, Receipt, and Certificate. Confirm the existing docs-contract lane (21 tests) passes unchanged.

Purpose: CONTRACT-01 — every new public surface (PAGE primitive, Statement, Receipt, Certificate) has a terminal support-matrix row. This plan runs last (Wave 3) because it references test files that must exist and pass (Plans 02 + 03). Phase 75 success criterion #5: "no surface ships as silent unverified."

Output: priv/support_matrix.json with 4 new top-level keys, mix.exs with updated Canonical Recipes group.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-CONTEXT.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-RESEARCH.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-PATTERNS.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-02-SUMMARY.md
@.planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add four terminal support-matrix rows and update mix.exs Canonical Recipes group</name>
  <files>priv/support_matrix.json, mix.exs</files>

  <read_first>
    - priv/support_matrix.json — read fully to understand the current top-level structure, the positions of existing keys (forms, signing, protection), and the exact JSON surrounding the last key to determine where to insert the new keys without breaking structure
    - priv/schemas/support_matrix.schema.json — read lines 1–30 to confirm "additionalProperties": true at root (line 11) and that "required" keys are ["forms", "signing", ...] (still satisfied); confirm viewer_row $def is ONLY under $defs/viewer_map — new rows without a "viewers" sub-key must NOT trigger it
    - lib/rendro/viewer_evidence/matrix.ex — scan for @viewer_maps attribute to confirm it does NOT include new surface keys (new rows will not be enumerated by enumerate_viewer_cells/1, so validate_promotion_complete will not check them)
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-PATTERNS.md — support_matrix.json Pattern 5 showing the exact JSON shape of the five new rows with no "viewers" sub-key
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-RESEARCH.md — Pattern 5 (non-viewer row shape, schema analysis, D-09/D-10), Common Pitfall 4 (schema rejection if "viewers" sub-key accidentally added)
    - .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-CONTEXT.md — D-09 (non-viewer-sensitive, flat object shape), D-10 (five surface keys: page_numbering, statement, receipt_report, certificate; backfill Statement row)
    - mix.exs — read lines 121–148 to see current groups_for_modules structure under "Canonical Recipes" (currently: Rendro.Recipes, Invoice, BrandedInvoice)
  </read_first>

  <action>
    Edit priv/support_matrix.json to add four new top-level keys after the existing "protection" block (before closing brace of root object if there is one, or following the last existing key).

    The four keys are (D-10):

    "page_numbering": flat object with surface: "page_numbering", status: "supported", evidence: "test/rendro/pipeline/paginate_test.exs", recorded_at: "2026-05-29", capabilities object with single_pass_substitution: "supported", deterministic_output: "supported", suppress_on_first_page: "supported". No "viewers" sub-key.

    "statement": flat object with surface: "statement", status: "supported", evidence: "test/rendro/recipes/statement_test.exs", recorded_at: "2026-05-29", capabilities object with multi_page_table_continuation: "supported", running_footer_page_number: "supported", deterministic_output: "supported". No "viewers" sub-key. (This is the Statement backfill per D-10 — shipped Phase 74 without a row.)

    "receipt_report": flat object with surface: "receipt_report", status: "supported", evidence: "test/rendro/recipes/receipt_test.exs", recorded_at: "2026-05-29", capabilities object with multi_page_table_continuation: "supported", running_footer_page_number: "supported", deterministic_output: "supported". No "viewers" sub-key.

    "certificate": flat object with surface: "certificate", status: "supported", evidence: "test/rendro/recipes/certificate_test.exs", recorded_at: "2026-05-29", capabilities object with geometry_derived_layout: "supported", multi_page_size: "supported", branded_output: "supported", deterministic_output: "supported". No "viewers" sub-key.

    CRITICAL: Do NOT add a "viewers" key inside any of these objects. The viewer_row $def in the JSON-Schema enforces a strict pattern (evidence path must match ^priv/viewer_evidence/...) that would reject non-viewer evidence strings. Flat objects without "viewers" bypass this $def entirely.

    After editing the JSON, verify it is valid JSON (well-formed braces/commas) before proceeding to tests.

    Edit mix.exs groups_for_modules "Canonical Recipes" list to add Rendro.Recipes.Statement, Rendro.Recipes.Receipt, and Rendro.Recipes.Certificate alongside the existing Rendro.Recipes, Rendro.Recipes.Invoice, Rendro.Recipes.BrandedInvoice entries.

    Run mix format after editing mix.exs.
  </action>

  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs --no-deps-check 2>&1 | tail -5</automated>
  </verify>

  <done>
    mix test test/docs_contract/viewer_evidence_claims_test.exs exits 0 (21 tests, 0 failures). priv/support_matrix.json contains keys "page_numbering", "statement", "receipt_report", "certificate" at the top level. None of the new keys has a "viewers" sub-object. mix.exs Canonical Recipes group includes Rendro.Recipes.Statement, Rendro.Recipes.Receipt, Rendro.Recipes.Certificate.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full suite green gate</name>
  <files></files>

  <read_first>
    - No files to read — this task runs the full test suite and confirms zero regressions across all Phase 75 deliverables.
  </read_first>

  <action>
    Run the full test suite: mix test. If any tests fail, investigate and fix before marking this task complete.

    Key test groups that must all pass:
    - test/rendro/recipes/statement_test.exs (51 tests — D-04 regression)
    - test/rendro/recipes/receipt_test.exs (V1..V10 — RCPT-01/02/03)
    - test/rendro/recipes/certificate_test.exs (C1..C13 — CERT-01/02/03)
    - test/docs_contract/viewer_evidence_claims_test.exs (21 tests — CONTRACT-01 schema gate)
    - All other existing tests (no regressions in engine or other recipe tests)

    If mix test passes, also run mix compile --warnings-as-errors to confirm no warnings were introduced.
  </action>

  <verify>
    <automated>mix test 2>&1 | tail -10</automated>
  </verify>

  <done>
    mix test exits 0 with 0 failures across the full suite. mix compile --warnings-as-errors exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| priv/support_matrix.json edit | Additive JSON edit; new rows must be valid JSON and schema-valid; the docs-contract test lane is the automated gate |
| Support-matrix "supported" claim integrity | A surface must NOT be marked "supported" without real test evidence — this is the CONTRACT-01 integrity threat (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Spoofing | Marking a surface "supported" without evidence | mitigate | Plan 04 depends_on Plans 02 and 03 — evidence files (receipt_test.exs, certificate_test.exs) must exist and pass before this plan runs; evidence pointer strings reference actual test files, not fabricated paths |
| T-75-04-02 | Tampering | Adding a "viewers" sub-key to new rows, triggering viewer_row $def validation failure | mitigate | CRITICAL action note: new rows must NOT have "viewers" sub-key; after edit, docs-contract test gate (Task 1 verify) catches schema rejection immediately |
| T-75-04-03 | Tampering | JSON syntax error in support_matrix.json breaking schema validation | mitigate | Task 1 action: verify JSON is well-formed before running tests; docs-contract test gate catches parse errors |
| T-75-04-04 | Information Disclosure | Canonical Recipes mix.exs group exposing internal module Rendro.Recipes.Pagination | accept | Rendro.Recipes.Pagination has @moduledoc false and is NOT added to groups_for_modules; only public recipe modules (Statement, Receipt, Certificate) are added |
| T-75-04-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — pure JSON and Elixir config edits |
</threat_model>

<verification>
After both tasks complete:
- mix test exits 0 (full suite, 0 failures)
- mix compile --warnings-as-errors exits 0
- grep -c "\"page_numbering\"" priv/support_matrix.json returns >= 1
- grep -c "\"statement\"" priv/support_matrix.json returns >= 1
- grep -c "\"receipt_report\"" priv/support_matrix.json returns >= 1
- grep -c "\"certificate\"" priv/support_matrix.json returns >= 1
- mix test test/docs_contract/viewer_evidence_claims_test.exs exits 0 (21 tests, 0 failures)
- grep -c "Rendro.Recipes.Receipt" mix.exs returns >= 1 (Canonical Recipes group)
</verification>

<success_criteria>
- CONTRACT-01: priv/support_matrix.json has terminal rows for page_numbering, statement (backfill), receipt_report, certificate — all with status: "supported" and evidence pointers
- No new surface ships as silent unverified
- docs-contract lane (21 tests) passes unchanged
- Full suite green: 0 failures across statement (51), receipt (V1..V10), certificate (C1..C13), docs-contract (21), and all existing tests
- mix compile --warnings-as-errors exits 0
</success_criteria>

<output>
Create .planning/phases/75-receipt-report-and-certificate-recipes-support-contract/75-04-SUMMARY.md when done
</output>
