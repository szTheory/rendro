---
phase: 115-invoice-anatomy-upgrade-format-public-promotion-palette-alig
plan: 03
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - lib/rendro/cell.ex
  - lib/rendro/table.ex
  - lib/rendro.ex
  - lib/rendro/pipeline/paginate.ex
  - lib/rendro/pdf/writer.ex
  - test/rendro/table_cell_align_test.exs
autonomous: true
requirements: [INV-05]
must_haves:
  truths:
    - "A table cell/column with `cell_align: :right` right-aligns its content within the column width."
    - "A table with NO `cell_align` set renders byte-identically to the pre-Phase-115 baseline (plan-01 table golden still passes)."
    - "No new public function is added to the frozen Stable tier (`cell_align` is a struct field + an option on the existing `Rendro.table/2`)."
  artifacts:
    - lib/rendro/cell.ex
    - lib/rendro/table.ex
    - test/rendro/table_cell_align_test.exs
  key_links:
    - "The right-align x-offset is gated STRICTLY on `cell_align == :right`; the `nil`/`:left` branch takes the exact unchanged layout path (RESEARCH Pitfall 2)."
---

<objective>
Add the additive `cell_align: :right` primitive (INV-05) — the ONLY net-new engine primitive this phase and the only place byte-compatibility can silently break. Cell/Table carry an inert `cell_align` field (default preserves today's left path), `Rendro.table/2` accepts it as an option, and the layout applies a right-align x-offset ONLY when `:right`. Resolves RESEARCH Open Question 2 (OQ2: paginate `stack_cells` vs writer `render_text_block`) via a first-task spike.

Purpose: right-align tabular money without widening the frozen Stable tier and without perturbing any existing table's bytes.
Output: an opt-in `cell_align: :right`, guarded by the plan-01 table byte-identity golden plus a new positive-alignment test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-RESEARCH.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-PATTERNS.md
@lib/rendro/cell.ex
@lib/rendro/table.ex
@lib/rendro.ex
@lib/rendro/pipeline/paginate.ex
@lib/rendro/pdf/writer.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1 (SPIKE + inert scaffold): add inert cell_align field/option and decide the offset location</name>
  <files>lib/rendro/cell.ex, lib/rendro/table.ex, lib/rendro.ex</files>
  <read_first>
    - lib/rendro/cell.ex:5 (`@moduledoc tags: [:stable]`), :8-21 (defstruct with inert `keep_together: false` peers), :30-40 (@type)
    - lib/rendro/table.ex:5 (`:stable`), :8-24 (inert `borders: :none`/`header_fill: nil` block + @type)
    - lib/rendro.ex:324-335 (`table/2` + `@spec table([Table.row()], keyword()) :: Table.t()`), :369-394 (`normalize_table_attrs/1` → `normalize_table_borders/1` pipeline shape)
    - lib/rendro/pipeline/paginate.ex:594-602 (`stack_cells/4` — cell x set to column-left, advances by col_w; measured cell.width already set by Measure)
    - lib/rendro/pdf/writer.ex:702-703 (`render_text_block/8` computes text x = `block.x + ox + page.margin_left`)
  </read_first>
  <action>
Two moves in one task.

(a) INERT SCAFFOLD — add a `cell_align` field defaulting to `:left` to the `Cell` defstruct (mirror the inert `keep_together: false` peers) and to its `@type`. Add an optional column-level/table-level `cell_align` to `Table` following the inert `borders: :none`/`header_fill: nil` comment block and `@type`. Surface it in the `Rendro.table/2` facade by adding a `normalize_table_cell_align/1` stage in the same shape as `normalize_table_borders/1`, so an unset `cell_align` yields the inert default. CRITICAL: `Cell`/`Table` are `@moduledoc tags: [:stable]` — adding a struct FIELD does not widen the manifest (functions+types only). Do NOT add any new public function such as a `Rendro.cell_align/…` helper, and keep `@spec table(...)` arity unchanged, or the surface-equality + `@spec`-coverage assertions fail (RESEARCH Pitfall 3).

(b) SPIKE / DECISION (OQ2) — determine where the measured per-line text width needed for right-align slack (`col_width - text_width - padding`) is already available: at `stack_cells` time in `paginate.ex` (post-Measure, cell.width known) versus at `render_text_block` time in `writer.ex`. Prototype the offset in both locations, pick the one where the measured width is already known so no re-measuring is required, and RECORD the chosen location + rationale in the plan summary. Apply NO offset in this task — the field stays inert.

Acceptance of inertness: with the field/option added and NO offset applied, the plan-01 table byte-identity golden (`test/rendro/table_byte_identity_test.exs`) must still pass — proving the additive field changes nothing by default (byte-identical either way, per the OQ2 acceptance bar).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/rendro/table_byte_identity_test.exs test/docs_contract/public_api_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Cell` and `Table` structs each carry a `cell_align` field with an inert default; both `@type`s updated.
    - `Rendro.table/2` accepts `cell_align:` via a `normalize_table_cell_align/1` stage; `@spec table/2` arity unchanged; no new public function added.
    - `mix test test/rendro/table_byte_identity_test.exs` still passes (inert field = byte-identical default).
    - `mix test test/docs_contract/public_api_contract_test.exs` still passes (Stable tier not widened).
    - The plan summary records the chosen offset location (paginate vs writer) and why.
  </acceptance_criteria>
  <done>An inert `cell_align` field/option exists end-to-end, the Stable tier is not widened, the default render is byte-identical, and the offset location is decided.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the right-align x-offset gated strictly on cell_align == :right</name>
  <files>lib/rendro/pipeline/paginate.ex, lib/rendro/pdf/writer.ex</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex:594-602 (`stack_cells/4` default-left reduce — this exact branch must remain the path for nil/:left)
    - lib/rendro/pdf/writer.ex:702-703 (text-x computation — the no-cell_align path that must NOT change for left/default cells)
    - The offset-location decision recorded in Task 1's work / summary
  </read_first>
  <action>
At the location chosen in Task 1, compute a right-align x-offset = `col_width - measured_text_width - trailing_padding` and shift the cell's content x rightward, applied ONLY when the cell's `cell_align == :right`. The `nil`/`:left` (default) branch MUST take the existing unchanged code path — do not centralize the x computation across all cells (RESEARCH Pitfall 2: any diff to the no-`cell_align` text-x path breaks byte-compat for every existing table). Use the measured width already available post-Measure; do not re-measure. Guard with an explicit match/branch on `:right` so no other value alters placement.

Do not touch the left/default arithmetic (`block.x + ox + page.margin_left` in writer, or `x + col_w` advance in paginate) for non-right cells.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/rendro/table_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The right-align offset code path is entered ONLY when `cell_align == :right` (explicit branch).
    - `mix test test/rendro/table_byte_identity_test.exs` STILL passes after the offset is added (no-op guarantee for default tables — INV-05).
    - No change to the left/default x arithmetic for non-right cells (diff shows the default branch untouched).
  </acceptance_criteria>
  <done>`cell_align: :right` shifts content to the column's right edge while every non-right cell renders byte-identically to before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Positive right-align test + no-op regression proof</name>
  <files>test/rendro/table_cell_align_test.exs</files>
  <behavior>
    - A two-column table with `cell_align: :right` on the money column places that column's text further right than the same table without the option (measurable via a coordinate/offset difference or a distinct sha256).
    - The same table with no `cell_align` produces the plan-01 baseline bytes (no-op).
    - `cell_align: :right` on a table is deterministic across two renders (`pdf1 == pdf2`).
  </behavior>
  <read_first>
    - test/rendro/recipes/branded_invoice_test.exs:166-171 (two-render determinism pattern)
    - test/rendro/table_byte_identity_test.exs (plan-01 no-op baseline this task must NOT break)
  </read_first>
  <action>
Create `test/rendro/table_cell_align_test.exs` (async: true) with: (1) a POSITIVE test rendering a table twice — once with `cell_align: :right` on a money column and once without — asserting the two renders differ (proves the offset is applied) AND that the right-aligned render is deterministic across two passes; (2) an explicit no-op assertion that a table built with no `cell_align` matches the plan-01 baseline behavior (re-derive or cross-reference the byte-identity golden). Do NOT edit `test/rendro/table_byte_identity_test.exs` — it stays the frozen guard; this file only adds positive + differential coverage.
  </action>
  <verify>
    <automated>mix test test/rendro/table_cell_align_test.exs test/rendro/table_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/table_cell_align_test.exs` passes.
    - The positive test proves right vs no-right renders DIFFER (offset applied) and the right render is deterministic.
    - `test/rendro/table_byte_identity_test.exs` remains green (no-op preserved).
  </acceptance_criteria>
  <done>Right-alignment is proven to work and proven to be a strict no-op when unused.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `Cell.cell_align` field (default `:left`) — `lib/rendro/cell.ex` (struct + @type).
- `Table` `cell_align` option/field (inert default) — `lib/rendro/table.ex` (struct + @type).
- `normalize_table_cell_align/1` private stage + `cell_align:` option on `Rendro.table/2` — `lib/rendro.ex`.
- Right-align x-offset (gated on `:right`) in the chosen layout module — `lib/rendro/pipeline/paginate.ex` OR `lib/rendro/pdf/writer.ex`.
- `test/rendro/table_cell_align_test.exs` — new positive + differential + no-op test.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new layout branch → existing render output | The right-align offset must not leak into the default path that produces every existing table's bytes. |
| struct field → frozen Stable manifest | `Cell`/`Table` are Stable-tier; the manifest must not gain a new function/type. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-03-01 | Tampering | Default table byte-compat | high | mitigate | Offset gated strictly on `cell_align == :right`; nil/:left branch is the exact unchanged path; the plan-01 table byte-identity golden runs in every task's verify to catch any silent perturbation (INV-05). |
| T-115-03-02 | Elevation of Privilege | Stable-tier surface freeze | high | mitigate | `cell_align` is a struct FIELD + an option on the existing `Rendro.table/2` (arity/@spec unchanged); no new public function; the public-API contract test's surface-equality + @spec-coverage assertions run in Task-1 verify to prove the Stable manifest is unchanged. |
| T-115-03-03 | Denial of Service | Oversized/degenerate offset input | low | accept | `:right` computes a non-negative slack from measured widths; a negative slack (text wider than column) falls back to zero offset (no shift), avoiding a crash. Accept — no untrusted network input. |
</threat_model>

<verification>
- `mix test test/rendro/table_cell_align_test.exs test/rendro/table_byte_identity_test.exs` — green.
- `mix test test/docs_contract/public_api_contract_test.exs` — green (Stable tier unchanged).
- `mix compile --warnings-as-errors` — clean.
</verification>

<success_criteria>
- `cell_align: :right` right-aligns; unused tables are byte-identical; Stable tier not widened.
- OQ2 offset location decided and recorded.
</success_criteria>

<output>
Create `.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-03-SUMMARY.md` when done, recording the chosen offset location.
</output>
