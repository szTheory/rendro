---
phase: 115-invoice-anatomy-upgrade-format-public-promotion-palette-alig
plan: 02
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - lib/rendro/format.ex
  - lib/mix/tasks/rendro/api.gen.ex
  - test/docs_contract/public_api_contract_test.exs
  - priv/public_api.json
autonomous: true
requirements: [INV-04]
must_haves:
  truths:
    - "`Rendro.Format` reports its adapter tier (not `:hidden`) and appears in `priv/public_api.json` with functions money/1, date/1, label/1."
    - "The public-API contract lane (including Phase-79 hidden set) passes green after the promotion."
    - "`Rendro.Format` docs carry a migration note and an explicit 'output may evolve' caveat."
  artifacts:
    - lib/rendro/format.ex
    - priv/public_api.json
  key_links:
    - "All four artifacts (moduledoc flip, @public_modules append, hidden-set delete, regenerated manifest) move in ONE atomic commit or the contract lane stays red."
---

<objective>
Promote `Rendro.Format` from `@moduledoc false` to the public **adapter** tier — the milestone's single IRREVERSIBLE act (INV-04). The functions `money/1`, `date/1`, `label/1` already exist with `@spec`s and doctests; this is a tier flip plus documentation, NOT writing functions. Held to the adapter/Evolving tier with the smallest useful surface and an explicit "output may evolve" caveat so the SemVer promise is capped.

Purpose: Once `Format` is public and in `public_api.json`, the surface cannot be narrowed without a breaking change — so this plan is isolated as its own reviewable atomic commit.
Output: A public adapter-tier `Rendro.Format`, regenerated manifest, and a green contract lane (deliberately red until all four artifacts move together).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-RESEARCH.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-PATTERNS.md
@lib/rendro/format.ex
@lib/mix/tasks/rendro/api.gen.ex
@test/docs_contract/public_api_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Rendro.Format to public adapter tier with migration + evolving-output docs</name>
  <files>lib/rendro/format.ex</files>
  <read_first>
    - lib/rendro/format.ex:1-2 (current `@moduledoc false`) and :37,:59,:76 (existing money/1, date/1, label/1 @specs + doctests — DO NOT change their bodies)
    - lib/rendro/recipes/receipt.ex:1-76 (two-attribute shape: `@moduledoc """..."""` followed by `@moduledoc tags: [:adapter]`)
  </read_first>
  <action>
Replace `@moduledoc false` at `format.ex:2` with a real moduledoc string followed by `@moduledoc tags: [:adapter]` (mirror the two-attribute pattern in `receipt.ex`). The moduledoc must: describe the module as pure, locale-free, deterministic formatting helpers for money, dates, and labels; include a **migration note** (previously internal, now public at the adapter/Evolving tier); and include an explicit caveat that formatted OUTPUT may evolve across minor versions and callers who need exact strings should pin them in their own golden tests.

Do NOT modify the implementations or `@spec`s of `money/1`, `date/1`, `label/1` — they already satisfy the surface. Confirm at least one existing doctest remains public so HexDocs renders the adapter surface (Wave-0 doc gap). This is the tier flip only; per INV-02 the recipe money-routing lives in plan 04, not here.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/rendro/format_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `format.ex` no longer contains `@moduledoc false`; it has a moduledoc string AND `@moduledoc tags: [:adapter]`.
    - The moduledoc contains a migration note AND an "output may evolve" caveat.
    - `mix test test/rendro/format_test.exs` passes (doctests still green; function bodies unchanged).
  </acceptance_criteria>
  <done>`Rendro.Format` reports the adapter tier via its `@moduledoc tags:` and documents its evolving-output promise, with all three functions unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Register Format, delete it from the hidden set, regenerate the manifest (atomic)</name>
  <files>lib/mix/tasks/rendro/api.gen.ex, test/docs_contract/public_api_contract_test.exs, priv/public_api.json</files>
  <read_first>
    - lib/mix/tasks/rendro/api.gen.ex:77-92 (the Adapter-tier block of `@public_modules`; `Rendro.Inspector` at :86 is the alphabetical neighbor)
    - test/docs_contract/public_api_contract_test.exs:85-92 (the `hidden_modules` list; `Rendro.Format` is at :89) and :94-120 (the loop asserting each stays `:hidden`)
    - priv/public_api.json:40-70 (an existing adapter entry shape `{functions, tier, types}` nested under top-level `modules`)
  </read_first>
  <action>
Three coordinated edits that MUST land in the same commit as Task 1:

1. In `api.gen.ex`, add `Rendro.Format,` to the Adapter-tier block of `@public_modules` (alphabetical neighborhood of `Rendro.Inspector` at :86). Tier is inferred from the module's own `@moduledoc tags:`, so no tier argument is passed — the list is a membership registry only.

2. In `public_api_contract_test.exs`, DELETE the `Rendro.Format,` line (:89) from `hidden_modules`. Leaving it in makes the :94-120 loop fail with "Expected Rendro.Format to have @moduledoc false" once Task 1 flips the tag. Leave the other five hidden modules untouched.

3. Regenerate `priv/public_api.json` by running `mix rendro.api.gen` (do NOT hand-edit the JSON). The generator adds an `Elixir.Rendro.Format` entry under `modules` with `functions: ["date/1", "label/1", "money/1"]`, `tier: "adapter"`, `types: []`. Commit the regenerated file. The contract test regenerates in-memory and asserts byte-equality against the committed file, so a hand edit that drifts will fail the surface-equality assertion.
  </action>
  <verify>
    <automated>mix rendro.api.gen && git diff --exit-code priv/public_api.json; mix test test/docs_contract/public_api_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - After running `mix rendro.api.gen`, `git diff --exit-code priv/public_api.json` is clean (committed manifest already matches generator output).
    - `priv/public_api.json` contains an `Elixir.Rendro.Format` entry with tier `adapter` and functions date/1, label/1, money/1.
    - `Rendro.Format` no longer appears in the `hidden_modules` list.
    - `mix test test/docs_contract/public_api_contract_test.exs` passes (red→green resolved).
  </acceptance_criteria>
  <done>All four artifacts are consistent: `Format` is registered, removed from the hidden set, present in the regenerated manifest, and the contract lane is green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `lib/rendro/format.ex` — `@moduledoc tags: [:adapter]` (tier flip) + migration/evolving-output docs. No new functions.
- `lib/mix/tasks/rendro/api.gen.ex` — `Rendro.Format` added to `@public_modules` (adapter block).
- `test/docs_contract/public_api_contract_test.exs` — `Rendro.Format` removed from `hidden_modules`.
- `priv/public_api.json` — new `Elixir.Rendro.Format` adapter entry (functions date/1, label/1, money/1; types []).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public SemVer surface ← `public_api.json` | Once `Format` crosses into the public manifest, the surface is frozen against narrowing; this is a one-way commitment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-02-01 | Information Disclosure | Over-exposing internal surface | high | mitigate | Promote ONLY money/1, date/1, label/1 at the adapter/Evolving tier; the `@moduledoc tags: [:adapter]` inference means no accidental extra functions leak into the manifest (contract test asserts byte-equality). The "output may evolve" caveat caps the SemVer promise so formatting can change without a breaking release. |
| T-115-02-02 | Tampering | Drifted/hand-edited manifest | high | mitigate | `priv/public_api.json` is regenerated by `mix rendro.api.gen`, never hand-edited; `git diff --exit-code` + the contract test's in-memory byte-equality assertion block any drift. |
| T-115-02-03 | Repudiation | Partial (non-atomic) promotion | medium | mitigate | All four artifacts move in one commit; the contract lane stays red until they agree, making a partial promotion impossible to merge green. |
</threat_model>

<verification>
- `mix test test/docs_contract/public_api_contract_test.exs` — green (tier + surface-equality + hidden-set assertions).
- `mix rendro.api.gen && git diff --exit-code priv/public_api.json` — clean.
- `mix test test/rendro/format_test.exs` — green (function bodies unchanged).
</verification>

<success_criteria>
- `Rendro.Format` is public at the adapter tier with a capped, minimal, documented surface.
- The contract lane is green; the promotion is a single atomic, reviewable commit.
</success_criteria>

<output>
Create `.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-02-SUMMARY.md` when done.
</output>
