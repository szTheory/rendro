---
phase: 115-invoice-anatomy-upgrade-format-public-promotion-palette-alig
plan: 04
type: execute
wave: 3
depends_on: [115-01, 115-02]
files_modified:
  - lib/rendro/recipes/invoice.ex
  - test/rendro/recipes/invoice_test.exs
  - test/rendro/recipes/invoice_opts_threading_test.exs
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-06, INV-07]
must_haves:
  truths:
    - "The toy call `%{id:, date:, items:}` renders byte-identically to before (plan-01 golden still green) while `:issuer`/`:customer`/`:due_date`/`:terms`/`:totals` render only when present (INV-01)."
    - "New Decimal money fields route through `Rendro.Format.money/1`; the bare-number `price` still renders `\"$#{price}\"`; Floats in new fields are rejected instructively (INV-02)."
    - "A `:totals` block renders only when supplied, is asserted via `Decimal.equal?/2`, and is kept with the last table rows across a page break (INV-03)."
    - "`validate_data!/1` raises an instructive `ArgumentError` on malformed input (never leaking `BadMapError`/`FunctionClauseError`) and never rejects a valid toy call (INV-06)."
    - "Every section reads colors via a private `palette(opts)`; the `page_template/1` opts leak is closed via `Keyword.take` while top-level `opts` stays open (INV-07)."
  artifacts:
    - lib/rendro/recipes/invoice.ex
    - test/rendro/recipes/invoice_test.exs
  key_links:
    - "The frozen toy header lines (`invoice.ex:159-160`) and the `\"$#{item.price}\"` body cell (`:168`) MUST stay untouched — new code runs only for new fields (RESEARCH A1 / PATTERNS)."
---

<objective>
Upgrade `Rendro.Recipes.Invoice` from the toy recipe to full invoice anatomy — additive optional `:issuer`, `:customer`, `:due_date`, `:terms`, `:totals`; Decimal money via the now-public `Rendro.Format.money/1`; `validate_data!/1` errors-as-product; a `Decimal.equal?/2`-asserted totals block kept with the last rows; a private `palette(opts)` color seam (S1); and a `page_template/1` `Keyword.take` whitelist. This is ~90% a mechanical mirror of `Rendro.Recipes.Receipt` (totals/validation) plus `Rendro.Recipes.Statement` (whitelist, single-value assertion). Covers INV-01, INV-02, INV-03, INV-06, INV-07.

Purpose: close the toy→production gap for Invoice while keeping the pre-upgrade toy call byte-identical.
Output: an upgraded, validated, palette-seamed Invoice recipe with comprehensive tests, guarded by the plan-01 toy golden.
Note to executor: this is mechanical mirroring — read ONLY the cited `receipt.ex`/`statement.ex` line ranges (not the whole files) to keep context low.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-RESEARCH.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-PATTERNS.md
@lib/rendro/recipes/invoice.ex
@lib/rendro/recipes/receipt.ex
@lib/rendro/recipes/statement.ex
@lib/rendro/recipes/pagination.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validate_data!/1 (INV-06) + palette/1 + page_template/1 whitelist (INV-07)</name>
  <files>lib/rendro/recipes/invoice.ex, test/rendro/recipes/invoice_opts_threading_test.exs</files>
  <behavior>
    - A non-map `data` raises `ArgumentError` with a What/Where/Why/Next message naming `Rendro.Recipes.Invoice`, never `BadMapError`/`FunctionClauseError`.
    - A map missing `:id`/`:date`/`:items` raises an instructive `ArgumentError`; the valid toy call (`:id`,`:date`,`:items` only) is accepted.
    - A `:palette` and `:formatters` option threads through `opts` to `sections/2` WITHOUT reaching `struct!(PageTemplate, …)` (no `KeyError`).
    - The toy call still renders (plan-01 golden green).
  </behavior>
  <read_first>
    - lib/rendro/recipes/invoice.ex:62-96 (`page_template/1` — the `Keyword.merge(defaults, opts)` leak at :96), :112-118 (`sections/2`), :154-192 (private builders)
    - lib/rendro/recipes/receipt.ex:197-198 (validate_data! called at top of sections), :363-378 (non-map + missing-key heads), :479-502 (is_float head BEFORE generic non-Decimal head)
    - lib/rendro/recipes/statement.ex:184-196 (the `Keyword.take` whitelist)
    - lib/rendro/recipes/pagination.ex:57-73 (`formatter/3`, `label_resolver/1`), :76-82 (`type_name/1` for the Why line)
  </read_first>
  <action>
Add `validate_data!/1` to `invoice.ex` mirroring `receipt.ex:363-378`: a non-map head raising a multi-line `ArgumentError` (What: data must be a map / Where: `Rendro.Recipes.Invoice.validate_data!/1` / Why: received `#{inspect(data)}` of type `Rendro.Recipes.Pagination.type_name(data)` / Next: pass a map with `:id`, `:date`, `:items`), plus a map head asserting the three REQUIRED keys are present. Required keys are ONLY `:id`, `:date`, `:items` — all anatomy fields are optional; the toy call must never be rejected (INV-06 landmine). Call `validate_data!(data)` at the top of both `sections/2` and `document/2` (mirror receipt.ex:197-198).

Add the private `palette(opts)` seam (INV-07): a helper merging `Keyword.get(opts, :palette, %{})` over a default role map keyed `ink`/`muted`/`accent`/`on_accent`/`background`/`surface`/`rule` whose defaults reproduce today's literals (all-black inks, white surfaces) so untouched sections stay byte-identical. Any color a section SETS must come from a palette role (a `{r,g,b}` tuple passed as `color:`); do not hardcode literal RGB tuples in section builders. Note: the FROZEN toy header lines (`invoice.ex:159-160`) set NO color and MUST stay untouched — they remain compliant unchanged.

Close the `page_template/1` leak (INV-07) by replacing `Keyword.merge(defaults, opts)` at :96 with the Statement `Keyword.take` whitelist (`statement.ex:184-196`): take only `[:name, :width, :height, :margin_top, :margin_right, :margin_bottom, :margin_left, :regions]` into `template_opts`, then `Rendro.page_template(Keyword.merge(defaults, template_opts))`. Top-level `opts` stays fully open (still threads to `sections/2` and `palette/1`) for Milestone-B's future `theme:`. Use `Rendro.Recipes.Pagination.formatter/3` + `label_resolver/1` for any override plumbing — do not hand-roll opts parsing.

Extend `test/rendro/recipes/invoice_opts_threading_test.exs` proving `:palette`/`:formatters` thread through `opts` without reaching `struct!(PageTemplate, …)`, and that a `:palette` override changes only the intended section color.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/rendro/recipes/invoice_opts_threading_test.exs test/rendro/recipes/invoice_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Non-map data and missing-required-key data each raise `ArgumentError` (not `BadMapError`/`FunctionClauseError`); message contains "What:"/"Where:"/"Why:"/"Next:".
    - The toy call (`:id`,`:date`,`:items` only) is accepted and `test/rendro/recipes/invoice_byte_identity_test.exs` stays green.
    - `page_template/1` uses `Keyword.take`; passing `:palette`/`:formatters` does NOT raise `KeyError`.
    - No section builder hardcodes a literal RGB tuple; colors come from `palette(opts)`.
    - `mix test test/rendro/recipes/invoice_opts_threading_test.exs` passes.
  </acceptance_criteria>
  <done>Invoice validates input as errors-as-product, reads colors via the palette seam, and no longer leaks recipe opts into the page template — with the toy call still byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive optional anatomy fields + Decimal money split (INV-01, INV-02)</name>
  <files>lib/rendro/recipes/invoice.ex, test/rendro/recipes/invoice_test.exs</files>
  <behavior>
    - `:issuer`, `:customer`, `:due_date`, `:terms` render ONLY when present in `data`; absent → identical to toy output.
    - New Decimal money fields render via `Rendro.Format.money/1`; the legacy line-item `price` still renders `"$#{item.price}"` (bare number).
    - A `Float` in a new money field raises an instructive `ArgumentError` pointing at `Decimal.new/1`/`Decimal.from_float/1`; a `%Decimal{}` in the legacy `price` slot is rejected instructively.
  </behavior>
  <read_first>
    - lib/rendro/recipes/invoice.ex:154-169 (FROZEN header lines :159-160 and `"$#{item.price}"` body :168 — must stay literal)
    - lib/rendro/recipes/receipt.ex:247-260 (present-vs-absent field destructuring), :479-502 (is_float rejection message shape)
    - lib/rendro/format.ex:37-47 (`money/1` — the routing target for NEW Decimal fields)
  </read_first>
  <action>
Add optional anatomy rendering to the section builders. Header/section builders match on presence, prepending an issuer block and appending customer/due_date/terms blocks ONLY when `Map.get(data, key)` is non-nil (mirror `receipt.ex:247-260`). Colors on any NEW block come from `palette(opts)` (Task 1). The two existing toy header lines (`invoice.ex:159-160`) and the legacy body cell `"$#{item.price}"` (`:168`) MUST remain literally unchanged — new fields add new code, they do not rewrite the frozen path (RESEARCH A1 / Pitfall 4).

Money split (INV-02): route NEW Decimal money fields (in `:totals` and any Decimal-typed anatomy value) through `Rendro.Format.money/1` via `Rendro.Recipes.Pagination.formatter(opts, :amount, &Rendro.Format.money/1)`. Do NOT blanket-route the legacy line-item `price` — keeping `"$#{item.price}"` is required so the toy call stays byte-identical (routing a bare `200` through `money/1` would emit `"$200.00"` and break INV-01). Reject a `%Decimal{}` in the legacy `price` position and a `Float` in the new money fields, both with instructive `ArgumentError`s copied from the `is_float` head shape (`receipt.ex:479-502`), using `Rendro.Recipes.Pagination.type_name/1` for the Why-line type.

Extend `test/rendro/recipes/invoice_test.exs` with: present-vs-absent cases for each of `:issuer`/`:customer`/`:due_date`/`:terms`; a bare-`price` still-renders-`"$#{price}"` case; a new-Decimal-field-via-money/1 case; a Float-rejected case; a Decimal-in-legacy-price-rejected case.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Absent anatomy fields → the toy byte-identity golden (`invoice_byte_identity_test.exs`) stays green.
    - Each anatomy field renders content only when present (test proves present ≠ absent output).
    - Legacy `price` renders `"$#{price}"`; new Decimal fields render via `Rendro.Format.money/1`.
    - Float in a new money field and `%Decimal{}` in legacy `price` each raise instructive `ArgumentError`.
    - `mix test test/rendro/recipes/invoice_test.exs` passes.
  </acceptance_criteria>
  <done>Invoice renders full optional anatomy with the correct money split while the toy call is byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Totals block — Decimal.equal? assertion + kept with last rows (INV-03)</name>
  <files>lib/rendro/recipes/invoice.ex, test/rendro/recipes/invoice_test.exs</files>
  <behavior>
    - A `:totals` block renders ONLY when `:totals` is a map; absent → no totals block.
    - Supplied totals are validated as a caller assertion: derived subtotal (Σ line amounts via `Decimal.add/2`) must satisfy `Decimal.equal?/2` against the supplied value, else an instructive `ArgumentError` (Supplied vs Derived).
    - When the last table page is near capacity, the totals block stays with the last rows (does NOT flow alone to a fresh page).
  </behavior>
  <read_first>
    - lib/rendro/recipes/receipt.ex:333-357 (`build_totals_blocks/2` + `maybe_append_totals_line/4`), :504-556 (`maybe_validate_totals!/1` via `Decimal.equal?/2`), :285-310 (`effective_capacity` formula + totals append)
    - lib/rendro/recipes/statement.ex:341 (reserving forward rows in `effective_capacity`), :690-708 (single-value `Decimal.equal?/2` assertion)
    - lib/rendro/recipes/pagination.ex:18-49 (`chunk_rows_into_pages/2` empty-page guard)
  </read_first>
  <action>
Copy `build_totals_blocks/2` + `maybe_append_totals_line/4` from `receipt.ex:333-357` (swap module name): render Subtotal/Tax/Discount/Total lines only when the corresponding `Map.get(totals, key)` is a `%Decimal{}`, formatting via `Rendro.Recipes.Pagination.formatter(opts, :amount, &Rendro.Format.money/1)`; return `[]` when no lines (so nothing renders without `:totals`). Copy `maybe_validate_totals!/1` from `receipt.ex:504-556`: derive subtotal by folding line amounts with `Decimal.add/2` from `Decimal.new(0)` and assert `Decimal.equal?(supplied, derived)` (NEVER `==`, which compares struct fields so `1.0` ≠ `1.00`), raising an instructive `ArgumentError` naming Supplied and Derived on mismatch. Call `maybe_validate_totals!/1` from `validate_data!/1` (mirror `receipt.ex:379`).

INV-03 "kept with the last rows" — the ONE place Invoice must exceed a pure Receipt copy (RESEARCH Pitfall 5 / A3). Receipt appends totals after the last table block WITHOUT reserving space (`receipt.ex:308-310`), so totals can flow to a fresh page. Reserve the measured totals-block height in `effective_capacity` for the FINAL page, mirroring how Statement subtracts forward rows (`statement.ex:341`), OR emit the last table page + totals as one trailing chunker unit. Do NOT wrap totals in `keep_together` (oversized group → `:content_overflow`, RESEARCH anti-pattern).

Extend `test/rendro/recipes/invoice_test.exs`: totals-absent (no block); totals-present renders lines; `Decimal.equal?` mismatch raises; a pagination case with a near-full last page proving totals stay on the last table page (assert totals and last rows share the final page, e.g. via page count / block placement).
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - No `:totals` → no totals block; the toy golden stays green.
    - Mismatched supplied-vs-derived totals raise an `ArgumentError` naming Supplied and Derived; validation uses `Decimal.equal?/2`, not `==`.
    - A near-full-last-page case keeps totals on the same page as the last rows (test asserts placement / page count).
    - Totals are NOT wrapped in `keep_together`.
    - `mix test test/rendro/recipes/invoice_test.exs` passes.
  </acceptance_criteria>
  <done>Invoice renders a validated totals block that stays with the last rows across a page break, asserted via `Decimal.equal?/2`.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `Rendro.Recipes.Invoice.validate_data!/1` (private) — instructive `ArgumentError` boundary (INV-06).
- `Rendro.Recipes.Invoice.palette/1` (private) — role map `ink`/`muted`/`accent`/`on_accent`/`background`/`surface`/`rule`, defaults to today's literals (INV-07, S1).
- `page_template/1` `Keyword.take` whitelist (INV-07) — closes the recipe-opts leak.
- New optional Invoice fields `:issuer`, `:customer`, `:due_date`, `:terms`, `:totals` (render-when-present) (INV-01).
- `build_totals_blocks/2`, `maybe_append_totals_line/4`, `maybe_validate_totals!/1` (private) — Decimal totals + `Decimal.equal?/2` assertion + kept-with-last-rows (INV-03).
- Float-rejection / Decimal-in-legacy-price validation heads (INV-02).
- Extended `test/rendro/recipes/invoice_test.exs` and `test/rendro/recipes/invoice_opts_threading_test.exs`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller data map → Invoice recipe | Untrusted `%{}` shape/types cross here; malformed input must yield an instructive `ArgumentError`, never a leaked internal error. |
| supplied totals → derived totals | Caller-asserted financial values must be validated for numeric (not struct) equality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-04-01 | Denial of Service / Information Disclosure | `validate_data!/1` boundary | high | mitigate | Non-map and missing-key heads raise a structured `ArgumentError` (What/Where/Why/Next) at the boundary, so malformed callers never trigger a leaked `BadMapError`/`FunctionClauseError` (INV-06). Required keys limited to `:id`/`:date`/`:items` so the toy call is never rejected. |
| T-115-04-02 | Tampering (data integrity) | Money type confusion | high | mitigate | New money fields are Decimal-only; a `Float` is rejected instructively (Float arithmetic is inexact) and a `%Decimal{}` in the legacy `price` slot is rejected; the bare-number `price` stays `"$#{price}"` to preserve INV-01 (INV-02). |
| T-115-04-03 | Tampering (data integrity) | Totals caller assertion | high | mitigate | Supplied totals validated with `Decimal.equal?/2` (numeric), never `==` (struct-field compare where `1.0` ≠ `1.00`), raising on mismatch so silently-wrong financial output cannot render (INV-03). |
| T-115-04-04 | Tampering | Toy-call byte-compat | high | mitigate | Frozen header lines + legacy `price` cell untouched; new code runs only for new fields; the plan-01 toy golden runs in every task's verify to catch drift (INV-01). |
</threat_model>

<verification>
- `mix test test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_opts_threading_test.exs test/rendro/recipes/invoice_byte_identity_test.exs` — green.
- `mix compile --warnings-as-errors` — clean.
- Toy golden unbroken across all three tasks (byte-identity guard).
</verification>

<success_criteria>
- Full optional Invoice anatomy renders when present; toy call byte-identical.
- Decimal money split correct; totals validated via `Decimal.equal?/2` and kept with last rows; input validated as errors-as-product; palette seam + whitelist in place.
</success_criteria>

<output>
Create `.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-04-SUMMARY.md` when done.
</output>
