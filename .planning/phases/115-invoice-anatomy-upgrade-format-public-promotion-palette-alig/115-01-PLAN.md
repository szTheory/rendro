---
phase: 115-invoice-anatomy-upgrade-format-public-promotion-palette-alig
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rendro/recipes/invoice_byte_identity_test.exs
  - test/rendro/table_byte_identity_test.exs
autonomous: true
requirements: [INV-01, INV-05]
must_haves:
  truths:
    - "A committed sha256 of the CURRENT toy-call render exists and a test asserts a fresh render still equals it (INV-01 baseline)."
    - "A committed sha256 of a CURRENT no-cell_align table render exists and a test asserts a fresh render still equals it (INV-05 baseline)."
  artifacts:
    - test/rendro/recipes/invoice_byte_identity_test.exs
    - test/rendro/table_byte_identity_test.exs
  key_links:
    - "Both goldens are captured on pristine (pre-upgrade) code, so downstream plans 03/04 are byte-checked against a frozen reference."
---

<objective>
Capture machine-checkable byte-identity baselines BEFORE any `lib/` edit lands, resolving RESEARCH Open Question 1 (OQ1). This is the phase's Wave-0 gate: a sha256 golden of the pre-upgrade Invoice toy call (`%{id:, date:, items:}`) and a sha256 golden of an existing no-`cell_align` table render. Every later plan (03 cell_align, 04 Invoice anatomy) is measured against these frozen references.

Purpose: INV-01 and INV-05 both require "renders byte-identically to before". "Before" only has meaning if it is recorded on pristine code — this plan freezes it.
Output: Two new byte-identity test files, each embedding a captured sha256 that passes today and must keep passing after all Phase-115 edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-RESEARCH.md
@.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-PATTERNS.md
@lib/rendro/recipes/invoice.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze toy-call render sha256 golden (INV-01 baseline)</name>
  <files>test/rendro/recipes/invoice_byte_identity_test.exs</files>
  <read_first>
    - lib/rendro/recipes/invoice.ex:154-169 (the FROZEN toy code path — header lines and `"$#{item.price}"` body)
    - test/rendro/recipes/branded_invoice_test.exs:166-171 (the two-render `pdf1 == pdf2` determinism pattern to mirror)
  </read_first>
  <action>
Create `test/rendro/recipes/invoice_byte_identity_test.exs` (async: true). Build the canonical pre-upgrade toy document from a fixed, deterministic data map — `%{id: "INV-001", date: ~D[2026-01-15], items: [<= 2 fixed items with integer qty and bare-number price>]}` — via `Rendro.Recipes.Invoice.document/1`, then render with `Rendro.render(doc, deterministic: true)`.

Capture the golden: at authoring time, compute `:crypto.hash(:sha256, pdf) |> Base.encode16(case: :lower)` for this render on the current (unedited) `invoice.ex` and embed the resulting hex string as a module attribute `@toy_golden_sha256`. Write two assertions: (1) two-render determinism (`pdf1 == pdf2`); (2) the fresh render's sha256 equals `@toy_golden_sha256`. Add a load-bearing comment on `@toy_golden_sha256` stating it is the pre-Phase-115 baseline and that any change to it is a defect per INV-01 — do NOT phrase that comment as a literal string another gate greps for.

This baseline MUST be captured before Task-04 edits `invoice.ex`; because this plan is Wave 1, the code is pristine when it runs. The toy data map here must use ONLY the required keys `:id`, `:date`, `:items` — no anatomy fields — so it exercises the frozen path exactly.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/recipes/invoice_byte_identity_test.exs` passes (2 assertions green).
    - `@toy_golden_sha256` is a concrete lowercase hex string, not a placeholder.
    - The test renders via `Rendro.Recipes.Invoice.document/1` using ONLY `:id`, `:date`, `:items`.
  </acceptance_criteria>
  <done>The pre-upgrade toy render's sha256 is frozen in a committed passing test, ready to catch any INV-01 byte-drift introduced by later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze existing no-cell_align table render sha256 golden (INV-05 baseline)</name>
  <files>test/rendro/table_byte_identity_test.exs</files>
  <read_first>
    - lib/rendro/table.ex:8-24 (current Table defstruct — confirm no cell_align field exists yet)
    - lib/rendro/cell.ex:8-21 (current Cell defstruct — confirm no cell_align field exists yet)
    - test/rendro/recipes/branded_invoice_test.exs:166-171 (two-render determinism pattern)
  </read_first>
  <action>
Create `test/rendro/table_byte_identity_test.exs` (async: true). Build a representative multi-column, multi-row table via `Rendro.table/2` (a `header:` plus 3+ rows, a mix of `{:share, 1}` and `{:fixed, N}` columns) with NO `cell_align` key set anywhere, wrap it in a minimal document/section, and render with `Rendro.render(doc, deterministic: true)`.

Capture the golden: compute the sha256 hex of this render on current code and embed it as `@table_golden_sha256`. Assert (1) two-render determinism and (2) the fresh render sha256 equals `@table_golden_sha256`. This is the regression guard that plan 03's `cell_align: :right` primitive must not perturb the default (left) path (RESEARCH Pitfall 2). Add a load-bearing comment marking it the pre-`cell_align` baseline; do NOT phrase that comment as a literal another gate negative-greps for.
  </action>
  <verify>
    <automated>mix test test/rendro/table_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/table_byte_identity_test.exs` passes.
    - The table constructed sets NO `cell_align` option (proves the default-path baseline).
    - `@table_golden_sha256` is a concrete lowercase hex string.
  </acceptance_criteria>
  <done>The pre-`cell_align` table render sha256 is frozen in a committed passing test, ready to prove plan 03's INV-05 no-op guarantee.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `test/rendro/recipes/invoice_byte_identity_test.exs` — new test; symbol `@toy_golden_sha256` (frozen INV-01 baseline).
- `test/rendro/table_byte_identity_test.exs` — new test; symbol `@table_golden_sha256` (frozen INV-05 baseline).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller data map → Invoice recipe | Fixed deterministic fixtures only in this plan; no untrusted input crosses here yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-01-01 | Tampering | Captured golden hash | medium | mitigate | Golden is computed from a live render on pristine code and asserted equal to a fresh render in the same test run, so a mis-typed/stale hash fails immediately rather than silently freezing wrong bytes. |
| T-115-01-02 | Repudiation | Baseline provenance | low | accept | Baseline captured on Wave-1 pristine code; the two-render determinism assertion documents the render is reproducible. Accept — no security-sensitive data involved. |
</threat_model>

<verification>
- `mix test test/rendro/recipes/invoice_byte_identity_test.exs test/rendro/table_byte_identity_test.exs` — both green.
- No `lib/` file is modified by this plan (grep the diff: only the two test files change).
</verification>

<success_criteria>
- Both byte-identity baselines captured on pristine code and passing.
- Zero `lib/` edits — this plan only records references.
</success_criteria>

<output>
Create `.planning/phases/115-invoice-anatomy-upgrade-format-public-promotion-palette-alig/115-01-SUMMARY.md` when done, recording both captured sha256 values.
</output>
