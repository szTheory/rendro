---
phase: 134-core-architecture-readability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/QUALITY.md
  - test/rendro/recipes/palette_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]
must_haves:
  truths:
    - "Per D-01, D-02, D-03, and D-04, every historical Phase 134 candidate has a permanent ledger disposition before any product repair, and zero accepted candidates yields an evidence-backed no-op."
    - "Per D-05, D-06, D-07, and D-08, candidate evaluation is stable and bounded: Analyzer first, then cohesive duplication, while size/xref and narration remain signals unless concrete harm is demonstrated."
    - "Per D-09, D-11, D-12, D-17, D-18, and D-19, an accepted palette extraction has fail-first characterization of all seven default-map shapes and exact precedence before implementation; a rejected extraction creates no source change."
    - "The evidence tracer is restart-safe: interruption leaves unique permanent finding IDs and explicit lifecycle states, and rerunning validation updates rather than duplicates them."
  artifacts:
    - path: .planning/QUALITY.md
      provides: "Permanent evidence-backed dispositions and lifecycle contracts for every Phase 134 candidate"
    - path: test/rendro/recipes/palette_test.exs
      provides: "Conditional Wave 0 fail-first contract for Rendro.Recipes.Palette.resolve/2"
  key_links:
    - from: .planning/QUALITY.md
      to: .planning/phases/134-core-architecture-readability/134-CONTEXT.md
      via: "candidate records cite the locked evidence, scope, verification, trigger, and closure rules"
      pattern: "QL-[0-9]{3}"
    - from: test/rendro/recipes/palette_test.exs
      to: lib/rendro/recipes/{invoice,receipt,branded_invoice,payslip,ticket,statement,certificate}.ex
      via: "cases transcribe the seven current no-theme maps and common theme/override ordering"
      pattern: "Palette.resolve"
  edge_cases:
    - requirement_id: ARCH-01
      category: concurrency
      verification: backstop
      resolution: "Ledger writes are serialized in one tracer task; permanent IDs are never duplicated on restart."
    - requirement_id: ARCH-02
      category: adjacency
      verification: backstop
      resolution: "Two candidates with equal impact or shared evidence remain separately dispositioned unless they are demonstrably one concern."
    - requirement_id: ARCH-02
      category: empty
      verification: backstop
      resolution: "Zero accepted candidates closes as a no-op; one accepted candidate schedules only that candidate; absent evidence never implies acceptance."
    - requirement_id: ARCH-02
      category: ordering
      verification: backstop
      resolution: "Evaluation order is deterministic: Analyzer, palette, shaping hint, existing QL-001 topology, then narration."
    - requirement_id: ARCH-03
      category: unclassified
      verification: backstop
      resolution: "Flagged assumption: seven palette sites share one resolution responsibility; Wave 0 must reject or confirm this before extraction."
  prohibitions: []
---

<objective>
Establish the production-quality evidence/ledger tracer for Phase 134 and place the palette candidate behind a fail-first characterization gate.

Purpose: Make the phase truthful when evidence rejects every candidate, and prevent any cleanup from outrunning its finding authority or compatibility proof.
Output: Permanent candidate dispositions in `.planning/QUALITY.md` and, only for an accepted palette candidate, the red Wave 0 `palette_test.exs` contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/QUALITY.md
@.planning/quality/baselines/132-initial.json
@.planning/phases/134-core-architecture-readability/134-CONTEXT.md
@.planning/phases/134-core-architecture-readability/134-RESEARCH.md
@.planning/phases/134-core-architecture-readability/134-PATTERNS.md
@.planning/phases/134-core-architecture-readability/134-VALIDATION.md
</context>

## Artifacts this phase produces

- Conditional new file `test/rendro/recipes/palette_test.exs` with test module `Rendro.Recipes.PaletteTest` when the palette candidate is accepted.
- New permanent `QL-NNN` ledger records for Phase 134 candidates; exact IDs are allocated from the current ledger and must never be reused.

<tasks>

<task type="tracer">
  <name>Task 1: Trace every candidate from current evidence to a durable ledger disposition</name>
  <files>.planning/QUALITY.md</files>
  <read_first>
    - `.planning/QUALITY.md` for the current highest permanent ID, finding schema, QL-001 disposition, lifecycle, and closure rules.
    - `.planning/quality/baselines/132-initial.json` for immutable diagnostic evidence and source boundary.
    - `.planning/phases/134-core-architecture-readability/134-CONTEXT.md` for D-01 through D-08, D-17, and D-19.
    - `.planning/phases/134-core-architecture-readability/134-RESEARCH.md` and `134-PATTERNS.md` for the five bounded candidate groups and predeclared evidence.
    - `.planning/milestones/v2.6-phases/83-claim-accuracy-shaping-hygiene/83-03-SUMMARY.md` and `.planning/milestones/v2.11-phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-REVIEW.md` for historical candidate provenance.
    - `lib/rendro/i18n/analyzer.ex`, `test/rendro/i18n/analyzer_test.exs`, `lib/rendro/text/shaper/simple.ex`, and `lib/rendro/error.ex` for current Analyzer and shaping-hint ownership.
    - All seven `lib/rendro/recipes/{invoice,receipt,branded_invoice,payslip,ticket,statement,certificate}.ex` files for current palette bodies and defaults.
  </read_first>
  <action>Run the bounded source/reference scans, `mix xref callers Rendro.I18n.Analyzer`, compile-connected stats/cycles, focused shaping/error tests, public-manifest tests, and the runtime phase/date narration scan. Allocate unique permanent ledger IDs after the current maximum and record separate complete records for Analyzer, palette resolution, shaping-hint fallback, and narration; retain QL-001 for topology rather than duplicating it. Each record must include current evidence, impact, confidence, compatibility risk, owner 134, exact scope, focused verification, status, trigger, and closure evidence per D-01, D-02, D-05, D-06, D-07, D-08, D-17, and D-19. Accept only demonstrated harm; otherwise use `reject_signal` or a trigger-backed deferral per D-03. Preserve the D-04 no-op outcome and do not edit product code in this tracer.</action>
  <verify>
    <automated>mix quality.governance &amp;&amp; mix xref callers Rendro.I18n.Analyzer &amp;&amp; mix xref graph --format stats --label compile-connected &amp;&amp; mix xref graph --format cycles --label compile-connected &amp;&amp; mix test test/rendro/text/shaper_test.exs test/rendro/error_test.exs test/rendro/public_api/manifest_test.exs test/docs_contract/public_api_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix quality.governance` exits 0 with unique permanent finding IDs and all required ledger fields populated.
    - Analyzer, palette, shaping-hint fallback, topology, and narration each have an explicit disposition; none is silently omitted or merged solely because evidence/impact is equal.
    - No `lib/`, `test/`, guide, CI, dependency, catalog, or public-manifest repair occurs in this task.
    - If every candidate is rejected/deferred, the ledger explicitly states that Phase 134 may close as an evidence-backed no-op per D-04.
  </acceptance_criteria>
  <done>The evidence tracer deterministically carries every candidate from present repository facts to an auditable repair, rejection, or deferral decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the accepted palette candidate's fail-first Wave 0 contract</name>
  <files>test/rendro/recipes/palette_test.exs</files>
  <read_first>
    - `.planning/QUALITY.md` for the palette record produced by Task 1; its disposition controls whether this task creates a file.
    - `test/rendro/recipes/pagination_test.exs` for async helper-test structure and assertion style.
    - All seven `lib/rendro/recipes/{invoice,receipt,branded_invoice,payslip,ticket,statement,certificate}.ex` files for exact existing default maps and merge order.
    - `lib/rendro/theme.ex` for `Rendro.Theme.resolve/1` behavior and color-map shape.
    - Existing `test/rendro/recipes/*_byte_identity_test.exs` and `test/rendro/recipes/themed_render_smoke_test.exs` for the compatibility backstop.
  </read_first>
  <behavior>
    - Test 1: `resolve([], defaults)` returns each of the three exact legacy default-map shapes used by the seven recipes.
    - Test 2: an explicit `theme: nil` is identical to no theme, including empty and single-key default maps used as boundary probes.
    - Test 3: a supplied theme returns `Rendro.Theme.resolve(theme).colors` before overrides.
    - Test 4: `:palette` wins key-by-key over either legacy defaults or theme colors, including equal values and a single override.
    - Test 5: invalid non-map palette input retains the existing `Map.merge/2` failure shape; the contract adds no coercion, validation, rounding, or geometry behavior.
  </behavior>
  <action>If and only if Task 1 accepted the cohesive palette drift finding, add `Rendro.Recipes.PaletteTest` with the exact behavior matrix above, cite the accepted ledger ID, and run it before any helper exists to prove a targeted RED caused by missing `Rendro.Recipes.Palette.resolve/2`. Do not add the helper or modify a recipe here. If the candidate was rejected/deferred, preserve the absent test file and record that no-op in the summary rather than forcing extraction, per D-03, D-04, D-06, D-09, D-11, and D-19.</action>
  <verify>
    <automated>if test -f test/rendro/recipes/palette_test.exs; then if mix test test/rendro/recipes/palette_test.exs &gt;/tmp/rendro-134-palette-red.log 2&gt;&amp;1; then exit 1; fi; rg -n 'Rendro\.Recipes\.Palette|UndefinedFunctionError|undefined' /tmp/rendro-134-palette-red.log; else mix quality.governance &amp;&amp; rg -n -A 16 'palette' .planning/QUALITY.md | rg 'reject_signal|defer'; fi</automated>
  </verify>
  <acceptance_criteria>
    - An accepted palette finding produces a fail-first test file before `lib/rendro/recipes/palette.ex` exists or any recipe changes.
    - The RED output names the missing helper contract, while all expectations are sourced from current recipe behavior rather than invented semantics.
    - A rejected/deferred palette finding leaves both the helper and test absent and has a mechanically discoverable non-repair disposition in the ledger.
  </acceptance_criteria>
  <done>Palette extraction is either blocked by a precise red characterization contract or truthfully suppressed by its ledger disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical evidence → quality ledger | Review prose and diagnostic signals are untrusted repair inputs until corroborated against current source and tests. |
| current source → repair authorization | Source facts may justify a finding but may not bypass the ledger lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-01 | Tampering | `.planning/QUALITY.md` candidate intake | high | mitigate | Require current source/xref/focused-test evidence and a complete permanent ledger record before any repair per D-01–D-04. |
| T-134-02 | Repudiation | narration/provenance audit | medium | mitigate | Preserve operational phase/date provenance and record a line-specific stale claim before authorizing an edit per D-08 and D-16. |
| T-134-03 | Denial of service | palette characterization | high | mitigate | Prove exact legacy defaults, nil/theme branches, override precedence, and existing failure shape before extraction. |
</threat_model>

<verification>
The ledger governance check, bounded xref/source evidence, focused shaping contracts, and public-manifest contracts all run in the tracer. The palette test is deliberately red only when the ledger accepts extraction; rejection follows the verified no-file branch.
</verification>

<success_criteria>
- Every Phase 134 candidate has one complete and current ledger disposition.
- No product code changes precede ledger acceptance.
- The palette candidate has a fail-first Wave 0 contract before extraction, or an explicit evidence-backed no-op disposition.
</success_criteria>

<output>
Create `.planning/phases/134-core-architecture-readability/134-01-SUMMARY.md` when done.
</output>
