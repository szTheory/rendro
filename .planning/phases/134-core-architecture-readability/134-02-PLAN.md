---
phase: 134-core-architecture-readability
plan: "02"
type: execute
wave: 2
depends_on: [134-01]
files_modified:
  - .planning/QUALITY.md
  - lib/rendro/i18n/analyzer.ex
  - test/rendro/i18n/analyzer_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "Per D-05 and D-09, Analyzer is removed with its isolated test only when the accepted ledger record still proves no production, dynamic, documentation, package, or public consumer."
    - "Per D-03 and D-04, contrary evidence produces a rejection/deferral and no deletion rather than manufactured cleanup."
    - "Per D-10, D-12, D-17, D-18, and D-19, Analyzer removal is one atomic concern whose ledger closure includes focused active-shaper proof and byte-identical public API evidence."
  artifacts:
    - path: .planning/QUALITY.md
      provides: "Verified Analyzer closure or evidence-backed non-repair disposition"
  key_links:
    - from: .planning/QUALITY.md
      to: lib/rendro/i18n/analyzer.ex
      via: "the Plan 01 Analyzer finding ID authorizes deletion or preserves the file"
      pattern: "Rendro.I18n.Analyzer"
    - from: test/rendro/text/shaper_test.exs
      to: lib/rendro/text/shaper/simple.ex
      via: "focused tests prove the authoritative shaping gate remains intact after conditional deletion"
      pattern: "shaping_required"
  edge_cases:
    - requirement_id: ARCH-01
      category: concurrency
      verification: backstop
      resolution: "The module and isolated test are one atomic concern; an interrupted in_progress ledger state is resumed under the same permanent ID."
    - requirement_id: ARCH-02
      category: empty
      verification: backstop
      resolution: "An Analyzer caller found at execution causes zero deletions and a recorded non-repair disposition."
  prohibitions:
    - "No broad cleanup, formatting sweep, renaming campaign, or repair beyond the accepted Analyzer finding."
    - "No size/xref-driven split of Writer, Paginate, recipe modules, or any other large module."
    - "No Phase 135 test consolidation or CI topology work, and no Phase 136 catalog visual or renderer-review work."
    - "No dependency upgrade, new capability, optional-adapter coupling, or package installation."
    - "No geometry, typography, visual-output, public-manifest, or deterministic golden refresh/change."
    - "No alternate render/layout pipeline; the single build-to-validate engine remains authoritative."
---

<objective>
Remove the obsolete Analyzer pair only under its accepted evidence contract and close that finding with compatibility proof.

Purpose: Eliminate verified dead internal code without risking an accidental public, dynamic, or operational dependency deletion.
Output: Conditional deletion of the Analyzer implementation/test and a verified ledger lifecycle record; otherwise a documented no-op.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/QUALITY.md
@.planning/phases/134-core-architecture-readability/134-CONTEXT.md
@.planning/phases/134-core-architecture-readability/134-RESEARCH.md
@.planning/phases/134-core-architecture-readability/134-01-SUMMARY.md
</context>

## Artifacts this phase produces

- None. This plan conditionally deletes `Rendro.I18n.Analyzer` and `Rendro.I18n.AnalyzerTest`; it creates no symbol or file.

<tasks>

<task type="auto">
  <name>Task 1: Remove or reject the Analyzer pair under the accepted finding contract</name>
  <reversibility rating="costly">Undoing a mistaken deletion would restore an obsolete module and retest every indirect contract surface, though no published API is intentionally changed.</reversibility>
  <files>.planning/QUALITY.md, lib/rendro/i18n/analyzer.ex, test/rendro/i18n/analyzer_test.exs</files>
  <read_first>
    - `.planning/QUALITY.md` and `134-01-SUMMARY.md` for the exact Analyzer finding ID, accepted scope, verification, and current lifecycle state.
    - `lib/rendro/i18n/analyzer.ex` and `test/rendro/i18n/analyzer_test.exs` in their current state before deletion.
    - `lib/rendro/text/shaper/simple.ex`, `lib/rendro/error.ex`, `test/rendro/text/shaper_test.exs`, `test/rendro/error_test.exs`, `test/rendro/i18n_test.exs`, and `test/rendro/pipeline/measure_test.exs` for the authoritative replacement path and focused behavior.
    - `priv/public_api.json`, `test/rendro/public_api/manifest_test.exs`, and `test/docs_contract/public_api_contract_test.exs` for the frozen public contract.
    - `test/rendro/recipes/*_byte_identity_test.exs` for the deterministic rendered-byte contract required around this product-code deletion.
    - `mix.exs` and package/hygiene configuration for compile and package surfaces.
  </read_first>
  <action>Re-run source, guide, task, dynamic invocation, package, manifest, and `mix xref callers` checks before editing. If the Plan 01 finding is accepted and still has no consumer, advance that same permanent record to in_progress, then delete `lib/rendro/i18n/analyzer.ex` and its solely-owned test together and run the focused shaper/error/i18n/measure gates plus every deterministic recipe byte-identity test per D-05, D-09, D-10, D-12, and D-19. Do not refresh deterministic goldens or the public manifest. Leave verified/closed lifecycle changes for Task 2 after compatibility evidence exists. If any consumer or unsupported behavior appears, keep both files, update the same record to `reject_signal` or trigger-backed deferral per D-03/D-04, and make no compensating cleanup.</action>
  <verify>
    <automated>mix quality.governance &amp;&amp; if test -e lib/rendro/i18n/analyzer.ex || test -e test/rendro/i18n/analyzer_test.exs; then test -e lib/rendro/i18n/analyzer.ex &amp;&amp; test -e test/rendro/i18n/analyzer_test.exs &amp;&amp; rg -n -A 18 'Rendro\.I18n\.Analyzer' .planning/QUALITY.md | rg 'reject_signal|defer'; else ! rg -n 'Rendro\.I18n\.Analyzer|I18n\.Analyzer' lib test guides README.md priv/public_api.json &amp;&amp; mix test test/rendro/text/shaper_test.exs test/rendro/error_test.exs test/rendro/i18n_test.exs test/rendro/pipeline/measure_test.exs test/rendro/recipes/*_byte_identity_test.exs; fi</automated>
  </verify>
  <acceptance_criteria>
    - The Analyzer implementation and isolated test are either both absent after a proven zero-consumer removal or both retained after a documented rejection/deferral.
    - A successful deletion leaves no source, guide, README, manifest, or test reference to the module; all focused replacement-path tests and every deterministic recipe byte-identity test pass without a golden refresh.
    - The same permanent ledger ID reaches in_progress or records the evidence-backed non-repair outcome; no duplicate finding is created.
  </acceptance_criteria>
  <done>Analyzer is atomically removed under focused replacement-path proof and marked in_progress, or retained under a truthful non-repair disposition ready for Task 2 confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Prove compatibility and close the Analyzer disposition</name>
  <files>.planning/QUALITY.md</files>
  <read_first>
    - `.planning/QUALITY.md` in its current post-Task-1 state and the exact Analyzer finding ID.
    - Current `lib/rendro/i18n/analyzer.ex` and `test/rendro/i18n/analyzer_test.exs` existence state before editing the ledger.
    - `priv/public_api.json`, `test/rendro/public_api/manifest_test.exs`, and `test/docs_contract/public_api_contract_test.exs` for byte-identical public-surface proof.
    - `test/rendro/text/shaper_test.exs`, `test/rendro/error_test.exs`, `test/rendro/i18n_test.exs`, and `test/rendro/pipeline/measure_test.exs` for the focused replacement-path result Task 1 established.
    - `test/rendro/recipes/*_byte_identity_test.exs` for the unchanged rendered-byte results that must be recorded in the original Analyzer finding.
  </read_first>
  <action>For a completed deletion, run both public-manifest contracts and every deterministic recipe byte-identity test without regenerating `priv/public_api.json` or refreshing any golden, then move the original finding through verified to closed with the focused results, exact zero-reference proof, unchanged rendered-byte results, before/after statement, compatibility evidence, and Task 1 resolution commit per D-12, D-17, D-18, and D-19. For a retained pair, confirm the ledger contains the contrary evidence, non-repair disposition, and reopening trigger and do not claim removal. Keep this task ledger-only.</action>
  <verify>
    <automated>mix test test/rendro/public_api/manifest_test.exs test/docs_contract/public_api_contract_test.exs test/rendro/recipes/*_byte_identity_test.exs &amp;&amp; mix quality.governance</automated>
  </verify>
  <acceptance_criteria>
    - `priv/public_api.json` remains byte-identical to a fresh generated manifest and is not edited.
    - A deleted pair has a closed original finding with focused proof, unchanged deterministic rendered-byte results, before/after statement, compatibility result, and resolution reference.
    - A retained pair has an explicit rejection/deferral with contrary evidence and trigger, never a false closed-removal claim.
  </acceptance_criteria>
  <done>The Analyzer repository state and permanent ledger disposition agree under exact public-contract evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository references → deletion authority | Static source absence alone is insufficient because compiled, dynamic, documentation, package, or public surfaces may consume a module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-04 | Denial of service | `Rendro.I18n.Analyzer` deletion | high | mitigate | Combine current source/guide/package scan, xref callers, manifest identity, and active shaper tests before and after atomic pair deletion. |
| T-134-05 | Tampering | ledger lifecycle | medium | mitigate | Update the original permanent finding through lifecycle states and require exact resolution evidence; never create a replacement finding to hide contrary evidence. |
</threat_model>

<verification>
The conditional gate proves either complete pair retention plus a ledger non-repair disposition, or complete pair deletion plus zero remaining references and green authoritative replacement/public-contract tests.
</verification>

<success_criteria>
- No partial deletion state is accepted.
- No public or dynamic dependency is removed without proof.
- The ledger and repository agree on the final Analyzer disposition.
</success_criteria>

<output>
Create `.planning/phases/134-core-architecture-readability/134-02-SUMMARY.md` when done.
</output>
