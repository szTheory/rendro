---
phase: 134-core-architecture-readability
plan: "03"
type: execute
wave: 3
depends_on: [134-01, 134-02]
files_modified:
  - .planning/QUALITY.md
  - lib/rendro/recipes/palette.ex
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes/receipt.ex
  - lib/rendro/recipes/branded_invoice.ex
  - lib/rendro/recipes/payslip.ex
  - lib/rendro/recipes/ticket.ex
  - lib/rendro/recipes/statement.ex
  - lib/rendro/recipes/certificate.ex
  - test/rendro/recipes/palette_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]
must_haves:
  truths:
    - "Per D-06, D-09, and D-11, an accepted palette finding creates exactly one cohesive internal resolution owner while each recipe retains its exact legacy defaults and call boundary."
    - "Default, explicit-nil, themed, equal-value, and last-wins override cases preserve stable output; invalid palette input retains its current failure boundary without coercion."
    - "Per D-03, D-04, and D-19, rejected characterization leaves every recipe unchanged and closes the candidate as an evidence-backed no-op."
    - "Per D-12 and D-18, `priv/public_api.json` and every affected deterministic recipe byte golden remain byte-identical; no geometry or visual target changes."
    - "Per D-10, the shared palette implementation and its seven call-site migrations are the only repair concern in this plan; truthfulness audit and terminal ledger closure execute separately in Plan 04."
  artifacts:
    - path: lib/rendro/recipes/palette.ex
      provides: "Conditional hidden internal owner `Rendro.Recipes.Palette.resolve/2` for existing resolution semantics"
      exports: ["resolve/2"]
    - path: test/rendro/recipes/palette_test.exs
      provides: "Green characterization for all legacy defaults, theme resolution, precedence, and failure shape"
  key_links:
    - from: lib/rendro/recipes/{invoice,receipt,branded_invoice,payslip,ticket,statement,certificate}.ex
      to: lib/rendro/recipes/palette.ex
      via: "private palette/1 passes opts and the recipe-owned legacy map to resolve/2"
      pattern: "Rendro\.Recipes\.Palette\.resolve\(opts"
    - from: test/rendro/recipes/palette_test.exs
      to: lib/rendro/recipes/palette.ex
      via: "the Wave 0 RED contract becomes green without changing expectations"
      pattern: "Palette.resolve"
  edge_cases:
    - requirement_id: ARCH-02
      category: ordering
      verification: backstop
      resolution: "Equal candidate impact does not alter ordering; palette proceeds only after Analyzer disposition and its own Wave 0 contract."
    - requirement_id: ARCH-02
      category: adjacency
      verification: backstop
      resolution: "Equal palette values remain equal after Map.merge/2; no special collision or normalization rule is introduced."
    - requirement_id: ARCH-02
      category: empty
      verification: backstop
      resolution: "Empty opts uses recipe defaults; a single override changes only its key; no accepted findings yields terminal no-op closure."
    - requirement_id: ARCH-03
      category: unclassified
      verification: backstop
      resolution: "Flagged assumption is resolved here: uniform resolution semantics must pass across all seven recipes or extraction is rejected."
  prohibitions:
    - "No broad cleanup, formatting sweep, renaming campaign, or repair beyond the accepted palette finding."
    - "No size/xref-driven split of Writer, Paginate, recipe modules, or any other large module."
    - "No Phase 135 test consolidation or CI topology work, and no Phase 136 catalog visual or renderer-review work."
    - "No dependency upgrade, new capability, optional-adapter coupling, or package installation."
    - "No geometry, typography, visual-output, public-manifest, or deterministic golden refresh/change."
    - "No alternate render/layout pipeline; the single build-to-validate engine remains authoritative."
---

<objective>
Conditionally extract the proven palette responsibility and migrate all seven recipe call sites with exact compatibility evidence.

Purpose: Prevent seven-site precedence drift without creating a new render path while keeping this change set limited to one accepted repair concern per D-10.
Output: A hidden palette helper and seven narrow call-site migrations only if accepted, with focused deterministic compatibility evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/QUALITY.md
@.planning/phases/134-core-architecture-readability/134-CONTEXT.md
@.planning/phases/134-core-architecture-readability/134-RESEARCH.md
@.planning/phases/134-core-architecture-readability/134-PATTERNS.md
@.planning/phases/134-core-architecture-readability/134-VALIDATION.md
@.planning/phases/134-core-architecture-readability/134-01-SUMMARY.md
@.planning/phases/134-core-architecture-readability/134-02-SUMMARY.md

<interfaces>
The accepted helper contract is `Rendro.Recipes.Palette.resolve(opts :: keyword(), defaults :: map()) :: map()`. It is hidden with `@moduledoc false`. `nil` theme selects the passed defaults; a non-nil theme selects `Rendro.Theme.resolve(theme).colors`; `Map.merge/2` applies `Keyword.get(opts, :palette, %{})` last. Recipe public signatures, rendering flow, and default maps remain unchanged.
</interfaces>
</context>

## Artifacts this phase produces

- Conditional new file `lib/rendro/recipes/palette.ex`.
- Conditional new internal module `Rendro.Recipes.Palette` and function `resolve/2`.
- No other new files or symbols; `test/rendro/recipes/palette_test.exs` and `Rendro.Recipes.PaletteTest` were conditionally created in Plan 01.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the palette contract green through one hidden owner</name>
  <reversibility rating="costly">The internal helper is cheap to delete, but undoing its shared ownership requires coordinated edits across seven recipe call sites.</reversibility>
  <files>.planning/QUALITY.md, lib/rendro/recipes/palette.ex, test/rendro/recipes/palette_test.exs</files>
  <read_first>
    - `.planning/QUALITY.md` and `134-01-SUMMARY.md` for the exact palette finding ID, disposition, and accepted responsibility.
    - `test/rendro/recipes/palette_test.exs` in its current fail-first state and Plan 01 RED evidence.
    - `lib/rendro/recipes/pagination.ex` for the hidden internal helper/module/spec pattern.
    - All seven current recipe files for exact default maps and the common private `palette/1` interface; read every source-of-truth palette body before extracting any one of them.
    - `lib/rendro/theme.ex` for the existing theme resolution contract.
    - Existing recipe byte-identity and opts-threading tests for the downstream compatibility boundary.
  </read_first>
  <behavior>
    - No-theme and explicit nil return the caller's exact map without adding/removing keys.
    - A theme selects only its resolved colors before override.
    - Override map wins on collisions, including equal values and one-key maps.
    - Invalid override values fail through the same `Map.merge/2` boundary.
  </behavior>
  <action>If the palette finding is accepted and the Plan 01 characterization is targeted RED, advance its original ledger record to in_progress. Create `Rendro.Recipes.Palette` with `@moduledoc false`, the accurate `resolve/2` spec, and only the established branch/merge algorithm, making the unchanged Wave 0 tests green before any recipe migration. Do not add validation, rescue, normalization, typography, geometry, rendering, public docs, imports, or a second engine per D-06, D-09, D-10, D-11, D-12, and D-19. If the finding is not accepted or the characterization exposes non-uniform behavior, do not create the helper or modify the test; retain the ledger no-op path.</action>
  <verify>
    <automated>if test -f lib/rendro/recipes/palette.ex; then mix test test/rendro/recipes/palette_test.exs; else mix quality.governance &amp;&amp; rg -n -A 18 'palette' .planning/QUALITY.md | rg 'reject_signal|defer'; fi</automated>
  </verify>
  <acceptance_criteria>
    - Accepted extraction makes the unchanged Wave 0 tests green with one hidden `resolve/2` responsibility.
    - No recipe call site changes before the helper contract is green.
    - Rejected/non-uniform characterization produces no helper or test edit and remains explicit in the ledger.
  </acceptance_criteria>
  <done>The palette contract is green through its cohesive owner before recipe migration, or the evidence-backed no-op remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the first four recipes to the proven palette owner</name>
  <files>lib/rendro/recipes/invoice.ex, lib/rendro/recipes/receipt.ex, lib/rendro/recipes/branded_invoice.ex, lib/rendro/recipes/payslip.ex</files>
  <read_first>
    - Current `lib/rendro/recipes/palette.ex` and `test/rendro/recipes/palette_test.exs` after Task 1 for the proven interface.
    - Current Invoice, Receipt, BrandedInvoice, and Payslip source files before editing, especially their complete `palette/1` functions and exact defaults.
    - Their four byte-identity and opts-threading test files plus `test/rendro/recipes/themed_render_smoke_test.exs`.
    - `.planning/QUALITY.md` for the same in-progress finding ID and exact accepted scope.
  </read_first>
  <behavior>
    - Invoice, Receipt, BrandedInvoice, and Payslip retain the seven-role legacy default map exactly.
    - All four preserve theme colors followed by last-wins palette overrides.
  </behavior>
  <action>When Task 1 created the accepted helper, replace only the duplicated resolution mechanics in Invoice, Receipt, BrandedInvoice, and Payslip with `Rendro.Recipes.Palette.resolve(opts, legacy_defaults)`, keeping each existing defaults map, private `palette/1`, option precedence, and render path exactly as characterized. Do not refresh any golden or modify visual/geometry behavior. When Task 1 followed the no-op branch, leave all four files untouched and carry that same disposition forward. This expansion implements D-09 through D-12 and remains one palette concern per D-10.</action>
  <verify>
    <automated>if test -f lib/rendro/recipes/palette.ex; then mix test test/rendro/recipes/palette_test.exs test/rendro/recipes/invoice_byte_identity_test.exs test/rendro/recipes/receipt_byte_identity_test.exs test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/recipes/payslip_byte_identity_test.exs test/rendro/recipes/invoice_opts_threading_test.exs test/rendro/recipes/receipt_opts_threading_test.exs test/rendro/recipes/branded_invoice_opts_threading_test.exs test/rendro/recipes/payslip_opts_threading_test.exs test/rendro/recipes/themed_render_smoke_test.exs; else mix quality.governance; fi</automated>
  </verify>
  <acceptance_criteria>
    - The first four recipe call sites use the accepted helper, or none do when the candidate is rejected.
    - Their exact default map, theme branch, override collision rule, invalid-input failure shape, and affected byte goldens remain unchanged.
    - No public signature, manifest entry, test organization, catalog cell, typography value, or geometry calculation changes.
  </acceptance_criteria>
  <done>The proven palette responsibility covers the first four recipes without semantic or byte drift, or they remain unchanged under the no-op disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate the remaining three recipes to the proven palette owner</name>
  <files>lib/rendro/recipes/ticket.ex, lib/rendro/recipes/statement.ex, lib/rendro/recipes/certificate.ex</files>
  <read_first>
    - `.planning/QUALITY.md` and `134-01-SUMMARY.md` for the same accepted palette finding ID and exact scope; this task reads but does not close the finding.
    - Current `lib/rendro/recipes/palette.ex` and `test/rendro/recipes/palette_test.exs` after Task 1 for the proven interface.
    - Current Ticket, Statement, and Certificate files before editing, with their full recipe-specific palette defaults.
    - Their byte-identity and opts-threading tests plus `test/rendro/recipes/themed_render_smoke_test.exs`.
  </read_first>
  <behavior>
    - Ticket retains its exact seven-role legacy default map and theme-then-override ordering.
    - Statement retains its exact five-role map, including surface `{245, 245, 245}`, and theme-then-override ordering.
    - Certificate retains its exact four-role map, including rule `{34, 34, 34}`, and theme-then-override ordering.
  </behavior>
  <action>When the helper exists, migrate only Ticket, Statement, and Certificate to `Rendro.Recipes.Palette.resolve/2`, retaining their exact recipe-owned maps, private `palette/1` call boundaries, option precedence, failure shape, and render paths. If the candidate followed the no-op branch, leave all three files unchanged. Run the three focused byte-identity and opts-threading suites, shared characterization, and themed smoke test without refreshing a golden. Do not audit or edit narration/spec/docs/comments and do not close ledger findings here; those distinct D-08 and D-13 through D-18 concerns belong to dependent Plan 04 per D-10.</action>
  <verify>
    <automated>if test -f lib/rendro/recipes/palette.ex; then mix test test/rendro/recipes/palette_test.exs test/rendro/recipes/ticket_byte_identity_test.exs test/rendro/recipes/statement_byte_identity_test.exs test/rendro/recipes/certificate_byte_identity_test.exs test/rendro/recipes/ticket_opts_threading_test.exs test/rendro/recipes/statement_opts_threading_test.exs test/rendro/recipes/certificate_opts_threading_test.exs test/rendro/recipes/themed_render_smoke_test.exs; else mix quality.governance; fi</automated>
  </verify>
  <acceptance_criteria>
    - Ticket, Statement, and Certificate use the accepted helper with their exact recipe-owned defaults, or remain unchanged under the no-op disposition.
    - Their helper characterization, byte-identity, opts-threading, and themed-render tests pass without changing deterministic goldens.
    - No test consolidation, CI topology change, dependency upgrade, catalog visual change, behavior-changing geometry work, broad formatting/renaming, or unsupported large-module split appears in the diff.
  </acceptance_criteria>
  <done>The accepted palette owner covers all seven recipes with exact deterministic behavior, or all recipe source remains unchanged under the evidence-backed no-op disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe options → shared helper | Existing caller-controlled `:theme` and `:palette` values cross into a new internal owner whose semantics must remain exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-06 | Tampering | `Rendro.Recipes.Palette.resolve/2` | high | mitigate | Preserve exact nil/theme/default maps and `Map.merge/2` ordering under Wave 0 unit cases plus all affected deterministic byte tests. |
| T-134-07 | Denial of service | recipe option failure boundary | high | mitigate | Retain existing invalid non-map override failure; add no coercion, validation, rescue, or alternate render path. |
</threat_model>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Accepted internal quality findings close with cohesive code and stable supported contracts | 01-04 | COVERED | Evidence tracer, conditional repairs, separate truthfulness audit, and terminal closure.
REQ | ARCH-01 | Public manifest and unrelated rendered bytes remain unchanged | 01-04 | COVERED | Manifest and byte-identity gates surround and close every product repair.
REQ | ARCH-02 | Every accepted/rejected finding follows ledger disposition | 01-04 | COVERED | Permanent lifecycle records include no-op and terminal paths.
REQ | ARCH-03 | Extraction requires cohesive responsibility and characterization | 01,03 | COVERED | Fail-first palette gate; size/xref remains rejected.
REQ | ARCH-04 | Specs/docs/comments remain truthful | 01,04 | COVERED | Bounded narration audit plus docs/Credo/Dialyzer.
RESEARCH | — | Analyzer conditional atomic removal | 01-02 | COVERED | Current caller proof precedes deletion.
RESEARCH | — | Palette Wave 0 then narrow helper across seven recipes | 01,03 | COVERED | Exact defaults/precedence and byte tests.
RESEARCH | — | Reject/defer shaping hint, topology, and narration absent new evidence | 01,04 | COVERED | Explicit ledger records and triggers.
CONTEXT | D-01,D-02,D-03,D-04 | Ledger-first authority and truthful no-op | 01-04 | COVERED | Cited in tracer, conditional branches, and terminal closure.
CONTEXT | D-05,D-06,D-07,D-08 | Candidate priority and bounded audits | 01-04 | COVERED | Analyzer first; palette next; no size churn; separate narration audit.
CONTEXT | D-09,D-10,D-11,D-12 | Characterization, concern isolation, cohesive ownership, compatibility | 01-04 | COVERED | Wave 0, separated repair/audit/closure boundaries, exact gates.
CONTEXT | D-13,D-14,D-15,D-16 | Truthful specs/docs/comments and preserved provenance | 04 | COVERED | Separate bounded audit.
CONTEXT | D-17,D-18,D-19 | Ledger closure, deterministic evidence, requirement mapping | 01-04 | COVERED | Lifecycle and dependent terminal gate.
</source_coverage_audit>

<verification>
Run focused helper and recipe tests after each palette expansion without refreshing contract artifacts; Plan 04 owns the manifest, docs/static/type, governance, and `mix ci.fast` terminal gates.
</verification>

<success_criteria>
- The accepted palette responsibility is centralized with exact semantics, or evidence rejects it and all recipe source remains unchanged.
- Public surface, rendered bytes, errors, and the one-engine architecture remain intact across the palette repair; Plan 04 proves truthfulness and terminal ledger state.
</success_criteria>

<output>
Create `.planning/phases/134-core-architecture-readability/134-03-SUMMARY.md` when done.
</output>
