---
phase: 113-dx-local-reproducibility-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["mix.exs", "test/guardrails/required_checks_contract_test.exs", ".github/workflows/ci.yml"]
autonomous: true
requirements: ["DX-01"]
must_haves:
  truths:
    - "Developers have individual scoped mix aliases for ci fast, proofs, and advisory"
    - "Guardrails enforce that ci.fast exactly matches the GitHub Actions split steps"
    - "CI failures have step-level timings and logs grouped by task in GitHub UI"
    - "D-03: Utilize both GitHub Problem Matchers AND Step Summaries."
    - "D-04: Use Problem Matchers for warnings so they appear inline on the PR diff."
    - "D-05: Use Step Summaries to consolidate test timings, cache hits, and overall pipeline health."
  artifacts:
    - path: "mix.exs"
      provides: "Split CI aliases"
      contains: "ci.fast"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Structural validation"
      contains: "ci.fast"
    - path: ".github/workflows/ci.yml"
      provides: "Split CI steps"
      contains: "name: Test"
  key_links:
    - from: "test/guardrails/required_checks_contract_test.exs"
      to: "mix.exs"
      via: "alias extraction"
      pattern: "Keyword.fetch!(aliases, :\"ci.fast\")"
    - from: ".github/workflows/ci.yml"
      to: "/tmp/mix-test-output.log"
      via: "test step piping"
      pattern: "tee /tmp/mix-test-output.log"
---

<objective>
Update `mix.exs` aliases and ExUnit Guardrails to decompose `mix ci` into scoped aliases (`ci.fast`, `ci.proofs`, `ci.advisory`) and validate their structure.
Purpose: To allow local reproduction of CI gates without running the entire monolithic suite, while ensuring guardrails correctly enforce the split-step execution on GitHub Actions.
Output: Updated `mix.exs`, `required_checks_contract_test.exs`, and `.github/workflows/ci.yml` files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-dx-local-reproducibility-validation/113-CONTEXT.md
@.planning/phases/113-dx-local-reproducibility-validation/113-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose mix ci aliases</name>
  <read_first>mix.exs</read_first>
  <action>Decompose the existing `ci` alias list into `ci.fast`, `ci.proofs`, and `ci.advisory`.
Set `ci` to `["ci.fast", "ci.proofs"]`.
`ci.fast` MUST contain the exact current 7 commands in `ci`: "format --check-formatted", "hex.build", "compile --warnings-as-errors", "test --exclude quarantine --slowest 10", "docs --warnings-as-errors", "credo --strict", "dialyzer".
`ci.proofs` MUST contain: "test --include live_pdf_tools test/rendro/adapters/forms_viewer_evidence_live_test.exs test/rendro/adapters/embedded_files_viewer_evidence_live_test.exs test/rendro/adapters/links_viewer_evidence_live_test.exs test/rendro/adapters/protection_viewer_evidence_live_test.exs test/rendro/adapters/signature_widget_viewer_evidence_live_test.exs test/rendro/adapters/signed_artifact_viewer_evidence_live_test.exs test/rendro/adapters/trust_sensitive_viewer_evidence_live_test.exs", "test --include live_signing test/rendro/adapters/signing_live_test.exs", "test --include live_pdf_tools test/rendro/adapters/signing_live_test.exs", "run scripts/release_preflight_proof.exs --current-version-tag --worktree /tmp/rendro-release-proof".
`ci.advisory` MUST contain: "test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs", "rendro.launch_artifacts.check", "rendro.comparison.check", "rendro.livebook.check", "cmd npm ci --prefix scripts/pdfjs_observer", "cmd node scripts/pdfjs_observer/observe.mjs --check", "deps.audit", "hex.audit".</action>
  <acceptance_criteria>
    - `mix help ci.fast` does not crash
    - `mix.exs` contains `ci: ["ci.fast", "ci.proofs"]`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ci.fast" mix.exs && grep -q "ci.proofs" mix.exs</automated>
  </verify>
  <done>mix.exs has the split CI aliases defined correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Update guardrail contracts for split CI alias</name>
  <read_first>test/guardrails/required_checks_contract_test.exs</read_first>
  <action>Update `test/guardrails/required_checks_contract_test.exs`.
In `describe "mix ci alias structural validation"`, change the test to verify `:ci.fast` contains the expected 7 fast steps instead of `:ci`. Also, change `:ci` validation to assert it equals `["ci.fast", "ci.proofs"]`.
In `describe "required/advisory CI separation"`, the `test_block =~ "run: mix ci"` assertion will fail because GitHub Actions will use split steps. Change this test to assert that `test_block` contains individual step substrings like `"run: mix format"`, `"run: mix test"`, `"run: mix credo"`, `"run: mix dialyzer"`, rather than `"run: mix ci"`. This allows Phase 113 Plan 02 to correctly split the GitHub Actions steps.</action>
  <acceptance_criteria>
    - `test/guardrails/required_checks_contract_test.exs` uses `Keyword.fetch!(aliases, :"ci.fast")`
    - `test/guardrails/required_checks_contract_test.exs` asserts `test_block =~ "run: mix test"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "run: mix format" test/guardrails/required_checks_contract_test.exs</automated>
  </verify>
  <done>Guardrail tests align with the split CI alias architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Split steps in GitHub Actions</name>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>In `.github/workflows/ci.yml`, under the `test` job, split the single `name: Run CI` step (`run: mix ci 2>&1 | tee /tmp/mix-ci-output.log`) into explicitly named steps matching the `ci.fast` alias from `mix.exs`:
- `name: Format` (`run: mix format --check-formatted`)
- `name: Hex Build` (`run: mix hex.build`)
- `name: Compile` (`run: mix compile --warnings-as-errors`)
- `name: Test` (`run: mix test --exclude quarantine --slowest 10 2>&1 | tee /tmp/mix-test-output.log`)
- `name: Docs` (`run: mix docs --warnings-as-errors`)
- `name: Credo` (`run: mix credo --strict`)
- `name: Dialyzer` (`run: mix dialyzer`)
Ensure all steps use `if: matrix.primary == true` if the original did.
Update the `CI Baseline Summary` step (lines 98-111 approx) to read slowest tests from `/tmp/mix-test-output.log` instead of `/tmp/mix-ci-output.log` (`grep -A 25 'Top [0-9]* slowest' /tmp/mix-test-output.log`).</action>
  <acceptance_criteria>
    - `ci.yml` contains `name: Test` and `run: mix test` separately from `mix format`.
    - `CI Baseline Summary` step references `/tmp/mix-test-output.log`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "run: mix format --check-formatted" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI steps are split allowing step-level log grouping in GitHub UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI to External | CI dependencies execution and log teeing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | GitHub Actions CI Guardrails | mitigate | Guardrails ensure local alias and remote CI jobs remain consistent, preventing malicious bypassing of tests. |
</threat_model>

<verification>
mix format mix.exs test/guardrails/required_checks_contract_test.exs
</verification>

<success_criteria>
The CI aliases are properly split and ExUnit guardrails are fully updated.
</success_criteria>

<output>
Create `.planning/phases/113-dx-local-reproducibility-validation/113-01-SUMMARY.md` when done
</output>
