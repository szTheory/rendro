---
phase: 113-dx-local-reproducibility-validation
plan: 02
type: execute
wave: 2
depends_on: ["113-01"]
files_modified: ["README.md", "CONTRIBUTING.md"]
autonomous: true
requirements: ["DX-02", "DX-03", "DX-04"]
must_haves:
  truths:
    - "README badge strictly targets the ci-success job"
    - "Contributors have a clear guide on reproducing required checks and flaky tests locally"
    - "D-06: The README status badge MUST target the ci-success job, not the overall workflow."
  artifacts:
    - path: "README.md"
      provides: "Accurate status badge"
      contains: "img.shields.io/github/check-runs/szTheory/rendro/main?name=ci-success"
    - path: "CONTRIBUTING.md"
      provides: "Contributor guidelines"
      contains: "mix verify.flake"
  key_links: []
---

<objective>
Replace the `README.md` badge with a job-specific Shields.io badge, and create `CONTRIBUTING.md` to document local reproducibility patterns.
Purpose: To enable actionable step-level timings and log folding in the GitHub Actions UI, accurately badge the repo based on the `ci-success` required merge gate, and empower developers to locally run what fails in CI.
Output: Updated `README.md` and new `CONTRIBUTING.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-dx-local-reproducibility-validation/113-CONTEXT.md
@.planning/phases/113-dx-local-reproducibility-validation/113-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace README badge with job-specific Shields.io check</name>
  <read_first>README.md</read_first>
  <action>In `README.md`, update the `[![CI]` markdown badge on line 3. Replace the GitHub Actions native badge `https://github.com/szTheory/rendro/actions/workflows/ci.yml/badge.svg` with the Shields.io Check-Runs badge targeting the `ci-success` job: `https://img.shields.io/github/check-runs/szTheory/rendro/main?name=ci-success`. Ensure the markdown link target still points to the `ci.yml` workflow URL.</action>
  <acceptance_criteria>
    - `README.md` contains the Shields.io `check-runs` URL with `?name=ci-success`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "name=ci-success" README.md</automated>
  </verify>
  <done>README badge accurately reflects the required merge gate instead of yellow advisory workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md</name>
  <read_first>README.md</read_first>
  <action>Create `CONTRIBUTING.md` in the project root. Document:
1. The CI execution architecture: The root `mix ci` alias runs the required merge gate locally.
2. The decomposed aliases: Explain what `mix ci.fast`, `mix ci.proofs`, and `mix ci.advisory` are used for and how developers can run them locally to reproduce CI failures.
3. Reproducing flaky tests: Explain the `mix verify.flake` alias (which runs `quarantine` tagged tests) and how to reproduce a seed-dependent failure (e.g., `mix test --seed 123456`).</action>
  <acceptance_criteria>
    - `CONTRIBUTING.md` exists and contains instructions for `mix ci.fast` and `mix verify.flake`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "mix verify.flake" CONTRIBUTING.md</automated>
  </verify>
  <done>Developer documentation established for local CI reproducibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Contributor instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02 | Info Disclosure | Badging | mitigate | Specific `ci-success` badge prevents false-negative advisory workflow failures from masking true health. |
</threat_model>

<verification>
mix test test/guardrails/required_checks_contract_test.exs
</verification>

<success_criteria>
The badge correctly signals pipeline health, and developers have clear CONTRIBUTING documentation.
</success_criteria>

<output>
Create `.planning/phases/113-dx-local-reproducibility-validation/113-02-SUMMARY.md` when done
</output>
