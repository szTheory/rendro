---
status: complete
phase: 113-dx-local-reproducibility-validation
source: [113-01-SUMMARY.md, 113-02-SUMMARY.md, 113-03-SUMMARY.md]
started: 2026-07-10T23:02:52Z
updated: 2026-07-11T01:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Scoped CI Aliases Are Discoverable
expected: Running `mix help ci.fast`, `mix help ci.proofs`, and `mix help ci.advisory` shows each alias exists; `mix ci` remains the required local gate composed from fast and proof lanes.
result: pass
verifier: automated
evidence: `mix help ci.fast`, `mix help ci.proofs`, and `mix help ci.advisory` returned help output; `test/mix/tasks/ci_alias_contract_test.exs` enforces the scoped alias contract.

### 2. CI Workflow Presents Split Fast-Lane Steps
expected: Opening `.github/workflows/ci.yml` shows separate named Format, Hex Build, Compile, Test, Docs, Credo, and Dialyzer steps, and the Test step writes output used by the slowest-test summary.
result: pass
verifier: automated
evidence: `test/guardrails/required_checks_contract_test.exs` enforces the split fast-lane steps and `/tmp/mix-test-output.log` summary source.

### 3. Contributor Docs Map Failures To Local Commands
expected: The README badge targets `ci-success`, and `CONTRIBUTING.md` tells contributors which scoped Mix command to run for fast, proof, advisory, flaky, and seeded ExUnit reproduction.
result: pass
verifier: automated
evidence: `test/docs_contract/dx_local_reproducibility_claims_test.exs` enforces the README badge and CONTRIBUTING local reproduction commands.

### 4. Validation Report Labels Evidence Honestly
expected: `113-METRICS.md` and `C1-AUDIT.md` show the local `mix ci.fast` result, record the three green remote `ci.yml` runs, and distinguish the advisory `security-audit` signal from the required `ci-success` gate.
result: pass
verifier: automated
evidence: `test/docs_contract/dx_local_reproducibility_claims_test.exs` enforces local-gate wording, remote p50/p95/cache evidence, and advisory/non-required security-audit wording.

### 5. Local Fast Gate Reproduces Successfully
expected: From a clean shell in the repository, running `mix ci.fast` completes successfully through format, compile, tests, docs, Credo, and Dialyzer without failures.
result: pass
verifier: automated
evidence: `113-METRICS.md` records the green `mix ci.fast` run; recurring protection is provided by the existing `ci.fast` alias and `ci-success` required gate.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None.
