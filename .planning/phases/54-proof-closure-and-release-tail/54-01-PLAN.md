---
phase: 54
plan: 01
type: execute
wave: 1
depends_on:
  - "52-02"
  - "53-02"
files_modified:
  - .planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md
  - scripts/protected_viewer_proof_fixture.exs
  - priv/support_matrix.json
  - guides/api_stability.md
  - test/docs_contract/protection_claims_test.exs
autonomous: false
requirements:
  - TRUST-03
must_haves:
  truths:
    - "`protection` viewer rows stay `unverified` until a recorded Phase 54 checklist exists for that specific viewer."
    - "A viewer is promoted only when all five locked proof items pass for that viewer; open-only or owner-password-only observations never qualify."
    - "Any viewer promotion is synchronized in the same change set across validation notes, `priv/support_matrix.json`, `guides/api_stability.md`, and docs-contract assertions."
  artifacts:
    - path: ".planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md"
      provides: "Canonical per-viewer proof record with the locked five-check protection checklist"
    - path: "scripts/protected_viewer_proof_fixture.exs"
      provides: "Repeatable representative protected-fixture generator for the manual viewer-proof lane"
    - path: "priv/support_matrix.json"
      provides: "Canonical machine-readable per-viewer protection status after proof recording"
    - path: "guides/api_stability.md"
      provides: "Human-readable protection viewer posture aligned to the recorded proof result"
  key_links:
    - from: "scripts/protected_viewer_proof_fixture.exs"
      to: ".planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md"
      via: "the generator writes the named fixture path and invocation contract that the manual checklist records"
      pattern: "protected_viewer_proof_fixture|Fixture|worktree|qpdf"
    - from: ".planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md"
      to: "priv/support_matrix.json"
      via: "recorded pass/fail results determine whether a specific viewer row stays `unverified` or is promoted"
      pattern: "Adobe Acrobat Reader|Apple Preview|opens_with_open_password"
    - from: "priv/support_matrix.json"
      to: "guides/api_stability.md"
      via: "viewer-status prose mirrors the same proof-backed support posture"
      pattern: "unverified|supported|protection"
---

<objective>
Close the proof-backed support contract for the `protection` family without widening the product surface.

Purpose: satisfy `TRUST-03` by making the Phase 54 viewer-proof lane reproducible, explicitly gated on accepted Phase 52 completion and host-tool readiness, and synchronized across validation notes, support matrix rows, and guide/test claims.
Output: representative protected-fixture generation, a completed manual proof record, and same-change-set support-contract sync for any per-viewer promotion.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/phases/54-proof-closure-and-release-tail/54-CONTEXT.md
@.planning/phases/54-proof-closure-and-release-tail/54-RESEARCH.md
@.planning/phases/54-proof-closure-and-release-tail/54-PATTERNS.md
@.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-VALIDATION.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-VALIDATION.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-VERIFICATION.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-02-PLAN.md
@priv/support_matrix.json
@guides/api_stability.md
@test/docs_contract/protection_claims_test.exs
@test/rendro/adapters/protected_validation_live_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the representative protected-fixture generator and tighten the Phase 54 proof record scaffold</name>
  <read_first>
    - .planning/phases/54-proof-closure-and-release-tail/54-CONTEXT.md
    - .planning/phases/54-proof-closure-and-release-tail/54-RESEARCH.md
    - .planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md
    - .planning/phases/47-form-validation-and-viewer-proof-closure/47-VALIDATION.md
    - test/rendro/adapters/protected_validation_live_test.exs
  </read_first>
  <files>.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md, scripts/protected_viewer_proof_fixture.exs</files>
  <acceptance_criteria>
    - `scripts/protected_viewer_proof_fixture.exs` exists and exposes a repeatable CLI contract for writing one representative protected PDF fixture to a caller-specified output path.
    - The generator's help or dry-run output states the Phase 54 host-tool preconditions explicitly: accepted Phase 52 completion, `qpdf` availability, and `pdfinfo` availability per D-16 and the research host note.
    - `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md` records the locked five proof items from D-05, the metadata fields from D-06, and a blocking prerequisite note that viewer-promotion execution cannot proceed until Phase 52 is accepted and the real protected fixture can be generated.
    - The validation record keeps owner-password-only behavior observational only per D-07 and does not invent any new support states per D-08.
  </acceptance_criteria>
  <action>
    Create `scripts/protected_viewer_proof_fixture.exs` as the small reproducible proof-lane entrypoint for this phase. Reuse the established Phase 52 protection path and sample-document shape instead of inventing a second protection API, and make the script write one named protected fixture plus a concise operator summary suitable for the Phase 54 manual checklist. The script must not check in binaries, must not introduce a new adapter surface, and must surface missing `qpdf`/`pdfinfo` or missing accepted Phase 52 readiness as explicit operator-visible preconditions rather than burying them.
    Update `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md` to become the canonical Phase 54 proof sheet: preserve the existing validation-table structure, replace any stale protection proof wording with the locked D-05 names (`opens_with_open_password`, `displays_authored_content_correctly`, `advisory_print_behavior`, `advisory_copy_behavior`, `save_and_reopen_readability`), and add an explicit prerequisite block that no viewer row may be promoted until the Phase 52 live-tool path is accepted and the representative fixture is generated successfully. Keep this document distinct from structural validation and release-tail verification.
  </action>
  <verify>
    <automated>sh -c 'OUT=$(mix run scripts/protected_viewer_proof_fixture.exs --dry-run --output /tmp/rendro-phase54-proof.pdf); printf "%s\n" "$OUT" | rg -n "Phase 52|qpdf|pdfinfo" && rg -n "opens_with_open_password|displays_authored_content_correctly|advisory_print_behavior|advisory_copy_behavior|save_and_reopen_readability|Phase 52|qpdf|pdfinfo" .planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md'</automated>
  </verify>
  <done>A reproducible fixture-generation entrypoint exists, and the validation record encodes the exact proof checklist plus the upstream/tooling gate instead of assuming proof readiness.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the manual per-viewer protection checklist only after the Phase 52 and host-tool prerequisites are satisfied</name>
  <read_first>
    - .planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md
    - scripts/protected_viewer_proof_fixture.exs
    - priv/support_matrix.json
    - guides/api_stability.md
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md` contains at least one fully completed viewer row with viewer name, version when easily available, OS, fixture path/name, date checked, pass/fail for all five D-05 checks, result, and one short notes field.
    - Any viewer missing one or more passing checklist items remains recorded as `unverified`; no row is marked `supported` on open-only success per D-09 through D-11.
    - Any owner-password-only success is recorded only in notes/observation form and is not used as the basis for a promoted support row per D-07.
    - The checkpoint does not proceed until Phase 52 completion is accepted and `qpdf` is actually available on the execution host.
  </acceptance_criteria>
  <action>
    After `52-02` is accepted and the execution host has `qpdf` plus `pdfinfo`, run the new fixture generator to produce the representative protected PDF, then perform the Phase 54 manual checklist in Adobe Acrobat Reader and/or Apple Preview. Record the exact per-check result in `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md`, including viewer/version/OS/fixture/date/notes per D-06. Promote nothing during this task; it is evidence capture only.
  </action>
  <verify>
    <automated>rg -nP '^\| (Adobe Acrobat Reader|Apple Preview) \| (?!pending \|)([^|]+) \| (?!pending \|)([^|]+) \| (?!pending \|)([^|]+) \| (?!pending \|)([^|]+) \| (pass|fail) \| (pass|fail) \| (pass|fail) \| (pass|fail) \| (pass|fail) \| (supported|unverified) \| (?! pending)([^|]+) \|$' .planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md</automated>
    <manual>Human checkpoint only. Before resuming, review `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md` and confirm at least one viewer row has non-`pending` values for version, OS, fixture, date, all five proof checks, result, and notes.</manual>
  </verify>
  <what-built>Representative protected fixture generation plus a completed Phase 54 manual proof record.</what-built>
  <how-to-verify>
    1. Confirm Phase 52 is accepted and `qpdf` is available locally with `command -v qpdf`.
    2. Run `mix run scripts/protected_viewer_proof_fixture.exs --output /tmp/rendro-phase54-proof.pdf`.
    3. Open the generated PDF in Adobe Acrobat Reader and/or Apple Preview.
    4. Record pass/fail for the five locked proof items and fill viewer/version/OS/fixture/date/notes in `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md`.
    5. Keep any failing or unchecked viewer row `unverified`.
  </how-to-verify>
  <resume-signal>Type `approved` when the proof record is filled in, or reply with the recorded viewer results/issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Sync any per-viewer promotion across the support matrix, API-stability prose, and docs-contract tests</name>
  <read_first>
    - .planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md
    - priv/support_matrix.json
    - guides/api_stability.md
    - test/docs_contract/protection_claims_test.exs
  </read_first>
  <files>priv/support_matrix.json, guides/api_stability.md, test/docs_contract/protection_claims_test.exs</files>
  <acceptance_criteria>
    - `priv/support_matrix.json` uses the D-05 proof names for `protection.viewers.*.proof`.
    - Only viewers with a recorded full checklist pass are promoted to `"supported"` per D-01 through D-03; all other protection viewers remain `"unverified"`.
    - `guides/api_stability.md` mirrors the exact per-viewer result from the validation record while preserving the existing structural-vs-viewer distinction and unsupported narrative wording.
    - `test/docs_contract/protection_claims_test.exs` asserts the same support posture and fails on drift.
  </acceptance_criteria>
  <action>
    Use the completed proof sheet from Task 2 as the only promotion source of truth. Update `priv/support_matrix.json` so `protection.viewers` uses the locked proof names from D-05 and promote viewers independently per D-01 through D-03; if only Acrobat passes, promote only `adobe_acrobat_reader`, and if neither passes, leave both rows `unverified`. Then update `guides/api_stability.md` to publish the smallest truthful proven contract without widening the security story, and revise `test/docs_contract/protection_claims_test.exs` in the same change set so the support matrix and guide wording cannot drift.
  </action>
  <verify>
    <automated>mix test test/docs_contract/protection_claims_test.exs && mix docs.contract</automated>
  </verify>
  <done>The public protection viewer contract matches the recorded proof exactly, with no promoted viewer lacking a full checklist pass and no unsynced matrix/guide/test drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 52 structural lane -> Phase 54 viewer-proof lane | Structural decryptability evidence feeds the manual viewer-proof surface but does not itself justify viewer promotion. |
| Manual operator -> public support contract | Human proof notes become machine-readable support claims and guide prose here. |
| Fixture generator -> host CLI tools | The proof fixture crosses from Elixir into `qpdf`/`pdfinfo` availability and filesystem handling here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Spoofing / semantic confusion | `priv/support_matrix.json` | mitigate | Require a full five-check recorded pass before any viewer row is promoted, and keep open-only success insufficient per D-09 through D-11. |
| T-54-02 | Information disclosure | `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md` | mitigate | Record only viewer/version/OS/fixture/date/pass-fail/short notes and keep password material or raw secrets out of proof notes per D-06 and D-07. |
| T-54-03 | Repudiation | `.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md` | mitigate | Make the validation record the canonical source of truth for promotion, with explicit viewer/date/fixture metadata and per-check outcomes. |
| T-54-04 | Denial of service / workflow breakage | `scripts/protected_viewer_proof_fixture.exs` | mitigate | Surface missing `qpdf`, missing `pdfinfo`, or unaccepted Phase 52 readiness as explicit blocking prerequisites instead of partial execution or silent fallback. |
</threat_model>

<verification>
mix run scripts/protected_viewer_proof_fixture.exs --dry-run --output /tmp/rendro-phase54-proof.pdf
mix test test/docs_contract/protection_claims_test.exs
mix docs.contract
</verification>

<success_criteria>
- The Phase 54 validation artifact contains the locked protection checklist and the explicit Phase 52 plus host-tool gate.
- Manual proof is recorded per viewer with the D-06 metadata and no password leakage.
- `priv/support_matrix.json`, `guides/api_stability.md`, and docs-contract tests publish the same smallest truthful proven contract.
- No native encryption, new adapter API, second integration tutorial, or broader viewer taxonomy is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/54-proof-closure-and-release-tail/54-01-SUMMARY.md`
`54-VERIFICATION.md` is intentionally owned by the later verification workflow (`$gsd-verify-work` / verifier), not by this execution plan.
</output>
