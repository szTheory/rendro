---
phase: 54
plan: 02
type: execute
wave: 2
depends_on:
  - "54-01"
files_modified:
  - CHANGELOG.md
  - guides/integrations.md
  - test/docs_contract/integrations_claims_test.exs
  - lib/mix/tasks/release/preflight.ex
  - test/mix/tasks/release_preflight_test.exs
  - test/scripts/release_preflight_proof_test.exs
autonomous: true
requirements:
  - RELEASE-01
must_haves:
  truths:
    - "Release-tail wording points downstream users to the one canonical protected-delivery recipe instead of creating a second integration story."
    - "Release readiness is executable: changelog state, docs-contract state, and release-preflight checks close before publish."
    - "The existing isolated exact-tag proof path remains the final release gate rather than a manual narrative-only checklist."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release-ready Phase 54 protected-delivery entry for the current version"
    - path: "guides/integrations.md"
      provides: "Thin publish-tail pointer back to the canonical protected-delivery recipe"
    - path: "lib/mix/tasks/release/preflight.ex"
      provides: "Executable release-readiness checks that include the changelog closure expected by this phase"
  key_links:
    - from: "CHANGELOG.md"
      to: "guides/integrations.md"
      via: "release notes point to the canonical protected-delivery recipe instead of duplicating it"
      pattern: "render_to_artifact -> Protect.password -> store/deliver|Mailglass|Oban"
    - from: "lib/mix/tasks/release/preflight.ex"
      to: "CHANGELOG.md"
      via: "preflight enforces release-tail readiness rather than assuming changelog closure"
      pattern: "CHANGELOG|current version|0.1.0"
    - from: "scripts/release_preflight_proof.exs"
      to: "lib/mix/tasks/release/preflight.ex"
      via: "exact-tag worktree proof shells through the hardened preflight checks"
      pattern: "release.preflight|worktree"
---

<objective>
Close the v1.10 release tail once proof status is explicit, using the existing release-preflight path instead of inventing a new workflow.

Purpose: satisfy `RELEASE-01` by making changelog/readiness closure executable, adding one thin downstream pointer to the canonical protected-delivery recipe, and preserving the isolated exact-tag proof path as the last publish gate.
Output: release-ready changelog wording, a narrow integrations pointer, and hardened release-preflight/proof regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/phases/54-proof-closure-and-release-tail/54-CONTEXT.md
@.planning/phases/54-proof-closure-and-release-tail/54-RESEARCH.md
@.planning/phases/54-proof-closure-and-release-tail/54-PATTERNS.md
@.planning/phases/54-proof-closure-and-release-tail/54-VALIDATION.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-VERIFICATION.md
@CHANGELOG.md
@guides/integrations.md
@lib/mix/tasks/release/preflight.ex
@scripts/release_preflight_proof.exs
@test/docs_contract/integrations_claims_test.exs
@test/mix/tasks/release_preflight_test.exs
@test/scripts/release_preflight_proof_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the thin release-tail pointer to the canonical protected-delivery recipe and close the changelog entry</name>
  <read_first>
    - .planning/phases/54-proof-closure-and-release-tail/54-CONTEXT.md
    - .planning/phases/53-delivery-threading-and-truthful-support-contract/53-VERIFICATION.md
    - CHANGELOG.md
    - guides/integrations.md
    - test/docs_contract/integrations_claims_test.exs
  </read_first>
  <files>CHANGELOG.md, guides/integrations.md, test/docs_contract/integrations_claims_test.exs</files>
  <acceptance_criteria>
    - `CHANGELOG.md` includes a concise v1.10 release-tail note that points Phoenix/Mailglass users back to the canonical protected-delivery recipe and restates the boundary from D-14 without creating a second tutorial.
    - `guides/integrations.md` receives only a thin publish-tail pointer or reinforcement line; it does not add new Mailglass APIs, orchestration helpers, or a second protected-delivery walkthrough per D-13 through D-15.
    - `test/docs_contract/integrations_claims_test.exs` asserts the same pointer language and continues to reject password persistence or Mailglass-managed protection semantics.
  </acceptance_criteria>
  <action>
    Update `CHANGELOG.md` so the current unreleased entry closes the v1.10 protected-delivery story in one concise operator-facing note: point to the canonical `render_to_artifact -> Protect.password -> store/deliver` recipe, reiterate that passwords stay out of persisted Oban args, and state that Mailglass transports already-protected artifacts rather than password material, all per D-12 through D-15. Reinforce the same point in `guides/integrations.md` only where it improves the publish-tail handoff; do not add another end-to-end tutorial, extra code paths, or new API names.
    Extend `test/docs_contract/integrations_claims_test.exs` so the release-tail pointer becomes executable contract coverage. Keep the assertions narrow and pointer-shaped: they should lock the canonical recipe and the no-password-in-Oban/Mailglass boundary, not assert a second integration narrative.
  </action>
  <verify>
    <automated>mix test test/docs_contract/integrations_claims_test.exs</automated>
  </verify>
  <done>The changelog and integrations guide point to one canonical protected-delivery recipe, and docs-contract tests prevent drift into a second integration story.</done>
</task>

<task type="auto">
  <name>Task 2: Make release-tail readiness executable through preflight and exact-tag proof coverage</name>
  <read_first>
    - lib/mix/tasks/release/preflight.ex
    - scripts/release_preflight_proof.exs
    - test/mix/tasks/release_preflight_test.exs
    - test/scripts/release_preflight_proof_test.exs
    - CHANGELOG.md
  </read_first>
  <files>lib/mix/tasks/release/preflight.ex, test/mix/tasks/release_preflight_test.exs, test/scripts/release_preflight_proof_test.exs</files>
  <acceptance_criteria>
    - `mix release.preflight` checks the changelog/readiness condition this phase depends on instead of assuming it informally.
    - `test/mix/tasks/release_preflight_test.exs` covers both pass and fail behavior for the new release-tail readiness check without weakening the existing clean-worktree or exact-tag guards.
    - `test/scripts/release_preflight_proof_test.exs` still proves the isolated worktree path shells through `mix release.preflight` and cleans up synthetic tag/worktree state on both success and failure.
    - No new release workflow or parallel checklist is introduced; the existing `release.preflight` plus `release_preflight_proof.exs` path remains canonical.
  </acceptance_criteria>
  <action>
    Harden `lib/mix/tasks/release/preflight.ex` so Phase 54's release-tail closure is executable, not narrative-only. Add the smallest useful readiness check tied to this phase's actual source of truth, such as confirming the current version's changelog heading exists and the protected-delivery release note is present before the Hex build/dry-run stage proceeds. Do not convert preflight into a general documentation linter or invent new publish orchestration.
    Update `test/mix/tasks/release_preflight_test.exs` to pin the new check in both failing and passing paths, keeping the existing phase ordering intact. Update `test/scripts/release_preflight_proof_test.exs` only as needed so the exact-tag worktree proof remains aligned with the hardened preflight behavior and still proves cleanup on success/failure. Preserve the existing isolated-worktree strategy and keep `scripts/release_preflight_proof.exs` as the canonical final gate.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/release_preflight_test.exs test/scripts/release_preflight_proof_test.exs</automated>
  </verify>
  <done>Release-tail readiness is enforced by executable checks, and the existing isolated exact-tag proof path remains the final publish gate for v1.10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release notes/docs -> downstream users | Public wording shapes user expectations about the protected-delivery contract here. |
| Active workspace -> release-preflight task | Local repo state is evaluated as publish-ready or not at this boundary. |
| Release proof script -> isolated worktree | The final publish proof crosses from the active repo into a detached exact-tag workspace here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | Tampering | `lib/mix/tasks/release/preflight.ex` | mitigate | Keep clean-worktree and exact-tag checks intact, and add an explicit changelog/readiness gate so release proof cannot pass on incomplete publish metadata. |
| T-54-06 | Spoofing / semantic confusion | `CHANGELOG.md` and `guides/integrations.md` | mitigate | Keep the release-tail wording pointer-thin and route users back to the canonical Phase 53 recipe per D-13 through D-15 instead of publishing a second workflow. |
| T-54-07 | Repudiation | `scripts/release_preflight_proof.exs` | mitigate | Preserve the isolated exact-tag worktree proof and test its cleanup behavior so release readiness remains reproducible and auditable. |
| T-54-08 | Scope drift | `guides/integrations.md` | mitigate | Reject new Mailglass APIs, orchestration helpers, or broader tutorial expansion in this plan and lock the narrow pointer with docs-contract assertions. |
</threat_model>

<verification>
mix test test/docs_contract/integrations_claims_test.exs test/mix/tasks/release_preflight_test.exs test/scripts/release_preflight_proof_test.exs
mix docs.contract
mix release.preflight
mix run scripts/release_preflight_proof.exs --current-version-tag --worktree /tmp/rendro-release-proof
</verification>

<success_criteria>
- Release notes and integration docs publish one canonical protected-delivery recipe pointer and do not fork the Phase 53 story.
- `mix release.preflight` encodes the release-tail readiness expected by this milestone.
- The isolated exact-tag proof path still drives final release validation and cleanup.
- No new orchestration helper, second integration tutorial, native encryption work, or adapter API widening is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/54-proof-closure-and-release-tail/54-02-SUMMARY.md`
`54-VERIFICATION.md` is intentionally owned by the later verification workflow (`$gsd-verify-work` / verifier), not by this execution plan.
</output>
