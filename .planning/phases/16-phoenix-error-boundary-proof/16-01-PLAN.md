---
phase: 16-phoenix-error-boundary-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/adapters/phoenix.ex
  - test/rendro/adapters/phoenix_test.exs
autonomous: true
requirements:
  - OBS-03
must_haves:
  truths:
    - JSON requests receiving an error get a 500 status with structured JSON payload.
    - Non-JSON requests receiving an error get a 500 status with plain-text fallback.
    - Error response contains stable operator-facing envelope fields.
  artifacts:
    - path: lib/rendro/adapters/phoenix.ex
      provides: HTTP format negotiation and error serialization
    - path: test/rendro/adapters/phoenix_test.exs
      provides: Committed conn-boundary proof for error paths
  key_links:
    - from: lib/rendro/adapters/phoenix.ex
      to: Rendro.Error
      via: JSON serialization mapping
---

<objective>
Update the Phoenix adapter error response to use JSON format when the request negotiates JSON, and add a deterministic boundary test for the error path.

Purpose: Convert the Phoenix adapter's structured error response from inferred behavior into a committed operator-facing boundary contract, closing OBS-03.
Output: Updated Phoenix adapter and committed conn-boundary tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/rendro/adapters/phoenix.ex
@test/rendro/adapters/phoenix_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phoenix Adapter Format-Aware Error Response</name>
  <files>lib/rendro/adapters/phoenix.ex, test/rendro/adapters/phoenix_test.exs</files>
  <behavior>
    - Test 1 (JSON format): When an error occurs (e.g., deterministic `:content_overflow`), a request with `phoenix_format: "json"` returns a 500 status, `application/json` content-type, and a JSON body containing `what`, `where`, `why`, `next`, `stage`, and `render_id`.
    - Test 2 (Text format): When an error occurs, a request without `"json"` format (or default) returns a 500 status, `text/plain` content-type, and stringified error body.
  </behavior>
  <action>
    In `test/rendro/adapters/phoenix_test.exs`:
    - Add tests for both JSON and text fallback error responses using a deterministic overflow document: `Rendro.flow([Rendro.block(Rendro.text("Too big"), height: 2000)])`.
    
    In `lib/rendro/adapters/phoenix.ex`:
    - Update error branches (e.g., `{:error, error}`) to retrieve the format safely: `try do Phoenix.Controller.get_format(conn) rescue _ -> "text" end`.
    - If format is `"json"`, encode the stable envelope fields (`what`, `where`, `why`, `next`, `stage`, `render_id`) using `Phoenix.json_library().encode_to_iodata!()`. Send as `application/json`.
    - If not JSON, use the existing `to_string(error)` behavior and send as `text/plain`.
    - Ensure raw internal `reason` or `details` are NOT exposed in the JSON response by default.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/phoenix_test.exs</automated>
  </verify>
  <done>Adapter correctly routes format to JSON or text, and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter→Client | Error responses sent back to the HTTP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | lib/rendro/adapters/phoenix.ex | mitigate | Explicitly map only stable envelope fields to JSON (`what`, `where`, `why`, `next`, `stage`, `render_id`) and avoid exposing raw internal `reason` or `details` maps. |
</threat_model>

<verification>
- `mix test test/rendro/adapters/phoenix_test.exs` passes.
- JSON errors contain only expected stable fields.
</verification>

<success_criteria>
- A committed Phoenix conn-boundary test exercises the adapter error path through an HTTP response.
- The HTTP error response proves the structured `Rendro.Error` envelope is preserved at the operator-facing boundary.
- OBS-03 is fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-phoenix-error-boundary-proof/16-01-SUMMARY.md`
</output>