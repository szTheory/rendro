---
phase: 38-advanced-layout-verification-and-recipe-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rendro/recipes/invoice_test.exs
  - README.md
  - test/docs_contract/readme_doctest_test.exs
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03]
must_haves:
  truths:
    - "Invoice recipe can render multi-page tables with Arabic text deterministically."
    - "Documentation accurately reflects Arabic and multi-page layout usage in an evaluable contract."
  artifacts:
    - path: test/rendro/recipes/invoice_test.exs
      provides: Deterministic verification of Arabic multi-page output.
    - path: README.md
      provides: Docs-contract example of advanced layout.
    - path: test/docs_contract/readme_doctest_test.exs
      provides: Verification of the docs contract.
  key_links:
    - from: test/rendro/recipes/invoice_test.exs
      to: Rendro.Recipes.Invoice
      via: Rendro.render(doc, deterministic: true)
---

<objective>
Verify that multi-page tables with Arabic content render deterministically with correct shaping and fallbacks, and add the corresponding docs-contract test.

Purpose: This serves as the final integration and verification step for the v1.4 Advanced Layout & Typography milestone, ensuring the new shaping and fragmentation behaviors work perfectly together.
Output: An updated Invoice test suite and README contract.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Advanced Layout Deterministic Test</name>
  <files>test/rendro/recipes/invoice_test.exs</files>
  <action>
    Add a new test block in `describe "document/2"` named "multi-page invoice with Arabic item names produces stable deterministic output". 
    1. Register an embedded Arabic font into the document (e.g., `"NotoSansArabic"`). Do NOT use a font that lacks Arabic glyphs.
    2. Generate an `items` list of 60 items with names containing Arabic text (e.g., `%{name: "تفاصيل العميل #{i} (Consulting)", qty: 1, price: 100}`).
    3. Render the invoice document sequentially 5 times with `deterministic: true`. 
    4. Assert byte-for-byte equivalence (`assert pdf == ref_pdf`). 
    5. Assert the PDF structure indicates multi-page pagination by verifying multiple `/Type /Page` dictionary entries exist in the output binary (e.g., `assert length(Regex.scan(~r/\/Type\s*\/Page\b/, ref_pdf)) > 1`).
    6. Assert the presence of the Arabic font dictionary in the output PDF binary (e.g., `assert ref_pdf =~ "NotoSansArabic"`), indicating successful shaping.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_test.exs</automated>
  </verify>
  <done>The test runs successfully, verifying that rendering a multi-page table with Arabic text is stable, deterministic, fragments correctly across pages, and includes the expected font dictionary.</done>
</task>

<task type="auto">
  <name>Task 2: Docs-Contract Test Update</name>
  <files>README.md, test/docs_contract/readme_doctest_test.exs</files>
  <action>
    1. Update `README.md` to add a new section demonstrating Advanced Layout and i18n support. Add an Elixir code block tagged with `<!-- fences: readme-invoice-i18n-compile -->`. This snippet should render a `Rendro.Recipes.Invoice` document passing an Arabic customer name or item name. Use fully qualified module names (e.g., `Rendro.Recipes.Invoice.document`) and avoid placeholders like `...` so the snippet compiles cleanly.
    2. Update `test/docs_contract/readme_doctest_test.exs` to append `"readme-invoice-i18n-compile"` to the list of expected fences in the `README compile/eval fences are explicit and compile cleanly` test.
  </action>
  <verify>
    <automated>mix test test/docs_contract/readme_doctest_test.exs</automated>
  </verify>
  <done>The docs contract test passes, proving the new snippet in the README compiles cleanly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code → Test | Tests assert the behavior of internal libraries, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `test/rendro/recipes/invoice_test.exs` | accept | Test files are internal and only run in trusted CI environments; mock inputs are statically defined. |
</threat_model>

<verification>
Ensure both the new deterministic invoice test and the docs-contract test pass.
</verification>

<success_criteria>
- The invoice test validates deterministic byte-for-byte PDF rendering of a multi-page table with Arabic text.
- The README has a working code snippet demonstrating the feature.
- All docs-contract tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/38-advanced-layout-verification-and-recipe-updates/38-01-SUMMARY.md`
</output>