---
phase: 124-address-v2-11-tech-debt-stale-113-docs-contract-test-formatt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes/background.ex
  - lib/rendro/launch_artifacts.ex
  - test/docs_contract/theme_industry_guard_test.exs
  - test/docs_contract/theming_claims_test.exs
  - test/rendro/recipes/payslip_opts_threading_test.exs
  - test/rendro/recipes/themed_render_smoke_test.exs
  - test/rendro/recipes/certificate_typography_test.exs
  - test/rendro/recipes/theme_mode_background_golden_test.exs
  - test/docs_contract/dx_local_reproducibility_claims_test.exs
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "mix dialyzer reports 0 errors (was 133 across 10 files), fixed via a single-file open-map @spec widening on Rendro.Recipes.Background.emit?/1 -- D-03."
    - "mix format --check-formatted exits 0; the reformat touched exactly the 7 known WINDOWS-id-4 files and is formatting-only (whitespace/paren/wrap, zero logic or identifier change) -- D-02."
    - "mix test test/docs_contract/dx_local_reproducibility_claims_test.exs reports 3 tests, 0 failures (was 5 tests, 2 failures); the 2 removed cases guarded only archived, deleted Phase-113 evidence with no forward-looking regression value -- D-01."
    - "The byte-identity/golden regression suite (9 files: 7 recipe *_byte_identity_test.exs + table_byte_identity_test.exs + theme_mode_background_golden_test.exs) shows 0 failures both before and after the background.ex spec fix -- the milestone's central regression guard (D-03/D-06)."
    - "mix ci.fast runs green end-to-end across all 7 steps (format, hex.build, compile, test, docs, credo, dialyzer) -- D-06."
  artifacts:
    - lib/rendro/recipes/background.ex
    - test/docs_contract/dx_local_reproducibility_claims_test.exs
    - lib/rendro/launch_artifacts.ex
    - test/docs_contract/theme_industry_guard_test.exs
    - test/docs_contract/theming_claims_test.exs
    - test/rendro/recipes/payslip_opts_threading_test.exs
    - test/rendro/recipes/themed_render_smoke_test.exs
    - test/rendro/recipes/certificate_typography_test.exs
    - test/rendro/recipes/theme_mode_background_golden_test.exs
  key_links:
    - "Background.emit?/1's @spec <-> all 14 call sites across all 7 recipes' palette(opts)-derived colors maps (statement/payslip/ticket/certificate/branded_invoice/invoice/receipt) -- widening the single shared seam's spec clears the entire 133-error dialyzer cascade with zero change to any recipe file."
    - "dx_local_reproducibility_claims_test.exs's 3 surviving tests <-> scripts/verify_docs.exs / .github/workflows/ci.yml / README.md+CONTRIBUTING.md -- the live docs-contract guarantees these tests actually enforce, left untouched by this phase."
  prohibitions:
    - "MUST NOT change rendered output / PDF bytes for any recipe -- the byte-identity/golden suite (9 files) MUST report the identical failure count (0) before and after (D-03/D-06, the milestone's central regression guard)."
    - "MUST NOT touch lib/rendro/recipes/ticket.ex, its role mapping, type scale, or visual hierarchy -- WINDOWS id 2 is a LOCKED Phase-122 design outcome, explicitly out of scope (D-04)."
    - "MUST NOT create, restore, or write to any file under .planning/phases/113-dx-local-reproducibility-validation/ -- those evidence files were deleted by design during milestone cleanup (D-01)."
    - "MUST NOT let `mix format` (bare, no path args) touch any file outside the known bounded 7-file set (WINDOWS id 4) -- if it does, STOP, do not commit, and treat it as a formatter config/version mismatch to investigate (D-02)."
    - "MUST NOT expand the dialyzer fix beyond lib/rendro/recipes/background.ex -- no edits to recipes.ex, launch_artifacts.ex logic, ticket.ex, or rendro.visual_uat.ex; the entire 133-error cascade traces to the single background.ex spec (D-03, D-05)."
---

<objective>
Clear the three non-blocking v2.11 tech-debt items so `mix ci.fast` runs green end-to-end, WITHOUT
changing any rendered output: (1) fix the stale `dx_local_reproducibility_claims_test.exs` (D-01), (2)
resolve formatter-version drift on 7 files (D-02), and (3) correct the `Background.emit?/1` type contract
so `mix dialyzer` passes (D-03) -- preserving the byte-identity golden guard (D-06) and leaving the locked
Ticket visual-hierarchy regression (WINDOWS id 2, D-04) untouched.

Purpose: this is the tech-debt closeout for the shipped v2.11 milestone -- clearing the three remaining
red/false-red `ci.fast` gates (steps 1, 4, 7) identified by the milestone audit, so the milestone can be
archived clean.
Output: `mix ci.fast` green end-to-end; `mix dialyzer` 0 errors; `mix format --check-formatted` exit 0;
the stale docs-contract test file reduced to its 3 live cases; zero rendered-output change.

RESEARCH.md (this phase) reproduced and empirically verified all three fixes live (applied, tested,
reverted) before this plan was written -- treat its recommendations as validated, not hypotheses. Two
targets turned out wider in blast radius than CONTEXT.md's framing (dialyzer touches 133 errors across 10
files, not just ticket.ex), but the *fix* for each is exactly what D-01/D-02/D-03 anticipated: a single-file
spec widening, a bounded `mix format`, and two test-case deletions.

## Explicitly NOT in this plan (D-05 scope fence -- deferred, not dropped)
- Ticket visual-hierarchy re-mapping (WINDOWS id 2, locked Phase-122 non-monotone role-assignment decision)
  -- future milestone.
- `pdfium-cli` v0.11.0 install/PATH provisioning (WINDOWS id 7) -- environment setup, not code.
- `/gsd-validate-phase 121`, `122`, `123` -- Nyquist coverage completion, not this phase.
- `from_brand/2` byte-level E2E golden -- integration-hardening follow-up.
- SUMMARY frontmatter `requirements_completed` backfill -- bookkeeping follow-up.

## Artifacts This Phase Produces

This phase MODIFIES existing files only -- no new public symbols, modules, or files are created. The
only symbol-shape change is the widened `@spec` on `Rendro.Recipes.Background.emit?/1` (a type contract,
not a new function or a behavior change).

| File | Change |
|------|--------|
| `lib/rendro/recipes/background.ex` | `@spec emit?/1` widened from a closed 1-key map type to an open map type (`required(:background) => {non_neg_integer(), non_neg_integer(), non_neg_integer()}, optional(atom()) => any()`). Contract-only; `def emit?/1`'s implementation and every caller's runtime behavior stay byte-identical. |
| `test/docs_contract/dx_local_reproducibility_claims_test.exs` | 2 stale test cases + 3 orphaned module attributes (`@uat_path`, `@metrics_path`, `@audit_path`) removed; 3 tests remain, unmodified. |
| `lib/rendro/launch_artifacts.ex` | `mix format` whitespace-only (multi-line string-literal collapse). No logic change. |
| `test/docs_contract/theme_industry_guard_test.exs` | `mix format` whitespace-only (added parens around `refute source =~ term`). |
| `test/docs_contract/theming_claims_test.exs` | `mix format` whitespace-only (blank-line insertion). |
| `test/rendro/recipes/payslip_opts_threading_test.exs` | `mix format` whitespace-only (map/keyword literal re-wrap). |
| `test/rendro/recipes/themed_render_smoke_test.exs` | `mix format` whitespace-only (map/keyword literal re-wrap). |
| `test/rendro/recipes/certificate_typography_test.exs` | `mix format` whitespace-only (map/keyword literal re-wrap). |
| `test/rendro/recipes/theme_mode_background_golden_test.exs` | `mix format` whitespace-only (blank-line insertion). |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/124-address-v2-11-tech-debt-stale-113-docs-contract-test-formatt/124-CONTEXT.md
@.planning/phases/124-address-v2-11-tech-debt-stale-113-docs-contract-test-formatt/124-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen Background.emit?/1's @spec to clear the dialyzer cascade (D-03)</name>
  <reversibility rating="reversible">Single @spec line in one file; RESEARCH.md confirmed live that `git checkout -- lib/rendro/recipes/background.ex` restores the prior 133-error baseline exactly.</reversibility>
  <precondition>The dialyzer PLT (`_build/dev/dialyxir_erlang-28.4.1_elixir-1.19.5_deps-dev.plt`) already exists (confirmed built in RESEARCH.md); if it is somehow absent, `mix dialyzer` will build it first (several minutes) before reporting the baseline 133 errors.</precondition>
  <files>lib/rendro/recipes/background.ex</files>
  <read_first>
    - lib/rendro/recipes/background.ex:25-27 (the `emit?/1` @spec + implementation to widen)
    - lib/rendro/recipes/ticket.ex:132,171,224,256,273 (confirm zero change needed here -- ticket.ex's own @specs are already correct; they only surfaced `invalid_contract`/`no_return` because `Background.emit?(colors)` poisoned their success typing)
    - 124-RESEARCH.md "Target 3 -- Ticket/Background dialyzer contract" (root cause: `%{background: T}` is a CLOSED map type in a Dialyzer @spec, not open; every real caller passes a 7-9-key colors map; the fix is empirically verified live, 133 -> 0 errors, zero runtime change)
    - 124-CONTEXT.md D-03 (spec-only fix, zero rendered-output change) and D-04 (Ticket hierarchy stays out)
  </read_first>
  <action>
    In `lib/rendro/recipes/background.ex`, widen the `@spec emit?/1` (currently the closed-map shorthand
    `%{background: {non_neg_integer(), non_neg_integer(), non_neg_integer()}}`) to an open map type:
    require `:background` with its existing `{non_neg_integer(), non_neg_integer(), non_neg_integer()}`
    tuple type, and add `optional(atom()) => any()` so the type accepts any map that has at least a
    `:background` key (the shape every real caller's `colors` map actually has). Keep the return type
    `:: boolean()` unchanged. Do NOT touch `def emit?(%{background: bg}), do: bg != @paper_white` -- the
    implementation is correct today; only the type annotation is wrong. Do NOT edit any other function in
    this file (`region/2`, `section/3` already use open/no-shorthand specs and have zero dialyzer errors).
    Do NOT edit `lib/rendro/recipes/ticket.ex` or any other recipe file -- RESEARCH.md confirmed this
    single-file, single-line change clears the entire cascade.
  </action>
  <verify>
    <automated>mix dialyzer | grep -E "Total errors: 0, Skipped: 0, Unnecessary Skips: 0" && mix test test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/recipes/certificate_byte_identity_test.exs test/rendro/recipes/invoice_byte_identity_test.exs test/rendro/recipes/payslip_byte_identity_test.exs test/rendro/recipes/receipt_byte_identity_test.exs test/rendro/recipes/statement_byte_identity_test.exs test/rendro/recipes/ticket_byte_identity_test.exs test/rendro/recipes/theme_mode_background_golden_test.exs test/rendro/table_byte_identity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix dialyzer` prints "Total errors: 0, Skipped: 0, Unnecessary Skips: 0" (was "Total errors: 133").
    - The 9-file byte-identity/golden suite above exits 0 (RESEARCH.md baseline: 27 tests, 0 failures both before and after this exact fix -- the count is informational, the hard bar is 0 failures).
    - `git diff --stat -- lib/rendro/recipes/ticket.ex` is empty (ticket.ex untouched -- D-04 guardrail).
    - `git diff -- lib/rendro/recipes/background.ex` shows only the `@spec emit?/1` hunk changed; the `def emit?/1` body line is identical before and after.
  </acceptance_criteria>
  <done>mix dialyzer reports 0 errors (was 133 across 10 files); the byte-identity/golden suite is unchanged; only background.ex's @spec changed; ticket.ex was not touched.</done>
</task>

<task type="auto">
  <name>Task 2: Apply bounded mix format to clear the ci.fast step-1 formatter gate (D-02)</name>
  <reversibility rating="reversible">`mix format` is deterministic and idempotent; git revert of these 7 files restores the prior (drifted) formatting exactly.</reversibility>
  <files>lib/rendro/launch_artifacts.ex, test/docs_contract/theme_industry_guard_test.exs, test/docs_contract/theming_claims_test.exs, test/rendro/recipes/payslip_opts_threading_test.exs, test/rendro/recipes/themed_render_smoke_test.exs, test/rendro/recipes/certificate_typography_test.exs, test/rendro/recipes/theme_mode_background_golden_test.exs</files>
  <read_first>
    - 124-RESEARCH.md "Target 2 -- Formatter drift" (exact 7-file list, live-confirmed bounded-check: `git diff --stat` was `7 files changed, 47 insertions(+), 17 deletions(-)`, every hunk whitespace/paren/wrap only, 43 affected-test-file tests unchanged after formatting)
    - 124-CONTEXT.md D-02 (bounded, formatting-only guardrail; STOP if the reformat touches more than the known set)
    - .formatter.exs (confirm the input globs are unchanged before running -- no config edit in this task)
  </read_first>
  <action>
    Run bare `mix format` (no path arguments -- uses `.formatter.exs`'s existing input globs). Then run
    `git status --short` / `git diff --name-only` and confirm the changed-file set is EXACTLY these 7
    paths, no more and no fewer: lib/rendro/launch_artifacts.ex, test/docs_contract/theme_industry_guard_test.exs,
    test/docs_contract/theming_claims_test.exs, test/rendro/recipes/payslip_opts_threading_test.exs,
    test/rendro/recipes/themed_render_smoke_test.exs, test/rendro/recipes/certificate_typography_test.exs,
    test/rendro/recipes/theme_mode_background_golden_test.exs. If `mix format` touches any file outside
    this set: STOP, do not commit, and report it as a formatter-config/version mismatch requiring
    investigation (D-02's hard guardrail) rather than committing a mass reformat. Spot-check
    `git diff -- lib/rendro/launch_artifacts.ex` (the one `lib/` file in the set, and the one that ships in
    the Hex tarball) line by line to confirm every hunk is whitespace/parenthesization/line-wrap only --
    no identifier, string content, operator, or control-flow change anywhere in the 7-file diff.
  </action>
  <verify>
    <automated>mix format && mix format --check-formatted && [ "$(git diff --name-only | wc -l | tr -d ' ')" = "7" ] && mix test test/docs_contract/theme_industry_guard_test.exs test/docs_contract/theming_claims_test.exs test/rendro/recipes/certificate_typography_test.exs test/rendro/recipes/payslip_opts_threading_test.exs test/rendro/recipes/theme_mode_background_golden_test.exs test/rendro/recipes/themed_render_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix format --check-formatted` exits 0.
    - `git diff --name-only | wc -l` equals exactly 7, and the 7 paths match the list above verbatim (cross-check via `git diff --stat`).
    - The 6 affected test files (excluding launch_artifacts.ex, which is exercised transitively by the byte-identity suite) pass: RESEARCH.md baseline is 43 tests, 0 failures.
    - Manual spot-check (per 124-VALIDATION.md's Manual-Only Verifications table -- not machine-checkable): `git diff -- lib/rendro/launch_artifacts.ex` shows only whitespace/string-collapse, confirmed by human read of the diff.
  </acceptance_criteria>
  <done>mix format --check-formatted exits 0; exactly 7 files changed (bounded, matches WINDOWS id 4's list); the 6 affected test files pass unchanged; the diff is formatting-only.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the 2 stale Phase-113 docs-contract test cases + 3 orphaned attributes (D-01)</name>
  <reversibility rating="reversible">git revert restores the two deleted test cases and attributes exactly; no data migration, no external state.</reversibility>
  <files>test/docs_contract/dx_local_reproducibility_claims_test.exs</files>
  <read_first>
    - test/docs_contract/dx_local_reproducibility_claims_test.exs (whole file -- 5 test blocks + 3 module attributes; the 2 cases to remove read `@metrics_path`/`@audit_path` and `@uat_path` respectively; the surviving 3 cases exercise `scripts/verify_docs.exs`, `.github/workflows/ci.yml`, and `README.md`/`CONTRIBUTING.md`)
    - 124-RESEARCH.md "Target 1 -- Stale 113 docs-contract test" (per-case mechanism: both cases assert only frozen, archive-specific historical facts -- specific GitHub Actions run IDs, a specific p50/p95 timing pair, a specific UAT pass count -- from the Phase-113/C1-milestone archive deleted by milestone-cleanup commit `0de2de8`; neither has forward-looking regression value; mechanism for both is DELETE)
    - 124-CONTEXT.md D-01 (fix the test, do not resurrect the deleted planning files; the 3 currently-passing tests MUST stay green)
  </read_first>
  <action>
    Delete the 3 module attributes declaring the phase-113-archive evidence paths (none is referenced
    anywhere else in the file once the two cases below are removed). Delete the two entire test blocks
    whose bodies `File.read!` those deleted archived files -- one asserts specific frozen numbers shared
    between a phase-113 local metrics snapshot and the C1 milestone audit (GitHub Actions run IDs, a
    specific p50/p95 timing pair); the other asserts internal completeness facts of the phase-113 UAT file
    itself (a specific pass count, specific status strings). Both are entirely scoped to the archived
    Phase-113 evidence trail and have zero forward-looking regression value per RESEARCH.md Target 1 --
    delete both in full (docstring/test name through closing `end`). Leave the file's other 3 test blocks
    (the docs-verification-script lane, the scoped CI aliases/workflow wiring, the public contributor docs)
    completely untouched -- do not reformat, reorder, or re-word them. Optionally add a one-line comment
    above the remaining tests noting why the two cases were removed (the milestone-cleanup commit deleted
    their evidence files; see 124-RESEARCH.md Target 1 for the full rationale). Do not create, restore, or
    write to any file under `.planning/phases/113-dx-local-reproducibility-validation/`.
  </action>
  <verify>
    <automated>OUT=$(mix test test/docs_contract/dx_local_reproducibility_claims_test.exs 2>&1); echo "$OUT" | grep -E "3 tests?, 0 failures" && ! echo "$OUT" | grep -q "set but never used" && test ! -e .planning/phases/113-dx-local-reproducibility-validation/113-UAT.md && test ! -e .planning/phases/113-dx-local-reproducibility-validation/113-METRICS.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/docs_contract/dx_local_reproducibility_claims_test.exs` output shows "3 tests, 0 failures" (down from "5 tests, 2 failures") with zero module-attribute "set but never used" compiler warnings.
    - The 3 surviving tests are byte-for-byte unmodified: `git diff -- test/docs_contract/dx_local_reproducibility_claims_test.exs` shows only removed lines (the 2 deleted test blocks + 3 deleted attributes, plus optionally one added comment line) -- no edits inside the 3 surviving test bodies.
    - `.planning/phases/113-dx-local-reproducibility-validation/113-UAT.md` and `113-METRICS.md` remain absent (not resurrected).
  </acceptance_criteria>
  <done>The file compiles/tests green at 3 tests, 0 failures; the 3 surviving tests are untouched; no phase-113 archive files were resurrected; the 3 orphaned module attributes are removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | This phase touches only a `@spec` type annotation (compile-time only, zero runtime effect), test-body deletions, and `mix format` whitespace changes. No user input, no network boundary, no auth/session/crypto path, and no new trust boundary is introduced or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-01 | Tampering | `Rendro.Recipes.Background.emit?/1` @spec (lib/rendro/recipes/background.ex) | low | accept | Widening the spec from closed- to open-map only affects Dialyzer's *static* view of which keys are checked -- `emit?/1` never read any key but `:background`, so no caught-bug surface is lost versus the (broken) status quo (RESEARCH.md Risks item 1, empirically confirmed: 133->0 errors with zero runtime/test regression). The byte-identity/golden suite (27 tests) provides independent runtime-behavior coverage that does not depend on the static type contract. |

No package installs in this phase (`T-124-SC` not applicable -- zero new deps, zero `mix.exs` dependency change per RESEARCH.md's Package Legitimacy Audit). No content-stream/render surface, no auth/session/crypto path touched.
</threat_model>

<verification>
- `mix ci.fast` runs green end-to-end (all 7 steps: format, hex.build, compile, test, docs, credo, dialyzer -- exit 0).
- `mix dialyzer`: 0 errors (was 133 across 10 files).
- `mix format --check-formatted`: exit 0; diff bounded to exactly the 7 known WINDOWS-id-4 files; formatting-only.
- `mix test test/docs_contract/dx_local_reproducibility_claims_test.exs`: 3 tests, 0 failures (was 5 tests, 2 failures).
- Byte-identity/golden suite (9 files): 0 failures, unchanged before and after the background.ex spec fix.
- `git diff --stat -- lib/rendro/recipes/ticket.ex`: empty (untouched, D-04).
- Note: `mix hex.build` (ci.fast step 2) can drop a stray `rendro-*.tar` artifact in the repo root when run locally (RESEARCH.md Risk 6) -- not part of this phase's scope; do not commit it if present.
- After all 3 tasks verify green, mark the corresponding ledger entries fixed: `gsd-tools windows fixed 4` (formatter), `gsd-tools windows fixed 5` (stale test), `gsd-tools windows fixed 6` (dialyzer) -- WINDOWS.md ids 4/5/6 map 1:1 to this plan's Targets 2/1/3 (RESEARCH.md Risk 5). Ids 1, 2, 3, 7 stay open/out of scope.
</verification>

<success_criteria>
- All three previously-red/false-red `mix ci.fast` gates (steps 1, 4, 7) now pass.
- Zero change to any recipe's rendered output, role mapping, or type scale -- the byte-identity golden suite and the locked Ticket hierarchy regression (WINDOWS id 2) are both untouched.
- The stale Phase-113 evidence dependency is removed without resurrecting any deleted planning file.
- Deferred items (D-05: pdfium-cli, Nyquist 121-123 validation, from_brand E2E golden, SUMMARY bookkeeping, Ticket hierarchy remap) remain explicitly out of scope and unplanned.
</success_criteria>

<output>
Create `.planning/phases/124-address-v2-11-tech-debt-stale-113-docs-contract-test-formatt/124-01-SUMMARY.md` when done.
</output>
