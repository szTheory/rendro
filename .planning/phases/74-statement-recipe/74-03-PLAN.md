---
phase: 74-statement-recipe
plan: 03
type: execute
wave: 2
depends_on: ["74-01", "74-02"]
files_modified:
  - lib/rendro/recipes/statement.ex
autonomous: true
requirements: [STMT-01, STMT-03, STMT-04]
must_haves:
  truths:
    - "Rendro.Recipes.Statement.document/2 accepts a bare atom-keyed data map and returns a renderable %Rendro.Document{} (no template authoring required)."
    - "validate_data!/1 raises an instructive ArgumentError on missing required keys, non-Decimal/Float amounts, caller-supplied per-line :balance, and malformed period."
    - "The running balance is computed by the recipe as opening_balance + Σ amount via an exact Decimal fold (caller does not supply balances)."
    - "The footer running region carries ONLY Page X of Y via Rendro.page_number/1, and has a NON-ZERO height so it reserves space."
    - "The three-rung escape hatch document/2, page_template/1, sections/2 is present and structurally consistent with Rendro.Recipes.Invoice."
  artifacts:
    - path: "lib/rendro/recipes/statement.ex"
      provides: "Statement recipe: document/2, page_template/1, sections/2, validate_data!/1, balance fold, header/summary/footer sections"
      contains: "def document(data, opts"
  key_links:
    - from: "lib/rendro/recipes/statement.ex footer section"
      to: "Rendro.page_number/1"
      via: "footer-region section content (D-03)"
      pattern: "page_number"
    - from: "lib/rendro/recipes/statement.ex balance fold"
      to: "Decimal"
      via: "Decimal.add/2 map_reduce over lines (D-05/D-06)"
      pattern: "Decimal\\.add"
---

<objective>
Build the `Rendro.Recipes.Statement` core: the three-rung skeleton (D-07/STMT-03) mirrored from `Invoice`, the errors-as-product `validate_data!/1` (D-08), the exact Decimal running-balance fold (D-05/D-06), the `page_template/1` with a NON-ZERO footer region carrying the PAGE primitive (D-03/STMT-04), and the non-paginated sections (header, summary, footer). The transaction-table pagination + carried/brought-forward body rows (D-01/D-02/D-10) are deliberately deferred to plan 74-04, which extends `sections/2` and adds the full test suite.

Purpose: Establish the recipe contract and all the parts that do NOT require knowing page-break points, so 74-04 can focus solely on the novel pagination/carried-forward work against a stable skeleton.

Output: `lib/rendro/recipes/statement.ex` with `document/2`, `page_template/1`, `sections/2` (returning header + a single-block body placeholder for now + footer), `validate_data!/1`, and the private Decimal balance fold. Compiles clean; a smoke render of a single-page statement succeeds. (Full multi-page + carried/brought-forward tests land in 74-04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-statement-recipe/74-CONTEXT.md
@.planning/phases/74-statement-recipe/74-RESEARCH.md
@lib/rendro/recipes/invoice.ex
@lib/rendro/recipes/branded_invoice.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-rung skeleton + page_template with non-zero footer carrying the PAGE primitive (STMT-03/STMT-04/D-03/D-07)</name>
  <files>lib/rendro/recipes/statement.ex</files>
  <read_first>
    - lib/rendro/recipes/invoice.ex (document/2 → page_template/1 → sections/2 chain and the Document builder reduce at ~93-105; private *_section/1 builder structure)
    - lib/rendro/recipes/branded_invoice.ex (non-default page_template with EXPLICIT region heights at ~48-92 — the pattern for setting a non-zero footer height; note its footer currently has height: 0 which Statement must NOT copy)
    - lib/rendro.ex (page_number/1 at ~209-214; section/1, region/1, block/2, text/2, page_template/1 builders)
    - lib/rendro/page_template.ex (default footer region height is 0 — Pitfall 2; A4 595.28×841.89, default body height)
    - .planning/phases/74-statement-recipe/74-CONTEXT.md (D-03, D-07)
    - .planning/phases/74-statement-recipe/74-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 2; Open Q3 period shape recommendation)
  </read_first>
  <action>
    Create `defmodule Rendro.Recipes.Statement`. Mirror `Invoice` verbatim in spirit:
    - `document(data, opts \\ [])` — call `validate_data!(data)` (Task 2), build `page_template(opts)` and `sections(data, opts)`, then `Rendro.Document.new() |> Rendro.Document.add_template(template) |> Rendro.Document.set_template(template.name)` and reduce the sections in with `Rendro.Document.add_section/2` (same chain as `invoice.ex`).
    - `page_template(opts \\ [])` — define `:header` (role `:header`, anchor `:top`), `:body` (role `:body`, anchor `:flow`), and `:footer` (role `:footer`, anchor `:bottom`) regions with EXPLICIT heights. The footer height MUST be non-zero (recommend ~24pt — magnitude tunable, the requirement is `> 0`) so `body_capacity` reserves space and the page number does not overlap the last body row. Position the footer region just above the bottom margin (mirror branded_invoice region coordinates). Allow `opts` to override via `Keyword.merge`. Default `name: :statement`.
    - `footer_section/2` (private) — `Rendro.section(name: :statement_footer, region: :footer, content: [Rendro.page_number(Keyword.get(opts, :page_number_opts, []))])`. Footer carries ONLY the page number (D-03). Do NOT put balances in the footer.
    - `sections(data, opts)` — return `[header_section(data, opts), body_section(data, opts), footer_section(data, opts)]`. For THIS plan, `body_section/2` may emit a single non-paginated table block of all rows (placeholder); plan 74-04 replaces it with per-page chunking. `header_section/2` renders account + period + opening_balance using `Rendro.Format` (from plan 74-02).
    Pick the `period` shape `%{from: Date.t(), to: Date.t()}` (RESEARCH Open Q3) and keep it consistent. Each public function gets `@doc`/`@spec`. Keep `Rendro.Recipes.Base`/`recipes.ex` extraction OPTIONAL — only create it if shared helpers genuinely warrant it (RESEARCH Open Q5); default to keeping helpers private to `Statement`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix run -e 'data = %{period: %{from: ~D[2026-05-01], to: ~D[2026-05-31]}, account: %{name: "Acme"}, opening_balance: Decimal.new("100.00"), lines: [%{date: ~D[2026-05-02], description: "Inv", amount: Decimal.new("50.00")}]}; {:ok, _pdf} = Rendro.render(Rendro.Recipes.Statement.document(data))'</automated>
  </verify>
  <acceptance_criteria>
    - `lib/rendro/recipes/statement.ex` defines `def document(data, opts \\ [])`, `def page_template(opts \\ [])`, and `def sections(data, opts)`, each with a `@spec` (three-rung consistent with Invoice).
    - `page_template/1` returns a template whose `:footer` region has a height `> 0` (grep/assert the footer region height is non-zero).
    - The footer section content is `[Rendro.page_number(...)]` only (no balance text in footer).
    - The smoke render in `<verify>` returns `{:ok, _pdf}` (a single-page statement renders).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Statement exposes the three-rung API consistent with Invoice; footer reserves non-zero height and carries the PAGE primitive; a single-page statement renders.</done>
</task>

<task type="auto">
  <name>Task 2: validate_data!/1 (errors-as-product) + Decimal balance fold (STMT-01/D-05/D-06/D-08)</name>
  <files>lib/rendro/recipes/statement.ex</files>
  <read_first>
    - lib/rendro/recipes/statement.ex (the skeleton from Task 1 — add validation + fold to the same module)
    - lib/rendro/recipes/branded_invoice.ex (validate_data!/1 raise-with-guidance idiom at ~195-214 — the exact ArgumentError pattern to mirror)
    - lib/rendro/error.ex (Rendro.Error is a plain defstruct, NOT a defexception — Pitfall 6: do NOT `raise Rendro.Error`; use ArgumentError at this boundary)
    - .planning/phases/74-statement-recipe/74-CONTEXT.md (D-05, D-06, D-08)
    - .planning/phases/74-statement-recipe/74-RESEARCH.md (Code Examples §"D-08 validation"; Pitfall 4 Float, Pitfall 5 Decimal.equal?, Pitfall 6 not a defexception; Open Q4 summary shape)
  </read_first>
  <action>
    Add to `Rendro.Recipes.Statement` a private `validate_data!/1` mirroring `BrandedInvoice.validate_data!/1` — raise `ArgumentError` (NOT `Rendro.Error`, which is not a defexception — Pitfall 6) with what/where/why/next messages for: missing any required top-level key (`period`, `account`, `opening_balance`, `lines`); `opening_balance` not a `%Decimal{}` (with a specific Float branch: "money must be a Decimal, e.g. Decimal.new(...)"); any line not matching `%{date: %Date{}, description: binary, amount: %Decimal{}}`; a line amount that is a `Float` (instructive Float-specific message); a caller-supplied per-line `:balance` key (rejected per D-06 so callers don't think it is honored); and a malformed `period` (not `%{from: %Date{}, to: %Date{}}`). Call `validate_data!(data)` first thing in both `document/2` and `sections/2` (mirror BrandedInvoice).

    Add a private `fold_balance/1` that runs `Enum.map_reduce(lines, opening_balance, fn %{amount: amt} = line, bal -> nb = Decimal.add(bal, amt); {Map.put(line, :balance, nb), nb} end)` returning `{rows_with_balance, closing_balance}` — exact, signed (D-05/D-06). When the caller supplies an optional `closing_balance` or `summary`, validate them against the derived values using `Decimal.equal?/2` (NOT `==` — Pitfall 5) and raise an instructive ArgumentError on mismatch; derive them when absent. `summary` shape is Claude's discretion (recommend `%{total_debits:, total_credits:, line_count:, closing_balance:}` per Open Q4). Wire `fold_balance/1` into `body_section/2` so the placeholder table shows signed Amount + running Balance columns formatted via `Rendro.Format`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix run -e 'try do (Rendro.Recipes.Statement.document(%{period: %{from: ~D[2026-05-01], to: ~D[2026-05-31]}, account: %{name: "A"}, opening_balance: Decimal.new("0"), lines: [%{date: ~D[2026-05-02], description: "X", amount: 1.10}]}); raise "should have raised" rescue ArgumentError -> :ok end'</automated>
  </verify>
  <acceptance_criteria>
    - `validate_data!/1` raises `ArgumentError` (not `Rendro.Error`) for: missing required key, non-Decimal `opening_balance`, Float line amount, line missing `date`/`description`/`amount`, caller-supplied per-line `:balance`, and malformed `period`.
    - The Float message names Decimal explicitly (so the caller knows the fix).
    - `fold_balance/1` uses `Decimal.add/2` and returns rows annotated with `:balance` plus the closing balance; optional caller `closing_balance`/`summary` are validated with `Decimal.equal?/2`.
    - The `<verify>` command rescues an `ArgumentError` for a Float amount (exits 0).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Malformed statement data raises instructive ArgumentErrors at the recipe boundary; the running balance is an exact signed Decimal fold; caller-supplied balances are rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller data map → Rendro.Recipes.Statement | The ONLY untrusted input boundary in the phase (D-08). Crosses into validate_data!/1. |
| recipe → engine (Rendro.render) | In-process, trusted; recipe emits builder structs only. |

Pure Elixir library: no network, no auth, no I/O, no persistence, no frontend.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-07 | Tampering | Malformed / non-Decimal / Float amounts entering the document and corrupting financial output | mitigate | `validate_data!/1` rejects missing keys, non-Decimal/Float amounts, malformed period, and caller-supplied `:balance` with instructive ArgumentErrors at the single input boundary (D-08); Task 2 acceptance exercises each branch. |
| T-74-08 | Repudiation | Float drift undermining audit trust of balances | mitigate | Running balance is an exact `Decimal.add/2` fold (no Float); Float amounts rejected before they reach the fold. |
| T-74-09 | Information Disclosure | Caller text (descriptions, account name) reaching the PDF text stream | accept | Text is treated as literal glyphs by the engine; only the curated `{{page_number}}`/`{{total_pages}}` tokens are substituted (`replace_page_numbers/3`). No arbitrary substitution surface. Low risk for a local document library. |
| T-74-10 | Denial of Service | Pathologically large `lines` list driving unbounded assembly work | accept | Assembly is O(lines), linear; the per-page pagination DoS surface (and conservative margin) is addressed in plan 74-04. No amplification introduced here. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- A single-page statement renders via `Rendro.render/1` → `{:ok, _pdf}`.
- A Float line amount raises `ArgumentError` from `validate_data!/1`.
- `page_template/1` footer region height is `> 0`.
</verification>

<success_criteria>
- Three-rung `document/2` / `page_template/1` / `sections/2` consistent with `Invoice` (STMT-03).
- Footer carries ONLY the PAGE primitive at a non-zero reserved height (STMT-04/D-03).
- `validate_data!/1` is errors-as-product (D-08); the Decimal fold is exact and signed (D-05/D-06).
- A single-page statement renders end-to-end (STMT-01).
</success_criteria>

<output>
Create `.planning/phases/74-statement-recipe/74-03-SUMMARY.md` when done.
</output>
