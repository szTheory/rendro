---
phase: 20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/table.ex
  - lib/rendro.ex
  - lib/rendro/pipeline/compose.ex
  - lib/rendro/pipeline/measure.ex
  - lib/rendro/pipeline/paginate.ex
  - lib/rendro/pdf/writer.ex
  - lib/rendro/error.ex
  - test/rendro_builders_test.exs
  - test/rendro/pipeline/measure_test.exs
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/pdf/writer_test.exs
  - test/rendro/flow_test.exs
autonomous: true
requirements: [LAY-10]

must_haves:
  truths:
    - "Tables resolve column geometry from explicit authored rules against the enclosing block or body-region width every time."
    - "Measured table row and header heights come from actual measured cell blocks, not fixed `14.4` shortcuts."
    - "Multi-page tables repeat headers, keep rows atomic, and fail truthfully when one row cannot fit on a fresh page."
  artifacts:
    - path: "lib/rendro/table.ex"
      provides: "Public explicit column-rule and row-split contract for tables"
      min_lines: 25
    - path: "lib/rendro/pipeline/measure.ex"
      provides: "Deterministic table geometry resolution and measured row/header heights"
      min_lines: 80
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Measured-row pagination, repeated headers, and impossible-row overflow details"
      min_lines: 90
    - path: "lib/rendro/pdf/writer.ex"
      provides: "Writer consumption of resolved table coordinates instead of fixed cell offsets"
      min_lines: 35
  key_links:
    - from: "lib/rendro/table.ex"
      to: "lib/rendro/pipeline/measure.ex"
      via: "authored column rules and explicit atomic split policy"
      pattern: "columns|split_policy|atomic"
    - from: "lib/rendro/pipeline/measure.ex"
      to: "lib/rendro/pipeline/paginate.ex"
      via: "measured column widths, row heights, and header height"
      pattern: "column_widths|row_height|header_height"
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "lib/rendro/pdf/writer.ex"
      via: "stacked table-cell coordinates and continuation header blocks"
      pattern: "stack_table_cells|rows|header"
---

<objective>
Replace the demo-grade table constants with a real deterministic table engine inside the existing `compose -> measure -> paginate -> render` pipeline.

Purpose: satisfy the Phase 20 geometry and pagination requirement without adding heuristic auto-layout, row fragmentation, or a second rendering path.
Output: explicit table column-rule contract, measured row/header geometry, atomic-row continuation behavior, writer-aligned cell stacking, and stage-level proofs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-table-layout-maturity/20-CONTEXT.md
@.planning/phases/20-table-layout-maturity/20-RESEARCH.md
@.planning/phases/20-table-layout-maturity/20-PATTERNS.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-02-SUMMARY.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-03-SUMMARY.md
@.planning/phases/18-layout-contract-and-page-template-model/18-03-SUMMARY.md

<interfaces>
From `lib/rendro/table.ex`:
```elixir
defstruct [
  :rows,
  header: nil,
  width: :fill,
  border: true
]
```

From `lib/rendro/pipeline/measure.ex`:
```elixir
defp measure_block(%Rendro.Block{content: %Rendro.Table{} = table, width: nil} = block, font) do
  row_height = 14.4
  col_width = 100
  header_h = if table.header, do: row_height, else: 0
  rows_h = length(table.rows) * row_height
  height = header_h + rows_h
```

From `lib/rendro/pipeline/paginate.ex`:
```elixir
defp split_table(%Rendro.Table{rows: rows, header: header} = table, available_h) do
  row_height = 14.4
  header_h = if header, do: row_height, else: 0
  fit_count = floor((available_h - header_h) / row_height)
```

From `lib/rendro/pdf/writer.ex`:
```elixir
defp render_block(%Rendro.Block{content: %Rendro.Table{} = table} = block, page, font) do
  header_ops =
    if table.header do
      Enum.map(table.header, &render_block(&1, page, font, block.x, block.y))
    else
      []
    end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the authored table geometry contract and measure real table dimensions</name>
  <files>lib/rendro/table.ex, lib/rendro.ex, lib/rendro/pipeline/compose.ex, lib/rendro/pipeline/measure.ex, test/rendro_builders_test.exs, test/rendro/pipeline/measure_test.exs</files>
  <read_first>
    - lib/rendro/table.ex
    - lib/rendro.ex
    - lib/rendro/pipeline/compose.ex
    - lib/rendro/pipeline/measure.ex
    - test/rendro_builders_test.exs
    - test/rendro/pipeline/measure_test.exs
    - .planning/phases/20-table-layout-maturity/20-CONTEXT.md
    - .planning/phases/20-table-layout-maturity/20-RESEARCH.md
  </read_first>
  <action>
    Narrow `%Rendro.Table{}` to an explicit authored column-rule contract per D-01, D-02, and D-03. Add a public field for deterministic column rules such as fixed widths and proportional shares resolved against the enclosing table width, plus an explicit row split policy that defaults to atomic behavior per D-04. Keep this surface small and product-facing; do not add auto-sizing hints, styling DSL hooks, or continuation-chrome flags per D-09, D-12, and the deferred items list. Update `Rendro.table/2` and `Compose` only as needed so authored column rules normalize cleanly while cell coercion stays in Compose and geometry stays out of Compose.

    Replace the current `row_height = 14.4` / `col_width = 100` path in `Measure` with deterministic table measurement. Resolve the table width from `block.width` when present, otherwise from the authored body-region width or fixed-page bounds, then resolve the explicit column rules within that width per D-03. Measure each cell block under its resolved column width, derive row heights from the max measured cell height in each row, derive header height from the measured header row, and persist render-ready geometry on the measured table so downstream stages can reuse one source of truth. Preserve the Phase 19 pattern that measured text stays measured once; do not re-measure strings later in paginate or writer. Add direct tests proving: authored fixed-width/share rules resolve deterministically; a cell with wrapped measured text increases row height while sibling cells share the same row height; table width comes from enclosing geometry rather than a `%Rendro.Table{width: ...}` field; and builder tests accept the new explicit column surface while still rejecting unknown keys.
  </action>
  <acceptance_criteria>
    - `lib/rendro/table.ex` contains the new explicit column-rule surface and an atomic row-split default per D-01, D-02, and D-04.
    - `lib/rendro/pipeline/measure.ex` no longer contains `row_height = 14.4` or `col_width = 100` in the main table-measure path.
    - `test/rendro/pipeline/measure_test.exs` contains a test proving deterministic authored column-rule resolution.
    - `test/rendro/pipeline/measure_test.exs` contains a test proving measured row height follows the tallest cell in the row.
    - `test/rendro_builders_test.exs` contains a table-builder assertion for the new explicit column-rule surface.
    - `mix test test/rendro_builders_test.exs test/rendro/pipeline/measure_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro_builders_test.exs test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>The table surface and measurement stage now produce one deterministic geometry contract that later pagination and render work can consume unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Paginate measured rows atomically, repeat headers, and stack writer-ready coordinates</name>
  <files>lib/rendro/pipeline/paginate.ex, lib/rendro/pdf/writer.ex, lib/rendro/error.ex, test/rendro/pipeline/paginate_test.exs, test/rendro/pdf/writer_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - lib/rendro/pdf/writer.ex
    - lib/rendro/error.ex
    - test/rendro/pipeline/paginate_test.exs
    - test/rendro/pdf/writer_test.exs
    - test/rendro/flow_test.exs
    - lib/rendro/pipeline/measure.ex
    - .planning/phases/20-table-layout-maturity/20-CONTEXT.md
    - .planning/phases/20-table-layout-maturity/20-RESEARCH.md
  </read_first>
  <action>
    Replace the current row-count split logic in `Paginate` with measured-row accumulation. Keep the existing table split seam inside `paginate_block/5`, but make it consume measured header height, measured row heights, and resolved column widths from Task 1. Honor D-04 through D-08 exactly: rows are atomic, a continuation page repeats the measured header automatically, and header repetition stays body-region-aware under the Phase 18 template model. When the current page cannot fit any more measured rows, create a continuation `%Rendro.Table{}` block carrying the remaining measured rows and the same header; when a single measured row plus repeated header cannot fit on an empty page/body region, return `%Rendro.Error{stage: :paginate, reason: :content_overflow}` with row-specific details rather than splitting, clipping, or shrinking per D-05 and D-06. Include details such as `row_index`, `row_height`, `header_height`, `column_widths`, `page_index`, `region`, and `overflow_source`; reuse the existing paginate error family rather than inventing a table-only error family.

    Update the table stacking/writer path so render consumes finalized geometry instead of fixed offsets. `stack_table_cells/1` should position header and row cells using resolved per-column x offsets and measured per-row y offsets, and `Writer` should continue rendering only already-positioned child blocks. Do not add borders, shading, styling chrome, continuation labels, or any render-time layout recomputation per D-09, D-10, and D-12. Extend tests so they prove: multi-page flow tables repeat headers once per page; a wrapped/tall cell causes deterministic continuation based on measured height; an impossible single row returns typed overflow metadata; and writer output includes the repeated header text and final row text without relying on the retired fixed `100`-unit cell grid.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/paginate.ex` no longer uses `floor((available_h - header_h) / row_height)` for table continuation.
    - `lib/rendro/pipeline/paginate.ex` contains row-specific overflow detail keys for impossible fresh-page fits.
    - `lib/rendro/pipeline/paginate_test.exs` contains a test proving measured-row continuation with repeated headers.
    - `lib/rendro/pipeline/paginate_test.exs` contains a test asserting typed impossible-row overflow details.
    - `lib/rendro/pdf/writer_test.exs` contains a test proving the writer consumes stacked table coordinates instead of a fixed-width grid assumption.
    - `test/rendro/flow_test.exs` contains an end-to-end multi-page table render proof with repeated headers.
    - `mix test test/rendro/pipeline/paginate_test.exs test/rendro/pdf/writer_test.exs test/rendro/flow_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/pdf/writer_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>Measured table geometry now paginates and renders deterministically across pages without hidden row fragmentation or unsupported table chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public table authoring surface | Column rules and split policy become contract-level behavior that callers will automate against. |
| Measure to paginate | Pagination must consume measured row/header geometry exactly once, without fallback heuristics. |
| Paginate to writer | Writer must render stacked coordinates only, not make new layout decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `lib/rendro/table.ex` public API | mitigate | Expose only explicit authored column rules and atomic split policy per D-01 through D-04; do not retain heuristic width or styling affordances in this core plan. |
| T-20-02 | Repudiation | `lib/rendro/pipeline/measure.ex` and `lib/rendro/pipeline/paginate.ex` | mitigate | Persist measured column widths and row/header heights once, then prove the same geometry drives continuation and overflow tests. |
| T-20-03 | Denial of service | table continuation logic | mitigate | Fail impossible fresh-page rows through typed `:content_overflow` instead of recursive retries, clipping, or fragmentation per D-05 and D-06. |
| T-20-04 | Information disclosure | `lib/rendro/error.ex` overflow details | accept | Row-specific detail keys are necessary product diagnostics and do not expose sensitive content beyond geometry metadata. |
</threat_model>

<verification>
Run `mix test test/rendro_builders_test.exs test/rendro/pipeline/measure_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/pdf/writer_test.exs test/rendro/flow_test.exs` to prove the authored table contract, measured geometry, atomic-row pagination, repeated headers, and writer consumption land together.
</verification>

<success_criteria>
- Explicit authored column rules, not fixed constants or auto-sizing heuristics, determine table geometry.
- Table header and row heights come from measured cell content, and pagination repeats headers while keeping rows atomic.
- Impossible single-row fits fail through the existing typed paginate overflow surface with actionable row metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/20-table-layout-maturity/20-01-SUMMARY.md`
</output>
