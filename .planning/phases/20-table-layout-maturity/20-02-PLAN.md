---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/rendro/table.ex
  - lib/rendro.ex
  - lib/rendro/recipes.ex
  - lib/rendro/adapters/accrue.ex
  - README.md
  - guides/integrations.md
  - test/rendro_builders_test.exs
  - test/rendro/flow_test.exs
  - test/rendro/adapters/accrue_test.exs
  - test/docs_contract/readme_doctest_test.exs
  - test/docs_contract/integrations_contract_test.exs
autonomous: true
requirements: [LAY-10]

must_haves:
  truths:
    - "Public table builders and docs teach only the narrow supported Phase 20 contract: explicit column rules, repeated headers, atomic rows, and typed overflow on impossible rows."
    - "The public `%Rendro.Table{}` surface no longer implies unsupported width or border behavior."
    - "Recipes and adapters demonstrate serious business-document tables through the same single engine contract used by core flow rendering."
  artifacts:
    - path: "README.md"
      provides: "Truthful Phase 20 public authoring guidance and exclusions"
      min_lines: 25
    - path: "guides/integrations.md"
      provides: "Adapter guidance that treats table semantics as core-library behavior"
      min_lines: 20
    - path: "lib/rendro/recipes.ex"
      provides: "Canonical invoice recipe using explicit column rules"
      min_lines: 20
    - path: "test/docs_contract/integrations_contract_test.exs"
      provides: "Executable docs proof for the supported table contract"
      min_lines: 20
  key_links:
    - from: "lib/rendro/recipes.ex"
      to: "README.md"
      via: "shared explicit-column invoice/report examples"
      pattern: "Rendro.table|columns"
    - from: "README.md"
      to: "test/docs_contract/readme_doctest_test.exs"
      via: "docs-contract compile/eval fences"
      pattern: "docs-contract"
    - from: "guides/integrations.md"
      to: "test/docs_contract/integrations_contract_test.exs"
      via: "executable Accrue integration fences"
      pattern: "integrations-accrue"
---

<objective>
Finish Phase 20 by narrowing the public table contract, migrating examples and recipes to that contract, and proving the docs do not overstate unsupported table behavior.

Purpose: ship a trustworthy authoring surface for invoices and reports instead of leaving demo-era table affordances in public structs and examples.
Output: truthful `%Rendro.Table{}` surface, updated builders/recipes/adapters/docs, and executable regression and docs-contract proof for the supported table contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-table-layout-maturity/20-CONTEXT.md
@.planning/phases/20-table-layout-maturity/20-RESEARCH.md
@.planning/phases/20-table-layout-maturity/20-PATTERNS.md
@.planning/phases/20-table-layout-maturity/20-01-PLAN.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-03-SUMMARY.md

<interfaces>
From `lib/rendro/table.ex`:
```elixir
@type t :: %__MODULE__{
        rows: [row()],
        header: row() | nil,
        width: number() | :fill,
        border: boolean()
      }
```

From `lib/rendro/recipes.ex`:
```elixir
table = Rendro.table(table_rows, header: ["Item", "Qty", "Price"])
```

From `lib/rendro/adapters/accrue.ex`:
```elixir
table =
  Rendro.table(rows,
    header: ["Description", "Qty", "Unit", "Subtotal"]
  )
```

From `test/docs_contract/integrations_contract_test.exs`:
```elixir
assert Enum.map(fences, & &1.id) == [
         "integrations-threadline-happy-path",
         "integrations-mailglass-swoosh",
         "integrations-mailglass-message",
         "integrations-accrue-verification"
       ]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove misleading table affordances and migrate public builders and recipes to the explicit contract</name>
  <files>lib/rendro/table.ex, lib/rendro.ex, lib/rendro/recipes.ex, lib/rendro/adapters/accrue.ex, test/rendro_builders_test.exs, test/rendro/adapters/accrue_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - lib/rendro/table.ex
    - lib/rendro.ex
    - lib/rendro/recipes.ex
    - lib/rendro/adapters/accrue.ex
    - test/rendro_builders_test.exs
    - test/rendro/adapters/accrue_test.exs
    - test/rendro/flow_test.exs
    - .planning/phases/20-table-layout-maturity/20-CONTEXT.md
  </read_first>
  <action>
    Make the public surface truthful per D-11 and D-13. Prefer hard removal of `%Rendro.Table{width, border}` rather than pseudo-deprecation because Elixir structs cannot emit reliable field-level deprecation warnings and retaining no-op fields would continue to advertise unsupported behavior. If a compatibility shim is necessary in `Rendro.table/2`, keep it narrow and migration-oriented: reject or warn on legacy `:width` / `:border` attrs at the builder boundary while steering callers to block width plus explicit column rules, but do not preserve those fields as live engine input. Update builder coverage accordingly.

    Move the first-party invoice surfaces onto the new authored contract from Plan 01. `Rendro.Recipes.invoice/1` and `Rendro.Adapters.Accrue.recipe/1` should declare explicit table columns appropriate for invoice/report layouts and rely on repeated headers plus atomic rows, not on any removed width/border behavior. Keep adapters optional and consumer-only; do not push new layout semantics into adapter-specific modules beyond choosing the truthful core table args. Extend flow and adapter tests so they assert the produced documents include the new column-rule surface, continue to render to PDF successfully, and do not retain `width` or `border` fields in the public table contract.
  </action>
  <acceptance_criteria>
    - `lib/rendro/table.ex` no longer advertises live `width` or `border` table fields as supported behavior.
    - `test/rendro_builders_test.exs` contains a regression covering the removal or builder-level rejection/migration path for `:width` and `:border`.
    - `lib/rendro/recipes.ex` and `lib/rendro/adapters/accrue.ex` each declare explicit table columns using the new authored contract from Plan 01.
    - `test/rendro/adapters/accrue_test.exs` contains an assertion that the generated document reflects the new table contract.
    - `test/rendro/flow_test.exs` contains a public render proof for the narrowed table surface.
    - `mix test test/rendro_builders_test.exs test/rendro/adapters/accrue_test.exs test/rendro/flow_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro_builders_test.exs test/rendro/adapters/accrue_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>The public builder and first-party recipes now expose only the supported Phase 20 table semantics and no longer imply unsupported width or border behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Publish truthful Phase 20 docs and executable contract proofs</name>
  <files>README.md, guides/integrations.md, test/docs_contract/readme_doctest_test.exs, test/docs_contract/integrations_contract_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - README.md
    - guides/integrations.md
    - test/docs_contract/readme_doctest_test.exs
    - test/docs_contract/integrations_contract_test.exs
    - test/rendro/flow_test.exs
    - .planning/PROJECT.md
    - .planning/phases/20-table-layout-maturity/20-CONTEXT.md
    - .planning/phases/20-table-layout-maturity/20-RESEARCH.md
  </read_first>
  <action>
    Rewrite the README and integration guidance so the public story matches Phase 20 exactly. Add one core table example that shows explicit authored column rules, a repeated header across continuation pages, and the documented overflow posture for an impossible single row. State the exclusions explicitly: no auto-sizing, no row fragmentation, no border/styling DSL, and no automatic continuation labels or chrome per D-09, D-12, and the deferred ideas list. Keep the docs centered on the public builders and recipes rather than internal pipeline structs.

    Update the docs-contract proof files so the examples stay executable and narrow. Extend the README docs-contract fence list for any new compile/eval snippets and keep the integration guide fence list aligned if the Accrue example changes shape. Add or refresh any targeted `flow_test` assertions needed to prove the public docs examples correspond to real behavior. Verify through the repo’s supported docs path: run the direct docs-contract tests and `mix run scripts/verify_docs.exs`. Do not publish claims about measured content auto-fit, CSS-like tables, row fragmentation, borders, shading, or continuation chrome unless the code added in Plan 01 truly implements them.
  </action>
  <acceptance_criteria>
    - `README.md` contains an explicit table example using authored column rules.
    - `README.md` contains explicit boundary language covering lack of auto-sizing, row fragmentation, borders/styling, and continuation chrome.
    - `guides/integrations.md` states that table layout semantics remain core-library behavior and reflects the explicit-column Accrue recipe.
    - `test/docs_contract/readme_doctest_test.exs` or its fence expectations are updated for the new README examples.
    - `test/docs_contract/integrations_contract_test.exs` continues to evaluate the curated integration fences successfully after the Accrue example changes.
    - `mix test test/docs_contract/readme_doctest_test.exs test/docs_contract/integrations_contract_test.exs test/rendro/flow_test.exs` exits 0.
    - `mix run scripts/verify_docs.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/readme_doctest_test.exs test/docs_contract/integrations_contract_test.exs test/rendro/flow_test.exs && mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>The README, guides, and executable docs proof describe the narrow supported Phase 20 table contract truthfully and nothing broader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs and builders | README, guides, and builders are product contracts and must not keep demo-era promises alive. |
| Core to optional adapters | Recipes and adapters may consume the new table contract but must not redefine layout semantics outside core. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Repudiation | `README.md` and `guides/integrations.md` | mitigate | Publish explicit supported semantics plus explicit exclusions for auto-sizing, row fragmentation, borders/styling, and continuation chrome, then verify them through docs-contract tests. |
| T-20-06 | Tampering | `%Rendro.Table{}` public surface | mitigate | Remove or builder-reject `width` and `border` per D-11 instead of keeping misleading no-op fields alive. |
| T-20-07 | Elevation of privilege | optional adapters | mitigate | Keep `Rendro.Adapters.Accrue` as a consumer of core table args only; do not add adapter-local layout behavior or a second table contract. |
| T-20-08 | Information disclosure | docs examples | accept | Examples intentionally expose only narrow invoice/report data and supported table semantics; no sensitive runtime state is surfaced. |
</threat_model>

<verification>
Run `mix test test/rendro_builders_test.exs test/rendro/adapters/accrue_test.exs test/rendro/flow_test.exs test/docs_contract/readme_doctest_test.exs test/docs_contract/integrations_contract_test.exs` and `mix run scripts/verify_docs.exs` to prove the public surface, recipes, and docs-contract guidance all match the supported Phase 20 contract.
</verification>

<success_criteria>
- `%Rendro.Table{}` and `Rendro.table/2` no longer imply unsupported width or border behavior.
- First-party recipes, Accrue integration examples, and public docs teach explicit columns, repeated headers, atomic rows, and typed impossible-row overflow.
- Regression and docs-contract coverage prove the narrow supported table contract and explicitly exclude unsupported styling and fragmentation promises.
</success_criteria>

<output>
After completion, create `.planning/phases/20-table-layout-maturity/20-02-SUMMARY.md`
</output>
