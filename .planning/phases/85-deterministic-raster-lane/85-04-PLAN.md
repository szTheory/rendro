---
phase: 85-deterministic-raster-lane
plan: 04
type: execute
wave: 3
depends_on:
  - 85-02
  - 85-03
files_modified:
  - .github/workflows/ci.yml
  - priv/guardrails/required_status_checks.json
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - RAST-02
  - RAST-03

must_haves:
  truths:
    - "CI has a raster-advisory job with no needs: dependency (graph-disconnected), continue-on-error: true, installing pdfium-cli v0.11.0 with sha256sum --check"
    - "raster-advisory is NOT in required_contexts in priv/guardrails/required_status_checks.json"
    - "raster-advisory IS in advisory_contexts in priv/guardrails/required_status_checks.json"
    - "scripts/verify_docs.exs contains a 'Raster claims lane' entry referencing test/docs_contract/raster_claims_test.exs"
    - "A pdfium-cli download failure in raster-advisory cannot block the four required engine lanes (test, signing-live-proof, release-proof, long-lived-live-proof)"
    - "mix test (full suite) exits 0 — all raster_claims_test.exs tests now pass"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "raster-advisory job: graph-disconnected, continue-on-error: true, pdfium-cli v0.11.0 sha256-pinned install, mix test --include raster_snapshot"
      contains: "raster-advisory"
    - path: "priv/guardrails/required_status_checks.json"
      provides: "raster-advisory entry in advisory_contexts (NOT in required_contexts)"
      contains: "raster-advisory"
    - path: "scripts/verify_docs.exs"
      provides: "Raster claims lane entry in the lanes list"
      contains: "Raster claims lane"
  key_links:
    - from: ".github/workflows/ci.yml raster-advisory job"
      to: "priv/guardrails/required_status_checks.json advisory_contexts"
      via: "advisory_contexts entry name matches CI job name"
      pattern: "raster-advisory"
    - from: "scripts/verify_docs.exs Raster claims lane"
      to: "test/docs_contract/raster_claims_test.exs"
      via: "lane entry ['test', 'test/docs_contract/raster_claims_test.exs']"
      pattern: "raster_claims_test"
---

<objective>
Wire the advisory CI lane and close all remaining RED docs-contract assertions. Adds the `raster-advisory` GitHub Actions job (graph-disconnected, `continue-on-error: true`, pdfium-cli v0.11.0 with sha256sum verification, runs `mix test --include raster_snapshot`). Registers the lane in `priv/guardrails/required_status_checks.json` under `advisory_contexts` only. Adds the `"Raster claims lane"` entry to `scripts/verify_docs.exs`. After these changes all six `raster_claims_test.exs` tests pass.

Purpose: RAST-02 (advisory lane never gates engine merges) and RAST-03 (guardrails isolation verified by docs-contract).
Output: Three files modified; all `raster_claims_test.exs` tests green; `mix test` passes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-deterministic-raster-lane/85-RESEARCH.md
@.planning/phases/85-deterministic-raster-lane/85-PATTERNS.md
@.planning/phases/85-deterministic-raster-lane/85-VALIDATION.md
@.planning/phases/85-deterministic-raster-lane/85-01-SUMMARY.md
@.planning/phases/85-deterministic-raster-lane/85-03-SUMMARY.md

<interfaces>
From .github/workflows/ci.yml (example-phoenix job — structural template to follow):
```yaml
example-phoenix:
  runs-on: ubuntu-latest
  # no `needs:` -> graph-disconnected (D-09); no continue-on-error (D-10)
  steps:
    - name: Checkout
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    - name: Setup Beam
      uses: erlef/setup-beam@v1
      with:
        otp-version: '28'
        elixir-version: '1.19.5'
    - name: Run example app tests
      working-directory: examples/phoenix_example
      run: |
        mix deps.get
        mix test
```

New job shape (from RESEARCH.md Pattern 3 + PATTERNS.md ci.yml section):
```yaml
raster-advisory:
  runs-on: ubuntu-latest
  continue-on-error: true
  # no `needs:` -> graph-disconnected; not in required_contexts
  steps:
    - name: Checkout
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    - name: Setup Beam
      uses: erlef/setup-beam@v1
      with:
        otp-version: '28'
        elixir-version: '1.19.5'
    - name: Install pdfium-cli (pinned)
      run: |
        EXPECTED_SHA256="b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a"
        curl -fsSL -o pdfium-cli \
          "https://github.com/klippa-app/pdfium-cli/releases/download/v0.11.0/pdfium-webassembly-linux-amd64"
        echo "${EXPECTED_SHA256}  pdfium-cli" | sha256sum --check
        chmod +x pdfium-cli
        sudo mv pdfium-cli /usr/local/bin/pdfium-cli
    - name: Install Elixir Dependencies
      run: mix deps.get
    - name: Run Raster Snapshot Tests
      env:
        MIX_RASTER_BLESS: "false"
      run: mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs
```

From priv/guardrails/required_status_checks.json (current advisory_contexts — extend, do not replace):
```json
"advisory_contexts": [
  { "name": "viewer-evidence-live-proof", ... },
  { "name": "example-phoenix", ... }
]
```
New entry to append:
```json
{
  "name": "raster-advisory",
  "semantic_class": "raster_snapshot",
  "ci_job": "raster-advisory",
  "command": "mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs",
  "notes": "Phase 85 deterministic raster lane; not required on main. Download failure must never block engine merges."
}
```

From scripts/verify_docs.exs (current lanes list — append one entry):
```elixir
{"Path claims lane", ["test", "test/docs_contract/path_claims_test.exs"]}
# append after this:
{"Raster claims lane", ["test", "test/docs_contract/raster_claims_test.exs"]}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add raster-advisory job to .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read the full file to understand the job ordering and YAML indentation; identify where to append the new job (after the last existing job, or between example-phoenix and viewer-evidence-live-proof to keep advisory jobs together)
    - .planning/phases/85-deterministic-raster-lane/85-PATTERNS.md — ci.yml section: "example-phoenix" structural template (no needs:), "viewer-evidence-live-proof" pdfium-cli install step analog, "new job shape for raster-advisory" paragraph
    - .planning/phases/85-deterministic-raster-lane/85-RESEARCH.md — Pattern 3 (advisory CI lane) including the continue-on-error rationale and the full SHA256 value for v0.11.0; also the note distinguishing D-10 (example-phoenix deliberately has no continue-on-error) vs raster-advisory (explicitly wants continue-on-error: true)
    - priv/pdfium_pin.json — read to confirm the sha256 value (b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a) matches what goes in the CI step
  </read_first>
  <action>
    Append a new top-level job `raster-advisory` to .github/workflows/ci.yml after the `example-phoenix` job definition. The job must:

    1. Have `runs-on: ubuntu-latest`
    2. Have `continue-on-error: true` (unlike example-phoenix which deliberately omits this per D-10; raster-advisory is distinct — download failure should not create a red CI signal for contributors)
    3. Have NO `needs:` key (graph-disconnected; cannot block engine merges)
    4. Steps in order:
       a. Checkout: uses actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2 (same pinned SHA as all other jobs)
       b. Setup Beam: uses erlef/setup-beam@v1 with otp-version: '28' and elixir-version: '1.19.5' (matching all other jobs)
       c. "Install pdfium-cli (pinned)": run block that sets EXPECTED_SHA256="b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a", curls the v0.11.0 webassembly-linux-amd64 binary, runs `echo "${EXPECTED_SHA256}  pdfium-cli" | sha256sum --check`, chmods +x, and moves to /usr/local/bin/pdfium-cli
       d. "Install Elixir Dependencies": `mix deps.get`
       e. "Run Raster Snapshot Tests": env MIX_RASTER_BLESS: "false"; run `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs`

    Add a YAML comment on the job: `# no 'needs:' -> graph-disconnected; continue-on-error: true -> download failure cannot block engine merges (RAST-02)`

    Do NOT modify the existing viewer-evidence-live-proof pdfium-cli install step (it uses v0.10.3; the raster lane uses v0.11.0 — intentionally separate versions in separate jobs).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml').read()); print('YAML valid')" && grep -c "raster-advisory" .github/workflows/ci.yml</automated>
  </verify>
  <done>
    - YAML is syntactically valid (python3 yaml.safe_load exits 0)
    - grep confirms "raster-advisory" is present in ci.yml
    - grep confirms "continue-on-error: true" is present in the raster-advisory job (not in other jobs)
    - grep confirms no "needs:" key under raster-advisory
    - The sha256 value b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a is present in the install step
  </done>
</task>

<task type="auto">
  <name>Task 2: Register raster-advisory in guardrails JSON + add Raster claims lane to verify_docs.exs</name>
  <files>priv/guardrails/required_status_checks.json, scripts/verify_docs.exs</files>
  <read_first>
    - priv/guardrails/required_status_checks.json — read the full file; confirm the advisory_contexts array location and the exact shape of existing entries; confirm required_contexts does NOT include raster-advisory (it must not); verify JSON is valid after appending
    - scripts/verify_docs.exs — read the full file; confirm the exact format of the last lanes list entry (Path claims lane) so the new Raster claims lane is appended with matching comma placement and format
    - .planning/phases/85-deterministic-raster-lane/85-PATTERNS.md — "priv/guardrails/required_status_checks.json" section (new entry shape with name, semantic_class, ci_job, command, notes); "docs-contract test: matrix JSON assertion + verify_docs lane registration" section (exact string expected by raster_claims_test.exs test 6)
    - test/docs_contract/raster_claims_test.exs — read to confirm the exact string that test 6 asserts: ~s|{"Raster claims lane", ["test", "test/docs_contract/raster_claims_test.exs"]}|
  </read_first>
  <action>
    Edit priv/guardrails/required_status_checks.json: append a new entry to the advisory_contexts array (after the existing "example-phoenix" entry). The new entry must be valid JSON with fields: "name": "raster-advisory", "semantic_class": "raster_snapshot", "ci_job": "raster-advisory", "command": "mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs", "notes": "Phase 85 deterministic raster lane; not required on main. Download failure must never block engine merges." Do NOT add raster-advisory to required_contexts. Verify JSON is syntactically valid.

    Edit scripts/verify_docs.exs: in the lanes list, after the last entry `{"Path claims lane", ["test", "test/docs_contract/path_claims_test.exs"]}`, add a new entry on the next line: `{"Raster claims lane", ["test", "test/docs_contract/raster_claims_test.exs"]}`. Maintain the exact same formatting (2-space indentation, comma after the preceding entry's closing brace). The string must match exactly what raster_claims_test.exs test 6 asserts: ~s|{"Raster claims lane", ["test", "test/docs_contract/raster_claims_test.exs"]}|.
  </action>
  <verify>
    <automated>mix test test/docs_contract/raster_claims_test.exs</automated>
  </verify>
  <done>
    - mix test test/docs_contract/raster_claims_test.exs exits 0 (all 6 tests pass — RAST-01c, RAST-03a, RAST-03b partial, RAST-03c, RAST-03d, and verify_docs lane registration)
    - mix test (full suite) exits 0
    - python3 -c "import json; d=json.load(open('priv/guardrails/required_status_checks.json')); assert 'raster-advisory' not in d['required_contexts']; assert any(c['name']=='raster-advisory' for c in d['advisory_contexts']); print('OK')" exits 0
    - grep confirms "Raster claims lane" in scripts/verify_docs.exs
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions raster-advisory job → required engine lanes | Isolation enforced by: no `needs:` (graph-disconnected) + NOT in required_contexts; continue-on-error: true prevents red status from confusing contributors |
| pdfium-cli download (CI step) → binary execution | sha256sum --check validates the downloaded binary before chmod +x and move |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-07 | Tampering | pdfium-cli binary download in CI (supply chain) | mitigate | sha256sum --check in install step using pinned SHA b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a; WASM variant (sandboxed execution, no native syscalls beyond file I/O); priv/pdfium_pin.json records the pin for auditability |
| T-85-11 | Denial of Service | raster-advisory job blocking engine merges on download failure | mitigate | continue-on-error: true + no needs: + NOT in required_contexts; guardrails test (RAST-03a) verifies isolation in CI on every push |
| T-85-12 | Tampering | raster-advisory accidentally added to required_contexts | mitigate | raster_claims_test.exs test 3 (RAST-03a) explicitly asserts "raster-advisory" not in guardrails["required_contexts"]; this test runs in mix test on every PR |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No new packages; YAML + JSON config changes only |
</threat_model>

<verification>
After both tasks complete:
- `mix test test/docs_contract/raster_claims_test.exs` exits 0 (all 6 tests pass — covers RAST-01c, RAST-03a, RAST-03c, RAST-03d, and verify_docs lane registration)
- `mix test` (full suite) exits 0
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read()); print('OK')"` exits 0
- `python3 -c "import json; d=json.load(open('priv/guardrails/required_status_checks.json')); assert 'raster-advisory' not in d['required_contexts']; assert any(c['name']=='raster-advisory' for c in d['advisory_contexts']); print('OK')"` exits 0
- `grep -c "Raster claims lane" scripts/verify_docs.exs` returns 1
- `grep "continue-on-error" .github/workflows/ci.yml | grep -c "true"` returns at least 1 (raster-advisory job)
- `grep -A5 "raster-advisory:" .github/workflows/ci.yml | grep -c "needs:"` returns 0 (no needs: key)
</verification>

<success_criteria>
Plan 04 is complete when:
1. `raster-advisory` CI job exists with `continue-on-error: true`, no `needs:`, and pdfium-cli v0.11.0 sha256-pinned install
2. `raster-advisory` is in `advisory_contexts` and absent from `required_contexts` in guardrails JSON
3. All 6 `raster_claims_test.exs` tests pass (RAST-01c, RAST-03a, RAST-03c, RAST-03d, guardrails isolation, verify_docs registration)
4. `mix test` (full suite) exits 0
5. Phase 85 success criteria 1–4 are all verifiable by source assertions + test commands
</success_criteria>

<output>
Create `.planning/phases/85-deterministic-raster-lane/85-04-SUMMARY.md` when done.
</output>
