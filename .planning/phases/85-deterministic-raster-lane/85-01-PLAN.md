---
phase: 85-deterministic-raster-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rendro/adapters/pdfium_raster_snapshot_test.exs
  - test/rendro/adapters/pdfium_test.exs
  - test/docs_contract/raster_claims_test.exs
  - test/test_helper.exs
  - priv/raster_refs/.gitkeep
  - priv/pdfium_pin.json
autonomous: true
requirements:
  - RAST-01
  - RAST-02
  - RAST-03

must_haves:
  truths:
    - "mix test passes with the new test files present (tests are in RED/pending state but not crashing on compile)"
    - "raster_snapshot tag is excluded by default so `mix test` does not attempt pdfium-cli invocation"
    - "pdfium_pin.json exists with version and sha256 keys for v0.11.0"
    - "priv/raster_refs/ directory exists and is tracked by git"
    - "pdfium_test.exs contains a test asserting render/2 with nil executable returns {:error, {:missing_executable, 'pdfium-cli'}}"
    - "pdfium_raster_snapshot_test.exs contains tagged bless-guard test and hash-equality fast-path test stubs"
    - "raster_claims_test.exs contains docs-contract stubs for RAST-01c, RAST-03a, RAST-03c, RAST-03d"
  artifacts:
    - path: "test/rendro/adapters/pdfium_raster_snapshot_test.exs"
      provides: "Wave 0 snapshot harness stubs — raster_snapshot tag gating, bless guard, hash-equality fast path"
      contains: "@tag raster_snapshot: true"
    - path: "test/docs_contract/raster_claims_test.exs"
      provides: "Docs-contract stubs for pin file, guardrails isolation, GUI-viewer boundary, raster matrix section"
      contains: "RasterClaimsTest"
    - path: "priv/pdfium_pin.json"
      provides: "Version + sha256 pin for pdfium-webassembly-linux-amd64 v0.11.0"
      contains: "v0.11.0"
    - path: "priv/raster_refs/.gitkeep"
      provides: "Git-tracked placeholder for future blessed PNG hash refs"
  key_links:
    - from: "test/test_helper.exs"
      to: "test/rendro/adapters/pdfium_raster_snapshot_test.exs"
      via: "ExUnit.configure exclude: [raster_snapshot: true]"
      pattern: "raster_snapshot"
    - from: "test/rendro/adapters/pdfium_raster_snapshot_test.exs"
      to: "priv/raster_refs/"
      via: "File.read! ref path at runtime"
      pattern: "priv/raster_refs/"
---

<objective>
Wave 0 test scaffolding: create all test stubs in RED/compile-passing state before any implementation. Extends `pdfium_test.exs` with `render/2` unit tests, creates `pdfium_raster_snapshot_test.exs` with tag-excluded snapshot harness stubs, creates `raster_claims_test.exs` with docs-contract stubs, seeds `priv/pdfium_pin.json`, creates `priv/raster_refs/.gitkeep`, and registers the `raster_snapshot` ExUnit tag in `test_helper.exs`.

Purpose: Establish the Nyquist Wave 0 — all test infrastructure must exist before Plan 02 (render/2) and Plan 03 (schema sync) implement against it.
Output: Six files created/modified; `mix test` passes (stubs compile, snapshot tests are tag-excluded from default run, docs-contract stubs fail as expected until implementation).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-deterministic-raster-lane/85-RESEARCH.md
@.planning/phases/85-deterministic-raster-lane/85-PATTERNS.md
@.planning/phases/85-deterministic-raster-lane/85-VALIDATION.md

<interfaces>
From test/test_helper.exs (current — must extend, not replace):
```elixir
ExUnit.start()
ExUnit.configure(exclude: [live_pdf_tools: true, live_signing: true])
```

From test/rendro/adapters/pdfium_test.exs (pattern to extend with render/2 test):
```elixir
defmodule Rendro.Adapters.PdfiumTest do
  use ExUnit.Case, async: true
  alias Rendro.Adapters.Pdfium
  test "returns missing executable when pdfium-cli is absent" do
    Application.put_env(:rendro, :pdfium_cli_executable_finder, fn _ -> nil end)
    on_exit(fn -> Application.delete_env(:rendro, :pdfium_cli_executable_finder) end)
    assert {:error, {:missing_executable, "pdfium-cli"}} = Pdfium.info(...)
  end
end
```

From lib/rendro/adapters/pdfium.ex (render/2 will be added in Plan 02 — stubs reference it):
```elixir
@spec render(binary(), keyword()) :: {:ok, [binary()]} | {:error, term()}
def render(pdf_binary, opts \\ [])   # not yet implemented — Plan 02 adds it
```

From test/docs_contract/path_claims_test.exs (docs-contract test shape):
```elixir
defmodule Rendro.DocsContract.PathClaimsTest do
  use ExUnit.Case, async: true
  test "support matrix has ..." do
    matrix = File.read!("priv/support_matrix.json")
    assert matrix =~ ~s|"section_key"|
  end
  test "docs verification script includes the ... lane" do
    script = File.read!("scripts/verify_docs.exs")
    assert script =~ ~s|{"Label", ["test", "test/docs_contract/...exs"]}|
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend pdfium_test.exs + create pdfium_raster_snapshot_test.exs (Wave 0 stubs)</name>
  <files>test/rendro/adapters/pdfium_test.exs, test/rendro/adapters/pdfium_raster_snapshot_test.exs, test/test_helper.exs, priv/raster_refs/.gitkeep</files>
  <read_first>
    - test/rendro/adapters/pdfium_test.exs — existing test; append render/2 missing-executable test using same Application.put_env + on_exit pattern
    - test/test_helper.exs — existing ExUnit.configure line; must add raster_snapshot: true to the exclude list without disturbing existing entries
    - .planning/phases/85-deterministic-raster-lane/85-PATTERNS.md — Pattern Map section for pdfium_raster_snapshot_test.exs (Analog 1: pdfium_test.exs injectable finder; Analog 2: tag exclusion pattern)
    - .planning/phases/85-deterministic-raster-lane/85-RESEARCH.md — Pattern 2 (golden snapshot harness) and Pattern bless guard (GITHUB_ACTIONS check); also Pitfall 3 (bless-outside-CI guard)
  </read_first>
  <behavior>
    pdfium_test.exs additions (no tag — run in normal mix test):
    - Test: render/2 with nil finder returns {:error, {:missing_executable, "pdfium-cli"}}
      Input: Application.put_env(:rendro, :pdfium_cli_executable_finder, fn _ -> nil end); Pdfium.render(<<"PDF">>)
      Expected: {:error, {:missing_executable, "pdfium-cli"}}

    pdfium_raster_snapshot_test.exs (new file, async: false):
    - @tag raster_snapshot: true on every live-render test (excluded from default mix test)
    - Unit test (no tag): bless guard raises when MIX_RASTER_BLESS=true and GITHUB_ACTIONS != "true"
      Input: temporarily set env MIX_RASTER_BLESS=true, GITHUB_ACTIONS=nil, call assert_or_bless_stub()
      Expected: raises with message containing "must only run in the pinned CI container"
    - @tag raster_snapshot: true test: hash-equality fast path reads a ref file and compares — stub that skips gracefully when priv/raster_refs/invoice/page_1.sha256 does not exist
    - Module-level helper functions: assert_golden_hashes/2, bless_refs/2, assert_or_bless/2 stubs matching RESEARCH.md Pattern 2 shape (using :crypto.hash(:sha256, png_binary) and Base.encode16/2 with case: :lower)
  </behavior>
  <action>
    In test/test_helper.exs: change the ExUnit.configure line to add raster_snapshot: true alongside the existing live_pdf_tools and live_signing exclusions. The line must become ExUnit.configure(exclude: [live_pdf_tools: true, live_signing: true, raster_snapshot: true]). Do not touch any other line in the file.

    In test/rendro/adapters/pdfium_test.exs: append a second test after the existing one. Use the same Application.put_env + on_exit pattern. Call Pdfium.render(<<37, 80, 68, 70>>, []) (a minimal non-empty binary standing in for a PDF). Assert the result matches {:error, {:missing_executable, "pdfium-cli"}}. Note: render/2 does not exist yet in Plan 01 — this test will fail to compile until Plan 02 adds it. Mark with @tag :skip or wrap in a guard so Plan 01's mix test run still passes. Preferred approach: use @tag :skip so the test exists as a stub but does not execute. Add a comment: "# T-85-01: un-skip in Plan 02 after render/2 is implemented".

    Create test/rendro/adapters/pdfium_raster_snapshot_test.exs with module Rendro.Adapters.PdfiumRasterSnapshotTest. Use ExUnit.Case with async: false (writes to priv/raster_refs/ during bless). Include:
    - A unit test (no tag) for the bless guard: temporarily assign MIX_RASTER_BLESS env in process, set GITHUB_ACTIONS to nil, call a private assert_or_bless/2 function with dummy args, expect a RuntimeError with message matching "must only run in the pinned CI container". Use System.put_env/System.delete_env carefully; restore state in on_exit.
    - A @tag raster_snapshot: true test for the hash-equality fast path: reads priv/raster_refs/invoice/page_1.sha256; if the file does not exist, call IO.puts("Skipping: no ref hashes yet — bless in CI first") and succeed. If the file exists, compute expected hash via Base.encode16(:crypto.hash(:sha256, png_binary), case: :lower) and assert equality.
    - Private helpers: assert_golden_hashes/2 (takes fixture_name, [png_binary]), bless_refs/2 (takes fixture_name, [png_binary]), assert_or_bless/2 (routes based on MIX_RASTER_BLESS env + GITHUB_ACTIONS guard per RESEARCH.md code example). Use ref path pattern "priv/raster_refs/#{fixture_name}/page_#{page_num}.sha256".

    Create priv/raster_refs/.gitkeep as an empty file (so git tracks the directory).
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/pdfium_test.exs test/rendro/adapters/pdfium_raster_snapshot_test.exs</automated>
  </verify>
  <done>
    - test/test_helper.exs line 2 contains "raster_snapshot: true" in the exclude list
    - pdfium_raster_snapshot_test.exs compiles and the bless-guard unit test passes
    - pdfium_test.exs compiles (render/2 stub test is @tag :skip)
    - priv/raster_refs/.gitkeep exists
    - mix test (default, no --include) does not attempt pdfium-cli invocation (raster_snapshot tests excluded)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create raster_claims_test.exs stubs + priv/pdfium_pin.json</name>
  <files>test/docs_contract/raster_claims_test.exs, priv/pdfium_pin.json</files>
  <read_first>
    - test/docs_contract/path_claims_test.exs — exact shape for docs-contract test assertions using File.read! + assert =~ patterns; also the verify_docs lane registration assertion
    - test/docs_contract/viewer_evidence_claims_test.exs — guardrails assertion pattern using File.read! + JSON.decode! on priv/guardrails/required_status_checks.json; refute membership in required_contexts
    - .planning/phases/85-deterministic-raster-lane/85-PATTERNS.md — raster_claims_test.exs section (key assertions list, module name convention Rendro.DocsContract.RasterClaimsTest)
    - .planning/phases/85-deterministic-raster-lane/85-RESEARCH.md — Pitfall 6 (advisory lane must appear in advisory_contexts, not required_contexts); Pattern 4 (pdfium_pin.json shape)
    - priv/guardrails/required_status_checks.json — current advisory_contexts shape
  </read_first>
  <behavior>
    raster_claims_test.exs assertions (all must pass after Plans 02 and 03 implement; some will fail here in Plan 01 — that is correct Wave 0 RED state):
    - Test "support matrix has raster section with boundary declarations": matrix =~ ~s|"raster"|, matrix =~ ~s|"gui_viewer_equivalence"|, matrix =~ ~s|"unsupported"|
    - Test "pdfium_pin.json exists and has required keys": File.exists?("priv/pdfium_pin.json") is true, JSON-decoded map has "version" and "sha256" keys
    - Test "advisory lane is not in required_contexts": guardrails["required_contexts"] does not include "raster-advisory"
    - Test "advisory lane is in advisory_contexts": guardrails["advisory_contexts"] includes an entry with name "raster-advisory"
    - Test "GUI-viewer rows do not carry viewer_kind pdfium-render": matrix does not match ~r/"forms".*?"viewer_kind"\s*:\s*"pdfium-render"/s
    - Test "docs verification script includes the raster claims lane": script =~ ~s|{"Raster claims lane", ["test", "test/docs_contract/raster_claims_test.exs"]}|
  </behavior>
  <action>
    Create priv/pdfium_pin.json with content: {"version": "v0.11.0", "binary": "pdfium-webassembly-linux-amd64", "sha256": "b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a"}. The sha256 value is the verified hash from RESEARCH.md (downloaded and sha256sum computed 2026-06-10 — VERIFIED).

    Create test/docs_contract/raster_claims_test.exs as module Rendro.DocsContract.RasterClaimsTest with use ExUnit.Case, async: true.

    Include six tests following the path_claims_test.exs pattern:

    Test 1 ("support matrix has raster section with boundary declarations"): read priv/support_matrix.json, assert it contains ~s|"raster"|, ~s|"gui_viewer_equivalence"|, ~s|"unsupported"|, and ~s|"pdfium-render"|. This test WILL FAIL in Plan 01 (RED state) — correct.

    Test 2 ("pdfium_pin.json exists and has required keys"): assert File.exists?("priv/pdfium_pin.json"); read and JSON.decode! the file; assert Map.has_key?(pin, "version") and Map.has_key?(pin, "sha256"). This test PASSES in Plan 01 since the file is created in Task 2.

    Test 3 ("advisory lane is not in required_contexts"): read and JSON.decode! priv/guardrails/required_status_checks.json; refute "raster-advisory" in guardrails["required_contexts"]. This test PASSES immediately (lane not added yet to required_contexts).

    Test 4 ("advisory lane is in advisory_contexts"): read guardrails JSON; assert Enum.any?(guardrails["advisory_contexts"], &(&1["name"] == "raster-advisory")). This test WILL FAIL in Plan 01 (RED state) — correct.

    Test 5 ("GUI-viewer rows do not carry viewer_kind pdfium-render"): read support_matrix.json; refute it matches ~r/"forms".*?"viewer_kind"\s*:\s*"pdfium-render"/s. This test PASSES immediately.

    Test 6 ("docs verification script includes the raster claims lane"): read scripts/verify_docs.exs; assert it contains ~s|{"Raster claims lane", ["test", "test/docs_contract/raster_claims_test.exs"]}|. This test WILL FAIL in Plan 01 (RED state) — correct.
  </action>
  <verify>
    <automated>mix test test/docs_contract/raster_claims_test.exs 2>&1 | grep -E "(test|failure|error)" | head -20</automated>
  </verify>
  <done>
    - priv/pdfium_pin.json exists and JSON.decode! returns a map with "version" == "v0.11.0" and "sha256" == "b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a"
    - raster_claims_test.exs compiles without errors
    - Tests 2, 3, and 5 pass; tests 1, 4, and 6 fail with assertion errors (RED state for items implemented in later plans)
    - mix test (no raster_claims arg) still passes (raster_claims tests run but only the 3 passing ones matter for this wave)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → priv/raster_refs/ | Hash ref files written here are ground truth; any mutation produces false negatives |
| priv/pdfium_pin.json → CI install step | Pin file content is consumed by CI to validate binary download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Tampering | priv/raster_refs/*.sha256 — committed hash refs | mitigate | Files committed to git; CI verifies on bless run; SHA-256 collision resistance; bless-guard (GITHUB_ACTIONS check) prevents laptop writes |
| T-85-02 | Spoofing | MIX_RASTER_BLESS=true bless path | mitigate | GITHUB_ACTIONS guard in assert_or_bless/2 raises if env is not "true" — tested in Task 1 bless-guard unit test (per RAST-02b) |
| T-85-03 | Tampering | priv/pdfium_pin.json sha256 value | mitigate | Pin file is git-committed; CI step reads it for sha256sum --check; any manual edit is caught by docs-contract test in Task 2 |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | Phase 85 adds zero new Hex packages; pdfium-cli is a binary download with pinned sha256 — no package manager installs required |
</threat_model>

<verification>
After both tasks complete:
- `mix test test/rendro/adapters/pdfium_test.exs test/rendro/adapters/pdfium_raster_snapshot_test.exs` exits 0
- `mix test test/docs_contract/raster_claims_test.exs` exits non-zero (RED state for tests 1, 4, 6) but does not crash on compile
- `mix test` (full default suite) exits 0 (raster_snapshot tag excluded, failing docs-contract stubs are expected to fail but do not block other tests — NOTE: if raster_claims_test.exs failures block `mix test`, wrap the RED tests in `@tag :pending` temporarily)
- `cat priv/pdfium_pin.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'sha256' in d and 'version' in d"` exits 0
- `ls priv/raster_refs/.gitkeep` exits 0
</verification>

<success_criteria>
Wave 0 scaffolding is complete when:
1. All new test files compile without errors
2. `pdfium_raster_snapshot_test.exs` bless-guard unit test passes (covers RAST-02b)
3. `raster_claims_test.exs` pin-file test and GUI-viewer-boundary test pass (covers RAST-01c partial, RAST-03c partial)
4. `priv/pdfium_pin.json` contains v0.11.0 and the verified sha256 hash
5. `priv/raster_refs/` directory is git-tracked
6. `mix test` (default suite) passes with raster_snapshot tests excluded
</success_criteria>

<output>
Create `.planning/phases/85-deterministic-raster-lane/85-01-SUMMARY.md` when done.
</output>
