---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/rendro/font_registry.ex
  - lib/rendro/pipeline/measure.ex
  - lib/rendro/pipeline/measured_text.ex
  - lib/rendro/pipeline/paginate.ex
  - test/rendro/pipeline/measure_test.exs
  - test/rendro/pipeline/paginate_test.exs
autonomous: true
requirements: [FONT-02]

must_haves:
  truths:
    - "Measurement, wrapping, and pagination derive from the same resolved font metrics payload that rendering will later use."
    - "Phase 26 preserves the existing deterministic wrapping/pagination algorithms and swaps only the metrics source behind them."
    - "Measured text remains the truth carrier between Measure and Writer."
  artifacts:
    - path: "lib/rendro/pipeline/measure.ex"
      provides: "Embedded-font-aware deterministic measurement path"
      min_lines: 50
    - path: "lib/rendro/pipeline/measured_text.ex"
      provides: "Shared resolved-font payload carried into rendering"
      min_lines: 15
    - path: "test/rendro/pipeline/paginate_test.exs"
      provides: "Deterministic pagination proof for embedded metrics"
      min_lines: 25
  key_links:
    - from: "lib/rendro/font_registry.ex"
      to: "lib/rendro/pipeline/measure.ex"
      via: "shared resolved metrics payload"
      pattern: "resolve|metrics|embedded|default_font"
    - from: "lib/rendro/pipeline/measure.ex"
      to: "lib/rendro/pipeline/measured_text.ex"
      via: "measure-to-render parity carrier"
      pattern: "resolved_font|lines|height|width"
    - from: "lib/rendro/pipeline/measure.ex"
      to: "test/rendro/pipeline/paginate_test.exs"
      via: "embedded metrics affect row heights and page breaks deterministically"
      pattern: "height|row_heights|page|wrap"
---

<objective>
Route measurement and pagination through the shared embedded-font metrics contract.

Purpose: after Plan 01 establishes deterministic embedded-font descriptors, Phase 26 must make those descriptors affect real layout behavior so custom-font documents paginate according to their resolved metrics instead of Phase 25's built-in Helvetica widths.
Output: an embedded-font-aware measurement path, preserved `MeasuredText` parity, and focused regression coverage for wrapped lines, measured heights, and pagination behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-CONTEXT.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-RESEARCH.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-PATTERNS.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-VALIDATION.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-01-PLAN.md
@lib/rendro/pipeline/measure.ex
@lib/rendro/pipeline/measured_text.ex
@lib/rendro/pipeline/paginate.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Measure through embedded-font metrics without changing the wrapping algorithm</name>
  <files>lib/rendro/font_registry.ex, lib/rendro/pipeline/measure.ex, lib/rendro/pipeline/measured_text.ex, test/rendro/pipeline/measure_test.exs</files>
  <read_first>
    - lib/rendro/font_registry.ex
    - lib/rendro/pipeline/measure.ex
    - lib/rendro/pipeline/measured_text.ex
    - test/rendro/pipeline/measure_test.exs
    - .planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-RESEARCH.md
  </read_first>
  <action>
    Replace the assumption that resolved fonts always use the Phase 25 built-in Helvetica widths with the widened shared metrics payload from Plan 01. Preserve the current wrapping, grapheme split, and measured-height algorithm structure; Phase 26 should change the metrics source, not redesign text layout semantics.

    Keep `MeasuredText` as the carrier of the exact resolved font selected during measurement so the later writer stage can reuse it directly. Extend `test/rendro/pipeline/measure_test.exs` so it proves at least one supported embedded font produces deterministic widths/line wrapping distinct from the built-in baseline where appropriate, while untouched built-in documents retain stable behavior.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/measure.ex` uses the shared resolved metrics payload for width and line-wrap decisions.
    - `lib/rendro/pipeline/measured_text.ex` still carries the exact resolved font payload needed by Writer.
    - No parallel ad hoc embedded-font measurement path is introduced outside the existing resolver/wrapping flow.
    - `test/rendro/pipeline/measure_test.exs` proves deterministic measurement for at least one supported embedded font.
    - Existing built-in document behavior remains covered and truthful.
    - `mix test test/rendro/pipeline/measure_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Measurement now uses the same resolved embedded-font metrics contract that rendering will later consume.</done>
</task>

<task type="auto">
  <name>Task 2: Prove embedded-font-driven pagination stays deterministic</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - test/rendro/pipeline/paginate_test.exs
    - lib/rendro/pipeline/measure.ex
    - lib/rendro/pipeline/measured_text.ex
    - .planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-CONTEXT.md
  </read_first>
  <action>
    Add focused regression proof that pagination consumes embedded-font-derived measured heights and wrapped lines deterministically. If the current pagination code already depends only on `MeasuredText` and block heights, prefer tests over implementation churn; only modify paginate internals if a real parity gap exists.

    Cover at least one case where the resolved embedded font materially affects line wrapping, row heights, or page-count/page-break outcomes. Keep the assertions scoped to deterministic layout truth, not whole-file PDF bytes or future fallback/i18n behavior.
  </action>
  <acceptance_criteria>
    - `test/rendro/pipeline/paginate_test.exs` contains focused assertions tying embedded-font measurement to deterministic pagination outcomes.
    - `lib/rendro/pipeline/paginate.ex` is only changed if required for actual parity correctness.
    - The plan proves page-count, wrapped-line, or row-height stability using the shared measured-text payload.
    - No fallback-chain or unsupported-glyph behavior is introduced.
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Pagination proof now covers embedded-font-driven layout outcomes and keeps the public determinism contract honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolver -> Measure | The resolved descriptor must expose the same metrics Measure uses to derive wrapping and heights. |
| Measure -> Paginate | Page breaks must consume embedded-font-derived measured heights without hidden recomputation drift. |
| Layout contract -> future typography scope | Deterministic layout proof must stay focused on metrics parity, not future fallback or shaping semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Repudiation | measure/paginate parity | mitigate | Carry the same resolved font through `MeasuredText` and cover deterministic pagination with focused regression tests. |
</threat_model>

<verification>
Run `mix test test/rendro/pipeline/measure_test.exs test/rendro/pipeline/paginate_test.exs` to prove embedded-font measurement and pagination parity land together.
</verification>

<success_criteria>
- Measurement and pagination derive from the shared embedded-font metrics payload.
- Wrapped lines, measured heights, and page-break outcomes remain deterministic.
- `MeasuredText` continues to be the truth carrier between Measure and Writer.
</success_criteria>

<output>
After completion, create `.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-02-SUMMARY.md`
</output>

