---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/document.ex
  - lib/rendro.ex
  - lib/rendro/font_registry.ex
  - lib/rendro/pipeline/build.ex
  - lib/rendro/pdf/font.ex
  - test/rendro/document_test.exs
  - test/rendro_builders_test.exs
  - test/rendro/pdf/font_test.exs
autonomous: true
requirements: [FONT-03]

must_haves:
  truths:
    - "Embedded custom fonts are an explicit document-owned contract, not an ambient runtime lookup."
    - "Path and binary inputs normalize into Rendro-owned pure data before measure, paginate, or render run."
    - "Invalid explicit custom-font setup fails early and typed instead of silently falling back to a different face."
  artifacts:
    - path: "lib/rendro/font_registry.ex"
      provides: "Embedded-font registration, normalization, and preflighted descriptor contract"
      min_lines: 140
    - path: "lib/rendro/pipeline/build.ex"
      provides: "Early deterministic failure boundary for invalid embedded-font setup"
      min_lines: 40
    - path: "lib/rendro/pdf/font.ex"
      provides: "Generalized metrics carrier used by both built-in and embedded descriptors"
      min_lines: 40
  key_links:
    - from: "lib/rendro/document.ex"
      to: "lib/rendro/font_registry.ex"
      via: "document-owned embedded-font registration helpers"
      pattern: "register_embedded|font_registry|default_font"
    - from: "lib/rendro/font_registry.ex"
      to: "lib/rendro/pipeline/build.ex"
      via: "shared preflight and typed invalid-font failures"
      pattern: "resolve|preflight|embedded|error"
    - from: "lib/rendro/pdf/font.ex"
      to: "test/rendro/pdf/font_test.exs"
      via: "metrics payload and embeddability proof surface"
      pattern: "width|embedded|descriptor|unknown"
---

<objective>
Add explicit embedded-font registration and deterministic preflight to the shared logical-font contract.

Purpose: Phase 26 begins by making supported custom fonts first-class authored document state without allowing temp-file lifetime, system font lookup, or late-stage parse failures to influence layout or rendering.
Output: document/top-level embedded-font registration helpers, widened registry descriptors, eager path/binary normalization into owned pure data, generalized font metrics payload support, and Build-time failure for unreadable, unsupported, or non-embeddable explicit font setup.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-CONTEXT.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-RESEARCH.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-PATTERNS.md
@.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-VALIDATION.md
@lib/rendro/document.ex
@lib/rendro.ex
@lib/rendro/font_registry.ex
@lib/rendro/pipeline/build.ex
@lib/rendro/pdf/font.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit embedded-font registration and eager input normalization</name>
  <files>lib/rendro/document.ex, lib/rendro.ex, lib/rendro/font_registry.ex, test/rendro/document_test.exs, test/rendro_builders_test.exs</files>
  <read_first>
    - lib/rendro/document.ex
    - lib/rendro.ex
    - lib/rendro/font_registry.ex
    - test/rendro/document_test.exs
    - test/rendro_builders_test.exs
    - .planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-RESEARCH.md
    - .planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-PATTERNS.md
  </read_first>
  <action>
    Extend the document-owned font contract with an explicit embedded-font registration path rather than overloading the existing built-in API. Support caller-provided tagged inputs for `{:path, path}` and `{:binary, bytes}` and preserve the existing logical-name authoring contract from Phase 25. The registration flow must eagerly normalize external inputs into Rendro-owned pure data so later stages do not depend on temp files, project layout, or mutable host state.

    Keep the public scope narrow and explicit. Implement the narrow four-variant family helper for `regular`, `bold`, `italic`, and `bold_italic` through caller-provided variants, but do not add implicit family discovery, faux bold/italic synthesis, system-font lookup, or remote fetching. Missing required variants must fail early and typed instead of partially registering the family, silently remapping a style request, or synthesizing bold/italic. The missing-variant failure surface must include the logical family name, the missing variant key or keys, the source kind context for provided variants (`:path` or `:binary`), and a concrete typed reason so operators can fix setup errors without reading PDF internals. Add focused tests that prove the document and top-level APIs can register supported embedded-font sources, that normalized state stays deterministic pure data, and that partial-family registrations fail explicitly with those details.
  </action>
  <acceptance_criteria>
    - `lib/rendro/document.ex` exposes explicit embedded-font registration helpers on the document contract.
    - `lib/rendro.ex` exposes top-level wrappers for the embedded-font registration path.
    - `lib/rendro/font_registry.ex` accepts `{:path, path}` and `{:binary, bytes}` inputs and normalizes them into owned state.
    - The embedded-font API includes an explicit four-variant family helper for `regular`, `bold`, `italic`, and `bold_italic`.
    - Missing `regular`, `bold`, `italic`, or `bold_italic` variants fail early and typed with no faux style synthesis or silent remap.
    - Missing-variant failures include the logical family name, missing variant key or keys, source-kind context for provided variants, and a concrete typed reason.
    - No later-stage API requires reopening a filesystem path to know what font was registered.
    - `test/rendro/document_test.exs` and `test/rendro_builders_test.exs` contain assertions for both supported source kinds and at least one partial-family failure case with asserted error details.
    - `mix test test/rendro/document_test.exs test/rendro_builders_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/document_test.exs test/rendro_builders_test.exs</automated>
  </verify>
  <done>Embedded fonts are now explicit authored document state with deterministic source normalization.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight embedded-font readiness and generalize the shared font descriptor</name>
  <files>lib/rendro/font_registry.ex, lib/rendro/pipeline/build.ex, lib/rendro/pdf/font.ex, test/rendro/pdf/font_test.exs</files>
  <read_first>
    - lib/rendro/font_registry.ex
    - lib/rendro/pipeline/build.ex
    - lib/rendro/pdf/font.ex
    - test/rendro/pdf/font_test.exs
    - .planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-CONTEXT.md
    - .planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-RESEARCH.md
  </read_first>
  <action>
    Widen the shared resolved-font contract so it can carry deterministic metrics and embedding metadata for both built-in and supported embedded fonts. Preflight the embedded-font inputs at the earliest deterministic boundary: unreadable data, unsupported format, missing required metrics, or a non-embeddable font must fail through typed errors before measurement or rendering begins.

    Reuse the existing Phase 25 pattern where `Build` is the earliest hard failure boundary for explicit font misuse. The implementation may introduce internal helpers or types, but it must preserve `Rendro.FontRegistry` as the single logical-font registry and resolution seam. The invalid-font error surface must include the logical font name, source kind (`:path` or `:binary`), and concrete failure reason so operators can fix bad setup without reading PDF internals. Extend `test/rendro/pdf/font_test.exs` to prove successful preflight for supported data and explicit typed failure for invalid inputs, and add a build- or pipeline-entry regression that proves the failure occurs before measure or render begin.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pdf/font.ex` or its immediate helper layer can represent metrics for both built-in and embedded fonts.
    - `lib/rendro/font_registry.ex` exposes one shared resolution/preflight path used by stage consumers.
    - `lib/rendro/pipeline/build.ex` rejects invalid explicit embedded-font setup before downstream stages run.
    - Invalid embedded-font failures expose the logical font name, source kind, and concrete failure reason through a typed error surface.
    - Unknown logical-font errors remain explicit and no code path silently falls back from an embedded font to Helvetica.
    - `test/rendro/pdf/font_test.exs` covers at least one successful embedded-font preflight and one invalid-source failure with asserted error details.
    - A build- or pipeline-entry test proves invalid embedded-font setup fails before measure or render.
    - `mix test test/rendro/pdf/font_test.exs test/rendro/pipeline/measure_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pdf/font_test.exs test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>The shared logical-font resolver now preflights supported embedded fonts and fails invalid explicit setup early.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External font input -> document state | Filesystem paths and raw binaries must become owned deterministic document state before later pipeline stages run. |
| Authoring contract -> Build | Explicit invalid custom-font setup must fail early instead of turning into hidden layout or render drift. |
| Current scope -> future typography scope | Phase 26 must stay narrow and avoid claiming fallback, shaping, or system-font support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `{:path, path}` registration flow | mitigate | Eagerly read and normalize path inputs into Rendro-owned pure data before later stages run. |
| T-26-02 | Information disclosure / nondeterminism | ambient filesystem state | mitigate | Do not keep live path dependencies past registration or preflight. |
| T-26-03 | Repudiation | invalid explicit font setup | mitigate | Reject unreadable, unsupported, or non-embeddable fonts through typed Build-time failures instead of hidden fallback. |
</threat_model>

<verification>
Run `mix test test/rendro/document_test.exs test/rendro_builders_test.exs test/rendro/pdf/font_test.exs` to prove embedded-font registration, deterministic source normalization, and typed preflight failures land together.
</verification>

<success_criteria>
- Embedded custom-font sources are registered explicitly through the document contract.
- Path and binary inputs normalize into Rendro-owned pure data before later stages run.
- Invalid explicit embedded-font setup fails early and typed without silent fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/26-deterministic-font-metrics-and-pdf-embedding/26-01-SUMMARY.md`
</output>
