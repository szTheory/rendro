---
phase: 133-repository-evidence-hygiene
plan: "03"
type: execute
wave: 3
depends_on: ["133-01", "133-02"]
files_modified:
  - dev/rendro/repository_hygiene.ex
  - dev/mix/tasks/quality/hygiene.ex
  - priv/quality/package-members-v1.json
  - test/quality/repository_hygiene_test.exs
  - mix.exs
  - .github/workflows/release.yml
  - .planning/QUALITY.md
autonomous: true
requirements: [HYGIENE-01, HYGIENE-03, HYGIENE-04]
must_haves:
  truths:
    - "D-21: `mix quality.hygiene` is the one deterministic contract used locally, in `ci.fast`, and in the clean release checkout."
    - "D-22/D-23: the command builds and unpacks the actual Hex artifact, compares stable normalized members with a versioned expected manifest, reports missing and unexpected members, and separately rejects forbidden classes while retaining only narrow owner-bearing public exceptions such as `priv/adoption_evidence/2026-08-21.json` (D-07)."
    - "D-24/D-25: NUL-safe tracked-path validation accepts only project planning files, canonical active phase directories, milestone archives, and explicit `gsd_tooling`; operational scans find zero archive consumers outside those named exceptions."
    - "D-26: unrelated untracked or ignored local files never fail hygiene unless they enter the built package, become tracked in forbidden placement, or influence an authoritative workflow."
    - "D-27: normalized package members and diagnostics use deterministic stable ordering even for equal values; success is concise and failures include path, rule, reason, and exact next action without color/emoji-only meaning."
    - "Hygiene checks are read-only over authoritative repository/evidence state and use per-run isolated temporary package output; interrupted or parallel runs cannot share/corrupt package or evidence state."
    - "QL-002 closes only after its predeclared focused zero-consumer scan, compatibility review, and relevant deterministic/proof gate evidence are recorded."
  artifacts:
    - path: "dev/rendro/repository_hygiene.ex"
      provides: "Pure deterministic package, placement, inventory, and consumer policy engine"
      contains: "defmodule Rendro.RepositoryHygiene"
    - path: "dev/mix/tasks/quality/hygiene.ex"
      provides: "Purpose-named `mix quality.hygiene` CLI"
      contains: "Mix.Tasks.Quality.Hygiene"
    - path: "priv/quality/package-members-v1.json"
      provides: "Versioned exact normalized unpacked-Hex membership contract with owned exceptions"
      contains: "schema_version"
    - path: "test/quality/repository_hygiene_test.exs"
      provides: "Package diff, forbidden class, NUL-path, archive consumer, inventory, concurrency, and UX regression coverage"
      contains: "RepositoryHygieneTest"
  key_links:
    - from: "dev/mix/tasks/quality/hygiene.ex"
      to: "dev/rendro/repository_hygiene.ex"
      via: "single CLI delegates all checks to one policy implementation"
      pattern: "RepositoryHygiene"
    - from: "dev/rendro/repository_hygiene.ex"
      to: "priv/quality/package-members-v1.json"
      via: "exact sorted comparison against actual unpacked artifact members"
      pattern: "package-members-v1\\.json"
    - from: "mix.exs"
      to: "dev/mix/tasks/quality/hygiene.ex"
      via: "preferred test env and ci.fast invocation"
      pattern: "quality.hygiene"
    - from: ".planning/QUALITY.md"
      to: "test/quality/repository_hygiene_test.exs"
      via: "QL-002 closure cites focused scan and compatibility/gate evidence"
      pattern: "QL-002"
  prohibitions:
    - requirement_id: HYGIENE-04
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "flagged-unverified: a green hygiene result must never silently certify mix.exs intent while ignoring the actual unpacked package users receive"
    - requirement_id: HYGIENE-01
      category: safety
      status: unresolved
      verification: null
      resolution: null
      reason: "flagged-unverified: a planning-tooling exception must never silently acquire product, release, package, or ordinary-regression authority"
---

<objective>
Enforce the actual package, tracked planning placement, helper inventory, and archive-consumer boundaries through one deterministic maintainer command.

Purpose: Repository and release hygiene becomes a reproducible artifact-boundary contract without policing harmless local developer state.
Output: `mix quality.hygiene`, its dev/test-only policy module and ExUnit suite, a versioned package member manifest, CI/release wiring, and evidence-backed QL-002 closure.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/QUALITY.md
@.planning/phases/133-repository-evidence-hygiene/133-CONTEXT.md
@.planning/phases/133-repository-evidence-hygiene/133-RESEARCH.md
@.planning/phases/133-repository-evidence-hygiene/133-PATTERNS.md
@.planning/phases/133-repository-evidence-hygiene/133-VALIDATION.md
@.planning/phases/133-repository-evidence-hygiene/133-01-SUMMARY.md
@.planning/phases/133-repository-evidence-hygiene/133-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove actual package membership and narrow repository policy</name>
  <read_first>
    mix.exs
    dev/rendro/catalog.ex
    dev/mix/tasks/rendro/catalog/check.ex
    scripts/README.md
    .planning/quality/baselines/132-initial.json
    test/guardrails/required_checks_contract_test.exs
    .github/workflows/release.yml
  </read_first>
  <files>dev/rendro/repository_hygiene.ex, dev/mix/tasks/quality/hygiene.ex, priv/quality/package-members-v1.json, test/quality/repository_hygiene_test.exs</files>
  <behavior>
    - A real `mix hex.build --unpack` member set equal to the versioned manifest passes; any missing/unexpected member or forbidden class fails with sorted path/rule/why/next-action diagnostics.
    - `.planning`, internal `evidence`, internal schemas, tests, scripts, CI metadata, caches/editor debris, raw proof artifacts, and internal journey history are rejected from the artifact; the one public adoption asset remains an explicit owner-bearing exception.
    - NUL-delimited tracked paths with spaces remain intact; canonical current phase and milestone archive shapes pass, loose/misplaced phase files fail.
    - Operational archive references fail while exact named `gsd_tooling` entries with owner/purpose pass; missing/duplicate inventory rows fail closed.
    - Two concurrent runs receive different private temporary roots and leave no authoritative evidence/package mutation after success, failure, or interruption cleanup.
  </behavior>
  <action>Write the ExUnit behaviors RED, then implement `Rendro.RepositoryHygiene` as pure stable policy helpers plus `Mix.Tasks.Quality.Hygiene` as the single command. Generate `priv/quality/package-members-v1.json` from the actual current unpacked Hex artifact, classify every member and every narrow exception with owner/reason/review trigger, and remove raw proof/benchmark debris from `mix.exs` package inputs when it has no D-23 public owner. Build/unpack into a unique `System.tmp_dir!()` child created per invocation with private permissions and cleanup in `after`; never share an output path and never write capsule/manifest/package state. Parse `git ls-files -z` as binary NUL records, validate D-24 placement, compare `scripts/README.md` with tracked executables, and scan only D-25 operational surfaces. Preserve explicit `gsd_tooling` purpose/owner exceptions, never inspect arbitrary untracked/ignored files per D-26, and return stable sorted structured violations. The Mix task prints concise text success and on failure emits path, rule ID, reason, and exact next action with no color/emoji-only semantics per D-27. Use existing Mix, ExUnit, JSON/JSV, `:crypto`, and dev/test compilation only; add no dependency or public API.</action>
  <verify>
    <automated>mix test test/quality/repository_hygiene_test.exs -x &amp;&amp; mix quality.hygiene</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove exact actual-member parity plus non-vacuous mutations for one unexpected member, one missing member, and every forbidden package class.
    - Tests prove NUL-safe paths, accepted canonical/archived/`gsd_tooling` placement, rejected loose placement, zero operational archive consumers, and duplicate/missing helper inventory failures.
    - Tests launch parallel isolated runs (or inject distinct temp roots) and prove output roots differ and authoritative capsule/package manifest bytes do not change; cleanup is safe after simulated failure.
    - `mix quality.hygiene` succeeds against the real current unpacked artifact with deterministic output and ignores an unrelated untracked scratch fixture.
  </acceptance_criteria>
  <done>The actual artifact and tracked repository boundaries are enforced by one tested, deterministic, read-only policy command.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the canonical gate and close QL-002 with predeclared evidence</name>
  <read_first>
    mix.exs
    .github/workflows/release.yml
    .planning/QUALITY.md
    .planning/quality/baselines/132-initial.json
    test/quality/repository_hygiene_test.exs
    test/guardrails/required_checks_contract_test.exs
    .planning/phases/133-repository-evidence-hygiene/133-01-SUMMARY.md
    .planning/phases/133-repository-evidence-hygiene/133-02-SUMMARY.md
  </read_first>
  <files>mix.exs, .github/workflows/release.yml, .planning/QUALITY.md</files>
  <action>Add `quality.hygiene: :test` to preferred environments and invoke the same command from `ci.fast` and the clean release checkout per D-21; do not create a second implementation or alter the Phase 135-owned catalog workflow topology. Run the predeclared focused scan from QL-002, the complete migrated consumer suite, `mix quality.hygiene`, `mix ci.fast`, and `mix ci.proofs` as separate evidence lanes. Perform the compatibility review by comparing the public API manifest and confirming no `lib/` or unrelated rendered-byte inputs changed. Only after all available required evidence is produced, update QL-002 in `.planning/QUALITY.md` from `accepted` to `closed` with dated resolution reference, before/after statement, exact focused scan, compatibility review, and deterministic/proof command results. If `mix ci.proofs` is unavailable for an environmental reason, record it as explicit non-blocking proof-lane deferral with reason and rerun trigger; never fabricate a pass. Leave QL-002 open if the focused scan, compatibility review, or relevant deterministic evidence is absent.</action>
  <verify>
    <automated>mix quality.hygiene &amp;&amp; mix test test/scripts/repository_evidence_test.exs test/scripts/phoenix_clean_room_proof_test.exs test/scripts/public_release_verifier_test.exs test/docs_contract/phoenix_newcomer_contract_test.exs test/guardrails/required_checks_contract_test.exs test/quality/repository_hygiene_test.exs -x &amp;&amp; mix ci.fast</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` exposes exactly one `quality.hygiene` implementation path, runs it in `ci.fast`, and `.github/workflows/release.yml` invokes it from the clean checkout.
    - The focused scan reports zero operational Phase 131/archive consumers and only explicit named `gsd_tooling` structural references.
    - QL-002 is `closed` only if its closure text cites the focused scan, public API/`lib/`/rendered-byte compatibility review, `mix quality.hygiene`, focused consumer tests, `mix ci.fast`, and the separate proof result or explicit unavailable deferral.
    - `priv/adoption_evidence/2026-08-21.json` remains package-visible; the internal capsule, its schemas, journey records, planning, scripts, tests, and raw proof data are absent from unpacked membership.
  </acceptance_criteria>
  <done>The same gate protects local, CI-fast, and release-clean paths, and QL-002 closes only on its predeclared evidence contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Rendro.RepositoryHygiene` in `dev/rendro/repository_hygiene.ex` — internal `check/0`/policy surface for package, placement, inventory, and archive consumers.
- `Mix.Tasks.Quality.Hygiene` in `dev/mix/tasks/quality/hygiene.ex` — `mix quality.hygiene` CLI.
- `priv/quality/package-members-v1.json` — versioned exact unpacked-member manifest and owner-bearing exceptions.
- `test/quality/repository_hygiene_test.exs` — deterministic negative, concurrency, and artifact-boundary suite.
- Updated `mix.exs` alias/preferred-env/`ci.fast` wiring and release clean-checkout invocation.
- Updated `.planning/QUALITY.md` QL-002 closure evidence, only if the declared proof exists.
- No public API, runtime dependency, external API integration, or rendered artifact is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package configuration -> unpacked Hex artifact | Intended files can diverge from what users actually receive. |
| Git tracked index -> planning placement | Malformed/loose paths or record splitting could bypass policy. |
| planning-tooling exception -> operational scan | A narrow structural exception could acquire executable product authority. |
| parallel command -> temporary package output | Shared paths could corrupt or substitute evidence between runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-09 | Information Disclosure | actual Hex package membership | high | mitigate | Build/unpack, exact versioned member diff, and independent forbidden-class checks with mutation coverage. |
| T-133-10 | Tampering | NUL-safe planning placement and helper inventory | high | mitigate | Parse binary NUL records, enforce canonical forms and uniqueness, and fail closed on missing/duplicate inventory entries. |
| T-133-11 | Elevation of Privilege | `gsd_tooling` archive-consumer exception | high | mitigate | Require exact named purpose/owner entries outside operational surfaces and prove zero product/release/package/regression consumers. |
| T-133-12 | Tampering / Denial of Service | temporary build output | high | mitigate | Unique private temp root per invocation, read-only authoritative inputs, guaranteed cleanup, and concurrent/failure-path tests. |
| T-133-13 | Repudiation | QL-002 lifecycle closure | high | mitigate | Permit closure only with the predeclared scan, compatibility review, exact relevant gate evidence, and explicit unavailable proof handling. |

All high-severity threats block completion until their specific automated mitigation evidence is green; unrelated CI success cannot override a missing control.
</threat_model>

<verification>
- Run `mix quality.hygiene`, the focused evidence/consumer/hygiene tests, `mix ci.fast`, and `mix ci.proofs` as distinct lanes.
- Capture and compare `priv/public_api.json`; verify `git diff --name-only -- lib` is empty and no approved catalog/rendering input changed.
- Run the API-coverage detector over final phase scope. No external API integration: Phase 133 only adds local repository control-plane tooling and reads Git/Hex build output already available through Mix; GitHub/Hex workflow references do not add an API/SDK/service integration.
- Prohibition probe retains two bespoke transparency/safety constraints descriptor-less and flagged-unverified; package leakage and executable-planning authority are canonical security threats handled by T-133-09/T-133-11.
</verification>

<success_criteria>
- The actual unpacked package exactly matches the versioned reviewed manifest and contains no forbidden internal classes.
- Tracked planning placement, helper ownership, and archive-consumer rules fail closed with deterministic actionable diagnostics.
- Parallel/interrupted checks cannot share or mutate authoritative output, and unrelated local debris is ignored.
- QL-002 has complete closure evidence or truthfully remains open; package/public API/unrelated rendered bytes remain unchanged.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Current behavior uses durable inputs while planning is safely archived | 01-03 | COVERED | Capsule cutover, archive ownership, and artifact-boundary enforcement. |
| REQ | HYGIENE-01 | Zero active archive consumers with narrow named tooling exceptions | 01, 03 | COVERED | Atomic scan plus permanent policy. |
| REQ | HYGIENE-02 | Versioned schema-validated durable evidence and migrated consumers | 01 | COVERED | Complete atomic tracer. |
| REQ | HYGIENE-03 | Correct archives and owned helpers | 02, 03 | COVERED | Moves/inventory plus permanent placement check. |
| REQ | HYGIENE-04 | Actual package excludes internal evidence/debris | 03 | COVERED | Unpacked-member exact diff and forbidden classes. |
| RESEARCH | capsule/loader architecture | Authority-separated capsule, JSV, confinement, digest/binding | 01 | COVERED | Existing stack only. |
| RESEARCH | artifact/placement/inventory gate | Mix gate, NUL-safe Git input, exact unpacked membership | 02-03 | COVERED | Narrow deterministic policy. |
| RESEARCH | QL-002 closure contract | Focused scan, compatibility review, relevant gates | 03 | COVERED | Conditional close is explicit. |
| CONTEXT | D-01..D-13 | Durable evidence structure and truthful retention | 01 | COVERED | Each decision cited in tracer truths/action. |
| CONTEXT | D-14..D-20 | Archive and helper ownership | 02 | COVERED | Each decision cited in truths/tasks. |
| CONTEXT | D-21..D-27 | Package and repository hygiene | 03 | COVERED | Each decision cited in truths/tasks; D-24/D-27 also Plan 02. |
| EDGE | 1 | Zero consumers and named `gsd_tooling` exceptions | 01, 03 | COVERED | Explicit truth and scan. |
| EDGE | 2 | Duplicate ID/path/equal identity collision | 01 | COVERED | Fail closed, no merge. |
| EDGE | 3 | Empty/missing manifest/record; one valid record | 01 | COVERED | Explicit negative and singleton cases. |
| EDGE | 4 | Stable manifest/index/diagnostic ordering | 01, 02, 03 | COVERED | Equal values cannot destabilize output. |
| EDGE | 5 | Isolated read-only concurrent checks; sequential archive move | 02, 03 | COVERED | Explicit truth and tests. |
| EDGE | 6 | Actual unpacked membership, forbidden classes, planning placement | 03 | COVERED | Exact artifact and tracked index checks. |
| PROHIBITION | PROH-133-01..05 | Advisory inflation, preservation rewrite, invented provenance, intent-only package proof, tooling-authority inflation | 01-03 | COVERED | Descriptor-less `unresolved` items with `flagged-unverified` reasons; generic security canon stays in threat models. |

Deferred and excluded: the version-agnostic future-release framework and Phase 135 catalog workflow consolidation are not gaps.

<output>
Create `.planning/phases/133-repository-evidence-hygiene/133-03-SUMMARY.md` when done.
</output>
