---
phase: 133-repository-evidence-hygiene
plan: "10"
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - scripts/README.md
  - scripts/repo_hygiene_check.sh
  - scripts/audit_branch_protection.exs
  - scripts/render_logo.exs
autonomous: true
requirements: [HYGIENE-03]
must_haves:
  truths:
    - "D-17/D-18: every retained helper has complete role/caller/invocation/lane/removal-trigger inventory; the three resolved ownerless scripts are removed."
    - "D-19/D-20/D-27: `gsd_tooling` is narrow, no wrapper/second hygiene implementation exists, and rows are stable/accessibly ordered."
  artifacts:
    - path: "scripts/README.md"
      provides: "Complete helper inventory"
  key_links:
    - from: "scripts/README.md"
      to: "scripts/"
      via: "one row per retained tracked helper"
      pattern: "Current callers"
  prohibitions:
    - requirement_id: HYGIENE-03
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "flagged-unverified: historical mention must not count as current helper ownership"
---
<objective>Inventory retained helpers and remove the three resolved ownerless scripts.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/133-repository-evidence-hygiene/133-RESEARCH.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Establish helper ownership and exact removals</name>
  <read_first>scripts/repo_hygiene_check.sh, scripts/audit_branch_protection.exs, scripts/render_logo.exs, mix.exs, .github/workflows/ci.yml, .github/workflows/release.yml</read_first>
  <files>scripts/README.md, scripts/repo_hygiene_check.sh, scripts/audit_branch_protection.exs, scripts/render_logo.exs</files>
  <action>Create the D-17 inventory using the research-resolved current caller set; verify every retained row. Remove exactly `repo_hygiene_check.sh`, `audit_branch_protection.exs`, and `render_logo.exs` per D-18/D-20, with no wrapper. Mark structural planning helpers `gsd_tooling` per D-19 and explicitly deny product/release/package/regression authority.</action>
  <verify><automated>test -f scripts/README.md &amp;&amp; ! test -e scripts/repo_hygiene_check.sh &amp;&amp; ! test -e scripts/audit_branch_protection.exs &amp;&amp; ! test -e scripts/render_logo.exs</automated></verify>
  <acceptance_criteria>
    - Three exact removals are absent; every retained executable has one complete current-caller/owner row.
    - No compatibility wrapper or second hygiene policy exists.
  </acceptance_criteria>
  <done>Helper ownership is complete and resolved obsolete scripts are removed.</done>
</task>
</tasks>
## Artifacts this phase produces
- `scripts/README.md`; removal of three exact scripts.
<threat_model>
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---|---|---|
| helper -> authority | T-133-13 privilege inflation | high | mitigate | owner/caller/lane/removal-trigger inventory |
</threat_model>
<verification>Exact removals and complete inventory are checked.</verification>
<success_criteria>Every retained helper is supported; no ownerless canonical command remains.</success_criteria>
<output>Create `133-10-SUMMARY.md`.</output>
