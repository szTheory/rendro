---
phase: 133-repository-evidence-hygiene
plan: "02"
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - evidence/releases/v1.3.4/manifest.json
  - evidence/releases/v1.3.4/release_identity.json
  - evidence/releases/v1.3.4/validation.json
  - evidence/releases/v1.3.4/journey/index.json
  - priv/schemas/release_evidence_identity.schema.json
  - priv/schemas/release_evidence_validation.schema.json
  - priv/schemas/release_evidence_journey_index.schema.json
  - dev/rendro/repository_evidence.ex
  - test/scripts/repository_evidence_test.exs
autonomous: true
requirements: [HYGIENE-02]
must_haves:
  truths:
    - "D-03/D-04: release identity, validation identity, and ordered journey index are separate strict schema_version 1 roles indexed by the manifest."
    - "D-05: maintainer-internal `Rendro.RepositoryEvidence.load_role/1` dispatches `:release_identity`, `:validation`, and `:journey_index` through the same fail-closed schema/path/digest/binding pipeline as `:public_prerequisite`; unknown or mismatched roles never return facts."
    - "D-08/D-10/D-11/D-13: provenance/import metadata remain separate, historical identity is append-only, advisory lanes remain truthful, and Git controls remain authority."
  artifacts:
    - path: "evidence/releases/v1.3.4/release_identity.json"
      provides: "Sealed candidate/tag/release provenance"
    - path: "evidence/releases/v1.3.4/validation.json"
      provides: "Durable successful journey identities"
    - path: "evidence/releases/v1.3.4/journey/index.json"
      provides: "Stable ordered attempt index"
    - path: "dev/rendro/repository_evidence.ex"
      provides: "Maintainer-internal `load_role/1` dispatch for all four sealed capsule roles"
  key_links:
    - from: "evidence/releases/v1.3.4/manifest.json"
      to: "evidence/releases/v1.3.4/release_identity.json"
      via: "role/path/digest record"
      pattern: "release_identity"
    - from: "dev/rendro/repository_evidence.ex"
      to: "evidence/releases/v1.3.4/release_identity.json"
      via: "`load_role(:release_identity)` validates schema, path, digest, and release/candidate/tag binding"
      pattern: "load_role.*release_identity"
    - from: "dev/rendro/repository_evidence.ex"
      to: "evidence/releases/v1.3.4/validation.json"
      via: "`load_role(:validation)` validates schema, path, digest, and release identity binding"
      pattern: "load_role.*validation"
    - from: "dev/rendro/repository_evidence.ex"
      to: "evidence/releases/v1.3.4/journey/index.json"
      via: "`load_role(:journey_index)` validates schema, path, digest, and capsule release binding"
      pattern: "load_role.*journey_index"
  prohibitions:
    - requirement_id: HYGIENE-02
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "flagged-unverified: import metadata must never be presented as original observation"
---

<objective>Expand the inert capsule with its remaining core roles and schemas.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/133-repository-evidence-hygiene/133-CONTEXT.md
@.planning/phases/133-repository-evidence-hygiene/133-RESEARCH.md
@.planning/phases/133-repository-evidence-hygiene/133-01-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add authority-separated core capsule roles</name>
  <read_first>
    evidence/releases/v1.3.4/manifest.json
    dev/rendro/repository_evidence.ex
    test/scripts/repository_evidence_test.exs
    .planning/milestones/v2.13-phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
    .planning/milestones/v2.13-phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
  </read_first>
  <files>evidence/releases/v1.3.4/manifest.json, evidence/releases/v1.3.4/release_identity.json, evidence/releases/v1.3.4/validation.json, evidence/releases/v1.3.4/journey/index.json, priv/schemas/release_evidence_identity.schema.json, priv/schemas/release_evidence_validation.schema.json, priv/schemas/release_evidence_journey_index.schema.json, dev/rendro/repository_evidence.ex, test/scripts/repository_evidence_test.exs</files>
  <action>Add strict D-03/D-04 role schemas and preserved records, with original source/digest/commit where available, separate import metadata/redaction class, stable IDs, append-only `supersedes`, truthful D-11 advisory classifications, and D-13 authority wording. Extend the Plan 01 maintainer-internal module with the exact non-runtime dispatch `Rendro.RepositoryEvidence.load_role/1`, accepting only `:public_prerequisite`, `:release_identity`, `:validation`, and `:journey_index`. Route all four atoms through one manifest lookup followed by role equality, role-specific JSV schema validation, confined regular-file path validation, media-type validation, SHA-256 verification, and capsule/release/candidate/tag cross-binding before returning `{:ok, payload}`; return `{:error, diagnostic}` for unknown atoms or any mismatch. Do not add the module under `lib/`, expose it in public docs, or add a runtime API. Update the manifest in stable order and extend the focused test with positive dispatch for each of the three new roles plus rejection of wrong requested/manifest role, wrong role schema, binding mismatch, digest mismatch, unsafe/missing path, and missing/duplicate core roles. Leave journey index entries empty until Plans 03-04 add files; empty dataset preparation is valid but no consumer is active.</action>
  <verify><automated>mix test test/scripts/repository_evidence_test.exs -x</automated></verify>
  <acceptance_criteria>
    - All nine physical paths exist and validate; the manifest digests resolve and core-role collision/binding mutations fail.
    - Focused tests prove `load_role(:release_identity)`, `load_role(:validation)`, and `load_role(:journey_index)` each return the correctly typed role payload only after schema/path/digest/binding validation.
    - Focused tests prove an unknown atom, requested-role versus manifest-role mismatch, role payload validated against the wrong schema, release/candidate/tag mismatch, digest mismatch, traversal/absolute/missing path, and missing/duplicate role each return an error without exposing payload facts.
    - No active consumer changes and the capsule remains non-authoritative.
  </acceptance_criteria>
  <done>All non-attempt capsule roles are sealed and validated.</done>
</task>
</tasks>
## Artifacts this phase produces
- `release_identity.json`, `validation.json`, `journey/index.json`, three exact role schemas, updated `manifest.json`, maintainer-internal `Rendro.RepositoryEvidence.load_role/1` role dispatch, and expanded repository evidence tests.
<threat_model>
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---|---|---|
| source -> core record | T-133-03 provenance rewrite | high | mitigate | source/import separation and mutation tests |
| core roles -> identity | T-133-04 stale substitution | high | mitigate | strict role/binding/uniqueness checks |
</threat_model>
<verification>`mix test test/scripts/repository_evidence_test.exs -x` proves positive dispatch and fail-closed wrong-role/schema/binding/digest/path behavior; active consumers, runtime/public API, package, and rendered outputs remain unchanged.</verification>
<success_criteria>All core roles are valid, separate, deterministic, inert, and reachable only through the shared maintainer-internal validated dispatch.</success_criteria>
<output>Create `133-02-SUMMARY.md`.</output>
