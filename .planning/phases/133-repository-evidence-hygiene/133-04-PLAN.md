---
phase: 133-repository-evidence-hygiene
plan: "04"
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - scripts/README.md
  - scripts/repo_hygiene_check.sh
  - scripts/audit_branch_protection.exs
  - scripts/render_logo.exs
autonomous: true
requirements: [HYGIENE-03]
must_haves:
  truths:
    - "D-17: scripts/README.md inventories every retained tracked helper with purpose, role owner, invocation, inputs/outputs, authority lane, callers, and review/removal trigger."
    - "D-18/D-20: the three resolved ownerless helpers are removed; no compatibility wrapper survives and no second hygiene command competes with `mix quality.hygiene`."
    - "D-19: planning-aware retained checks are named `gsd_tooling` and explicitly lack product, release-fact, package, and ordinary-regression authority."
    - "D-27: inventory rows are stable path-ordered and accessible without color/emoji-only meaning."
  artifacts:
    - path: "scripts/README.md"
      provides: "Complete tracked-helper ownership inventory"
      contains: "Authority lane"
  key_links:
    - from: "scripts/README.md"
      to: "scripts/"
      via: "one row per retained tracked helper with proven caller"
      pattern: "Current callers"
  prohibitions:
    - requirement_id: HYGIENE-03
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "flagged-unverified: a historical mention must never silently count as current helper ownership or supported invocation"
---

<objective>
Make helper ownership complete and remove the three research-proven ownerless scripts.

Purpose: The supported maintainer command set becomes explicit, current, and non-duplicative.
Output: Complete scripts inventory and evidence-backed deletion of obsolete helpers.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-repository-evidence-hygiene/133-CONTEXT.md
@.planning/phases/133-repository-evidence-hygiene/133-RESEARCH.md
@.planning/phases/133-repository-evidence-hygiene/133-PATTERNS.md
@.planning/phases/133-repository-evidence-hygiene/133-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory retained helpers and remove all resolved ownerless helpers</name>
  <read_first>
    scripts/repo_hygiene_check.sh
    scripts/audit_branch_protection.exs
    scripts/render_logo.exs
    mix.exs
    .github/workflows/ci.yml
    .github/workflows/release.yml
    .planning/phases/133-repository-evidence-hygiene/133-RESEARCH.md
  </read_first>
  <files>scripts/README.md, scripts/repo_hygiene_check.sh, scripts/audit_branch_protection.exs, scripts/render_logo.exs</files>
  <action>Create D-17 `scripts/README.md` in stable tracked-path order with exact columns for path, purpose, stable role owner, supported invocation, inputs, outputs, authority lane, current callers, and review/removal trigger. Use the resolved research caller inventory for retained adoption, clean-room, viewer/signing, governance, release-preflight, docs/livebook/public-URL, public-release, proof-requirement, PDF.js, and configurator helpers; verify each row against current workflow/Mix/docs/test references. Mark only structural planning checks `gsd_tooling` per D-19. Remove `scripts/repo_hygiene_check.sh`, `scripts/audit_branch_protection.exs`, and `scripts/render_logo.exs` exactly as resolved by research: each has zero current workflow/Mix/docs/test caller. Per D-20 create no wrapper; Plan 05 supplies the sole hygiene command. Do not delete or retain anything based only on historical planning mentions.</action>
  <verify>
    <automated>test -f scripts/README.md &amp;&amp; ! test -e scripts/repo_hygiene_check.sh &amp;&amp; ! test -e scripts/audit_branch_protection.exs &amp;&amp; ! test -e scripts/render_logo.exs &amp;&amp; git ls-files -z 'scripts/*' | tr '\0' '\n' | LC_ALL=C sort | while IFS= read -r path; do case "$path" in scripts/README.md|scripts/proof_requirements.in|scripts/proof_requirements.txt|scripts/pdfjs_observer/*) continue;; esac; grep -Fq "$path" scripts/README.md || { echo "missing inventory row: $path"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All three resolved ownerless files are absent and no compatibility wrapper exists.
    - Every retained executable has exactly one complete, stable path-ordered row with a current caller and role owner.
    - Every `gsd_tooling` row explicitly excludes product/release/package/regression authority; absence or caller loss is a recorded removal trigger.
  </acceptance_criteria>
  <done>The tracked helper set is complete, owner-bearing, and free of the three resolved obsolete scripts.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/README.md` helper-inventory table/schema.
- Removal of `scripts/repo_hygiene_check.sh`, `scripts/audit_branch_protection.exs`, and `scripts/render_logo.exs` with no wrapper.
- No runtime module, public API, package member, or external integration.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked helper -> maintainer authority | Ownerless or planning-only scripts could appear supported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-133-09 | Elevation of Privilege | helper inventory / `gsd_tooling` | high | mitigate | Require owner, caller, lane, and removal trigger for every retained helper; explicitly bound planning-only authority. |
| T-133-10 | Tampering | helper deletion | medium | mitigate | Delete only the three research-resolved zero-caller files and re-run entry-point scans. |

Missing owner/caller data blocks retention; historical references are insufficient.
</threat_model>

<verification>
- Run the inventory/deletion command and focused reference scans for all three removed paths.
- Confirm no Phase 135 workflow, product, package, public API, or rendered input changed.
</verification>

<success_criteria>
- Every retained helper is explicitly supported and every resolved ownerless helper is gone.
- `mix quality.hygiene` will be the only canonical hygiene command after Plan 05.
</success_criteria>

<output>
Create `.planning/phases/133-repository-evidence-hygiene/133-04-SUMMARY.md` when done.
</output>
