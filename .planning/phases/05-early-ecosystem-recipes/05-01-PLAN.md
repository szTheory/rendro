---
phase: "05-early-ecosystem-recipes"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/audit.ex
  - lib/rendro/adapters/threadline.ex
  - lib/rendro/adapters/mailglass.ex
  - test/rendro/adapters/threadline_test.exs
  - test/rendro/adapters/mailglass_test.exs
autonomous: true
requirements:
  - ADPT-05
must_haves:
  truths:
    - "Rendro.Audit behavior is defined and documented"
    - "Threadline adapter is optional and gated by Code.ensure_loaded?"
    - "Threadline adapter attaches to Telemetry and records render events"
    - "Mailglass adapter is optional and provides PDF attachment helper"
  artifacts:
    - path: "lib/rendro/audit.ex"
      provides: "Audit behavior definition"
    - path: "lib/rendro/adapters/threadline.ex"
      provides: "Optional Threadline integration"
    - path: "lib/rendro/adapters/mailglass.ex"
      provides: "Optional Mailglass integration"
  key_links:
    - from: "lib/rendro/adapters/threadline.ex"
      to: "Threadline.record_action/2"
      via: "Telemetry attachment"
    - from: "lib/rendro/adapters/mailglass.ex"
      to: "Swoosh.Email.attachment/2"
      via: "Mailglass pipe"
---

<objective>
Implement optional ecosystem adapters for auditing (Threadline) and email (Mailglass) to allow users to easily integrate Rendro into existing szTheory-style workflows.

Purpose: Provide "batteries-included" integrations without adding hard dependencies to the core library.
Output: Audit behavior, Threadline adapter, and Mailglass attachment helper.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-early-ecosystem-recipes/RESEARCH.md
@.planning/phases/05-CONTEXT.md
@lib/rendro/adapters/oban/render_worker.ex
@lib/rendro/telemetry.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Rendro.Audit behavior and Threadline adapter</name>
  <files>lib/rendro/audit.ex, lib/rendro/adapters/threadline.ex</files>
  <action>
    - Create `lib/rendro/audit.ex` defining a behavior with `track_render/2` callback (per D-03).
    - Create `lib/rendro/adapters/threadline.ex` gated by `Code.ensure_loaded?(Threadline)`.
    - Implement `attach/0` in `Threadline` adapter to subscribe to `[:rendro, :render, :stop]` and `[:rendro, :render, :exception]` events.
    - Map Telemetry events to `Threadline.record_action/2` calls:
      - `:stop` with `:ok` -> `record_action(:render_succeeded, metadata)`
      - `:stop` with `:error` or `:exception` -> `record_action(:render_failed, metadata)`
    - Ensure PII-safe metadata (use existing Telemetry metadata structure).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Audit behavior defined and Threadline adapter implemented with Telemetry hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Mailglass attachment helper</name>
  <files>lib/rendro/adapters/mailglass.ex</files>
  <action>
    - Create `lib/rendro/adapters/mailglass.ex` gated by `Code.ensure_loaded?(Mailglass)`.
    - Implement `attach_pdf/3` which accepts a Mailglass message (or Swoosh email), a Rendro document, and an optional filename (per D-04).
    - The helper should call `Rendro.render/1`, and then use `Swoosh.Email.attachment/2` to attach the resulting binary.
    - If `Mailglass` is loaded, it should handle piping correctly into `Mailglass.Message.update_swoosh/2` if necessary, or just return the modified Swoosh email.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Mailglass adapter helper implemented and compilation verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify adapters with contract mocks</name>
  <files>test/rendro/adapters/threadline_test.exs, test/rendro/adapters/mailglass_test.exs, test/support/mocks.ex</files>
  <behavior>
    - Mock Threadline: Verify that rendering triggers a Telemetry event that the adapter catches and passes to Threadline.record_action.
    - Mock Mailglass/Swoosh: Verify that attach_pdf renders the document and adds a PDF attachment to the Swoosh email struct.
  </behavior>
  <action>
    - Create `test/support/mocks.ex` to define minimal `Threadline`, `Mailglass`, and `Swoosh` modules if they aren't loaded (for testing purposes).
    - Implement `test/rendro/adapters/threadline_test.exs` verifying the Telemetry -> Threadline mapping.
    - Implement `test/rendro/adapters/mailglass_test.exs` verifying the attachment logic.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/</automated>
  </verify>
  <done>Adapters verified via unit tests with contract mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendro -> Threadline | Internal telemetry data passed to audit logger |
| Rendro -> Swoosh/Mailglass | Rendered PDF binary passed to email attachment system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Threadline Adapter | mitigate | Redact PII from Telemetry metadata before passing to Threadline (per Rendro core policy) |
| T-05-02 | Denial of Service | Mailglass Helper | mitigate | Attachment helper uses core render which is subject to existing policy bounds (max pages/bytes) |
</threat_model>

<verification>
mix compile
mix test test/rendro/adapters/
</verification>

<success_criteria>
- [ ] `Rendro.Audit` behavior exists.
- [ ] `Rendro.Adapters.Threadline` attaches to telemetry and calls `Threadline.record_action`.
- [ ] `Rendro.Adapters.Mailglass.attach_pdf` correctly attaches rendered PDFs to Swoosh emails.
- [ ] All tests pass without requiring external libraries to be installed in mix.exs.
</success_criteria>

<output>
After completion, create .planning/phases/05-early-ecosystem-recipes/05-01-SUMMARY.md
</output>
