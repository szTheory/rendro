---
phase: "109"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/hexdocs.yml
  - .github/workflows/release.yml
  - mix.exs
autonomous: true
requirements:
  - CACHE-03
  - CACHE-04
must_haves:
  truths:
    - setup-beam is consistently pinned to a specific SHA per D-04.
    - Dialyzer stores its PLT files in priv/plts per D-02.
  artifacts:
    - path: .github/workflows/ci.yml
      provides: pinned action
    - path: mix.exs
      provides: PLT config
  key_links:
    - from: .github/workflows/ci.yml
      to: erlef/setup-beam
      via: SHA pinning
      pattern: erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7
---

<objective>
Unify `setup-beam` SHA pinning across all CI workflows and configure Mix to isolate the Dialyzer PLT in `priv/plts`.

Purpose: Secure the supply chain (CACHE-04) and prepare the repository structure for Dialyzer PLT caching (CACHE-03).
Output: Updated workflow configurations and `mix.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-caching-setup-beam/109-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify erlef/setup-beam SHA pinning</name>
  <files>.github/workflows/ci.yml, .github/workflows/hexdocs.yml, .github/workflows/release.yml</files>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/hexdocs.yml</file>
    <file>.github/workflows/release.yml</file>
  </read_first>
  <action>
    Per D-04, find every occurrence of `uses: erlef/setup-beam@v1` in the workflows and replace it with the pinned version `uses: erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7 # v1`.
    This affects multiple jobs in `ci.yml` (e.g., test, example-phoenix, raster-advisory, comparison-advisory, livebook-advisory, viewer-evidence-live-proof, signing-live-proof, long-lived-live-proof, release-proof) and `release.yml`.
    Ensure `hexdocs.yml` maintains the same pinned SHA.
  </action>
  <acceptance_criteria>
    `grep -r "erlef/setup-beam@v1" .github/workflows/` returns no matches (exits 1).
    `grep -r "erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7" .github/workflows/` shows it applied to all necessary jobs.
  </acceptance_criteria>
  <verify>
    <automated>! grep -r "erlef/setup-beam@v1" .github/workflows/</automated>
  </verify>
  <done>No unpinned erlef/setup-beam@v1 usages exist in workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate Dialyzer PLT path</name>
  <files>mix.exs</files>
  <read_first>
    <file>mix.exs</file>
  </read_first>
  <action>
    Per D-02, modify `mix.exs` to store Dialyzer PLTs in `priv/plts` instead of the default `_build` directory, allowing independent caching.
    Inside the `def project` block, update the `dialyzer:` list to include `plt_core_path: "priv/plts"` and `plt_local_path: "priv/plts"`.
    Do not remove the existing `plt_add_apps` configuration.
  </action>
  <acceptance_criteria>
    `grep 'plt_core_path: "priv/plts"' mix.exs` returns a match.
    Running `mix dialyzer` locally does not crash due to syntax mistakes in `mix.exs`.
  </acceptance_criteria>
  <verify>
    <automated>grep 'plt_core_path: "priv/plts"' mix.exs</automated>
  </verify>
  <done>mix.exs stores PLTs in priv/plts independently.</done>
</task>

</tasks>

<verification>
Overall phase verification handled in downstream plans.
</verification>

<success_criteria>
Workflow and mix.exs files successfully updated.
</success_criteria>

<output>
Create `.planning/phases/109-caching-setup-beam/109-01-SUMMARY.md` when done
</output>
