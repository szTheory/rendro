---
phase: "15"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/adapters/oban/render_worker.ex
  - guides/integrations.md
  - test/rendro/adapters/oban/render_worker_test.exs
  - test/docs_contract/integrations_claims_test.exs
autonomous: true
requirements:
  - ADPT-04
  - OBS-04
must_haves:
  truths:
    - "The Oban worker accepts a small explicit bounded-render policy surface only: `max_pages`, `max_bytes`, and `timeout`."
    - "Document-authored policies remain canonical; async job input fills only missing policy keys and never silently overrides existing document policies."
    - "Malformed required worker fields, malformed policy values, and unknown policy keys fail as typed worker-boundary errors instead of crashing via pattern matches or `String.to_existing_atom/1`."
  artifacts:
    - path: lib/rendro/adapters/oban/render_worker.ex
      provides: "Validated Oban worker boundary and fill-missing policy injection into `doc.options[:policies]`"
    - path: test/rendro/adapters/oban/render_worker_test.exs
      provides: "Wave 0 and regression proof for worker-path input validation, policy precedence, and timeout/max_pages/max_bytes injection"
    - path: guides/integrations.md
      provides: "Truthful async adapter contract describing accepted policy shape and precedence behavior"
    - path: test/docs_contract/integrations_claims_test.exs
      provides: "Executable docs-contract proof for the Oban async policy surface and precedence wording"
  key_links:
    - from: lib/rendro/adapters/oban/render_worker.ex
      to: lib/rendro/pipeline.ex
      via: "Worker injects only missing policy keys and relies on existing core enforcement"
      pattern: "options\\[:policies\\]|max_pages|max_bytes|timeout"
    - from: test/rendro/adapters/oban/render_worker_test.exs
      to: test/rendro/policy_test.exs
      via: "Worker-path proof complements core policy enforcement proof"
      pattern: "max_pages|max_bytes|timeout"
---

<objective>
Restore the bounded-async side of the Phase 15 contract by hardening `Rendro.Adapters.Oban.RenderWorker` into a narrow, typed boundary that validates known job args and injects only missing policy bounds before render.

Purpose: Close `INT-ASYNC-POLICY-INJECTION` without widening Rendro's async API or duplicating policy enforcement outside the core pipeline.
Output: A strict worker boundary, worker-path regression tests, and truthful adapter guide text.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-CONTEXT.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-RESEARCH.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-PATTERNS.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-VALIDATION.md
@lib/rendro/adapters/oban/render_worker.ex
@lib/rendro/pipeline.ex
@lib/rendro/error.ex
@guides/integrations.md
@test/rendro/policy_test.exs
@test/docs_contract/integrations_claims_test.exs

<interfaces>
Current worker boundary:
```elixir
def perform(%Oban.Job{args: %{"module" => module_str, "args" => args, "output_path" => path}}) do
  module = String.to_existing_atom(module_str)
  doc = module.build_document(args)

  case Rendro.render(doc, output: path) do
    {:ok, _binary} -> :ok
    {:error, error} -> {:error, error.reason}
  end
end
```

Locked phase decisions:
```text
- Accept only explicit `max_pages`, `max_bytes`, `timeout`
- Fill missing document policies only
- Unknown policy keys fail fast
- Invalid worker input must produce typed non-raising failures
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 worker-path proof for policy injection and boundary failures</name>
  <files>test/rendro/adapters/oban/render_worker_test.exs</files>
  <behavior>
    - Prove worker-path injection for `max_pages`, `max_bytes`, and `timeout` when the document lacks those policies.
    - Prove document-authored policies are preserved when the same keys are already present.
    - Prove unknown policy keys and invalid policy values fail with explicit typed worker-boundary errors.
    - Prove malformed required worker fields do not crash the worker path.
  </behavior>
  <action>
    Create `test/rendro/adapters/oban/render_worker_test.exs` as the Wave 0 proof surface required by `15-VALIDATION.md`. Use focused worker-level tests that exercise the `%Oban.Job{args: ...}` boundary directly rather than re-proving the full pipeline internals. Cover the clean injection path, fill-missing precedence, invalid module/output-path/args shapes, invalid policy values, and unknown policy keys. Use stable fixtures or small test helper modules instead of broad mocking; the purpose is to pin the adapter contract, not to duplicate rendering engine tests already covered elsewhere.
  </action>
  <acceptance_criteria>
    - New worker test file exists and runs as the primary proof surface for `ADPT-04`.
    - At least one test covers each supported injected key: `max_pages`, `max_bytes`, and `timeout`.
    - At least one test proves fill-missing semantics by preserving a document-authored policy.
    - Invalid boundary inputs fail as typed tuples, not via raised runtime exceptions.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/adapters/oban/render_worker_test.exs</automated>
  </verify>
  <done>Wave 0 proof exists for the worker-path contract and will fail if policy injection or boundary validation regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Implement strict worker-boundary normalization and fill-missing policy injection</name>
  <files>lib/rendro/adapters/oban/render_worker.ex</files>
  <behavior>
    - The worker validates required fields and accepted policy keys before building or rendering the document.
    - Supported policy values are normalized into `doc.options[:policies]` only when the document has not already set them.
    - The worker does not accept generic pass-through into `doc.options` or `Rendro.render/2`.
    - Render failures still propagate as typed Rendro failures instead of accidental adapter crashes.
  </behavior>
  <action>
    Refactor `lib/rendro/adapters/oban/render_worker.ex` into a small validation/normalization pipeline. Replace the single pattern-match happy path with explicit required-field extraction, safe module resolution, narrow policy parsing, and fill-missing merge logic into `doc.options[:policies]`. Reuse `Rendro.Error` or an equally explicit typed tuple shape for invalid worker inputs so operators can distinguish boundary misuse from render failures. Keep all actual timeout/page/byte enforcement in `Rendro.Pipeline`; the worker only translates external job input into the document contract.
  </action>
  <acceptance_criteria>
    - The worker no longer depends on a single `%{"module" => ..., "args" => ..., "output_path" => ...}` match plus `String.to_existing_atom/1` as its only contract.
    - Supported policy keys are injected only when absent from the document.
    - Unknown policy keys and invalid values fail before render with typed boundary errors.
    - Existing render failures still return `{:error, reason}` from the worker rather than leaking a different public error family.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/adapters/oban/render_worker_test.exs test/rendro/policy_test.exs</automated>
  </verify>
  <done>The async worker restores the bounded-render contract truthfully without changing core policy semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Update async adapter guide text to match the final worker contract</name>
  <files>guides/integrations.md, test/docs_contract/integrations_claims_test.exs</files>
  <behavior>
    - Public docs name the accepted policy surface and precedence behavior truthfully.
    - Docs do not imply silent override, generic pass-through, or a richer async abstraction than the phase actually delivers.
    - Guide text stays consistent with the worker-path tests.
    - The Oban async contract is pinned by an executable docs-contract assertion rather than a keyword-only grep.
  </behavior>
  <action>
    Update `guides/integrations.md` so the Oban integration section documents the final accepted policy shape, fill-missing precedence, and typed boundary-failure posture. Keep the wording narrow and operationally useful. If the guide currently implies broader job-arg flexibility or omits precedence, correct it rather than letting the tests become the only truthful source. Extend `test/docs_contract/integrations_claims_test.exs` with focused assertions for the Oban async contract so execution proves the guide documents only the supported `"policies"` shape, does not claim silent override, and does not imply generic pass-through into Rendro options.
  </action>
  <acceptance_criteria>
    - The guide documents only `max_pages`, `max_bytes`, and `timeout` as worker policy input.
    - The guide states that document-authored policies win when already present.
    - The guide does not claim arbitrary worker metadata or generic option pass-through is supported.
    - Docs-contract coverage fails if the Oban integration text drifts into override or pass-through claims.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/integrations_claims_test.exs</automated>
  </verify>
  <done>The public async integration contract is truthful and aligned with the implemented worker boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oban job args -> Rendro worker | External job input can disable safety bounds or crash the worker if not validated narrowly. |
| Worker normalization -> core pipeline | The adapter must translate inputs into the existing document policy contract without changing core semantics. |
| Guide text -> operators | Public docs can overstate async behavior if they describe a broader contract than the worker actually implements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T | `render_worker.ex` | mitigate | Reject unknown policy keys and invalid values before render with typed boundary failures. |
| T-15-02 | D | `render_worker.ex` | mitigate | Replace crash-prone pattern matches and `String.to_existing_atom/1` reliance with explicit validation and safe module resolution. |
| T-15-03 | T | policy merge logic | mitigate | Use fill-missing semantics only; never silently override document-authored policies. |
| T-15-04 | R | `guides/integrations.md` | mitigate | Keep the async adapter contract explicit and bounded so docs cannot imply unsupported behavior. |
</threat_model>

<verification>
- Run `mix test test/rendro/adapters/oban/render_worker_test.exs`.
- Run `mix test test/rendro/adapters/oban/render_worker_test.exs test/rendro/policy_test.exs`.
- Inspect `guides/integrations.md` for a truthful bounded-policy contract.
</verification>

<success_criteria>
1. The Oban worker validates required inputs and supported policy keys without incidental runtime crashes.
2. `max_pages`, `max_bytes`, and `timeout` are injected only when missing from the document.
3. Worker-path regression tests prove the bounded-async contract directly.
</success_criteria>

<output>
After completion, create `.planning/phases/15-async-policy-injection-timeout-audit-closure/15-01-SUMMARY.md`
</output>
