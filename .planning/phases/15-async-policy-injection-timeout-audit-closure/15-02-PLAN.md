---
phase: "15"
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - lib/rendro/pipeline.ex
  - lib/rendro/adapters/threadline.ex
  - guides/integrations.md
  - test/rendro/telemetry_test.exs
  - test/rendro/adapters/threadline_test.exs
  - test/docs_contract/integrations_claims_test.exs
autonomous: true
requirements:
  - ADPT-05
  - OBS-04
must_haves:
  truths:
    - "A render timeout closes the top-level telemetry lifecycle with a terminal `[:rendro, :render, :stop]` event using the established failed-render schema."
    - "Threadline continues to classify timeout as `:render_failed` under the existing action family and preserves timeout subtype metadata for filtering."
    - "Public docs and docs-contract tests stop claiming timeout audits are absent once the runtime seam is closed."
  artifacts:
    - path: lib/rendro/pipeline.ex
      provides: "Synthetic top-level timeout stop and balanced timeout lifecycle behavior"
    - path: lib/rendro/adapters/threadline.ex
      provides: "Timeout subtype forwarding under the existing audit action family"
    - path: test/rendro/telemetry_test.exs
      provides: "Top-level timeout lifecycle proof with no dangling render span"
    - path: test/rendro/adapters/threadline_test.exs
      provides: "Timeout audit forwarding proof under `:render_failed`"
    - path: test/docs_contract/integrations_claims_test.exs
      provides: "Truth-contract regression proving the guide no longer carries the timeout-audit limitation"
    - path: guides/integrations.md
      provides: "Truthful Threadline timeout/audit contract text"
  key_links:
    - from: lib/rendro/pipeline.ex
      to: lib/rendro/adapters/threadline.ex
      via: "Top-level timeout stop metadata must be sufficient for audit forwarding"
      pattern: "render.*stop|error.*timeout|render_failed"
    - from: test/rendro/telemetry_test.exs
      to: test/rendro/adapters/threadline_test.exs
      via: "Telemetry lifecycle proof pairs with audit forwarding proof"
      pattern: "timeout|render_failed|error"
    - from: guides/integrations.md
      to: test/docs_contract/integrations_claims_test.exs
      via: "Semantic guide claims are pinned by live behavior tests"
      pattern: "Threadline|timeout|render_failed"
---

<objective>
Close the timeout-to-audit half of Phase 15 by emitting a truthful top-level timeout stop event, forwarding timeout subtype metadata through Threadline under the existing failed-render action, and flipping the guide/docs-contract truth surface from limitation to closure.

Purpose: Close `INT-TIMEOUT-AUDIT-HANDOFF` without inventing a new timeout event family, audit action family, or timeout-specific parser contract.
Output: Balanced timeout telemetry, Threadline audit forwarding, and docs/tests that prove the closure end-to-end.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-CONTEXT.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-RESEARCH.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-PATTERNS.md
@.planning/phases/15-async-policy-injection-timeout-audit-closure/15-VALIDATION.md
@lib/rendro/pipeline.ex
@lib/rendro/adapters/threadline.ex
@guides/integrations.md
@test/rendro/telemetry_test.exs
@test/rendro/adapters/threadline_test.exs
@test/docs_contract/integrations_claims_test.exs

<interfaces>
Current timeout gap:
```elixir
case Task.yield(task, timeout) || Task.shutdown(task) do
  {:ok, result} -> result
  nil -> {:error, Error.from_stage(:render, :timeout, base_meta)}
end
```

Current Threadline metadata allowlist:
```elixir
Map.take(metadata, [
  :render_id,
  :stage,
  :status,
  :page_count,
  :byte_size,
  :document_type,
  :deterministic,
  :kind,
  :reason
])
```

Locked timeout semantics:
```text
- Timeout is a normal failed render
- Primary signal is top-level [:rendro, :render, :stop]
- Timeout subtype lives in metadata, not a new event/action family
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeout lifecycle proof and Threadline timeout audit proof before runtime edits drift</name>
  <files>test/rendro/telemetry_test.exs, test/rendro/adapters/threadline_test.exs, test/docs_contract/integrations_claims_test.exs</files>
  <behavior>
    - Timeout tests prove a balanced top-level render lifecycle with a terminal `:stop`.
    - Audit tests prove timeout maps to `:render_failed` and preserves subtype metadata.
    - Docs-contract tests flip from proving absence of timeout audit rows to proving presence of the final truthful contract.
  </behavior>
  <action>
    Extend `test/rendro/telemetry_test.exs`, `test/rendro/adapters/threadline_test.exs`, and `test/docs_contract/integrations_claims_test.exs` to express the intended final behavior explicitly before or alongside runtime edits. Reuse the repo's telemetry helper and ETS-backed Threadline mock patterns. Focus on high-signal closure proof: top-level start plus synthetic timeout stop, no dangling render span, `:render_failed` action for timeout, and timeout subtype metadata visible to audit consumers. The docs-contract suite should stop asserting `Mocks.threadline_calls() == []` for timeout and instead pin the final audit-visible truth.
  </action>
  <acceptance_criteria>
    - Telemetry tests include a timeout-specific top-level stop assertion.
    - Threadline tests include a timeout-specific `:render_failed` assertion with subtype metadata.
    - Docs-contract tests prove the guide's final timeout story instead of the current limitation.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/telemetry_test.exs test/rendro/adapters/threadline_test.exs test/docs_contract/integrations_claims_test.exs</automated>
  </verify>
  <done>Execution now has explicit closure proof targets for timeout telemetry, audit forwarding, and public contract truth.</done>
</task>

<task type="auto">
  <name>Task 2: Emit a synthetic top-level timeout stop with the stable failed-render schema</name>
  <files>lib/rendro/pipeline.ex</files>
  <behavior>
    - A timeout no longer returns before a top-level terminal render event exists.
    - The synthetic timeout stop uses the established metadata schema: `render_id`, `document_type`, `deterministic`, `stage`, `status`, `page_count`, `byte_size`, and nested `error`.
    - The timeout branch does not fabricate misleading duration measurements.
  </behavior>
  <action>
    Refactor the timeout branch in `lib/rendro/pipeline.ex` so the top-level render lifecycle closes cleanly on timeout. Emit a synthetic `[:rendro, :render, :stop]` event in the timeout branch using the stable failed-render metadata shape, then return the existing timeout error. Preserve the architecture contract and keep timeout represented as `error.kind: :timeout` at stage `:render`. Do not introduce a new public timeout event or a fake top-level `:exception` for this path.
  </action>
  <acceptance_criteria>
    - Timeout renders emit a top-level `:stop` before returning the timeout error.
    - Timeout metadata matches the existing failed-render schema and uses nested timeout classification.
    - No timeout-specific action/event family is added.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/telemetry_test.exs</automated>
  </verify>
  <done>Timeout renders become audit-visible top-level failures without widening the telemetry surface.</done>
</task>

<task type="auto">
  <name>Task 3: Forward timeout subtype metadata through Threadline and update the public integration contract</name>
  <files>lib/rendro/adapters/threadline.ex, guides/integrations.md</files>
  <behavior>
    - Threadline keeps the existing action-family contract: success -> `:render_succeeded`, failure -> `:render_failed`.
    - Timeout subtype metadata is preserved in the forwarded payload without widening into a new audit action family.
    - Guide text documents timeout audit visibility truthfully and does not leave the old limitation in place.
  </behavior>
  <action>
    Update `lib/rendro/adapters/threadline.ex` so the PII-safe allowlist preserves the timeout subtype metadata needed by audit consumers, preferably via the nested `:error` field that matches the pipeline schema. If a flat convenience field is added, keep it explicitly secondary to the nested error contract. Then update `guides/integrations.md` so Threadline timeout behavior, failure classification, and metadata-filtering expectations match the final implementation and docs-contract tests.
  </action>
  <acceptance_criteria>
    - Threadline timeout audit rows are still classified as `:render_failed`.
    - Audit payloads expose timeout subtype metadata without requiring a timeout-specific parser path.
    - The guide no longer describes timeout audits as unsupported or absent.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/adapters/threadline_test.exs test/docs_contract/integrations_claims_test.exs</automated>
    <automated>rg -n "Threadline|timeout|render_failed" guides/integrations.md</automated>
  </verify>
  <done>Timeout failures are visible through the existing audit surface and the public docs tell the same story as the runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline timeout branch -> telemetry consumers | A timeout can leave the top-level lifecycle incomplete if the branch returns before emitting a terminal event. |
| Telemetry metadata -> Threadline audit payload | Metadata allowlists can either drop the timeout subtype or accidentally widen the audit payload shape unsafely. |
| Runtime behavior -> guide/docs-contract claims | Public docs become false if the timeout-audit limitation remains after the runtime seam is closed, or if docs claim more than the code emits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | R | `pipeline.ex` | mitigate | Emit a terminal top-level timeout stop so failed renders always leave auditable lifecycle evidence. |
| T-15-06 | T | `threadline.ex` | mitigate | Preserve nested timeout subtype metadata under the existing `:render_failed` action instead of inventing a new failure surface. |
| T-15-07 | I | `threadline.ex` | mitigate | Keep the payload PII-safe by forwarding only stable render metadata and the nested error subtype, not document content. |
| T-15-08 | R | `guides/integrations.md`, docs-contract tests | mitigate | Flip the limitation proof into closure proof so docs and runtime cannot drift silently. |
</threat_model>

<verification>
- Run `mix test test/rendro/telemetry_test.exs`.
- Run `mix test test/rendro/adapters/threadline_test.exs test/docs_contract/integrations_claims_test.exs`.
- Inspect `guides/integrations.md` for truthful timeout-audit language.
</verification>

<success_criteria>
1. Timeout renders close the top-level telemetry lifecycle with a terminal failed-render stop.
2. Threadline records timeout failures under `:render_failed` with usable subtype metadata.
3. Docs and docs-contract tests move from timeout-limitation truth to timeout-closure truth.
</success_criteria>

<output>
After completion, create `.planning/phases/15-async-policy-injection-timeout-audit-closure/15-02-SUMMARY.md`
</output>
