---
phase: 129-docs-manifest-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 129-02
files_modified:
  - priv/public_api.json
  - scripts/verify_docs.exs
  - test/guardrails/required_checks_contract_test.exs
  - priv/guardrails/required_status_checks.json
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "`priv/public_api.json` is generator output and proves `Rendro.Theme.preset/2` at its existing adapter tier/shape without adding or promoting another public symbol (D-23)."
    - "Exactly one preset public-claims lane owns cross-surface coherence while existing catalog, configurator, theming, accessibility, and API lanes retain narrower behavioral ownership (D-24)."
    - "The lane rejects missing proof, snippet/link drift, forbidden guarantees, and misleading preview/quality terms as well as asserting required positive language (D-25)."
    - "The registry, exact 27-lane guard, and deterministic test-context manifest change in one task commit, with `ci-success` unchanged and browser/raster evidence left advisory or graph-disconnected as already designed (D-26)."
    - "`mix ci.fast` closes package construction, evaluated examples, ExDoc warnings, API/support drift, 27-lane accounting, links/assets, and no-overclaim predicates end to end (D-27)."
  artifacts:
    - path: "priv/public_api.json"
      provides: "Generator-reconciled Theme.preset/2 adapter-tier public API manifest"
    - path: "scripts/verify_docs.exs"
      provides: "Exactly 27 deterministic docs-contract lanes including the preset public-claims lane"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Exact registry/CI topology lockstep proof"
    - path: "priv/guardrails/required_status_checks.json"
      provides: "Truthful 27-lane deterministic test-context description"
  key_links:
    - from: "lib/rendro/theme.ex"
      to: "priv/public_api.json"
      via: "mix rendro.api.gen deterministic introspection"
      pattern: "preset/2"
    - from: "scripts/verify_docs.exs"
      to: "test/docs_contract/presets_claims_test.exs"
      via: "single registered test tuple inside the existing deterministic docs runner"
      pattern: "Preset public-claims lane"
    - from: "test/guardrails/required_checks_contract_test.exs"
      to: "priv/guardrails/required_status_checks.json"
      via: "exact 27-lane count and unchanged ci-success/required-vs-advisory topology assertions"
      pattern: "27"
  prohibitions:
    - statement: "Public preset documentation must not disguise a representative catalog raster as the reader's exact requested accent or complete-document evidence."
      status: unverified
      flagged: true
    - statement: "Public quality language must not turn needs-work or unscored evidence into approval, certification, or an undifferentiated quality badge."
      status: unverified
      flagged: true
    - statement: "Documentation must not collapse deterministic, pinned-raster, human-review, and claim-contract evidence into a visual-quality, accessibility, PDF/UA, WCAG, print-safety, or universal-viewer guarantee."
      status: unverified
      flagged: true
---

<objective>
Reconcile the generated API manifest and close the deterministic docs registry/guardrail transaction with the complete CI gate.

Purpose: Make the milestone's final public claim surface auditable from code to manifests to required checks without creating a new CI status or conflating advisory evidence.
Output: Fresh public API manifest, 27-lane registry, matching guardrails, and green `mix ci.fast`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/129-docs-manifest-closure/129-CONTEXT.md
@.planning/phases/129-docs-manifest-closure/129-RESEARCH.md
@.planning/phases/129-docs-manifest-closure/129-VALIDATION.md
@.planning/phases/129-docs-manifest-closure/129-02-SUMMARY.md
@lib/mix/tasks/rendro/api.gen.ex
@test/docs_contract/public_api_contract_test.exs
@scripts/verify_docs.exs
@test/guardrails/required_checks_contract_test.exs
@priv/guardrails/required_status_checks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and prove the public API manifest</name>
  <files>priv/public_api.json</files>
  <read_first>
    - lib/rendro/theme.ex
    - lib/mix/tasks/rendro/api.gen.ex
    - priv/public_api.json
    - priv/schemas/public_api.schema.json
    - test/docs_contract/public_api_contract_test.exs
  </read_first>
  <action>
    Run `mix rendro.api.gen` as the sole writer of `priv/public_api.json` per D-23; do not hand-edit the JSON. Inspect the resulting deterministic diff and require `Rendro.Theme.preset/2` to remain in the existing adapter tier with its actual arity/shape. If generation is byte-identical, record that regeneration evidence in the task summary rather than inventing a manifest change. Reject any unrelated new module/function, tier promotion, Theme API broadening, or edit to the generator/test contract.
  </action>
  <acceptance_criteria>
    - A fresh generator run is byte-identical to the committed manifest after the task.
    - `Theme.preset/2` appears exactly once with the correct existing adapter-tier representation.
    - Existing schema validation, hidden-internal checks, and two-list drift diagnostics remain green.
    - No public surface changes beyond reconciliation are present.
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.api.gen &amp;&amp; mix test test/docs_contract/public_api_contract_test.exs --max-failures 1 &amp;&amp; git diff --exit-code -- priv/public_api.json</automated>
  </verify>
  <done>The checked-in API manifest is proven generator-fresh and truthfully includes only the already-shipped `Theme.preset/2` surface at its established tier.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the 27-lane registry and guardrail closure as one transaction</name>
  <files>scripts/verify_docs.exs, test/guardrails/required_checks_contract_test.exs, priv/guardrails/required_status_checks.json</files>
  <read_first>
    - test/docs_contract/presets_claims_test.exs
    - scripts/verify_docs.exs
    - test/guardrails/required_checks_contract_test.exs
    - priv/guardrails/required_status_checks.json
    - .github/workflows/ci.yml
    - mix.exs
  </read_first>
  <action>
    Perform D-26 as one atomic task commit: append exactly one `Preset public-claims lane` tuple pointing to `test/docs_contract/presets_claims_test.exs` inside the formatter-off registry; update the guardrail test description, exact count from 26 to 27, and regex proving the new tuple occurs once; update only the stale docs-lane count prose in the deterministic `test` context notes to 27. Preserve `required_contexts: ["ci-success"]`, the existing `mix ci` command, the existing required integration topology, and all advisory/browser/raster separation. Run the new lane alone, the registry guard, the complete four-file quick contract, and finally D-27 `mix ci.fast`; fix only Phase-129-caused failures within the authorized docs/manifest scope. Commit all three accounting files together and do not split them across commits.
  </action>
  <acceptance_criteria>
    - The registry has exactly 27 docs-contract tuples and exactly one points to the preset claims test.
    - The exact-count/tuple guard and status-manifest prose both say 27 and change in the same task commit as the registry.
    - No new GitHub required status is introduced; `ci-success` remains the sole required context and advisory/browser/raster work is not moved into deterministic docs execution.
    - `mix ci.fast` passes package, compiled/evaluated docs examples, ExDoc warnings, public API/support drift, all docs lanes, links/assets, formatting, Credo, and Dialyzer.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/presets_claims_test.exs test/docs_contract/theming_claims_test.exs test/docs_contract/public_api_contract_test.exs test/guardrails/required_checks_contract_test.exs --max-failures 1 &amp;&amp; mix run scripts/verify_docs.exs &amp;&amp; mix ci.fast</automated>
  </verify>
  <done>The preset claims lane is the 27th deterministic docs contract, every accounting surface agrees in one commit, and the milestone closes with the complete required CI lane green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 applicability

| ASVS area | Applies | Phase control / rationale |
|-----------|---------|---------------------------|
| V2 Authentication | no | Generated manifests and CI registry files are static local configuration with no user identity surface. |
| V3 Session Management | no | No browser/server session, cookie, or token lifecycle is introduced. |
| V4 Access Control | no | No authorization decision or privileged application route changes; repository write authority remains outside phase scope. |
| V5 Validation, Sanitization and Encoding | limited | There is no external/user input path; generator output, JSON schemas, exact tuple regexes, and deterministic tests validate the fixed project-owned configuration inputs. |
| V14 Configuration | yes | Generator-only API reconciliation, exact 27-lane accounting, same-commit guardrails, and unchanged required/advisory topology fail closed on configuration drift. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled public modules → generated manifest | Introspection output defines the committed public API inventory. |
| docs test file → required lane registry | A new semantic contract becomes required deterministic execution. |
| registry → guardrail/status manifest | Public CI claims must match executable lane accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-08 | Tampering | `priv/public_api.json` | medium | mitigate | Allow only `mix rendro.api.gen` to write it and prove byte equality with the existing drift test. |
| T-129-09 | Denial of service | docs lane registry/count mismatch | medium | mitigate | Exact 27 count, exact tuple regex, same-task commit, and full runner execution. |
| T-129-10 | Repudiation | required/advisory CI classification | medium | mitigate | Assert `ci-success` remains sole required context and compare manifest/workflow topology in the existing guardrail suite. |
| T-129-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm, Hex, pip, or cargo package; it uses only already-locked project tools. |
</threat_model>

<verification>
- Require the task-specific commands after each commit.
- Run `mix ci.fast` after all phase files settle; no narrower command substitutes for D-27.
- Verify the D-26 commit contains all three accounting files together.
- Keep the unresolved DOCS-01 probe assumption and all three flagged-unverified prohibitions visible for phase verification.
</verification>

<success_criteria>
- DOCS-01 is closed from reader copy through support/API manifests, package/ExDoc delivery, registry accounting, and required CI.
- D-23 through D-27 are implemented exactly, with no public API expansion and no new required status context.
- Every new milestone surface is described only at the evidence level it actually earned.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Reconcile presets, catalog, and configurator into proof-backed public claims without overclaim | 01-03 | COVERED | Tracer, reader/package expansion, and manifest/CI closure. |
| REQ | DOCS-01 | Support row, preset guide, API manifest, README/HexDocs, docs/guardrails lockstep | 01-03 | COVERED | Every named artifact and final CI gate is explicit. |
| CONTEXT | D-01–D-04 | Dedicated guide, focused Theming, no portal, current brand precedence | 02 | COVERED | Task 1 and truths. |
| CONTEXT | D-05–D-11 | Golden path, formatter ownership, six-row chooser, distinct progressive jobs, outcomes first | 01-02 | COVERED | Tracer plus complete journey. |
| CONTEXT | D-12–D-18 | Layered truth, exact boundaries/disclosures/labels, support semantics, evidence separation, accessible docs | 01-02 | COVERED | Claims row/test and reader expansion. |
| CONTEXT | D-19–D-22 | Compact README route, outcome links, Guides placement, source/package/ExDoc integrity | 02 | COVERED | Both tasks. |
| CONTEXT | D-23–D-27 | Generated API, one fail-both-ways lane, atomic 27-lane accounting, full CI | 03 | COVERED | Both tasks. |
| RESEARCH | — | Formatter-owned executable docs and mutation teeth | 01 | COVERED | Byte equality, evaluation, non-vacuity, and synthetic failures. |
| RESEARCH | — | Additive support row with complete capability/boundary split | 01 | COVERED | No schema expansion. |
| RESEARCH | — | Narrow package closure for configurator/catalog/tokens plus private exclusions | 02 | COVERED | Tarball and generated-doc checks. |
| RESEARCH | — | Use measured README/guide links, root ExDoc extra pages, copied asset paths, and the absolute HexDocs configurator URL | 02 | COVERED | Task 1 writes the exact four links; Task 2 builds a temporary ExDoc tree and asserts every resolved route/asset. |
| RESEARCH | — | Add only the measured catalog/configurator/token CSS package paths and retain private/token-source exclusions | 02 | COVERED | Task 2 names the exact four additions and every required negative tarball assertion. |
| RESEARCH | — | Generated API and existing 26→27 deterministic lane topology | 03 | COVERED | Generator-only and same-commit transaction. |
| VALIDATION | Wave 0 | Create `test/docs_contract/presets_claims_test.exs` before dependent edits | 01 | COVERED | Leading tracer creates and greens it. |

Excluded without gap: the CONTEXT deferred standalone hub/Studio, new presets/catalog cells/arbitrary previews/exhaustive review/server work; REQUIREMENTS items assigned to Phases 125–128; RESEARCH out-of-scope product changes. The external-API detector returned `detected:false`, the assumption-delta scan returned `detected:false`, and no configured ORM schema path matched phase scope, so no coverage declaration, assumption promotion, or schema-push task is emitted.

## Spec-less Probe Coverage Audit

| Requirement | Probe | Authored disposition |
|-------------|-------|----------------------|
| DOCS-01 | unclassified | Flagged unresolved assumption retained in Plan 01 `must_haves.truths`; D-25/VALIDATION fail-both-ways predicates provide concrete checks but do not resolve or silently backstop the probe. |

No-silent-drop equality: one applicable unresolved edge prompt equals one explicit flagged assumption. Three precision-surviving product-transparency prohibitions equal the three descriptor-less flagged-unverified `must_haves.prohibitions` entries repeated across the execution plans. Routine link correctness/package hygiene is planned as normal engineering; safe-DOM/input-validation and generic injection concerns are routed to the threat model/existing security lanes rather than minted as extra prohibitions.

## Artifacts this phase produces

- Reconciled generated manifest path: `priv/public_api.json`, containing the existing adapter-tier `Rendro.Theme.preset/2` representation.
- New deterministic lane label/path: `Preset public-claims lane` → `test/docs_contract/presets_claims_test.exs` in `scripts/verify_docs.exs`.
- Updated guardrail invariant: exactly 27 docs-contract tuples in `test/guardrails/required_checks_contract_test.exs`.
- Updated public guardrail manifest fact: deterministic `test` context notes state 27 docs-contract lanes; `required_contexts` remains `ci-success` only.
- CLI-visible verification paths used, not created: `mix rendro.api.gen`, `mix run scripts/verify_docs.exs`, and `mix ci.fast`.
- No new public module/function, CI status context, package dependency, external service, ORM schema, server, or docs application.

<output>
Create `.planning/phases/129-docs-manifest-closure/129-03-SUMMARY.md` when done.
</output>
