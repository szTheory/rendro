---
phase: 129-docs-manifest-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/presets.md
  - priv/support_matrix.json
  - test/docs_contract/presets_claims_test.exs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "The canonical first-success path is Invoice × Swiss × #2C6BED × light, with the formatter-owned source, recipe call, and explicit document-owned font registration preserved byte-for-byte (D-06, D-07)."
    - "Immediately after that example, the guide carries the exact strong-starting-point and no-guarantee boundary, while the support matrix separates supported capabilities from unsupported guarantees (D-12, D-13, D-16)."
    - "Deterministic bytes, bounded catalog previews, human quality dispositions, and documentation claims remain separate evidence levels; neither 504 valid snippets nor a page-one raster becomes broad visual proof (D-17)."
    - "The dedicated claims lane fails when a proof row/key is absent, a forbidden guarantee is promoted, or the formatter-owned example drifts, and its own synthetic mutations prove those predicates have teeth (D-24, D-25)."
    - "FLAGGED UNRESOLVED EDGE ASSUMPTION DOCS-01/unclassified: the spec-less probe supplied no narrower category; missing proof rows, forbidden guarantees, broken public links/package omissions, and formatter-snippet drift are the explicit fail-both-ways predicates, pending verifier confirmation that they exhaust the unclassified edge."
  artifacts:
    - path: "guides/presets.md"
      provides: "Canonical preset first-success path and adjacent truth boundary"
      contains: "rendro-theme-snippet:start"
    - path: "priv/support_matrix.json"
      provides: "Canonical proof-backed theming.presets public claim row"
      contains: "theming"
    - path: "test/docs_contract/presets_claims_test.exs"
      provides: "Fail-loud semantic contract for the guide, formatter source, and support row"
      contains: "Rendro.DocsContract.PresetsClaimsTest"
  key_links:
    - from: "guides/presets.md"
      to: "lib/rendro/theme/snippet.ex"
      via: "marker-bounded guide source equals usage_snippet/4 for Invoice/Swiss/#2C6BED/light"
      pattern: "rendro-theme-snippet"
    - from: "priv/support_matrix.json"
      to: "test/docs_contract/presets_claims_test.exs"
      via: "exact capability and boundary-key assertions plus missing/promoted synthetic mutations"
      pattern: "theming.presets"
  prohibitions:
    - statement: "Public preset documentation must not disguise a representative catalog raster as the reader's exact requested accent or complete-document evidence."
      status: unverified
      flagged: true
    - statement: "Public quality language must not turn needs-work or unscored evidence into approval, certification, or an undifferentiated quality badge."
      status: unverified
      flagged: true
    - statement: "Documentation must not collapse deterministic, pinned-raster, human-review, and claim-contract evidence into a visual-quality, accessibility, PDF/UA, WCAG, print-safety, or universal-viewer guarantee."
      status: unverified
      flagged: true
---

<objective>
Prove one complete public-claim path from canonical preset source through reader documentation to the support matrix and a fail-loud contract.

Purpose: Establish the production-quality vertical tracer that later documentation and packaging expansion can reuse without inventing another source or claim model.
Output: The canonical preset guide entry, the complete `theming.presets` support row, and the Wave-0 claims contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/129-docs-manifest-closure/129-CONTEXT.md
@.planning/phases/129-docs-manifest-closure/129-RESEARCH.md
@.planning/phases/129-docs-manifest-closure/129-PATTERNS.md
@.planning/phases/129-docs-manifest-closure/129-VALIDATION.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-01-SUMMARY.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-04-SUMMARY.md
@lib/rendro/theme/snippet.ex
@guides/livebook/first_invoice.livemd
@test/docs_contract/theming_claims_test.exs
@priv/support_matrix.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one proof-backed preset claim from formatter to guide to support matrix</name>
  <reversibility rating="costly">D-01 and D-16 publish a guide URL and canonical support-row contract that later consumers will link and test.</reversibility>
  <files>guides/presets.md, priv/support_matrix.json, test/docs_contract/presets_claims_test.exs</files>
  <read_first>
    - lib/rendro/theme/snippet.ex
    - guides/livebook/first_invoice.livemd
    - test/docs_contract/theming_claims_test.exs
    - test/rendro/theme/snippet_test.exs
    - priv/support_matrix.json
    - priv/schemas/support_matrix.schema.json
    - test/support/realistic_fixtures.ex
  </read_first>
  <behavior>
    - The marked guide body equals `Rendro.Theme.Snippet.usage_snippet("invoice", "swiss", "#2C6BED", "light")` byte-for-byte and parses/evaluates with the realistic Invoice fixture.
    - The resulting document uses `Rendro.Theme.preset/2` and `Rendro.Theme.Presets.register_fonts/2` in the locked order.
    - `theming.presets` exposes all six D-16 capabilities as `supported` and every locked guarantee boundary as `unsupported`.
    - Removing a capability/proof key or promoting a boundary makes the predicate fail; empty key/term lists are rejected as vacuous.
  </behavior>
  <action>
    Create the Wave-0 `Rendro.DocsContract.PresetsClaimsTest` first, following the existing async semantic-contract style and reusable recursive-leaf predicate from the theming lane. Then create `guides/presets.md` with a semantic heading, an outcome-first introduction, the D-06 Invoice/Swiss/#2C6BED/light formatter block between the existing Livebook marker names, and the exact D-13 boundary immediately after it. Add `theming.presets` beside `light` and `dark`, using the research-approved capability keys `strict_six_preset_constructors`, `deterministic_rendering`, `explicit_font_registration`, `bounded_catalog_previews`, `canonical_snippet_and_codegen`, and `focused_livebook_example`; add unsupported boundaries for design quality, accessibility/PDF-UA, WCAG contrast, print safety, universal viewer fidelity, exhaustive catalog coverage, and live arbitrary preview. Tie each capability to a real existing artifact in test assertions rather than adding unproved prose or changing the support schema. Keep D-12/D-17 evidence classes distinct and preserve the three flagged prohibitions in plan verification.
  </action>
  <acceptance_criteria>
    - `guides/presets.md` contains one marker-bounded canonical example and the exact D-13 adjacent boundary.
    - `priv/support_matrix.json` validates and contains the complete D-16 supported/unsupported split without modifying `theming.light` or `theming.dark` semantics.
    - The claims test proves formatter equality, parse/evaluation, support-row presence, non-vacuity, missing-key failure, and forbidden-promotion failure.
    - The test does not claim that 504 snippets, page-one previews, or human dispositions are equivalent evidence.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/presets_claims_test.exs test/docs_contract/theming_claims_test.exs test/rendro/theme/snippet_test.exs --max-failures 1</automated>
  </verify>
  <done>A real reader can execute the canonical preset path, its complete claim boundary is machine-readable, and the dedicated contract fails both when required proof disappears and when an unsupported guarantee is promoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formatter → Markdown | Canonical generated Elixir becomes public copy-paste source. |
| evidence artifacts → support matrix | Internal proof is summarized as public support status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-01 | Tampering | `theming.presets` claim leaves | medium | mitigate | Assert every capability/boundary key and make synthetic missing/promoted fixtures fail. |
| T-129-02 | Repudiation | guide source provenance | medium | mitigate | Byte-compare the marked example to `Theme.Snippet`, then parse and evaluate it. |
| T-129-03 | Information disclosure | guide/support row | low | accept | Only already-public APIs, paths, and bounded evidence labels are exposed; private rubric detail remains outside the row. |
</threat_model>

<verification>
- Run the focused tracer command after the task commit.
- Confirm the unresolved DOCS-01 edge and all three descriptor-less prohibitions remain present in frontmatter.
- Validate JSON through the existing support-matrix contract reached by the focused suite.
</verification>

<success_criteria>
- The tracer is green end to end and becomes the source of truth for subsequent documentation expansion.
- D-06, D-07, D-12, D-13, D-16, D-17, D-24, and D-25 are implemented without broadening public API or visual claims.
</success_criteria>

## Artifacts this phase produces

- New public file/HexDocs route: `guides/presets.md`.
- New test module/file: `Rendro.DocsContract.PresetsClaimsTest` in `test/docs_contract/presets_claims_test.exs`.
- New manifest row: `theming.presets` with `status`, six `capabilities` keys, and seven `boundaries` keys listed in Task 1.
- No new runtime symbol, dependency, server, build system, package install, or external API surface.

<output>
Create `.planning/phases/129-docs-manifest-closure/129-01-SUMMARY.md` when done.
</output>
