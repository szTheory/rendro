---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/examples_data.ex
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes/statement.ex
  - lib/rendro/recipes/receipt.ex
  - lib/rendro/recipes/certificate.ex
  - lib/rendro/recipes/payslip.ex
  - lib/rendro/recipes/ticket.ex
  - priv/examples/invoice/acme-phoenix-saas/invoice.json
  - priv/examples/statement/northwind-ledger-co/statement.json
  - priv/examples/receipt/harbor-and-oak-cafe/receipt.json
  - priv/examples/certificate/summit-training-institute/certificate.json
  - priv/examples/payslip/aurora-live/payslip.json
  - priv/examples/ticket/aurora-live/ticket.json
autonomous: true
gap_closure: true
requirements: [SHOW-01]

must_haves:
  truths:
    - "The invoice demo renders an issuer block, a customer/bill-to block, and a totals block whose amount-due (Total) is the single dominant element on the page (SHOW-01, per D-11)."
    - "Every money value in the invoice render shows faithful 2-decimal cents (no `$79.0` one-decimal defect anywhere), and the invoice money path in examples_data.ex uses no Decimal.to_float/1 or Decimal.to_integer/1."
    - "The invoice fixture carries a realistic handful of DISTINCT SaaS line items (e.g. plan, seats, add-ons) reconciling to subtotal/tax/total — never 36 identical filler rows."
    - "The statement demo surfaces the closing balance as a dominant boxed summary element (not merely the last cell of the running-balance column) so content_hierarchy can honestly reach 5."
    - "The receipt demo renders the merchant identity (Harbor & Oak Cafe) and makes the Total the dominant element."
    - "The certificate demo centers content within the border and makes the recipient name the dominant element (body measure constrained)."
    - "The payslip earnings/deductions table is de-crowded so headers no longer collide ('YTDDeductions') and YTD figures do not wrap a digit, while the Net Pay box stays dominant (content_hierarchy = 5 preserved)."
    - "The ticket renders at native A6 (or a canvas sized to content) with the seat-locator group as the clear focal point — no ~65% empty A4 canvas."
  artifacts:
    - "lib/rendro/examples_data.ex — transform_invoice/1 passes issuer/customer/due_date/terms/totals through to the recipe; the invoice price/money coercion is faithful (no lossy float/integer conversion)."
    - "lib/rendro/recipes/{invoice,statement,receipt,certificate,payslip,ticket}.ex — each recipe renders its family's key fact as the dominant element per 118-06-FINDINGS.md."
    - "priv/examples/{invoice,statement,receipt,certificate,payslip,ticket} fixtures — realistic, fictional-only, Decimal-string money, valid against examples.schema.json."
  key_links:
    - "transform_invoice/1 -> Rendro.Recipes.Invoice.document/2: parties + totals must reach the recipe (the recipe already renders issuer_block/customer_block/build_totals_blocks when present — the gap is upstream in the transform, not the recipe)."
    - "transform_receipt/1 -> Rendro.Recipes.Receipt.document/2: merchant identity must be threaded fixture -> transform -> recipe (currently no merchant field exists in the receipt data path)."
  prohibitions:
    - "D-11: rubric passing is EARNED, never assigned. This plan improves demo composition ONLY. Scores are recorded honestly in the separate re-score plan (118-09). A demo that cannot honestly reach hierarchy=5 / cores>=4 / both gates after this rework is surfaced as a finding — never scored up."
    - "Money must remain exact (Decimal). Do not introduce Decimal.to_float/1 or Decimal.to_integer/1 into any invoice/demo money path (INV-02 doctrine)."
    - "Fixtures are fictional-only, no real PII (Payslip is the acute risk); money stays as decimal strings in JSON, never JSON floats (D-03)."
---

<objective>
Close the SHOW-01 gap by reworking the six family×domain demonstration documents so each one can HONESTLY clear the reader-quality rubric (content_hierarchy = 5, every other core dimension >= 4, both gates pass). This plan owns the recipe / transform / fixture composition work only; the honest re-render and re-score happen in plan 118-09.

Verification found Phase 118 at 3/4: SHOW-02/03/04 pass; SHOW-01 fails because all six demos in priv/quality/rubric_scores.json are `passed:false` (recorded honestly per D-11). The per-demo remediation is catalogued in 118-06-FINDINGS.md; this plan lifts each finding into a concrete composition fix.

Purpose: make each demo's single key fact visually dominant and each family conventionally complete, so passing is EARNED rather than assigned.
Output: enriched transform_invoice/1, faithful invoice money, six reworked recipe compositions + fixtures.

Scope note: Only SHOW-01 is addressed. SHOW-02 (118-07), SHOW-03 (118-03/04/05), and SHOW-04 (118-07) are already verified and are NOT re-planned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-FINDINGS.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@priv/quality/rubric_scores.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invoice demo overhaul — enrich transform, faithful money, amount-due dominant</name>
  <files>lib/rendro/examples_data.ex, lib/rendro/recipes/invoice.ex, priv/examples/invoice/acme-phoenix-saas/invoice.json, test/rendro/examples_data_test.exs, test/rendro/recipes/invoice_test.exs, test/rendro/recipes/invoice_byte_identity_test.exs</files>
  <read_first>
    - lib/rendro/examples_data.ex (transform_invoice/1 lines 32-50; bare_money/1 line 191 — the Decimal.to_float defect)
    - lib/rendro/recipes/invoice.ex (issuer_block/customer_block/due_date_block/terms_block already exist ~310-330; build_totals_blocks ~336-353; body_section frozen `$#{item.price}` cell ~218-273; validate_item_price! rejects %Decimal{} and requires is_number ~565-591)
    - priv/examples/invoice/acme-phoenix-saas/invoice.json (the fixture feeding the demo)
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-FINDINGS.md (invoice section, lines 47-61 — the primary remediation source)
    - test/rendro/examples_data_test.exs, test/rendro/recipes/invoice_test.exs, test/rendro/recipes/invoice_byte_identity_test.exs (existing behavior to keep green / update)
  </read_first>
  <action>
    Fix the biggest gap (invoice IA/CH/DF=2). Four moves per 118-06-FINDINGS.md:
    (a) In transform_invoice/1 (lib/rendro/examples_data.ex), pass issuer, customer, due_date, terms, and totals through to the recipe — currently it forwards only id/date/items, so Rendro.Recipes.Invoice's existing issuer_block/customer_block/build_totals_blocks never fire. Coerce totals money via the faithful money/1 helper (Decimal.new/1), dates via date/1, keeping the atom-keyed shapes Invoice.validate_data!/1 requires (:issuer/:customer maps, :due_date Date, :terms string, :totals with Decimal :subtotal/:tax/:total).
    (b) Make amount due (Total) the dominant element so content_hierarchy honestly reaches 5 — the invoice reader's one first-needed fact. Achieve dominance through the recipe's totals rendering (emphasis/size on the Total line relative to everything else); every other element must recede in proportion (rubric CH=5 anchor).
    (c) Eliminate the `$79.0` one-decimal money defect. Money must render with faithful 2-decimal cents everywhere it appears. Remove Decimal.to_float/1 from bare_money/1; the invoice money path must not use Decimal.to_float/1 or Decimal.to_integer/1. NOTE the real tension: the recipe's frozen body cell interpolates `"$#{item.price}"` and validate_item_price! requires a bare number while rejecting %Decimal{} — a bare float cannot render faithful cents. Resolve honestly: route faithful cents through the totals block (Rendro.Format.money/1 already yields 2 decimals) and/or adjust the invoice recipe's legacy line-price rendering so no value renders with false/one-decimal precision. Adjusting the recipe's frozen INV-01 line is authorized gap-closure (D-11 reaches into recipe scope) — if you do, update invoice_byte_identity_test.exs / invoice_test.exs expectations accordingly; money stays exact (Decimal), never float.
    (d) In the invoice fixture, replace the 36 identical "$79.00 Monthly platform service" rows with a realistic handful of DISTINCT SaaS line items (e.g. platform plan, per-seat licenses, an add-on) that reconcile to subtotal/tax/total. Fictional-only; money as decimal strings matching examples.schema.json.
    Do NOT touch rubric_scores.json — scoring is 118-09's job (D-11).
  </action>
  <behavior>
    - transform_invoice/1 output includes :issuer, :customer, :due_date, :totals keys with the recipe's required types.
    - Rendering the invoice demo produces a page with issuer, bill-to, and a dominant Total/amount-due; no one-decimal money.
    - Invoice fixture validates against priv/schemas/examples.schema.json and its distinct line items sum to the declared totals (Invoice.validate_data!/1 subtotal/total assertions pass).
  </behavior>
  <verify>
    <automated>mix test test/rendro/examples_data_test.exs test/rendro/recipes/invoice_test.exs test/rendro/recipes/invoice_byte_identity_test.exs test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Decimal\.to_float|Decimal\.to_integer' lib/rendro/examples_data.ex` returns no hit on the invoice money path (target: 0 matches, or none reachable from transform_invoice/1).
    - transform_invoice/1 returns a map containing :issuer, :customer, :due_date, :terms, and :totals (with Decimal money), verified by an examples_data_test.exs assertion.
    - The invoice fixture contains distinct line items (not 36 identical rows) and validates against examples.schema.json (examples_schema_contract_test.exs green).
    - No rendered invoice money string matches a one-decimal pattern (e.g. `$79.0` with a single trailing decimal digit).
    - `mix test` for the four files above exits 0.
  </acceptance_criteria>
  <done>Invoice demo renders parties + totals with amount-due dominant over realistic distinct line items; money is faithful 2-decimal with no float/integer coercion; existing invoice tests are green (updated if the frozen line-price path was re-blessed).</done>
</task>

<task type="auto">
  <name>Task 2: Statement, receipt, certificate — make the key fact dominant</name>
  <files>lib/rendro/recipes/statement.ex, lib/rendro/recipes/receipt.ex, lib/rendro/recipes/certificate.ex, lib/rendro/examples_data.ex, priv/examples/statement/northwind-ledger-co/statement.json, priv/examples/receipt/harbor-and-oak-cafe/receipt.json, priv/examples/certificate/summit-training-institute/certificate.json</files>
  <read_first>
    - lib/rendro/recipes/statement.ex (data contract lines 19-53; closing_balance is optional/derived; running-balance fold)
    - lib/rendro/recipes/receipt.ex (customer + lines + optional totals; currently no merchant/issuer render — the "Sales Receipt / Walk-in Guest" render)
    - lib/rendro/recipes/certificate.ex (title/recipient/date, optional body/seal_line, border: true keyline; the top-20%/empty-80% layout)
    - lib/rendro/examples_data.ex (transform_statement/1 already passes closing_balance/summary; transform_receipt/1 passes title/date/customer/lines/totals but NO merchant — add one here)
    - priv/examples/statement/northwind-ledger-co/statement.json, priv/examples/receipt/harbor-and-oak-cafe/receipt.json, priv/examples/certificate/summit-training-institute/certificate.json
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-FINDINGS.md (statement/receipt/certificate sections lines 63-81)
    - test/rendro/recipes/statement_test.exs, receipt_test.exs, certificate_test.exs
  </read_first>
  <action>
    Three targeted "make the one key fact dominant" fixes (rubric CH=5 anchor), per 118-06-FINDINGS.md:
    - STATEMENT: surface the closing balance as a dominant boxed summary element (like the payslip Net Pay box), not merely the last cell of the Balance column. The fixture/transform already carry closing_balance; add a dominant closing-balance summary render in statement.ex. Keep the running-balance ledger. Optional (deferred-items fold-in): if it improves realism/domain_fit, restore realistic glyphs (em-dash, middle-dot account masking) via the same with_unicode_fallback_font/1 (B612) + glyph_safe/1 treatment payslip used in 116-02, instead of the ASCII workaround.
    - RECEIPT: render the merchant identity (Harbor & Oak Cafe) — currently absent from the data path. Thread merchant fixture -> transform_receipt/1 (add a merchant/issuer passthrough, string map) -> receipt.ex header render. Make the Total the dominant element (not small bottom text). Keep the Subtotal/Tax/Total ladder.
    - CERTIFICATE: center content vertically and horizontally within the border (the lower ~80% is currently empty), make the recipient name the dominant element (larger/bolder than the title), and constrain the body paragraph measure so it does not run edge-to-edge.
    All money stays Decimal via money/1; fixtures stay fictional-only with decimal-string money.
    Do NOT touch rubric_scores.json (D-11 — scoring is 118-09).
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/statement_test.exs test/rendro/recipes/receipt_test.exs test/rendro/recipes/certificate_test.exs test/rendro/examples_data_test.exs test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - statement.ex renders a dominant closing-balance summary element sourced from :closing_balance (assertable in statement_test.exs).
    - transform_receipt/1 forwards a merchant identity, and receipt.ex renders it; the merchant name (Harbor & Oak Cafe) appears in the receipt render.
    - receipt.ex renders the Total as the dominant element (larger and/or heavier weight than the line-item rows and the Subtotal/Tax lines) — not small bottom text (assertable in receipt_test.exs).
    - certificate.ex renders the recipient name as the dominant element with content centered within the border.
    - All three fixtures validate against examples.schema.json (examples_schema_contract_test.exs green).
    - `mix test` for the files above exits 0.
  </acceptance_criteria>
  <done>Statement shows a dominant closing-balance box, receipt shows merchant identity with a dominant Total, certificate centers content with the recipient name dominant — each family's one key fact is now the clear focal point.</done>
</task>

<task type="auto">
  <name>Task 3: De-crowd payslip table + size ticket to native A6 with seat-locator focal</name>
  <files>lib/rendro/recipes/payslip.ex, lib/rendro/recipes/ticket.ex, priv/examples/payslip/aurora-live/payslip.json, priv/examples/ticket/aurora-live/ticket.json, test/rendro/recipes/payslip_test.exs, test/rendro/recipes/payslip_byte_identity_test.exs, test/rendro/recipes/ticket_test.exs, test/rendro/recipes/ticket_byte_identity_test.exs</files>
  <read_first>
    - lib/rendro/recipes/payslip.ex (two-column earnings/deductions table; Net Pay box already dominant / CH=5 honest; the header-collision + YTD digit-wrap defect)
    - lib/rendro/recipes/ticket.ex (fixed-box ticket; SECTION/ROW/SEAT/GATE locator; perforated reference stub; page-size handling)
    - lib/rendro/page_size.ex (A6 vs A4 geometry)
    - priv/examples/payslip/aurora-live/payslip.json, priv/examples/ticket/aurora-live/ticket.json
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-FINDINGS.md (payslip/ticket sections lines 83-94)
    - test/rendro/recipes/payslip_test.exs, payslip_byte_identity_test.exs, ticket_test.exs, ticket_byte_identity_test.exs
  </read_first>
  <action>
    Two fixes per 118-06-FINDINGS.md (payslip is the closest to passing — already CH=5; ticket is CH=4):
    - PAYSLIP: de-crowd the earnings/deductions table so the header labels no longer collide ("YTDDeductions" run together) and the base-salary YTD figure ("$25,200.00") no longer wraps a digit onto a second line. Widen/space the two columns and their headers. Keep the Net Pay box dominant (do not regress CH=5). This raises information_architecture / reader_affordances / typographic_craft to >= 4.
    - TICKET: render at native A6 (or size the canvas to content) so the render is not ~65% empty A4, and make the seat-locator (the SEAT field, or the SECTION/ROW/SEAT/GATE group) the clear focal point rather than four equally-large fields — so content_hierarchy honestly reaches 5. Keep the perforated reference stub and non-transferable terms.
    Do NOT touch rubric_scores.json (D-11 — scoring is 118-09).
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_test.exs test/rendro/recipes/payslip_byte_identity_test.exs test/rendro/recipes/ticket_test.exs test/rendro/recipes/ticket_byte_identity_test.exs test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - payslip.ex earnings/deductions columns are widened/spaced; no header-label collision and no YTD figure wrapping (assertable via updated byte-identity/recipe tests); the Net Pay box remains dominant.
    - ticket.ex renders at A6 (or content-sized canvas) with the seat-locator as the dominant focal element.
    - Both fixtures validate against examples.schema.json (examples_schema_contract_test.exs green).
    - `mix test` for the files above exits 0 (byte-identity goldens updated + re-blessed where geometry legitimately changed).
  </acceptance_criteria>
  <done>Payslip table is de-crowded with Net Pay still dominant; ticket renders at native A6 with the seat-locator as the clear focal point.</done>
</task>

</tasks>

<verification>
- `mix test test/rendro/recipes/ test/rendro/examples_data_test.exs test/docs_contract/examples_schema_contract_test.exs` — all recipe + transform + fixture-schema tests green.
- `grep -nE 'Decimal\.to_float|Decimal\.to_integer' lib/rendro/examples_data.ex` — no invoice money-path hit.
- `mix format --check-formatted lib/rendro/examples_data.ex lib/rendro/recipes/*.ex` — no NEW formatting debt introduced by this plan.
- Composition only — priv/quality/rubric_scores.json is UNCHANGED by this plan (git diff shows no edit); re-scoring is 118-09 (D-11 honesty).
</verification>

<success_criteria>
All six demo compositions are reworked per 118-06-FINDINGS.md so each family's key fact is visually dominant and each family is conventionally complete, transforms/fixtures are faithful (no lossy money coercion), and every recipe/transform/schema test is green — ready for honest re-render + re-score in 118-09.
</success_criteria>

<artifacts_produced>
- Enriched `transform_invoice/1` (issuer/customer/due_date/terms/totals passthrough; faithful money) in lib/rendro/examples_data.ex.
- Merchant passthrough added to `transform_receipt/1`.
- Reworked recipe compositions in invoice/statement/receipt/certificate/payslip/ticket.ex (dominant key fact per family).
- Realistic reworked fixtures for all six families under priv/examples/**.
- Updated recipe / byte-identity test expectations where geometry or the invoice line-price path was legitimately re-blessed.
</artifacts_produced>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-08-SUMMARY.md` when done.
</output>
