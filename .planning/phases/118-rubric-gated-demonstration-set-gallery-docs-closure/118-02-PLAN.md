---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/examples/statement/DOMAIN.md
  - priv/examples/receipt/DOMAIN.md
  - priv/examples/certificate/DOMAIN.md
  - priv/examples/payslip/DOMAIN.md
  - priv/examples/ticket/DOMAIN.md
  - test/docs_contract/domain_md_contract_test.exs
autonomous: true
requirements: [SHOW-01]
must_haves:
  truths:
    - "Each of the five new domains has a co-located DOMAIN.md carrying all four required headings (D-04)"
    - "The DomainMdContractTest enforces a DOMAIN.md per demonstrated domain, not merely 'at least one' (D-04 strengthening)"
  artifacts:
    - priv/examples/statement/DOMAIN.md
    - priv/examples/receipt/DOMAIN.md
    - priv/examples/certificate/DOMAIN.md
    - priv/examples/payslip/DOMAIN.md
    - priv/examples/ticket/DOMAIN.md
  key_links:
    - "domain_md_contract_test.exs globs priv/examples/*/DOMAIN.md → the four-heading loop covers new files automatically; the strengthened per-domain assertion binds each demonstrated domain to a DOMAIN.md"
    - "the DOMAIN.md path is the citation target for the D-05 demo-cites-DOMAIN.md check authored in 118-06"
---

<objective>
Author the five new co-located `DOMAIN.md` files (statement, receipt, certificate, payslip, ticket) that encode each domain's language, personas/JTBD, reading context, and layout/typographic conventions — the anchor the rubric scores each demo against — and strengthen the DOMAIN.md contract test to require one per demonstrated domain (D-04).

Purpose: SHOW-01 requires each demo to cite its DOMAIN.md; that citation is only meaningful if the DOMAIN.md exists and carries the four contract headings. This plan authors those docs and closes the "at least one" loophole in the contract.
Output: Five four-heading DOMAIN.md files + a strengthened `domain_md_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@priv/examples/invoice/DOMAIN.md
@test/docs_contract/domain_md_contract_test.exs
</context>

## Artifacts this phase produces (Plan 118-02 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `priv/examples/{statement,receipt,certificate,payslip,ticket}/DOMAIN.md` | docs | four required headings each (D-04) |
| `test/docs_contract/domain_md_contract_test.exs` (strengthened) | test | per-demonstrated-domain assertion (D-04) |

<tasks>

<task type="auto">
  <name>Task 1: Author five new DOMAIN.md files (four-heading contract, D-01/D-04)</name>
  <files>priv/examples/statement/DOMAIN.md, priv/examples/receipt/DOMAIN.md, priv/examples/certificate/DOMAIN.md, priv/examples/payslip/DOMAIN.md, priv/examples/ticket/DOMAIN.md</files>
  <read_first>
    - priv/examples/invoice/DOMAIN.md (the full section shape to mirror — Domain Language nouns/verbs, 2-3 personas with the ONE fact each needs first, Reading Context situations, Layout & Typographic Conventions visual grammar)
    - test/docs_contract/domain_md_contract_test.exs (the four required headings: `## Domain Language`, `## Personas & Jobs-to-be-Done`, `## Reading Context`, `## Layout & Typographic Conventions`)
    - lib/rendro/recipes/{statement,receipt,certificate,payslip,ticket}.ex (for domain-accurate language)
  </read_first>
  <action>
    Author one DOMAIN.md per new domain directory (`priv/examples/<domain>/DOMAIN.md`), each mirroring the invoice/DOMAIN.md structure and carrying all four required headings verbatim. Domain Language: bulleted **Nouns** (the things on the page) + **Verbs and events** (the lifecycle), each as `**Term** — definition`. Personas & Jobs-to-be-Done: 2-3 personas labeled primary/secondary/tertiary, each stating the ONE fact that reader needs first (statement → closing balance; receipt → amount paid + proof; certificate → recipient + credential; payslip → net pay; ticket → seat/gate/section). Reading Context: the real situations (queue/triage, fast first scan, slower second read, print vs screen). Layout & Typographic Conventions: the visual grammar each rubric dimension will be scored against (what is most prominent, money/table alignment, arithmetic ladder, the anchor element). Keep prose bounded to information-design craft — do NOT introduce any accessibility/tagged-PDF/screen-reader/reading-order-standard claim (guarded by the D-14 tripwire in 118-07).
  </action>
  <verify>
    <automated>mix test test/docs_contract/domain_md_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All five DOMAIN.md files exist and each contains all four required headings (`mix test test/docs_contract/domain_md_contract_test.exs` green).
    - `grep -rl "## Layout & Typographic Conventions" priv/examples/*/DOMAIN.md | wc -l` returns 6 (invoice + five new).
    - No accessibility-standard claims in the prose.
  </acceptance_criteria>
  <done>Five four-heading DOMAIN.md files exist, each encoding its domain's language, personas, reading context, and layout conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen DomainMdContractTest to require a DOMAIN.md per demonstrated domain (D-04)</name>
  <files>test/docs_contract/domain_md_contract_test.exs</files>
  <read_first>
    - test/docs_contract/domain_md_contract_test.exs (current "at least one" assertion at lines 12-15; four-heading loop at 17-26)
  </read_first>
  <action>
    Strengthen the contract from "at least one DOMAIN.md exists" to "a DOMAIN.md exists per demonstrated domain." Derive the demonstrated-domain set from the fixture directories (`Path.wildcard("priv/examples/*/*/*.json")` → domain segment) and assert each demonstrated domain has a co-located `priv/examples/<domain>/DOMAIN.md`. Keep the existing four-heading loop intact (it already covers new files via the glob). Keep the assertion additive — do not couple it to a hardcoded family list; derive from disk so future domains inherit the contract automatically.
  </action>
  <verify>
    <automated>mix test test/docs_contract/domain_md_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts a DOMAIN.md per demonstrated domain (derived from fixture dirs, not hardcoded).
    - `mix test test/docs_contract/domain_md_contract_test.exs` green with all six domains present.
    - Removing any one DOMAIN.md would fail the strengthened test (teeth check — reviewer confirms the derivation is non-vacuous).
  </acceptance_criteria>
  <done>The DOMAIN.md contract requires one per demonstrated domain and passes with all six domains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → shipped guides/`priv/examples/**` | Domain prose ships text-only; the claims-integrity risk is an accessibility overclaim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-03 | Repudiation/legal | DOMAIN.md prose | medium | mitigate | Keep prose to information-design craft; no PDF-UA/tagged/reading-order/screen-reader claim (the D-14 tripwire in 118-07 enforces this repo-wide) |
</threat_model>

<verification>
- `mix test test/docs_contract/domain_md_contract_test.exs` green.
- `mix test test/docs_contract/` green (no regression).
</verification>

<success_criteria>
Five new four-heading DOMAIN.md files exist; the contract test requires one per demonstrated domain and passes.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-02-SUMMARY.md` when done.
</output>
