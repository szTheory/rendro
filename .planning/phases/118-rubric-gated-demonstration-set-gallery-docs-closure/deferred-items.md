# Deferred Items — Phase 118

Out-of-scope discoveries logged during execution (not fixed in the discovering plan).

- **[118-03] Statement recipe lacks a unicode fallback font.** The Statement recipe builds `Rendro.Document.new()` (built-in Helvetica, ASCII-only) with no opts injection point, so realistic typographic glyphs (em-dash `—`, middle-dot account masking `····`) abort rendering with `{:unsupported_glyph}`. Payslip solved the same problem in 116-02 via `with_unicode_fallback_font/1` (B612) + `glyph_safe/1` (·→•). 118-03 worked around it by ASCII-ifying the statement fixture. **Realism-maximizing upgrade (future plan):** give Statement the same B612 fallback + middle-dot glyph_safe treatment, then restore the en/em-dash + middle-dot masking in `priv/examples/statement/northwind-ledger-co/statement.json`. No statement byte-goldens currently exist, so the font change is low-risk.

- **[118-09 / verification] ~~No standing tripwire cross-checks recorded rubric `passed` vs a live recomputation.~~ RESOLVED (post-verification, 2026-07-19).** Added the test "recorded `passed` matches recomputation from each entry's own dimensions/gates" to `test/docs_contract/rubric_manifest_contract_test.exs` — it recomputes each `priv/quality/rubric_scores.json` entry's verdict via the file's `passed?/2` helper and asserts equality with the recorded `passed`, plus a non-empty-scores guard so it can't pass vacuously. Guards this phase's exact original failure mode (asserted-but-fake passes). Green: 73/73.

- **[118-09 / verification] `priv/support_matrix.json` `demonstration_set.boundaries.reader_quality_rubric_pass` still `"unsupported"`.** Stale under-claim now that all six demos pass the rubric. Safe direction (under-claim never violates SHOW-04's "no overclaim") but inaccurate. **Intentionally left deferred** — on inspection this is NOT a one-line edit: `test/docs_contract/recipes_claims_test.exs:187` deliberately asserts `boundaries["reader_quality_rubric_pass"] == "unsupported"` as an overclaim guard, so flipping the matrix means reworking a defensive claim-tripwire and re-framing the "makes no rubric-pass claim" test. Deserves a scoped plan with its own review rather than pre-handoff cleanup. **Future plan:** move `reader_quality_rubric_pass` from `boundaries` into `capabilities` as `"supported"` (backed by the rubric_scores + rubric_contract evidence already listed) and update the guard test to assert the claim is *backed*, not *absent*.

- **[118-07] Pre-existing `mix format` debt (unrelated to the docs surface).** `mix format --check-formatted` (part of `mix ci.fast`) is RED on 10 files that 118-07 did **not** touch — pre-existing from earlier phases/waves (Phase 116 ticket work, 118-04/05 launch-artifacts, edge-matrix tests): `lib/rendro/recipes/ticket.ex`, `lib/rendro/launch_artifacts.ex`, `test/rendro/launch_artifacts_test.exs`, `test/rendro/recipes/ticket_test.exs`, `test/support/golden_test.exs`, `test/support/edge_fixtures_test.exs`, `test/support/edge_fixtures.ex`, `test/rendro/edge_matrix_test.exs`, `test/rendro/edge_error_matrix_test.exs`, `test/rendro/adapters/pdfium_raster_snapshot_test.exs`. All 9 files 118-07 changed are individually `mix format` clean. **Future plan / owning phase:** a dedicated `mix format` pass. Not folded into the docs-closure plan (executor scope boundary).
