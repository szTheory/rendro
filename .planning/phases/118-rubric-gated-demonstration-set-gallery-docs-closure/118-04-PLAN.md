---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 04
type: execute
wave: 3
depends_on: ["118-03"]
files_modified:
  - lib/rendro/launch_artifacts.ex
  - test/rendro/launch_artifacts_test.exs
autonomous: true
requirements: [SHOW-03]
must_haves:
  truths:
    - "LaunchArtifacts sources every tile's document from priv/examples/** through Rendro.Examples + Rendro.ExamplesData (D-06) — the inline *_data/* toy builders no longer feed the gallery"
    - "The gallery has 7 tiles in fixed order: invoice, branded_invoice, statement, receipt_report, certificate, payslip, ticket (D-07)"
    - "Every gallery entry carries optional S6 theme/mode/preset keys with explicit placeholder values; their absence never breaks older readers (D-13)"
  artifacts:
    - lib/rendro/launch_artifacts.ex
    - test/rendro/launch_artifacts_test.exs
  key_links:
    - "build_source_document/1 dispatch → Rendro.Examples.load!/1 |> Rendro.ExamplesData.transform_<family>/1 |> Recipes.<Family>.document/2 (D-06)"
    - "@gallery_specs order == manifest id order (enforced) → adding payslip/ticket tiles requires matching @expected_gallery_dimensions + shape contract"
    - "ordered_gallery_entry/1 fixed key order → S6 keys appended after caption; @gallery_required_keys stays WITHOUT the S6 keys (optional, D-13)"
---

<objective>
Perform the phase's one real `lib/` edit: repoint `Rendro.LaunchArtifacts` from its inline hardcoded toy-data builders to the realistic example library through `Rendro.Examples` + `Rendro.ExamplesData` (D-06), add Payslip and Ticket tiles to reach 7 tiles (D-07), and emit the S6 `theme`/`mode`/`preset` tags as explicit-null seams on every gallery entry (D-13). This is document-shape work verifiable locally via `Rendro.render(deterministic: true)`; the byte re-baseline + PNG rasters (which need pdfium) are deferred to the container wave 118-05.

Purpose: One source of truth — the demonstration set and the gallery render from the same fixtures.
Output: An extended `launch_artifacts.ex` (@moduledoc false, still out of `public_api.json`) + extended `launch_artifacts_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-RESEARCH.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@lib/rendro/launch_artifacts.ex
@test/rendro/launch_artifacts_test.exs
</context>

## Artifacts this phase produces (Plan 118-04 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `lib/rendro/launch_artifacts.ex` (repointed) | lib | dispatch → fixtures; 2 new `@gallery_specs`; S6 tags in `build_gallery_entries/1` + `ordered_gallery_entry/1`; extended `@expected_gallery_dimensions` + shape check |
| `test/rendro/launch_artifacts_test.exs` (extended) | test | source-document assertions for payslip/ticket; S6-tolerant shape check |

<tasks>

<task type="auto">
  <name>Task 1: Repoint build_source_document/1 dispatch to the fixture library (D-06)</name>
  <files>lib/rendro/launch_artifacts.ex, test/rendro/launch_artifacts_test.exs</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex:255-299 (source_document_for/1 accepts both %{id:} and %{"id"=>}; build_source_document/1 clauses; apply_launch_table_style/1)
    - lib/rendro/launch_artifacts.ex:838-903 (the inline *_data/* builders being replaced; note branded_invoice's synthesized brand refs at :850-854)
    - test/rendro/launch_artifacts_test.exs:9 (harness calls source_document_for with %{id: "..."})
    - 118-03-SUMMARY.md (the Rendro.ExamplesData transform signatures)
  </read_first>
  <action>
    Replace each inline `*_data/*` builder body in the `build_source_document/1` clauses with fixture-sourced data: `Rendro.Examples.load!("<domain>/<business>/<family>.json") |> Rendro.ExamplesData.transform_<family>() |> Rendro.Recipes.<Family>.document(opts...)`. Keep `apply_launch_table_style/1` for the table families (invoice, branded_invoice, statement, receipt_report). Keep certificate's `document(data, border: true)` polish. For `branded_invoice`, keep its synthesized brand refs (`%{font_name: :brand_heading, logo_name: :company_logo}`) layered over the invoice fixture data (the S4 fixture slot is empty this milestone — Assumption A1). Do NOT delete the inline builders if other call sites depend on them — grep first; if the only consumer is `build_source_document/1`, remove them; otherwise leave them and stop routing the gallery through them. Preserve the dual atom/string `id` key acceptance in `source_document_for/1`. Extend `launch_artifacts_test.exs` so the existing per-tile source-document assertions still pass against the fixture-sourced documents (update any toy-data-specific expectations to the realistic fixture values).
  </action>
  <verify>
    <automated>mix test test/rendro/launch_artifacts_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every existing tile's `source_document_for/1` returns a `%Rendro.Document{}` sourced from `priv/examples/**` (grep confirms `Rendro.Examples.load!` in build_source_document clauses).
    - `mix test test/rendro/launch_artifacts_test.exs` green.
    - No inline toy `*_data/*` builder still feeds the gallery dispatch (grep confirms the dispatch calls `Rendro.ExamplesData.transform_*`).
  </acceptance_criteria>
  <done>The five existing tiles render from realistic fixtures through the transform; branded_invoice keeps its brand refs; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Payslip + Ticket tiles + manual pages + extend dimension/shape contracts (D-07)</name>
  <files>lib/rendro/launch_artifacts.ex, test/rendro/launch_artifacts_test.exs</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex:39-94 (@gallery_specs entry shape: id/title/module/png_path/asset_name/fit/alt/caption)
    - lib/rendro/launch_artifacts.ex:26-32 (@expected_gallery_dimensions, advisory-tier), :458 (list-order == manifest-id-order enforcement), the recipe_page/1 manual wiring
    - 118-RESEARCH.md Open Question #3 (expected pixel dims; let the first container .gen report actual, then pin)
  </read_first>
  <action>
    Add two `@gallery_specs` entries — `payslip` (module Rendro.Recipes.Payslip) and `ticket` (module Rendro.Recipes.Ticket) — copying the existing entry shape (id/title/module/png_path=Path.join(@gallery_dir, "<id>.png")/asset_name atom/fit/alt/caption). Portrait families use `fit: {320, 452}`. Append them after `certificate` so the final order is exactly: invoice, branded_invoice, statement, receipt_report, certificate, payslip, ticket (list order == gallery order == required manifest id order, enforced at :458). Add matching `build_source_document/1` clauses for the two ids using the fixture+transform pattern from Task 1 (payslip: no table style; ticket: no table style). Wire the two new manual `recipe_page/1` pages. Add placeholder `@expected_gallery_dimensions` entries for payslip/ticket — use `{794,1123}` (A4 portrait) as the provisional value and mark in the SUMMARY that the container `.gen` in 118-05 will confirm/correct the actual pixel dims (that attribute is advisory-tier). Extend `launch_artifacts_test.exs` with source-document assertions for the two new tiles.
  </action>
  <verify>
    <automated>mix test test/rendro/launch_artifacts_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `@gallery_specs` has exactly 7 entries in the specified order (`grep -c 'id: "' ...` cross-checked, or a test asserting `length(gallery_specs) == 7`).
    - payslip and ticket each have a `build_source_document/1` clause sourcing their fixture.
    - `@expected_gallery_dimensions` has entries for all 7 tiles.
    - `mix test test/rendro/launch_artifacts_test.exs` green.
  </acceptance_criteria>
  <done>Seven tiles exist in fixed order with fixture-sourced documents, manual pages, and provisional dimension entries.</done>
</task>

<task type="auto">
  <name>Task 3: Emit S6 theme/mode/preset tags as explicit-null seams on every gallery entry (D-13)</name>
  <files>lib/rendro/launch_artifacts.ex, test/rendro/launch_artifacts_test.exs</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex:340-356 (build_gallery_entries/1 entry map)
    - lib/rendro/launch_artifacts.ex:942-959 (ordered_gallery_entry/1 fixed key order)
    - lib/rendro/launch_artifacts.ex:25 (@gallery_required_keys)
  </read_first>
  <action>
    Add three optional S6 keys — `theme`, `mode`, `preset` — to every gallery entry via three coordinated edits: (1) in `build_gallery_entries/1` add `"theme" => nil, "mode" => "light", "preset" => nil` to each entry map (explicit `null` for theme/preset as "seam not yet populated"; `"light"` is the one defensible non-null default for mode — document this convention in the SUMMARY); (2) in `ordered_gallery_entry/1` append `{"theme", entry["theme"]}, {"mode", entry["mode"]}, {"preset", entry["preset"]}` AFTER `{"caption", ...}` to keep a stable deterministic key order; (3) do NOT add the three keys to `@gallery_required_keys` (D-13 requires them OPTIONAL) — instead add a shape check (in the shape-contract path around :408-504) that tolerates their absence, so older/other readers never break. Extend `launch_artifacts_test.exs` to assert each entry carries the three keys with the placeholder convention and that a manifest missing them does not fail the required-keys contract.
  </action>
  <verify>
    <automated>mix test test/rendro/launch_artifacts_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every gallery entry emits `theme`, `mode`, `preset` with the documented placeholder convention.
    - `@gallery_required_keys` does NOT contain the three S6 keys (they are optional, D-13).
    - A manifest without the S6 keys still passes the required-keys shape check (tolerance test green).
    - `mix test test/rendro/launch_artifacts_test.exs` green.
  </acceptance_criteria>
  <done>S6 tags appear on every entry in deterministic key order as optional seams; the required-keys contract stays tolerant of their absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Rendro.LaunchArtifacts` → `priv/public_api.json` | The generator is `@moduledoc false`; repointing must not leak it or the transform into the public tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-06 | Tampering | fixture path in build_source_document/1 | low | accept | Loads via `Rendro.Examples.load!/1` (Path.safe_relative guarded); ids are hardcoded literals, no external input |
| T-118-07 | Information disclosure | public API leakage | medium | mitigate | `launch_artifacts.ex` stays `@moduledoc false`; no new public_api.json entry; verified by the existing public-api contract lane |
</threat_model>

<verification>
- `mix test test/rendro/launch_artifacts_test.exs` green.
- `mix test test/docs_contract/` green (public-api hidden contract still passes; no new public surface).
- `mix compile --warnings-as-errors` clean.
- NOTE: source-PDF/PNG hash re-baseline and PNG raster dims are NOT verified here — they are container-gated in 118-05.
</verification>

<success_criteria>
The gallery renders 7 fixture-sourced tiles in fixed order with S6 seam tags; document-shape correctness is proven locally; the generator stays out of the public API.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-04-SUMMARY.md` when done. Record the S6 placeholder convention chosen and flag that 118-05 must confirm the payslip/ticket pixel dims via the container `.gen`.
</output>
