---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 06
type: execute
wave: 4
depends_on: ["118-04"]
files_modified:
  - test/docs_contract/demo_cites_domain_md_test.exs
  - priv/quality/rubric_scores.json
autonomous: false
requirements: [SHOW-01]
must_haves:
  truths:
    - "Six demo score entries are appended to priv/quality/rubric_scores.json, each schema-valid ($defs.score_entry) with all 6 dimension_scores, both gate_results, a computed passed boolean, recorded_at, a justifications object, and a domain_md citation (D-09/D-10)"
    - "Every demo actually reaches hierarchy = 5 / core ≥ 4 / gates pass; passed is computed by the same passed?/2 arithmetic, never asserted (D-11)"
    - "demo_ids are disjoint from the 62 stress-fixture ids; stress_exempt is absent/false on every demo entry (D-12/D-15)"
    - "Each score entry's domain_md path exists on disk and each demonstrated domain is cited (D-05 machine-checkable)"
  artifacts:
    - test/docs_contract/demo_cites_domain_md_test.exs
    - priv/quality/rubric_scores.json
  key_links:
    - "rendered PDF → pinned pdfium raster → Claude visual self-score against the 1/3/4/5 anchors → rubric_scores.json scores[] append (D-09)"
    - "rubric_manifest_contract_test.exs passed?/2 arithmetic → the exact rule each entry's passed must satisfy (D-11); D-15iii disjointness guard reads demo_ids"
    - "demo_cites_domain_md_test.exs → asserts each score entry's domain_md path exists + each demonstrated domain is cited (D-05)"
---

<objective>
Perform the D-09 rubric self-scoring: for each of the six demos render the deterministic PDF, rasterize via the pinned pdfium adapter, evaluate all 6 rubric dimensions against the concrete 1/3/4/5 anchors plus both pass/fail gates, and append a schema-valid score entry (with per-dimension `justifications` and a `domain_md` citation) to `priv/quality/rubric_scores.json`. Author the D-05 demo-cites-DOMAIN.md contract test first (test-first) so the citation cannot silently rot.

Purpose: SHOW-01's quality gate. Passing is EARNED, not assigned (D-11) — a demo that cannot honestly reach the thresholds is a finding to surface (improve the composition), never scored up. This is CONTAINER-GATED (pdfium raster) and involves genuine visual judgment.
Output: The D-05 contract test + six appended, honestly-scored, schema-valid entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-RESEARCH.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@priv/quality/rubric_scores.json
@priv/schemas/rubric_scores.schema.json
@test/docs_contract/rubric_manifest_contract_test.exs
</context>

## Artifacts this phase produces (Plan 118-06 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `test/docs_contract/demo_cites_domain_md_test.exs` | test | D-05 machine-checkable citation contract (new) |
| `priv/quality/rubric_scores.json` (6 entries appended) | manifest | schema-valid `$defs.score_entry` + `justifications` + `domain_md`; `stress_exemption` block untouched |
| six `demo_id`s | data | demonstration namespace (`<family>-<business>`), disjoint from the 62 stress ids |

<tasks>

<task type="auto">
  <name>Task 1: Author the D-05 demo-cites-DOMAIN.md contract test (test-first, D-05)</name>
  <files>test/docs_contract/demo_cites_domain_md_test.exs</files>
  <read_first>
    - test/docs_contract/domain_md_contract_test.exs (file-scan style) + test/docs_contract/rubric_manifest_contract_test.exs (manifest-read style + passed?/2 helper)
    - priv/schemas/rubric_scores.schema.json ($defs.score_entry; additionalProperties: true permits a domain_md field)
    - 118-PATTERNS.md "D-05 demo-cites-DOMAIN.md check" section (Open Q #2 recommendation)
  </read_first>
  <action>
    Author `test/docs_contract/demo_cites_domain_md_test.exs` (`use ExUnit.Case, async: true`) asserting: (a) every entry in `priv/quality/rubric_scores.json` `scores[]` has a `domain_md` string field whose path exists on disk (`File.exists?`); and (b) every demonstrated domain (derived from the fixture directories, `Path.wildcard("priv/examples/*/*/*.json")`) is cited by at least one score entry's `domain_md`. Include a teeth/non-vacuity guard so the test cannot pass on an empty `scores[]` after this phase (e.g. assert `scores` is non-empty). Write this test BEFORE appending the scores in Task 2 — it will be RED until Task 2 lands the entries, which is the intended test-first signal.
  </action>
  <verify>
    <automated>mix test test/docs_contract/demo_cites_domain_md_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and asserts both (a) domain_md path existence and (b) per-demonstrated-domain citation.
    - It contains a non-vacuity guard (non-empty scores).
    - Before Task 2, the test is RED (expected test-first state); this is recorded, not a failure to fix here.
  </acceptance_criteria>
  <done>The D-05 citation contract exists and is authored test-first (RED until scores are appended).</done>
</task>

<task type="auto">
  <name>Task 2: Render → rasterize → self-score the six demos and append schema-valid entries (D-09/D-10/D-11/D-12)</name>
  <files>priv/quality/rubric_scores.json</files>
  <read_first>
    - priv/schemas/rubric_scores.schema.json ($defs.score_entry required keys: demo_id/domain/family/dimension_scores{6}/gate_results{2}/passed/recorded_at; dimension_scores is additionalProperties:false — justifications must be a SIBLING object)
    - test/docs_contract/rubric_manifest_contract_test.exs:33-45 (the passed?/2 arithmetic to mirror EXACTLY), :103-122 (D-15ii stress_exempt tripwire, D-15iii disjointness, D-15iv 62-cell teeth)
    - priv/quality/rubric_scores.json (the 6 dimension anchors + 2 gates + thresholds; current scores: []; stress_exemption block stays intact)
    - 118-RESEARCH.md §Rasterization + Pitfall 3/4/5; Rendro.Adapters.Pdfium.render(pdf, dpi: 96, pages: "1")
  </read_first>
  <action>
    For each of the six demos (invoice-acme-phoenix-saas, statement-<business>, receipt-<business>, certificate-<business>, payslip-aurora-live, ticket-aurora-live): render the deterministic PDF via the repointed `LaunchArtifacts` source document (or `Recipes.<Family>.document(transform(load!(...)))`), rasterize via `Rendro.Adapters.Pdfium.render(pdf, dpi: 96, pages: "1")` in the pinned container, and genuinely evaluate each of the 6 dimensions against the concrete 1/3/4/5 anchors in `rubric_scores.json` plus both gates (reading_order, print_safety). Append one schema-valid entry per demo to `scores[]` with: `demo_id` in the demonstration namespace `<family>-<business>` (hyphen + business guarantees disjointness from the `{family}_{dimension}` stress ids — D-12); `domain`, `family`; `dimension_scores` (all 6, int 1–5); `gate_results` (2 bools); `passed` computed by the EXACT `passed?/2` arithmetic (hierarchy == 5 AND other cores ≥ 4 AND all gates true — never assert it independently, D-11); `recorded_at` (2026-07-19); a `justifications` object (per-dimension short rationale, SIBLING of dimension_scores, never inside it); and a `domain_md` path (the D-05 citation, e.g. `priv/examples/<domain>/DOMAIN.md`). Do NOT set `stress_exempt` (D-15ii tripwire). If any demo does not honestly reach hierarchy=5/core≥4/gates, improve the fixture/composition within the recipe's existing knobs and re-score — NEVER inflate the number (D-11); if it still cannot pass, surface it as a finding in the SUMMARY. Leave `stress_exemption` untouched.
  </action>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/demo_cites_domain_md_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Six entries appended; each validates against `$defs.score_entry` and passes `rubric_manifest_contract_test.exs` (arithmetic + D-15 guards).
    - `mix test test/docs_contract/demo_cites_domain_md_test.exs` now GREEN (Task 1's test satisfied).
    - Every `passed` boolean equals the `passed?/2` computation for its scores (reviewer spot-checks; the contract test enforces arithmetic consistency).
    - demo_ids disjoint from the 62 stress ids (D-15iii green); no entry sets `stress_exempt` (D-15ii green).
    - Each entry has a `justifications` object (sibling of dimension_scores) and a `domain_md` path that exists.
  </acceptance_criteria>
  <done>Six honestly-scored, schema-valid demo entries exist; all passing arithmetic and D-15 guards hold; the D-05 citation contract is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human sign-off on the six honestly-earned rubric self-scores (D-09/D-11)</name>
  <action>Present the six appended score entries and their rationale for human review, confirming each score is honestly earned against the rendered demo (not inflated).</action>
  <what-built>Six rubric score entries appended to priv/quality/rubric_scores.json, each derived from a genuine visual assessment of the rasterized demo against the 1/3/4/5 anchors, with per-dimension justifications and a DOMAIN.md citation.</what-built>
  <how-to-verify>
    1. Review `git diff priv/quality/rubric_scores.json` — confirm 6 entries, each with all 6 dimension_scores, both gate_results, a computed `passed`, a `justifications` object, and a `domain_md` path.
    2. Spot-check 2-3 justifications against the rasterized demo PNGs — confirm the score matches what the render actually shows (D-09 honesty; D-11 not inflated).
    3. Confirm no entry sets `stress_exempt` and every demo_id uses the `<family>-<business>` namespace.
    4. Confirm `mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/demo_cites_domain_md_test.exs` is green.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the scores, or name any demo whose score is not honestly earned (to be improved, not inflated).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered PDF → pdfium raster → recorded score | The integrity risk is a score inflated past what the render honestly shows (a claims-integrity concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-10 | Repudiation | inflated rubric score (D-11) | high | mitigate | `passed` computed by the shared `passed?/2` arithmetic (never asserted); justifications recorded per dimension; blocking human-verify spot-checks scores against rasters; a demo that cannot pass honestly is surfaced, not scored up |
| T-118-11 | Tampering | stress_exempt loophole | medium | mitigate | D-15ii tripwire (`rubric_manifest_contract_test.exs:103-107`) fails loudly if any demo entry sets stress_exempt |
| T-118-12 | Tampering | pdfium raster invocation | low | accept | Adapter uses isolated tmp + list-form args (existing, unchanged) |
</threat_model>

<verification>
- `mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/demo_cites_domain_md_test.exs` green.
- `mix test test/docs_contract/` green (D-15 disjointness/teeth guards hold).
- Human self-score sign-off checkpoint approved.
</verification>

<success_criteria>
Six honestly-earned, schema-valid rubric score entries exist with justifications and DOMAIN.md citations; passing arithmetic and disjointness guards hold; the D-05 citation contract is enforced and green.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-SUMMARY.md` when done. Record the six demo_ids, any demo whose composition was improved to pass honestly, and any surfaced finding.
</output>
