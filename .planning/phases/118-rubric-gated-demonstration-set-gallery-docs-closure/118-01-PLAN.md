---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/schemas/examples.schema.json
  - priv/examples/statement/northwind-ledger-co/statement.json
  - priv/examples/receipt/harbor-and-oak-cafe/receipt.json
  - priv/examples/certificate/summit-training-institute/certificate.json
  - priv/examples/payslip/aurora-live/payslip.json
  - priv/examples/ticket/aurora-live/ticket.json
autonomous: true
requirements: [SHOW-01]
must_haves:
  truths:
    - "A payslip/ticket/statement/receipt/certificate fixture validates against the generalized examples.schema.json (the required test lane stays green with all six shapes)"
    - "Every new fixture uses Decimal-safe money as strings (never JSON floats) and carries the optional empty brand/logo S4 slot"
    - "Fixtures contain fictional-only businesses/people — no real PII (Payslip is the acute risk)"
  artifacts:
    - priv/schemas/examples.schema.json
    - priv/examples/statement/northwind-ledger-co/statement.json
    - priv/examples/receipt/harbor-and-oak-cafe/receipt.json
    - priv/examples/certificate/summit-training-institute/certificate.json
    - priv/examples/payslip/aurora-live/payslip.json
    - priv/examples/ticket/aurora-live/ticket.json
  key_links:
    - "examples_schema_contract_test.exs globs priv/examples/**/*.json → every new fixture must validate against the generalized schema"
    - "each fixture's payload keys are the string form of the atom keys its recipe's validate_data!/1 requires (consumed by the 118-03 transform)"
---

<objective>
Establish the SHOW-01 data foundation: generalize `priv/schemas/examples.schema.json` from its invoice-only shape to a per-family discriminated schema, then author five new realistic, domain-correct fixtures (statement, receipt, certificate, payslip, ticket) that validate against it.

Purpose: This is the hard precondition (RESEARCH finding #2). The required `test` job validates EVERY `priv/examples/**/*.json` against the single schema; a non-invoice fixture cannot satisfy `required: invoice+items`, so the schema MUST be generalized BEFORE any new fixture lands or the build goes red.
Output: A generalized schema + five text-only fixtures encoding fictional businesses with Decimal-string money.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-RESEARCH.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@priv/schemas/examples.schema.json
@priv/examples/invoice/acme-phoenix-saas/invoice.json
</context>

## Artifacts this phase produces (Plan 118-01 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `priv/schemas/examples.schema.json` (generalized) | schema | `family` discriminator + `allOf`/`if-then` per-family branches; reuses `money_string`/`party` `$defs` |
| `priv/examples/statement/northwind-ledger-co/statement.json` | fixture | fictional business (planner discretion per D-02) |
| `priv/examples/receipt/harbor-and-oak-cafe/receipt.json` | fixture | fictional business |
| `priv/examples/certificate/summit-training-institute/certificate.json` | fixture | fictional business |
| `priv/examples/payslip/aurora-live/payslip.json` | fixture | Aurora Live (D-02); fictional employee, masked payment method |
| `priv/examples/ticket/aurora-live/ticket.json` | fixture | Aurora Live (D-02) |

<tasks>

<task type="auto">
  <name>Task 1: Generalize examples.schema.json to a per-family discriminated schema (D-03, RESEARCH finding #2)</name>
  <files>priv/schemas/examples.schema.json</files>
  <read_first>
    - priv/schemas/examples.schema.json (current invoice-only shape; `required` at line 7; reusable `$defs.money_string` and `$defs.party` at lines 55-72)
    - test/docs_contract/examples_schema_contract_test.exs (globs `priv/examples/**/*.json`, validates each against this one schema; also the text-only tarball guard)
    - priv/examples/invoice/acme-phoenix-saas/invoice.json (the shape the invoice branch must keep validating)
  </read_first>
  <action>
    Replace the top-level invoice-only `required: ["fixture_id","issuer","customer","invoice","items"]` with a per-family discriminated structure in a single file. Add a top-level `family` string property (enum: invoice, statement, receipt, certificate, payslip, ticket) and use `allOf` with `if`/`then` branches keyed on `family` so each family requires only its own keys. Preserve the existing invoice branch requirements exactly so `priv/examples/invoice/acme-phoenix-saas/invoice.json` still validates byte-for-byte-schema-equivalent. KEEP and REUSE `$defs.money_string` (`^-?[0-9]+\.[0-9]{2}$`) and `$defs.party` for every family's money and party objects. Keep top-level `additionalProperties: true` and the optional `brand` object (S4 slot). Per-family required keys must match the atom-keyed contract each recipe's validate_data!/1 enforces (see 118-PATTERNS.md "Exact atom-keyed target shapes" table): statement = period{from,to}+account{name}+opening_balance+lines[]{date,description,amount}; receipt = title+date+customer{name}+lines[]{description,amount}; certificate = title+recipient+date; payslip = employer{name}+employee{name,id,tax_code}+period{from,to}+pay_date+earnings[]+deductions[]+net_pay; ticket = issuer{name}+title+placement[]{label,value}+code{reference}. All money fields reference `$defs.money_string`; all date fields use `format: date`. Verify JSV (draft-2020-12) accepts the chosen `if/then` construct. Do NOT add a `family` requirement in a way that breaks the existing invoice fixture — either default the invoice branch or add `family: "invoice"` to the invoice fixture in a separate follow-up (prefer making `family` optional with per-shape `if/then` on presence of discriminating keys if adding `family` to the existing invoice fixture would perturb its recorded byte-identity elsewhere).
  </action>
  <verify>
    <automated>mix test test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/docs_contract/examples_schema_contract_test.exs` passes with the existing invoice fixture still valid.
    - The schema contains a per-family branch structure (grep: `grep -c '"family"' priv/schemas/examples.schema.json` returns ≥ 1).
    - `$defs.money_string` and `$defs.party` are still present and referenced.
  </acceptance_criteria>
  <done>The schema validates the invoice fixture and is structurally ready to accept statement/receipt/certificate/payslip/ticket fixtures, each requiring only its own family keys.</done>
</task>

<task type="auto">
  <name>Task 2: Author statement / receipt / certificate fixtures (fictional businesses, D-01/D-02/D-03)</name>
  <files>priv/examples/statement/northwind-ledger-co/statement.json, priv/examples/receipt/harbor-and-oak-cafe/receipt.json, priv/examples/certificate/summit-training-institute/certificate.json</files>
  <read_first>
    - priv/examples/invoice/acme-phoenix-saas/invoice.json (fixture contract analog: fixture_id, optional paper/currency, party objects, money-as-decimal-strings, integer qty, optional empty "brand": {"logo": null})
    - lib/rendro/recipes/statement.ex, lib/rendro/recipes/receipt.ex, lib/rendro/recipes/certificate.ex (each `document/2` + `validate_data!/1` — the exact required keys)
    - 118-PATTERNS.md "Exact atom-keyed target shapes each document/2 requires" table
  </read_first>
  <action>
    Author three realistic, domain-correct fixtures with the string-keyed JSON shape the 118-03 transform will decode into atom-keyed recipe data. Statement (business: Northwind Ledger Co — planner discretion name per D-02, keep corpus voice) — period{from,to} ISO dates, account{name}, opening_balance + closing_balance as money strings, lines[]{date,description,amount} with a realistic ledger of debits/credits, optional summary. Receipt (business: Harbor & Oak Cafe) — title, date, customer{name}, lines[]{description,amount}, optional totals{subtotal,total}. Certificate (business: Summit Training Institute) — title, recipient, date, optional body/seal_line. ALL money as decimal strings matching `^-?[0-9]+\.[0-9]{2}$` (never JSON floats). Integer qty where present. Add `fixture_id`, optional `paper`/`currency`, and the optional empty S4 slot `"brand": {"logo": null}` to each. Fictional-only content — no real people, businesses, addresses, or tax IDs. Add the `family` discriminator field matching the schema convention chosen in Task 1.
  </action>
  <verify>
    <automated>mix test test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All three fixtures validate against the generalized schema (`mix test test/docs_contract/examples_schema_contract_test.exs` green).
    - No JSON float money: `grep -nE ':[[:space:]]*[0-9]+\.[0-9]+[^"]' priv/examples/statement/*/statement.json priv/examples/receipt/*/receipt.json priv/examples/certificate/*/certificate.json` returns nothing (money is quoted strings).
    - Each fixture has an S4 `"brand"` slot with `"logo": null`.
  </acceptance_criteria>
  <done>Three schema-valid, fictional, Decimal-string-money fixtures exist under priv/examples for statement, receipt, and certificate.</done>
</task>

<task type="auto">
  <name>Task 3: Author Aurora Live payslip + ticket fixtures (D-02, no-PII discipline)</name>
  <files>priv/examples/payslip/aurora-live/payslip.json, priv/examples/ticket/aurora-live/ticket.json</files>
  <read_first>
    - lib/rendro/recipes/payslip.ex, lib/rendro/recipes/ticket.ex (`document/2` + `validate_data!/1` required keys)
    - test/rendro/recipes/payslip_test.exs, test/rendro/recipes/ticket_test.exs (concrete data shapes to reuse as authoring references; NOTE Pitfall 5 business-name discrepancy)
    - 118-PATTERNS.md payslip/ticket rows in the shapes table (net_pay must reconcile earnings − deductions; masked payment_method; code.reference)
  </read_first>
  <action>
    Author the Aurora Live payslip and ticket fixtures. Payslip (employer name = Aurora Live per the D-02 lock; RECONCILE Pitfall 5 consciously — the payslip recipe test uses "Aurora Textiles Co." but D-02 locks "Aurora Live"; use "Aurora Live" for the fixture and note the deliberate choice in the SUMMARY): employer{name,address}, fictional employee{name,id,tax_code}, period{from,to}, pay_date, earnings[]{description,amount,ytd}, deductions[]{description,amount,ytd}, net_pay — and net_pay MUST equal (sum earnings − sum deductions) as decimal strings. Include optional payment_method as a MASKED token (e.g. the recipe's masking token displaying only trailing digits) — never a real account number. Ticket (issuer name = Aurora Live): title, optional subtitle (≤200 chars), placement[]{label,value} (seat/gate/section anchor), code{reference} human-readable reference string, optional terms (≤600 chars). No `code.image` (no caller-supplied PNG this milestone — keep text-only). ALL money as decimal strings. Fictional-only — the employee is invented; no real PII. Add the `family` discriminator field.
  </action>
  <verify>
    <automated>mix test test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both fixtures validate against the generalized schema.
    - Payslip net_pay equals earnings-minus-deductions (arithmetic checkable by the transform's reconciliation in 118-03; assert here that all three totals are decimal strings).
    - Payslip payment_method (if present) is a masked token with no full account/card number.
    - Ticket has a human-readable `code.reference` and no `code.image`.
    - Businesses are Aurora Live in both fixture directory names (`priv/examples/payslip/aurora-live/`, `priv/examples/ticket/aurora-live/`).
  </acceptance_criteria>
  <done>Two schema-valid Aurora Live fixtures exist with fictional, no-PII payslip and ticket data; the Payslip business-name discrepancy is consciously reconciled to "Aurora Live".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture author → shipped `priv/examples/**` | Authored data ships in the Hex tarball (text-only); the acute risk is real PII in the Payslip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-01 | Information disclosure | payslip.json fictional employee | high | mitigate | Fictional-only employee/business/tax_code; masked payment_method (trailing digits only, no full account number); reviewer PII check at phase verification (D-02, milestone guard) |
| T-118-02 | Tampering | examples.schema.json validation surface | low | accept | Schema is a repo-only build-time contract; generalization keeps `money_string` regex so float money is rejected mechanically |
</threat_model>

<verification>
- `mix test test/docs_contract/examples_schema_contract_test.exs` green with all six fixture shapes (invoice + five new).
- `mix test test/docs_contract/` green (no regression in the docs-contract lane).
- No JSON-float money anywhere in the new fixtures.
</verification>

<success_criteria>
The generalized schema validates all six fixture families; five new fictional, Decimal-string-money, S4-slotted fixtures exist; no real PII; the required test lane is green.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-01-SUMMARY.md` when done. Record the chosen fictional business names, the schema generalization construct used (if/then vs oneOf), and the Payslip "Aurora Live" reconciliation decision.
</output>
