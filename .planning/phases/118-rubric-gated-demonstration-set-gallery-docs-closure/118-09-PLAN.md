---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 09
type: execute
wave: 2
depends_on: ["118-08"]
files_modified:
  - assets/rendro/artifacts.json
  - assets/rendro/gallery/invoice.png
  - assets/rendro/gallery/branded_invoice.png
  - assets/rendro/gallery/statement.png
  - assets/rendro/gallery/receipt_report.png
  - assets/rendro/gallery/certificate.png
  - assets/rendro/gallery/payslip.png
  - assets/rendro/gallery/ticket.png
  - priv/quality/rubric_scores.json
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [SHOW-01]

must_haves:
  truths:
    - "Every demo entry in priv/quality/rubric_scores.json is `passed:true`, where `passed` is the exact passed?/2 computation (content_hierarchy == 5 AND every other core dimension >= 4 AND both gates true) — recorded honestly from the re-rendered PNGs, never asserted independently (SHOW-01, D-10/D-11)."
    - "The gallery and artifacts.json are regenerated from the 118-08 fixture/transform/recipe changes via `mix rendro.launch_artifacts.gen`, and `mix rendro.launch_artifacts.check` reports zero drift ('Launch artifacts VERIFIED')."
    - "REQUIREMENTS.md SHOW-01 (line ~51) and its coverage row (line ~103) accurately reflect the closed state — no longer marked with a GAP note while demos pass."
    - "The six already-green docs-contract lanes (rubric manifest, demo-cites-DOMAIN.md, DOMAIN.md contract, examples schema, accessibility overclaim, launch-artifacts claims) remain green — no regression from the composition rework."
  artifacts:
    - "priv/quality/rubric_scores.json — six honest score entries, every `passed:true`, `stress_exempt` absent/false, each citing an on-disk DOMAIN.md path, disjoint demo_ids from the stress-fixture set."
    - "assets/rendro/artifacts.json + assets/rendro/gallery/*.png — re-blessed to the improved renders with matching SHA-256 and S6 theme/mode/preset keys intact."
    - ".planning/REQUIREMENTS.md — SHOW-01 reconciled to Complete."
  key_links:
    - "118-08 fixture/recipe changes -> LaunchArtifacts (via Rendro.Examples + ExamplesData.transform, D-06) -> mix rendro.launch_artifacts.gen: regen must pick up the improved compositions automatically (one data source)."
    - "rubric_scores.json entries -> rubric_manifest_contract_test.exs passed?/2 arithmetic -> demo_cites_domain_md_test.exs DOMAIN.md paths: both contracts must stay green with the new honest scores."
  prohibitions:
    - "D-11: passing is EARNED, never assigned. Score each demo from the actual re-rendered raster against the concrete anchors. If any demo genuinely cannot reach hierarchy=5 / cores>=4 / both gates after 118-08, record its honest sub-gate score AND surface it as a finding (paused-finding SUMMARY) — do NOT inflate the score and do NOT flip `passed` to true. SHOW-01 is only closed when every entry is honestly passed:true."
    - "Do not hand-edit assets/rendro/gallery/*.png or assets/rendro/artifacts.json — regenerate only via `mix rendro.launch_artifacts.gen` (generated-block + hash discipline)."
---

<objective>
Close SHOW-01: re-render the gallery/artifacts from the improved 118-08 compositions, re-score all six demos HONESTLY so every entry is `passed:true`, and reconcile REQUIREMENTS.md to reality — then prove no regression across the already-green docs-contract lanes.

Purpose: the composition rework in 118-08 is only meaningful once the renders are re-blessed and the demos honestly clear the rubric gate recorded in the manifest.
Output: re-blessed gallery + artifacts.json, an honest all-passing rubric_scores.json, reconciled REQUIREMENTS.md.

Environment note (from 118-PATTERNS.md): pdfium-cli is not on this dev machine (Linux-only pin). Route `mix rendro.launch_artifacts.gen` and the D-09 raster self-scoring through the pinned CI container (Phase-117 / 118-05 precedent). The required source-PDF SHA-256 lane, all contract tests, schema validation, and the rubric arithmetic run locally without pdfium.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-FINDINGS.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-08-SUMMARY.md
@priv/quality/rubric_scores.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-render gallery + artifacts.json from the improved compositions (zero drift)</name>
  <files>assets/rendro/artifacts.json, assets/rendro/gallery/invoice.png, assets/rendro/gallery/branded_invoice.png, assets/rendro/gallery/statement.png, assets/rendro/gallery/receipt_report.png, assets/rendro/gallery/certificate.png, assets/rendro/gallery/payslip.png, assets/rendro/gallery/ticket.png</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex (build_source_document path — sources from priv/examples via Rendro.Examples + ExamplesData.transform, D-06; ordered_gallery_entry/1 S6 keys)
    - lib/mix/tasks/rendro/launch_artifacts/gen.ex, lib/mix/tasks/rendro/launch_artifacts/check.ex
    - assets/rendro/artifacts.json (current 7-entry manifest with S6 keys + hashes)
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-05-SUMMARY.md (the environment note — how 118-05 ran gen through the pinned pdfium build)
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-08-SUMMARY.md (what composition changed, so you know which renders will move)
  </read_first>
  <action>
    Regenerate the launch artifacts so the gallery reflects the 118-08 improvements. Run `mix rendro.launch_artifacts.gen` through the pinned pdfium container/build (the required source-PDF SHA-256 lane is byte-stable/portable; the PNG raster lane is advisory/pinned). This re-baselines every source-PDF SHA-256, PNG, and the generated README/recipes.md blocks from the improved compositions — an authorized, reviewed re-bless (D-08), not a determinism regression. Keep the 7-tile membership (invoice, branded_invoice, statement, receipt_report, certificate, payslip, ticket) and the S6 theme/mode/preset keys intact. Then run `mix rendro.launch_artifacts.check` and confirm it reports zero drift. Do NOT hand-edit any PNG or artifacts.json.
  </action>
  <verify>
    <automated>mix rendro.launch_artifacts.check</automated>
  </verify>
  <acceptance_criteria>
    - `mix rendro.launch_artifacts.check` prints "Launch artifacts VERIFIED" (zero drift).
    - assets/rendro/artifacts.json still has 7 ordered entries, each carrying png_sha256 + source_pdf_sha256 and theme/mode/preset keys.
    - `mix test test/docs_contract/launch_artifacts_claims_test.exs` exits 0 (generated blocks + hashes consistent).
  </acceptance_criteria>
  <done>Gallery + artifacts.json regenerated from the improved compositions with matching SHA-256 and S6 tags; launch_artifacts.check reports VERIFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Re-score all six demos honestly — every entry passed:true (or surface a finding)</name>
  <files>priv/quality/rubric_scores.json</files>
  <read_first>
    - priv/quality/rubric_scores.json (the six current honest-but-failing entries + dimension anchors 1/3/4/5 + gates + thresholds hierarchy_min=5/core_min=4 + stress_exemption)
    - priv/schemas/rubric_scores.schema.json ($defs.score_entry — required fields; additionalProperties:true permits justifications + domain_md)
    - test/docs_contract/rubric_manifest_contract_test.exs (the passed?/2 arithmetic to mirror EXACTLY; D-15 disjointness/teeth guards; stress_exempt tripwire)
    - test/docs_contract/demo_cites_domain_md_test.exs (asserts each entry's domain_md path exists on disk)
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-06-FINDINGS.md (the prior honest per-demo scores + anchors to re-assess against)
    - assets/rendro/gallery/*.png (the NEW re-rendered demo rasters from Task 1 — score against THESE, not the old ones)
  </read_first>
  <action>
    Re-run the D-09 honest self-scoring against the NEW re-rendered rasters (Task 1 output). For each of the six demos: rasterize via the pinned pdfium adapter (dpi 96, page 1), evaluate all six dimensions against the concrete 1/3/4/5 anchors and both pass/fail gates, and record a per-dimension justification string. Overwrite priv/quality/rubric_scores.json so each `scores[]` entry carries the honest dimension_scores, gate_results, justifications, domain_md path, and a `passed` boolean computed by the EXACT passed?/2 arithmetic (content_hierarchy == 5 AND other cores >= 4 AND both gates true) — never asserted independently. Keep demo_ids in the demonstration namespace (business segment guarantees disjointness from stress ids); keep stress_exempt absent/false; leave the stress_exemption block intact.
    HONESTY GATE (D-11): if any demo still does not honestly clear the bar after 118-08, record its true (failing) score AND stop — write a paused-finding SUMMARY surfacing which demo/dimension fell short and why, rather than inflating the score. SHOW-01 is closed ONLY when all six are honestly passed:true.
  </action>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/demo_cites_domain_md_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - priv/quality/rubric_scores.json: every entry in `scores[]` has `passed == true` (target for SHOW-01 closure).
    - For every entry: content_hierarchy == 5, every other core dimension >= 4, both gate_results true — and `passed` equals the passed?/2 computation over those values (no independent assertion).
    - Every entry has a resolvable `domain_md` path (demo_cites_domain_md_test.exs green) and `stress_exempt` absent/false (rubric_manifest_contract_test.exs green).
    - `mix test` for the two contract files exits 0.
    - If honesty gate trips: a paused-finding SUMMARY exists naming the shortfall; `passed` is NOT inflated.
  </acceptance_criteria>
  <done>All six demos re-scored honestly from the improved renders; every rubric_scores.json entry is passed:true under the exact passed?/2 arithmetic (or a finding is surfaced without inflation).</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md + prove no regression across the docs-contract lanes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SHOW-01 line ~51 with its GAP note; coverage row line ~103 "Gap — demos scored honestly...")
    - .planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-VERIFICATION.md (the 3/4 status this closure resolves)
    - test/docs_contract/ (the six already-green lanes to re-run for regression)
  </read_first>
  <action>
    Once Task 2 confirms every demo is honestly passed:true, reconcile .planning/REQUIREMENTS.md: change SHOW-01 (line ~51) from `[ ]` to `[x]` and remove/replace the stale "GAP (2026-07-19)" note with the closed state; update the coverage row (line ~103) from "Gap — demos scored honestly, none passes rubric" to the satisfied state. Use scoped edits — do not rewrite unrelated requirement lines. Then re-run the six docs-contract lanes that already pass to prove the composition rework introduced no regression: rubric_manifest_contract_test, demo_cites_domain_md_test, domain_md_contract_test, examples_schema_contract_test, accessibility_overclaim_test, launch_artifacts_claims_test. Do NOT weaken any contract to make it pass — a red lane is a real regression to fix in composition, not a test to soften.
    If Task 2's honesty gate tripped (a demo could not honestly pass), do NOT mark SHOW-01 complete — leave the gap recorded and stop.
  </action>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/demo_cites_domain_md_test.exs test/docs_contract/domain_md_contract_test.exs test/docs_contract/examples_schema_contract_test.exs test/docs_contract/accessibility_overclaim_test.exs test/docs_contract/launch_artifacts_claims_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md SHOW-01 (line ~51) is `[x]` with no stale GAP note, and the coverage row (line ~103) reflects the satisfied state — only if every demo is honestly passed:true.
    - All six docs-contract lanes above are green (0 failures) — no contract weakened.
    - `mix rendro.launch_artifacts.check` still reports VERIFIED (no drift reintroduced).
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects SHOW-01 closed; the six docs-contract lanes are green with no weakened contracts and zero gallery drift — the phase is 4/4.</done>
</task>

</tasks>

<verification>
- `mix rendro.launch_artifacts.check` → "Launch artifacts VERIFIED" (zero drift after re-bless).
- `mix test test/docs_contract/` → the six SHOW-01/03/04-relevant lanes green, 0 failures.
- priv/quality/rubric_scores.json → every `scores[].passed == true` (honest passed?/2 computation).
- REQUIREMENTS.md → SHOW-01 reconciled to `[x]`, no stale GAP note.
</verification>

<success_criteria>
SHOW-01 is closed: the gallery/artifacts are re-blessed from the improved compositions with zero drift, all six demos are honestly recorded as passed:true in the rubric manifest under the exact passed?/2 arithmetic, REQUIREMENTS.md matches reality, and every docs-contract lane is green — Phase 118 reaches 4/4 with milestone v2.10 closed. (Per D-11, if any demo cannot honestly pass, a finding is surfaced instead and SHOW-01 stays open.)
</success_criteria>

<artifacts_produced>
- Re-blessed assets/rendro/artifacts.json + assets/rendro/gallery/*.png (matching SHA-256, S6 keys).
- Honest all-passing priv/quality/rubric_scores.json (six entries, passed:true).
- Reconciled .planning/REQUIREMENTS.md (SHOW-01 closed).
</artifacts_produced>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-09-SUMMARY.md` when done.
</output>
