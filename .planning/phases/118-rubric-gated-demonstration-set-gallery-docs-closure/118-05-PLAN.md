---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 05
type: execute
wave: 4
depends_on: ["118-04"]
files_modified:
  - assets/rendro/artifacts.json
  - assets/rendro/gallery/payslip.png
  - assets/rendro/gallery/ticket.png
  - assets/rendro/manual.pdf
  - README.md
  - guides/recipes.md
  - lib/rendro/launch_artifacts.ex
autonomous: false
requirements: [SHOW-03]
must_haves:
  truths:
    - "assets/rendro/artifacts.json holds 7 entries with realistic re-baselined source-PDF/manual SHA-256 hashes and the S6 tags (D-08 authorized re-bless)"
    - "The required docs-contract lane (source-PDF + manual SHA-256) is green against the re-blessed hashes"
    - "The README @readme_start block and guides/recipes.md @recipes_start block are regenerated (never hand-edited) and carry the 2 new tiles"
  artifacts:
    - assets/rendro/artifacts.json
    - assets/rendro/gallery/payslip.png
    - assets/rendro/gallery/ticket.png
  key_links:
    - "mix rendro.launch_artifacts.gen (pinned pdfium container) → re-baselines every source-PDF/PNG hash + regenerates artifacts.json + README/recipes generated blocks via replace_block!"
    - "@expected_gallery_dimensions payslip/ticket provisional values → confirmed/corrected from the actual container .gen output"
---

<objective>
Run the gallery regeneration in the pinned pdfium container to re-baseline every source-PDF/PNG/manual SHA-256 to the realistic renders (the D-08 authorized, reviewed re-bless), producing the 7-entry `artifacts.json` with S6 tags, the two new gallery PNGs, and the regenerated README/`guides/recipes.md` generated blocks. Confirm the provisional payslip/ticket `@expected_gallery_dimensions` against the actual container output.

Purpose: SHOW-03 completion — realistic renders with matching hashes. This is CONTAINER-GATED: `pdfium-cli` is absent locally and Linux-pinned (`priv/pdfium_pin.json` → `pdfium-webassembly-linux-amd64`), mirroring Phase 117's "bless in the pinned CI container, not locally."
Output: Re-blessed `artifacts.json` + gallery PNGs + regenerated docs blocks, with a human re-bless checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-RESEARCH.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@lib/mix/tasks/rendro/launch_artifacts/gen.ex
@lib/mix/tasks/rendro/launch_artifacts/check.ex
@assets/rendro/artifacts.json
</context>

## Artifacts this phase produces (Plan 118-05 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `assets/rendro/artifacts.json` (re-blessed) | build artifact | 7 entries, realistic hashes, S6 tags; regenerated (never hand-edited) |
| `assets/rendro/gallery/payslip.png`, `ticket.png` | build artifact | new raster tiles (advisory lane) |
| `assets/rendro/manual.pdf` | build artifact | re-blessed manual SHA-256 |
| README.md + guides/recipes.md generated blocks | docs | regenerated via `replace_block!` |
| `lib/rendro/launch_artifacts.ex` `@expected_gallery_dimensions` | lib | confirmed/corrected payslip/ticket dims |

<tasks>

<task type="auto">
  <name>Task 1: Regenerate gallery + artifacts + docs blocks in the pinned pdfium container (SHOW-03, D-08)</name>
  <files>assets/rendro/artifacts.json, assets/rendro/gallery/payslip.png, assets/rendro/gallery/ticket.png, assets/rendro/manual.pdf, README.md, guides/recipes.md, lib/rendro/launch_artifacts.ex</files>
  <read_first>
    - lib/mix/tasks/rendro/launch_artifacts/gen.ex + check.ex (the regen/drift-check tasks; both require pdfium via `--pdfium PATH` or `RENDRO_PDFIUM_CLI`)
    - lib/rendro/launch_artifacts.ex:1010-1034 (with_pdfium/2 tool locator), :402-406 (write_docs_blocks/1), :584-598 (collect_docs_block_errors/2 drift contract)
    - priv/pdfium_pin.json (Linux pin), 118-RESEARCH.md §Environment Availability + Pitfall 3
    - 118-04-SUMMARY.md (provisional @expected_gallery_dimensions to confirm)
  </read_first>
  <action>
    In the pinned pdfium container (set `RENDRO_PDFIUM_CLI` to the pinned binary, or pass `--pdfium PATH`), run `mix rendro.launch_artifacts.gen` to regenerate: the 7 gallery PNGs (invoice, branded_invoice, statement, receipt_report, certificate, payslip, ticket), `assets/rendro/manual.pdf`, `assets/rendro/artifacts.json` (7 entries with re-baselined source_pdf_sha256 + png_sha256 + the S6 theme/mode/preset tags), and the README `@readme_start..@readme_end` + `guides/recipes.md` `@recipes_start..@recipes_end` generated blocks via `replace_block!`. Do NOT hand-edit `artifacts.json`, the PNGs, or inside the doc-block markers — the drift contract (`collect_docs_block_errors/2`) fails hand edits; the two new tiles flow into both blocks automatically because they iterate `manifest["gallery"]`. Read the actual rendered pixel dims from the `.gen` output and correct the provisional payslip/ticket `@expected_gallery_dimensions` in `launch_artifacts.ex` to the observed values. Then run `mix rendro.launch_artifacts.check` to confirm zero drift. Document the re-bless in the SUMMARY as an authorized D-08 re-bless (repointing to realistic data legitimately changes every hash — NOT a determinism regression).
  </action>
  <verify>
    <automated>mix rendro.launch_artifacts.check</automated>
  </verify>
  <acceptance_criteria>
    - `assets/rendro/artifacts.json` has exactly 7 entries, each with re-baselined `source_pdf_sha256`, `png_sha256`, and the S6 `theme`/`mode`/`preset` keys.
    - `mix rendro.launch_artifacts.check` reports zero drift (required source-PDF + manual SHA-256 lane green; advisory PNG lane green in the container).
    - `@expected_gallery_dimensions` for payslip/ticket match the actual container-rendered dims.
    - README + guides/recipes.md generated blocks contain the payslip and ticket tiles.
  </acceptance_criteria>
  <done>The gallery, artifacts.json (with S6 tags), manual, and docs blocks are regenerated in the container with matching re-blessed hashes and zero drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human re-bless of the regenerated gallery/artifacts hashes (D-08)</name>
  <action>Present the regenerated artifacts for human review and authorization of the D-08 re-bless before commit.</action>
  <what-built>Regenerated 7-tile gallery, artifacts.json (with S6 tags + realistic re-baselined hashes), manual.pdf, and the README/recipes.md generated blocks — all produced in the pinned pdfium container as the authorized D-08 re-bless.</what-built>
  <how-to-verify>
    1. Review `git diff assets/rendro/artifacts.json` — confirm 7 entries, every hash changed (expected — realistic re-bless), and each entry carries `theme`/`mode`/`preset`.
    2. Visually inspect `assets/rendro/gallery/payslip.png` and `assets/rendro/gallery/ticket.png` — confirm they render as a recognizable payslip and ticket (net pay / seat-gate anchors visible).
    3. Confirm `mix rendro.launch_artifacts.check` reports zero drift.
    4. Confirm the README + guides/recipes.md gallery blocks now list all 7 tiles and were regenerated (not hand-edited).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the re-bless, or describe the render/hash issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF binary → pdfium rasterizer | Rasterization writes to an isolated tmp dir; list-form args, no shell interpolation |
| generated artifacts → committed re-bless | A hash change must be human-authorized (D-08), not silently accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-08 | Tampering | pdfium invocation | low | accept | Adapter uses list-form args + isolated 0700/0600 tmp dir (existing, unchanged); no shell interpolation |
| T-118-09 | Repudiation | unreviewed hash re-bless | medium | mitigate | Blocking human-verify checkpoint authorizes the D-08 re-bless; `check` proves zero drift before commit |
</threat_model>

<verification>
- `mix rendro.launch_artifacts.check` zero drift (in the container).
- `mix test test/docs_contract/` green (generated-block drift contract passes; source-PDF/manual SHA-256 required lane green).
- Human re-bless checkpoint approved.
</verification>

<success_criteria>
The 7-tile gallery + artifacts.json (with S6 tags) + manual + docs blocks are regenerated with matching realistic hashes; the re-bless is human-authorized; zero drift.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-05-SUMMARY.md` when done. Record the confirmed payslip/ticket pixel dims and note the D-08 re-bless authorization.
</output>
