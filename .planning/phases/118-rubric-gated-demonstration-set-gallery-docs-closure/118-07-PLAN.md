---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 07
type: execute
wave: 5
depends_on: ["118-05", "118-06"]
files_modified:
  - test/docs_contract/accessibility_overclaim_test.exs
  - guides/recipes.md
  - guides/branding.md
  - guides/livebook/first_invoice.livemd
  - examples/phoenix_example/README.md
  - priv/support_matrix.json
  - README.md
autonomous: true
requirements: [SHOW-02, SHOW-04]
must_haves:
  truths:
    - "A docs-contract test fails if showcase wording (production-grade) ever co-occurs with a tagged-PDF/PDF-UA/screen-reader/reading-order accessibility claim in README or guides (D-14)"
    - "guides/recipes.md, guides/branding.md, guides/livebook/first_invoice.livemd, and examples/phoenix_example demonstrate the upgraded Invoice + new families against the realistic example library, claims bounded to evidence (SHOW-02)"
    - "priv/support_matrix.json + README are reconciled so every new family/claim maps to a resolvable test/evidence pointer (SHOW-04/D-15)"
  artifacts:
    - test/docs_contract/accessibility_overclaim_test.exs
    - priv/support_matrix.json
  key_links:
    - "accessibility_overclaim_test.exs scans README.md + guides/**/*.md → the D-14 tripwire bounding every claim edit in this plan"
    - "support_matrix.json payslip/ticket rows (Phase 116) → confirmed present + proof-backed; demonstration/gallery claims → resolvable evidence pointers"
---

<objective>
Close the milestone's docs surface (SHOW-02) and reconcile the proof-backed-claims contract (SHOW-04). Author the D-14 accessibility-overclaim tripwire test first, then update the guides / Livebook / phoenix_example to demonstrate the upgraded Invoice + new families against the realistic library, and reconcile `support_matrix.json` + README so every new family/claim is proof-backed and no accessibility overclaim is made.

Purpose: The milestone's honesty line — "production-grade" = information-design craft only; no tagged-PDF/PDF-UA/reading-order/screen-reader claim anywhere. Reading order is a rubric gate, never a public accessibility claim.
Output: The D-14 tripwire test + updated docs + reconciled support matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-RESEARCH.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@test/docs_contract/branding_claims_test.exs
@priv/support_matrix.json
</context>

## Artifacts this phase produces (Plan 118-07 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `test/docs_contract/accessibility_overclaim_test.exs` | test | D-14 tripwire (clones branding_claims_test.exs discipline) |
| `guides/recipes.md`, `guides/branding.md`, `guides/livebook/first_invoice.livemd`, `examples/phoenix_example/README.md` | docs | SHOW-02 realistic-library demonstrations |
| `priv/support_matrix.json` (reconciled) | manifest | SHOW-04/D-15 proof-backed rows |
| `README.md` (prose, outside generated block) | docs | SHOW-04 wording guarded |

<tasks>

<task type="auto">
  <name>Task 1: Author the D-14 accessibility-overclaim tripwire test (test-first, SHOW-04)</name>
  <files>test/docs_contract/accessibility_overclaim_test.exs</files>
  <read_first>
    - test/docs_contract/branding_claims_test.exs (full — the tripwire discipline to clone: `use ExUnit.Case, async: true`, `File.read!` per path, `refute content =~ term`)
    - 118-PATTERNS.md "accessibility_overclaim_test.exs" section (showcase terms + accessibility terms word lists; whole-file first cut, refine to proximity if too coarse)
  </read_first>
  <action>
    Author `test/docs_contract/accessibility_overclaim_test.exs` cloning the branding tripwire discipline. Define a showcase-terms list (start with the "production-grade" phrase; extend at discretion) and an accessibility-terms list (the tagged-PDF / PDF-UA / screen-reader / reading-order-conformance / accessibility-conformance claim vocabulary). For each path in `["README.md" | Path.wildcard("guides/**/*.md")]`, read the file and, if any showcase term is present, assert none of the accessibility terms co-occur, with a message naming the file + term + D-14. Keep the honest affordances legitimate: "logical reading order" as a rubric GATE description is fine only where it is not framed as a public accessibility-standard claim — tune the accessibility-terms list so it targets standard/conformance claims, not the rubric-gate vocabulary. Author this BEFORE editing the docs so it actively guards the Task 2/3 edits.
  </action>
  <verify>
    <automated>mix test test/docs_contract/accessibility_overclaim_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test exists and scans README.md + guides/**/*.md.
    - It passes against the current docs (green baseline — if it goes RED, an existing overclaim is a finding to fix in Task 3).
    - Removing the guard vocabulary would make it vacuous — reviewer confirms the term lists are non-empty and the co-occurrence logic has teeth.
  </acceptance_criteria>
  <done>The D-14 tripwire exists and is green, actively guarding subsequent doc edits.</done>
</task>

<task type="auto">
  <name>Task 2: Update guides / Livebook / phoenix_example to the realistic library (SHOW-02)</name>
  <files>guides/recipes.md, guides/branding.md, guides/livebook/first_invoice.livemd, examples/phoenix_example/README.md</files>
  <read_first>
    - guides/recipes.md (the generated @recipes_start..@recipes_end block is regenerated by 118-05 — do NOT hand-edit inside the markers; only edit prose OUTSIDE them)
    - guides/branding.md, guides/livebook/first_invoice.livemd, examples/phoenix_example/README.md (current toy-data demonstrations)
    - lib/rendro/examples.ex + 118-03-SUMMARY.md (how to load realistic fixtures through Rendro.Examples + the transform)
  </read_first>
  <action>
    Update the hand-written docs (NEVER inside generated `@recipes_start`/`@readme_start` markers) to demonstrate the upgraded Invoice (issuer/customer/due_date/terms/totals/Decimal money) plus the new families against the realistic example library sourced through `Rendro.Examples`. In `guides/branding.md` show the palette/branded-invoice story against the realistic invoice fixture. In `guides/livebook/first_invoice.livemd` update the walkthrough to load the realistic invoice fixture and render it (keep it runnable). In `examples/phoenix_example` update the example prose/code to reference the new families and the realistic library. Bound every prose claim to evidence (a test, a fixture, or the rubric manifest) — do NOT introduce any accessibility/tagged-PDF/reading-order-standard claim (the Task 1 tripwire will fail the build). Keep all money as Decimal-faithful; no float money in examples.
  </action>
  <verify>
    <automated>mix test test/docs_contract/</automated>
  </verify>
  <acceptance_criteria>
    - The three guides + phoenix_example reference the realistic example library and the new families (grep confirms `Rendro.Examples` / new family names in the updated docs).
    - No edits inside generated-block markers (the drift contract `collect_docs_block_errors/2` stays green).
    - `mix test test/docs_contract/` green (including the Task 1 D-14 tripwire and the generated-block drift contract).
    - Livebook remains structurally valid (loads the realistic fixture).
  </acceptance_criteria>
  <done>The guides, Livebook, and phoenix_example demonstrate the realistic library + new families with evidence-bounded claims and no overclaim.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile support_matrix.json + README (proof-backed, no overclaim) (SHOW-04/D-15)</name>
  <files>priv/support_matrix.json, README.md</files>
  <read_first>
    - priv/support_matrix.json (payslip row ~:474, ticket row ~:486 — confirm present + proof-backed from Phase 116; the support-matrix schema/contract)
    - priv/schemas/support_matrix.schema.json + the recipes_claims / support-matrix contract test
    - README.md (prose OUTSIDE the @readme_start..@readme_end generated block regenerated by 118-05)
  </read_first>
  <action>
    Confirm the Phase-116 `payslip` + `ticket` rows in `priv/support_matrix.json` are present and each maps to a resolvable evidence pointer (test path). Add or adjust any demonstration-set / gallery claim rows so every new family/claim resolves to a test or evidence pointer (the milestone's proof-backed-claims DNA) — no claim ships without backing. In README.md, edit ONLY the hand-written prose (never inside the `@readme_start`/`@readme_end` markers regenerated by 118-05): ensure the "production-grade" wording is present only as information-design craft and never co-occurs with an accessibility-standard claim (the Task 1 tripwire enforces this repo-wide). If the D-14 tripwire surfaced an existing overclaim in Task 1, fix it here. Run the support-matrix contract test + the full docs-contract lane.
  </action>
  <verify>
    <automated>mix test test/docs_contract/</automated>
  </verify>
  <acceptance_criteria>
    - `priv/support_matrix.json` payslip/ticket rows present with resolvable evidence pointers; any new demonstration/gallery claim has backing (support-matrix contract test green).
    - README "production-grade" wording carries no co-occurring accessibility-standard claim (D-14 tripwire green).
    - No edits inside README generated-block markers (drift contract green).
    - `mix test test/docs_contract/` green.
  </acceptance_criteria>
  <done>The support matrix and README are reconciled: every new family/claim is proof-backed and no accessibility overclaim is made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/README prose → published claims | The risk is a claims-integrity failure: an accessibility overclaim or an unbacked capability claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-13 | Repudiation/legal | accessibility overclaim near "production-grade" | high | mitigate | D-14 tripwire test (accessibility_overclaim_test.exs) fails the build if showcase wording co-occurs with a tagged-PDF/PDF-UA/screen-reader/reading-order-standard claim in README/guides |
| T-118-14 | Repudiation | unbacked family/gallery claim | medium | mitigate | support_matrix contract test requires every claim to resolve to a test/evidence pointer (D-15 proof-backed-claims discipline) |
</threat_model>

<verification>
- `mix test test/docs_contract/` green (D-14 tripwire, generated-block drift, support-matrix contract all pass).
- `mix ci.fast` green (full local suite; container-only advisory raster lane excluded locally).
- No hand edits inside any generated-block marker.
</verification>

<success_criteria>
The D-14 tripwire is active and green; guides/Livebook/phoenix_example demonstrate the realistic library + new families with evidence-bounded claims; support_matrix + README are reconciled and proof-backed with no accessibility overclaim.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-07-SUMMARY.md` when done. Record any existing overclaim the D-14 tripwire surfaced and how it was resolved.
</output>
