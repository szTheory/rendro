---
phase: 118-rubric-gated-demonstration-set-gallery-docs-closure
plan: 03
type: execute
wave: 2
depends_on: ["118-01", "118-02"]
files_modified:
  - lib/rendro/examples_data.ex
  - test/rendro/examples_data_test.exs
  - test/docs_contract/public_api_contract_test.exs
autonomous: true
requirements: [SHOW-01, SHOW-03]
must_haves:
  truths:
    - "Rendro.ExamplesData.transform_<family>/1 exists for all six families and produces the atom-keyed, Decimal/Date-typed map each recipe's document/2 consumes"
    - "Money is coerced faithfully via Decimal.new/1 (never Decimal.to_integer — cents are preserved, INV-02)"
    - "Rendro.ExamplesData is @moduledoc false and asserted absent from priv/public_api.json (hidden-internals contract)"
  artifacts:
    - lib/rendro/examples_data.ex
    - test/rendro/examples_data_test.exs
  key_links:
    - "Rendro.Examples.load!/1 → Rendro.ExamplesData.transform_<family>/1 → Recipes.<Family>.document/2 (the D-06 single data source both the demo set and the gallery share)"
    - "public_api_contract_test.exs hidden_modules list must include Rendro.ExamplesData or the hidden-internals contract fails"
---

<objective>
Author the JSON→recipe transform layer (RESEARCH finding #1, the phase's single largest concealed task). `Rendro.Examples.load!/1` returns raw string-keyed JSON with money-as-strings, but every recipe's `document/2` consumes atom-keyed maps with `Date.t()`/`Decimal.t()` values. There is NO faithful transform today (the only analog is a lossy `.exs` bench script that `Decimal.to_integer`s money). This plan authors `Rendro.ExamplesData` with a per-family `transform_<family>/1` and a unit test covering all six shapes.

Purpose: This is the one-source-of-truth seam for D-06 — the demonstration set (SHOW-01) and the gallery (SHOW-03) both render from the same fixtures through this transform.
Output: `lib/rendro/examples_data.ex` (@moduledoc false) + unit test + hidden-internals contract update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-CONTEXT.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-RESEARCH.md
@.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-PATTERNS.md
@lib/rendro/examples.ex
@bench/comparison/fixtures/invoice_rendro.exs
</context>

## Artifacts this phase produces (Plan 118-03 scope)

| Artifact | Kind | Notes |
|----------|------|-------|
| `Rendro.ExamplesData` (`lib/rendro/examples_data.ex`, `@moduledoc false`) | lib | `transform_invoice/1`, `transform_statement/1`, `transform_receipt/1`, `transform_certificate/1`, `transform_payslip/1`, `transform_ticket/1` |
| `test/rendro/examples_data_test.exs` | test | round-trips each of the six fixtures through its transform into its recipe's `document/2` without KeyError/FunctionClauseError |
| `test/docs_contract/public_api_contract_test.exs` (hidden list) | test | adds `Rendro.ExamplesData` to `hidden_modules` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Rendro.ExamplesData with a faithful per-family transform (finding #1, INV-02)</name>
  <files>lib/rendro/examples_data.ex</files>
  <read_first>
    - lib/rendro/examples.ex (the `load!/1` loader signature + `@moduledoc false` pattern to mirror)
    - bench/comparison/fixtures/invoice_rendro.exs:8-23 (the ONLY existing transform — ADAPT, do NOT copy; it lossily does `Decimal.to_integer` which drops cents)
    - lib/rendro/recipes/{invoice,statement,receipt,certificate,payslip,ticket}.ex (each `validate_data!/1` — the exact required atom keys and value types)
    - 118-PATTERNS.md "Exact atom-keyed target shapes each document/2 requires" table
    - the six fixtures authored in 118-01 (invoice already existed; statement/receipt/certificate/payslip/ticket from Wave 1)
  </read_first>
  <behavior>
    - transform_invoice(load!("invoice/acme-phoenix-saas/invoice.json")) → %{id, date: Date.t(), items: [%{name, qty, price: Decimal.t()}]} accepted by Rendro.Recipes.Invoice.document/1 without error.
    - transform_payslip(...) → net_pay is a Decimal equal to (sum earnings − sum deductions); dates are Date.t(); all money Decimal.t().
    - Every money field is Decimal.new(string) — NEVER Decimal.to_integer (cents preserved).
    - Every ISO date string → Date.from_iso8601!/1.
    - String keys → atom keys per family; no residual string keys in the output map.
  </behavior>
  <action>
    Create `Rendro.ExamplesData` as `@moduledoc false`. Provide `transform_invoice/1`, `transform_statement/1`, `transform_receipt/1`, `transform_certificate/1`, `transform_payslip/1`, `transform_ticket/1`, each taking the raw string-keyed map from `Rendro.Examples.load!/1` and returning the atom-keyed, Decimal/Date-typed map its recipe's `validate_data!/1` requires (mirror the shapes in the 118-PATTERNS.md table precisely). Coerce money strings via `Decimal.new/1` (faithful — DO NOT `Decimal.to_integer`, that is the bench script's lossy bug per INV-02). Coerce ISO dates via `Date.from_iso8601!/1`. For payslip, preserve earnings/deductions ytd Decimals and the reconciling net_pay. For ticket, keep strings and pass the human-readable `code.reference`; do not synthesize a `code.image`. Keep the module out of any public surface — `@moduledoc false`, no `@doc` public entries beyond what mirrors `Rendro.Examples`. Do NOT read files directly; callers pass the loader output (or provide a thin `transform/2` that takes a `family` atom + loaded map and dispatches, callers using `Rendro.Examples.load!/1`).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/rendro/examples_data.ex` compiles with `--warnings-as-errors`.
    - Six `transform_<family>/1` functions exist (`grep -c 'def transform_' lib/rendro/examples_data.ex` returns 6).
    - No `Decimal.to_integer` in the module (`grep -c 'Decimal.to_integer' lib/rendro/examples_data.ex` returns 0).
    - Module is `@moduledoc false`.
  </acceptance_criteria>
  <done>A faithful, @moduledoc-false per-family transform module exists that preserves Decimal cents and produces recipe-ready atom-keyed data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test the six transforms through each recipe's document/2 + register the module hidden (finding #1, hidden-internals contract)</name>
  <files>test/rendro/examples_data_test.exs, test/docs_contract/public_api_contract_test.exs</files>
  <read_first>
    - test/rendro/launch_artifacts_test.exs (test harness style for driving recipe renders)
    - test/docs_contract/public_api_contract_test.exs:82-110 (the hidden-internals contract; `hidden_modules` list includes `Rendro.Examples` at ~line 90 — this is a non-vacuous contract that fails if a listed module is not actually `:hidden`)
  </read_first>
  <behavior>
    - For each of the six families: load the fixture, transform, feed to Recipes.<Family>.document/1(+opts), and assert a %Rendro.Document{} is returned (no KeyError/FunctionClauseError) — proving Pitfall 1 is closed.
    - Payslip test asserts net_pay reconciles (Decimal.equal? net_pay, earnings_sum minus deductions_sum).
    - The hidden-internals contract passes with Rendro.ExamplesData added.
  </behavior>
  <action>
    Author `test/rendro/examples_data_test.exs`: one test per family that calls `Rendro.Examples.load!("<domain>/<business>/<family>.json") |> Rendro.ExamplesData.transform_<family>() |> Rendro.Recipes.<Family>.document()` (certificate uses `document(data, border: true)`) and asserts the result is a `%Rendro.Document{}` — proving the transform output matches each recipe's contract. Add a payslip reconciliation assertion (`Decimal.equal?/2` net_pay vs earnings−deductions). Then add `Rendro.ExamplesData` to the `hidden_modules` list in `public_api_contract_test.exs` (mirroring how `Rendro.Examples` is listed) so the hidden-internals contract tracks the new module. Do NOT add the module to `priv/public_api.json` — it stays out of the public tier.
  </action>
  <verify>
    <automated>mix test test/rendro/examples_data_test.exs test/docs_contract/public_api_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All six family transform tests pass (render to `%Rendro.Document{}`).
    - Payslip reconciliation assertion passes.
    - `mix test test/docs_contract/public_api_contract_test.exs` passes with `Rendro.ExamplesData` in the hidden list.
    - `Rendro.ExamplesData` is absent from `priv/public_api.json` (`grep -c ExamplesData priv/public_api.json` returns 0).
  </acceptance_criteria>
  <done>The six transforms are proven to feed their recipes cleanly, payslip reconciles, and the new module is contract-registered as hidden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `priv/examples/**` path → `Rendro.Examples.load!/1` | Fixture path resolution; already guarded by `Path.safe_relative/1` in the loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-04 | Tampering | fixture path traversal | low | accept | The transform never reads files directly — it consumes `Rendro.Examples.load!/1` output, whose `Path.safe_relative/1` guard (examples.ex:41-50) still bounds the new domains; no new fixture-name input surface |
| T-118-05 | Information disclosure | public API leakage of internal transform | medium | mitigate | `@moduledoc false` + hidden-internals contract entry keeps `Rendro.ExamplesData` out of `priv/public_api.json` (mirrors `Rendro.Examples`) |
</threat_model>

<verification>
- `mix test test/rendro/examples_data_test.exs test/docs_contract/public_api_contract_test.exs` green.
- `mix test test/docs_contract/` green (no regression).
- `mix compile --warnings-as-errors` clean.
</verification>

<success_criteria>
A faithful six-family JSON→recipe transform exists, is unit-tested through each recipe, preserves Decimal cents, and is contract-registered as a hidden internal module.
</success_criteria>

<output>
Create `.planning/phases/118-rubric-gated-demonstration-set-gallery-docs-closure/118-03-SUMMARY.md` when done. Record the module name/API and the transform signatures the 118-04 repoint will call.
</output>
