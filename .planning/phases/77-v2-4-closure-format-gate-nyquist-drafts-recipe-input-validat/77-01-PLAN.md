---
phase: 77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes/statement.ex
  - lib/rendro/recipes/receipt.ex
  - lib/rendro/recipes/certificate.ex
  - test/rendro/recipes/receipt_test.exs
  - test/rendro/recipes/certificate_test.exs
  - test/rendro/recipes/statement_test.exs
autonomous: true
requirements: []   # closure phase — no new REQ-IDs; maps to Success Criterion 4 + D-05..D-09
must_haves:
  truths:
    - "[SC-4][D-05] Statement.document/2 raises structured ArgumentError (not BadMapError) for a non-map :account"
    - "[SC-4][D-06] Receipt.document/2 raises structured ArgumentError (not BadMapError) for a non-map :customer"
    - "[SC-4][D-06] Receipt.document/2 raises structured ArgumentError (not FunctionClauseError) for a non-%Date{} :date"
    - "[SC-4][D-06] Receipt :totals.total validation is shape-consistent with the :subtotal check"
    - "[SC-4][D-07] Certificate.document/2 raises structured ArgumentError (not FunctionClauseError) for a non-%Date{} :date"
    - "[SC-4][D-07] Certificate.document/2 raises structured ArgumentError for a non-binary :body"
    - "[SC-4][D-08] Negative-path assert_raise ArgumentError tests exist for every new validation clause and pass"
    - "[SC-4][D-09] statement.ex capacity comment is factually corrected; 14.4 is a named module attribute; discarded map_reduce in maybe_validate_summary!/1 is Enum.reduce; certificate.ex dead _content_w binding removed"
    - "[SC-4][D-09] Rendered output unchanged — full existing suite stays green, determinism tests unaffected"
  artifacts:
    - path: "lib/rendro/recipes/statement.ex"
      provides: "validate_account!/1 clause + cosmetic D-09 fixes"
      contains: "defp validate_account!"
    - path: "lib/rendro/recipes/receipt.ex"
      provides: "validate_customer!/1 + validate_date!/1 clauses"
      contains: "defp validate_customer!"
    - path: "lib/rendro/recipes/certificate.ex"
      provides: "validate_date!/1 + validate_body!/1 clauses; dead binding removed"
      contains: "defp validate_body!"
    - path: "test/rendro/recipes/receipt_test.exs"
      provides: "negative-path ArgumentError tests for :customer and :date"
    - path: "test/rendro/recipes/certificate_test.exs"
      provides: "negative-path ArgumentError tests for :date and :body"
  key_links:
    - from: "lib/rendro/recipes/statement.ex validate_data!/1"
      to: "validate_account!/1"
      via: "call inserted between validate_period! and validate_lines!"
      pattern: "validate_account!\\(data.account\\)"
    - from: "lib/rendro/recipes/receipt.ex validate_data!/1"
      to: "validate_customer!/1, validate_date!/1"
      via: "calls inserted before validate_lines!"
      pattern: "validate_customer!\\(data.customer\\)"
    - from: "lib/rendro/recipes/certificate.ex validate_data!/1"
      to: "validate_date!/1, validate_body!/1"
      via: "calls inserted after required-keys block"
      pattern: "validate_body!"
    - from: "new validation clauses"
      to: "Rendro.Recipes.Pagination.type_name/1"
      via: "Why: line of each structured-error heredoc"
      pattern: "Rendro.Recipes.Pagination.type_name"
---

<objective>
Harden malformed-input handling across the three v2.4 recipes (Statement, Receipt, Certificate) by replicating the existing in-file structured-`ArgumentError` validation pattern, add negative-path `assert_raise` tests for each new clause, and apply the four cosmetic D-09 cleanups (corrected capacity comment, named `14.4` attribute, `Enum.reduce` for the discarded `map_reduce`, removed dead `_content_w` binding).

Purpose: Closes Success Criterion 4 — callers receive predictable, structured `What:/Where:/Why:/Next:` errors instead of raw `BadMapError`/`FunctionClauseError` on attacker/caller-controlled data maps (no info leak via raw stacktraces, deterministic failure mode). Closes audit WARNINGs (74) and WR-01..06 (75) plus the info-tier cosmetic items.
Output: Three modified recipe modules, two test files with new negative-path blocks. No rendered-output change; existing 920-test suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-CONTEXT.md
@.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Statement :account validation + D-09 cosmetic cleanups</name>
  <files>lib/rendro/recipes/statement.ex, test/rendro/recipes/statement_test.exs</files>
  <read_first>
    - lib/rendro/recipes/statement.ex — validate_data!/1 (lines 432-451), validate_period!/1 (canonical map-shape analog, ~498-509), validate_line_date!/2 (~576-587), typical_row_h block (~308-314), maybe_validate_summary!/1 (~676-682), header_section/2 (~257 — Map.get(account, :name, "")), geometry @-constants (~86-112), capacity comment (~293-297)
    - lib/rendro/recipes/pagination.ex — type_name/1 (lines 76-82)
    - .planning/phases/77-*/77-PATTERNS.md — "lib/rendro/recipes/statement.ex (recipe — D-05 + D-09)" section, including the verified line-number corrections (676-682 NOT 705-711; 14.4 at 311; do NOT touch fold_balance/2 at 405-413)
    - test/rendro/recipes/statement_test.exs — describe "V8: validate_data!/1 rejects malformed input" block (lines 430-513) for negative-path test style
  </read_first>
  <behavior>
    - Test: Statement.document/2 with :account replaced by a non-map (e.g. "Acme") raises ArgumentError matching ~r/account/i
    - Test: Statement.document/2 with a valid %{name: "..."} account still renders (existing happy-path test stays green)
    - No determinism/byte-output test regresses (cosmetic D-09 edits change no rendered output)
  </behavior>
  <action>
    D-05: Add a `validate_account!/1` to statement.ex modeled on `validate_period!/1` — a passing guard clause `defp validate_account!(%{name: name}) when is_binary(name), do: :ok` followed by a catch-all that raises `ArgumentError` with the `What:/Where:/Why:/Next:` heredoc. The `Where:` line is `Rendro.Recipes.Statement.validate_data!/1`; the `Why:` line names the received type via `Rendro.Recipes.Pagination.type_name(value)` exactly as the existing clauses at statement.ex:493/517/622 do. Wire the call into `validate_data!/1` between `validate_period!(data.period)` and `validate_lines!(data.lines)`. Prefer the map-shape-with-string-name guard as the floor (header_section already degrades gracefully on missing :name via Map.get default).
    D-09 cosmetic (must NOT change rendered output): (1) Rewrite the capacity comment at statement.ex:293-297 to state the true behavior — a conservative ~8% under-pack with no overflow risk — and leave the `@body_height - @header_height - @footer_height` arithmetic at line 297 numerically unchanged. (2) Extract the magic `14.4` at statement.ex:311 into a named module attribute placed with the other geometry `@`-constants near lines 86-112 (planner discretion on the name; e.g. `@default_row_height`); reference it in the `typical_row_h` empty-list branch. Tighten the 308-309 comment only if it misleads on mean-vs-median. (3) In `maybe_validate_summary!/1` (statement.ex:676-682) replace the `Enum.map_reduce` whose `rows` result is discarded (`_ = rows`) with `Enum.reduce` that yields only `derived_closing`, mirroring `maybe_validate_closing_balance!/1` at ~656. Do NOT touch `fold_balance/2` at 405-413 — its map_reduce result is used.
    D-08 (this file): Add one negative-path test in the V8 describe block: start from a valid `fixture_data(0)`, `Map.put(:account, "not-a-map")`, and `assert_raise ArgumentError, ~r/account/i, fn -> Statement.document(data) end`, mirroring the existing "malformed period raises" shape-only test.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/statement_test.exs && mix test test/rendro/deterministic_test.exs</automated>
  </verify>
  <done>
    statement.ex has a `validate_account!/1` clause called from `validate_data!/1`; a non-map account raises ArgumentError matching ~r/account/i; the capacity comment is factually corrected; `14.4` is a named module attribute; `maybe_validate_summary!/1` uses `Enum.reduce`; the new negative-path test and all existing statement + determinism tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Receipt + Certificate input validation, cosmetic cleanup, and negative-path tests</name>
  <files>lib/rendro/recipes/receipt.ex, lib/rendro/recipes/certificate.ex, test/rendro/recipes/receipt_test.exs, test/rendro/recipes/certificate_test.exs</files>
  <read_first>
    - lib/rendro/recipes/receipt.ex — validate_data!/1 (lines 362-378), maybe_validate_totals!/1 (~476-525, note asymmetric :total vs :subtotal branches), header_section/2 (~248 Map.get(customer, :name, ""); ~256 fmt_date.(date))
    - lib/rendro/recipes/certificate.ex — validate_data!/1 (lines 200-238), validate_brand!/1 (240-257), body_section/3 (~189-190 Rendro.text(body_text); fmt_date.(data.date)), dead binding _content_w (line 180, with 178-179 comment)
    - lib/rendro/recipes/statement.ex — validate_period!/1, validate_line_date!/2, validate_line_description!/2 (the three canonical analog clauses to copy: map-shape, %Date{}, is_binary)
    - lib/rendro/recipes/pagination.ex — type_name/1 (lines 76-82)
    - .planning/phases/77-*/77-PATTERNS.md — "receipt.ex (D-06)" and "certificate.ex (D-07 + D-09)" sections (full replicated-clause guidance + WR-01..06 follow-ups)
    - test/rendro/recipes/receipt_test.exs and test/rendro/recipes/certificate_test.exs — existing structure to slot new negative-path blocks into; test/rendro/recipes/statement_test.exs V8 block as the style analog
  </read_first>
  <behavior>
    - Test: Receipt.document/2 with non-map :customer raises ArgumentError ~r/customer/i
    - Test: Receipt.document/2 with non-%Date{} :date raises ArgumentError ~r/date/i
    - Test: Certificate.document/2 with non-%Date{} :date raises ArgumentError ~r/date/i
    - Test: Certificate.document/2 with non-binary :body raises ArgumentError ~r/body/i
    - Test: valid Receipt and Certificate fixtures still render (happy-path tests stay green)
  </behavior>
  <action>
    D-06 (receipt.ex): Add `validate_customer!/1` (copy statement `validate_account!/1` — `%{name: name} when is_binary(name)` passing clause + catch-all heredoc) and `validate_date!/1` (copy statement `validate_line_date!/2` — `%Date{}` passing clause + catch-all heredoc, but arity-1 and non-indexed). `Where:` lines read `Rendro.Recipes.Receipt.validate_data!/1`; `Why:` lines use `Rendro.Recipes.Pagination.type_name(value)`. Wire both calls into `validate_data!/1` before `validate_lines!`. In `maybe_validate_totals!/1`, make the `:totals.total` branch shape-consistent with the `:subtotal` branch (the audit/WR notes the total check is asymmetric) — restructure for symmetry only, do NOT add new validation behavior. Note: brand validation does not apply to Receipt.
    D-07 (certificate.ex): Add `validate_date!/1` (`%Date{}` guard + catch-all heredoc) and `validate_body!/1` as three clauses — `is_binary(body) and byte_size(body) > 2000` (raises the existing 2000-byte message), `is_binary(body)` returns `:ok`, and a non-binary catch-all that raises the structured `:body` type error. `Where:` lines read `Rendro.Recipes.Certificate.validate_data!/1`. Wire `validate_date!(data.date)` and `validate_body!(Map.get(data, :body, ""))` into `validate_data!/1` after the missing-keys block, replacing the current `if is_binary(body)` length check. Leave `validate_brand!/1` clause ordering as-is (audit flags fragility but does not require a change; upgrading to the heredoc template is optional and out of this plan's required scope).
    D-09 (certificate.ex): Remove the dead `_content_w` binding at certificate.ex:180 and the 2-line comment at 178-179 that references it. No output change.
    D-08 tests: In receipt_test.exs add an `assert_raise ArgumentError, ~r/customer/i, ...` and an `assert_raise ArgumentError, ~r/date/i, ...` (start from a valid receipt fixture, `Map.put` the malformed key). In certificate_test.exs add `assert_raise ArgumentError, ~r/date/i, ...` and `assert_raise ArgumentError, ~r/body/i, ...` the same way. Mirror the statement_test.exs V8 shape-only style.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/receipt_test.exs test/rendro/recipes/certificate_test.exs</automated>
  </verify>
  <done>
    receipt.ex has validate_customer!/1 + validate_date!/1 wired into validate_data!/1 with the :totals.total branch made symmetric; certificate.ex has validate_date!/1 + validate_body!/1 wired in and the dead _content_w binding removed; the four new negative-path tests pass; all existing receipt and certificate tests stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → recipe `document/2` | The recipe receives a caller/attacker-controlled data map (`:account`, `:customer`, `:date`, `:body`, `:totals`). This is the untrusted-input surface this plan hardens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Information Disclosure | Statement/Receipt/Certificate `validate_data!/1` | mitigate | Convert raw `BadMapError`/`FunctionClauseError` (which leak internal struct/clause detail in stacktraces) into structured `ArgumentError` with a bounded `What:/Where:/Why:/Next:` message; `Why:` reports only the caller's own `inspect`-ed value + `type_name/1`, no engine internals. |
| T-77-02 | Denial of Service | Certificate `:body` | mitigate | `validate_body!/1` keeps the existing 2000-byte cap as an explicit guard clause and now rejects non-binary `:body` before it reaches the renderer, giving a predictable early failure mode rather than a deep downstream crash. |
| T-77-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan — Elixir recipe/test edits only, no new deps. |
</threat_model>

<verification>
- `mix test test/rendro/recipes/statement_test.exs test/rendro/recipes/receipt_test.exs test/rendro/recipes/certificate_test.exs` — all green, includes the new negative-path tests.
- `mix test test/rendro/deterministic_test.exs` — green (cosmetic D-09 edits change no rendered output).
- Each new validation clause names the received type via `Rendro.Recipes.Pagination.type_name/1` in its `Why:` line.
- `grep -n "defp validate_account!" lib/rendro/recipes/statement.ex` and the equivalent for receipt/certificate return the new clauses.
</verification>

<success_criteria>
- Maps to ROADMAP Success Criterion 4 (structured ArgumentError + cosmetic cleanup).
- Statement non-map `:account`, Receipt non-map `:customer` / non-Date `:date`, Certificate non-Date `:date` / non-binary `:body` all raise structured `ArgumentError`.
- Four cosmetic D-09 items applied with no rendered-output change.
- Full existing suite + new negative-path tests green.
</success_criteria>

<output>
Create `.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-01-SUMMARY.md` when done.
</output>
