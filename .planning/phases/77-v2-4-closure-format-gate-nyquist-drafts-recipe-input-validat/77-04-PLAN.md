---
phase: 77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat
plan: 04
type: execute
wave: 2
depends_on: ["77-01", "77-02"]
files_modified:
  - test/docs_contract/recipes_claims_test.exs
  - test/guardrails/required_checks_contract_test.exs
  - lib/rendro/pipeline/paginate.ex
  - test/rendro/deterministic_test.exs
  - test/rendro/recipes/statement_test.exs
  - guides/recipes.md
autonomous: true
requirements: []   # closure phase — no new REQ-IDs; maps to Success Criteria 1 + 2 + D-01 / D-02 / D-10 terminal gate
must_haves:
  truths:
    - "[SC-1][D-01] mix format --check-formatted exits 0 from a clean committed tree — the two committed offenders (recipes_claims_test.exs, required_checks_contract_test.exs) and all 77-01/77-02 edits are formatted"
    - "[SC-1][D-10] mix ci passes its first step (format --check-formatted) from a clean tree — the required test branch-protection lane is green"
    - "[SC-2][D-02] The audit-flagged working-tree changes (paginate.ex, deterministic_test.exs, statement_test.exs, guides/recipes.md) are committed with intent — no stray milestone drift remains"
    - "[SC-2][D-02] guides/recipes.md doc-language tightening still passes the docs-contract tests"
    - "[D-10] The full mix test suite (920+ tests including the new 77-01 negative-path tests) is green"
  artifacts:
    - path: "test/docs_contract/recipes_claims_test.exs"
      provides: "format-compliant committed test (was a format offender)"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "format-compliant committed test (was a format offender)"
  key_links:
    - from: "mix ci alias"
      to: "format --check-formatted"
      via: "first step of the ci alias must pass from a clean tree"
      pattern: "format --check-formatted"
---

<objective>
Terminal gate plan: format the two committed offenders, run a final `mix format` so every 77-01/77-02 edit and every audit-flagged working-tree change is format-clean, run the full suite, commit everything with intent (the pure-format normalizations in `paginate.ex`/`deterministic_test.exs`/`statement_test.exs`, the `guides/recipes.md` doc tightening), and prove `mix format --check-formatted` exits 0 and `mix ci` passes its first step from a clean committed tree.

Purpose: Closes Success Criterion 1 (green the `mix ci` format gate / required `test` lane) and the remainder of Success Criterion 2 (commit the working-tree changes with intent), and is the D-10 terminal gate. Runs in Wave 2 so that the 77-01 code edits and 77-02 mix.exs/guide edits are already applied and formatted before the clean-tree proof — no plan may claim "format gate green" while another plan still has un-formatted pending edits.
Output: A clean committed tree with a green format gate and full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.4-MILESTONE-AUDIT.md
@.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-CONTEXT.md
@.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format the committed offenders and verify the guides/recipes.md tightening</name>
  <files>test/docs_contract/recipes_claims_test.exs, test/guardrails/required_checks_contract_test.exs, guides/recipes.md</files>
  <read_first>
    - .planning/v2.4-MILESTONE-AUDIT.md — Tech Debt items #1 (format offenders) and #2 (uncommitted changes); the two named offender files
    - .planning/phases/77-*/77-CONTEXT.md — D-01 (format gate), D-02 (working-tree resolution: guides/recipes.md doc tightening is intentional, commit it)
    - guides/recipes.md — the CONTRACT-02 doc-language tightening already in the working tree (removes the explicit unsupported-array enumeration in favor of "claims that exceed the support matrix are not made here")
    - test/docs_contract/recipes_claims_test.exs — must still pass with the tightened guides/recipes.md
  </read_first>
  <action>
    D-01: Run `mix format` so the two committed offenders (test/docs_contract/recipes_claims_test.exs — long assert/Enum.find lines that must wrap; test/guardrails/required_checks_contract_test.exs) become format-compliant. Then confirm the already-applied guides/recipes.md doc-language tightening (D-02) still satisfies the docs-contract suite — run `mix test test/docs_contract/`. Do NOT revert the guides/recipes.md change; it is intentional per D-02. Do not change behavior of either test file beyond line-wrapping that `mix format` produces.
  </action>
  <acceptance_criteria>
    - `mix format --check-formatted test/docs_contract/recipes_claims_test.exs test/guardrails/required_checks_contract_test.exs` exits 0.
    - `mix test test/docs_contract/` exits 0 with the tightened guides/recipes.md.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted test/docs_contract/recipes_claims_test.exs test/guardrails/required_checks_contract_test.exs && mix test test/docs_contract/</automated>
  </verify>
  <done>
    Both former format offenders pass `--check-formatted`; the guides/recipes.md tightening still passes the docs-contract suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final mix format, full suite, commit with intent, prove the clean-tree format gate</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/deterministic_test.exs, test/rendro/recipes/statement_test.exs, guides/user_flows_and_jtbd.md</files>
  <read_first>
    - .planning/v2.4-MILESTONE-AUDIT.md — Tech Debt #2 (the uncommitted edits to paginate.ex, deterministic_test.exs, statement_test.exs are pure mix format normalizations — correct and intentional, commit them; do not revert)
    - .planning/phases/77-*/77-CONTEXT.md — D-02 (commit working-tree changes with intent), D-10 (terminal gate: clean committed tree, format --check-formatted exits 0, full mix test green, mix ci first step passes)
    - mix.exs — the `ci` alias (lines 63+): format --check-formatted is the first step
  </read_first>
  <action>
    D-02/D-10: Run `mix format` across the whole tree as the LAST mechanical step so every 77-01 recipe/test edit and 77-02 mix.exs/guide edit is format-clean (CONTEXT specifies mix format must be the final step before commit). Run the full `mix test` suite and confirm 920+ tests green (including the new 77-01 negative-path tests). Then commit ALL the audit-flagged working-tree changes WITH INTENT in one focused commit: the pure-format normalizations in lib/rendro/pipeline/paginate.ex, test/rendro/deterministic_test.exs, test/rendro/recipes/statement_test.exs (line-wrapping, blank-line insertion, `& &1.x` → `&(&1.x)` — correct, do NOT revert), the guides/recipes.md doc tightening, plus the formatted offenders and all 77-01/77-02/77-03 edits. Explicitly `git add guides/user_flows_and_jtbd.md` — it is UNTRACKED, so a bare `git commit -a` will skip it; stage it by name so the JTBD guide is committed, not left untracked. From the resulting clean committed tree, prove `mix format --check-formatted` exits 0 and the `mix ci` first step (`format --check-formatted`) is green. End commit messages with the Co-Authored-By trailer; branch first if on the default branch (current branch is plan/phase-74-statement-recipe — confirm before committing).
  </action>
  <acceptance_criteria>
    - `git status --porcelain` is empty after the commit (clean tree, including no untracked guides/user_flows_and_jtbd.md).
    - `mix format --check-formatted` exits 0 from the clean tree.
    - `mix test` reports 0 failures with 920+ tests.
    - `mix ci` reaches and passes its `format --check-formatted` first step.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted && mix test && test -z "$(git status --porcelain)" && echo CLEAN_TREE</automated>
  </verify>
  <done>
    The working tree is clean and committed with intent; `mix format --check-formatted` exits 0; the full suite is green; `mix ci`'s format step passes — the required `test` branch-protection lane is green (D-10 terminal gate satisfied).
  </done>
</task>

</tasks>

<threat_model>
No new attack surface — git hygiene / format / commit only. This plan runs `mix format`, the existing test suite, and `git commit`; it introduces no new code paths, no untrusted-input handling, and no dependencies. The hardening of the input-validation surface lives in 77-01; this plan only commits and proves it. T-77-SC (package tampering): n/a — no installs.
</threat_model>

<verification>
- `git status --porcelain` empty (clean committed tree, no untracked JTBD guide left).
- `mix format --check-formatted` exits 0.
- `mix test` — 0 failures, 920+ tests.
- `mix ci` passes its `format --check-formatted` first step.
</verification>

<success_criteria>
- Maps to ROADMAP Success Criteria 1 + 2 + D-01/D-02/D-10.
- Clean committed tree, green format gate, green full suite, green `mix ci` format step — the required `test` lane is green.
</success_criteria>

<output>
Create `.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-04-SUMMARY.md` when done.
</output>
