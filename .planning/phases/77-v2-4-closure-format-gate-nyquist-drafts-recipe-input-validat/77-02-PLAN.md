---
phase: 77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/user_flows_and_jtbd.md
  - mix.exs
autonomous: true
requirements: []   # closure phase — no new REQ-IDs; maps to Success Criterion 2 + D-02 (JTBD guide) / D-03
must_haves:
  truths:
    - "[SC-2][D-02] The untracked guides/user_flows_and_jtbd.md is preserved as a real, useful artifact (not deleted)"
    - "[SC-2][D-03] guides/user_flows_and_jtbd.md is wired into mix.exs ExDoc extras AND a groups_for_extras group, consistent with the existing guides"
    - "[SC-2][D-03] The JTBD guide makes no claim beyond priv/support_matrix.json — docs-contract tests pass with it wired in"
  artifacts:
    - path: "guides/user_flows_and_jtbd.md"
      provides: "JTBD guide for Phoenix engineers (within support-matrix claim bounds)"
    - path: "mix.exs"
      provides: "ExDoc extras + groups_for_extras entry for the JTBD guide"
      contains: "guides/user_flows_and_jtbd.md"
  key_links:
    - from: "mix.exs docs/0 extras"
      to: "guides/user_flows_and_jtbd.md"
      via: "string entry in the extras list"
      pattern: "guides/user_flows_and_jtbd.md"
    - from: "mix.exs docs/0 groups_for_extras"
      to: "guides/user_flows_and_jtbd.md"
      via: "string entry in a group (Guides)"
      pattern: "user_flows_and_jtbd"
---

<objective>
Wire the audit-flagged untracked `guides/user_flows_and_jtbd.md` JTBD guide into the ExDoc `extras`/`groups_for_extras` block in `mix.exs` so it ships in HexDocs rather than rotting untracked, and ensure its language stays within `priv/support_matrix.json` so the docs-contract tests pass.

Purpose: Closes part of Success Criterion 2 (resolve the untracked working-tree artifact with intent) and D-03 (HexDocs wiring follow-through). This plan does NOT commit — the terminal commit + format proof is plan 77-04.
Output: Modified `mix.exs` (ExDoc extras + group entry); the JTBD guide possibly tightened to stay within the support matrix. Disjoint file set from 77-01 and 77-03 (parallel-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-CONTEXT.md
@.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the JTBD guide into ExDoc extras and keep it within the support matrix</name>
  <files>mix.exs, guides/user_flows_and_jtbd.md</files>
  <read_first>
    - mix.exs — docs/0 block: extras list (lines 106-114), groups_for_extras (115-128), skip_undefined_reference_warnings_on (96-104)
    - guides/user_flows_and_jtbd.md — full content, to confirm it makes no claim exceeding priv/support_matrix.json
    - priv/support_matrix.json — the bound on what guides may claim
    - test/docs_contract/recipes_claims_test.exs — how guide claims are bound to the matrix (what would trip if the JTBD guide overclaims)
    - .planning/phases/77-*/77-PATTERNS.md — "mix.exs (config — D-03 ExDoc wiring)" section
  </read_first>
  <action>
    D-03: Add `"guides/user_flows_and_jtbd.md"` to the `extras` list in mix.exs docs/0 (after the existing guides entries). Add the same path to a `groups_for_extras` group — the `Guides` group is the natural home (planner discretion per D-03/CONTEXT). If the guide references modules (consistent with how `recipes.md`/`page_primitive.md` appear), also add it to `skip_undefined_reference_warnings_on`. Then read guides/user_flows_and_jtbd.md and verify it makes no claim beyond priv/support_matrix.json; if wiring it would trip test/docs_contract/recipes_claims_test.exs (a claim exceeding the support matrix), TIGHTEN the guide's language to stay within the matrix rather than dropping the wiring (D-03 is explicit on this). Do not delete the guide.
  </action>
  <acceptance_criteria>
    - `grep -c 'user_flows_and_jtbd' mix.exs` returns at least 2 (extras + groups_for_extras).
    - `mix test test/docs_contract/` exits 0 with the guide wired in.
    - `mix docs` builds without an undefined-reference error attributable to the new guide (warnings-clean per the skip list if needed).
    - guides/user_flows_and_jtbd.md still exists on disk.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/ && grep -c 'user_flows_and_jtbd' mix.exs</automated>
  </verify>
  <done>
    The JTBD guide is wired into mix.exs extras and a groups_for_extras group, the docs-contract suite passes, and the guide's claims are within priv/support_matrix.json.
  </done>
</task>

</tasks>

<threat_model>
No new attack surface — docs/config-only change. Wiring a Markdown guide into ExDoc extras and (if needed) tightening its prose introduces no new untrusted-input path, no dependency, and no executable code. The docs-contract test (`test/docs_contract/recipes_claims_test.exs`) is the integrity check that the guide does not overclaim beyond `priv/support_matrix.json`. T-77-SC (package tampering): n/a — no installs.
</threat_model>

<verification>
- `mix test test/docs_contract/` green with the JTBD guide wired in.
- `grep -c 'user_flows_and_jtbd' mix.exs` >= 2.
- `guides/user_flows_and_jtbd.md` present and within support-matrix claim bounds.
</verification>

<success_criteria>
- Maps to ROADMAP Success Criterion 2 + D-02/D-03.
- JTBD guide preserved and shipped via HexDocs extras; docs-contract tests pass.
</success_criteria>

<output>
Create `.planning/phases/77-v2-4-closure-format-gate-nyquist-drafts-recipe-input-validat/77-02-SUMMARY.md` when done.
</output>
