---
phase: 87-comparison-page-livebook
plan: 03
type: execute
wave: 3
depends_on:
  - 87-01
  - 87-02
files_modified:
  - guides/comparison.md
  - lib/rendro/comparison.ex
  - test/docs_contract/comparison_claims_test.exs
  - test/rendro/comparison_test.exs
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - CMP-02

must_haves:
  truths:
    - "guides/comparison.md is titled 'Generating PDFs in Elixir without Chrome' and is structured as an honest decision guide."
    - "Measured result tables, fit matrix, and evidence summary blocks are generated from bench/results/comparison.json, not hand-maintained."
    - "Every public [bench:CMP-*] citation in the guide resolves to a manifest claim, and every public manifest claim appears in the guide."
    - "Uncited comparative phrases such as faster, smaller, lower RSS, fewer dependencies, lighter, and no Chrome runtime fail the docs-contract test unless covered by a valid [bench:CMP-*] citation or a non-comparative boundary context."
    - "The guide explicitly praises ChromicPDF and Typst where true and states Rendro's HTML/CSS, complex-script, and RTL boundaries."
    - "Decision coverage: D-12 through D-23 are represented in generated blocks, guide structure, caveats, and overclaim guards."
    - "D-12: only public result tables, fit/caveat tables, and evidence summary blocks are generated; intro and interpretation stay human-authored."
    - "D-13: visible [bench:CMP-*] citations resolve to the manifest and every public manifest claim intended for docs appears in the guide."
    - "D-14: docs-contract tests fail uncited comparative language such as faster, smaller, lower RSS, fewer dependencies, lighter, and no Chrome runtime."
    - "D-17: guide title is Generating PDFs in Elixir without Chrome."
    - "D-18: guide structure is The Short Version, Choose By Job, Measured Operational Tradeoffs, Where HTML/CSS Renderers Still Win, Text Fonts and Complex Scripts, Reproduce These Numbers, Try Rendro In Livebook."
    - "D-19: guide leads with a compact fit matrix using Best fit, Good fit, and Use another tool labels, with no winner badges."
    - "D-20: guide copy explicitly praises ChromicPDF, Typst, and Rendro for true-fit cases."
    - "D-21: limitation block states no arbitrary HTML/CSS rendering and complex-script/RTL boundaries from priv/support_matrix.json."
    - "D-22: guide avoids attack and hype words and uses fit/tradeoff/evidence language."
    - "D-23: guide includes Try the invoice workflow in Livebook and Reproduce the benchmark results CTAs."
  artifacts:
    - path: "guides/comparison.md"
      provides: "HexDocs comparison guide"
      contains: "Generating PDFs in Elixir without Chrome"
    - path: "test/docs_contract/comparison_claims_test.exs"
      provides: "Claim citation, generated block, and overclaim docs contract"
      contains: "[bench:CMP-"
---

<objective>
Ship the HexDocs comparison guide and bind every measured or comparative public
claim to the committed benchmark manifest. This plan turns the Plan 02 benchmark
results into a fair, cited, docs-contract-checked buyer guide.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-comparison-page-livebook/87-CONTEXT.md
@.planning/phases/87-comparison-page-livebook/87-RESEARCH.md
@.planning/phases/87-comparison-page-livebook/87-PATTERNS.md
@.planning/phases/87-comparison-page-livebook/87-UI-SPEC.md
@.planning/phases/87-comparison-page-livebook/87-01-PLAN.md
@.planning/phases/87-comparison-page-livebook/87-02-PLAN.md
</context>

<decision_traceability>
- D-12: Only public result tables, fit/caveat tables, and evidence summaries are generated; intro and interpretation stay human-authored.
- D-13: Visible `[bench:CMP-*]` citations must resolve to the manifest, and every public manifest claim intended for docs must appear in the guide.
- D-14: Uncited comparative language such as faster, smaller, lower RSS, fewer dependencies, lighter, and no Chrome runtime fails the docs contract unless in a clearly non-metric scope.
- D-17: The guide title is `Generating PDFs in Elixir without Chrome`.
- D-18: The guide uses the required section structure from `The Short Version` through `Try Rendro In Livebook`.
- D-19: The guide leads with a compact fit matrix and uses `Best fit`, `Good fit`, and `Use another tool`, not winner badges.
- D-20: Public copy explicitly praises ChromicPDF, Typst, and Rendro for their true-fit cases.
- D-21: The limitation block visibly states no arbitrary HTML/CSS rendering and complex-script/RTL boundaries from `priv/support_matrix.json`.
- D-22: The guide avoids attack and hype words and uses fit/tradeoff/evidence language.
- D-23: The guide includes `Try the invoice workflow in Livebook` and benchmark reproduction CTAs.
</decision_traceability>

<tasks>

<task type="auto">
  <name>Task 1: Generate comparison guide blocks from the benchmark manifest</name>
  <files>lib/rendro/comparison.ex, test/rendro/comparison_test.exs</files>
  <read_first>
    - lib/rendro/comparison.ex - Plan 01 helper surface to extend
    - bench/results/comparison.json - source of truth for measured result rows and public claim records
    - lib/rendro/launch_artifacts.ex - `readme_block/1`, `recipes_block/1`, marker extraction, deterministic generated-block formatting
    - .planning/phases/87-comparison-page-livebook/87-UI-SPEC.md - table column contracts and generated block marker names
  </read_first>
  <action>
    Extend `Rendro.Comparison` so:
    - `fit_block/1` emits a compact fit matrix with columns `Job`, `Rendro`, `ChromicPDF`, `pdf_generator`, `Typst CLI`, `Reason`.
    - `results_block/1` emits a measured operational table with columns `Metric`, `Rendro`, `ChromicPDF cold`, `ChromicPDF warm pool`, `pdf_generator`, `Typst CLI`, `Evidence`.
    - `evidence_block/1` emits benchmark metadata: run id, scenario fixture, comparator versions, Docker image digest, OS, arch, CPU, memory limit, sample count, median, p95, and raw artifact paths.
    - generated output includes visible `[bench:CMP-*]` citations for any metric or comparative statement.
    - generated output includes the phrase `Measured in this harness` near the measured results table.

    Add or update tests in `test/rendro/comparison_test.exs` so generated blocks:
    - contain their start and end markers;
    - contain `chromic_pdf_warm_pool`;
    - contain `[bench:CMP-`;
    - do not contain `TODO`, `TBD`, or `placeholder`;
    - produce deterministic output from the same manifest twice.
  </action>
  <acceptance_criteria>
    - `mix test test/rendro/comparison_test.exs` exits 0.
    - `Rendro.Comparison.results_block(Rendro.Comparison.read_manifest!())` contains `Measured in this harness`.
    - Generated blocks include no `TODO`, `TBD`, or `placeholder` text.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the comparison guide with generated blocks and fair framing</name>
  <files>guides/comparison.md</files>
  <read_first>
    - .planning/phases/87-comparison-page-livebook/87-CONTEXT.md - D-17 through D-23 guide title, structure, copy, caveats, and anti-hype language
    - .planning/phases/87-comparison-page-livebook/87-UI-SPEC.md - required section order and table rules
    - README.md - current public tone and guide list style
    - guides/recipes.md - HexDocs guide tone and generated block placement
    - priv/support_matrix.json - source of truth for complex-script/RTL boundaries
  </read_first>
  <action>
    Create `guides/comparison.md`.

    The H1 must be exactly:
    `# Generating PDFs in Elixir without Chrome`

    Add sections in this exact order:
    - `## The Short Version`
    - `## Choose By Job`
    - `## Measured Operational Tradeoffs`
    - `## Where HTML/CSS Renderers Still Win`
    - `## Text, Fonts, and Complex Scripts`
    - `## Reproduce These Numbers`
    - `## Try Rendro In Livebook`

    Insert generated blocks from `Rendro.Comparison`:
    - fit block under `Choose By Job`;
    - results block under `Measured Operational Tradeoffs`;
    - evidence block under `Reproduce These Numbers`.

    Include these exact public fit sentences:
    - `Choose ChromicPDF when your source of truth is already HTML/CSS or browser CSS fidelity is the requirement.`
    - `Choose Typst when your team wants Typst templates and its layout language is already part of your workflow.`
    - `Choose Rendro when your PDF is authored from Elixir data and you want deterministic layout, pagination, telemetry, and no browser runtime.`

    Include a limitation block that states:
    - `Rendro does not render arbitrary HTML/CSS.`
    - `Complex-script and RTL support are bounded by priv/support_matrix.json.`
    - `Unsupported shaping cases fail explicitly instead of producing silent broken output.`

    Do not use any of these words or phrases in positive marketing copy:
    - `bloated`
    - `kills Chrome`
    - `replaces every PDF tool`
    - `pixel-perfect HTML-to-PDF`
    - `works everywhere`
  </action>
  <acceptance_criteria>
    - `guides/comparison.md` contains the exact H1 and all seven required H2 sections in order.
    - `guides/comparison.md` contains the three exact `Choose ...` sentences listed in the action.
    - `guides/comparison.md` contains `Rendro does not render arbitrary HTML/CSS.`
    - `guides/comparison.md` contains at least one `[bench:CMP-` citation.
    - `guides/comparison.md` does not contain `bloated`, `kills Chrome`, `replaces every PDF tool`, `pixel-perfect HTML-to-PDF`, or `works everywhere`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Harden docs-contract claim binding and overclaim guards</name>
  <files>test/docs_contract/comparison_claims_test.exs, scripts/verify_docs.exs</files>
  <read_first>
    - test/docs_contract/comparison_claims_test.exs - file being modified; Plan 01 scaffold
    - test/docs_contract/launch_artifacts_claims_test.exs - generated block equality and public overclaim guard patterns
    - guides/comparison.md - guide under test
    - bench/results/comparison.json - claim IDs and public claim records
    - scripts/verify_docs.exs - lane registry
  </read_first>
  <action>
    Extend `test/docs_contract/comparison_claims_test.exs` so the required docs-contract lane asserts:
    - `Rendro.Comparison.static_contract_errors() == []`.
    - generated fit, results, and evidence blocks in `guides/comparison.md` exactly equal `Rendro.Comparison.fit_block/1`, `results_block/1`, and `evidence_block/1`.
    - every `[bench:CMP-*]` citation in the guide resolves to a manifest claim ID.
    - every manifest claim with `"public": true` appears in the guide.
    - the guide contains all seven required H2 sections.
    - the guide contains the three exact `Choose ...` sentences from Task 2.
    - the guide contains the required HTML/CSS and complex-script limitation sentences.
    - uncited comparative phrases fail. Implement this with a helper that strips or records valid citation scopes and flags uncited occurrences of `faster`, `smaller`, `lower RSS`, `fewer dependencies`, `lighter`, and `no Chrome runtime`.
    - the guide does not contain `bloated`, `kills Chrome`, `replaces every PDF tool`, `pixel-perfect HTML-to-PDF`, or `works everywhere`.

    Ensure `scripts/verify_docs.exs` still includes exactly one `Comparison claims lane` entry.
  </action>
  <acceptance_criteria>
    - `mix test test/docs_contract/comparison_claims_test.exs` exits 0.
    - Removing one generated block row from `guides/comparison.md` causes the generated block equality test to fail.
    - Adding `Rendro is faster than Chrome tools.` without a valid `[bench:CMP-*]` citation causes the docs-contract test to fail.
    - Removing one public manifest claim citation from the guide causes the docs-contract test to fail.
    - `mix run scripts/verify_docs.exs` includes and runs the comparison claims lane.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Human-authored guide interpretation is trusted only when bounded by generated result blocks and citation tests.
- Benchmark manifest claims are trusted only when public citations resolve and generated blocks are fresh.
- Reader-facing limitations are part of product behavior and must be enforced by docs-contract tests.
</trust_boundaries>

<threats>
- T-87-11 stale guide blocks: mitigate with generated block equality tests.
- T-87-12 unsupported comparative claim: mitigate with citation resolution and uncited phrase guards.
- T-87-13 unfair/attack copy: mitigate with exact positive alternative-fit sentences and banned phrase assertions.
- T-87-14 complex-script overclaim: mitigate with required limitation sentence tied to `priv/support_matrix.json`.
- T-87-15 public placeholder claims: mitigate by forbidding `TODO`, `TBD`, and `placeholder` in generated blocks and public guide copy.
</threats>

<high_severity_blockers>
Stop if `bench/results/comparison.json` still contains placeholder data or if
the guide needs uncited measured claims to read well. Public comparison claims
must be bounded to the manifest before CMP-02 can pass.
</high_severity_blockers>
</threat_model>

<verification>
Required commands after this plan:
- `mix test test/rendro/comparison_test.exs`
- `mix test test/docs_contract/comparison_claims_test.exs`
- `mix run scripts/verify_docs.exs`
</verification>

<success_criteria>
Plan 87-03 is complete when `guides/comparison.md` is a fair HexDocs decision
guide and every measured/comparative claim is mechanically bounded to checked-in
benchmark evidence.
</success_criteria>
