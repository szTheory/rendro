---
phase: 57
plan: 01
type: execute
wave: 1
depends_on: ["56-02"]
files_modified:
  - priv/support_matrix.json
  - guides/api_stability.md
  - test/docs_contract/forms_claims_test.exs
  - test/docs_contract/signing_claims_test.exs
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
must_haves:
  truths:
    - The public contract separates authored unsigned signature widgets from artifact-first signing preparation instead of collapsing them into one broad "signature support" claim.
    - Machine-readable support metadata and human-facing docs use the same narrow vocabulary for supported surfaces, proof lanes, and unsupported trust/compliance narratives.
    - Docs-contract verification fails if support-matrix leaves, guide wording, or signature-support claims drift apart.
  artifacts:
    - path: priv/support_matrix.json
      provides: "Machine-readable publication of unsigned signature-widget support and the separate signing-preparation family"
    - path: guides/api_stability.md
      provides: "Canonical human-facing wording that distinguishes unsigned widgets, signing preparation, and unsupported digital-signature narratives"
    - path: test/docs_contract/forms_claims_test.exs
      provides: "Executable lock on the forms-family signature contract"
    - path: test/docs_contract/signing_claims_test.exs
      provides: "Executable lock on signing-preparation wording and negative trust-boundary claims"
    - path: scripts/verify_docs.exs
      provides: "Canonical docs-contract gate that executes the new signature/support claims lane"
  key_links:
    - from: priv/support_matrix.json
      to: guides/api_stability.md
      via: "supported leaves and unsupported narratives must match exactly for unsigned widgets and signing preparation"
      pattern: "signature|signing_preparation|digital_signatures|tamper_evidence|pades"
    - from: test/docs_contract/forms_claims_test.exs
      to: priv/support_matrix.json
      via: "the forms lane must fail if rendered unsigned widget claims or signature viewer posture drift"
      pattern: "forms|signature|unverified|unsupported"
    - from: test/docs_contract/signing_claims_test.exs
      to: guides/api_stability.md
      via: "the signing-preparation lane must fail if public wording widens into cryptographic or compliance claims"
      pattern: "prepare|digital signatures|tamper evidence|PAdES|LTV|TSA|OCSP|CRL"
---

<objective>
Publish the Phase 57 support contract so Rendro names exactly what is supported about unsigned signature widgets and signing preparation, and nothing more.

Purpose: satisfy `TRUST-01` and `TRUST-02` by publishing one narrow, auditable support story across the support matrix, API-stability guide, and docs-contract tests.
Output: synchronized machine-readable and human-readable contract artifacts plus an executable docs gate for the new signature/support claims lane.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v2.0-ROADMAP.md
@.planning/milestones/v2.0-REQUIREMENTS.md
@.planning/phases/57-support-contract-and-proof-closure/57-CONTEXT.md
@.planning/phases/57-support-contract-and-proof-closure/57-RESEARCH.md
@.planning/phases/57-support-contract-and-proof-closure/57-PATTERNS.md
@.planning/phases/57-support-contract-and-proof-closure/57-VALIDATION.md
@.planning/phases/55-signature-field-authoring-contract/55-02-SUMMARY.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-02-SUMMARY.md
</execution_context>

<context>
@priv/support_matrix.json
@guides/api_stability.md
@test/docs_contract/forms_claims_test.exs
@scripts/verify_docs.exs
@test/rendro/pdf/writer_test.exs
@test/rendro/sign_test.exs

<interfaces>
From `priv/support_matrix.json`:
```json
"forms": {
  "authored_helpers": {
    "signature_field": "supported_unsigned_placeholder_only"
  },
  "widgets": {
    "signature": "unsupported"
  }
}
```

From `guides/api_stability.md`:
```md
The `Rendro.signature_field/2` helper is an authored unsigned-placeholder contract only.
Digital signatures, signer metadata, tamper evidence, compliance narratives, and PAdES/LTV/TSA/OCSP/CRL support remain unsupported.
```

From `scripts/verify_docs.exs`:
```elixir
{"Forms semantic-claims lane", ["test", "test/docs_contract/forms_claims_test.exs"]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish the machine-readable signature widget and signing-preparation support contract without widening the trust surface</name>
  <read_first>
    - priv/support_matrix.json
    - test/docs_contract/forms_claims_test.exs
    - .planning/phases/57-support-contract-and-proof-closure/57-CONTEXT.md
    - .planning/phases/57-support-contract-and-proof-closure/57-RESEARCH.md
    - .planning/phases/55-signature-field-authoring-contract/55-02-SUMMARY.md
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-02-SUMMARY.md
  </read_first>
  <files>priv/support_matrix.json, test/docs_contract/forms_claims_test.exs, test/docs_contract/signing_claims_test.exs</files>
  <acceptance_criteria>
    - `priv/support_matrix.json` changes `forms.widgets.signature` from `"unsupported"` to a supported unsigned-widget value that does not read like digital-signature support
    - `priv/support_matrix.json` contains a top-level `"signing_preparation"` family with explicit supported capability/behavior leaves and explicit unsupported boundary leaves for `"digital_signatures"`, `"tamper_evidence"`, and `"pades_ltv_tsa_ocsp_crl"`
    - `priv/support_matrix.json` contains signature-specific viewer rows whose statuses are `"unverified"` unless a recorded proof leaf already exists for that exact surface and viewer
    - `test/docs_contract/forms_claims_test.exs` no longer asserts that `forms.widgets.signature` is unsupported and instead asserts the new narrow unsigned-widget contract
    - `test/docs_contract/signing_claims_test.exs` exists and contains both positive support assertions for signing preparation and negative assertions against digital-signature, compliance, and blanket viewer claims
  </acceptance_criteria>
  <action>
    Update `priv/support_matrix.json` so Phase 57 publishes two separate public surfaces. First, advance the forms family from an authored-helper-only posture to a narrow rendered unsigned-widget posture by promoting `forms.widgets.signature` to a supported unsigned-widget value such as `supported_unsigned_only` or equivalent language that is explicitly not digital-signature support. Keep `forms.authored_helpers.signature_field` intact. Second, add a sibling top-level `signing_preparation` family with compact `capabilities`, `behaviors`, `boundaries`, and `viewers` subsections. The supported leaves must cover the Phase 56 seam only: external artifact preparation, final-byte handoff, and adapter-local metadata isolation. The unsupported leaves must explicitly cover digital signatures, tamper evidence, and PAdES/LTV/TSA/OCSP/CRL narratives.
    Keep viewer posture per surface and per viewer. Do not rely on the existing generic `forms.viewers` rows as proof for signature widgets. Add signature-specific viewer rows under the new signing-related surface shape and default them to `unverified` unless this repo already has a recorded checklist for that exact viewer and signature surface. Do not publish a blanket "signature viewers supported" claim anywhere in the matrix.
    Update `test/docs_contract/forms_claims_test.exs` so it freezes the new forms-family truth: authored signature helpers remain supported, rendered signature widgets are supported only as unsigned widgets, structural proof is not viewer proof, and digital signatures stay unsupported. Create `test/docs_contract/signing_claims_test.exs` to lock the new `signing_preparation` family and refute wording drift such as "digital signatures are supported", "tamper-evident signing", "PAdES is supported", or generic signature-viewer support.
  </action>
  <verify>
    <automated>mix test test/docs_contract/forms_claims_test.exs test/docs_contract/signing_claims_test.exs</automated>
  </verify>
  <done>The support matrix publishes two narrow signing-related surfaces, and docs-contract tests fail on both omission and overclaim drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize API-stability wording and the docs verification gate around one truthful signature-support story</name>
  <read_first>
    - guides/api_stability.md
    - scripts/verify_docs.exs
    - test/docs_contract/forms_claims_test.exs
    - test/docs_contract/signing_claims_test.exs
    - priv/support_matrix.json
    - .planning/phases/57-support-contract-and-proof-closure/57-RESEARCH.md
  </read_first>
  <files>guides/api_stability.md, scripts/verify_docs.exs, test/docs_contract/forms_claims_test.exs, test/docs_contract/signing_claims_test.exs</files>
  <acceptance_criteria>
    - `guides/api_stability.md` contains one short lifecycle preamble that names unsigned placeholder authoring, render, artifact preparation, and external signing as separate steps
    - `guides/api_stability.md` contains one section for unsigned signature widgets and one section for signing preparation, and each section states the supported surface, proof lane, and unsupported narratives in that order
    - `guides/api_stability.md` explicitly says structural proof is not viewer proof and not cryptographic validity proof
    - `scripts/verify_docs.exs` includes a new docs-contract lane for `test/docs_contract/signing_claims_test.exs`
    - the guide does not claim digital-signature support, signer identity/trust, tamper evidence, compliance, or broad viewer guarantees
  </acceptance_criteria>
  <action>
    Rewrite the signing portion of `guides/api_stability.md` so the public story follows the Phase 57 context exactly. Add one short lifecycle preamble that says: Rendro can author an unsigned placeholder, render an artifact, prepare that final artifact for an external signer, and then stop; external signing and verification remain outside core. After that preamble, split the contract into two sections. The unsigned signature-widget section must state that Rendro supports authored and rendered unsigned widgets, that the proof lane is the deterministic writer/structural tests, and that digital signatures, signer identity, tamper evidence, compliance, and unproven viewer behavior remain outside contract. The signing-preparation section must state that `Rendro.Sign.prepare/2` is supported as an artifact-first preparation seam, that the proof lane is the prepare-stage and manifest tests, and that signer execution, trust policy, cryptographic validity, PAdES/LTV/TSA/OCSP/CRL, and blanket viewer guarantees remain unsupported.
    Update `scripts/verify_docs.exs` to add one explicit signature/support semantic-claims lane for `test/docs_contract/signing_claims_test.exs`. Keep the lane table flat and preserve the existing forms/protection lanes. Reuse the same exact-string and regex-based docs-contract style already used elsewhere in the repo; do not build a second docs verification script or a parser-heavy framework.
  </action>
  <verify>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>The guide and docs-contract runner now publish and enforce the same narrow signature-support contract as the support matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal runtime proof -> public support contract | Structural tests and prepare tests become outward-facing capability claims here. |
| Unsigned widget and preparation support -> digital-signature expectations | The boundary between supported artifact seams and unsupported trust/compliance narratives is easy to blur here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Spoofing / semantic confusion | `priv/support_matrix.json` | mitigate | Publish separate unsigned-widget and signing-preparation families with explicit unsupported digital-signature and compliance leaves. |
| T-57-02 | Repudiation | `guides/api_stability.md` + docs-contract tests | mitigate | Keep one tested vocabulary for supported surfaces, proof lanes, and unsupported narratives so public claims remain auditable. |
| T-57-03 | Scope drift | signature-related viewer wording | mitigate | Default signature-specific viewer rows to `unverified` and refute blanket viewer-support wording in docs-contract tests. |
</threat_model>

<verification>
mix test test/docs_contract/forms_claims_test.exs test/docs_contract/signing_claims_test.exs
mix run scripts/verify_docs.exs
</verification>

<success_criteria>
- The support matrix publishes unsigned signature widgets and signing preparation as separate narrow surfaces.
- Public docs explain those two surfaces without implying digital-signature, compliance, or blanket viewer support.
- Docs-contract tests and `scripts/verify_docs.exs` fail if support-matrix leaves and guide wording drift apart.
- No first-party signing workflow, trust policy surface, or broad signature taxonomy is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/57-support-contract-and-proof-closure/57-01-SUMMARY.md`
</output>
