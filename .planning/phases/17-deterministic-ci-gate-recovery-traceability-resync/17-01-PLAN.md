---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scripts/release_preflight_proof_test.exs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "test/scripts/release_preflight_proof_test.exs is properly formatted per mix format"
    - "mix ci executes successfully without format regressions"
    - "REQUIREMENTS.md correctly lists QUAL-01 as Done"
  artifacts:
    - path: "test/scripts/release_preflight_proof_test.exs"
      provides: "Formatting fix"
      min_lines: 10
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated Traceability Matrix"
      contains: "| QUAL-01 |"
  key_links:
    - from: "test/scripts/release_preflight_proof_test.exs"
      to: "mix ci"
      via: "format check"
      pattern: "mix format --check-formatted"
---

<objective>
Fix the deterministic CI gate regression caused by formatting failure in `release_preflight_proof_test.exs`, and resync `REQUIREMENTS.md` traceability state for QUAL-01.

Purpose: Reopen and close the current quality-gate regression, ensuring the repo state is green and docs accurately reflect the verified status of the CI gate.
Output: Formatted test file and updated requirements documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-deterministic-ci-gate-recovery-traceability-resync/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format release_preflight_proof_test.exs</name>
  <files>test/scripts/release_preflight_proof_test.exs</files>
  <read_first>
    - test/scripts/release_preflight_proof_test.exs
  </read_first>
  <action>
    Run `mix format test/scripts/release_preflight_proof_test.exs` to apply standard Elixir formatting. Ensure the file compiles and the formatting rules are correctly applied so that `mix format --check-formatted` will pass.
  </action>
  <acceptance_criteria>
    - `mix format --check-formatted test/scripts/release_preflight_proof_test.exs` completes with exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted test/scripts/release_preflight_proof_test.exs</automated>
  </verify>
  <done>File is formatted correctly and format check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Resync Traceability Status in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` to reflect that the QUAL-01 requirement is fully verified:
    1. Change `- [ ] **QUAL-01**:` to `- [x] **QUAL-01**:`.
    2. In the "Traceability" table, change the "Status" for "QUAL-01" from "Pending" to "Done".
    3. In the "Coverage" section at the bottom, update "Verified (Done): 22" to "Verified (Done): 23" and "Pending verification: 2" to "Pending verification: 1".
  </action>
  <acceptance_criteria>
    - `grep -E "\- \[x\] \*\*QUAL-01\*\*:" .planning/REQUIREMENTS.md` exits 0.
    - `grep -E "\| QUAL-01 .*\| Done \|" .planning/REQUIREMENTS.md` exits 0.
    - `grep "Verified (Done): 23" .planning/REQUIREMENTS.md` exits 0.
    - `grep "Pending verification: 1" .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -E "\| QUAL-01 .*\| Done \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md traceability state perfectly mirrors the true gate status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tooling/CI | Code format enforced prior to execution in CI lanes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | test/scripts/release_preflight_proof_test.exs | mitigate | Use canonical `mix format` without manual edits to guarantee predictable results. |
</threat_model>

<verification>
Run `mix ci` locally to prove the format check now passes and no regression exists in the CI verification lane.
</verification>

<success_criteria>
- `test/scripts/release_preflight_proof_test.exs` is cleanly formatted.
- `REQUIREMENTS.md` accurately tracks QUAL-01 as Done.
- `mix ci` passes without format failures.
</success_criteria>

<output>
After completion, create `.planning/phases/17-deterministic-ci-gate-recovery-traceability-resync/17-01-SUMMARY.md`
</output>
