---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/support_matrix.json
  - guides/api_stability.md
  - test/docs_contract/embedded_artifact_claims_test.exs
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
must_haves:
  truths:
    - Rendro publishes one machine-readable support contract for embedded files and links without replacing the existing family-first matrix.
    - Public support wording stays conservative and keeps all embedded-file and link viewer claims unverified until recorded manual evidence exists.
    - The canonical docs gate fails when support-matrix shape, wording, or viewer-proof posture drift apart.
  artifacts:
    - path: priv/support_matrix.json
      provides: "Family-first embedded-files and links support contract"
    - path: guides/api_stability.md
      provides: "Canonical public wording for truthful artifact-surface boundaries"
    - path: test/docs_contract/embedded_artifact_claims_test.exs
      provides: "Executable lock on support-matrix shape, docs wording, and docs-lane registration"
    - path: scripts/verify_docs.exs
      provides: "Canonical docs-contract lane registration"
  key_links:
    - from: guides/api_stability.md
      to: priv/support_matrix.json
      via: "embedded-file and link wording mirrors the matrix per D-01 through D-18 instead of drifting into broader viewer claims"
    - from: scripts/verify_docs.exs
      to: test/docs_contract/embedded_artifact_claims_test.exs
      via: "the canonical docs gate runs the artifact support claims lane automatically"
---

<objective>
Publish the Phase 50 support boundary as one explicit machine-readable matrix plus conservative public wording, with viewer status left unverified until evidence is recorded.

Purpose: Close TRUST-01 and the docs side of TRUST-02 without widening the contract beyond what Phases 48 and 49 already proved structurally.
Output: Support-matrix extensions for `embedded_files` and `links`, canonical support-boundary wording, and docs-contract automation that keeps them aligned.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/milestones/v1.9-ROADMAP.md
@.planning/milestones/v1.9-CONTEXT.md
@.planning/phases/50-support-boundary-and-proof-closure/50-CONTEXT.md
@.planning/phases/50-support-boundary-and-proof-closure/50-RESEARCH.md
@.planning/phases/50-support-boundary-and-proof-closure/50-PATTERNS.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-02-PLAN.md
</execution_context>

<context>
@priv/support_matrix.json
@guides/api_stability.md
@scripts/verify_docs.exs
@test/docs_contract/forms_claims_test.exs

<interfaces>
From `priv/support_matrix.json`:
```json
{
  "validators": {
    "pdfinfo": {
      "version": "22+",
      "validates": ["structural_integrity"]
    }
  },
  "forms": {
    "widgets": { "text": "supported", "checkbox": "supported", "radio": "supported" },
    "behaviors": { "prefilled_values": "supported", "authored_appearance": "supported" },
    "viewers": {
      "adobe_acrobat_reader": { "status": "unverified", "proof": ["open", "default_state_visible", "edit_or_toggle", "save"] },
      "apple_preview": { "status": "supported", "proof": ["open", "default_state_visible", "edit_or_toggle", "save"] }
    }
  }
}
```

From `scripts/verify_docs.exs`:
```elixir
lanes = [
  {"README doctest lane", ["test", "test/docs_contract/readme_doctest_test.exs"]},
  {"Integration contract lane", ["test", "test/docs_contract/integrations_contract_test.exs"]},
  {"Integration semantic-claims lane", ["test", "test/docs_contract/integrations_claims_test.exs"]},
  {"Forms semantic-claims lane", ["test", "test/docs_contract/forms_claims_test.exs"]}
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the support matrix with explicit embedded-file and link families</name>
  <files>priv/support_matrix.json, test/docs_contract/embedded_artifact_claims_test.exs</files>
  <behavior>
    - `priv/support_matrix.json` keeps the family-first top level per D-01 and D-02 and adds sibling `embedded_files` and `links` families instead of a generic wrapper.
    - `embedded_files` separates `capabilities`, `behaviors`, and `viewers` per D-03; `links` separates `targets`, `behaviors`, and `viewers` per D-04.
    - Viewer claims remain per surface and default to `unverified` until Plan 03 records proof per D-06 through D-14.
  </behavior>
  <action>
    Update `priv/support_matrix.json` so the existing top-level shape remains explicit and product-facing per D-01 and D-02: preserve `validators` and `forms`, then add `embedded_files` and `links` as siblings rather than introducing a `"surfaces"` wrapper or compatibility-database schema.
    Model `embedded_files` with the smallest truthful nested contract per D-03, D-05, D-08, D-09, and D-15: include `capabilities.document_level`, `behaviors.explicit_metadata`, `behaviors.authored_timestamps`, and `behaviors.page_attachment_annotations`; mark authored document-level features as `supported`, keep page attachment annotations `unsupported`, and use named viewer entries only where richer metadata is needed for proof checklists.
    Model `links` with the same explicit posture per D-04, D-05, D-08, D-09, and D-16: include `targets.external_uri_http_https`, `targets.internal_page`, `behaviors.fragment_rectangles`, and `behaviors.named_destinations`; mark the Phase 49 authored surface as `supported`, and keep named destinations `unsupported` because v1.9 does not widen into that deferred surface.
    Add named viewer objects for `adobe_acrobat_reader` and `apple_preview` under both `embedded_files.viewers` and `links.viewers`, each with `status: "unverified"` plus a minimal proof checklist that matches D-13 and D-14. Do not add blanket milestone viewer state, extra browser/viewer sprawl, or broad per-leaf statement objects.
    Create `test/docs_contract/embedded_artifact_claims_test.exs` as a literal claims test that parses the JSON, asserts the exact nested keys and provisional viewer statuses, and refutes regressions back to a generic `"surfaces"` wrapper, broad viewer buckets, or premature `supported` claims before viewer evidence exists.
  </action>
  <verify>
    <automated>mix test test/docs_contract/embedded_artifact_claims_test.exs</automated>
  </verify>
  <done>The support matrix exposes explicit embedded-file and link families with narrow supported, unsupported, and unverified boundaries that match the locked Phase 50 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync canonical support wording and docs gate to the provisional matrix posture</name>
  <files>guides/api_stability.md, test/docs_contract/embedded_artifact_claims_test.exs, scripts/verify_docs.exs</files>
  <behavior>
    - Public docs use `embedded files` and plain `links` per D-15 and D-16, and explicitly distinguish embedded PDF payloads from delivery attachments per D-17.
    - Structural proof is described as distinct from viewer behavior per D-10 through D-14.
    - `mix docs.contract` stays the canonical docs gate by running the new claims lane through `scripts/verify_docs.exs`.
  </behavior>
  <action>
    Extend `guides/api_stability.md` as the canonical support-boundary guide per D-17 and D-18. The wording must say that Rendro supports document-level embedded files with explicit metadata, supports authored links for external `http`/`https` URIs and internal page destinations, and treats structural validation through `pdfinfo`/Poppler as proof of PDF structure only rather than viewer discoverability, extraction, or navigation behavior.
    Keep every embedded-file and link viewer claim conservative in this plan per D-06 through D-14: Adobe Acrobat Reader and Apple Preview should be named as the only proof-tracked viewers for these surfaces, but both must remain `unverified` for `embedded_files` and `links` until Plan 03 records passing manual evidence for the named checklist. Do not use broad phrases such as “standard PDF viewers,” do not claim one blanket v1.9 viewer status, and do not blur delivery/email attachments with embedded files inside the PDF.
    Extend `test/docs_contract/embedded_artifact_claims_test.exs` so it asserts the exact support-guide wording, refutes broad viewer language, and verifies that the guide mirrors the matrix posture without promoting support too early.
    Add a new explicit artifact-support lane to `scripts/verify_docs.exs` so `mix docs.contract` and `mix run scripts/verify_docs.exs` both remain the canonical docs gate for this contract. Preserve the existing forms and integration lanes; do not replace or bypass the docs gate with ad hoc commands.
  </action>
  <verify>
    <automated>mix docs.contract</automated>
  </verify>
  <done>The canonical support guide, machine-readable matrix, and docs gate all publish the same conservative pre-proof contract for embedded files and links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal support data -> public support claims | Matrix values and guide wording leave implementation context and become external capability statements here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Repudiation | `priv/support_matrix.json` + docs | mitigate | Encode the support contract in one machine-readable matrix and lock it with literal claims tests so product claims remain auditable per D-01 through D-14. |
| T-50-02 | Information Integrity | `guides/api_stability.md` | mitigate | Refute broad viewer wording, require per-surface viewer state, and keep all viewer statuses unverified until manual proof exists. |
</threat_model>

<verification>
mix test test/docs_contract/embedded_artifact_claims_test.exs
mix docs.contract
</verification>

<success_criteria>
- `priv/support_matrix.json` keeps the family-first matrix and adds explicit `embedded_files` and `links` families without widening into a generic support database.
- `guides/api_stability.md` publishes conservative artifact-surface wording that keeps viewers unverified until evidence exists and separates structural proof from viewer behavior.
- `mix docs.contract` fails when the matrix, docs, and claims-test posture drift apart.
</success_criteria>

<output>
After completion, create `.planning/phases/50-support-boundary-and-proof-closure/50-01-SUMMARY.md`
</output>
