---
phase: 50
plan: 03
type: execute
wave: 2
depends_on:
  - "50-01"
  - "50-02"
files_modified:
  - .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
  - priv/support_matrix.json
  - guides/api_stability.md
  - test/docs_contract/embedded_artifact_claims_test.exs
autonomous: false
requirements:
  - TRUST-01
  - TRUST-02
must_haves:
  truths:
    - Viewer support can be promoted for embedded files and links only after the named manual checklist is recorded for that exact surface.
    - Viewer support remains per surface and per viewer; a pass for links does not imply a pass for embedded files, and vice versa.
    - If manual evidence is missing or partial, the public contract stays unverified rather than inferring support from structural proof.
  artifacts:
    - path: .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
      provides: "Recorded manual evidence table for embedded-files and links viewer checks"
    - path: priv/support_matrix.json
      provides: "Post-proof per-surface viewer statuses synchronized from the recorded evidence"
    - path: guides/api_stability.md
      provides: "Public wording synchronized to the recorded viewer evidence"
    - path: test/docs_contract/embedded_artifact_claims_test.exs
      provides: "Automation that fails when the published contract diverges from the recorded evidence"
  key_links:
    - from: .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
      to: priv/support_matrix.json
      via: "recorded viewer evidence drives `supported` vs `unverified` statuses instead of broad milestone claims"
    - from: .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
      to: guides/api_stability.md
      via: "public wording promotes support only for viewer/surface pairs whose checklist actually passed"
---

<objective>
Finish Phase 50 with an explicit human viewer-evidence checkpoint and sync any proof-backed promotions back into the support matrix, docs, and claims tests.

Purpose: Close TRUST-01 and TRUST-02 truthfully by treating manual viewer evidence as the only basis for `supported` viewer claims on embedded files and links.
Output: Recorded viewer evidence, a blocking manual verification step, and post-proof contract synchronization that keeps missing evidence unverified.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/50-support-boundary-and-proof-closure/50-CONTEXT.md
@.planning/phases/50-support-boundary-and-proof-closure/50-RESEARCH.md
@.planning/phases/50-support-boundary-and-proof-closure/50-PATTERNS.md
@.planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-03-PLAN.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-VALIDATION.md
</execution_context>

<context>
@priv/support_matrix.json
@guides/api_stability.md
@test/docs_contract/embedded_artifact_claims_test.exs
@test/support/embedded_artifact_support_fixture.ex

<interfaces>
From `priv/support_matrix.json`:
```json
{
  "embedded_files": {
    "viewers": {
      "adobe_acrobat_reader": { "status": "unverified", "proof": ["discoverable", "open_or_extract", "save_or_extract"] },
      "apple_preview": { "status": "unverified", "proof": ["discoverable", "open_or_extract", "save_or_extract"] }
    }
  },
  "links": {
    "viewers": {
      "adobe_acrobat_reader": { "status": "unverified", "proof": ["external_uri_handoff", "internal_page_navigation"] },
      "apple_preview": { "status": "unverified", "proof": ["external_uri_handoff", "internal_page_navigation"] }
    }
  }
}
```

From `test/support/embedded_artifact_support_fixture.ex`:
```elixir
path = Rendro.Test.EmbeddedArtifactSupportFixture.write_fixture(path)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Support-matrix/docs contract alignment, a representative embedded-artifact fixture, a Poppler structural proof lane, and a validation document with pending viewer-evidence tables.</what-built>
  <how-to-verify>
    1. Run `mix test test/docs_contract/embedded_artifact_claims_test.exs` and confirm the support-claims contract passes in its provisional pre-proof state.
    2. Run `mix docs.contract` and confirm the canonical docs gate passes through `scripts/verify_docs.exs`.
    3. Run `mix test test/rendro/adapters/poppler_test.exs` and confirm the representative embedded-artifact fixture validates structurally when `pdfinfo` is installed, or is explicitly skipped when it is not.
    4. Generate the exact manual-check fixture with `MIX_ENV=test mix run -e 'path = Path.expand("tmp/embedded_artifact_support_fixture.pdf"); path = Rendro.Test.EmbeddedArtifactSupportFixture.write_fixture(path); IO.puts(path)'`.
    5. Open that PDF in Adobe Acrobat Reader and Apple Preview.
    6. In `50-VALIDATION.md`, record for `embedded_files` per viewer: viewer name, version if easily available, OS, date checked, fixture path, `discoverable`, `open_or_extract`, `save_or_extract`, overall result, and notes only if surprising.
    7. In `50-VALIDATION.md`, record for `links` per viewer: viewer name, version if easily available, OS, date checked, fixture path, `external_uri_handoff`, `internal_page_navigation`, overall result, and notes only if surprising.
    8. If any viewer/surface pair is not checked or fails a required behavior, leave that pair `unverified`; do not infer support from Poppler or from another surface on the same viewer.
  </how-to-verify>
  <resume-signal>Type "approved" after updating `50-VALIDATION.md`, or reply with the viewer/surface pair that failed or could not be checked.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote only proof-backed viewer statuses and keep every other pair unverified</name>
  <files>.planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md, priv/support_matrix.json, guides/api_stability.md, test/docs_contract/embedded_artifact_claims_test.exs</files>
  <behavior>
    - Viewer promotions are per surface and per viewer, not blanket milestone claims.
    - Any missing or failing manual evidence leaves the corresponding viewer/surface pair unverified everywhere.
    - Docs-contract tests assert the exact post-proof state and fail if the published contract drifts from the recorded evidence.
  </behavior>
  <action>
    After the blocking human checkpoint, read the completed manual-proof tables in `50-VALIDATION.md` and synchronize the results back into `priv/support_matrix.json`, `guides/api_stability.md`, and `test/docs_contract/embedded_artifact_claims_test.exs`.
    Promote a viewer to `supported` for `embedded_files` only if that viewer has recorded passing results for every required embedded-file behavior in the validation table. Promote a viewer to `supported` for `links` only if that viewer has recorded passing results for every required link behavior in its own table. Keep the statuses independent per D-06 through D-14: one viewer may be supported for links and unverified for embedded files, or vice versa.
    Update `guides/api_stability.md` so the public wording states exactly which viewer/surface pairs are supported after the recorded proof. If no viewer/surface pair gains a full pass, keep the guide conservative and explicitly unverified. Do not claim “viewer support” generically, do not infer support from Poppler, and do not add any new named viewers beyond the recorded checklist.
    Update `test/docs_contract/embedded_artifact_claims_test.exs` so it asserts the exact post-proof matrix/docs posture and remains the guardrail against future drift between the validation record and public claims.
  </action>
  <verify>
    <automated>mix test test/docs_contract/embedded_artifact_claims_test.exs && mix docs.contract</automated>
  </verify>
  <done>The published support contract matches the recorded viewer evidence exactly, with unsupported or unproven viewer/surface pairs left unverified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human viewer evidence -> published support claims | Manual observations are converted into product-facing support claims here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Repudiation | viewer evidence tables | mitigate | Require recorded viewer/surface evidence in `50-VALIDATION.md` before any `supported` promotion and keep the tables as the source of truth. |
| T-50-06 | Information Integrity | support matrix + support guide | mitigate | Synchronize only proof-backed viewer/surface pairs and leave every missing or partial result unverified rather than inferring support from another lane. |
</threat_model>

<verification>
mix test test/docs_contract/embedded_artifact_claims_test.exs
mix docs.contract
Manual viewer evidence recorded in `.planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md`
</verification>

<success_criteria>
- Manual viewer evidence is recorded separately for embedded files and links.
- Only viewer/surface pairs with complete passing evidence are promoted to `supported`.
- The support matrix, public wording, and claims tests all mirror the recorded evidence exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/50-support-boundary-and-proof-closure/50-03-SUMMARY.md`
</output>
