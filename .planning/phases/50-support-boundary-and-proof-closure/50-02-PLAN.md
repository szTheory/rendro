---
phase: 50
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/embedded_artifact_support_fixture.ex
  - test/rendro/adapters/poppler_test.exs
  - test/rendro/embedded_artifact_support_fixture_test.exs
  - .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
autonomous: true
requirements:
  - TRUST-02
must_haves:
  truths:
    - Rendro has one reproducible structural proof lane for a representative PDF that exercises the embedded-file and link surfaces together.
    - Poppler validation is documented and tested as structural proof only, not as proof of viewer discoverability, extraction, or navigation behavior.
    - The phase validation document tells executors exactly how to regenerate the representative fixture and how the automated and manual lanes differ.
  artifacts:
    - path: test/support/embedded_artifact_support_fixture.ex
      provides: "Reusable representative fixture generator covering embedded files and both supported link target types"
    - path: test/rendro/adapters/poppler_test.exs
      provides: "Fixture-backed Poppler structural validation lane"
    - path: .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
      provides: "Phase closure contract with explicit automated and manual proof lanes"
  key_links:
    - from: test/support/embedded_artifact_support_fixture.ex
      to: test/rendro/adapters/poppler_test.exs
      via: "the same generated artifact-surface PDF feeds both automated structural proof and later manual viewer verification"
    - from: .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
      to: test/support/embedded_artifact_support_fixture.ex
      via: "the validation guide provides the exact regeneration command for the representative fixture"
---

<objective>
Create the integrated structural proof lane for Phase 50 using one representative embedded-artifact fixture and document that lane as distinct from viewer verification.

Purpose: Satisfy the proof-lane side of TRUST-02 with executable structural evidence while preserving the truth that viewer behavior remains a separate manual contract.
Output: Fixture-backed Poppler coverage and a validation scaffold that documents exact structural-proof commands and manual-proof placeholders.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/50-support-boundary-and-proof-closure/50-CONTEXT.md
@.planning/phases/50-support-boundary-and-proof-closure/50-RESEARCH.md
@.planning/phases/50-support-boundary-and-proof-closure/50-PATTERNS.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-03-PLAN.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-VALIDATION.md
@.planning/phases/48-embedded-file-core-surface/48-VALIDATION.md
@.planning/phases/49-curated-link-annotation-surface/49-VALIDATION.md
</execution_context>

<context>
@test/rendro/adapters/poppler_test.exs
@test/rendro/pdf/writer_test.exs
@test/rendro/deterministic_test.exs

<interfaces>
From `test/rendro/adapters/poppler_test.exs`:
```elixir
case System.find_executable("pdfinfo") do
  nil ->
    IO.puts("Skipping valid pdf test: pdfinfo not installed")
    :ok

  _executable ->
    assert {:ok, metadata} = Poppler.validate(path)
    assert is_map(metadata)
end
```

From `test/rendro/pdf/writer_test.exs`:
```elixir
doc
|> Rendro.register_embedded_file(:invoice_csv, {:binary, "a,b\n1,2\n"}, ...)
|> Rendro.link(uri: "https://example.com/docs")
|> Rendro.link(page: 2)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a representative embedded-artifact fixture and Poppler proof around it</name>
  <files>test/support/embedded_artifact_support_fixture.ex, test/rendro/adapters/poppler_test.exs</files>
  <behavior>
    - One deterministic fixture generator emits a PDF that contains document-level embedded files plus both supported link target types.
    - The Poppler lane validates that exact generated PDF when `pdfinfo` is available and skips explicitly when it is not.
    - The integrated fixture proves the artifact surfaces coexist structurally in one document rather than as isolated point tests.
    - A lightweight companion assertion proves the representative fixture actually includes one embedded file, one external URI link, and one internal page link.
  </behavior>
  <action>
    Create `test/support/embedded_artifact_support_fixture.ex` as a narrow helper modeled on `Rendro.Test.FormSupportFixture`. The fixture must build one representative PDF that exercises the supported v1.9 artifact surface only: at least one document-level embedded file with explicit deterministic metadata, one external `http` or `https` link, and one internal page destination link. Use authored timestamps and stable content so the same output can be reused for both automated proof and manual viewer checks.
    Extend `test/rendro/adapters/poppler_test.exs` so the Poppler suite writes the representative fixture to a temp path, validates it with `Rendro.Adapters.Poppler.validate/1`, and asserts basic metadata such as page count when `pdfinfo` exists. Preserve the current explicit skip behavior when `pdfinfo` is missing, and keep the test focused on structural validity instead of viewer interaction.
    Add `test/rendro/embedded_artifact_support_fixture_test.exs` or an equivalent narrow companion test that renders the same representative fixture and asserts the combined surface is really present, for example by checking for one embedded-file structure plus one external URI link and one internal page destination in the output. Keep this proof lightweight and structural; do not widen into viewer-behavior checks.
    Reuse existing writer and builder surfaces from Phases 48 and 49. Do not add new PDF features, generic annotation surfaces, extra URI schemes, or named destinations while building the representative fixture.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/poppler_test.exs test/rendro/embedded_artifact_support_fixture_test.exs</automated>
  </verify>
  <done>The repo can regenerate and structurally validate one representative embedded-artifact PDF without overstating that this proves viewer behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Phase 50 validation around separate structural and viewer proof lanes</name>
  <files>.planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md</files>
  <action>
    Write `50-VALIDATION.md` in the newer Phase 48/49 validation style, but carry forward the clearer proof-lane split from Phase 47. Include frontmatter, test-infrastructure metadata, sampling guidance, and a per-task verification map for Plans 01 through 03.
    The validation document must include exact commands for the support-claims lane and the structural proof lane: `mix docs.contract`, `mix test test/docs_contract/embedded_artifact_claims_test.exs`, `mix test test/rendro/adapters/poppler_test.exs`, and one explicit `MIX_ENV=test mix run -e '...'` command that regenerates the representative fixture through `Rendro.Test.EmbeddedArtifactSupportFixture.write_fixture/1`.
    Document the manual viewer-evidence lane separately with pending placeholders only. State clearly per D-10 through D-14 that Poppler proves PDF structure only, and that embedded-file discoverability, extract/save behavior, external-link handoff, and internal-link navigation require named manual evidence before any viewer support claim is promoted.
  </action>
  <verify>
    <automated>rg -n "mix docs.contract|mix test test/rendro/adapters/poppler_test.exs|EmbeddedArtifactSupportFixture|viewer proof lane|Poppler proves PDF structure only" .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md</automated>
  </verify>
  <done>The phase validation contract provides exact automation commands and leaves no ambiguity about the separation between structural proof and manual viewer proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated PDF -> Poppler validator | Rendro output is interpreted by an external structural validator here. |
| Generated PDF -> human-operated viewer | The same PDF will later cross into manual viewer verification, which is a different evidence lane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03 | Information Integrity | `test/rendro/adapters/poppler_test.exs` | mitigate | Use one representative generated PDF and document Poppler only as structural proof so the lane cannot be mistaken for viewer validation. |
| T-50-04 | Repudiation | `.planning/.../50-VALIDATION.md` | mitigate | Record exact fixture-generation and proof commands so future support promotions are traceable back to the same artifact and lane definitions. |
</threat_model>

<verification>
mix test test/rendro/adapters/poppler_test.exs
rg -n "mix docs.contract|EmbeddedArtifactSupportFixture|viewer proof lane" .planning/phases/50-support-boundary-and-proof-closure/50-VALIDATION.md
</verification>

<success_criteria>
- A deterministic representative PDF exercises embedded files and links together and passes Poppler validation when `pdfinfo` is available.
- `50-VALIDATION.md` provides exact automation commands for docs-contract and structural proof lanes.
- The structural proof lane is explicitly documented as different from viewer proof.
</success_criteria>

<output>
After completion, create `.planning/phases/50-support-boundary-and-proof-closure/50-02-SUMMARY.md`
</output>
