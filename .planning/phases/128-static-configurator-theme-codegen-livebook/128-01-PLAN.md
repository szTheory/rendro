---
phase: 128-static-configurator-theme-codegen-livebook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/theme/snippet.ex
  - dev/mix/tasks/rendro/configurator/gen.ex
  - assets/rendro/configurator/index.json
  - test/rendro/theme/snippet_test.exs
autonomous: true
requirements:
  - CONFIG-03
  - CONFIG-05
must_haves:
  truths:
    - "One packaged Elixir formatter owns every preset/mode/RGB serialization used by the configurator index, generated module source, and Livebook fragment (D-07, D-09)."
    - "The committed configurator index contains exactly 504 ordered family/preset/accent/mode records, and every snippet is formatter-owned, parseable, and controlled-harness executable (D-10, D-19)."
    - "Every working usage fragment constructs Theme.preset/2 before applying the recipe and explicit Presets.register_fonts/2 document transform (D-08, D-11)."
    - "FLAGGED UNRESOLVED EDGE ASSUMPTION CONFIG-03/unclassified: the deterministic probe supplied no narrower edge category; exhaustive source equality, parse/evaluate, and representative render assertions are the explicit Phase-128 accounting response."
    - "FLAGGED UNRESOLVED EDGE ASSUMPTION CONFIG-05/concurrency: parallel or interrupted consumer writes have no newly promised transaction semantics; this plan only guarantees deterministic source derivation and leaves filesystem mutation behavior to Plan 02's explicit safe-write contract."
  artifacts:
    - path: "lib/rendro/theme/snippet.ex"
      provides: "Private packaged Rendro.Theme.Snippet formatter and closed source vocabulary"
      contains: "defmodule Rendro.Theme.Snippet"
    - path: "dev/mix/tasks/rendro/configurator/gen.ex"
      provides: "Explicit dev/test generator and read-only drift check for the committed index"
      contains: "defmodule Mix.Tasks.Rendro.Configurator.Gen"
    - path: "assets/rendro/configurator/index.json"
      provides: "Versioned deterministic 504-record browser snippet index"
      contains: "schema_version"
    - path: "test/rendro/theme/snippet_test.exs"
      provides: "Exhaustive source/index/parse/evaluate and representative render contracts"
  key_links:
    - from: "dev/mix/tasks/rendro/configurator/gen.ex"
      to: "lib/rendro/theme/snippet.ex"
      via: "generator delegates index bytes to the packaged formatter"
      pattern: "Rendro.Theme.Snippet"
    - from: "assets/rendro/configurator/index.json"
      to: "lib/rendro/theme/snippet.ex"
      via: "committed bytes equal formatter-generated deterministic JSON"
      pattern: "generated_by"
    - from: "lib/rendro/theme/snippet.ex"
      to: "Rendro.Theme.Presets.register_fonts/2"
      via: "usage and module compositions preserve explicit document-first registration"
      pattern: "register_fonts"
  prohibitions:
    - statement: "The source model must not silently alter a requested family, preset, accent, or mode to agree with a representative raster."
      status: unresolved
      verification: null
      flagged: true
    - statement: "The formatter and index must not turn a bounded source-generation surface into a public general-purpose codegen interface."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Establish the one Elixir-owned source model that all three Phase-128 consumer surfaces project.

Purpose: Prevent JavaScript, the Mix task, and the Livebook from independently assembling Elixir or drifting on preset/mode/RGB/font-bridge behavior.
Output: Private formatter, explicit committed-index generator, 504-record index, and exhaustive deterministic tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-RESEARCH.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-PATTERNS.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace Invoice/Swiss/#2C6BED/light from formatter to committed index</name>
  <read_first>
    - lib/rendro/theme/presets.ex — canonical preset atoms, validation, and the document-first register_fonts/2 signature.
    - lib/rendro/theme.ex — stable Theme.preset/2 public constructor and adapter-tier boundary.
    - dev/rendro/catalog.ex — six family mappings, ordered catalog values, deterministic manifest encoding, and first selectable Invoice/Swiss record.
    - dev/mix/tasks/rendro/catalog/gen.ex — narrow dev Mix-task delegation and error boundary.
    - assets/rendro/catalog.json — preview-only evidence authority; do not copy PNG identity, hashes, quality, or disclosure into the snippet index.
    - test/rendro/theme/presets_test.exs — exhaustive closed-vocabulary ExUnit pattern.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md — D-07 through D-11 and D-19.
  </read_first>
  <files>lib/rendro/theme/snippet.ex, dev/mix/tasks/rendro/configurator/gen.ex, assets/rendro/configurator/index.json, test/rendro/theme/snippet_test.exs</files>
  <behavior>
    - The Invoice/Swiss/#2C6BED/light record contains the exact D-08 working fragment with normalized {44, 107, 237}, explicit :light, Invoice.document/2, and document-first register_fonts/2.
    - The fragment parses with Code.string_to_quoted!/1 and evaluates only as internally generated trusted source with an invoice binding.
    - Generating twice yields identical JSON bytes; check mode compares without writing.
  </behavior>
  <action>
Create the failing tracer assertions first, then add private packaged `Rendro.Theme.Snippet` (`@moduledoc false`) with explicit finite mappings for the six families, six preset atoms, seven strict uppercase accents, and two modes. Provide composable functions for the canonical preset call, family-specific `usage_snippet/4`, fixed wrapper `module_source/4`, option metadata, deterministic record construction, and index JSON bytes. Preserve the D-08 and D-11 order exactly: recipe document construction precedes `Rendro.Theme.Presets.register_fonts(document, preset)`. Map validated strings through closed tables; do not derive atoms from input or evaluate caller/browser strings (D-07, D-09; T-128-02). Add `Mix.Tasks.Rendro.Configurator.Gen` as the explicit dev/test operation that writes the formatter-owned index and supports a byte-comparison `--check`; keep catalog evidence solely in `assets/rendro/catalog.json` (D-10). The first production-quality tracer is the exact Invoice/Swiss/#2C6BED/light record, not a stub.
  </action>
  <acceptance_criteria>
    - `Rendro.Theme.Snippet.usage_snippet/4` returns the exact D-08 Invoice/Swiss/#2C6BED/light fragment and never serializes a Theme struct.
    - `mix rendro.configurator.gen` creates a versioned index containing the tracer record; a second generation is byte-identical.
    - `mix rendro.configurator.gen --check` exits zero on equality and leaves the index mtime and bytes unchanged.
    - The tracer test parses and evaluates only the formatter-produced string with controlled bindings and proves a `%Rendro.Document{}` result after explicit font registration.
    - No browser, server, database, Node/npm, Phoenix, or new dependency is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/theme/snippet_test.exs --max-failures 1 &amp;&amp; mix rendro.configurator.gen --check &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>The exact default selection travels from the packaged formatter through generated committed JSON and back through parse/evaluate/render proof without a second source template.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust the 504-record source vocabulary and module composition</name>
  <read_first>
    - lib/rendro/theme/snippet.ex — Task 1 formatter interfaces and finite mappings.
    - assets/rendro/configurator/index.json — Task 1 schema/version/order and generated bytes.
    - test/rendro/catalog_test.exs — exact-count, unique-ID, and ordered-registry assertions.
    - test/rendro/theme/presets_test.exs — strict preset error and explicit font-registration proof patterns.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md — Wave-0 504 parse/evaluate and representative bridge obligations.
  </read_first>
  <files>lib/rendro/theme/snippet.ex, dev/mix/tasks/rendro/configurator/gen.ex, assets/rendro/configurator/index.json, test/rendro/theme/snippet_test.exs</files>
  <behavior>
    - 6 families × 6 presets × 7 accents × 2 modes produces exactly 504 unique ordered keys and strings.
    - Every committed string equals a fresh formatter result, parses, and evaluates in the controlled family-data harness.
    - Generated module source is formatter-stable and composes the same preset call and explicit font bridge as usage snippets.
    - Invalid family/preset/accent/mode values fail through closed validation without dynamic atom creation.
  </behavior>
  <action>
Expand the tracer into the full D-10/D-19 bounded index. Use deterministic nested ordering declared by the formatter, stable labels/values, an explicit schema version and generation command, unique composite keys, and complete strings; keep all preview identity and provenance out. Add exhaustive equality, parse, controlled evaluation, repeat-byte, count, ordering, uniqueness, invalid-value, and representative family render/font-bridge tests. Cover `module_source/4` here as the common formatter seam Plan 02 consumes, including fixed `theme/0` and document-first `register_fonts/1`, but do not add the public Mix task in this plan. Preserve the distinction between 504 deterministic source proofs and the existing bounded 32-raster/human-review set (D-19).
  </action>
  <acceptance_criteria>
    - The committed index has exactly 504 records and the exact closed options 6/6/7/2, with no duplicate composite key or snippet.
    - Every index string is byte-equal to `usage_snippet/4`, parses with `Code.string_to_quoted!/1`, and evaluates only from trusted formatter output in a controlled harness.
    - Representative family cases produce documents whose curated fonts are registered through the explicit public bridge.
    - Fresh formatter JSON bytes equal the committed file; generation/check operations do not mutate `assets/rendro/catalog.json`.
    - Tests make no statement that all 504 selections received raster or human visual review.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/theme/snippet_test.exs --max-failures 1 &amp;&amp; mix rendro.configurator.gen --check &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>The complete bounded source vocabulary and both consumer compositions are deterministically owned by one tested Elixir formatter.</done>
</task>

</tasks>

## Artifacts this phase produces

- New file `lib/rendro/theme/snippet.ex` with private module `Rendro.Theme.Snippet` and durable internal functions for closed family/preset/accent/mode metadata, `preset_call/3`, `usage_snippet/4`, `module_source/4`, record enumeration, and deterministic index bytes.
- New file `dev/mix/tasks/rendro/configurator/gen.ex` with dev/test Mix task `rendro.configurator.gen` and CLI flag `--check`.
- New file `assets/rendro/configurator/index.json` with durable static contract keys `schema_version`, `generated_by`, closed option collections, and exactly 504 keyed snippet records; it contains no preview hashes, paths, quality state, or visual-review provenance.
- New file `test/rendro/theme/snippet_test.exs` containing the exhaustive source/index/parser/evaluator and representative render contracts.
- No external-service coverage matrix and no ORM/schema artifact: the deterministic integration detector is false for this platform-only, local-file phase, and no configured schema pattern is in scope.

## Assumption delta decision

Primary noun: canonical requested configuration/source model (`family`, `preset`, `accent`, `mode`). Decision: no-change. Rationale: the configurator, Mix task, and Livebook project the same Elixir-owned formatter and do not introduce parallel identities; ROADMAP's “alongside” wording is multiplicity of consumers, not an identity migration.

## Flagged edge assumptions

- CONFIG-03 `unclassified — review manually`: unresolved by the deterministic probe. This plan surfaces it and responds with exhaustive formatter/index equality and copy-source tests; it is not auto-dismissed.
- CONFIG-05 concurrency `If interrupted or run in parallel, what is guaranteed?`: unresolved by the probe. Formatter and index derivation are deterministic; Plan 02 specifies the only filesystem mutation contract, while no atomic multi-process writer guarantee is inferred.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| finite formatter input → Elixir source | Only closed, validated family/preset/accent/mode values may become source text. |
| formatter output → controlled test evaluator | Tests execute only strings freshly created by `Rendro.Theme.Snippet`, with supplied bindings. |
| generator → committed JSON | The explicit task owns deterministic bytes and a read-only drift path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation / owner | Automated verification | Residual risk |
|---|---|---|---|---|---|---|---|
| T-128-02 | Elevation of privilege / DoS | `Rendro.Theme.Snippet`, exhaustive evaluator test | high | mitigate | Task 1 uses finite string-to-existing-atom tables only and evaluates internally generated trusted strings; never convert external text into atoms. | `mix test test/rendro/theme/snippet_test.exs` | A future maintainer could weaken the finite mapping; the exhaustive invalid-input suite remains the guard. |
| T-128-05 | Repudiation | committed index provenance | medium | mitigate | Task 2 binds every committed record to fresh formatter bytes and separates deterministic source proof from visual evidence. | `mix rendro.configurator.gen --check` | The check proves source provenance, not visual quality; that limitation is explicit. |
</threat_model>

<verification>
Run the focused snippet suite and index check after each task; after the plan, run `mix test` as the wave gate. The Phase-128 final gate remains `mix ci.fast`, `mix rendro.livebook.check`, and the static-file smoke contracts after all dependent plans land.
</verification>

<success_criteria>
- One private packaged formatter is the only Elixir source owner.
- Exactly 504 committed records equal fresh formatter output and parse/evaluate under controlled tests.
- Explicit font registration order is preserved in usage and module compositions.
- Index generation is deterministic and its check path is read-only.
</success_criteria>

## Source Coverage Audit (Plan 01 subset)

| Source | ID | Feature / constraint | Status |
|---|---|---|---|
| GOAL | — | Three surfaces share one canonical snippet format | COVERED |
| REQ | CONFIG-03 | Working copy-source snippet | COVERED |
| REQ | CONFIG-05 | Shared canonical formatter seam | COVERED (intentionally shared with Plan 02) |
| RESEARCH | — | Packaged formatter + committed 504-record index | COVERED |
| CONTEXT | D-07..D-11, D-19 | Canonical formatter, explicit font bridge, exhaustive bounded proof | COVERED |

<output>
Create `.planning/phases/128-static-configurator-theme-codegen-livebook/128-01-SUMMARY.md` when done.
</output>
