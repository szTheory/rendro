---
phase: 128-static-configurator-theme-codegen-livebook
plan: 05
type: execute
wave: 3
depends_on:
  - 128-02
  - 128-03
  - 128-04
files_modified:
  - .github/workflows/ci.yml
  - assets/rendro/configurator/configurator.css
  - assets/rendro/configurator/configurator.js
  - scripts/configurator_e2e/.gitignore
  - scripts/configurator_e2e/README.md
  - scripts/configurator_e2e/package.json
  - scripts/configurator_e2e/package-lock.json
  - scripts/configurator_e2e/playwright.config.mjs
  - scripts/configurator_e2e/static-server.mjs
  - scripts/configurator_e2e/tests/configurator.spec.mjs
  - scripts/configurator_e2e/tests/configurator.spec.mjs-snapshots/
  - test/docs_contract/configurator_phase_gate_test.exs
  - test/docs_contract/dx_local_reproducibility_claims_test.exs
  - test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - CONFIG-04
  - CONFIG-05
  - CONFIG-06
must_haves:
  truths:
    - "After all Wave-2 consumers land, `mix ci.fast`, `mix rendro.livebook.check`, and the byte-exact read-only `mix rendro.configurator.gen --check` all pass against the same canonical formatter and committed index (D-07, D-10, D-20)."
    - "A direct-file static smoke contract proves the configurator HTML/CSS/JS, both committed JSON inputs, local raster references, and canonical snippet linkage without requiring a shipped server, build pipeline, Node/npm, Phoenix, database, or runtime service (D-06, D-21)."
    - "Pinned Playwright Chromium behavior, scoped accessibility-tree snapshots, enumerated axe checks, pinned-container pixel baselines, and a serial fresh-consumer Mix subprocess prove the bounded final gate without a human checkpoint (D-01..D-06, D-12..D-15, D-20)."
    - "The final gate verifies the existing bounded truth contracts for CONFIG-01..06 without adding product behavior or making Firefox/Safari, VoiceOver/NVDA, WCAG-certification, or aesthetic-quality claims (D-18, D-19, D-21)."
  artifacts:
    - path: "test/docs_contract/configurator_phase_gate_test.exs"
      provides: "Durable Wave-3 direct-file integration smoke for the complete static configurator asset graph and canonical committed inputs"
    - path: ".planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md"
      provides: "Execution record for automated final gates and their explicit evidence boundary"
  key_links:
    - from: "test/docs_contract/configurator_phase_gate_test.exs"
      to: "assets/rendro/configurator/index.html"
      via: "direct filesystem load verifies every local CSS/JS/JSON reference resolves without a server or build product"
      pattern: "configurator/index.html"
    - from: "assets/rendro/configurator/index.json"
      to: "lib/rendro/theme/snippet.ex"
      via: "phase gate combines exact generator drift check with snippet provenance tests"
      pattern: "mix rendro.configurator.gen --check"
    - from: "guides/livebook/first_invoice.livemd"
      to: "assets/rendro/configurator/index.json"
      via: "Livebook checker and phase-gate contract prove the same canonical Invoice/Swiss/#2C6BED/light source block"
      pattern: "usage_snippet"
---

<objective>
Own the deterministic post-integration proof for the complete Phase-128 surface after every Wave-2 consumer has landed.

Purpose: Close CONFIG-01..06 from one dependency-correct Wave-3 owner with a zero-human, pinned Chromium and fresh-consumer gate.
Output: One durable direct-file phase-gate test, green focused and terminal Wave-3 evidence, and recorded automated browser plus fresh-consumer evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-RESEARCH.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-02-SUMMARY.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-03-SUMMARY.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the integrated static graph with focused feedback, then run terminal full CI</name>
  <precondition>Plans 128-02, 128-03, and 128-04 have completed and their SUMMARY files exist.</precondition>
  <read_first>
    - assets/rendro/configurator/index.html — shipped static entry point and local CSS/JS/JSON references.
    - assets/rendro/configurator/configurator.css — token, responsive, state, focus, overflow, and reduced-motion contract.
    - assets/rendro/configurator/configurator.js — two-manifest load, URL, resolver, safe DOM, and clipboard controller.
    - assets/rendro/configurator/index.json — committed 504-record formatter-owned selection/source index.
    - assets/rendro/catalog.json — committed ordered preview authority and safe local raster paths.
    - lib/rendro/theme/snippet.ex — canonical usage/module/index formatter seam from Plan 01.
    - dev/mix/tasks/rendro/configurator/gen.ex — exact `mix rendro.configurator.gen --check` read-only byte-drift contract.
    - lib/mix/tasks/rendro/gen/theme.ex — packaged application-owned wrapper consumer from Plan 02.
    - guides/livebook/first_invoice.livemd — canonical third-surface block and one themed render from Plan 04.
    - test/docs_contract/configurator_static_contract_test.exs — Plan-03 static topology, semantics, scope, and safe-DOM assertions.
    - test/docs_contract/configurator_resolver_contract_test.exs — Plan-03 exact/representative/none, URL, source, and clipboard assertions.
    - test/rendro/theme/snippet_test.exs — exhaustive formatter/index equality and controlled evaluation contract.
    - test/mix/tasks/rendro_livebook_check_test.exs — exact notebook block, one-render, and no-server/no-interactive contract.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md — final automated and human coverage boundaries.
  </read_first>
  <files>test/docs_contract/configurator_phase_gate_test.exs</files>
  <behavior>
    - The static entry point's local stylesheet, script, index, catalog, and manifest-backed raster references resolve from committed repository files without starting or depending on a service.
    - The phase-gate test parses both JSON inputs, proves the expected closed index/catalog relationship, and links visible source ownership to the canonical formatter rather than a JavaScript template.
    - The direct smoke remains a test-only contract and introduces no shipped route, server, build output, package dependency, or product symbol.
    - The direct static smoke, no-server Livebook execution, and read-only committed-index equality form the task-level feedback command and stay within the declared approximately 60-second target.
    - Full CI runs afterward as the separately identified terminal Wave-3 gate, not as task-level feedback.
  </behavior>
  <action>
Write `ConfiguratorPhaseGateTest` as an ExUnit direct-filesystem integration contract. Resolve the HTML's local CSS/JS/JSON references relative to committed assets, parse both committed JSON documents, and assert referenced catalog rasters are safe repository-relative files that exist. Bind the selected source/index relationship to the existing snippet and resolver contracts; reuse their public test-facing helpers or data shapes instead of reimplementing product behavior. Assert the smoke requires no network listener, HTTP route, Phoenix endpoint, database, Node/npm command, package install, or generated docs directory (D-06, D-07, D-10, D-20, D-21). Keep this file verification-only: create no production module, browser controller, alternate resolver, build script, or second manifest. Run the focused test, exact read-only generator check selected in resolved research, and existing no-server Livebook checker as the task-level feedback loop. Only after that focused command is green, run `mix ci.fast` as the separately timed terminal Wave-3 gate; do not classify its approximately 180-second runtime as task-level feedback.
  </action>
  <acceptance_criteria>
    - `test/docs_contract/configurator_phase_gate_test.exs` loads the committed static entry point and proves all required local CSS/JS/JSON/raster links resolve directly from repository files.
    - The smoke parses both manifests and confirms canonical index/source provenance by composing existing Phase-128 contracts rather than duplicating formatter or resolver logic.
    - The smoke runs without starting a listener, generating docs, invoking Node/npm, or adding any shipped runtime/build dependency.
    - `mix rendro.configurator.gen --check` exits zero and leaves `assets/rendro/configurator/index.json` bytes and mtime unchanged.
    - The focused phase-gate smoke, index drift check, and Livebook check exit zero within the declared approximately 60-second task-level target.
    - The subsequent terminal `mix ci.fast` gate exits zero after Plans 02-04 are integrated and is recorded separately from task-level latency.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/configurator_phase_gate_test.exs --max-failures 1 &amp;&amp; mix rendro.configurator.gen --check &amp;&amp; mix rendro.livebook.check</automated>
  </verify>
  <terminal_gate>
    <automated>mix ci.fast</automated>
    <when>Run only after the focused task-level command is green and before Task 2 begins.</when>
    <latency_class>Terminal Wave-3 gate; expected approximately 180 seconds and excluded from the 60-second task-feedback target.</latency_class>
  </terminal_gate>
  <done>The complete product graph has one green post-Wave-2 deterministic proof owner, and the direct static smoke adds no shipped execution surface.</done>
</task>

<task type="auto">
  <name>Task 2: Automate pinned Chromium and fresh-consumer end-to-end gates</name>
  <precondition>Task 1's focused automated command and subsequent terminal `mix ci.fast` Wave-3 gate are green.</precondition>
  <read_first>
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md — approved responsive, theme, state, copy, focus, and accessibility judgment contract.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md — automated evidence boundary.
    - assets/rendro/configurator/index.html — static page under review.
  </read_first>
  <files>scripts/configurator_e2e/package.json, scripts/configurator_e2e/package-lock.json, scripts/configurator_e2e/playwright.config.mjs, scripts/configurator_e2e/static-server.mjs, scripts/configurator_e2e/tests/configurator.spec.mjs, scripts/configurator_e2e/tests/configurator.spec.ts-snapshots/, test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs, .github/workflows/ci.yml</files>
  <action>
Add an isolated `scripts/configurator_e2e` harness pinned to `@playwright/test` 1.62.0 and `@axe-core/playwright` 4.13.0. Its dependency-free localhost-only server may serve only safe repository assets. Run Chromium only with workers 1, retries 0, fixed locale/timezone/device scale, failure trace/video/screenshot retention, no external requests, and no console/page errors. Generate screenshot baselines only inside `mcr.microsoft.com/playwright:v1.62.0-noble@sha256:baed2032d533817f3dbe6425de795788430ba345e819a1201337009ba17c9d07`; CI never updates snapshots. Add a serial ExUnit subprocess test that creates a real `mix new` consumer with a local-path Rendro dependency and audits default/override/conflict/force/equal-different-missing check behavior, bytes, recursive paths, types, hashes, and fixed mtimes. Add a graph-disconnected required CI job and strict aggregate gate.
  </action>
  <what-built>All Phase-128 product surfaces plus the green focused direct-file/index-drift/Livebook checks and separately green terminal full-CI gate from Task 1.</what-built>
  <how-to-verify>
    Run the local and pinned-container commands in the harness README, then run the fresh-consumer ExUnit test and `mix ci.fast`. Inspect only automated artifacts on failure; no human browser, AT, screenshot approval, generator UAT, or `gsd-verify-work` action is required.
  </how-to-verify>
  <acceptance_criteria>
    - Chromium covers canonical URL fallback/roundtrip; exact/representative/none without requested-code mutation; source/clipboard identity and rejection recovery; manifest/image failures; keyboard, focus, live, and alert semantics; chrome/document-mode independence; breakpoints, mobile/desktop geometry, reduced motion, scoped ARIA snapshots, axe scans, and screenshot matrix.
    - Exact, representative, and none states preserve requested code identity, factual disclosure, and copy availability exactly as specified.
    - Loading, manifest/image error, long-code, keyboard, focus, live-region, copy-success, and copy-failure states are understandable and recoverable.
    - A fresh disposable consumer project passes default module/path, explicit module/output, conflict/no-overwrite, authorized `--force`, and equal/different/missing `--check`; failures contain exact actionable output and a recursive path/type/hash/fixed-mtime audit proves no unexpected writes.
    - The SUMMARY identifies Chromium-only, automated evidence and its explicit no-claim boundary.
  </acceptance_criteria>
  <verify>
    <automated>npm ci --prefix scripts/configurator_e2e &amp;&amp; npm test --prefix scripts/configurator_e2e &amp;&amp; mix test test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs &amp;&amp; mix ci.fast</automated>
  </verify>
  <done>The pinned automated browser and fresh-consumer gates are green and recorded with no human acceptance step.</done>
</task>

</tasks>

## Artifacts this phase produces

- New test-only file `test/docs_contract/configurator_phase_gate_test.exs` with durable `ConfiguratorPhaseGateTest` direct-file integration coverage for the committed static asset graph, both JSON inputs, raster path existence, and canonical snippet provenance.
- `scripts/configurator_e2e/` with locked test-only Playwright/axe dependencies, a localhost-only static server, Chromium tests, and pinned-container baselines; `test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs` with serial real-consumer subprocess evidence; and an isolated CI job.
- Execution-created `.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md` recording exact automated gates and the explicit Chromium-only / no-certification boundary.
- No production symbol, public API, generated-code format, manifest schema, browser feature, product build pipeline, server route, runtime dependency, or Hex package payload is added by this validation-only plan.

## Assumption delta decision

Primary noun: canonical requested configuration/source model (`family`, `preset`, `accent`, `mode`). Decision: no-change. Plan 05 verifies the already planned identity and consumer seams; it creates no alternate identity or product behavior.

## Aggregate edge and prohibition accounting

Plan 05 does not add or duplicate product prohibitions. Its automated gates verify the bounded truths and explicitly flagged assumptions already owned by Plans 01-04: exhaustive source/index equality, single-invocation generator behavior, atomic URL/three-state preview/source identity, and one focused no-server Livebook render. The test-only browser harness does not widen the product surface.

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| committed product assets → phase-gate test | The final proof must inspect the same checked-in files users receive, not fixtures or generated substitutes. |
| committed browser evidence → claim surface | Pinned Chromium proves enumerated automated behavior only, never broad browser/AT/compliance/aesthetic claims. |
| temporary local test tooling → shipped surface | A localhost static server may support automated tests but must not become a product/runtime/build dependency. |

## STRIDE Threat Register (OWASP ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation / owner | Automated verification | Residual risk |
|---|---|---|---|---|---|---|---|
| T-128-06 | Tampering / Repudiation | `ConfiguratorPhaseGateTest` and committed assets | high | mitigate | Task 1 resolves committed references directly, parses both committed JSON inputs, composes existing provenance contracts, runs byte-exact generator drift in focused feedback, then runs full CI as a separately recorded terminal gate after all Wave-2 dependencies. | Focused task-level command plus terminal `mix ci.fast` | Repository compromise outside CI trust remains out of scope; exact checked-in inputs and drift checks bound project evidence. |
| T-128-07 | Repudiation | browser and generator-DX evidence | medium | mitigate | Task 2 pins Chromium/container/tool versions, preserves failure evidence, takes scoped ARIA and axe checks, and audits a real fresh-consumer subprocess. | Browser suite plus serial ExUnit subprocess | Evidence is intentionally Chromium-only and does not establish cross-browser, AT, certification, or aesthetic conclusions. |
| T-128-08 | Information disclosure / Elevation of privilege | temporary static test server | low | accept | Bind only to localhost, serve only safe repository assets, add no credentials or user data, and stop after tests. | Server request allowlist and browser external-request assertions | Local browser extensions and host configuration remain operator-controlled. |
</threat_model>

<verification>
Task 1 first runs the direct static-file integration test, exact read-only configurator index check, and no-server Livebook checker as the approximately 60-second task-feedback command. After that passes, the same Wave-3 owner runs `mix ci.fast` as the separately timed approximately 180-second terminal gate. Task 2 runs the isolated pinned Chromium suite and serial fresh-consumer subprocess gate. Preserve the Validation artifact's `status: draft` and `nyquist_compliant: false`; only validate-phase may advance that lifecycle.
</verification>

<success_criteria>
- One Wave-3 owner runs after Plans 02, 03, and 04 and proves CONFIG-01..06 together.
- Static smoke inspects committed files directly and adds no shipped server/build/runtime dependency.
- Index drift, Livebook execution, and full deterministic CI are green.
- Pinned Chromium and the fresh-consumer subprocess cover the states named in 128-VALIDATION.md with retained automated evidence.
- No product scope changes; evidence makes no Firefox/Safari, VoiceOver/NVDA, WCAG-certification, or aesthetic-quality claim.
</success_criteria>

## Multi-Source Coverage Audit (complete Phase-128 set)

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Three canonical tinkerer surfaces with truthful zero-server browsing, code generation, and Livebook proof | 01-05 | COVERED | Plan 05 owns only integrated closure. |
| REQ | CONFIG-01 | Static zero-server ExDoc configurator | 03, 05 | COVERED | Product in 03; direct-file and pinned Chromium proof in 05. |
| REQ | CONFIG-02 | Exact/representative/none truthful preview | 03, 05 | COVERED | Product in 03; integrated automated states in 05. |
| REQ | CONFIG-03 | Exact working copy source and feedback | 01, 03, 05 | COVERED | Source, interaction, then final proof. |
| REQ | CONFIG-04 | Safe deterministic four-key URL | 03, 05 | COVERED | Product in 03; browser restoration/focus proof in 05. |
| REQ | CONFIG-05 | Canonical formatter generators and byte drift gates | 01, 02, 05 | COVERED | Plan 05 runs the resolved dev-index check and full integration. |
| REQ | CONFIG-06 | Existing Livebook third surface | 04, 05 | COVERED | Plan 05 runs the no-server checker after all consumers land. |
| RESEARCH | — | Resolved dev task `Mix.Tasks.Rendro.Configurator.Gen` at `dev/mix/tasks/rendro/configurator/gen.ex`, committed index, and read-only byte-exact `--check` | 01, 05 | COVERED | Creation in 01; final drift proof in 05. |
| RESEARCH | — | Canonical formatter, safe static two-manifest UI, Mix.Generator wrapper, focused Livebook, no new dependencies | 01-05 | COVERED | No package legitimacy gate applies. |
| CONTEXT | D-01..D-06 | Requested state, truthful preview/URL/UI/browser contract | 03, 05 | COVERED | |
| CONTEXT | D-07..D-11 | Canonical formatter/index/snippet/font bridge | 01, 03-05 | COVERED | |
| CONTEXT | D-12..D-15 | Generated wrapper and safe CLI/write/check | 02, 05 | COVERED | |
| CONTEXT | D-16..D-18 | Focused no-server Livebook and honest learning copy | 04, 05 | COVERED | |
| CONTEXT | D-19 | Exhaustive bounded source proof without visual overclaim | 01, 05 | COVERED | Final gate does not widen visual evidence. |
| CONTEXT | D-20 | Consumer seams and final integrated proof | 01-05 | COVERED | Plan 05 is the dependency-correct owner. |
| CONTEXT | D-21 | Bounded conventional surfaces without framework/runtime expansion | 02-05 | COVERED | |
| UI-SPEC | — | Responsive, theme, state, copy, focus, and bounded accessibility automation | 03, 05 | COVERED | Deterministic contracts in 03; pinned Chromium gate in 05. |

Deferred ideas remain excluded: interactive/all-preset Livebook, arbitrary colors or fuzzy/cross-dimension preview fallback, live PDF/server/accounts/persistence, catalog expansion, and a public general-purpose formatter API.

<output>
Create `.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md` when done.
</output>
