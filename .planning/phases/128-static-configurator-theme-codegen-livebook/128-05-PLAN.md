---
phase: 128-static-configurator-theme-codegen-livebook
plan: 05
type: execute
wave: 3
depends_on:
  - 128-02
  - 128-03
  - 128-04
files_modified:
  - test/docs_contract/configurator_phase_gate_test.exs
autonomous: false
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - CONFIG-04
  - CONFIG-05
  - CONFIG-06
must_haves:
  truths:
    - "After all Wave-2 consumers land, `mix ci.fast`, `mix rendro.livebook.check`, and the byte-exact read-only `mix rendro.configurator.gen --check` all pass against the same canonical formatter and committed index (D-07, D-10, D-20)."
    - "A direct-file static smoke contract proves the configurator HTML/CSS/JS, both committed JSON inputs, local raster references, and canonical snippet linkage without requiring a shipped server, build pipeline, Node/npm, Phoenix, database, or runtime service (D-06, D-21)."
    - "Human review covers responsive light/dark/system chrome, document-light/document-dark independence, exact/representative/none, loading/error/long-code states, keyboard/focus/live-region/copy failure, current Chrome/Firefox/Safari, and VoiceOver or NVDA before Phase 128 is accepted (D-01..D-06, D-20)."
    - "The final gate verifies the existing bounded truth contracts for CONFIG-01..06 without adding product behavior, widening visual evidence, or changing the deterministic/advisory/human lane separation (D-18, D-19, D-21)."
  artifacts:
    - path: "test/docs_contract/configurator_phase_gate_test.exs"
      provides: "Durable Wave-3 direct-file integration smoke for the complete static configurator asset graph and canonical committed inputs"
    - path: ".planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md"
      provides: "Execution record for automated final gates and the bounded human browser/assistive-technology disposition"
  key_links:
    - from: "test/docs_contract/configurator_phase_gate_test.exs"
      to: "assets/rendro/configurator/index.html"
      via: "direct filesystem load verifies every local CSS/JS/JSON reference resolves without a server or build product"
      pattern: "configurator/index.html"
    - from: "assets/rendro/configurator/index.json"
      to: "lib/rendro/theme/snippet.ex"
      via: "phase gate combines exact generator drift check with snippet provenance tests"
      pattern: "mix rendro.configurator.gen --check"
    - from: "guides/livebook/first_invoice.livemd"
      to: "assets/rendro/configurator/index.json"
      via: "Livebook checker and phase-gate contract prove the same canonical Invoice/Swiss/#2C6BED/light source block"
      pattern: "usage_snippet"
---

<objective>
Own the deterministic and human post-integration proof for the complete Phase-128 surface after every Wave-2 consumer has landed.

Purpose: Close CONFIG-01..06 from one dependency-correct Wave-3 owner, preserving the distinction between automated source/topology proof and human visual/assistive-technology judgment.
Output: One durable direct-file phase-gate test, green full CI/Livebook/index-drift evidence, and a recorded cross-browser accessibility disposition.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-RESEARCH.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-02-SUMMARY.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-03-SUMMARY.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the integrated static graph, canonical drift seam, Livebook, and full CI</name>
  <precondition>Plans 128-02, 128-03, and 128-04 have completed and their SUMMARY files exist.</precondition>
  <read_first>
    - assets/rendro/configurator/index.html — shipped static entry point and local CSS/JS/JSON references.
    - assets/rendro/configurator/configurator.css — token, responsive, state, focus, overflow, and reduced-motion contract.
    - assets/rendro/configurator/configurator.js — two-manifest load, URL, resolver, safe DOM, and clipboard controller.
    - assets/rendro/configurator/index.json — committed 504-record formatter-owned selection/source index.
    - assets/rendro/catalog.json — committed ordered preview authority and safe local raster paths.
    - lib/rendro/theme/snippet.ex — canonical usage/module/index formatter seam from Plan 01.
    - dev/mix/tasks/rendro/configurator/gen.ex — exact `mix rendro.configurator.gen --check` read-only byte-drift contract.
    - lib/mix/tasks/rendro/gen/theme.ex — packaged application-owned wrapper consumer from Plan 02.
    - guides/livebook/first_invoice.livemd — canonical third-surface block and one themed render from Plan 04.
    - test/docs_contract/configurator_static_contract_test.exs — Plan-03 static topology, semantics, scope, and safe-DOM assertions.
    - test/docs_contract/configurator_resolver_contract_test.exs — Plan-03 exact/representative/none, URL, source, and clipboard assertions.
    - test/rendro/theme/snippet_test.exs — exhaustive formatter/index equality and controlled evaluation contract.
    - test/mix/tasks/rendro_livebook_check_test.exs — exact notebook block, one-render, and no-server/no-interactive contract.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md — final automated and human coverage boundaries.
  </read_first>
  <files>test/docs_contract/configurator_phase_gate_test.exs</files>
  <behavior>
    - The static entry point's local stylesheet, script, index, catalog, and manifest-backed raster references resolve from committed repository files without starting or depending on a service.
    - The phase-gate test parses both JSON inputs, proves the expected closed index/catalog relationship, and links visible source ownership to the canonical formatter rather than a JavaScript template.
    - The direct smoke remains a test-only contract and introduces no shipped route, server, build output, package dependency, or product symbol.
    - Full CI, no-server Livebook execution, and read-only committed-index equality all pass after the three Wave-2 consumers are integrated.
  </behavior>
  <action>
Write `ConfiguratorPhaseGateTest` as an ExUnit direct-filesystem integration contract. Resolve the HTML's local CSS/JS/JSON references relative to committed assets, parse both committed JSON documents, and assert referenced catalog rasters are safe repository-relative files that exist. Bind the selected source/index relationship to the existing snippet and resolver contracts; reuse their public test-facing helpers or data shapes instead of reimplementing product behavior. Assert the smoke requires no network listener, HTTP route, Phoenix endpoint, database, Node/npm command, package install, or generated docs directory (D-06, D-07, D-10, D-20, D-21). Keep this file verification-only: create no production module, browser controller, alternate resolver, build script, or second manifest. Then run the test first, the exact read-only generator check selected in resolved research, the existing no-server Livebook checker, and the complete deterministic CI lane.
  </action>
  <acceptance_criteria>
    - `test/docs_contract/configurator_phase_gate_test.exs` loads the committed static entry point and proves all required local CSS/JS/JSON/raster links resolve directly from repository files.
    - The smoke parses both manifests and confirms canonical index/source provenance by composing existing Phase-128 contracts rather than duplicating formatter or resolver logic.
    - The smoke runs without starting a listener, generating docs, invoking Node/npm, or adding any shipped runtime/build dependency.
    - `mix rendro.configurator.gen --check` exits zero and leaves `assets/rendro/configurator/index.json` bytes and mtime unchanged.
    - `mix rendro.livebook.check` and `mix ci.fast` both exit zero after Plans 02-04 are integrated.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/configurator_phase_gate_test.exs --max-failures 1 &amp;&amp; mix rendro.configurator.gen --check &amp;&amp; mix rendro.livebook.check &amp;&amp; mix ci.fast</automated>
  </verify>
  <done>The complete product graph has one green post-Wave-2 deterministic proof owner, and the direct static smoke adds no shipped execution surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate responsive visual and assistive-technology behavior in current browsers</name>
  <precondition>Task 1's complete automated command is green.</precondition>
  <read_first>
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md — approved responsive, theme, state, copy, focus, and accessibility judgment contract.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md — explicit current-browser and VoiceOver/NVDA manual-only boundary.
    - assets/rendro/configurator/index.html — static page under review.
  </read_first>
  <files>None; record the browser/assistive-technology evidence and disposition in 128-05-SUMMARY.md.</files>
  <action>
Build ExDoc with the existing project command and serve only the generated documentation directory from a temporary localhost static-file server for review; this is evidence tooling, not a shipped server or product dependency. If safe browser automation is available, use it to collect viewport/state screenshots and keyboard traces, but do not treat those artifacts as substitutes for human visual hierarchy or screen-reader comprehension. Stop the temporary server after review. Record browser versions, viewport sizes, OS chrome-theme setting, document mode, preview state, and assistive technology used in the plan SUMMARY.
  </action>
  <what-built>All Phase-128 product surfaces plus the green direct-file, index-drift, Livebook, and full-CI integration gate from Task 1.</what-built>
  <how-to-verify>
    1. In current Chrome, Firefox, and Safari, review both a wide viewport at or above 900px and a narrow mobile viewport below 900px. Confirm the 320px/two-column layout becomes the locked configuration → preview → disclosure → snippet stack, images retain natural aspect, and complete long code scrolls horizontally without expanding or clipping the page.
    2. In each browser, review chrome under OS light, OS dark, and system-following behavior. For both document `light` and `dark`, confirm document mode does not change chrome theme and the dark manifest disclosure remains complete and verbatim.
    3. Exercise one `exact`, one manifest-ordered accent `representative`, and one `none` selection. Confirm requested family/preset/accent/mode and copied source never change to match the preview; none remains valid and copyable; factual relationship copy matches D-05.
    4. Throttle or block local JSON/image loading to inspect loading and actionable manifest/image error states. Confirm unavailable actions stay disabled, no preview is fabricated, and recovery explains what/where/why/next. Restore assets and test an intentionally long snippet/caption/disclosure for full wrapping or code overflow.
    5. Use keyboard only through all four native selects and copy. Confirm visible non-color focus, logical focus order, 44px targets, no focus movement after copy, polite preview/copy announcements, and alert semantics only for actionable failure.
    6. Allow clipboard access and confirm exact visible source copies with `Snippet copied` feedback, then deny/block clipboard permission and confirm failure is announced, source remains selectable, focus stays stable, and retry/recovery remains available.
    7. With VoiceOver on macOS or NVDA on Windows, repeat selection, exact/representative/none status, copy success, and copy failure. Confirm labels, selected values, live-region changes, disclosure, errors, and retry are understandable in reading/focus order.
    8. Record PASS/FAIL for every browser/state/assistive-technology row. Any visual or assistive-technology failure blocks approval; deterministic tests alone cannot override it.
  </how-to-verify>
  <acceptance_criteria>
    - Current Chrome, Firefox, and Safari pass wide/narrow responsive review across light/dark/system chrome and independent light/dark document modes.
    - Exact, representative, and none states preserve requested code identity, factual disclosure, and copy availability exactly as specified.
    - Loading, manifest/image error, long-code, keyboard, focus, live-region, copy-success, and copy-failure states are understandable and recoverable.
    - VoiceOver or NVDA confirms native labels, reading/focus order, status announcements, failure alerts, and retry behavior; the assistive-technology judgment is explicitly recorded.
    - The SUMMARY identifies which evidence is deterministic, which is browser-observed, and which is human/assistive-technology judgment without extending visual or compliance claims.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/configurator_phase_gate_test.exs --max-failures 1 &amp;&amp; mix rendro.configurator.gen --check &amp;&amp; mix rendro.livebook.check</automated>
    <human-check>Complete all eight browser and assistive-technology steps above and record a passing matrix before approving the checkpoint.</human-check>
  </verify>
  <resume-signal>Type "approved" only after recording the complete passing matrix, or describe each failed browser/state/assistive-technology row.</resume-signal>
  <done>The human-needed visual and assistive-technology boundaries are explicitly passed and recorded after the deterministic gate.</done>
</task>

</tasks>

## Artifacts this phase produces

- New test-only file `test/docs_contract/configurator_phase_gate_test.exs` with durable `ConfiguratorPhaseGateTest` direct-file integration coverage for the committed static asset graph, both JSON inputs, raster path existence, and canonical snippet provenance.
- Execution-created `.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md` recording exact automated gate commands/results plus the current Chrome/Firefox/Safari and VoiceOver-or-NVDA manual matrix.
- No production symbol, public API, generated-code format, manifest schema, browser feature, build pipeline, server route, runtime dependency, or package payload is added by this validation-only plan.

## Assumption delta decision

Primary noun: canonical requested configuration/source model (`family`, `preset`, `accent`, `mode`). Decision: no-change. Plan 05 verifies the already planned identity and consumer seams; it creates no alternate identity or product behavior.

## Aggregate edge and prohibition accounting

Plan 05 does not add or duplicate product prohibitions. Its automated and human gates verify the bounded truths and explicitly flagged assumptions already owned by Plans 01-04: exhaustive source/index equality, single-invocation generator behavior, atomic URL/three-state preview/source identity, and one focused no-server Livebook render. The unresolved edge/prohibition counts therefore remain those of the implementation plan set rather than being inflated by validation restatements.

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| committed product assets → phase-gate test | The final proof must inspect the same checked-in files users receive, not fixtures or generated substitutes. |
| automated evidence → human acceptance | Green source/CI checks cannot establish responsive visual quality or assistive-technology comprehension. |
| temporary local review tooling → shipped surface | A localhost static server may support review but must not become a product/runtime/build dependency. |

## STRIDE Threat Register (OWASP ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation / owner | Automated verification | Residual risk |
|---|---|---|---|---|---|---|---|
| T-128-06 | Tampering / Repudiation | `ConfiguratorPhaseGateTest` and committed assets | high | mitigate | Task 1 resolves committed references directly, parses both committed JSON inputs, composes existing provenance contracts, and runs byte-exact generator drift plus full CI after all Wave-2 dependencies. | Task 1 complete automated command | Repository compromise outside CI trust remains out of scope; exact checked-in inputs and drift checks bound project evidence. |
| T-128-07 | Repudiation | browser and assistive-technology disposition | medium | mitigate | Task 2 records browser versions, state matrix, AT used, and PASS/FAIL rows; approval blocks on any failure and distinguishes automation from human judgment. | Focused gate rerun before checkpoint plus recorded manual matrix | Human judgment is not mechanically reproducible; explicit environment/evidence records make the conclusion auditable. |
| T-128-08 | Information disclosure / Elevation of privilege | temporary static review server | low | accept | Bind only to localhost, serve only generated public docs, add no credentials or user data, and stop after review. | Direct-file smoke proves product behavior does not depend on the review server. | Local browser extensions and host configuration remain operator-controlled. |
</threat_model>

<verification>
Task 1 is the deterministic post-Wave-2 phase gate: run the direct static-file integration test, exact read-only configurator index check, no-server Livebook checker, and `mix ci.fast`. Task 2 reruns the focused deterministic seams, then blocks completion until current Chrome/Firefox/Safari and VoiceOver-or-NVDA responsive/state/accessibility review passes. Preserve the Validation artifact's `status: draft` and `nyquist_compliant: false`; only validate-phase may advance that lifecycle.
</verification>

<success_criteria>
- One Wave-3 owner runs after Plans 02, 03, and 04 and proves CONFIG-01..06 together.
- Static smoke inspects committed files directly and adds no shipped server/build/runtime dependency.
- Index drift, Livebook execution, and full deterministic CI are green.
- Current browser and assistive-technology review covers every state named in 128-VALIDATION.md and records the human-needed judgment separately.
- No product scope, visual evidence set, or deterministic/advisory/human claim boundary changes.
</success_criteria>

## Multi-Source Coverage Audit (complete Phase-128 set)

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Three canonical tinkerer surfaces with truthful zero-server browsing, code generation, and Livebook proof | 01-05 | COVERED | Plan 05 owns only integrated closure. |
| REQ | CONFIG-01 | Static zero-server ExDoc configurator | 03, 05 | COVERED | Product in 03; direct-file and browser proof in 05. |
| REQ | CONFIG-02 | Exact/representative/none truthful preview | 03, 05 | COVERED | Product in 03; integrated/manual states in 05. |
| REQ | CONFIG-03 | Exact working copy source and feedback | 01, 03, 05 | COVERED | Source, interaction, then final proof. |
| REQ | CONFIG-04 | Safe deterministic four-key URL | 03, 05 | COVERED | Product in 03; browser restoration/focus proof in 05. |
| REQ | CONFIG-05 | Canonical formatter generators and byte drift gates | 01, 02, 05 | COVERED | Plan 05 runs the resolved dev-index check and full integration. |
| REQ | CONFIG-06 | Existing Livebook third surface | 04, 05 | COVERED | Plan 05 runs the no-server checker after all consumers land. |
| RESEARCH | — | Resolved dev task `Mix.Tasks.Rendro.Configurator.Gen` at `dev/mix/tasks/rendro/configurator/gen.ex`, committed index, and read-only byte-exact `--check` | 01, 05 | COVERED | Creation in 01; final drift proof in 05. |
| RESEARCH | — | Canonical formatter, safe static two-manifest UI, Mix.Generator wrapper, focused Livebook, no new dependencies | 01-05 | COVERED | No package legitimacy gate applies. |
| CONTEXT | D-01..D-06 | Requested state, truthful preview/URL/UI/browser contract | 03, 05 | COVERED | |
| CONTEXT | D-07..D-11 | Canonical formatter/index/snippet/font bridge | 01, 03-05 | COVERED | |
| CONTEXT | D-12..D-15 | Generated wrapper and safe CLI/write/check | 02, 05 | COVERED | |
| CONTEXT | D-16..D-18 | Focused no-server Livebook and honest learning copy | 04, 05 | COVERED | |
| CONTEXT | D-19 | Exhaustive bounded source proof without visual overclaim | 01, 05 | COVERED | Final gate does not widen visual evidence. |
| CONTEXT | D-20 | Consumer seams and final integrated proof | 01-05 | COVERED | Plan 05 is the dependency-correct owner. |
| CONTEXT | D-21 | Bounded conventional surfaces without framework/runtime expansion | 02-05 | COVERED | |
| UI-SPEC | — | Responsive, theme, state, copy, focus, and accessibility judgment | 03, 05 | COVERED | Deterministic contracts in 03; manual gate in 05. |

Deferred ideas remain excluded: interactive/all-preset Livebook, arbitrary colors or fuzzy/cross-dimension preview fallback, live PDF/server/accounts/persistence, catalog expansion, and a public general-purpose formatter API.

<output>
Create `.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md` when done.
</output>
