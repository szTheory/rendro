---
phase: 36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/text.ex
  - test/rendro/text_test.exs
  - lib/rendro/pipeline/measured_text.ex
  - lib/rendro/pipeline/measure.ex
  - test/rendro/pipeline/measure_test.exs
autonomous: true
requirements: [REQ-01, REQ-02]
must_haves:
  truths:
    - Text struct has default widow and orphan values
    - MeasuredText struct requires and holds widow and orphan values
    - The measure pipeline explicitly propagates these values
  artifacts:
    - path: lib/rendro/text.ex
      provides: Schema with widow/orphan defaults
    - path: lib/rendro/pipeline/measured_text.ex
      provides: State definition with widow/orphan keys
    - path: lib/rendro/pipeline/measure.ex
      provides: Value mapping in measure_block
  key_links:
    - from: lib/rendro/pipeline/measure.ex
      to: lib/rendro/pipeline/measured_text.ex
      via: struct initialization
      pattern: "%MeasuredText{"
---

<objective>
Update core schemas and the measure pipeline to carry widow and orphan configuration.
Purpose: Layout algorithms down the pipeline need these constraints to make clean page-breaking decisions.
Output: Updated Text and MeasuredText structs, and a Measure pipeline that propagates them.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add widows and orphans to Text schema</name>
  <files>lib/rendro/text.ex, test/rendro/text_test.exs</files>
  <behavior>
    - Text struct defaults to `widows: 2` and `orphans: 2`
  </behavior>
  <action>
    Add `:widows` and `:orphans` to `defstruct` with defaults of `2`.
    Update `@type t` to include `widows: non_neg_integer()` and `orphans: non_neg_integer()`.
    Add test in `test_test.exs` ensuring they default to 2.
  </action>
  <verify>
    <automated>mix test test/rendro/text_test.exs</automated>
  </verify>
  <done>Text schema updated and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update MeasuredText and Measure pipeline</name>
  <files>lib/rendro/pipeline/measured_text.ex, lib/rendro/pipeline/measure.ex, test/rendro/pipeline/measure_test.exs</files>
  <behavior>
    - MeasuredText requires widows and orphans fields
    - measure_block propagates widows and orphans from Text to MeasuredText
  </behavior>
  <action>
    In `measured_text.ex`: Add `:widows` and `:orphans` to `@enforce_keys` and `defstruct`. Update `@type t`.
    In `measure.ex`: Update the `measure_block` function that handles `%Rendro.Text{}` to map `widows: text.widows` and `orphans: text.orphans` into the `%MeasuredText{}` struct.
    In `measure_test.exs`: Verify the returned MeasuredText includes the correct widow/orphan values.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Pipeline state requires and maps widow/orphan fields correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Schema -> Internal Pipeline | Input parameters are directly validated by struct defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Denial of Service | `text.ex` | accept | Extremely large widow/orphan values could cause infinite pagination loops. Accept for now, S01 assumes trusted operators. Limits will be addressed structurally via Paginate logic rather than schema. |
</threat_model>

<verification>
Unit tests passing for `test/rendro/text_test.exs` and `test/rendro/pipeline/measure_test.exs`
</verification>

<success_criteria>
`mix test` for affected files passes, confirming values are propagated from schema to pipeline state.
</success_criteria>

<output>
After completion, create `.planning/phases/36-widow-orphan-layout-controls/36-01-SUMMARY.md`
</output>