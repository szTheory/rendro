---
phase: 135-test-ci-cd-simplification
plan: "03"
type: execute
wave: 3
depends_on: ["135-02"]
files_modified:
  - .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md
  - .github/workflows/ci.yml
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
requirements: [CI-02, CI-03, CI-04]
must_haves:
  truths:
    - "CI-02 unclassified probe resolved: the matrix has exactly four `matched` old/new rows produced remotely on one identical full candidate SHA, with normalized semantic roles/per-file hashes and every authority fact proven before any legacy deletion (D-18, D-19, D-20)."
    - "One matched legacy/generic run is decisive for each row; a repeat generic run is corroboration only and unavailable/missing/extra/mismatch/unexplained evidence blocks retirement truthfully (D-20)."
    - "Local tests prove comparator/workflow/topology behavior, paired Ubuntu/PDFium runs prove raster payload identity, and neither claims Phase 136 human visual quality (D-21)."
    - "After and only after all four rows bind the same exact SHA and pass, one dedicated cutover commit removes only Phase 126/127/130 branch patterns, conditional generation/staging/upload steps, and legacy string assertions; that commit is the rollback unit (D-22)."
    - "Post-cutover ordinary CI retains main/PR/schedule behavior, deterministic/proof/advisory lane membership, cache policy, least privilege, immutable action pins, and sole `ci-success` needs exactly (D-11, D-21, D-22)."
  artifacts:
    - path: .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md
      provides: "Committed four-row exact-SHA parity record with remote run facts"
      contains: "| matched |"
    - path: .github/workflows/ci.yml
      provides: "Ordinary CI without milestone-specific catalog routes"
      contains: "ci-success:"
    - path: test/guardrails/required_checks_contract_test.exs
      provides: "Post-cutover route-absence and unchanged-authority contracts"
      contains: "catalog-evidence.yml"
  key_links:
    - from: .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md
      to: .github/workflows/ci.yml
      via: "all four matched rows are a hard precondition for the deletion task"
      pattern: "phase130_canonical.*matched"
    - from: test/guardrails/required_checks_contract_test.exs
      to: .github/workflows/ci.yml
      via: "legacy-route absence plus preserved lane and ci-success topology"
      pattern: "ci-success"
    - from: .github/workflows/catalog-evidence.yml
      to: .github/workflows/ci.yml
      via: "generic evidence remains standalone after legacy route removal"
      pattern: "workflow_dispatch"
  prohibitions:
    - statement: "Repeated generic runs must not be presented as a substitute for any missing legacy/generic parity row."
      status: unverified
      flagged: true
    - statement: "The cutover must not remove or reclassify ordinary deterministic, proof, advisory, cache, permission, pin, or `ci-success` authority."
      status: unverified
      flagged: true
    - statement: "Remote payload parity must not silently become a claim of human visual quality or approval."
      status: unverified
      flagged: true
---

<objective>
Prove all four legacy routes against the generic workflow on one exact SHA, then retire only the legacy machinery in one rollback-sized commit.

Purpose: Make remote paired evidence the hard predecessor of deletion while keeping every additive local improvement executable even if GitHub/PDFium evidence is temporarily unavailable.
Output: A committed four-row matched parity record followed by a dedicated legacy-route deletion commit with route-absence and unchanged-authority proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/135-test-ci-cd-simplification/135-CONTEXT.md
@.planning/phases/135-test-ci-cd-simplification/135-RESEARCH.md
@.planning/phases/135-test-ci-cd-simplification/135-02-SUMMARY.md
@.planning/phases/135-test-ci-cd-simplification/135-test-inventory.md
@.github/workflows/ci.yml
@.github/workflows/catalog-evidence.yml
@.github/workflows/CATALOG-EVIDENCE.md
</context>

## Artifacts this phase produces

- No net-new file, public symbol, command, schema, field, dependency, or artifact type is created in this plan.
- `.planning/phases/135-test-ci-cd-simplification/135-test-inventory.md` finalizes `candidate_sha`, legacy/generic run URL, run ID/attempt, PDFium version/binary SHA/DPI, normalized role/count/hash result, permission/pin result, upload digest, and `matched` fields for exactly four rows.
- `.github/workflows/ci.yml` deletes the four milestone branch patterns and their Phase 126/127/130 conditional generation/staging/upload routes; it preserves its existing public trigger/job/lane/`ci-success` schema.
- `test/guardrails/required_checks_contract_test.exs` replaces legacy positive route assertions with route-absence assertions while retaining generic workflow and authority contracts.

<tasks>

<task type="auto">
  <name>Task 1: Capture four paired remote parity rows on one exact full SHA</name>
  <precondition>`gh auth status` succeeds for this repository, the Plan 02 commit is reachable on the default branch, and the authenticated maintainer may dispatch/read artifacts and create/delete temporary legacy branch refs.</precondition>
  <read_first>
    - .github/workflows/CATALOG-EVIDENCE.md
    - .github/workflows/catalog-evidence.yml
    - .github/workflows/ci.yml
    - dev/rendro/catalog_evidence_parity.ex
    - test/rendro/catalog_evidence_parity_test.exs
    - .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md
    - .planning/phases/135-test-ci-cd-simplification/135-CONTEXT.md
  </read_first>
  <files>.planning/phases/135-test-ci-cd-simplification/135-test-inventory.md</files>
  <action>Select the one full lowercase 40-character SHA containing Plans 01-02 while all four legacy routes still exist and write it into every matrix row. For `phase126_preset_review`, `phase127_catalog_review`, `phase130_review`, and `phase130_canonical`, trigger the corresponding legacy `ci.yml` route at that exact commit using its existing temporary branch naming contract, then dispatch generic `review` for the first three and generic `canonical` for the fourth with `gh workflow run catalog-evidence.yml -f candidate_sha=$CANDIDATE_SHA -f operation=...`. Watch each run to terminal status, download both artifacts to fresh isolated directories, validate the generic bundle, and run `Rendro.CatalogEvidenceParity.compare/3`. Record full run URLs/IDs/attempts, both upload digests, renderer version/binary hash/DPI, role/count/hash comparison, candidate/HEAD identity, candidate reviewer-field absence, action-pin/permission result, and `matched` only on complete equality. Use one generic review run for the three review mappings only when its closed bundle contains all three normalized role sets; otherwise dispatch separate review attempts but keep every row on the same candidate SHA. Delete only the temporary remote legacy branch refs after their artifacts and run identities are recorded. If any run or evidence is unavailable, missing, extra, mismatched, or unexplained, record that exact status and stop before Task 2; a repeated generic run may corroborate but cannot replace a legacy pair.</action>
  <acceptance_criteria>
    - Exactly four rows say `matched`, all four carry the same literal candidate SHA, and every legacy/generic run reached success on that SHA.
    - Every row records normalized roles/per-file hashes and all D-19 authority facts; ZIP/archive digests are transport columns only.
    - No `matched` row contains missing, unavailable, mismatch, pending, or unexplained fields.
    - The generic workflow remains graph-disconnected and no repository content is published or materialized by the remote runs.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_evidence_parity_test.exs --max-failures 1 &amp;&amp; test "$(rg -c '^\| phase(126_preset_review|127_catalog_review|130_review|130_canonical) \| [0-9a-f]{40} \|.*\| matched \|' .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md)" = 4 &amp;&amp; test "$(rg -o '^\| phase(126_preset_review|127_catalog_review|130_review|130_canonical) \| [0-9a-f]{40}' .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md | awk '{print $4}' | sort -u | wc -l | tr -d ' ')" = 1</automated>
  </verify>
  <done>One committed matrix proves all four legacy/generic pairs on the same full SHA; any unavailable or mismatched row stops the plan before deletion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete legacy routes in the dedicated rollback commit</name>
  <precondition>The committed parity matrix passes Task 1's automated gate with exactly four matched rows on one full SHA.</precondition>
  <read_first>
    - .planning/phases/135-test-ci-cd-simplification/135-test-inventory.md
    - .github/workflows/ci.yml
    - .github/workflows/catalog-evidence.yml
    - test/guardrails/required_checks_contract_test.exs
    - priv/guardrails/required_status_checks.json
    - mix.exs
  </read_first>
  <files>.github/workflows/ci.yml, test/guardrails/required_checks_contract_test.exs</files>
  <behavior>
    - "Legacy Phase 126/127/130 branch triggers and conditional generation/staging/upload steps are absent after cutover."
    - "Generic workflow security/route contracts, main/PR/schedule CI behavior, lane membership, cache behavior, immutable pins, and ci-success needs remain green."
    - "A fixture reintroducing any retired branch/step string or adding catalog-evidence to ci-success makes the guardrail fail."
  </behavior>
  <action>First replace the legacy positive assertions in `required_checks_contract_test.exs` with post-cutover route-absence and weakening-mutation contracts while preserving all generic workflow, permission, pin, lane, cache, and `ci-success` assertions. Then, per D-22, edit `.github/workflows/ci.yml` only to remove the four Phase 126/127/130 push branch patterns and their conditional generation, staging, upload, and legacy exception/string machinery. Preserve main/pull_request/schedule triggers, every ordinary job, deterministic/proof/advisory membership, ordinary cache restore/save policy, top-level read permission, immutable action pins, and `ci-success` needs exactly `[test, configurator-browser, integration-proofs, quality-governance]`. Do not modify `catalog-evidence.yml`, the required-status baseline, Mix aliases, catalog payload generation, or any product/runtime file. Commit this task by itself with a message identifying legacy catalog-route cutover; record that commit SHA as the explicit rollback unit in the task summary.</action>
  <acceptance_criteria>
    - Source assertions show no Phase 126/127/130 legacy branch route or conditional artifact machinery remains in `ci.yml`.
    - Parsed workflow tests prove ordinary CI triggers/jobs/lanes/caches/permissions/pins and sole ci-success membership are unchanged.
    - Generic catalog evidence stays standalone/manual and is absent from ordinary `needs` and required contexts.
    - Git history contains one task-local cutover commit touching only `ci.yml` and its guardrail test; its SHA is recorded for rollback.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs test/rendro/catalog_evidence_bundle_test.exs test/rendro/catalog_evidence_parity_test.exs --max-failures 1</automated>
  </verify>
  <done>All legacy catalog routes are absent in one isolated rollback commit, while the generic manual workflow and every existing ordinary CI authority contract remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Default-branch workflow -> exact candidate | All paired runs bind trusted workflow controls to one detached untrusted full SHA. |
| Legacy artifact + generic bundle -> comparator | Remote outputs remain untrusted until normalized semantic roles, hashes, and authority facts match. |
| Remote parity -> deletion authority | Only four complete matched rows authorize the dedicated cutover task. |
| Generic evidence -> ordinary CI | The manual workflow remains outside required topology before and after cutover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-135-01 | Spoofing | paired candidate identity | high | mitigate | All eight paired sides bind one full SHA and literal HEAD; matrix gate rejects multiple/short/missing SHAs. |
| T-135-02 | Tampering | default control vs candidate | high | mitigate | Candidate uses detached credential-free checkout and env-mediated inputs; control helper validates outputs. |
| T-135-03 | Tampering / Information disclosure | downloaded artifacts/paths | high | mitigate | Fresh isolated download roots, safe path confinement, closed roles/counts, and recomputed hashes precede comparison. |
| T-135-04 | Information disclosure | remote tokens/secrets/caches | high | mitigate | Read-only permissions, no candidate credentials/secrets/cache, and no log emission of credentials; legacy routes are used only as already-defined controls. |
| T-135-05 | Elevation of privilege | dispatch/cutover topology | high | mitigate | No `workflow_run` bridge or write permission; route absence tests preserve generic disconnection and sole `ci-success`. |
| T-135-06 | Tampering | action/PDFium identities | high | mitigate | Matrix records immutable action pins plus PDFium version/binary hash/DPI for both sides and rejects disagreement. |
| T-135-07 | Repudiation / Tampering | parity/artifact integrity | high | mitigate | Record run URL/ID/attempt/upload digest and compare normalized per-file hashes; transport digest cannot substitute for payload parity. |
| T-135-08 | Elevation of authority | parity and visual claims | high | mitigate | Matrix proves only route/payload authority; no scores/approval are recorded and Phase 136 remains visual authority. |
| T-135-09 | Denial of service | GitHub/PDFium availability | medium | accept | Unavailability is truthful evidence and stops only this plan/cutover; Plans 01-02 remain complete and no deletion occurs. |
| T-135-SC | Tampering | package installs | low | accept | No package is installed or added during parity/cutover; existing pinned workflow dependencies remain unchanged. |

## Verification and Residual Risk

- The matrix and local comparator independently reject identity, renderer, role, hash, count, permission, pin, reviewer-field, run/attempt, and availability drift.
- Residual GitHub retention or service loss can make old artifacts unavailable; that is a hard cutover blocker, not evidence to infer.
- Post-cutover defects are recoverable by reverting the one dedicated deletion commit without reverting additive workflow/tooling/test work.
</threat_model>

## Spec-less probe fallback audit

- The CI-02 unclassified probe is resolved by the exact four-row same-SHA predecessor predicate above. CI-03/CI-04 continue to be resolved by topology/security predicates; no probe row is auto-dismissed.
- Prohibition Stage 1 recalled replacement-by-repeat, authority collapse, visual overclaim, remote branch cleanup, generic injection/path/supply-chain concerns, and routine command failure.
- Stage 2 kept the three project-specific parity/authority prohibitions descriptor-less and flagged/unverified. Routine command failure is acceptance/verification work. Canon injection/path/supply-chain items remain in STRIDE and `$gsd-secure-phase` breadcrumbs rather than duplicate prohibitions.

## Multi-source coverage audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Easier tests/automation without weaker behavior, trust, or CI authority | 01-03 | COVERED | Bounded cleanup, additive workflow, parity-gated cutover. |
| REQ | TEST-01 | Inventory plus fail-first replacement proof | 01 | COVERED | Recipe ledger and retained-owner negative control. |
| REQ | TEST-02 | Consolidate brittle overlap without contract loss | 01 | COVERED | One duplicate deletion and truthful rename only. |
| REQ | CI-01 | One exact-SHA read-only pinned workflow | 01-02 | COVERED | Bundle contract and standalone workflow. |
| REQ | CI-02 | Four-route parity before removal | 01,03 | COVERED | Comparator plus hard-gated matrix. |
| REQ | CI-03 | Preserve lanes and ci-success | 02-03 | COVERED | Additive baseline and post-cutover topology tests. |
| REQ | CI-04 | Cache/secret/pin/permission trust | 01-03 | COVERED | Bundle, workflow, parity, and cutover guards. |
| REQ | CI-05 | Current operator reproduction docs | 02 | COVERED | Adjacent runbook and docs lane. |
| RESEARCH | — | Dev/test bundle helper, normalized comparator, static/local vs remote authority, no new dependency | 01-03 | COVERED | Responsibility map tiers preserved. |
| CONTEXT | D-01..D-08 | Bounded cleanup and tests with teeth | 01 | COVERED | Decision IDs cited in truths/tasks. |
| CONTEXT | D-09..D-17 | Workflow/artifact/operator contract | 01-02 | COVERED | Decision IDs cited in truths/tasks. |
| CONTEXT | D-18..D-22 | Parity and dedicated cutover | 01,03 | COVERED | Same-SHA matrix is hard predecessor. |
| CONTEXT | D-23..D-24 | Current runbook and four operator jobs | 02 | COVERED | Adjacent guide plus docs contract. |

Deferred whole-suite architecture, artifact attestations, catalog visual changes, score updates, and human approval are excluded by explicit user decision and do not constitute gaps.

<verification>
- `mix test test/rendro/catalog_evidence_bundle_test.exs test/rendro/catalog_evidence_parity_test.exs test/guardrails/required_checks_contract_test.exs test/docs_contract/catalog_evidence_runbook_test.exs --max-failures 1`
- Four matrix rows match one exact SHA with complete remote run facts.
- `mix run scripts/verify_docs.exs`
- `mix ci.fast`
- `mix ci`
</verification>

<success_criteria>
- Four old/new route pairs match on one exact full SHA before deletion.
- One dedicated cutover commit removes only legacy catalog routes and their string assertions.
- Ordinary CI, caches, permissions, immutable pins, deterministic/proof/advisory lanes, and sole `ci-success` authority remain unchanged.
- The generic workflow remains read-only, manual, graph-disconnected, one-bundle-only, and non-visual-authoritative.
- Remote unavailability blocks this plan only and is never converted into inferred parity.
</success_criteria>

<output>
Create `.planning/phases/135-test-ci-cd-simplification/135-03-SUMMARY.md` when done.
</output>
