---
phase: "13"
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - mix.exs
  - lib/mix/tasks/release/preflight.ex
  - test/mix/tasks/release_preflight_test.exs
autonomous: true
requirements:
  - QUAL-04
must_haves:
  truths:
    - "`mix release.preflight` fails immediately on dirty worktrees, tag/version mismatch, and missing release prerequisites"
    - "`mix release.preflight` runs expensive release checks only after boundary blockers pass"
    - "Release preflight reaches package-build and publish dry-run parity from the exact tagged ref instead of stopping at mocked checks"
    - "Phase 2 prints one aggregated summary and exits once after all runnable checks complete"
  artifacts:
    - path: mix.exs
      provides: "Hex package metadata and release prerequisites needed for truthful package-build parity"
    - path: lib/mix/tasks/release/preflight.ex
      provides: "Strict two-phase release gate with boundary-first blockers and aggregated expensive checks"
    - path: test/mix/tasks/release_preflight_test.exs
      provides: "Regression coverage for blocker failures, phase ordering, and aggregated final-exit semantics"
  key_links:
    - from: lib/mix/tasks/release/preflight.ex
      to: mix.exs
      via: "exact version/tag and package metadata checks"
      pattern: "Mix.Project.config\\(\\)\\[:version\\]"
    - from: lib/mix/tasks/release/preflight.ex
      to: git
      via: "exact-tag and dirty-worktree boundary checks"
      pattern: "git.*describe|git.*status"
    - from: lib/mix/tasks/release/preflight.ex
      to: test/mix/tasks/release_preflight_test.exs
      via: "Mix task summary and exit regression coverage"
      pattern: "release preflight"
---

<objective>
Turn `mix release.preflight` into the strict release gate defined by Phase 13 so it blocks mutable or mismatched release state early and only reaches package/publish parity from a clean exact-tagged ref.

Purpose: `QUAL-04` is still blocked because the current task only logs dirty state, does not enforce exact release parity, and never exercises full publish dry-run behavior.
Output: Completed package metadata in `mix.exs`, a boundary-first two-phase preflight task, and focused regression coverage for its failure and summary semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/13-docs-and-release-preflight-closure/13-CONTEXT.md
@.planning/phases/13-docs-and-release-preflight-closure/13-RESEARCH.md
@.planning/phases/13-docs-and-release-preflight-closure/13-VALIDATION.md
@mix.exs
@lib/mix/tasks/release/preflight.ex
@scripts/verify_docs.exs
@test/mix/tasks/verify_test.exs

<interfaces>
From `lib/mix/tasks/release/preflight.ex`:
```elixir
def run(_args) do
  Mix.shell().info("Running release preflight checks...")

  version = Mix.Project.config()[:version]
  Mix.shell().info("Current version: #{version}")

  if Mix.Task.run("ci") == :error do
    Mix.raise("CI checks failed!")
  end

  {git_status, 0} = System.cmd("git", ["status", "--short"])

  if git_status != "" do
    Mix.shell().error("Uncommitted changes detected:\n#{git_status}")
  end
end
```

From `mix.exs`:
```elixir
@version "0.1.0"

def project do
  [
    version: @version,
    description: "...",
    source_url: @source_url
  ]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete Hex package metadata and strict release prerequisites in `mix.exs`</name>
  <files>mix.exs</files>
  <behavior>
    - The package metadata is sufficient for `mix hex.build --unpack` per D-14 and D-18.
    - Release preflight prerequisites are explicit rather than implied by the local machine state per D-12.
    - Core purity and optional Phoenix/Oban boundaries remain unchanged.
  </behavior>
  <action>
    Update `mix.exs` so the release surface is truthful and package-build parity can actually run. Research already confirmed `mix hex.build --unpack` fails because required package metadata is incomplete, so add the canonical Hex package fields needed for local package verification and publish dry-run parity without broadening product claims. Keep the existing application, dependency, and optional-adapter boundaries intact; this task is about release metadata and prerequisites, not runtime behavior.

    Treat this as part of the strict preflight contract per D-12, D-14, and D-18: the release task should not attempt a full happy path if the package itself is structurally non-publishable. Use the research-backed minimal metadata set directly instead of inventing repo-local checks, and do not add any branch-time bypasses or softer fallback mode (D-15, D-19, D-20, D-21).
  </action>
  <acceptance_criteria>
    - `mix.exs` exposes the Hex package metadata required for package build/unpack validation.
    - `mix hex.build --unpack` no longer fails because of missing metadata fields.
    - No new hard dependency is added to satisfy release metadata checks.
  </acceptance_criteria>
  <verify>
    <automated>mix hex.build --unpack</automated>
  </verify>
  <done>The repo has the package metadata required for truthful release-package validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild `mix release.preflight` as a two-phase strict gate with regression coverage</name>
  <files>lib/mix/tasks/release/preflight.ex, test/mix/tasks/release_preflight_test.exs</files>
  <behavior>
    - Phase 1 groups cheap blocker checks and exits before expensive work when any blocker fails per D-10 through D-13.
    - Dirty worktree and exact tag/version mismatch are hard failures per D-16 and D-17.
    - Phase 2 runs `mix ci`, docs-contract verification, `mix hex.build --unpack`, and `mix hex.publish --dry-run --yes`, then exits once after a combined summary per D-14 and D-18.
    - The task remains a strict release gate rather than a branch-time rehearsal helper per D-15 and D-19.
  </behavior>
  <action>
    Rewrite `lib/mix/tasks/release/preflight.ex` to follow the research-backed boundary-first design exactly. Phase 1 must check the cheap blockers together: dirty worktree, exact `vX.Y.Z` tag parity against `Mix.Project.config()[:version]`, missing strict prerequisites, and wrong environment assumptions (D-11 through D-17). If any Phase 1 blocker fails, print a boundary summary and exit non-zero before running expensive commands.

    Only after Phase 1 passes, Phase 2 should execute the expensive checks as subprocesses so the behavior matches CLI reality rather than assuming in-process Mix alias semantics: `mix ci`, the docs-contract command surface created in Phase 13, `mix hex.build --unpack`, and `mix hex.publish --dry-run --yes` (D-13, D-14, D-18). Capture each result, print one aggregated final summary, and exit once at the end. Add `test/mix/tasks/release_preflight_test.exs` using `Mix.Shell.Process` and a narrow command-runner seam so unit tests can assert blocker ordering, summary output, and single-final-exit behavior without hitting the real Hex publish path on every run. Default to the researched subprocess/orchestration approach without escalating routine formatting or result-struct choices (D-20, D-21).
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/release/preflight.ex` has a distinct Phase 1 blocker path and Phase 2 aggregated runner path.
    - Dirty worktree causes a failing Phase 1 result instead of only logging a warning.
    - Exact tag parity is enforced against `v#{Mix.Project.config()[:version]}`.
    - Phase 2 includes `mix ci`, docs-contract verification, `mix hex.build --unpack`, and `mix hex.publish --dry-run --yes`.
    - `test/mix/tasks/release_preflight_test.exs` proves blocker-first ordering and single-final-exit semantics.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/release_preflight_test.exs</automated>
    <automated>mix hex.build --unpack</automated>
  </verify>
  <done>`mix release.preflight` now behaves like a real release gate: strict on mutable state, exact about tag parity, and complete about publish-parity checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo state -> release gate | Git status, exact tags, and package metadata are external release facts the Mix task must not misreport. |
| Release gate -> Hex/Git subprocesses | The task shells out to authoritative tools whose failures must be surfaced truthfully and in order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | S | `lib/mix/tasks/release/preflight.ex` tag/version parity | mitigate | Enforce exact `git describe --tags --exact-match` parity with `Mix.Project.config()[:version]` so a mismatched ref cannot masquerade as a releasable build. |
| T-13-05 | T | dirty worktree and release prerequisites | mitigate | Fail Phase 1 on dirty status, missing prerequisites, and wrong environment assumptions before any publish-parity commands can run. |
| T-13-06 | R | Phase 2 summary output | mitigate | Use subprocess-backed result capture plus regression tests so the preflight summary reflects every expensive check and exits only once after reporting them. |
</threat_model>

<verification>
- Run `mix test test/mix/tasks/release_preflight_test.exs`.
- Run `mix hex.build --unpack`.
- In a later clean tagged proof environment, run `mix release.preflight` end to end after Phase 3's proof helper is available.
</verification>

<success_criteria>
1. `mix release.preflight` fails dirty or mismatched release state before any expensive release work begins.
2. Package-build and publish dry-run parity are part of the real preflight command rather than future intent.
3. Regression tests pin the two-phase ordering and aggregated final-exit behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/13-docs-and-release-preflight-closure/13-02-SUMMARY.md`
</output>
