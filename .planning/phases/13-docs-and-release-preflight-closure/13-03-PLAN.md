---
phase: "13"
plan: "03"
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - lib/mix/tasks/docs.contract.ex
  - lib/mix/tasks/verify.ex
  - lib/mix/tasks/release/preflight.ex
  - test/mix/tasks/docs_contract_task_test.exs
  - test/mix/tasks/verify_test.exs
  - test/mix/tasks/release_preflight_test.exs
  - scripts/release_preflight_proof.exs
  - test/scripts/release_preflight_proof_test.exs
autonomous: true
requirements:
  - QUAL-02
  - QUAL-04
must_haves:
  truths:
    - "`mix docs.contract` is a named rerunnable docs gate used consistently by automation"
    - "`mix verify` continues to execute the canonical docs-contract command surface rather than a divergent one-off script path"
    - "Strict preflight happy-path proof can be exercised from an isolated clean tagged ref instead of the active dirty workspace"
    - "Docs-contract and release-preflight evidence can be rerun intentionally as milestone gates"
  artifacts:
    - path: lib/mix/tasks/docs.contract.ex
      provides: "Canonical Mix task for the docs-contract gate"
    - path: lib/mix/tasks/verify.ex
      provides: "Verification lane wiring to the canonical docs-contract gate"
    - path: lib/mix/tasks/release/preflight.ex
      provides: "Release gate wiring to the canonical docs-contract gate"
    - path: test/mix/tasks/docs_contract_task_test.exs
      provides: "Regression coverage for the named docs-contract task surface"
    - path: scripts/release_preflight_proof.exs
      provides: "Isolated clean-tagged proof helper for strict release preflight"
    - path: test/scripts/release_preflight_proof_test.exs
      provides: "Safety coverage for proof-helper argument and isolation behavior"
  key_links:
    - from: lib/mix/tasks/verify.ex
      to: lib/mix/tasks/docs.contract.ex
      via: "deterministic verification lane step"
      pattern: "Docs Contract"
    - from: lib/mix/tasks/release/preflight.ex
      to: lib/mix/tasks/docs.contract.ex
      via: "phase-2 docs contract release check"
      pattern: "docs\\.contract"
    - from: scripts/release_preflight_proof.exs
      to: "lib/mix/tasks/release/preflight.ex"
      via: "isolated worktree execution of `mix release.preflight`"
      pattern: "release.preflight"
    - from: scripts/release_preflight_proof.exs
      to: test/scripts/release_preflight_proof_test.exs
      via: "proof-helper safety regression coverage"
      pattern: "worktree|ref|cleanup"
---

<objective>
Add canonical proof surfaces so docs-contract and strict release-preflight checks can be rerun deliberately as evidence-backed milestone gates instead of remaining ad hoc local flows.

Purpose: The phase goal is not just to implement checks, but to make them repeatable proof surfaces even when the active workspace is dirty and untagged.
Output: A named `mix docs.contract` task, `mix verify` wiring to that task, and an isolated release-proof helper with regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-docs-and-release-preflight-closure/13-CONTEXT.md
@.planning/phases/13-docs-and-release-preflight-closure/13-RESEARCH.md
@.planning/phases/13-docs-and-release-preflight-closure/13-VALIDATION.md
@.planning/phases/12-verification-chain-closure/12-02-PLAN.md
@scripts/verify_docs.exs
@lib/mix/tasks/verify.ex
@lib/mix/tasks/release/preflight.ex
@test/mix/tasks/verify_test.exs

<interfaces>
From `lib/mix/tasks/verify.ex`:
```elixir
{"DETERMINISTIC (CORE)",
 [
   {"CI", fn -> Mix.Task.run("ci") end},
   {"Docs Contract", fn -> Mix.Task.run("run", ["scripts/verify_docs.exs"]) end}
 ]}
```

From `13-VALIDATION.md` Wave 0:
```markdown
- A minimal command-runner seam or fixture helper so preflight tests do not invoke the real Hex publish path during unit runs.
- Clean/tagged proof instructions or helper workflow for exercising the strict happy path outside the active dirty workspace.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote docs-contract verification to a canonical Mix task and wire both `mix verify` and `mix release.preflight` to it</name>
  <files>lib/mix/tasks/docs.contract.ex, lib/mix/tasks/verify.ex, lib/mix/tasks/release/preflight.ex, test/mix/tasks/docs_contract_task_test.exs, test/mix/tasks/verify_test.exs, test/mix/tasks/release_preflight_test.exs</files>
  <behavior>
    - Docs-contract verification is invokable through a stable named command rather than only via a raw script path.
    - `mix verify` uses the canonical docs gate so deterministic verification and standalone docs proof cannot drift apart.
    - `mix release.preflight` uses the same canonical docs gate in Phase 2 so release proof and verification proof stay aligned.
    - Existing lane-ordering and shutdown-boundary guarantees from Phase 12 remain intact.
  </behavior>
  <action>
    Introduce `lib/mix/tasks/docs.contract.ex` as the canonical command surface for the docs gate created in Plan 13-01. The task should delegate to the explicit docs lanes already implemented rather than duplicating verification logic, and its output should remain truthful about which docs lanes ran and what failed. Rewire the `Docs Contract` step in `lib/mix/tasks/verify.ex` to invoke this named task instead of hard-coding `mix run scripts/verify_docs.exs`, so reruns in CI, local verification, and milestone evidence all use the same contract surface.

    Update `lib/mix/tasks/release/preflight.ex` so its Phase 2 docs check also calls the same named task rather than a divergent script invocation. Add `test/mix/tasks/docs_contract_task_test.exs` to pin the new command surface, and update `test/mix/tasks/verify_test.exs` plus `test/mix/tasks/release_preflight_test.exs` only as needed to assert that both command paths now flow through `mix docs.contract` while preserving the summary-before-exit semantics already established in earlier phases. Keep the implementation narrow and recommendation-first: no new docs verification engine, no branching fallback mode, and no weakening of the deterministic/advisory split (D-02, D-05, D-20, D-21).
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/docs.contract.ex` exists and runs the canonical docs-contract verification.
    - `lib/mix/tasks/verify.ex` no longer hard-codes `Mix.Task.run("run", ["scripts/verify_docs.exs"])` for the docs step.
    - `lib/mix/tasks/release/preflight.ex` uses `mix docs.contract` as its Phase 2 docs check surface.
    - `test/mix/tasks/docs_contract_task_test.exs` proves the named docs task runs the expected contract surface.
    - `test/mix/tasks/verify_test.exs` and `test/mix/tasks/release_preflight_test.exs` still prove summary output and final-exit ordering after the docs step is rewired.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/docs_contract_task_test.exs test/mix/tasks/verify_test.exs test/mix/tasks/release_preflight_test.exs</automated>
    <automated>mix docs.contract</automated>
  </verify>
  <done>Docs-contract verification is a stable named gate that `mix verify`, `mix release.preflight`, and maintainers can rerun without divergent code paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an isolated clean-tagged proof helper for strict release-preflight evidence</name>
  <files>scripts/release_preflight_proof.exs, test/scripts/release_preflight_proof_test.exs</files>
  <behavior>
    - Strict preflight happy-path proof runs from an isolated clean tagged ref or equivalent worktree per D-18.
    - The helper refuses to treat the active dirty workspace as happy-path evidence.
    - Local automated verification only proves the helper's safety/refusal behavior from the current workspace; the true happy-path run remains a tagged-ref verification step.
    - Proof automation is explicit and reusable rather than embedded in manual tribal knowledge.
  </behavior>
  <action>
    Create `scripts/release_preflight_proof.exs` as the proof helper called for by `13-VALIDATION.md` and the research warning about dirty, untagged local state. The helper should orchestrate an isolated proof run for `mix release.preflight` from a clean tagged ref or a disposable worktree rooted at an exact `vX.Y.Z` ref matching the package version. It must not weaken the strict gate by auto-accepting an untagged branch or dirty working tree; instead it should require explicit proof inputs, run the preflight command in the isolated location, surface the final summary, and clean up after itself unless the operator asks to keep the worktree for inspection. From the current repo state, local automation should validate only the helper's refusal path and isolation logic; the true happy-path invocation is a later verification step that requires a real exact tag or CI/release ref.

    Add `test/scripts/release_preflight_proof_test.exs` to pin the safety envelope: argument validation, refusal of ambiguous refs, isolation/cleanup behavior, and a dry-run mode or injected command-runner seam so tests stay local and do not create real release worktrees unless explicitly requested. Use the research-backed isolated-worktree approach directly and avoid any separate softened branch-time preflight mode (D-18 through D-21).
  </action>
  <acceptance_criteria>
    - `scripts/release_preflight_proof.exs` requires an explicit tagged ref or equivalent isolated-proof target.
    - The helper never treats the active dirty workspace as happy-path release evidence.
    - `test/scripts/release_preflight_proof_test.exs` covers argument validation and cleanup/isolation behavior.
    - Local verification covers refusal and isolation behavior without assuming a repo tag already exists.
    - Maintainers can rerun strict preflight proof through one documented helper command instead of reconstructing the worktree process manually.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scripts/release_preflight_proof_test.exs</automated>
    <automated>mix run scripts/release_preflight_proof.exs --dry-run --ref not-a-real-tag --worktree tmp/release-preflight-proof</automated>
  </verify>
  <done>The repo has an explicit, reusable path for evidence-backed strict preflight proof from release-like state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification commands -> maintainers | Maintainers rely on named commands as milestone evidence and need them to correspond to the actual contract surfaces. |
| Proof helper -> git worktree/release ref | The helper manipulates isolated repo state and must not blur mutable branch state with strict release evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | R | `mix docs.contract` / `mix verify` docs step | mitigate | Route both standalone docs proof and verification-lane docs proof through the same named Mix task and pin the link with regression tests. |
| T-13-08 | T | `scripts/release_preflight_proof.exs` | mitigate | Require explicit tagged-ref isolation, refuse dirty in-place happy-path proof, and test cleanup/argument validation so proof state cannot be quietly mutated or misrepresented. |
</threat_model>

<verification>
- Run `mix test test/mix/tasks/docs_contract_task_test.exs test/mix/tasks/verify_test.exs test/mix/tasks/release_preflight_test.exs test/scripts/release_preflight_proof_test.exs`.
- Run `mix docs.contract`.
- Run `mix run scripts/release_preflight_proof.exs --dry-run --ref not-a-real-tag --worktree tmp/release-preflight-proof` to confirm the helper refuses non-release refs from the active workspace without mutating the tree.
- During phase verification, run the same helper against a real exact `vX.Y.Z` ref or CI/release ref to capture strict happy-path proof.
</verification>

<success_criteria>
1. Docs-contract verification is rerunnable through a stable named command used consistently by automation.
2. Strict release-preflight happy-path proof has an explicit isolated-worktree helper instead of depending on the current dirty branch state.
3. Phase 13 leaves both `QUAL-02` and `QUAL-04` with reusable evidence gates, not just one-off fixes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-docs-and-release-preflight-closure/13-03-SUMMARY.md`
</output>
