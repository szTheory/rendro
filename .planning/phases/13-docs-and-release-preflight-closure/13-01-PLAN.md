---
phase: "13"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/integrations.md
  - scripts/verify_docs.exs
  - test/docs_contract/readme_doctest_test.exs
  - test/docs_contract/integrations_contract_test.exs
  - test/docs_contract/integrations_claims_test.exs
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - "README quickstart examples fail truthfully when the public contract drifts"
    - "Only curated guide happy-path snippets remain in the executable docs contract"
    - "Guide semantic claims are enforced by direct tests instead of compile-only heuristics"
    - "Docs verification no longer treats `...` or `%{...}` as an implicit pass"
  artifacts:
    - path: README.md
      provides: "Executable README contract surface with truthful snippet classification"
    - path: guides/integrations.md
      provides: "Curated integration-guide contract surface with explicit schematic markers"
    - path: scripts/verify_docs.exs
      provides: "Explicit multi-lane docs verifier entrypoint with no heuristic skip path"
    - path: test/docs_contract/readme_doctest_test.exs
      provides: "README doctest lane"
    - path: test/docs_contract/integrations_contract_test.exs
      provides: "Curated integration-guide compile/eval lane"
    - path: test/docs_contract/integrations_claims_test.exs
      provides: "Guide semantic-claim regression coverage"
  key_links:
    - from: README.md
      to: test/docs_contract/readme_doctest_test.exs
      via: "markdown doctest coverage"
      pattern: "doctest_file\\s+\"README.md\""
    - from: guides/integrations.md
      to: test/docs_contract/integrations_contract_test.exs
      via: "curated happy-path snippet execution"
      pattern: "guides/integrations.md"
    - from: guides/integrations.md
      to: test/docs_contract/integrations_claims_test.exs
      via: "semantic claims pinned by live adapter tests"
      pattern: "never raises|known limitation|optional-dependency"
    - from: scripts/verify_docs.exs
      to: README.md
      via: "docs verification runner"
      pattern: "README.md"
---

<objective>
Close the docs-contract gap by replacing the current heuristic README-only checker with an explicit multi-lane contract covering README quickstarts, curated guide happy paths, and direct semantic-claim tests.

Purpose: `QUAL-02` is still partial because docs verification silently blesses placeholder snippets and ignores guide semantics that public users rely on.
Output: Curated docs sources, a no-heuristics docs verifier, and focused ExUnit coverage for executable docs and claim-heavy guide behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/13-docs-and-release-preflight-closure/13-CONTEXT.md
@.planning/phases/13-docs-and-release-preflight-closure/13-RESEARCH.md
@.planning/phases/13-docs-and-release-preflight-closure/13-VALIDATION.md
@README.md
@guides/integrations.md
@scripts/verify_docs.exs

<interfaces>
From `scripts/verify_docs.exs`:
```elixir
readme = File.read!("README.md")
code_blocks = Regex.scan(~r/```elixir\n(.*?)```/s, readme)

Enum.each(code_blocks, fn [_, code] ->
  try do
    Code.compile_string(code)
  rescue
    e ->
      if code =~ "..." or code =~ "%{...}" do
        Mix.shell().info("  - Code block (partial) skipped: OK")
      else
        Mix.shell().error("  - Code block failed to compile:\n#{code}\n#{inspect(e)}")
        System.halt(1)
      end
  end
end)
```

From `lib/mix/tasks/verify.ex`:
```elixir
{"Docs Contract", fn -> Mix.Task.run("run", ["scripts/verify_docs.exs"]) end}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curate the executable docs surface and remove schematic Elixir from verified fences</name>
  <files>README.md, guides/integrations.md</files>
  <behavior>
    - README examples remain part of the executable docs contract per D-02.
    - README fenced `elixir` quickstarts are either rewritten into `iex>` doctest form when output matters or kept in a dedicated README compile/eval lane in the docs verifier.
    - Only selected happy-path blocks in `guides/integrations.md` stay executable per D-03.
    - Semantic or schematic examples are reclassified out of verified `elixir` fences per D-04 and D-08.
    - No verified fence relies on `...` or `%{...}` placeholders per D-05 and D-09.
  </behavior>
  <action>
    Edit `README.md` and `guides/integrations.md` to make the contract surface explicit and truthful per D-01 through D-09. Keep `README.md` in-contract end to end (D-02), but remove schematic placeholders from verified Elixir fences by rewriting those examples into valid Elixir or reclassifying them to `text`, `elixir-schematic`, or an explicit skip marker with a reason (D-08, D-09).

    In `guides/integrations.md`, keep only the selected happy-path examples in executable `elixir` fences (D-03). Any guide block whose value is explanatory rather than contract-worthy should be reclassified as narrative or schematic instead of remaining in the verified lane. Preserve public truthfulness: when the guide states semantic behavior such as "never raises", optional-dependency behavior, or known limitations, leave those as prose claims for Task 2 to pin with direct ExUnit coverage rather than pretending compile success is sufficient (D-04). For `README.md`, explicitly choose one lane per example: convert it to doctest form if asserted output matters, otherwise keep it in the dedicated README compile/eval lane that Task 2 will implement. Use research-backed curation choices directly without escalating routine naming or lane-label decisions (D-20, D-21).
  </action>
  <acceptance_criteria>
    - `README.md` contains no verified `elixir` fence that still depends on `...` or `%{...}` placeholders.
    - Every README quickstart example is explicitly assigned to either the doctest lane or the README compile/eval lane.
    - `guides/integrations.md` clearly distinguishes executable happy-path fences from schematic or narrative examples.
    - Any remaining schematic snippet is explicitly marked outside the verified Elixir lane.
    - Public guide claims remain narrow and truthful rather than broadened to fit the verifier.
  </acceptance_criteria>
  <verify>
    <automated>elixir -e 'paths = [\"README.md\", \"guides/integrations.md\"]; regex = ~r/```elixir\\n(.*?)```/ms; bad = for path <- paths, [_, code] <- Regex.scan(regex, File.read!(path)), String.contains?(code, \"...\") or String.contains?(code, \"%{...}\"), do: path; if bad == [], do: :ok, else: (IO.puts(:stderr, \"placeholder content remains in verified elixir fences: #{Enum.join(Enum.uniq(bad), \", \")}\"); System.halt(1))'</automated>
  </verify>
  <done>The docs source files expose a curated executable contract instead of relying on silent placeholder skips.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit docs-verification lanes and semantic-claim regression coverage</name>
  <files>scripts/verify_docs.exs, test/docs_contract/readme_doctest_test.exs, test/docs_contract/integrations_contract_test.exs, test/docs_contract/integrations_claims_test.exs</files>
  <behavior>
    - `iex>` examples execute in an explicit doctest lane where output or error shape matters per D-06.
    - Verified fenced `elixir` blocks compile or evaluate cleanly with no heuristic skip path per D-05, D-07, and D-09.
    - Guide semantic claims are asserted against live behavior rather than regex or prose matching per D-04.
  </behavior>
  <action>
    Replace the current README-only regex-and-rescue behavior in `scripts/verify_docs.exs` with an explicit multi-lane docs verifier per D-05 through D-09. The script should fail when a verified fence contains schematic placeholders instead of silently treating them as success (D-09). Use the standard ExUnit markdown doctest path for README `iex>` examples where output or error shape matters (D-06), and add dedicated compile/eval coverage for README fenced `elixir` quickstarts plus the selected guide happy-path examples that remain in verified `elixir` fences (D-02, D-03, D-07).

    Add `test/docs_contract/readme_doctest_test.exs`, `test/docs_contract/integrations_contract_test.exs`, and `test/docs_contract/integrations_claims_test.exs` as the Wave 0 coverage called for in `13-VALIDATION.md`. `integrations_claims_test.exs` must pin the guide-level semantics called out in research: optional-dependency boundaries, documented failure tuples, and any currently documented limitation that must remain truthful or be rewritten. Keep the docs verifier as the CLI entrypoint for now, but make its output explicit about which lane is running and why a block is included or excluded. Follow the repo's truthful-small-contract posture and default to the research-backed lane split without escalating routine implementation details (D-20, D-21).
  </action>
  <acceptance_criteria>
    - `scripts/verify_docs.exs` no longer contains an implicit success branch for `...` or `%{...}`.
    - `test/docs_contract/readme_doctest_test.exs` exists and doctests `README.md`.
    - README fenced `elixir` quickstarts are covered by an explicit compile/eval lane if they are not converted to doctest form.
    - `test/docs_contract/integrations_contract_test.exs` exists and executes curated guide happy paths.
    - `test/docs_contract/integrations_claims_test.exs` exists and covers semantic claims compile-only checks cannot prove.
    - `mix run scripts/verify_docs.exs` fails when a verified docs block is schematic or drifting.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/readme_doctest_test.exs test/docs_contract/integrations_contract_test.exs test/docs_contract/integrations_claims_test.exs</automated>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>`QUAL-02` is backed by explicit docs lanes and direct regression coverage rather than compile-only heuristics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> maintainers/users | README and guide examples are treated as contracts and can mislead users if schematic snippets pass as executable proof. |
| Docs verifier -> repo docs | The docs verifier decides which snippets are considered trusted executable surface versus narrative text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | R | `scripts/verify_docs.exs` | mitigate | Remove silent placeholder success and emit explicit per-lane failures so docs coverage cannot over-report what was actually checked. |
| T-13-02 | T | `README.md`, `guides/integrations.md` | mitigate | Reclassify schematic examples out of verified `elixir` fences and keep only curated contract-worthy snippets executable. |
| T-13-03 | I | `guides/integrations.md` semantic claims | mitigate | Pin claim-heavy behavior with direct ExUnit regressions so public docs cannot imply unsupported adapter/runtime guarantees. |
</threat_model>

<verification>
- Run `mix test test/docs_contract/readme_doctest_test.exs test/docs_contract/integrations_contract_test.exs test/docs_contract/integrations_claims_test.exs`.
- Run `mix run scripts/verify_docs.exs`.
- Inspect `README.md` and `guides/integrations.md` to confirm executable versus schematic docs lanes are explicit.
</verification>

<success_criteria>
1. README quickstart examples are executable proof rather than partial placeholders.
2. `guides/integrations.md` exposes only a curated executable docs surface, with semantic claims pinned by tests.
3. Docs verification fails loudly on unsupported or drifting public claims instead of silently skipping them.
</success_criteria>

<output>
After completion, create `.planning/phases/13-docs-and-release-preflight-closure/13-01-SUMMARY.md`
</output>
