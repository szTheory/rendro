---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [mix.exs, LICENSE]
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - Hex package metadata declares the MIT license.
    - Hex package metadata includes homepage URL, correct source links, and description.
    - Standard MIT LICENSE text is present at the repository root.
    - Building the hex package unpacks the LICENSE file.
  artifacts:
    - path: "mix.exs"
      provides: "Package and project configuration"
      contains: "licenses: [\"MIT\"]"
    - path: "LICENSE"
      provides: "Standard open source license"
  key_links:
    - from: "mix.exs"
      to: "LICENSE"
      via: "package files array"
      pattern: "files: ~w(.*LICENSE.*)"
---

<objective>
Finalize the project's open-source license and ensure Hex package metadata accurately reflects the project.

Purpose: To allow public distribution of the package on Hex.pm with clear usage rights and correct metadata.
Output: Updated `mix.exs` and a standard MIT `LICENSE` file at the root.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MIT LICENSE file</name>
  <files>LICENSE</files>
  <action>
    Create a standard MIT LICENSE file at the repository root. Reference the recommended structure pattern from 31-RESEARCH.md for the LICENSE file.
    Use the standard MIT license text. The copyright year should be the current year, and the copyright holder should be "szTheory".
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE</automated>
  </verify>
  <done>Standard MIT LICENSE file exists at project root.</done>
</task>

<task type="auto">
  <name>Task 2: Update Hex metadata in mix.exs</name>
  <files>mix.exs</files>
  <action>
    Update `mix.exs` to prepare for Hex publication. Reference analog patterns from 31-PATTERNS.md.
    1. In the `project/0` function list, add `homepage_url: @source_url` under `source_url: @source_url`.
    2. Verify the maintainer-facing release copy in `:description` (or add it if missing/inadequate) to clearly state the project's purpose.
    3. In the `package/0` function list, change `licenses: ["UNLICENSED"]` to `licenses: ["MIT"]`.
    4. In the `package/0` function list, explicitly ensure the `:links` property is correct (e.g., `%{"GitHub" => @source_url}`).
    5. In the `package/0` function list, add `LICENSE` to the `files:` word list. Ensure the formatting remains consistent.
  </action>
  <verify>
    <automated>mix test && mix hex.build --unpack && ls rendro-*/LICENSE > /dev/null && rm -rf rendro-*/</automated>
  </verify>
  <done>mix.exs declares MIT license, contains description, homepage_url, links, includes LICENSE in packaged files, and hex.build correctly includes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build System | Hex packaging relies on mix.exs for correct metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | mix.exs / LICENSE | accept | Changes to license require repo access, standard PR process mitigates unauthorized license changes |
</threat_model>

<verification>
Run `mix hex.build --unpack` and inspect the extracted folder to ensure `LICENSE` is present and metadata reflects the MIT license. Run `mix test` to ensure `mix.exs` changes did not introduce syntax errors.
</verification>

<success_criteria>
- `LICENSE` file is at the repository root with MIT License text.
- `mix.exs` `package.licenses` is `["MIT"]`.
- `mix.exs` `package.files` contains `LICENSE`.
- `mix.exs` `project` contains `homepage_url` and valid `:description`.
- `mix.exs` `package` contains valid `:links`.
- `mix test` passes without errors.
- `mix hex.build --unpack` successfully builds the package and includes `LICENSE`.
</success_criteria>

<output>
After completion, create `.planning/phases/31-licensing-and-hex-metadata/31-01-SUMMARY.md`
</output>