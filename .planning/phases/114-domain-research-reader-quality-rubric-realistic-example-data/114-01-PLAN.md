---
phase: 114
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/comparison/fixtures/invoice_data.json
  - priv/examples/invoice/acme-phoenix-saas/invoice.json
  - bench/comparison/run.exs
  - bench/comparison/fixtures/invoice_rendro.exs
  - bench/comparison/fixtures/invoice_typst.typ
  - bench/results/comparison.json
autonomous: true
requirements: [EXL-04]

must_haves:
  truths:
    - "The single realistic invoice fixture used by the bench-comparison harness is de-quarantined into the shared priv/examples/ library; nothing about its bytes or the harness's recorded evidence changes as a result of the move."
    - "mix rendro.comparison.check stays green after the move, and a fresh render through the repointed bench script is byte-identical (sha256-equal) to the PDF sha256 already recorded in bench/results/raw/rendro.json."
  artifacts:
    - "priv/examples/invoice/acme-phoenix-saas/invoice.json (new — moved verbatim from bench/comparison/fixtures/invoice_data.json)"
  key_links:
    - "bench/comparison/run.exs's @fixture_path attribute -> priv/examples/invoice/acme-phoenix-saas/invoice.json"
    - "bench/comparison/fixtures/invoice_rendro.exs's literal path -> priv/examples/invoice/acme-phoenix-saas/invoice.json"
    - "bench/comparison/run.exs's typst_cli --input data-path= -> absolute path to the new location"
---

<objective>
De-quarantine the single realistic invoice fixture from `bench/comparison/fixtures/invoice_data.json` into the shared `priv/examples/` library as a **provable no-op**: a byte-identical file move plus a repoint of every hardcoded consumer path, verified both by a direct hash diff of the moved file and by a fresh Rendro render matching the already-recorded benchmark PDF hash. Money-string normalization and the S4 brand/logo slot are explicitly **out of scope** for this plan (Plan 114-03's separate commit, per RESEARCH.md Pitfall 6) — this plan must not change a single byte of fixture content.

Purpose: EXL-04 requires the move to be provably behavior-preserving, not just "`mix rendro.comparison.check` happens to stay green" (that command does not re-read the fixture at all — see Pitfall 2). This plan supplies the missing explicit proof.
Output: `priv/examples/invoice/acme-phoenix-saas/invoice.json` (byte-identical to the prior `bench/comparison/fixtures/invoice_data.json`), with all three hardcoded path references repointed.
</objective>

## Artifacts This Phase Produces

(Full phase-114 manifest — every plan in this phase creates a subset; listed here so any single plan has the whole picture.)

- `lib/rendro/examples.ex` — `Rendro.Examples` module (`@moduledoc false`), functions `load!/1`, `list/1` (Plan 114-04)
- `priv/examples/invoice/acme-phoenix-saas/invoice.json` — de-quarantined + later normalized realistic Invoice fixture (this plan moves it verbatim; Plan 114-03 normalizes money/brand)
- `priv/examples/invoice/DOMAIN.md` — Invoice domain-research doc (Plan 114-05)
- `priv/schemas/examples.schema.json` — repo-only JSON Schema for example fixtures (Plan 114-02)
- `priv/schemas/rubric_scores.schema.json` — repo-only JSON Schema for the rubric manifest (Plan 114-02)
- `priv/quality/rubric_scores.json` — repo-only appendable rubric manifest: dimensions/gates/thresholds + empty `scores` array (Plan 114-06)
- `test/rendro/examples_test.exs` — loader behavior + wildcard extension-ban test (Plan 114-04)
- `test/docs_contract/examples_schema_contract_test.exs` — fixture schema validation + tarball inclusion/text-only test (Plans 114-03, 114-07)
- `test/docs_contract/rubric_manifest_contract_test.exs` — rubric manifest schema + threshold-arithmetic test (Plan 114-06)
- `test/docs_contract/domain_md_contract_test.exs` — DOMAIN.md structural headings test (Plan 114-05)
- `test/docs_contract/public_api_contract_test.exs` (modified) — adds `Rendro.Examples` to the hidden-modules list (Plan 114-04)
- `test/docs_contract/branding_claims_test.exs` (modified) — adds repo-only exclusion assertions for the new schemas/manifest (Plan 114-07)
- `mix.exs` (modified) — `package/0` `:files` gains `priv/examples` (Plan 114-07)
- `.gitignore` (modified) — raster/binary extension-ban block for `priv/examples/**` (Plan 114-07)
- `bench/comparison/run.exs`, `bench/comparison/fixtures/invoice_rendro.exs`, `bench/comparison/fixtures/invoice_typst.typ`, `bench/comparison/fixtures/invoice_pdf_generator.html.eex`, `bench/comparison/fixtures/invoice_chromic_pdf.html.eex`, `bench/results/comparison.json` (modified) — repointed to, then adapted for, the de-quarantined fixture (this plan + Plan 114-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-RESEARCH.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verbatim de-quarantine move (byte-identical proof)</name>
  <files>bench/comparison/fixtures/invoice_data.json, priv/examples/invoice/acme-phoenix-saas/invoice.json</files>
  <read_first>
    bench/comparison/fixtures/invoice_data.json (full file, 95 lines — the exact content that must survive the move byte-for-byte);
    114-RESEARCH.md lines 276-281 (Pitfall 2 — why `mix rendro.comparison.check` alone cannot prove this);
    114-RESEARCH.md lines 129-153 (Recommended Project Structure — confirms the exact destination path `priv/examples/invoice/acme-phoenix-saas/invoice.json`, matching the `Rendro.Examples.load!("invoice/acme-phoenix-saas/invoice.json")` example in the Code Examples section).
  </read_first>
  <action>
    Capture the sha256 of the currently-committed blob at the old path via `git show HEAD:bench/comparison/fixtures/invoice_data.json | shasum -a 256`. Create the destination directory `priv/examples/invoice/acme-phoenix-saas/`. Run `git mv bench/comparison/fixtures/invoice_data.json priv/examples/invoice/acme-phoenix-saas/invoice.json` — a pure rename with zero content edits. Do not touch any bytes of the file in this task; money-string normalization and the S4 brand/logo slot are Plan 114-03's separate, later commit (per Pitfall 6 — conflating the move with the normalization would make the no-op unprovable).
  </action>
  <verify>
    <automated>test "$(git show HEAD:bench/comparison/fixtures/invoice_data.json | shasum -a 256 | awk '{print $1}')" = "$(shasum -a 256 priv/examples/invoice/acme-phoenix-saas/invoice.json | awk '{print $1}')" && echo BYTE_IDENTICAL</automated>
  </verify>
  <done>The destination file exists with a sha256 hash byte-identical to the last-committed blob at the old path; `git status` reports a rename, not an add+delete.</done>
  <acceptance_criteria>
    - `priv/examples/invoice/acme-phoenix-saas/invoice.json` exists and is readable JSON.
    - The sha256 comparison above prints `BYTE_IDENTICAL`.
    - `bench/comparison/fixtures/invoice_data.json` no longer exists on disk.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repoint the three hardcoded consumer paths + prove Rendro's render stays byte-identical</name>
  <files>bench/comparison/run.exs, bench/comparison/fixtures/invoice_rendro.exs, bench/comparison/fixtures/invoice_typst.typ, bench/results/comparison.json</files>
  <read_first>
    bench/comparison/run.exs lines 1-14 (`@fixture_path` module attribute) and lines 146-167 (`run_comparator("typst_cli", ...)`, the `--input data-path=` invocation) and line 339 (`scenario.fixture` manifest string, which already reads from `@fixture_path` — no separate edit needed there);
    bench/comparison/fixtures/invoice_rendro.exs (full, 28 lines — the literal path string on line 2);
    bench/comparison/fixtures/invoice_typst.typ (full, 25 lines — the `default:` fallback string on line 2);
    114-RESEARCH.md lines 276-281 (Pitfall 2, full detail on all three references and why the Typst default resolves against the compiling file's own directory, which is no longer where the fixture lives).
  </read_first>
  <action>
    In `bench/comparison/run.exs`, change the `@fixture_path` module attribute (currently `"bench/comparison/fixtures/invoice_data.json"`) to `"priv/examples/invoice/acme-phoenix-saas/invoice.json"`. In the `run_comparator("typst_cli", ...)` clause, change the hardcoded `"data-path=invoice_data.json"` CLI arg to `"data-path=#{Path.expand(@fixture_path)}"` — passing an absolute path removes any ambiguity about which directory Typst resolves the relative default against, since the fixture no longer lives next to `invoice_typst.typ`. In `bench/comparison/fixtures/invoice_rendro.exs`, change the literal `"bench/comparison/fixtures/invoice_data.json"` string (line 2) to `"priv/examples/invoice/acme-phoenix-saas/invoice.json"`. In `bench/comparison/fixtures/invoice_typst.typ`, update the `default:` fallback string inside `sys.inputs.at("data-path", default: "invoice_data.json")` (line 2) to the same new path, for fallback-accuracy only — `run.exs` always supplies `--input data-path=` explicitly so this default is never actually used by the CI-adjacent path. Optionally update `bench/results/comparison.json`'s `scenario.fixture` string value (a free-text documentation field, not a hash-bearing one) to the new path for accuracy; this has no effect on `mix rendro.comparison.check`'s hash verification.
  </action>
  <verify>
    <automated>mix rendro.comparison.check && RENDRO_BENCH_OUTPUT=/tmp/rendro_dequarantine_check.pdf mix run bench/comparison/fixtures/invoice_rendro.exs && ACTUAL=$(shasum -a 256 /tmp/rendro_dequarantine_check.pdf | awk '{print $1}') && EXPECTED=$(python3 -c "import json;print(json.load(open('bench/results/raw/rendro.json'))['output_pdf']['sha256'])") && test "$ACTUAL" = "$EXPECTED" && echo BYTE_IDENTICAL_PDF</automated>
  </verify>
  <done>`mix rendro.comparison.check` prints "Comparison benchmark evidence VERIFIED" and exits 0; re-rendering via the repointed `invoice_rendro.exs` produces a PDF whose sha256 matches the sha256 already recorded in `bench/results/raw/rendro.json`'s `output_pdf.sha256` field — proving the repoint introduced zero rendering drift.</done>
  <acceptance_criteria>
    - Both commands in the verify block succeed and print `BYTE_IDENTICAL_PDF`.
    - `grep -rn "fixtures/invoice_data.json" bench/` returns no matches (every hardcoded reference to the old path is gone).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repo-controlled shell/git operations | `git mv`, `mix rendro.comparison.check`, and `mix run` all operate on files committed to this repository; no network or attacker-controlled input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-01-01 | Tampering | `git mv` + bench-script path repoint | low | accept | The move is a pure rename verified byte-identical via sha256 diff against the last-committed git blob (Task 1); no attacker-controlled input is involved. |
| T-114-01-02 | Information Disclosure | `bench/results/comparison.json`'s `scenario.fixture` free-text string | low | accept | A documentation-only string field, never re-read or executed by `mix rendro.comparison.check`'s hash verification; updating it changes no security-relevant behavior. |
</threat_model>

<verification>
Both tasks' automated commands must pass. Full-suite confirmation: `mix rendro.comparison.check` stays green and the byte-diff assertions in Task 1 and Task 2 both succeed. No `mix test` files are created by this plan (that begins in Plans 114-03/114-04); this plan's correctness is proven entirely via the sha256 diffs and the existing `mix rendro.comparison.check` command.
</verification>

<success_criteria>
- `priv/examples/invoice/acme-phoenix-saas/invoice.json` exists, byte-identical to the pre-move `bench/comparison/fixtures/invoice_data.json`.
- All three hardcoded consumer paths (`run.exs`, `invoice_rendro.exs`, `invoice_typst.typ`) point at the new location.
- `mix rendro.comparison.check` stays green; a fresh Rendro render is sha256-identical to the recorded benchmark evidence.
</success_criteria>

<output>
Create `.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-01-SUMMARY.md` when done
</output>
