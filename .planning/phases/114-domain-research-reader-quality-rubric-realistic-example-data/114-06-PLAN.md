---
phase: 114
plan: 06
type: execute
wave: 2
depends_on: ["114-02"]
files_modified:
  - priv/quality/rubric_scores.json
  - test/docs_contract/rubric_manifest_contract_test.exs
autonomous: true
requirements: [RUB-02, RUB-03]

must_haves:
  truths:
    - "A reader-quality rubric is defined with 6 core 1-5 dimensions and 2 pass/fail gates, each with concrete 1/3/4/5 anchors a non-designer can apply."
    - "The rubric is recorded in a schema-backed, appendable manifest whose docs-contract lane enforces structure and the threshold arithmetic (hierarchy = 5, core >= 4, gates pass) — not any subjective score."
  artifacts:
    - "priv/quality/rubric_scores.json"
    - "test/docs_contract/rubric_manifest_contract_test.exs"
  key_links:
    - "priv/quality/rubric_scores.json -> priv/schemas/rubric_scores.schema.json (validated by test/docs_contract/rubric_manifest_contract_test.exs)"
    - "priv/quality/rubric_scores.json's empty scores array -> appended to by Phase 118's demo scoring (S5 seam)"
---

<objective>
Author the reader-quality rubric's content and seed manifest at `priv/quality/rubric_scores.json`: 6 core 1-5 dimensions (information architecture, content hierarchy, domain-fit/least-surprise, reader affordances, typographic craft, restraint/cohesion) each with concrete 1/3/4/5 anchor prose a non-designer can apply, 2 pass/fail gates (reading-order, print-safety), the threshold constants (hierarchy_min=5, core_min=4), and an empty appendable `scores` array (S5 — populated by Phase 118, never by this phase). Then write `test/docs_contract/rubric_manifest_contract_test.exs`, which validates the manifest's structure against Plan 114-02's schema AND separately proves the pass/fail arithmetic itself is correct using synthetic (not real) score inputs — RUB-03's explicit "structure and threshold arithmetic... not the subjective score" boundary.

Purpose: RUB-02 is genuinely research-heavy domain content (flagged MEDIUM/LOW confidence in 114-RESEARCH.md); RUB-03 requires this be schema-backed and appendable so Milestone C's quality-ratchet only ever appends. No `lib/` module is created for the arithmetic — per this phase's "no `lib/` product change except the loader" boundary, the pass/fail helper lives only inside the test file.
Output: `priv/quality/rubric_scores.json` and a new docs-contract test.
</objective>

## Artifacts This Phase Produces

(Full phase-114 manifest — every plan in this phase creates a subset; listed here so any single plan has the whole picture.)

- `lib/rendro/examples.ex` — `Rendro.Examples` module (`@moduledoc false`), functions `load!/1`, `list/1` (Plan 114-04)
- `priv/examples/invoice/acme-phoenix-saas/invoice.json` — de-quarantined + normalized realistic Invoice fixture (Plans 114-01, 114-03)
- `priv/examples/invoice/DOMAIN.md` — Invoice domain-research doc (Plan 114-05)
- `priv/schemas/examples.schema.json`, `priv/schemas/rubric_scores.schema.json` — repo-only JSON Schemas (Plan 114-02)
- `priv/quality/rubric_scores.json` — repo-only appendable rubric manifest (this plan)
- `test/rendro/examples_test.exs` — loader behavior + wildcard extension-ban test (Plan 114-04)
- `test/docs_contract/examples_schema_contract_test.exs` — fixture schema validation + tarball inclusion/text-only test (Plans 114-03, 114-07)
- `test/docs_contract/rubric_manifest_contract_test.exs` — rubric manifest schema + threshold-arithmetic test (this plan)
- `test/docs_contract/domain_md_contract_test.exs` — DOMAIN.md structural headings test (Plan 114-05)
- `test/docs_contract/public_api_contract_test.exs` (modified) — adds `Rendro.Examples` to the hidden-modules list (Plan 114-04)
- `test/docs_contract/branding_claims_test.exs` (modified) — adds repo-only exclusion assertions (Plan 114-07)
- `mix.exs` (modified) — `package/0` `:files` gains `priv/examples` (Plan 114-07)
- `.gitignore` (modified) — raster/binary extension-ban block for `priv/examples/**` (Plan 114-07)
- `bench/comparison/*` (modified) — repointed + adapted for the new money format (Plans 114-01, 114-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author priv/quality/rubric_scores.json (6 dims + 2 gates + thresholds + empty scores)</name>
  <files>priv/quality/rubric_scores.json</files>
  <read_first>
    priv/schemas/rubric_scores.schema.json (created by Plan 114-02 — the exact shape this manifest must satisfy: `schema_version`, `dimensions` array of 6, `gates` array of 2, `thresholds`, `scores`);
    114-RESEARCH.md lines 105-113 (rubric-manifest architecture diagram box);
    ROADMAP.md Phase 114 success criterion 5 and REQUIREMENTS.md RUB-02 (6 named core dimensions + 2 named gates, non-designer anchors).
  </read_first>
  <action>
    Write `priv/quality/rubric_scores.json` with `"schema_version": 1`. `"dimensions"`: 6 entries, each `{id, label, anchors: {"1","3","4","5"}}`, using exactly these ids/labels and this anchor prose — `information_architecture`/"Information architecture": 1="Related information is scattered with no discernible grouping; the reader must hunt across the whole page to assemble any one concept.", 3="Basic grouping exists (e.g. issuer vs. customer are visually separate) but placement is inconsistent or some related fields are still split across distant regions.", 4="Every field lives in one clearly bounded group matching the domain's natural structure, each in an expected location.", 5="Grouping is clear AND the zones are ordered to match the reader's natural scanning path, so the reader never backtracks to find related information."; `content_hierarchy`/"Content hierarchy": 1="Every field has the same size/weight; no single fact stands out from the rest.", 3="Some size/weight variation exists, but the one fact the reader needs first is not clearly the most prominent element on the page.", 4="The one fact the reader needs first is visually dominant — larger and/or bolder than everything else.", 5="The key fact is dominant AND every other element recedes in proportion to its importance, producing one unambiguous focal point."; `domain_fit`/"Domain-fit / least-surprise": 1="The layout could be mistaken for a different kind of document entirely; it lacks the conventional elements a reader expects for this document type.", 3="The document is recognizable as the right family but is missing one or more conventional elements a reader would expect.", 4="The document includes and positions the conventional elements a reader of this domain expects.", 5="The document is indistinguishable in structure and convention from a real-world professional document of this type."; `reader_affordances`/"Reader affordances": 1="No navigational aids exist — a wall of unlabeled text or numbers with no alignment or whitespace to guide scanning.", 3="Some labels exist, but alignment or whitespace is inconsistent.", 4="Labels, alignment, and whitespace are applied consistently throughout.", 5="Affordances actively anticipate the reader's exact scanning path."; `typographic_craft`/"Typographic craft": 1="Type sizing/weight is arbitrary or inconsistent; text is cramped, overflowing, or wrapped awkwardly.", 3="Sizing/weight is mostly consistent, but a few awkward line breaks or spacing issues remain.", 4="Sizing, weight, and spacing are consistent throughout with no awkward breaks.", 5="Every typographic choice reads as intentional — a coherent, deliberate type system with zero awkward breaks."; `restraint_cohesion`/"Restraint / cohesion": 1="The document is visually noisy or internally inconsistent across sections.", 3="The document is mostly restrained, but one or two decorative inconsistencies remain.", 4="The document is fully restrained and internally consistent.", 5="The document is restrained, consistent, AND every visual element earns its place — nothing could be removed without losing information." `"gates"`: 2 entries `{id, label, description}` — `reading_order`/"Reading order": "The rendered PDF's content reads top-to-bottom, left-to-right in the same order a human would naturally scan it, with no illogical jumps between regions. Pass/fail, no partial credit."; `print_safety`/"Print safety": "The document renders correctly at its actual print/paper size: no clipped text or table columns at page margins, and no essential content lost or orphaned across a page break. Pass/fail, no partial credit." `"thresholds"`: `{"hierarchy_dimension": "content_hierarchy", "hierarchy_min": 5, "core_min": 4}`. `"scores"`: `[]` (empty — Phase 118 appends real per-demo entries against this same schema; this phase records only the rubric definition itself).
  </action>
  <verify>
    <automated>mix run -e 'result = JSV.validate(JSON.decode!(File.read!("priv/quality/rubric_scores.json")), JSV.build!(JSON.decode!(File.read!("priv/schemas/rubric_scores.schema.json")))); IO.inspect(result)'</automated>
  </verify>
  <done>The manifest validates against the schema (prints an `{:ok, _}` tuple); exactly 6 dimensions and 2 gates are present; thresholds match RUB-03's arithmetic (`hierarchy_min: 5`, `core_min: 4`); `scores` is an empty array.</done>
  <acceptance_criteria>
    - The `mix run -e` command above prints `{:ok, ...}`, not `{:error, ...}`.
    - `grep -c '"id"' priv/quality/rubric_scores.json` is at least 8 (6 dimension ids + 2 gate ids).
    - `grep -c '"scores": \[\]' priv/quality/rubric_scores.json` is 1 (no real demo scores recorded this phase).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write test/docs_contract/rubric_manifest_contract_test.exs (structure + threshold arithmetic, not the subjective score)</name>
  <files>test/docs_contract/rubric_manifest_contract_test.exs</files>
  <read_first>
    lib/rendro/comparison.ex lines 93-108 (`static_contract_errors/1`'s accumulator style — collect errors via `add_error_unless`-style helpers, mirrored here at test-file scope only, no `lib/` module) and lines 201-237 (`collect_manifest_shape_errors/2`, the concrete accumulator pattern to imitate);
    lib/rendro/public_api/validator.ex (full, 21 lines — the `JSV.build!`/`JSV.validate` pattern for the schema-validation assertion).
  </read_first>
  <action>
    Create `test/docs_contract/rubric_manifest_contract_test.exs`, module `Rendro.DocsContract.RubricManifestContractTest`, `use ExUnit.Case, async: true`. Test 1 ("schema validation"): the checked-in `priv/quality/rubric_scores.json` validates against `priv/schemas/rubric_scores.schema.json` via `JSV.build!/1` + `JSV.validate/2`. Test 2 ("structural enumeration"): decode the manifest and assert `length(manifest["dimensions"]) == 6`, `length(manifest["gates"]) == 2`, `manifest["thresholds"]["hierarchy_min"] == 5`, and `manifest["thresholds"]["core_min"] >= 4`. Test 3 ("threshold-arithmetic correctness, not the subjective score"): define a private test-only helper `passed?(dimension_scores, gate_results)` that returns `true` only when `dimension_scores["content_hierarchy"] == 5`, every other core dimension score is `>= 4`, and every value in `gate_results` is `true` — mirroring `Comparison`'s accumulator style but scoped entirely to this test file (no `lib/` module, per this phase's "no `lib/` product change except the loader" boundary). Assert this helper returns `true` for a synthetic all-5s/all-`true` input, and returns `false` for each of three synthetic near-miss inputs: `content_hierarchy: 4` (fails hierarchy=5), one other core dimension `= 3` (fails core>=4), and one gate `= false` (fails gates-pass) — proving the arithmetic itself is correctly enforced independent of any real demo's subjective score, since `scores` is empty this phase.
  </action>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs</automated>
  </verify>
  <done>All 3 tests pass: schema validation, structural enumeration, and arithmetic correctness on synthetic (not real) inputs.</done>
  <acceptance_criteria>
    - `mix test test/docs_contract/rubric_manifest_contract_test.exs` exits 0.
    - The test file defines no new `lib/` module — the `passed?/2` helper is private to the test file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repo-controlled appendable manifest | `priv/quality/rubric_scores.json` is read/validated only by this repo's test suite; Phase 118 appends to it through the same schema gate, never at runtime by a shipped consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-06-01 | Tampering | `priv/quality/rubric_scores.json` appendable manifest | low | accept | Repo-controlled and schema-validated; not runtime-writable by any shipped consumer; Phase 118's appends go through the identical schema gate authored in Plan 114-02. |
| T-114-06-02 | Repudiation | Threshold-arithmetic logic exists only as a test helper, not `lib/` code | low | accept | Intentional per this phase's boundary ("no `lib/` product change except the loader"); no runtime code path depends on this logic, so there is no repudiation surface to mitigate beyond the test itself. |
</threat_model>

<verification>
`mix test test/docs_contract/rubric_manifest_contract_test.exs` passes all 3 tests.
</verification>

<success_criteria>
- `priv/quality/rubric_scores.json` defines exactly 6 core dimensions (with concrete 1/3/4/5 anchors) and 2 gates, validates against its schema, and starts with an empty `scores` array.
- A docs-contract test enforces both the manifest's structure and its threshold arithmetic, independent of any subjective score.
</success_criteria>

<output>
Create `.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-06-SUMMARY.md` when done
</output>
