---
phase: 114
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/schemas/examples.schema.json
  - priv/schemas/rubric_scores.schema.json
autonomous: true
requirements: [EXL-03, RUB-03]

must_haves:
  truths:
    - "A repo-only JSON Schema (draft 2020-12) exists that structurally constrains every priv/examples/ fixture, forbidding JSON-float money and requiring the S4 optional brand/logo slot shape."
    - "A repo-only JSON Schema exists that structurally constrains priv/quality/rubric_scores.json, enumerating exactly 6 core dimensions and 2 gates and fixing the RUB-03 threshold constants (hierarchy_min == 5, core_min >= 4)."
  artifacts:
    - "priv/schemas/examples.schema.json"
    - "priv/schemas/rubric_scores.schema.json"
  key_links:
    - "priv/schemas/examples.schema.json -> consumed by test/docs_contract/examples_schema_contract_test.exs (Plan 114-03)"
    - "priv/schemas/rubric_scores.schema.json -> consumed by test/docs_contract/rubric_manifest_contract_test.exs (Plan 114-06)"
---

<objective>
Author the two repo-only JSON Schemas (draft 2020-12) that back this phase's structural contracts: `priv/schemas/examples.schema.json` (EXL-03 — validates every realistic fixture, forbids JSON-float money, defines the S4 optional brand/logo slot) and `priv/schemas/rubric_scores.schema.json` (RUB-03 — validates the appendable rubric manifest, fixing the 6-dimension/2-gate shape and the hierarchy=5/core>=4 threshold constants that Plan 114-06's content must satisfy and Plan 114-06's test enforces).

Purpose: these are pure structural contracts, mirroring the repo's three existing schemas exactly (`public_api.schema.json`, `support_matrix.schema.json`, `viewer_evidence.schema.json`) — no new validation library or approach is introduced.
Output: two new schema files under `priv/schemas/` (both stay repo-only — never added to `mix.exs`'s Hex package allowlist; Plan 114-07 asserts their absence from the tarball).
</objective>

## Artifacts This Phase Produces

(Full phase-114 manifest — every plan in this phase creates a subset; listed here so any single plan has the whole picture.)

- `lib/rendro/examples.ex` — `Rendro.Examples` module (`@moduledoc false`), functions `load!/1`, `list/1` (Plan 114-04)
- `priv/examples/invoice/acme-phoenix-saas/invoice.json` — de-quarantined + normalized realistic Invoice fixture (Plans 114-01, 114-03)
- `priv/examples/invoice/DOMAIN.md` — Invoice domain-research doc (Plan 114-05)
- `priv/schemas/examples.schema.json` — repo-only JSON Schema for example fixtures (this plan)
- `priv/schemas/rubric_scores.schema.json` — repo-only JSON Schema for the rubric manifest (this plan)
- `priv/quality/rubric_scores.json` — repo-only appendable rubric manifest: dimensions/gates/thresholds + empty `scores` array (Plan 114-06)
- `test/rendro/examples_test.exs` — loader behavior + wildcard extension-ban test (Plan 114-04)
- `test/docs_contract/examples_schema_contract_test.exs` — fixture schema validation + tarball inclusion/text-only test (Plans 114-03, 114-07)
- `test/docs_contract/rubric_manifest_contract_test.exs` — rubric manifest schema + threshold-arithmetic test (Plan 114-06)
- `test/docs_contract/domain_md_contract_test.exs` — DOMAIN.md structural headings test (Plan 114-05)
- `test/docs_contract/public_api_contract_test.exs` (modified) — adds `Rendro.Examples` to the hidden-modules list (Plan 114-04)
- `test/docs_contract/branding_claims_test.exs` (modified) — adds repo-only exclusion assertions for the new schemas/manifest (Plan 114-07)
- `mix.exs` (modified) — `package/0` `:files` gains `priv/examples` (Plan 114-07)
- `.gitignore` (modified) — raster/binary extension-ban block for `priv/examples/**` (Plan 114-07)
- `bench/comparison/*` (modified) — repointed to, then adapted for, the de-quarantined fixture (Plans 114-01, 114-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-RESEARCH.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author priv/schemas/examples.schema.json</name>
  <files>priv/schemas/examples.schema.json</files>
  <read_first>
    priv/schemas/support_matrix.schema.json (full, 165 lines — the exact `$schema`/`$id`/`title`/`description` header shape and `$defs` convention to mirror);
    114-RESEARCH.md lines 304-308 (Pitfall 6 — the money-as-JSON-float trap this schema must forbid) and lines 71-98 (Architecture Patterns diagram showing the schema's place in the pipeline);
    114-PATTERNS.md lines 84-101 (money-field pitfall + header-shape analog assignment).
  </read_first>
  <action>
    Create `priv/schemas/examples.schema.json` using the identical `"$schema": "https://json-schema.org/draft/2020-12/schema"` header convention as `support_matrix.schema.json`, with `$id: "examples.schema.json"`, a `title`, and a `description` naming this as the structural contract for `priv/examples/{domain}/{business}/{family}.json` fixtures. Define a top-level object requiring `fixture_id` (non-empty string), `issuer` and `customer` (each a `$defs/party`: required `name`, optional `street`/`city`/`region`/`postal_code`/`country`, all strings), `invoice` (object: `id`, `date` and `due_date` as ISO-8601 `date`-format strings, `terms` string), and `items` (array of objects, each requiring `name`, `qty` — non-negative integer — and `price`; `description` optional string). Every money field anywhere in the schema (`items[].price`, and `totals.subtotal`/`totals.tax`/`totals.total` inside an optional `totals` object) must reference a shared `$defs/money_string` definition typed `"string"` with `"pattern": "^-?[0-9]+\\.[0-9]{2}$"` — never `"type": "number"` (the exact trap Pitfall 6 warns against; the pre-normalization de-quarantined fixture still uses integer `_cents` fields until Plan 114-03 runs, which is why this schema is authored in a separate Wave-1 plan from the fixture normalization). Add a top-level optional `brand` property (S4 seam): an object with an optional `logo` property typed `["string", "null"]`, not required at the top level and not required to be non-empty — this is the "optional empty brand/logo slot" EXL-06 calls for. Use `"additionalProperties": true` at the object level (mirroring `support_matrix.schema.json`'s permissive top-level shape) so future domains/fields can be added without a schema-breaking change.
  </action>
  <verify>
    <automated>mix run -e 'schema = "priv/schemas/examples.schema.json" |> File.read!() |> JSON.decode!() |> JSV.build!(); IO.puts("SCHEMA_OK")'</automated>
  </verify>
  <done>The schema file parses as valid JSON and builds via `JSV.build!/1` without raising; every money field references the shared string+pattern definition; `brand`/`logo` are present but optional.</done>
  <acceptance_criteria>
    - `grep -c '"type": "number"' priv/schemas/examples.schema.json` reports 0 for any field describing money (no bare numeric money type anywhere in the schema).
    - `grep -c "money_string" priv/schemas/examples.schema.json` is at least 1 (the shared `$defs` reference exists and is used).
    - The `mix run -e` smoke command above prints `SCHEMA_OK`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author priv/schemas/rubric_scores.schema.json</name>
  <files>priv/schemas/rubric_scores.schema.json</files>
  <read_first>
    priv/schemas/support_matrix.schema.json (full — header/`$defs` convention, same as Task 1);
    114-RESEARCH.md lines 105-113 (rubric-manifest architecture diagram box — "structure + threshold arithmetic only... NOT the subjective score");
    ROADMAP.md Phase 114 success criterion 5 and REQUIREMENTS.md RUB-02/RUB-03 (the exact 6 dimensions, 2 gates, and threshold wording: "hierarchy = 5, core >= 4, gates pass").
  </read_first>
  <action>
    Create `priv/schemas/rubric_scores.schema.json` with the same draft-2020-12 header convention. Top-level object requires `schema_version` (const `1`), `dimensions`, `gates`, `thresholds`, `scores`. `dimensions` is an array with `minItems`/`maxItems` both `6`, each item requiring `id` (string enum: `information_architecture`, `content_hierarchy`, `domain_fit`, `reader_affordances`, `typographic_craft`, `restraint_cohesion`), `label` (non-empty string), and `anchors` (object requiring keys `"1"`, `"3"`, `"4"`, `"5"`, each a non-empty string — the concrete non-designer-facing anchor prose Plan 114-06 authors). `gates` is an array with `minItems`/`maxItems` both `2`, each item requiring `id` (string enum: `reading_order`, `print_safety`), `label`, and `description`. `thresholds` is an object requiring `hierarchy_dimension` (const `"content_hierarchy"`), `hierarchy_min` (const `5`), and `core_min` (integer, minimum `4`, maximum `5`) — this is the RUB-03 "threshold arithmetic" the docs-contract lane enforces structurally. `scores` is an array (the RUB-03 appendable manifest; empty at the end of this phase, populated by Phase 118) whose items reference a `$defs/score_entry`: object requiring `demo_id`, `domain`, `family` (strings), `dimension_scores` (object requiring all 6 dimension ids as integer keys 1-5, `additionalProperties: false`), `gate_results` (object requiring both gate ids as boolean keys, `additionalProperties: false`), `passed` (boolean), `recorded_at` (date-format string), and an optional `stress_exempt` (boolean) — this last field is a forward-compat seam for Phase 117's EDGE-03 requirement ("stress fixtures are exempt from the rubric beauty gate... explicit in the rubric manifest"), pre-declared now so Phase 117 can populate it without a non-additive schema change.
  </action>
  <verify>
    <automated>mix run -e 'schema = "priv/schemas/rubric_scores.schema.json" |> File.read!() |> JSON.decode!() |> JSV.build!(); IO.puts("SCHEMA_OK")'</automated>
  </verify>
  <done>Schema builds via `JSV.build!/1` without error; enumerates exactly the 6 dimension ids and 2 gate ids; `thresholds.hierarchy_min` is a const `5` and `thresholds.core_min` has `minimum: 4`.</done>
  <acceptance_criteria>
    - `grep -c "information_architecture\|content_hierarchy\|domain_fit\|reader_affordances\|typographic_craft\|restraint_cohesion" priv/schemas/rubric_scores.schema.json` is at least 6.
    - `grep -c "reading_order\|print_safety" priv/schemas/rubric_scores.schema.json` is at least 2.
    - The `mix run -e` smoke command above prints `SCHEMA_OK`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `:dev`/`:test` schema-validation tooling | `JSV.build!/1` parses repo-authored, version-controlled schema files; no runtime/attacker input is ever validated by these schemas outside CI/test execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-02-01 | Tampering | `priv/schemas/*.json` parsed via `JSON.decode!`/`JSV.build!` | low | accept | Both files are repo-authored and version-controlled; they are never fed attacker-controlled input — only ever the repo's own checked-in fixtures/manifest at test/CI time. |
| T-114-02-02 | Information Disclosure | `priv/schemas/` accidentally shipping in the Hex tarball | medium | mitigate | These two files must stay absent from `mix.exs`'s `package/0` `:files` allowlist (this plan does not touch `mix.exs`); Plan 114-07 adds an explicit `refute` assertion in the tarball-exclusion test to enforce this repo-only convention, matching how `priv/public_api.json`'s sibling schemas are already excluded today. |
</threat_model>

<verification>
Both `mix run -e` smoke commands must print `SCHEMA_OK`. No fixtures or manifest exist yet to validate against these schemas in this plan — that validation happens in Plans 114-03 and 114-06, which depend on this plan's output.
</verification>

<success_criteria>
- `priv/schemas/examples.schema.json` and `priv/schemas/rubric_scores.schema.json` both exist, parse, and build via `JSV.build!/1`.
- Money fields are string+pattern, never JSON numbers.
- The rubric schema enumerates exactly 6 dimensions and 2 gates and fixes the hierarchy=5/core>=4 threshold constants.
</success_criteria>

<output>
Create `.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-02-SUMMARY.md` when done
</output>
