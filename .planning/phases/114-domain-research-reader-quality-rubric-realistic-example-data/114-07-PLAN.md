---
phase: 114
plan: 07
type: execute
wave: 3
depends_on: ["114-01", "114-02", "114-03", "114-06"]
files_modified:
  - mix.exs
  - .gitignore
  - test/docs_contract/examples_schema_contract_test.exs
  - test/docs_contract/branding_claims_test.exs
autonomous: true
requirements: [EXL-05]

must_haves:
  truths:
    - "priv/examples/ ships in the Hex tarball as text-only (.json/.md/.svg), added to the mix.exs package allowlist."
    - "priv/schemas/ and priv/quality/ (the two new schemas + the rubric manifest) stay repo-only, never shipped in the Hex tarball."
  artifacts:
    - "mix.exs (package/0 :files includes priv/examples)"
    - ".gitignore (raster/binary extension-ban block for priv/examples/**)"
  key_links:
    - "mix.exs package/0 :files -> priv/examples (shipped) vs priv/schemas, priv/quality (never shipped)"
    - "test/docs_contract/examples_schema_contract_test.exs (extended) -> Rendro.Test.HexBuildCache -> tar -xOf ... | tar -tzf - inclusion + text-only assertion"
    - "test/docs_contract/branding_claims_test.exs (extended) -> refute the two new schemas + priv/quality/ from the tarball listing"
---

<objective>
Add `priv/examples` to `mix.exs`'s `package/0` `:files` Hex allowlist (and only that — `priv/schemas`/`priv/quality` must stay absent), add a `.gitignore` raster/binary extension-ban block for `priv/examples/**` mirroring `brand/`'s convention, then prove both directions with real `mix hex.build` tarball assertions: `priv/examples/` ships and is text-only-only (extending `examples_schema_contract_test.exs`), and the two new schemas plus the rubric manifest never leak into the shipped tarball (extending `branding_claims_test.exs`).

Purpose: this is the last piece of EXL-05 — the earlier in-repo wildcard-ban (Plan 114-04) proves no raster file is ever committed; this plan proves the actual Hex package respects the same boundary, both for inclusion (fixtures ship) and exclusion (repo-only contracts don't).
Output: an updated `mix.exs`, an updated `.gitignore`, and two extended docs-contract test files.
</objective>

## Artifacts This Phase Produces

(Full phase-114 manifest — every plan in this phase creates a subset; listed here so any single plan has the whole picture.)

- `lib/rendro/examples.ex` — `Rendro.Examples` module (`@moduledoc false`), functions `load!/1`, `list/1` (Plan 114-04)
- `priv/examples/invoice/acme-phoenix-saas/invoice.json` — de-quarantined + normalized realistic Invoice fixture (Plans 114-01, 114-03)
- `priv/examples/invoice/DOMAIN.md` — Invoice domain-research doc (Plan 114-05)
- `priv/schemas/examples.schema.json`, `priv/schemas/rubric_scores.schema.json` — repo-only JSON Schemas (Plan 114-02)
- `priv/quality/rubric_scores.json` — repo-only appendable rubric manifest (Plan 114-06)
- `test/rendro/examples_test.exs` — loader behavior + wildcard extension-ban test (Plan 114-04)
- `test/docs_contract/examples_schema_contract_test.exs` — fixture schema validation (Plan 114-03) + tarball inclusion/text-only test (this plan)
- `test/docs_contract/rubric_manifest_contract_test.exs` — rubric manifest schema + threshold-arithmetic test (Plan 114-06)
- `test/docs_contract/domain_md_contract_test.exs` — DOMAIN.md structural headings test (Plan 114-05)
- `test/docs_contract/public_api_contract_test.exs` (modified) — adds `Rendro.Examples` to the hidden-modules list (Plan 114-04)
- `test/docs_contract/branding_claims_test.exs` (modified) — adds repo-only exclusion assertions (this plan)
- `mix.exs` (modified) — `package/0` `:files` gains `priv/examples` (this plan)
- `.gitignore` (modified) — raster/binary extension-ban block for `priv/examples/**` (this plan)
- `bench/comparison/*` (modified) — repointed + adapted for the new money format (Plans 114-01, 114-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-RESEARCH.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: mix.exs package allowlist + .gitignore raster-ban block</name>
  <files>mix.exs, .gitignore</files>
  <read_first>
    mix.exs lines 110-129 (`package/0`, the current `:files` list — `priv/branded` at line 117 is the insertion anchor);
    .gitignore lines 56-67 (the `brand/**/*.png` etc. extension-ban block — the exact convention to mirror for `priv/examples/**`);
    114-RESEARCH.md lines 290-295 (Pitfall 4 — `priv/examples` goes in the allowlist, `priv/schemas`/`priv/quality` never do).
  </read_first>
  <action>
    In `mix.exs`'s `package/0` `:files` list, add `"priv/examples"` as a new entry immediately after `"priv/branded"` (line 117). Do not add `"priv/schemas"` or `"priv/quality"` anywhere in this list — they must stay repo-only, matching the existing convention (neither appears in the list today). In `.gitignore`, append a new block after the existing `brand/` extension-ban block (after line 67), with a header comment analogous to it, banning the same 11 extensions under `priv/examples/**`: `priv/examples/**/*.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`, `.pdf`, `.woff`, `.woff2`, `.ttf`, `.otf`, `.eot`.
  </action>
  <verify>
    <automated>test "$(grep -c 'priv/examples' mix.exs)" -ge "1" && test "$(grep -c 'priv/schemas\|priv/quality' mix.exs)" = "0" && test "$(grep -c 'priv/examples' .gitignore)" -ge "1" && echo PACKAGING_OK</automated>
  </verify>
  <done>`mix.exs`'s `package/0` `:files` includes `priv/examples` and excludes `priv/schemas`/`priv/quality`; `.gitignore` bans the same 11 raster/binary extensions under `priv/examples/**` as `brand/`'s existing block.</done>
  <acceptance_criteria>
    - The verify command prints `PACKAGING_OK`.
    - `mix compile --warnings-as-errors` still succeeds (mix.exs is syntactically valid Elixir).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tarball inclusion + text-only assertion (extend examples_schema_contract_test.exs)</name>
  <files>test/docs_contract/examples_schema_contract_test.exs</files>
  <read_first>
    test/docs_contract/branding_claims_test.exs lines 41-55 (the "built tarball includes branded assets and NOTICE" test — the exact `Rendro.Test.HexBuildCache.get_build_output/0` + `tar -xOf ... contents.tar.gz | tar -tzf -` pipeline to mirror);
    test/support/hex_build_cache.ex (full, 44 lines — confirms the cache Agent avoids redundant `mix hex.build` calls and is already started globally in `test/test_helper.exs`);
    114-RESEARCH.md lines 333-352 (the exact inclusion + text-only assertion code shape to add).
  </read_first>
  <action>
    Add a new `describe "hex tarball contents"` block to `test/docs_contract/examples_schema_contract_test.exs` (created by Plan 114-03). Inside it, build the tarball via `Rendro.Test.HexBuildCache.get_build_output()` (reuse the cached Agent — do not call `mix hex.build` directly), list its contents via `tar -xOf #{tarball} contents.tar.gz | tar -tzf -` through `System.cmd("sh", ["-c", list_cmd], stderr_to_stdout: true)`, filter the listing to lines starting with `"priv/examples/"`, assert that filtered list is non-empty, and assert every filtered path's `Path.extname/1` is one of `[".json", ".md", ".svg"]`.
  </action>
  <verify>
    <automated>mix test test/docs_contract/examples_schema_contract_test.exs</automated>
  </verify>
  <done>The new tarball-contents test passes, proving `priv/examples/` ships in the built Hex tarball and every shipped entry is text-only.</done>
  <acceptance_criteria>
    - `mix test test/docs_contract/examples_schema_contract_test.exs` exits 0 (including the pre-existing schema-validation tests from Plan 114-03).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Repo-only exclusion assertions (extend branding_claims_test.exs)</name>
  <files>test/docs_contract/branding_claims_test.exs</files>
  <read_first>
    test/docs_contract/branding_claims_test.exs lines 57-69 (the existing "built tarball excludes operator-only priv paths" test and its current two `refute` lines).
  </read_first>
  <action>
    Add three more `refute contents =~ "..."` lines to the existing "built tarball excludes operator-only priv paths" test (lines 57-69): `refute contents =~ "priv/schemas/examples.schema.json"`, `refute contents =~ "priv/schemas/rubric_scores.schema.json"`, `refute contents =~ "priv/quality/"`. No other change to this test file.
  </action>
  <verify>
    <automated>mix test test/docs_contract/branding_claims_test.exs</automated>
  </verify>
  <done>All assertions in this test (the two pre-existing refutes plus the three new ones) pass, proving the new repo-only schemas and rubric manifest never leak into the shipped Hex tarball.</done>
  <acceptance_criteria>
    - `mix test test/docs_contract/branding_claims_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hex package build/publish boundary | `mix.exs`'s `package/0` `:files` list is the sole gate determining what a downstream Hex consumer receives; everything outside it (including `priv/schemas/`, `priv/quality/`, and non-`.json`/`.md`/`.svg` files under `priv/examples/`) must never cross this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-07-01 | Information Disclosure | `priv/schemas/*.json` + `priv/quality/` accidentally shipping in the Hex tarball | medium | mitigate | Task 3's extended `refute` assertions in `branding_claims_test.exs` fail CI if either new schema or the rubric manifest ever appears in the built tarball listing; `mix.exs`'s `:files` list only ever adds `priv/examples`, never `priv/schemas`/`priv/quality` (Task 1). |
| T-114-07-02 | Tampering | `priv/examples/` shipping a non-text (raster/binary) asset | medium | mitigate | The `.gitignore` extension-block (Task 1) prevents ever committing a raster file under `priv/examples/**`; Task 2's tarball text-only assertion independently re-verifies every actually-shipped entry's extension, closing the gap for any file that bypassed `.gitignore` (e.g. via `git add -f`). |
| T-114-07-03 | Information Disclosure | `mix hex.build` invoked via `Rendro.Test.HexBuildCache` (shells out to system `mix`) | low | accept | Existing, already-relied-upon test infrastructure shared with `branding_claims_test.exs`'s current tests; this plan introduces no new attack surface by reusing it. |
</threat_model>

<verification>
`mix test test/docs_contract/examples_schema_contract_test.exs` and `mix test test/docs_contract/branding_claims_test.exs` both pass. Phase-gate confirmation: `mix ci.fast` (runs `hex.build`, full test suite, docs, credo, dialyzer) should be run once all Wave-3 plans land, per 114-VALIDATION.md's phase-gate sampling rate.
</verification>

<success_criteria>
- `priv/examples/` ships in the Hex tarball, text-only.
- `priv/schemas/examples.schema.json`, `priv/schemas/rubric_scores.schema.json`, and `priv/quality/` are all absent from the shipped tarball.
- `mix.exs` and `.gitignore` both reflect the correct, asymmetric packaging treatment for the two new `priv/` trees.
</success_criteria>

<output>
Create `.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-07-SUMMARY.md` when done
</output>
