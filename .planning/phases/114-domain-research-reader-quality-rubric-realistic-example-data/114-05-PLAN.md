---
phase: 114
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/examples/invoice/DOMAIN.md
  - test/docs_contract/domain_md_contract_test.exs
autonomous: true
requirements: [RUB-01]

must_haves:
  truths:
    - "The Invoice domain has a co-located DOMAIN.md capturing domain language, personas + JTBD (the ONE fact the reader needs first), reading context, and layout/typographic conventions."
    - "A docs-contract test structurally verifies every domain's DOMAIN.md carries the required sections."
  artifacts:
    - "priv/examples/invoice/DOMAIN.md"
    - "test/docs_contract/domain_md_contract_test.exs"
  key_links:
    - "priv/examples/invoice/DOMAIN.md -> cited by Phase 118's rubric-gated demos (SHOW-01)"
---

<objective>
Author `priv/examples/invoice/DOMAIN.md` — genuine domain-research content for the Invoice family: domain language (nouns/verbs/events), personas + JTBD (who reads it, in what context, and the ONE fact they need first), reading context, and layout/typographic conventions. Then write `test/docs_contract/domain_md_contract_test.exs`, a structural (headings-present) docs-contract lane.

Purpose: RUB-01 requires this content to exist per domain so Phase 118's demos can cite it when scored against the rubric (Plan 114-06). This is domain-research content flagged MEDIUM/LOW confidence in 114-RESEARCH.md (necessarily synthesized, not verifiable via tooling) — treated here as the locked, research-first recommendation per this project's stated planning preference, with a light human sanity-check available at phase verification.
Output: `priv/examples/invoice/DOMAIN.md` and a new structural contract test.
</objective>

## Artifacts This Phase Produces

(Full phase-114 manifest — every plan in this phase creates a subset; listed here so any single plan has the whole picture.)

- `lib/rendro/examples.ex` — `Rendro.Examples` module (`@moduledoc false`), functions `load!/1`, `list/1` (Plan 114-04)
- `priv/examples/invoice/acme-phoenix-saas/invoice.json` — de-quarantined + normalized realistic Invoice fixture (Plans 114-01, 114-03)
- `priv/examples/invoice/DOMAIN.md` — Invoice domain-research doc (this plan)
- `priv/schemas/examples.schema.json`, `priv/schemas/rubric_scores.schema.json` — repo-only JSON Schemas (Plan 114-02)
- `priv/quality/rubric_scores.json` — repo-only appendable rubric manifest (Plan 114-06)
- `test/rendro/examples_test.exs` — loader behavior + wildcard extension-ban test (Plan 114-04)
- `test/docs_contract/examples_schema_contract_test.exs` — fixture schema validation + tarball inclusion/text-only test (Plans 114-03, 114-07)
- `test/docs_contract/rubric_manifest_contract_test.exs` — rubric manifest schema + threshold-arithmetic test (Plan 114-06)
- `test/docs_contract/domain_md_contract_test.exs` — DOMAIN.md structural headings test (this plan)
- `test/docs_contract/public_api_contract_test.exs` (modified) — adds `Rendro.Examples` to the hidden-modules list (Plan 114-04)
- `test/docs_contract/branding_claims_test.exs` (modified) — adds repo-only exclusion assertions (Plan 114-07)
- `mix.exs` (modified) — `package/0` `:files` gains `priv/examples` (Plan 114-07)
- `.gitignore` (modified) — raster/binary extension-ban block for `priv/examples/**` (Plan 114-07)
- `bench/comparison/*` (modified) — repointed + adapted for the new money format (Plans 114-01, 114-03)
- `scripts/verify_docs.exs`, `test/guardrails/required_checks_contract_test.exs` (modified) — register the 3 new docs-contract lanes + bump the guardrail's lane-count assertion 22 -> 25 (Plan 114-07)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author priv/examples/invoice/DOMAIN.md</name>
  <files>priv/examples/invoice/DOMAIN.md</files>
  <read_first>
    114-RESEARCH.md lines 362-369 (Assumptions Log A1 — the accounts-payable/issuer/auditor reader model this content should follow) and lines 129-153 (Recommended Project Structure — confirms `priv/examples/invoice/DOMAIN.md`'s placement, co-located with the fixture directory);
    ROADMAP.md Phase 114 success criterion 5 (DOMAIN.md required content: domain language, personas + JTBD, reading context, layout/typographic conventions).
  </read_first>
  <action>
    Write `priv/examples/invoice/DOMAIN.md` with exactly these four `## `-level headings, each with genuine, specific content (not placeholder text): "## Domain Language" (nouns: invoice, invoice number/ID, issuer, customer/bill-to, line item, quantity, unit price, subtotal, tax, total due, due date, payment terms e.g. "Net 30", remittance instructions; verbs/events: issue, bill, remit, pay, settle, overdue, dispute); "## Personas & Jobs-to-be-Done" (Accounts Payable clerk — primary reader, processes invoices in a queue, needs total due + due date first, everything else consulted only on discrepancy; issuer/business owner — secondary reader, reviews for professional/legal correctness before sending; bookkeeper/auditor — tertiary reader, reconciles invoice number + total against payments, needs itemization and tax broken out); "## Reading Context" (invoices are triaged in a stack/inbox alongside many similar documents; the first read is a few-second top-to-bottom scan identifying who/how-much/by-when before either filing or acting; a slower second read happens during actual payment processing or audit; invoices are as often printed as read on-screen, so information must survive both); "## Layout & Typographic Conventions" (total due is the single most visually prominent number on the page; issuer and customer are two clearly separated blocks near the top; due date/terms sit near the top or adjacent to totals, never buried; the invoice number is an unambiguous, prominent identifier; line items render as an aligned table with right-aligned money columns; subtotal/tax/total are visually stepped toward the final total; money uses fixed 2-decimal precision and consistent currency-symbol placement throughout).
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' priv/examples/invoice/DOMAIN.md)" -ge "4" && echo HEADINGS_OK</automated>
  </verify>
  <done>DOMAIN.md exists with all four required section headings, each containing genuine (non-placeholder) domain content.</done>
  <acceptance_criteria>
    - `grep -F "## Domain Language" priv/examples/invoice/DOMAIN.md`, `grep -F "## Personas" priv/examples/invoice/DOMAIN.md`, `grep -F "## Reading Context" priv/examples/invoice/DOMAIN.md`, and `grep -F "## Layout" priv/examples/invoice/DOMAIN.md` each match.
    - The file is at least 30 lines long (substantive content, not a stub).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write test/docs_contract/domain_md_contract_test.exs</name>
  <files>test/docs_contract/domain_md_contract_test.exs</files>
  <read_first>
    test/docs_contract/branding_claims_test.exs lines 31-38 (the `File.read!(...) =~ "..."` substring-assertion style used throughout this repo's `*_claims_test.exs` files — the nearest existing analog, though no prior test checks "required markdown headings").
  </read_first>
  <action>
    Create `test/docs_contract/domain_md_contract_test.exs`, module `Rendro.DocsContract.DomainMdContractTest`, `use ExUnit.Case, async: true`. Assert `Path.wildcard("priv/examples/*/DOMAIN.md") != []` (guards against an accidentally-empty match). For every path found, read its content and assert it contains each of the four required headings ("## Domain Language", "## Personas & Jobs-to-be-Done", "## Reading Context", "## Layout & Typographic Conventions"), naming the offending path in any failure message.
  </action>
  <verify>
    <automated>mix test test/docs_contract/domain_md_contract_test.exs</automated>
  </verify>
  <done>Test passes; asserts at least one DOMAIN.md found and all four headings present in each.</done>
  <acceptance_criteria>
    - `mix test test/docs_contract/domain_md_contract_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation-only content | `DOMAIN.md` is pure prose, read only by humans and (in Phase 118) cited by demo test assertions; it has no executable surface and accepts no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-05-01 | Information Disclosure | `DOMAIN.md` domain-research prose | low | accept | Pure documentation; no real business/personal data is referenced (generic domain/persona descriptions only). |
| T-114-05-02 | Tampering | `domain_md_contract_test.exs` substring assertions | low | accept | Read-only structural check against a repo-controlled markdown file; no write path, no external input. |
</threat_model>

<verification>
`mix test test/docs_contract/domain_md_contract_test.exs` passes.
</verification>

<success_criteria>
- `priv/examples/invoice/DOMAIN.md` exists with all four required sections and genuine content.
- An automated structural test enforces the heading contract for every domain's DOMAIN.md.
</success_criteria>

<output>
Create `.planning/phases/114-domain-research-reader-quality-rubric-realistic-example-data/114-05-SUMMARY.md` when done
</output>
