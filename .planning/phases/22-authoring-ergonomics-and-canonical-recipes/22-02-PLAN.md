---
phase: 22-authoring-ergonomics-and-canonical-recipes
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes.ex
  - lib/rendro/adapters/accrue.ex
  - test/rendro/recipes/invoice_test.exs
  - test/rendro/adapters/accrue_test.exs
autonomous: true
requirements:
  - LAY-12
must_haves:
  truths:
    - "The canonical invoice recipe exposes a tiered composition API (document, page_template, sections)."
    - "The Accrue adapter recipe strictly maps content to explicit template sections instead of legacy kwargs."
  artifacts:
    - path: "lib/rendro/recipes/invoice.ex"
      provides: "Tiered composition functions (document/2, page_template/1, sections/2)"
    - path: "lib/rendro/adapters/accrue.ex"
      provides: "Updated recipe using page_template and sections"
  key_links:
    - from: "lib/rendro/recipes/invoice.ex"
      to: "lib/rendro/document.ex"
      via: "Builder API calls (new, add_section, add_template)"
      pattern: "Rendro\\.Document\\.new"
---

<objective>
Refactor recipes to use the new layout primitives (`page_template` and `sections`) and the builder API, replacing ad hoc block stacking (the `header:` and `footer:` kwargs).

Purpose: Demonstrate serious report composition without relying on implicit pagination defaults, showing engineers how to build maintainable documents. Provide a zero-to-one "just works" experience (`document/2`) alongside advanced escape hatches (`page_template/1`, `sections/2`).
Output: The new `Rendro.Recipes.Invoice` module and updated `Rendro.Adapters.Accrue` module, both adopting explicit sections.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Tiered Composition Invoice Recipe</name>
  <files>lib/rendro/recipes/invoice.ex, test/rendro/recipes/invoice_test.exs, lib/rendro/recipes.ex</files>
  <behavior>
    - Test 1: `Rendro.Recipes.Invoice.page_template(opts)` returns a `%Rendro.PageTemplate{}` with explicitly named regions (`:header`, `:body`, `:footer`).
    - Test 2: `Rendro.Recipes.Invoice.sections(data, opts)` returns a list of `%Rendro.Section{}` mapped to those regions.
    - Test 3: `Rendro.Recipes.Invoice.document(data, opts)` uses the builder API to assemble the template and sections into a `%Rendro.Document{}`.
  </behavior>
  <action>
    Create `lib/rendro/recipes/invoice.ex`. Implement the Tiered Composition pattern (`document(data, opts \\ [])`, `page_template(opts \\ [])`, `sections(data, opts \\ [])`). Use the builder API from Plan 01 in `document/2`. Ensure it maps to explicitly defined regions rather than legacy kwargs.
    Update `lib/rendro/recipes.ex` to delegate `invoice/1` to `Rendro.Recipes.Invoice.document/1` (to maintain backward compatibility, or replace it if acceptable).
    Create `test/rendro/recipes/invoice_test.exs` using structural AST assertions.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_test.exs</automated>
  </verify>
  <done>Tiered Invoice recipe is built and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Accrue Adapter Recipe to use Sections</name>
  <files>lib/rendro/adapters/accrue.ex, test/rendro/adapters/accrue_test.exs</files>
  <behavior>
    - Test 1: `Rendro.Adapters.Accrue.recipe/1` returns a `%Rendro.Document{}` whose `page_templates` contains a template with named regions `:header`, `:body`, `:footer`.
    - Test 2: The returned document's `sections` list assigns content to each of those three regions (one section per region).
    - Test 3: The document does NOT carry legacy `header` or `footer` keyword content — `doc.header` and `doc.footer` are empty/nil.
    - Test 4: Source check — `lib/rendro/adapters/accrue.ex` no longer references `header:` or `footer:` keyword arguments when calling `Rendro.flow/2`.
  </behavior>
  <action>
    Refactor `Rendro.Adapters.Accrue.recipe/1` to stop using `header:` and `footer:` kwargs in `Rendro.flow/2`.
    Instead, define a `page_template` with regions (`:header`, `:body`, `:footer`) and use `Rendro.section/1` (or `Rendro.Document.add_section`) to assign content to these regions explicitly.
    Update `test/rendro/adapters/accrue_test.exs` to assert on the new structure per the behavior list above.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/accrue_test.exs</automated>
  </verify>
  <done>Accrue adapter recipe uses explicit sections and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Input -> Recipe | Recipe modules ingest data maps to produce document ASTs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Information Disclosure | `lib/rendro/recipes/invoice.ex` | accept | Recipes only reflect provided data into layout blocks; no sensitive backend state is queried implicitly. |
</threat_model>

<verification>
`mix test test/rendro/recipes/invoice_test.exs test/rendro/adapters/accrue_test.exs` passes successfully.
</verification>

<success_criteria>
- The Accrue and Invoice recipes rely strictly on explicit sections and page templates.
- No new instances of `header:` or `footer:` layout kwargs are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-02-SUMMARY.md`
</output>
