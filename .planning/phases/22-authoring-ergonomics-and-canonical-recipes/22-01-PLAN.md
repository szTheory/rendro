---
phase: 22-authoring-ergonomics-and-canonical-recipes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/document.ex
  - test/rendro/document_test.exs
autonomous: true
requirements:
  - LAY-12
must_haves:
  truths:
    - "Engineers can compose documents dynamically using a pipeable builder API."
    - "Builder API correctly accumulates sections and templates without losing data."
  artifacts:
    - path: "lib/rendro/document.ex"
      provides: "Pipeline builder functions (new, put_metadata, add_template, add_section, put_options)"
    - path: "test/rendro/document_test.exs"
      provides: "AST-based assertions for the builder API"
  key_links:
    - from: "lib/rendro/document.ex"
      to: "Rendro.Document struct"
      via: "Struct updates"
      pattern: "%__MODULE__{doc | ...}"
---

<objective>
Introduce a pipeable builder API for dynamic document construction, mirroring the ergonomics of `Plug.Conn` or `Ecto.Changeset`.

Purpose: Elixir developers expect pipelines. While `Rendro.document(sections: [...])` is great for declarative definitions, a builder API supports dynamic, conditional assembly during a request cycle.
Output: A new builder API on `Rendro.Document` and tests proving its determinism via AST structure checks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Pipeline Builder API to Rendro.Document</name>
  <files>lib/rendro/document.ex, test/rendro/document_test.exs</files>
  <behavior>
    - Test 1: `Rendro.Document.new()` returns an empty struct.
    - Test 2: `put_metadata/2` updates the metadata struct.
    - Test 3: `add_template/2` appends to `page_templates`.
    - Test 4: `set_template/2` sets the active `page_template` name.
    - Test 5: `add_section/2` appends to `sections` list.
    - Test 6: `put_options/2` merges options map.
  </behavior>
  <action>
    Modify `lib/rendro/document.ex` to include builder functions: `new/0`, `new/1`, `put_metadata/2`, `add_template/2`, `set_template/2`, `add_section/2`, and `put_options/2`.
    Update `test/rendro/document_test.exs` to test these functions by asserting on the returned `%Rendro.Document{}` struct (AST-based testing), avoiding binary rendering tests.
  </action>
  <verify>
    <automated>mix test test/rendro/document_test.exs</automated>
  </verify>
  <done>Builder API is fully implemented and covered by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> Document Builder | Builder API is a pure data transformation, bounded entirely within trusted memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `lib/rendro/document.ex` | accept | Pure struct update; malformed input is caught by downstream pipeline validation stages. |
</threat_model>

<verification>
`mix test test/rendro/document_test.exs` passes successfully.
</verification>

<success_criteria>
- The `Rendro.Document` module exposes a pipeable builder API.
- All tests pass verifying the structural changes to the AST.
</success_criteria>

<output>
After completion, create `.planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-01-SUMMARY.md`
</output>
