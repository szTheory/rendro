---
phase: 22-authoring-ergonomics-and-canonical-recipes
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex
  - examples/phoenix_example/test/phoenix_example_web/controllers/pdf_controller_test.exs
  - README.md
autonomous: true
requirements:
  - LAY-12
must_haves:
  truths:
    - "The Phoenix example controller serves a realistic canonical invoice rather than a trivial text block."
    - "The README documentation explains how to compose documents using the new builder API and tiered composition."
  artifacts:
    - path: "examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex"
      provides: "Phoenix controller serving the tiered Invoice recipe"
    - path: "README.md"
      provides: "Documentation of the new builder API"
  key_links:
    - from: "examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex"
      to: "lib/rendro/recipes/invoice.ex"
      via: "Function call"
      pattern: "Rendro\\.Recipes\\.Invoice\\.document"
---

<objective>
Update the Phoenix example and the top-level README to guide adopters toward the new, robust authoring semantics (builder API and tiered composition) instead of ad hoc block stacking.

Purpose: Serving PDFs in Phoenix should feel as natural as rendering JSON. The examples must provide a clear, idiomatic implementation of loading data, building the document via the recipe API, and sending the `application/pdf` response inline.
Output: Upgraded Phoenix example and README.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Phoenix Example Controller</name>
  <files>examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex, examples/phoenix_example/test/phoenix_example_web/controllers/pdf_controller_test.exs</files>
  <behavior>
    - Test 1: GET request to the PDF route returns HTTP 200 with `content-type` `application/pdf`.
    - Test 2: Source check — `pdf_controller.ex` invokes `Rendro.Recipes.Invoice.document/2` (or the delegated `Rendro.Recipes.invoice/1`); the legacy single-block `Rendro.flow` literal is removed.
    - Test 3: Controller-level structural test — the document built inside the controller (extracted via a small helper or by re-running the recipe in the test with the same dummy data) is a `%Rendro.Document{}` whose `page_templates` includes a template with named regions and whose `sections` list is non-empty (proving the canonical recipe is what drove the response, not a trivial flow).
    - Test 4: Response body is a non-empty binary that begins with the PDF magic bytes `%PDF-`.
  </behavior>
  <action>
    Modify `pdf_controller.ex` in the Phoenix example app. Replace the trivial single-block `Rendro.flow` document with a call to `Rendro.Recipes.Invoice.document/2` (or the delegated `Rendro.Recipes.invoice/1`), passing realistic dummy data (an invoice ID and a list of line items).
    Render the document via `RendroPhoenix.render_pdf`.
    Update `pdf_controller_test.exs` to cover the assertions in the behavior list above.
  </action>
  <verify>
    <automated>cd examples/phoenix_example && mix test test/phoenix_example_web/controllers/pdf_controller_test.exs</automated>
  </verify>
  <done>Phoenix controller serves a canonical invoice PDF via the recipe API and tests prove it.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md Authoring Documentation</name>
  <files>README.md</files>
  <behavior>
    - Check 1: README contains a builder API example invoking `Rendro.Document.new`.
    - Check 2: README introduces the term "Tiered Composition" with at least one paragraph of explanation.
    - Check 3: README's primary flow / "getting started" section no longer presents `header:` and `footer:` kwargs as the recommended approach (any remaining mention is in a backward-compatibility / migration note, not the primary example).
    - Check 4: README references the canonical invoice recipe (`Rendro.Recipes.Invoice`) as the worked example for serious documents.
  </behavior>
  <action>
    Update `README.md` to showcase the new Builder API (`Rendro.Document.new() |> ...`) and explicit section assignment.
    Remove or de-emphasize legacy `header:` and `footer:` keyword arguments in the primary flow documentation (move any remaining reference into a clearly labeled compatibility note).
    Introduce the "Tiered Composition" concept with a short explanation and an `Rendro.Recipes.Invoice` example covering `document/2`, `page_template/1`, `sections/2`.
  </action>
  <verify>
    <automated>grep -q "Rendro.Document.new" README.md && grep -q "Tiered Composition" README.md && grep -q "Rendro.Recipes.Invoice" README.md</automated>
  </verify>
  <done>README accurately reflects the new canonical authoring patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP -> Controller | Controller exposes PDF generation over HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Denial of Service | `pdf_controller.ex` | accept | This is just example code; real implementations will need rate limiting for expensive PDF generation. |
</threat_model>

<verification>
`cd examples/phoenix_example && mix test` passes successfully.
</verification>

<success_criteria>
- The Phoenix example uses a real, realistic recipe instead of a trivial demo.
- The README reflects the modern pipeline and tiered composition APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-03-SUMMARY.md`
</output>
