---
phase: 80-stability-contract-migration-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/api_stability.md
  - test/docs_contract/protection_claims_test.exs
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-04

must_haves:
  truths:
    - "guides/api_stability.md leads with the two-tier SemVer contract (Tier-1 Stable / Tier-2 Evolving), the byte-output carve-out, and the NOT covered by SemVer list"
    - "The deprecation policy (soft-deprecate-first) and Deprecations table with None as of 1.0.0 sentinel row appear in the guide"
    - "No internal milestone or phase labels remain in guides/api_stability.md"
    - "mix test test/docs_contract/ exits 0 after both files are committed together"
  artifacts:
    - path: "guides/api_stability.md"
      provides: "Rewritten two-tier stability contract guide"
      contains: "deterministic within a version, not frozen across versions"
    - path: "test/docs_contract/protection_claims_test.exs"
      provides: "Updated CI-pinned assertions tracking the label scrub"
      contains: "Rendro supports only `:aes_256`"
  key_links:
    - from: "test/docs_contract/protection_claims_test.exs"
      to: "guides/api_stability.md"
      via: "File.read! + guide =~ substring assertion"
      pattern: "assert guide =~ \"Rendro supports only"
---

<objective>
Rewrite `guides/api_stability.md` with a contract-first structure so the two-tier SemVer contract leads the document, then scrub all internal milestone/phase labels, updating the two CI-pinned test assertions in `protection_claims_test.exs` in the same commit.

Purpose: STAB-01 (two-tier contract + carve-out + NOT covered list), STAB-02 (deprecation policy + table), and STAB-04 (label scrub) require this guide to be the authoritative public statement of what 1.0 promises.

Output: A rewritten `guides/api_stability.md` and an updated `test/docs_contract/protection_claims_test.exs`, committed atomically so `mix test test/docs_contract/` is green after the single commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-stability-contract-migration-docs/80-CONTEXT.md
@.planning/phases/80-stability-contract-migration-docs/80-RESEARCH.md
@.planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md

<interfaces>
<!-- Exact substrings the docs-contract test suite currently pins against api_stability.md.
     These must survive verbatim in the rewritten guide (byte-identical block moves, per D-02).
     Source: test/docs_contract/ grep on "api_stability.md" read assertions. -->

From test/docs_contract/protection_claims_test.exs (lines 41-66):
  assert guide =~ "Rendro supports password-to-open PDF protection through an external artifact-first boundary."
  assert guide =~ "`Rendro.Protect.password/2`"
  CHANGES (D-05):
    line 48: "Rendro v1.10 supports only `:aes_256`"  →  "Rendro supports only `:aes_256`"
    line 56: "Phase 53 does not introduce a first-party protected worker or orchestration API."  →  "Rendro does not introduce a first-party protected worker or orchestration API."
  assert guide =~ "Advisory permissions are an honor-system PDF flag surface"
  assert guide =~ "Protection is not compliance, not tamper evidence, and not digital signing."
  assert guide =~ "Delivery and storage seams should transport already-protected artifacts, not password material."
  assert guide =~ "If validation succeeds only with `owner_password`"
  assert guide =~ "Apple Preview is `supported` for the `protection` surface"
  assert guide =~ "`save_and_reopen_readability`"
  assert guide =~ "Adobe Acrobat Reader is `supported` for the `protection` surface"
  assert guide =~ "priv/viewer_evidence/protection/adobe_acrobat_reader.md"
  refute guide =~ "secure PDF"
  refute guide =~ "tamper-proof"
  refute guide =~ "PDF/A compliant"

40-char prefix safety (viewer_evidence_claims_test.exs:74-83 — these openings must survive):
  "PDF.js failed the forms four-check save-"   (first 40 chars of line 148 deferral reason)
  "Apple Preview Attachments UI still does "   (first 40 chars of line 155 deferral reason)

Current per-surface blocks to move VERBATIM (byte-identical) below a subordinate heading:
  ## Interactive Forms Support Boundary      (line 32)
  ## Signing Preparation Support Boundary   (line 60)
  ## Signed Artifact Support Boundary       (line 72)
  ## Long-Lived Evidence Support Boundary   (line 86)
  ## Embedded Files Support Boundary        (line 98)
  ## Curated Links Support Boundary         (line 106)
  ## Embedded Artifact Viewer Posture       (line 112)
  ## Protected PDF Support Boundary         (line 122)
  ## Explicit Deferral Reasons (matrix-mirrored)  (line 144)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite guides/api_stability.md — contract-first structure, label scrub, D-05 lockstep</name>
  <files>guides/api_stability.md, test/docs_contract/protection_claims_test.exs</files>

  <read_first>
    - guides/api_stability.md — current 155-line file; read in full to extract per-surface blocks for byte-identical relocation
    - test/docs_contract/protection_claims_test.exs — lines 41-66 and 90-95; know every currently-pinned substring before rewording anything
    - test/docs_contract/signing_claims_test.exs — lines 84-93; refute-guard inventory to avoid banned phrases in new prose
    - priv/public_api.json — tier assignments for Rendro.Inspector (adapter) vs stable modules; informs Tier-1/Tier-2 framing and D-09 reconcile
    - .planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md — exact per-file instructions at "guides/api_stability.md (MODIFY — full rewrite)" section
  </read_first>

  <action>
    Rewrite `guides/api_stability.md` to lead with the two-tier SemVer contract. The new document structure is:

    1. Top-level heading: `# API Stability and Support Boundaries` (keep or update as Claude's discretion).

    2. New CONTRACT SECTION leading the document — add all of the following before the per-surface blocks:

       a. `## Tier-1 Stable` — covers the `stable` tier modules per D-10/D-09 reconcile: `Rendro.Document`, `Rendro.PageTemplate`, `Rendro.Section`, `Rendro.Metadata`, `Rendro.Artifact` (struct), `Rendro.Sign` (`prepare/2`, `sign/2`, `augment/2`, `validate/2`), `Rendro.Protect` (`password/2`), and the top-level `Rendro` functions (`flow/2`, `signature_field/2`, `render_signed/3`, `render_protected/3`). These receive strict SemVer guarantees. Do NOT name `Rendro.Inspector` as Tier-1; instead describe the `:diagnostics` map common keys (`:level`, `:type`) as the stable contract for diagnostics consumers.

       b. `## Tier-2 Evolving` — covers `Rendro.Adapters.*` (adapter tier: `Rendro.Adapters.PyHanko`, `Rendro.Adapters.Qpdf`, others) and the `:diagnostics` map beyond common keys. These are additive-only; may break to follow upstream library majors.

       c. `## NOT covered by SemVer` — the exact 6 bullets from D-13:
          1. Byte-for-byte rendered PDF output across versions (deterministic within a version, not frozen across versions)
          2. Internal modules/functions marked `@moduledoc false` / `@doc false` (e.g. `Rendro.PDF.CidFont`, `Rendro.PDF.FontSubsetter`, the `redact_*` helpers). Public ≡ what ExDoc renders.
          3. Exact shape of `:diagnostics` / metadata maps beyond documented common keys
          4. Adapter APIs that track upstream library majors (`Rendro.Adapters.*` — Tier-2, may break to follow underlying tools' majors)
          5. Error-message wording (presence/category of an error is contract; the human-readable string is not)
          6. Log/telemetry message text (documented event names/measurements are covered; free-text descriptions are not)
          The headline byte-output carve-out MUST contain the exact string "deterministic within a version, not frozen across versions".

       d. `## Deprecation Policy` — the soft-deprecate-first lifecycle per D-16: `@doc deprecated:` + CHANGELOG by default; `@deprecated` hard-warning only once no in-tree caller remains (because `mix ci` compiles `--warnings-as-errors`); removal only in 2.0. Include one illustrative fenced Elixir code block showing the `@doc deprecated:` pattern, clearly labeled "example, not a live deprecation" (NOT a real table row).

       e. `## Deprecations` — a Markdown table with columns: `Symbol | Soft-deprecated (@doc deprecated:) | Hard-deprecated (@deprecated) | Removed | Replacement`. Single data row: `_None as of 1.0.0_` spanning or in the Symbol column with em-dashes for other columns.

    3. Add a subordinate heading (e.g., `## Per-Surface Support Boundaries`) immediately before the relocated blocks to make it clear they are below the contract in the document hierarchy.

    4. RELOCATE the following per-surface blocks BYTE-IDENTICAL (copy exact text, no rewording) below the new subordinate heading:
       - `## Interactive Forms Support Boundary` (current line 32 through line 59)
       - `## Signing Preparation Support Boundary` (current line 60 through line 71)
       - `## Signed Artifact Support Boundary` (current line 72 through line 85)
       - `## Long-Lived Evidence Support Boundary` (current line 86 through line 97)
       - `## Embedded Files Support Boundary` (current line 98 through line 105)
       - `## Curated Links Support Boundary` (current line 106 through line 111)
       - `## Embedded Artifact Viewer Posture` (current line 112 through line 121)
       - `## Protected PDF Support Boundary` (current line 122 through line 143)
       - `## Explicit Deferral Reasons (matrix-mirrored)` (current line 144 through line 155)
       - `## Viewer Evidence and CHANGELOG Discipline` section (current lines 27-30) — relocate after the per-surface blocks or keep near the contract section; Claude's discretion on placement.

    5. APPLY LABEL EDITS within the relocated per-surface blocks:

       FREE EDITS (D-06 — no CI pin, edit only these specific phrases):
       - Line 54 phrase `"during Phase 71 review"` → `"during operator review"`
       - Line 118 phrase `"after Phase 71 re-verify"` → `"on the version recorded"` (or delete if sentence reads cleanly without it)
       - Line 148 phrase `"during Phase 71 operator review"` → `"during operator review"` (ONLY edit this suffix; preserve the ENTIRE sentence opening "PDF.js failed the forms four-check save-and-reopen round-trip on the representative fixture" verbatim — the first 40 chars are CI-asserted by viewer_evidence_claims_test.exs:74-83)
       - Line 155 phrase `"after Phase 71 re-verify; v1.9 deferral stands"` → `"on the version recorded; the deferral stands"` (ONLY edit the ending suffix; preserve the sentence opening "Apple Preview Attachments UI still does not discover, open, or extract the representative embedded-artifact fixture" verbatim — the first 40 chars are CI-asserted)

       CI-PINNED LOCKSTEP EDITS (D-05 — MUST also update protection_claims_test.exs in same commit):
       - Line 128 phrase `"Rendro v1.10 supports only \`:aes_256\`"` → `"Rendro supports only \`:aes_256\`"`
       - Line 136 phrase `"Phase 53 does not introduce a first-party protected worker or orchestration API."` → `"Rendro does not introduce a first-party protected worker or orchestration API."`

    6. BANNED PHRASES — do NOT introduce any of: "secure PDF", "PAdES is supported", "all signature viewers are supported", "PDF/A compliant", "tamper-proof", "LT/LTA is supported", "PDF/A is supported", "regulatory approval", "enterprise compliance", "tamper-evident signing", "viewer portability is guaranteed", "Rendro owns signer identity trust".

    7. ALSO update `test/docs_contract/protection_claims_test.exs` in the SAME staged change:
       - Line 48: change assertion from `"Rendro v1.10 supports only \`:aes_256\`"` to `"Rendro supports only \`:aes_256\`"`
       - Lines 55-57: change assertion from `"Phase 53 does not introduce a first-party protected worker or orchestration API."` to `"Rendro does not introduce a first-party protected worker or orchestration API."`
       These are the ONLY two changes in protection_claims_test.exs.

    Commit both files together. Do not commit guide without the test update or vice versa.
  </action>

  <read_first>guides/api_stability.md, test/docs_contract/protection_claims_test.exs, test/docs_contract/signing_claims_test.exs, priv/public_api.json, .planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md</read_first>

  <verify>
    <automated>mix test test/docs_contract/protection_claims_test.exs test/docs_contract/signing_claims_test.exs test/docs_contract/viewer_evidence_claims_test.exs test/docs_contract/forms_claims_test.exs test/docs_contract/embedded_artifact_claims_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/docs_contract/` exits 0 (all 11 existing lanes pass; no new lane yet)
    - `guides/api_stability.md` contains the exact string "deterministic within a version, not frozen across versions"
    - `guides/api_stability.md` contains "## Tier-1 Stable"
    - `guides/api_stability.md` contains "## Tier-2 Evolving"
    - `guides/api_stability.md` contains "## NOT covered by SemVer"
    - `guides/api_stability.md` contains "## Deprecation Policy"
    - `guides/api_stability.md` contains "## Deprecations"
    - `guides/api_stability.md` contains "None as of 1.0.0"
    - `guides/api_stability.md` does NOT contain "Rendro v1.10"
    - `guides/api_stability.md` does NOT contain "Phase 53"
    - `guides/api_stability.md` does NOT contain "Phase 71"
    - `guides/api_stability.md` does NOT contain "Rendro.Inspector" in any Tier-1-stable context
    - `test/docs_contract/protection_claims_test.exs` line ~48 asserts `"Rendro supports only \`:aes_256\`"` (not "Rendro v1.10")
    - `test/docs_contract/protection_claims_test.exs` lines ~55-57 assert `"Rendro does not introduce a first-party protected worker or orchestration API."` (not "Phase 53")
    - `grep -r "Rendro v1.10\|Phase 53\|Phase 71" guides/api_stability.md` returns nothing
  </acceptance_criteria>

  <done>
    guides/api_stability.md is rewritten with contract-first structure. The two-tier SemVer contract (Tier-1/Tier-2), byte-output carve-out, NOT covered by SemVer list, deprecation policy, and Deprecations table with None as of 1.0.0 sentinel all lead the document. All nine per-surface blocks are relocated byte-identical below a subordinate heading. All internal labels are removed. protection_claims_test.exs lines 48 and 56 are updated to match the new guide text. mix test test/docs_contract/ is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose → published HexDocs | Markdown content becomes publicly-visible API promises to adopters |
| docs-contract test suite → CI gate | Test assertions are the enforcement mechanism for the lockstep invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Information Disclosure | guides/api_stability.md | mitigate | Lockstep edit (D-05): CI-pinned label strings in protection_claims_test.exs:48,56 must change with the guide in the same commit — leaving stale pins would cause CI failure revealing the old internal label is still referenced |
| T-80-02 | Tampering | guides/api_stability.md overclaim prose | mitigate | D-03 banned-phrase list enforced by refute guards in signing_claims_test.exs:84-93 and protection_claims_test.exs:64-66 — any banned phrase in new contract prose trips CI |
| T-80-03 | Information Disclosure | Internal milestone/phase labels in public guide | mitigate | D-04/D-05/D-06: scrub all label occurrences; CI-pinned ones (D-05) updated atomically; free-prose ones (D-06) edited in same commit; grep gate in acceptance_criteria confirms zero remaining labels |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — pure markdown + ExUnit file edits |
</threat_model>

<verification>
After this plan's single commit:

```
mix test test/docs_contract/
```

Must exit 0 (all 11 existing lanes green). Additionally:

```
grep -c "Rendro v1.10\|Phase 53\|Phase 71" guides/api_stability.md
```

Must output 0 (pipe through grep -v '^#' first to exclude any markdown headings containing those strings, though none are expected).

```
grep "deterministic within a version, not frozen across versions" guides/api_stability.md
```

Must return a match.
</verification>

<success_criteria>
1. guides/api_stability.md states the two-tier SemVer contract (Tier-1 Stable strict SemVer; Tier-2 Evolving additive-only), the byte-output carve-out with the exact phrase "deterministic within a version, not frozen across versions", and the six-bullet NOT covered by SemVer list (STAB-01)
2. The guide carries the soft-deprecate-first deprecation policy and a Deprecations table with the None as of 1.0.0 sentinel row (STAB-02)
3. No internal milestone/phase label remains in guides/api_stability.md (STAB-04 partial — api_stability.md portion)
4. protection_claims_test.exs is updated in lockstep with the two CI-pinned label changes (D-05)
5. mix test test/docs_contract/ exits 0
</success_criteria>

<output>
Create `.planning/phases/80-stability-contract-migration-docs/80-01-SUMMARY.md` when done
</output>
