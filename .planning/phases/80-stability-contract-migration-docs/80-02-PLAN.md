---
phase: 80-stability-contract-migration-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/viewer_evidence.md
  - test/docs_contract/signing_claims_test.exs
  - test/docs_contract/viewer_evidence_claims_test.exs
  - test/docs_contract/embedded_artifact_claims_test.exs
autonomous: true
requirements:
  - STAB-04

must_haves:
  truths:
    - "No internal milestone/phase labels remain in guides/viewer_evidence.md"
    - "Test titles/comments in signing_claims_test.exs, viewer_evidence_claims_test.exs, and embedded_artifact_claims_test.exs no longer contain phase/version labels"
    - "The refute guards at viewer_evidence_claims_test.exs:106-107 are untouched"
    - "mix test test/docs_contract/ exits 0 after commit"
  artifacts:
    - path: "guides/viewer_evidence.md"
      provides: "Label-scrubbed viewer evidence guide"
    - path: "test/docs_contract/signing_claims_test.exs"
      provides: "Test title rename: no Phase 71 label in test description"
    - path: "test/docs_contract/viewer_evidence_claims_test.exs"
      provides: "Test title rename: no Phase 71 label in test description"
    - path: "test/docs_contract/embedded_artifact_claims_test.exs"
      provides: "Comment rename: no Phase 71 label in comment"
  key_links:
    - from: "test/docs_contract/viewer_evidence_claims_test.exs"
      to: "guides/viewer_evidence.md"
      via: "refute guards at lines 106-107 (must NOT be touched)"
      pattern: "refute guide =~"
---

<objective>
Scrub internal milestone/phase labels from `guides/viewer_evidence.md` and rename test titles/comments in three sibling docs-contract tests, making all edits free-prose (no CI-pinned guide substrings are affected). Keep the refute guards at `viewer_evidence_claims_test.exs:106-107` intact.

Purpose: STAB-04 requires all public guides to be free of internal milestone/phase labels. `viewer_evidence.md` has 8 free-prose label occurrences at lines 31, 59, 97, 109, 157, 190, 327, 329. Three test files have titles/comments with phase labels that are pure hygiene (D-06).

Output: Cleaned `guides/viewer_evidence.md` and three updated test files committed atomically so `mix test test/docs_contract/` stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-stability-contract-migration-docs/80-CONTEXT.md
@.planning/phases/80-stability-contract-migration-docs/80-RESEARCH.md

<interfaces>
<!-- D-06 free-prose edits — none of these occurrences are pinned by any test assertion.
     D-07: viewer_evidence_claims_test.exs:106-107 refute guards are KEEPERS — do not touch. -->

guides/viewer_evidence.md label edits (approximate lines — read the file to confirm):
  line ~31:  "record Phase 70 consolidated legacy rows"
             → "record legacy rows" (or equivalent timeless phrasing)
  line ~59:  "Phase 71 trust-sensitive surfaces"
             → "trust-sensitive surfaces"
  line ~97:  "records all Phase 71 structural-proxy evidence files"
             → "records structural-proxy evidence files" (or equivalent)
  line ~109: "no unverified cells remain (v2.3 close)"
             → "no unverified cells remain" (drop the milestone label only)
  line ~157: "Tier-B promotion-complete validation passes for all `supported` rows at v2.3 close"
             → "Tier-B promotion-complete validation passes for all `supported` rows"
  line ~190: "same pdfium-cli structural proxy lane as Phase 70 automation"
             → "same pdfium-cli structural proxy lane" (or equivalent)
  line ~327: "see Phase 69 plan 03"
             → drop the reference or replace with "see the implementation notes"
  line ~329: "no GUI viewer sessions required for Phase 71 trust-sensitive closures"
             → "no GUI viewer sessions required for trust-sensitive closures"

Test title/comment hygiene edits (D-06 — no guide coupling, pure hygiene):
  test/docs_contract/signing_claims_test.exs:33
    test name "...terminal after Phase 71" → drop the "after Phase 71" label suffix

  test/docs_contract/viewer_evidence_claims_test.exs:94
    test name "...documents Phase 71 deferral templates" → drop the "Phase 71" label

  test/docs_contract/embedded_artifact_claims_test.exs:38
    comment "...per Phase 71 re-verify" → "...on the version checked"

KEEPER (DO NOT TOUCH):
  test/docs_contract/viewer_evidence_claims_test.exs:106-107 — refute guards enforcing scrub
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub viewer_evidence.md free-prose labels + rename test titles/comments</name>
  <files>guides/viewer_evidence.md, test/docs_contract/signing_claims_test.exs, test/docs_contract/viewer_evidence_claims_test.exs, test/docs_contract/embedded_artifact_claims_test.exs</files>

  <read_first>
    - guides/viewer_evidence.md — read in full to locate each label occurrence at lines ~31, ~59, ~97, ~109, ~157, ~190, ~327, ~329 and confirm exact surrounding text before editing
    - test/docs_contract/viewer_evidence_claims_test.exs — read lines 90-115 to confirm test title at ~94 and locate the refute guards at 106-107 that must NOT be touched
    - test/docs_contract/signing_claims_test.exs — read lines 28-40 to confirm test title at ~33
    - test/docs_contract/embedded_artifact_claims_test.exs — read lines 33-45 to confirm comment at ~38
    - .planning/phases/80-stability-contract-migration-docs/80-RESEARCH.md — sections "Verification: D-05 Exact Pinned Substrings" and "Complete label inventory" for exact phrase inventory
  </read_first>

  <action>
    Edit `guides/viewer_evidence.md` at the 8 approximate-line locations listed below. Read the file first to confirm exact surrounding context — these are free-prose edits with no CI-pinned test assertions, so reword to timeless language by dropping the phase/version labels:

    - Line ~31: Remove "Phase 70" from "record Phase 70 consolidated legacy rows" — reword to timeless equivalent, e.g. "record legacy rows without GUI viewers"
    - Line ~59: Remove "Phase 71" from "Phase 71 trust-sensitive surfaces" — reword to "trust-sensitive surfaces" or equivalent
    - Line ~97: Remove "Phase 71" from "records all Phase 71 structural-proxy evidence files" — reword to "records structural-proxy evidence files"
    - Line ~109: Remove "(v2.3 close)" milestone suffix from "no unverified cells remain (v2.3 close)" — reword to "no unverified cells remain"
    - Line ~157: Remove "at v2.3 close" milestone suffix from "Tier-B promotion-complete validation passes for all `supported` rows at v2.3 close" — reword to "Tier-B promotion-complete validation passes for all `supported` rows"
    - Line ~190: Remove "Phase 70" from "same pdfium-cli structural proxy lane as Phase 70 automation" — reword to "same pdfium-cli structural proxy lane" or equivalent
    - Line ~327: Remove "see Phase 69 plan 03" — delete the reference entirely or replace with a timeless note if context requires it
    - Line ~329: Remove "Phase 71" from "no GUI viewer sessions required for Phase 71 trust-sensitive closures" — reword to "no GUI viewer sessions required for trust-sensitive closures"

    Do NOT alter any other text in viewer_evidence.md. Do NOT touch lines 106-107 of viewer_evidence_claims_test.exs (refute guards — these enforce the scrub, not contradict it).

    Also apply these test hygiene edits (no guide-content coupling — purely internal test metadata):

    - `test/docs_contract/signing_claims_test.exs` line ~33: rename the test title to drop the "Phase 71" label. The test's ASSERTION content must remain untouched; only the string name of the `test "..."` block changes.

    - `test/docs_contract/viewer_evidence_claims_test.exs` line ~94: rename the test title to drop the "Phase 71" label. Again, only the test name string changes; assertions are untouched.

    - `test/docs_contract/embedded_artifact_claims_test.exs` line ~38: change the inline comment from "per Phase 71 re-verify" to "on the version checked". This is a comment only; the assertion on the same line or nearby is unchanged.

    Stage all four files together and commit. Run `mix test test/docs_contract/` before committing.
  </action>

  <verify>
    <automated>mix test test/docs_contract/signing_claims_test.exs test/docs_contract/viewer_evidence_claims_test.exs test/docs_contract/embedded_artifact_claims_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/docs_contract/` exits 0
    - `grep -c "Phase 70\|Phase 71\|Phase 69\|v2\.3 close" guides/viewer_evidence.md` returns 0
    - `viewer_evidence_claims_test.exs` lines 106-107 still contain `refute guide =~` assertions (KEEPER — verify they were not removed)
    - `grep -i "phase 71" test/docs_contract/signing_claims_test.exs` returns 0 (test title renamed)
    - `grep -i "phase 71" test/docs_contract/viewer_evidence_claims_test.exs | grep -v "refute"` returns 0 (title renamed; refute lines may remain if they assert content, but the title/comment label is gone)
    - `grep "Phase 71 re-verify" test/docs_contract/embedded_artifact_claims_test.exs` returns 0 (comment replaced)
    - `grep -r "Phase 7[01]\|Phase 69\|Rendro v1\.10\|Phase 53\|v2\.3 close" guides/viewer_evidence.md` returns nothing
  </acceptance_criteria>

  <done>
    guides/viewer_evidence.md has no internal milestone/phase labels. Test titles and comments in signing_claims_test.exs, viewer_evidence_claims_test.exs, and embedded_artifact_claims_test.exs no longer contain phase/version labels. The refute guards at viewer_evidence_claims_test.exs:106-107 are intact. mix test test/docs_contract/ exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose → published HexDocs | Markdown content becomes publicly-visible; internal labels must not reach adopters |
| refute guards → CI enforcement | viewer_evidence_claims_test.exs:106-107 enforce that certain internal phrases stay absent from the guide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04 | Information Disclosure | guides/viewer_evidence.md internal labels | mitigate | D-04/D-06: all 8 free-prose label occurrences replaced with timeless language; grep acceptance criterion confirms zero remaining matches |
| T-80-05 | Tampering | viewer_evidence_claims_test.exs refute guards (lines 106-107) | mitigate | D-07: keeper rule — read_first requires confirming lines 106-107 are present before and after edit; acceptance_criteria verifies `refute guide =~` assertions survive |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — pure markdown + test comment edits |
</threat_model>

<verification>
After commit:

```
mix test test/docs_contract/
```

Must exit 0 (all 11 existing lanes green).

```
grep -rn "Phase 70\|Phase 71\|Phase 69\|v2\.3 close" guides/viewer_evidence.md
```

Must return no matches.
</verification>

<success_criteria>
1. guides/viewer_evidence.md contains no internal milestone/phase labels (all 8 occurrences replaced with timeless language) — STAB-04 viewer_evidence.md portion
2. Test title/comment hygiene complete in signing_claims_test.exs, viewer_evidence_claims_test.exs, and embedded_artifact_claims_test.exs — D-06
3. refute guards at viewer_evidence_claims_test.exs:106-107 are untouched — D-07
4. mix test test/docs_contract/ exits 0
</success_criteria>

<output>
Create `.planning/phases/80-stability-contract-migration-docs/80-02-SUMMARY.md` when done
</output>
