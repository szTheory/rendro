---
phase: 80-stability-contract-migration-docs
plan: 03
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - guides/upgrading_to_1.0.md
  - mix.exs
autonomous: true
requirements:
  - STAB-03

must_haves:
  truths:
    - "guides/upgrading_to_1.0.md exists with reassurance-first content and a tier summary"
    - "mix.exs extras list includes guides/upgrading_to_1.0.md"
    - "mix.exs groups_for_extras Policies group includes guides/upgrading_to_1.0.md"
    - "The upgrade guide does NOT deep-link a CHANGELOG anchor for [1.0.0]"
    - "mix test test/docs_contract/ exits 0 after commit"
  artifacts:
    - path: "guides/upgrading_to_1.0.md"
      provides: "New upgrade migration guide"
      contains: "1.0 is a stability commitment, not a rewrite"
    - path: "mix.exs"
      provides: "ExDoc wiring for the new guide under Policies group"
  key_links:
    - from: "mix.exs"
      to: "guides/upgrading_to_1.0.md"
      via: "extras list + groups_for_extras Policies entry"
      pattern: "upgrading_to_1.0"
---

<objective>
Create `guides/upgrading_to_1.0.md` with reassurance-first content (D-11) and wire it into `mix.exs` ExDoc `extras` and the `groups_for_extras` Policies group (D-12/STAB-03). No deep link to the unwritten `## [1.0.0]` CHANGELOG anchor.

Purpose: STAB-03 requires the upgrade guide to exist, be reachable from HexDocs under the Policies group, and give a reader "what 1.0 means for you."

Output: New guide file and updated mix.exs Policies group, committed together. STAB-05 (Plan 80-04) will assert the guide's existence — that plan depends on this one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/80-stability-contract-migration-docs/80-CONTEXT.md
@.planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md
@.planning/phases/80-stability-contract-migration-docs/80-01-SUMMARY.md

<interfaces>
<!-- ExDoc wiring — current state from mix.exs lines 108-132 (verified in RESEARCH.md). -->

Current mix.exs extras (lines 108-117):
  extras: [
    "README.md",
    "guides/integrations.md",
    "guides/branding.md",
    "guides/api_stability.md",
    "guides/viewer_evidence.md",
    "guides/page_primitive.md",
    "guides/recipes.md",
    "guides/user_flows_and_jtbd.md"
  ],

Required after edit (add upgrading_to_1.0.md after api_stability.md):
  extras: [
    "README.md",
    "guides/integrations.md",
    "guides/branding.md",
    "guides/api_stability.md",
    "guides/upgrading_to_1.0.md",
    "guides/viewer_evidence.md",
    "guides/page_primitive.md",
    "guides/recipes.md",
    "guides/user_flows_and_jtbd.md"
  ],

Current mix.exs Policies group (lines 124-127):
  Policies: [
    "guides/api_stability.md",
    "guides/viewer_evidence.md"
  ],

Required after edit:
  Policies: [
    "guides/api_stability.md",
    "guides/upgrading_to_1.0.md",
    "guides/viewer_evidence.md"
  ],

D-12 CHANGELOG link rule: Use "For full change history, see [CHANGELOG.md](../CHANGELOG.md)."
  Do NOT use a fragment anchor like "#100" or "## [1.0.0]" — that section is unwritten until Phase 82.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guides/upgrading_to_1.0.md</name>
  <files>guides/upgrading_to_1.0.md</files>

  <read_first>
    - guides/api_stability.md — the rewritten guide (output of Plan 80-01); the upgrade guide summarizes the tier contract and links to api_stability.md for full details
    - guides/viewer_evidence.md — heading/style conventions (level-1 heading style, section heading style, cross-reference style)
    - .planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md — "guides/upgrading_to_1.0.md (NEW — documentation)" section for exact D-11 content structure
    - .planning/phases/80-stability-contract-migration-docs/80-CONTEXT.md — D-11 (reassurance-first), D-12 (no CHANGELOG anchor), D-13 (NOT covered list reference)
  </read_first>

  <action>
    Create `guides/upgrading_to_1.0.md` as a new file following the style conventions of the existing guides (level-1 heading, `##` sections, no decorative dividers).

    Required content structure per D-11 (exact prose is Claude's discretion; structure is locked):

    1. `# Upgrading to 1.0` — top-level heading.

    2. Opening reassurance paragraph — first sentence MUST be: "1.0 is a stability commitment, not a rewrite — if you're on 0.3.x no code changes are required." (or close equivalent preserving the key claim). State that the library surface is the same; this release formalizes the promise.

    3. `## What 1.0 Means for You` (or similar heading) — two-tier contract summary:
       - Tier-1 Stable: core builder API (`Rendro.Document`, `Rendro.PageTemplate`, `Rendro.Section`, `Rendro.Metadata`, `Rendro.Artifact`, `Rendro.Sign`, `Rendro.Protect`, top-level `Rendro` functions) under strict SemVer. Breaking changes only in 2.0.
       - Tier-2 Evolving: adapters (`Rendro.Adapters.*`) and diagnostics map beyond common keys — additive-only; may follow upstream library majors.
       - Reference `api_stability.md` for the full two-tier contract details (use a relative ExDoc link or a backtick-quoted path per the project's cross-reference convention).

    4. `## What's New Since 0.3.0` (or similar) — short digest of what consolidation delivered per D-11:
       - Viewer evidence and CHANGELOG discipline (v2.3 work: recorded checklists for embedded files, links, protection, long-lived signing viewer posture).
       - Batteries-included workflow features (v2.4 work: Oban/Threadline adapter wiring, branding/recipes updates, adoption-closure features).
       - Formal API tiers and deprecation policy (1.0 stability work: two-tier manifest, docs-contract enforcement lane).
       Keep each item to 1-2 sentences. This is a digest, not a full CHANGELOG.

    5. `## Support Matrix` (or similar) — point to `api_stability.md`'s per-surface boundary sections (the `## Per-Surface Support Boundaries` subordinate section added by Plan 80-01) for detailed viewer support posture.

    6. Generic CHANGELOG link — per D-12: `"For full change history, see [CHANGELOG.md](../CHANGELOG.md)."` No anchor fragment.

    Do NOT introduce any banned overclaim phrases (D-03): no "secure PDF", no "PAdES is supported", no "all signature viewers are supported", no "PDF/A compliant". Do not reference internal milestone/phase labels ("Phase 53", "Phase 71", "Rendro v1.10", "v2.3", "v2.4") — use timeless language ("the 0.3.x → 1.0.0 consolidation", "the 1.0.0 release").
  </action>

  <verify>
    <automated>test -f guides/upgrading_to_1.0.md</automated>
  </verify>

  <acceptance_criteria>
    - `guides/upgrading_to_1.0.md` exists
    - File contains the opening sentence "1.0 is a stability commitment" (or very close equivalent)
    - File contains "Tier-1" and "Tier-2" tier labels
    - File contains a reference to `api_stability.md` (cross-link for tier details)
    - File contains `[CHANGELOG.md](../CHANGELOG.md)` with no anchor fragment
    - File does NOT contain any of: "Phase 53", "Phase 71", "Rendro v1.10", "v2.3 close", "Phase 69"
    - File does NOT contain banned phrases: "secure PDF", "PAdES is supported", "PDF/A compliant"
  </acceptance_criteria>

  <done>
    guides/upgrading_to_1.0.md exists with reassurance-first content per D-11, a two-tier summary, a what's-new digest, a support-matrix pointer, and a generic CHANGELOG link. No internal labels. No banned phrases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire guides/upgrading_to_1.0.md into mix.exs ExDoc extras + Policies group</name>
  <files>mix.exs</files>

  <read_first>
    - mix.exs — lines 108-132; read the exact current extras list and groups_for_extras Policies group before editing
    - .planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md — "mix.exs (MODIFY — ExDoc wiring)" section for exact before/after diff
  </read_first>

  <action>
    Edit `mix.exs` at two locations (both within the `docs/0` function's return value, lines 108-132):

    1. In the `extras:` list (currently lines 108-117): insert `"guides/upgrading_to_1.0.md"` as a new entry AFTER `"guides/api_stability.md"` and BEFORE `"guides/viewer_evidence.md"`.

    2. In the `groups_for_extras:` Policies list (currently lines 124-127): insert `"guides/upgrading_to_1.0.md"` as a new entry AFTER `"guides/api_stability.md"` and BEFORE `"guides/viewer_evidence.md"`.

    No other changes to mix.exs. Do not alter module groups, Guides group, Recipes & Primitives group, or any other section.
  </action>

  <verify>
    <automated>mix docs 2>&1 | grep -c "upgrading_to_1.0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "upgrading_to_1.0" mix.exs` returns two matches (one in extras, one in groups_for_extras Policies)
    - `mix docs` exits 0 (ExDoc renders without error; the new guide is compiled into the docs)
    - `mix test test/docs_contract/` exits 0 (no regression from mix.exs change)
  </acceptance_criteria>

  <done>
    mix.exs extras list and groups_for_extras Policies group both include "guides/upgrading_to_1.0.md". mix docs exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    guides/upgrading_to_1.0.md has been created and mix.exs wires it into the ExDoc Policies group. mix docs was run successfully.
  </what-built>
  <how-to-verify>
    Run `mix docs` then open `doc/upgrading_to_1.0.html` in a browser. Confirm:
    1. The page renders (no 404).
    2. The guide appears in the sidebar under the **Policies** group alongside "API Stability" and "Viewer Evidence".
    3. The opening content reads as reassurance-first ("1.0 is a stability commitment...").
  </how-to-verify>
  <resume-signal>Type "approved" if the guide renders in the Policies group, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose → published HexDocs | New upgrade guide becomes publicly visible; must not contain internal labels or banned claims |
| mix.exs ExDoc config → rendered docs | extras + groups_for_extras determines whether the guide appears and in which sidebar group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-06 | Information Disclosure | guides/upgrading_to_1.0.md internal labels | mitigate | D-04/D-11: no internal phase/version labels; acceptance_criteria grep confirms |
| T-80-07 | Tampering | guides/upgrading_to_1.0.md overclaim prose | mitigate | D-03 banned-phrase rule enforced during authoring; guide does not introduce new claims that require refute guards (no protection/signing claims in an upgrade guide) |
| T-80-08 | Tampering | D-12 CHANGELOG link forward-pointer | mitigate | Link uses `[CHANGELOG.md](../CHANGELOG.md)` with no anchor fragment — anchoring `## [1.0.0]` before Phase 82 writes it would create a broken link; generic link is safe |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — new markdown file + mix.exs list edits |
</threat_model>

<verification>
After both tasks complete and human checkpoint approved:

```
mix test test/docs_contract/
```

Must exit 0.

```
grep "upgrading_to_1.0" mix.exs | wc -l
```

Must output 2 (one extras entry, one Policies entry).

```
test -f guides/upgrading_to_1.0.md && echo "exists"
```

Must output "exists".
</verification>

<success_criteria>
1. guides/upgrading_to_1.0.md exists with reassurance-first content: opening "1.0 is a stability commitment" sentence, two-tier contract summary, what's-new digest, support-matrix pointer, generic CHANGELOG link (STAB-03)
2. mix.exs extras list includes guides/upgrading_to_1.0.md (positioned after api_stability.md)
3. mix.exs groups_for_extras Policies group includes guides/upgrading_to_1.0.md (between api_stability.md and viewer_evidence.md)
4. mix docs exits 0; upgrading_to_1.0.html renders under the Policies sidebar group (human verified)
5. mix test test/docs_contract/ exits 0
</success_criteria>

<output>
Create `.planning/phases/80-stability-contract-migration-docs/80-03-SUMMARY.md` when done
</output>
