---
phase: 80-stability-contract-migration-docs
plan: 04
type: execute
wave: 3
depends_on:
  - 80-01
  - 80-03
files_modified:
  - test/docs_contract/api_stability_claims_test.exs
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - STAB-05
  - STAB-01
  - STAB-02
  - STAB-03

must_haves:
  truths:
    - "test/docs_contract/api_stability_claims_test.exs exists and passes with all five assertion categories"
    - "scripts/verify_docs.exs contains the API stability claims lane as lane 12"
    - "The test's lane self-assertion matches the verify_docs.exs entry byte-for-byte"
    - "Every symbol existence check uses assert with a false-pass guard message"
    - "Rendro.Inspector is NOT asserted as a Tier-1 stable symbol"
    - "mix test test/docs_contract/ exits 0 (all 12 lanes)"
    - "mix docs.contract exits 0"
  artifacts:
    - path: "test/docs_contract/api_stability_claims_test.exs"
      provides: "STAB-05 docs-contract claims test"
      contains: "ApiStabilityClaimsTest"
    - path: "scripts/verify_docs.exs"
      provides: "Lane 12 registration for the new claims test"
      contains: "API stability claims lane"
  key_links:
    - from: "test/docs_contract/api_stability_claims_test.exs"
      to: "guides/api_stability.md"
      via: "File.read! + guide =~ substring assertions"
      pattern: "guide = File.read!"
    - from: "test/docs_contract/api_stability_claims_test.exs"
      to: "scripts/verify_docs.exs"
      via: "lane self-assertion (script =~ lane label)"
      pattern: "script =~ ~s|{\"API stability claims lane\""
    - from: "scripts/verify_docs.exs"
      to: "test/docs_contract/api_stability_claims_test.exs"
      via: "lane entry in lanes list"
      pattern: "api_stability_claims_test.exs"
---

<objective>
Create `test/docs_contract/api_stability_claims_test.exs` (the STAB-05 test) and register it as lane 12 in `scripts/verify_docs.exs` in a single atomic commit. The test proves every named Tier-1 symbol exists/is exported, asserts tier headers and key promise sentences from the rewritten guide, asserts the upgrade guide exists, and self-asserts its own lane registration.

Purpose: STAB-05 closes the lockstep loop — the rewritten guide (Plan 80-01) makes promises; this test mechanically proves those promises are grounded in reality. The lockstep triple (new test + lane registration + lane self-assertion) is the guardrails pattern from Phases 76/79.

Output: Two new/modified files committed atomically. After this commit, `mix docs.contract` runs all 12 lanes and exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/80-stability-contract-migration-docs/80-CONTEXT.md
@.planning/phases/80-stability-contract-migration-docs/80-RESEARCH.md
@.planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md
@.planning/phases/80-stability-contract-migration-docs/80-01-SUMMARY.md
@.planning/phases/80-stability-contract-migration-docs/80-03-SUMMARY.md

<interfaces>
<!-- Analog source: test/docs_contract/protection_claims_test.exs (module declaration, guide read, lane self-assertion idiom)
     Analog source: test/docs_contract/public_api_contract_test.exs (false-pass-guard Code.ensure_loaded? + function_exported? pattern) -->

Module declaration pattern (from protection_claims_test.exs lines 1-2):
  defmodule Rendro.DocsContract.ApiStabilityClaimsTest do
    use ExUnit.Case, async: true

Lane label (byte-identical in test self-assertion AND verify_docs.exs entry):
  "API stability claims lane"

Lane entry to add at end of scripts/verify_docs.exs lanes list (after "Public API contract lane"):
  {"API stability claims lane", ["test", "test/docs_contract/api_stability_claims_test.exs"]}

Symbols to assert via Code.ensure_loaded? (all manifest tier=stable):
  Rendro.Document, Rendro.PageTemplate, Rendro.Section, Rendro.Metadata,
  Rendro.Artifact, Rendro.Sign, Rendro.Protect

Adapter modules to assert via Code.ensure_loaded? (manifest tier=adapter; D-10 includes adapters):
  Rendro.Adapters.PyHanko, Rendro.Adapters.Qpdf

DO NOT assert: Code.ensure_loaded?(Rendro.Inspector) — tier=adapter; D-09 landmine

Functions to assert via function_exported?/3 on Rendro (top-level, stable):
  flow/2, signature_field/2, render_signed/3, render_protected/3

Functions to assert via function_exported?/3 on Rendro.Sign (stable):
  prepare/2, sign/2, augment/2, validate/2

Functions to assert via function_exported?/3 on Rendro.Protect (stable):
  password/2

Struct existence assertion:
  assert match?(%Rendro.Artifact{}, struct(Rendro.Artifact)),
    "%Rendro.Artifact{} struct not present — was it deleted?"

Section headers the rewritten guide ships (assert these exact strings with guide =~):
  "## Tier-1 Stable"
  "## Tier-2 Evolving"
  "## NOT covered by SemVer"
  "## Deprecation Policy"
  "## Deprecations"

Key promise sentence (must appear verbatim in guide):
  "deterministic within a version, not frozen across versions"

Deprecations table sentinel assertions:
  assert guide =~ "| Symbol |"
  assert guide =~ "None as of 1.0.0"

Banned overclaim phrases (refute guards, D-03):
  "secure PDF", "PAdES is supported"

Upgrade guide existence assertion:
  assert File.exists?("guides/upgrading_to_1.0.md"),
    "guides/upgrading_to_1.0.md must exist (STAB-03)"

Lane self-assertion (must match verify_docs.exs entry byte-for-byte):
  script = File.read!("scripts/verify_docs.exs")
  assert script =~
    ~s|{"API stability claims lane", ["test", "test/docs_contract/api_stability_claims_test.exs"]}|
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api_stability_claims_test.exs + register lane 12 in verify_docs.exs</name>
  <files>test/docs_contract/api_stability_claims_test.exs, scripts/verify_docs.exs</files>

  <read_first>
    - guides/api_stability.md — the rewritten guide (Plan 80-01 output); extract the EXACT heading strings and key promise sentences the rewrite shipped so test assertions use byte-identical substrings
    - test/docs_contract/protection_claims_test.exs — module declaration, guide file-read + substring pin pattern, lane self-registration pattern (lines 1-2, 41-66, 90-95)
    - test/docs_contract/public_api_contract_test.exs — false-pass-guarded Code.ensure_loaded? + function_exported? pattern (lines 94-119)
    - scripts/verify_docs.exs — current 11-lane list; locate the last entry to know where to append lane 12
    - priv/public_api.json — confirm Rendro.Inspector tier=adapter before authoring the test (D-09 verification)
    - .planning/phases/80-stability-contract-migration-docs/80-PATTERNS.md — "api_stability_claims_test.exs (NEW — test, docs-contract)" section for full symbol list and pattern excerpts
  </read_first>

  <action>
    Create `test/docs_contract/api_stability_claims_test.exs` with the following structure (assembled from two analogs — protection_claims_test.exs and public_api_contract_test.exs):

    Module declaration:
    `defmodule Rendro.DocsContract.ApiStabilityClaimsTest` with `use ExUnit.Case, async: true`.

    ASSERTION CATEGORY 1 — Guide prose: tier headers and key promise sentences.
    One or more `test` blocks that call `guide = File.read!("guides/api_stability.md")` and assert:
      - `assert guide =~ "## Tier-1 Stable"` (use the EXACT heading string the rewritten guide ships — read the file first)
      - `assert guide =~ "## Tier-2 Evolving"` (same)
      - `assert guide =~ "## NOT covered by SemVer"` (same)
      - `assert guide =~ "## Deprecation Policy"` (same)
      - `assert guide =~ "## Deprecations"` (same)
      - `assert guide =~ "deterministic within a version, not frozen across versions"` (key promise sentence — exact string)
      - `assert guide =~ "| Symbol |"` (Deprecations table header column)
      - `assert guide =~ "None as of 1.0.0"` (Deprecations table sentinel row)
      - `refute guide =~ "secure PDF"` (D-03 banned phrase guard)
      - `refute guide =~ "PAdES is supported"` (D-03 banned phrase guard)

    ASSERTION CATEGORY 2 — Symbol existence (named Tier-1 symbols in the rewritten guide prose).
    One `test` block for symbol existence. Every `Code.ensure_loaded?` and `function_exported?` call MUST be wrapped in `assert` with a descriptive failure message (false-pass-guard rule from D-10 item 1 and PATTERNS.md):

    Modules via Code.ensure_loaded? (stable tier — confirmed per priv/public_api.json):
      Rendro.Document, Rendro.PageTemplate, Rendro.Section, Rendro.Metadata,
      Rendro.Artifact, Rendro.Sign, Rendro.Protect

    Adapter modules via Code.ensure_loaded? (adapter tier — D-10 explicitly includes adapters; assert their existence, NOT their stable-tier status):
      Rendro.Adapters.PyHanko, Rendro.Adapters.Qpdf

    DO NOT assert Code.ensure_loaded?(Rendro.Inspector) — its tier is adapter and the rewritten guide should NOT name it as a Tier-1 stable symbol per D-09.

    Functions via function_exported?/3 (each with assert + failure message):
      Rendro.flow/2, Rendro.signature_field/2, Rendro.render_signed/3, Rendro.render_protected/3
      Rendro.Sign.prepare/2, Rendro.Sign.sign/2, Rendro.Sign.augment/2, Rendro.Sign.validate/2
      Rendro.Protect.password/2

    Struct existence:
      assert match?(%Rendro.Artifact{}, struct(Rendro.Artifact)),
        "%Rendro.Artifact{} struct not present — was it deleted?"

    ASSERTION CATEGORY 3 — Upgrade guide existence (STAB-03, D-10 item 4).
    Separate `test` block:
      assert File.exists?("guides/upgrading_to_1.0.md"),
        "guides/upgrading_to_1.0.md must exist (STAB-03)"

    ASSERTION CATEGORY 4 — Lane self-registration (D-10 item 5, lockstep triple).
    Final `test` block (following the lane self-registration idiom from protection_claims_test.exs:90-95):
      script = File.read!("scripts/verify_docs.exs")
      assert script =~
        ~s|{"API stability claims lane", ["test", "test/docs_contract/api_stability_claims_test.exs"]}|

    Also update `scripts/verify_docs.exs`:
    Append a new lane entry after the current last entry `{"Public API contract lane", ...}`:
      {"API stability claims lane", ["test", "test/docs_contract/api_stability_claims_test.exs"]}
    The lane label string `"API stability claims lane"` must be byte-identical to the string in the test's lane self-assertion above.

    Stage both files together and commit. Run `mix test test/docs_contract/` before committing to confirm all 12 lanes pass.

    CRITICAL: Do NOT use bare `Code.ensure_loaded?` or `function_exported?` calls without assert wrappers. Do NOT assert `Rendro.Inspector` as a Tier-1 symbol. Do NOT use fenced code blocks or positional regex (`~r/.*?/`) for guide assertions.
  </action>

  <verify>
    <automated>mix test test/docs_contract/api_stability_claims_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/docs_contract/api_stability_claims_test.exs` exits 0
    - `mix test test/docs_contract/` exits 0 (all 12 lanes, including the new one)
    - `mix docs.contract` exits 0 (runs scripts/verify_docs.exs with all 12 lanes)
    - `grep "API stability claims lane" scripts/verify_docs.exs` returns one match
    - `grep "API stability claims lane" test/docs_contract/api_stability_claims_test.exs` returns one match (the self-assertion)
    - Both strings are byte-identical
    - `grep "Inspector" test/docs_contract/api_stability_claims_test.exs` returns 0 (Rendro.Inspector must NOT be asserted)
    - `grep -c "assert Code.ensure_loaded\|assert function_exported" test/docs_contract/api_stability_claims_test.exs` returns 9 (7 stable + 2 adapter modules + all function assertions — or count from the actual symbol list)
    - `grep "match?(%Rendro.Artifact{}" test/docs_contract/api_stability_claims_test.exs` returns one match (struct existence assertion)
    - `grep "File.exists.*upgrading_to_1.0" test/docs_contract/api_stability_claims_test.exs` returns one match (upgrade guide existence assertion)
    - `mix ci` exits 0 (compile --warnings-as-errors + full suite; confirms D-16 compliance)
  </acceptance_criteria>

  <done>
    test/docs_contract/api_stability_claims_test.exs exists and passes. scripts/verify_docs.exs has 12 lanes. mix test test/docs_contract/ exits 0. mix docs.contract exits 0. mix ci exits 0. STAB-05 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs-contract test → CI gate | The new claims test is a required lane in verify_docs.exs; if it passes with false-pass bugs, the CI gate provides no protection |
| guide prose → test assertion | Every key promise in the guide must have a corresponding test assertion — missing assertions leave the guide unverified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-09 | Tampering | api_stability_claims_test.exs false-pass guard | mitigate | D-10 item 1: every Code.ensure_loaded? and function_exported? call is wrapped in assert with failure message — a renamed/deleted symbol MUST fail the test, not silently pass; acceptance_criteria verifies assert count |
| T-80-10 | Tampering | Lockstep triple atomicity | mitigate | Both files committed together; lane self-assertion cannot pass if verify_docs.exs lane is missing — the test is self-enforcing |
| T-80-11 | Spoofing | D-09 Inspector landmine — asserting Inspector as Tier-1 | mitigate | DO NOT assert Code.ensure_loaded?(Rendro.Inspector); acceptance_criteria grep confirms Inspector is absent from test assertions; rewritten guide describes diagnostics map keys, not the Inspector module, at Tier-1 |
| T-80-12 | Tampering | Banned overclaim phrases in new test prose | mitigate | refute guards in the test enforce absence of "secure PDF" and "PAdES is supported" from the guide; D-03 rule applied during test authoring |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — new ExUnit test file + verify_docs.exs list append |
</threat_model>

<verification>
After commit:

```
mix test test/docs_contract/
```
Must exit 0 (all 12 lanes including new API stability claims lane).

```
mix docs.contract
```
Must exit 0 (runs scripts/verify_docs.exs, which now runs 12 lanes).

```
mix ci
```
Must exit 0 (compile --warnings-as-errors + test + credo + dialyzer + docs — the phase-level regression gate per VALIDATION.md).

```
grep -c "assert Code.ensure_loaded\|assert function_exported" test/docs_contract/api_stability_claims_test.exs
```
Must return at least 9 (one per asserted symbol: 7 stable modules + 2 adapter modules + Rendro functions + Sign functions + Protect functions — exact count varies by grouping strategy).
</verification>

<success_criteria>
1. test/docs_contract/api_stability_claims_test.exs passes with all five assertion categories: tier headers, key promise sentences (incl. "deterministic within a version, not frozen across versions"), symbol existence (false-pass-guarded), upgrade-guide existence, and lane self-registration (STAB-05)
2. scripts/verify_docs.exs contains the API stability claims lane as lane 12 with label byte-identical to the test's self-assertion
3. Rendro.Inspector is NOT asserted as a Tier-1 stable symbol (D-09 landmine avoided)
4. mix test test/docs_contract/ exits 0 (all 12 lanes green)
5. mix docs.contract exits 0
6. mix ci exits 0 (phase-level regression gate — D-16 compliance: no --warnings-as-errors violation)
</success_criteria>

<output>
Create `.planning/phases/80-stability-contract-migration-docs/80-04-SUMMARY.md` when done
</output>
