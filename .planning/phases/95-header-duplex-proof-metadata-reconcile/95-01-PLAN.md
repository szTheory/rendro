---
phase: 95-header-duplex-proof-metadata-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rendro/flow_test.exs
  - test/rendro/pipeline/paginate_test.exs
autonomous: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "A render-layer test renders header only_on content across a >=4-physical-page document and asserts Odd 1 / Even 2 / Odd 3 / Even 4 are present on the byte-stable content stream with no leaked {{page_number}} token (per D-01: content-stream byte assertions only; Poppler-per-page and golden-bytes rejected)"
    - "A paginate-layer test proves header physical odd/even parity coexists with section-local header/footer tokens under page_numbering: [restart: true]"
    - "First-page parity edge case proven: page 1 is odd"
    - "Single-page edge case proven: only the :odd header appears, :even does not"
    - "Header + footer only_on coexist on the same document via independent region_entries"
  artifacts:
    - path: "test/rendro/flow_test.exs"
      provides: "Header render-layer duplex proof + single-page edge + header/footer coexistence edge"
      contains: "role: :header"
    - path: "test/rendro/pipeline/paginate_test.exs"
      provides: "Header physical parity coexists with restart section tokens at paginate layer"
      contains: "only_on: :odd"
  key_links:
    - from: "test/rendro/flow_test.exs"
      to: "lib/rendro/pipeline/paginate.ex apply_only_on/3"
      via: "Rendro.render/1 content-stream assertion on physical page parity"
      pattern: "\\(Odd 1\\) Tj"
    - from: "test/rendro/pipeline/paginate_test.exs"
      to: "lib/rendro/pipeline/compose.ex region_entries"
      via: "page_texts/1 per-page header+footer block assertion"
      pattern: "HOdd P1"
---

<objective>
Add header-specific `only_on: :odd | :even` end-to-end proof at parity with the footer coverage shipped in v2.7. This is PURELY ADDITIVE test coverage of an already-shipped, region-agnostic engine (`apply_only_on/3`) — it changes NO `lib/` source, NO public API, NO engine behavior.

Purpose: Header odd/even running-content proof depth currently lags the footer coverage. PROOF-01 closes that gap by mirroring the shipped footer proofs at both the render layer (`flow_test.exs`) and the paginate layer (`paginate_test.exs`).

Output: New ExUnit tests in two existing files: a render-layer 4-page header duplex proof plus two edge-case tests (single-page odd-only, header+footer coexistence), and a paginate-layer test proving physical header parity coexists with restart section tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-header-duplex-proof-metadata-reconcile/95-RESEARCH.md

<scope_constraints>
HARD constraints (from CONTEXT.md D-01 and scope guardrails):
- Change NO `lib/` source, NO engine behavior, NO validation thresholds.
- `priv/public_api.json` and `test/docs_contract/public_api_contract_test.exs` MUST stay untouched and green (these tests add no public surface).
- Proof mechanism is content-stream byte assertions ONLY — Poppler-per-page rendering and golden-byte fixtures are REJECTED (D-01).
- Do NOT duplicate the existing `only_on` unit/validation coverage in `compose_test.exs` / `rendro_builders_test.exs`.
- Geometry rule: header region MUST stay strictly above body start (`region.y + region.height <= body.y`). Overlapping the body band subtracts `header_h` from `body_capacity` and breaks the page-count arithmetic.
</scope_constraints>

<interfaces>
<!-- Verified against the live engine. Executor uses these directly — no exploration needed. -->

Builders (all already exist and are used by the analog footer tests):
- Rendro.page_template(name:, width:, height:, margin_top:, margin_right:, margin_bottom:, margin_left:, regions:)
- Rendro.region(name:, role:, anchor:, x:, y:, width:, height:)  # role: :header | :body | :footer; anchor: :top | :flow | :bottom
- Rendro.section(region:, only_on:, content:)  # only_on: :odd | :even
- Rendro.block(...), Rendro.text("..."), Rendro.page_number(format: "...")
- Rendro.flow(blocks, page_template:, page_templates:, sections:)
- Rendro.render/1 returns {:ok, pdf_binary}

Paginate-test helpers (already defined in paginate_test.exs):
- paginate_flow/1  (line 1145) -> {:ok, paginated} where paginated.pages is a list
- page_texts/1     (line 1152) -> list of text strings for a page
- section_context_template/0 (line 1215) -> a %PageTemplate{} with ONLY :body + :footer regions (NO header) — you must add a :header region for the paginate test

Pagination math (verified):
- Default text 12pt, line_height 1.2 = 14.4 pt/line.
- body_capacity = body region height when header/footer do NOT overlap the body band.
- body height 60 -> 60 / 14.4 = 4 lines/page. 16 lines -> exactly 4 physical pages.

Block ordering caveat (VERIFIED — Research Pitfall 2): within a page, page_texts/1 lists HEADER region blocks FIRST, then FOOTER, then BODY. Header shows PHYSICAL page parity; footer S-token shows SECTION-LOCAL numbering under restart.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render-layer header duplex proof (SC #1) + first-page parity (SC #3a)</name>
  <files>test/rendro/flow_test.exs</files>
  <read_first>
    - test/rendro/flow_test.exs (read the footer analog test "only_on odd and even footer sections render on physical page parity" at line 268 — this is the EXACT template to clone)
    - lib/rendro/pipeline/paginate.ex (read apply_only_on/3 at lines 729-739 — the physical-parity filter under proof)
    - lib/rendro/pipeline/measure.ex (read body_capacity logic at lines 483-512 to confirm the no-overlap geometry rule)
    - .planning/phases/95-header-duplex-proof-metadata-reconcile/95-RESEARCH.md (Code Examples > "PROOF-01 render-layer (D-02)" — the verified geometry and assertions)
  </read_first>
  <behavior>
    - Document renders to EXACTLY 4 physical pages (`length(Regex.scan(~r"/Type\s*/Page\b", pdf)) == 4`).
    - Page 1 (odd) header renders "Odd 1"; page 2 (even) renders "Even 2"; page 3 renders "Odd 3"; page 4 renders "Even 4".
    - The raw `{{page_number}}` token never leaks into the content stream (substitution occurred).
  </behavior>
  <action>
    In test/rendro/flow_test.exs, add a new test named `"only_on odd and even header sections render on physical page parity"` by cloning the footer analog at line 268. Change the template `name:` to `:duplex_header_test`. Keep `width: 420, height: 220` and the same margins. Replace the footer region with a HEADER region: `Rendro.region(name: :header, role: :header, anchor: :top, x: 24, y: 24, width: 372, height: 16)` — placed ABOVE the body so its end (y:24 + h:16 = 40) stays below body start y:52, leaving body_capacity = 60 (4 lines/page). Keep the body region `Rendro.region(name: :body, role: :body, anchor: :flow, x: 24, y: 52, width: 372, height: 60)`. Build `odd_header` and `even_header` sections with `region: :header` and `only_on: :odd` / `:even`, content `Rendro.block(Rendro.text("Odd {{page_number}}"))` and `"Even {{page_number}}"` respectively. Bump the body content from `1..8` to `1..16` lines so 16 lines / 4-per-page = exactly 4 pages. After `{:ok, pdf} = Rendro.render(doc)`, assert page count == 4, then assert the content stream contains `"(Odd 1) Tj"`, `"(Even 2) Tj"`, `"(Odd 3) Tj"`, `"(Even 4) Tj"`, and `refute pdf =~ "{{page_number}}"`. Page 1 = odd is the first-page-parity anchor (SC #3a), so no separate test is needed for that sub-criterion. Do NOT rasterize; assert on the content stream byte string only (per D-01).
  </action>
  <verify>
    <automated>mix test test/rendro/flow_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/flow_test.exs` exits 0.
    - The new test asserts `length(Regex.scan(~r"/Type\s*/Page\b", pdf)) == 4`.
    - Content stream contains all of: `(Odd 1) Tj`, `(Even 2) Tj`, `(Odd 3) Tj`, `(Even 4) Tj`.
    - `refute pdf =~ "{{page_number}}"` passes.
    - No change to any `lib/` file; `git diff --name-only` shows only test/rendro/flow_test.exs.
  </acceptance_criteria>
  <done>The render-layer header duplex test exists, renders a 4-physical-page document, and asserts header odd/even parity on the byte-stable content stream with no token leak.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Edge cases — single-page odd-only (SC #3b) + header/footer coexistence (SC #3c)</name>
  <files>test/rendro/flow_test.exs</files>
  <read_first>
    - test/rendro/flow_test.exs (the new header test from Task 1, to reuse the template/section pattern)
    - lib/rendro/pipeline/compose.ex (read region_entries per-region map at lines 117-124 and validate_only_on! at 179-186 — independent region_entries is what makes header+footer coexistence work)
    - .planning/phases/95-header-duplex-proof-metadata-reconcile/95-RESEARCH.md (Code Examples > "PROOF-01 edge cases (D-04)" — all three verified)
  </read_first>
  <behavior>
    - Single-page test: a one-body-block document renders exactly 1 physical page; content stream contains `(Odd 1) Tj` and does NOT contain any `(Even` header.
    - Coexistence test: a 2-page document with BOTH header and footer odd/even sections renders both an odd header and odd footer on page 1, and both an even header and even footer on page 2 — proving header parity and footer parity do not interfere (independent region_entries keys).
  </behavior>
  <action>
    In test/rendro/flow_test.exs add TWO tests. (a) `"single-page header only_on renders odd header only"`: reuse the `:duplex_header_test` template shape but with a single body block (`[Rendro.block(Rendro.text("Solo"))]`) and the same odd_header/even_header sections; assert `length(Regex.scan(~r"/Type\s*/Page\b", pdf)) == 1`, `assert pdf =~ "(Odd 1) Tj"`, `refute pdf =~ "(Even"`. (b) `"header and footer only_on coexist via independent region_entries"`: define a template with BOTH a header region (`x: 24, y: 24, width: 372, height: 16`) and a footer region (`x: 24, y: 190, width: 372, height: 16`) — both outside the body band (body `y: 52, height: 60` spanning y:52..112). Add four sections: `odd_header`/`even_header` (region: :header, content `Rendro.text("HOdd {{page_number}}")` / `"HEven {{page_number}}"`) and `odd_footer`/`even_footer` (region: :footer, content `"FOdd {{page_number}}"` / `"FEven {{page_number}}"`). Use a 2-page body (`1..8` lines / 4-per-page = 2 pages). Assert `pdf =~ "(HOdd 1) Tj"` AND `pdf =~ "(FOdd 1) Tj"` (page 1 both odd) and `pdf =~ "(HEven 2) Tj"` AND `pdf =~ "(FEven 2) Tj"` (page 2 both even), plus `refute pdf =~ "{{page_number}}"`. This proves header and footer parity are driven by independent region_entries and do not interfere.
  </action>
  <verify>
    <automated>mix test test/rendro/flow_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/flow_test.exs` exits 0.
    - Single-page test: asserts page count == 1, `(Odd 1) Tj` present, `(Even` absent.
    - Coexistence test: asserts `(HOdd 1) Tj`, `(FOdd 1) Tj`, `(HEven 2) Tj`, `(FEven 2) Tj` all present and `{{page_number}}` refuted.
    - No change to any `lib/` file; `git diff --name-only` shows only test/rendro/flow_test.exs.
  </acceptance_criteria>
  <done>Both render-layer edge-case tests exist and pass: single-page proves odd-only; coexistence proves header and footer parity are independent (SC #3b, #3c).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Paginate-layer header parity + restart section-token coexistence (SC #2)</name>
  <files>test/rendro/pipeline/paginate_test.exs</files>
  <read_first>
    - test/rendro/pipeline/paginate_test.exs (read the analog "page_numbering restart … substitutes section tokens" test at line 685, the section_context_template/0 helper at 1215, and paginate_flow/1 at 1145, page_texts/1 at 1152)
    - lib/rendro/pipeline/paginate.ex (read apply_page_template/5 at 558-573 and running_entry_blocks/4 at 625-632 — confirms per-region iteration and the header-first block ordering)
    - .planning/phases/95-header-duplex-proof-metadata-reconcile/95-RESEARCH.md (Code Examples > "PROOF-01 paginate-layer (D-03)" — verified 3-page geometry, sections, and the page_texts ordering caveat)
  </read_first>
  <behavior>
    - A 3-physical-page document where a body section restarts numbering produces, per page via page_texts/1: page 1 header "HOdd P1" + footer section-local "S1/1" + body "Intro"; page 2 header "HEven P2" + footer "S1/2" + bodies "Appendix 1"/"Appendix 2"; page 3 header "HOdd P3" + footer "S2/2" + body "Appendix 3".
    - Header shows PHYSICAL page parity (HOdd/HEven on physical odd/even pages); footer S-token shows SECTION-LOCAL numbering under restart — the two coexist without interference.
  </behavior>
  <action>
    In test/rendro/pipeline/paginate_test.exs (inside the existing "running-region stubs (Wave 0)" describe block, near the restart analog at line 685), add a test named `"header only_on physical parity coexists with restart section tokens"`. Build a local `%PageTemplate{}` named `:section_context_header` mirroring `section_context_template/0` BUT adding a `:header` region: `%Region{name: :header, role: :header, anchor: :top, x: 24, y: 24, width: 372, height: 20}`, with `:body` (`x: 24, y: 52, width: 372, height: 30`) and `:footer` (`x: 24, y: 200, width: 600, height: 20`); template `width: 420, height: 240`, margins `margin_top: 20, margin_right: 24, margin_bottom: 28, margin_left: 24`. Define sections: `odd_header` / `even_header` (region: :header, only_on: :odd/:even, content `Rendro.page_number(format: "HOdd P{{page_number}}")` / `"HEven P{{page_number}}"`); `footer` (region: :footer, content `Rendro.page_number(format: "S{{section_page_number}}/{{section_total_pages}}")`); `appendix` (name: :appendix, region: :body, page_numbering: [restart: true], content `for(i <- 1..3, do: Rendro.block(Rendro.text("Appendix #{i}")))`). Build the flow with body `[Rendro.block(Rendro.text("Intro"))]`, `page_template: :section_context_header`, `page_templates: [template]`, `sections: [appendix, odd_header, even_header, footer]`. Assert `{:ok, paginated} = paginate_flow(doc)`, `length(paginated.pages) == 3`. Respecting the VERIFIED page_texts/1 ordering (header, then footer, then body), assert: `page_texts(page1) == ["HOdd P1", "S1/1", "Intro"]`; `page_texts(page2) == ["HEven P2", "S1/2", "Appendix 1", "Appendix 2"]`; `page_texts(page3) == ["HOdd P3", "S2/2", "Appendix 3"]`. Reuse the existing %PageTemplate{}/%Region{} aliases already imported in this test file.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0.
    - The new test asserts `length(paginated.pages) == 3` and the three full page_texts/1 lists exactly as above (header physical parity coexisting with section-local footer tokens).
    - No change to any `lib/` file; `git diff --name-only` shows only test/rendro/pipeline/paginate_test.exs.
  </acceptance_criteria>
  <done>The paginate-layer test exists and proves header physical odd/even parity coexists with restart section-local footer tokens across a 3-page document (SC #2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces NO new trust boundary. It adds ExUnit test code that exercises an already-shipped, region-agnostic engine code path. No new input handling, I/O, network, auth, session, access control, cryptography, or data persistence is introduced or modified. `priv/public_api.json` and the public-API contract test stay untouched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | new test files (test/rendro/flow_test.exs, paginate_test.exs) | accept | No new attack surface; test-only code, no runtime behavior change, no public API surface change. No threats at or above `high` severity introduced. |
| T-95-SC | Tampering | package installs | accept | Phase installs NO packages (Research "Standard Stack": no new dependencies). No package-legitimacy gate required. |
</threat_model>

<verification>
- `mix test test/rendro/flow_test.exs test/rendro/pipeline/paginate_test.exs` exits 0 (all new tests pass).
- `mix test` (full suite) green.
- `mix format --check-formatted` passes.
- `mix test test/docs_contract/public_api_contract_test.exs` stays green (no public surface change).
- `git diff --name-only` shows ONLY the two test files touched — no `lib/`, no `priv/`.
</verification>

<success_criteria>
- SC #1: Render-layer 4-physical-page header duplex test asserts `(Odd 1)`/`(Even 2)`/`(Odd 3)`/`(Even 4)` on the content stream with no `{{page_number}}` leak.
- SC #2: Paginate-layer test proves header physical parity coexists with restart section-local tokens (3 pages, exact page_texts lists).
- SC #3: All three edge cases covered — first-page parity (page 1 = odd, via Task 1), single-page odd-only, header+footer coexistence via independent region_entries.
</success_criteria>

<output>
Create `.planning/phases/95-header-duplex-proof-metadata-reconcile/95-01-SUMMARY.md` when done.
</output>
</output>
