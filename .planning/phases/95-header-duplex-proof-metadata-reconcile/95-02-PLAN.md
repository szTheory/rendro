---
phase: 95-header-duplex-proof-metadata-reconcile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v2.7-phases/90-duplex-running-content/90-VALIDATION.md
  - .planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md
  - .planning/milestones/v2.7-phases/92-docs-claims-release-hygiene/92-VALIDATION.md
autonomous: true
requirements: [META-01]

must_haves:
  truths:
    - "90-VALIDATION.md no longer shows any false-pending task-status cells while declaring status: approved / nyquist_compliant: true (per D-06: flip pending->passed in 90/92, reconciling to the existing passed token)"
    - "92-VALIDATION.md no longer shows any false-pending task-status cells while declaring status: approved / nyquist_compliant: true (per D-06; reconcile to existing passed token)"
    - "91-VALIDATION.md no longer declares a stale **Status:** Planned header (its milestone audit is passed); per D-06 the Status: Planned heading is flipped to passed"
    - "88-VALIDATION.md is left entirely untouched per D-06 (88 excluded — its draft markers are internally consistent; flipping would manufacture a new contradiction)"
    - "No frontmatter status: field is changed in any file (per D-07: do NOT flip frontmatter status:; reconcile only in-file table/heading markers); per D-08 git diff is scoped strictly to v2.7 archived phase folders and touches only v2.7 VALIDATION.md files (no v1.x/v2.5/current files)"
  artifacts:
    - path: ".planning/milestones/v2.7-phases/90-duplex-running-content/90-VALIDATION.md"
      provides: "Reconciled Per-Task Verification Map (pending -> passed)"
      contains: "passed"
    - path: ".planning/milestones/v2.7-phases/92-docs-claims-release-hygiene/92-VALIDATION.md"
      provides: "Reconciled Per-Task Verification Map (pending -> passed)"
      contains: "passed"
    - path: ".planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md"
      provides: "Reconciled status header (Planned -> passed)"
      contains: "passed"
  key_links:
    - from: ".planning/milestones/v2.7-phases/90-duplex-running-content/90-VALIDATION.md"
      to: ".planning/milestones/v2.7-MILESTONE-AUDIT.md (passed)"
      via: "intra-file consistency between status: approved frontmatter and task-status cells"
      pattern: "\\| pending \\|"
    - from: ".planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md"
      to: ".planning/milestones/v2.7-MILESTONE-AUDIT.md (passed)"
      via: "Status header reflecting terminal passed state"
      pattern: "Status:.*Planned"
---

<objective>
Reconcile the stale, false-pending status markers inside the archived v2.7 `*-VALIDATION.md` files so each file's recorded in-file status is internally consistent with its own `status: approved` / `nyquist_compliant: true` frontmatter and the already-`passed` v2.7 milestone audit (META-01).

Purpose: 90 and 92 declare themselves `approved` / `nyquist_compliant: true` in frontmatter yet their Per-Task Verification Map still shows `pending` cells — a genuine intra-file contradiction. 91 carries a stale `**Status:** Planned` header despite a `passed` milestone audit. Flipping these in place (D-05) makes each record truthful at the point a reader looks.

Output: Three edited VALIDATION.md files (markdown only). No code, no CI, no contract, no frontmatter `status:` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-header-duplex-proof-metadata-reconcile/95-RESEARCH.md

<critical_research_corrections>
The research (HIGH confidence, verified by direct file inspection) CORRECTED the CONTEXT.md D-06 target list. Where research and CONTEXT.md disagree, FOLLOW RESEARCH:

1. **88-VALIDATION.md — DO NOT TOUCH.** CONTEXT.md D-06 listed it as a 5-cell target, but its entire frontmatter is `status: draft` / `nyquist_compliant: false` / `wave_0_complete: false` with `**Approval:** pending` and all checkboxes unchecked. Its `pending` cells are INTERNALLY CONSISTENT with a draft Nyquist planning artifact (the v2.6 audit line 39 explicitly documents this). Flipping only its cells would MANUFACTURE the exact contradiction D-05 forbids. Leave 88 entirely untouched. It is not even in this plan's files_modified.
2. **Leave ALL frontmatter `status:` fields untouched (D-07 RESOLVED).** A full census shows NO milestone-wide terminal `status:` convention (`verified`/`complete`/`approved`/`draft` all coexist among audited-passed phases; `draft` sits on done current phases 93/94). Reconcile ONLY in-file table cells / the Status header.
3. **Terminal token is `passed`** — the dominant trailing-cell token (87-VALIDATION.md uses it in all rows). Do NOT invent a new token.
</critical_research_corrections>

<scope_constraints>
HARD constraints (CONTEXT.md D-08):
- Scope is strictly the v2.7 archived phase folders named in files_modified. Do NOT touch v1.x/v2.5 archives, v2.6 files, the current-milestone files (93/94/95/96), or any non-VALIDATION metadata.
- The genuine D-06 contradictions verified to exist are confined to v2.7 (90, 92, 91). v2.6 files 83/84/88 are NOT D-06 targets (88 is a consistent draft per correction #1; 83/84 fall under the D-07 frontmatter gate, out of scope).
- No code, CI, contract, or frontmatter changes.
</scope_constraints>

<verified_targets>
<!-- Exact line numbers VERIFIED by direct file read this planning session. -->
- 90-VALIDATION.md: frontmatter `status: approved` (line 4), `nyquist_compliant: true` (line 5). Three `pending` cells in the Per-Task Verification Map at lines 35, 36, 37 (the trailing `| pending |` column on tasks 90-01-01/02/03).
- 92-VALIDATION.md: frontmatter `status: approved` (line 4), `nyquist_compliant: true` (line 5). Four `pending` cells in the Per-Task Verification Map at lines 36, 37, 38, 39 (tasks 92-01-01/02, 92-02-01, 92-03-01).
- 91-VALIDATION.md: NO frontmatter. Header `**Status:** Planned` at line 4. No per-task status column (it is a plan-time Validation Map, 0 task cells).
</verified_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile 90-VALIDATION.md (3 false-pending cells -> passed)</name>
  <files>.planning/milestones/v2.7-phases/90-duplex-running-content/90-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v2.7-phases/90-duplex-running-content/90-VALIDATION.md (read in full BEFORE editing — confirm frontmatter status: approved / nyquist_compliant: true and the three pending cells at lines 35-37)
    - .planning/milestones/v2.6-phases/87-comparison-page-livebook/87-VALIDATION.md (read trailing task cells to confirm `passed` is the existing terminal token)
  </read_first>
  <action>
    In the Per-Task Verification Map table, flip the trailing `Status` column value `pending` -> `passed` on exactly the three task rows at lines 35, 36, 37 (tasks 90-01-01, 90-01-02, 90-01-03). Change ONLY the final `| pending |` cell on each of those three rows. Do NOT touch the frontmatter `status: approved` (line 4) or any other `status:` field. Do NOT touch the legend line, any prose, or any other cell. The result: a file declaring `status: approved` / `nyquist_compliant: true` whose task table now consistently reads `passed`.
  </action>
  <verify>
    <automated>test "$(grep -v '^#' .planning/milestones/v2.7-phases/90-duplex-running-content/90-VALIDATION.md | grep -c '| pending |')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - No `| pending |` task-status cell remains in 90-VALIDATION.md.
    - Frontmatter `status: approved` (line 4) is UNCHANGED.
    - `git diff` for this file shows only the three trailing `pending`->`passed` cell flips — no other lines.
  </acceptance_criteria>
  <done>90-VALIDATION.md is intra-file consistent: its approved/nyquist-compliant frontmatter no longer contradicts any pending task cell.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile 92-VALIDATION.md (4 false-pending cells -> passed)</name>
  <files>.planning/milestones/v2.7-phases/92-docs-claims-release-hygiene/92-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v2.7-phases/92-docs-claims-release-hygiene/92-VALIDATION.md (read in full BEFORE editing — confirm frontmatter status: approved / nyquist_compliant: true and the four pending cells at lines 36-39)
  </read_first>
  <action>
    In the Per-Task Verification Map table, flip the trailing `Status` column value `pending` -> `passed` on exactly the four task rows at lines 36, 37, 38, 39 (tasks 92-01-01, 92-01-02, 92-02-01, 92-03-01). Change ONLY the final `| pending |` cell on each of those four rows. Do NOT touch the frontmatter `status: approved` (line 4) or any other `status:` field, prose, legend, or other cells.
  </action>
  <verify>
    <automated>test "$(grep -v '^#' .planning/milestones/v2.7-phases/92-docs-claims-release-hygiene/92-VALIDATION.md | grep -c '| pending |')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - No `| pending |` task-status cell remains in 92-VALIDATION.md.
    - Frontmatter `status: approved` (line 4) is UNCHANGED.
    - `git diff` for this file shows only the four trailing `pending`->`passed` cell flips — no other lines.
  </acceptance_criteria>
  <done>92-VALIDATION.md is intra-file consistent: its approved/nyquist-compliant frontmatter no longer contradicts any pending task cell.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile 91-VALIDATION.md stale Status header (Planned -> passed)</name>
  <files>.planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md (read in full BEFORE editing — confirm there is NO frontmatter and the stale `**Status:** Planned` header at line 4; confirm there is no per-task status column)
    - .planning/milestones/v2.7-MILESTONE-AUDIT.md (confirm Phase 91 verdict is passed — the truth to reconcile against)
  </read_first>
  <action>
    Change the header line `**Status:** Planned` (line 4) to `**Status:** passed`. This is the file's only stale status marker (it is a plan-time Validation Map with no per-task status column, so there are no cells to flip). Do NOT add frontmatter, do NOT alter any other line, prose, or the Requirements line.
  </action>
  <verify>
    <automated>grep -q '^\*\*Status:\*\* passed' .planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md && ! grep -q 'Status:\*\* Planned' .planning/milestones/v2.7-phases/91-pdf-js-advisory-proof-lane/91-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Line 4 reads `**Status:** passed`; no `**Status:** Planned` remains.
    - No frontmatter was added; `git diff` for this file shows only the single header-line change.
  </acceptance_criteria>
  <done>91-VALIDATION.md's terminal status header matches its passed milestone audit; no stale Planned marker remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces NO new trust boundary. It edits archived internal planning markdown (`*-VALIDATION.md`) under `.planning/`. No `lib/` source, no I/O, no API surface, no auth, no data handling, no runtime behavior. The edited files are not loaded by any runtime path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02 | Tampering | archived VALIDATION.md metadata | accept | Edits are git-reversible (D-05), confined to three named v2.7 files, and change only status markers. No new attack surface; no threats at or above `high` severity introduced. |
</threat_model>

<verification>
- `grep -v '^#' <file> | grep -c '| pending |'` returns 0 for both 90 and 92.
- 91 header reads `**Status:** passed`; no `Planned` remains.
- 88-VALIDATION.md is UNCHANGED (not in files_modified; confirm via `git status`).
- No frontmatter `status:` field changed in any file.
- `git diff --name-only` shows ONLY the three v2.7 VALIDATION.md files — no v2.6 files, no v1.x/v2.5, no current-milestone files, no code.
</verification>

<success_criteria>
- SC #4 (partial — v2.7 reconcile): 90 and 92 no longer carry false-pending task cells; 91 no longer carries a stale `Planned` status; each file's recorded in-file status is consistent with its `passed` milestone audit. 88 is correctly left untouched (consistent draft; flipping would create a new contradiction). No frontmatter `status:` is altered, preserving cross-archive consistency.
</success_criteria>

<output>
Create `.planning/phases/95-header-duplex-proof-metadata-reconcile/95-02-SUMMARY.md` when done.
</output>
</output>
