---
phase: 93-recipes-facade-dx-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes.ex
autonomous: true
requirements:
  - DX-01
  - DX-02

must_haves:
  truths:
    - "Rendro.Recipes exposes invoice/1, invoice/2, branded_invoice/1, branded_invoice/2, statement/1, statement/2, receipt/1, receipt/2, certificate/1, certificate/2 — exactly 10 functions"
    - "All arity-1 wrappers delegate to the facade's own arity-2 (invoice(data, [])), never directly to the recipe module"
    - "All arity-2 wrappers call Module.document(data, opts) — opts pass through verbatim"
    - "All 10 functions carry @spec and a real (non-@doc false) docstring"
    - "The opts-drop footgun in the existing invoice/1 and branded_invoice/1 is fixed"
  artifacts:
    - path: "lib/rendro/recipes.ex"
      provides: "10-function facade: 5 recipes × {/1, /2}, all @spec'd"
      contains: "def statement(data, opts)"
      min_lines: 90
  key_links:
    - from: "lib/rendro/recipes.ex"
      to: "Rendro.Recipes.Invoice.document/2"
      via: "arity-2 wrapper delegation"
      pattern: "Rendro\\.Recipes\\.Invoice\\.document\\(data, opts\\)"
    - from: "lib/rendro/recipes.ex"
      to: "Rendro.Recipes.Statement.document/2"
      via: "arity-2 wrapper delegation"
      pattern: "Rendro\\.Recipes\\.Statement\\.document\\(data, opts\\)"
---

<objective>
Expand lib/rendro/recipes.ex with 8 new @spec'd function definitions, fix the opts-drop footgun in the two existing functions.

Purpose: Deliver the 10-function facade surface that makes all drift test assertions GREEN. This is the core implementation of DX-01: every shipped recipe reachable from Rendro.Recipes as name/1 and name/2 with transparent opts threading.

Output:
- lib/rendro/recipes.ex — rewritten to contain 10 hand-written @spec'd functions (5 recipes × arity-1 + arity-2), replacing the 2 footgun functions with correct versions and adding 8 new ones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-recipes-facade-dx-closure/93-CONTEXT.md
@.planning/phases/93-recipes-facade-dx-closure/93-RESEARCH.md
@.planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md
@.planning/phases/93-recipes-facade-dx-closure/93-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts. Executor must understand the CURRENT file state before editing. -->

Current lib/rendro/recipes.ex (32 lines, full content from PATTERNS.md):
```elixir
defmodule Rendro.Recipes do
  @moduledoc """
  Canonical PDF recipes for standard document types.
  ...
  """
  @moduledoc tags: [:stable]

  @doc "..."
  @spec invoice(map()) :: Rendro.Document.t()
  def invoice(data) do
    Rendro.Recipes.Invoice.document(data)    # FOOTGUN: opts dropped (must fix)
  end

  @doc "..."
  @spec branded_invoice(map()) :: Rendro.Document.t()
  def branded_invoice(data) do
    Rendro.Recipes.BrandedInvoice.document(data)    # FOOTGUN: opts dropped (must fix)
  end
end
```

Delegation targets (all confirmed document/2 present):
- Rendro.Recipes.Invoice.document(data, opts)         lib/rendro/recipes/invoice.ex:136
- Rendro.Recipes.BrandedInvoice.document(data, opts)  lib/rendro/recipes/branded_invoice.ex:115
- Rendro.Recipes.Statement.document(data, opts)        lib/rendro/recipes/statement.ex:236
- Rendro.Recipes.Receipt.document(data, opts)          lib/rendro/recipes/receipt.ex:228
- Rendro.Recipes.Certificate.document(data, opts)      lib/rendro/recipes/certificate.ex:225

Return type for all specs: Rendro.Document.t()

Required arity-1→arity-2 shape (from PATTERNS.md + D-01/D-03/D-04):
  @spec name(map()) :: Rendro.Document.t()
  def name(data), do: name(data, [])              # calls facade's OWN arity-2

  @spec name(map(), keyword()) :: Rendro.Document.t()
  def name(data, opts), do: Rendro.Recipes.Module.document(data, opts)   # explicit opts

Alpha-sorted order for the 10 functions (matches what mix rendro.api.gen will produce):
  branded_invoice/1, branded_invoice/2, certificate/1, certificate/2,
  invoice/1, invoice/2, receipt/1, receipt/2, statement/1, statement/2
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite lib/rendro/recipes.ex with 10 @spec'd facade functions</name>
  <files>lib/rendro/recipes.ex</files>
  <read_first>
    - lib/rendro/recipes.ex — full current file (32 lines) — MUST read before editing
    - lib/rendro/recipes/invoice.ex lines 130–145 — confirm document/2 signature at line 136
    - lib/rendro/recipes/statement.ex lines 230–245 — confirm document/2 signature at line 236
    - lib/rendro/recipes/certificate.ex lines 220–230 — confirm document/2 signature at line 225
    - .planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md — "lib/rendro/recipes.ex" section: current file verbatim, target pattern, delegation targets table
    - .planning/phases/93-recipes-facade-dx-closure/93-CONTEXT.md — D-01, D-02, D-03, D-04, D-05, D-11 (spec/doc requirements before regen)
    - test/rendro/recipes_facade_drift_test.exs — read to understand what assertions the drift test makes (failing tests are the spec)
  </read_first>
  <behavior>
    - invoice/1 delegates to invoice/2: apply(Rendro.Recipes, :invoice, [data, []]) ≡ Rendro.Recipes.Invoice.document(data, [])
    - invoice/2 passes opts verbatim: Rendro.Recipes.invoice(data, [sentinel: :val]) == Rendro.Recipes.Invoice.document(data, [sentinel: :val])
    - statement/2 passes opts: Rendro.Recipes.statement(data, [labels: %{balance: "Saldo"}]) == Rendro.Recipes.Statement.document(data, [labels: %{balance: "Saldo"}])
    - Rendro.Recipes.__info__(:functions) returns exactly 10 tuples: {branded_invoice,1}, {branded_invoice,2}, {certificate,1}, {certificate,2}, {invoice,1}, {invoice,2}, {receipt,1}, {receipt,2}, {statement,1}, {statement,2}
    - function_exported?(Rendro.Recipes, :statement, 1) == true
    - function_exported?(Rendro.Recipes, :certificate, 2) == true
    - All 10 functions have @spec (required by public_api_contract_test.exs Assertion 5 for stable tier)
    - All 10 functions have a real docstring — not @doc false (required by mix rendro.api.gen to surface them in the manifest)
  </behavior>
  <action>
    Read lib/rendro/recipes.ex fully before editing. Then rewrite the file.

    Preserve the existing @moduledoc (the prose description of the module), and preserve @moduledoc tags: [:stable].

    Replace the two existing FOOTGUN functions (invoice/1 and branded_invoice/1 each calling Module.document(data)) with correct arity-1 + arity-2 pairs. Add 3 new recipe pairs: statement, receipt, certificate.

    Write all 10 functions in alpha-sorted order (per D-12 and contract generator expectations):
      branded_invoice/1 + branded_invoice/2
      certificate/1 + certificate/2
      invoice/1 + invoice/2
      receipt/1 + receipt/2
      statement/1 + statement/2

    For each recipe pair, the pattern is (per D-01, D-03, D-04):
      @doc string (non-empty, non-@doc-false) — arity-1 should note it delegates to name/2 which threads opts; arity-2 should note it delegates to Rendro.Recipes.Module.document/2 and point callers to that module's ## Options section for accepted keys. Do NOT duplicate per-key option docs in the facade — per D-05/D-06.
      @spec name(map()) :: Rendro.Document.t()
      def name(data), do: name(data, [])                              # arity-1 → facade's OWN arity-2

      @doc string (non-empty)
      @spec name(map(), keyword()) :: Rendro.Document.t()
      def name(data, opts), do: Rendro.Recipes.Module.document(data, opts)   # arity-2 → recipe module

    CRITICAL rule (D-01, Pitfall 1 in RESEARCH.md): The arity-1 wrapper MUST call the facade's own arity-2 (e.g., invoice(data, [])), NOT Rendro.Recipes.Invoice.document(data). The arity-2 wrapper MUST call Module.document(data, opts) with explicit opts, NOT Module.document(data).

    FORBIDDEN patterns (per D-02): defdelegate, Kernel.defdelegate, macro loops, for-comprehension generating function defs.

    NO opts validation, NO NimbleOptions, NO defaults at facade level (per D-04, D-05). The recipe modules own opts.

    After writing, verify compilation: mix compile --warnings-as-errors (the new functions should compile cleanly).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && mix test test/rendro/recipes_facade_drift_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 (no compilation errors or warnings)
    - `mix test test/rendro/recipes_facade_drift_test.exs` exits 0 (ALL drift test assertions GREEN after facade expansion)
    - `mix test test/rendro/recipes/ test/docs_contract/` exits 0 (no regressions — note: public_api_contract_test.exs will FAIL at this stage because mix rendro.api.gen has not been run yet — this is expected RED for Plan 03 to fix; the recipe tests must pass)
    - `Rendro.Recipes.__info__(:functions) |> length()` returns 10 (verify via: mix run --no-start -e "IO.inspect(Rendro.Recipes.__info__(:functions))" or equivalent)
    - lib/rendro/recipes.ex contains def statement(data, opts) (grep confirms arity-2 for statement)
    - lib/rendro/recipes.ex contains def certificate(data, opts) (grep confirms arity-2 for certificate)
    - lib/rendro/recipes.ex does NOT contain defdelegate (grep -c "defdelegate" returns 0)
    - lib/rendro/recipes.ex contains @spec for all 10 functions: grep -c "@spec" lib/rendro/recipes.ex returns 10
    - The four drift test assertions (reachability, no-extra-fns, struct byte-identity, orphan sweep) all pass
    - The opts-threading regression block (5 tests) all pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Rendro.Recipes.name/2 | Opts keyword list passed in; forwarded verbatim to recipe module |
| Rendro.Recipes.name/2 → Rendro.Recipes.Module.document/2 | Thin delegation; no transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-03 | Tampering | lib/rendro/recipes.ex facade opts | None | accept | Opts are passed verbatim to recipe module. Recipe modules are the single opts authority via Keyword.get/3, tolerant of unknown keys. No injection surface: keyword lists cannot carry executable payloads in this context (no eval, no macro expansion). |
| T-93-04 | Information Disclosure | @spec public surface | None | accept | All new specs are intentional public-surface declarations on a :stable-tagged module. No secrets exposed. |
| T-93-05 | Denial of Service | facade arity-1 infinite recursion | None | accept | invoice/1 calls invoice/2 (not invoice/1) — there is no recursion path. The arity-2 wrapper calls Module.document/2 which is a different module. |
| T-93-SC | Tampering | npm/pip/cargo installs | N/A | accept | No new packages installed. Pure Elixir source changes only. Package legitimacy audit confirmed: no new deps. |

**Summary:** This plan adds 8 function definitions and rewrites 2 existing ones in a pure delegation module. No network surface, no auth, no user input parsing, no persistence, no new dependencies. Transparent opts pass-through with recipe modules as the sole opts authority. The public-API contract surface is guarded by the byte-compare contract test (Signal C, addressed in Plan 03) — additive-only change, no Tier-1 symbol replaced. Severity: none/low throughout.
</threat_model>

<verification>
Run after task completes:

1. `mix compile --warnings-as-errors` — exits 0
2. `mix test test/rendro/recipes_facade_drift_test.exs` — exits 0 (all 9 tests GREEN; if Plan 01 has run in parallel and the drift test file exists)
3. `grep -c "@spec" lib/rendro/recipes.ex` — returns 10
4. `grep -c "defdelegate" lib/rendro/recipes.ex` — returns 0
5. `grep "def statement\|def receipt\|def certificate" lib/rendro/recipes.ex` — shows both /1 and /2 for each
6. `mix test test/rendro/recipes/` — exits 0 (per-recipe tests unaffected)
7. `mix test test/docs_contract/public_api_contract_test.exs` — will FAIL (expected RED; priv/public_api.json stale with 2 entries vs 10 new facade functions) — do NOT block on this; it is Plan 03's job
</verification>

<success_criteria>
- lib/rendro/recipes.ex contains exactly 10 public functions (5 recipes × arity-1 + arity-2), all @spec'd with real docstrings
- All arity-1 wrappers call the facade's own arity-2 (structural invariant: name(data) ≡ name(data, []))
- All arity-2 wrappers call Module.document(data, opts) explicitly (no opts drop)
- drift test (test/rendro/recipes_facade_drift_test.exs) exits 0 — all 9 assertions GREEN
- Full suite `mix test` exits 0 except public_api_contract_test.exs (expected failure until Plan 03 runs mix rendro.api.gen)
</success_criteria>

<output>
Create .planning/phases/93-recipes-facade-dx-closure/93-02-SUMMARY.md when done.
</output>
