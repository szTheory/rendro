---
phase: 93-recipes-facade-dx-closure
plan: 03
type: execute
wave: 2
depends_on:
  - "93-02"
files_modified:
  - priv/public_api.json
  - guides/api_stability.md
autonomous: true
requirements:
  - DX-01
  - DX-02

must_haves:
  truths:
    - "priv/public_api.json Elixir.Rendro.Recipes.functions grows from 2 entries to 10 entries (additive-only diff)"
    - "public_api_contract_test.exs byte-compare assertion passes (fresh manifest == checked-in manifest)"
    - "Stable-tier @spec coverage assertion passes for all 10 new Rendro.Recipes functions"
    - "No other module in priv/public_api.json is touched by the regeneration"
    - "D-13: guides/api_stability.md notes that the Rendro.Recipes facade now exposes statement/receipt/certificate plus arity-2 opts forms"
    - "The full mix test suite exits 0"
  artifacts:
    - path: "priv/public_api.json"
      provides: "Updated contract with 10-entry Elixir.Rendro.Recipes.functions array"
      contains: "statement/2"
  key_links:
    - from: "lib/mix/tasks/rendro/api.gen.ex"
      to: "priv/public_api.json"
      via: "mix rendro.api.gen mechanical regeneration"
      pattern: "statement/[12]"
    - from: "test/docs_contract/public_api_contract_test.exs"
      to: "priv/public_api.json"
      via: "byte-compare assertion (Assertion 2)"
      pattern: "fresh_json == checked_in"
---

<objective>
Regenerate priv/public_api.json to reflect the 10-function facade surface and verify the additive contract diff passes.

Purpose: Close the final DX-01/DX-02 gap: the public-API contract must record all 10 new @spec'd facade functions. This is a mechanical step — run mix rendro.api.gen, verify the diff is additive-only (8 new + lines, 0 - lines in Elixir.Rendro.Recipes.functions, no other module touched), then confirm public_api_contract_test.exs exits 0.

Output:
- priv/public_api.json — regenerated (mechanical, via mix rendro.api.gen only — never hand-edited), with Elixir.Rendro.Recipes.functions growing from ["branded_invoice/1", "invoice/1"] to the 10-entry alpha-sorted set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-recipes-facade-dx-closure/93-CONTEXT.md
@.planning/phases/93-recipes-facade-dx-closure/93-RESEARCH.md
@.planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md
@.planning/phases/93-recipes-facade-dx-closure/93-VALIDATION.md
</context>

<interfaces>
<!-- Current vs. target state of priv/public_api.json Elixir.Rendro.Recipes section. From RESEARCH.md verified read. -->

Current priv/public_api.json (Elixir.Rendro.Recipes block, lines 319–325):
```json
"Elixir.Rendro.Recipes": {
  "functions": [
    "branded_invoice/1",
    "invoice/1"
  ],
  "tier": "stable",
  "types": []
}
```

Target after mix rendro.api.gen (additive: 8 new lines, 0 removed):
```json
"Elixir.Rendro.Recipes": {
  "functions": [
    "branded_invoice/1",
    "branded_invoice/2",
    "certificate/1",
    "certificate/2",
    "invoice/1",
    "invoice/2",
    "receipt/1",
    "receipt/2",
    "statement/1",
    "statement/2"
  ],
  "tier": "stable",
  "types": []
}
```

Generator behavior (from RESEARCH.md):
- Requires BEAM files on disk: mix compile must run first (the generator calls Mix.Task.run("compile") internally)
- Alpha-sorts all function arrays
- Derives tier from @moduledoc tags: [:stable]
- Drops @doc false functions; requires real docstring
- Requires @spec for stable-tier functions (contract test Assertion 5)
- Writes trailing newline — File.write!(path, json <> "\n")
- NEVER hand-edit the output file
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run mix rendro.api.gen and verify additive-only diff</name>
  <files>priv/public_api.json</files>
  <read_first>
    - lib/rendro/recipes.ex — verify all 10 @spec'd functions with real docstrings are present (this plan depends on Plan 02 completing, but read to confirm before running the generator)
    - lib/mix/tasks/rendro/api.gen.ex lines 60–130 — understand generator behavior: @public_modules list, alpha-sort, tier derivation, @doc false filtering, @spec requirement
    - priv/public_api.json lines 315–330 — current state of the Elixir.Rendro.Recipes block (2 entries)
    - test/docs_contract/public_api_contract_test.exs lines 1–60 — understand the 5 assertions, especially Assertion 2 (byte-compare) and Assertion 5 (@spec coverage)
    - .planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md — "priv/public_api.json" section: current state, target state, generator behavior notes
    - .planning/phases/93-recipes-facade-dx-closure/93-CONTEXT.md — D-11 (all 10 @spec + docstrings BEFORE regen), D-12 (additive-only diff verification, never hand-edit)
  </read_first>
  <action>
    Step 1 — Pre-flight: Confirm lib/rendro/recipes.ex has exactly 10 @spec annotations and no @doc false on any of the 10 functions before running the generator. Run: grep -c "@spec" lib/rendro/recipes.ex (must return 10). If it returns fewer, STOP and report — Plan 02 is incomplete.

    Step 2 — Compile: Run mix compile to ensure BEAM files are fresh on disk. The generator calls Mix.Task.run("compile") internally, but an explicit compile first catches any errors early.

    Step 3 — Regenerate: Run mix rendro.api.gen. This will rewrite priv/public_api.json in-place with the updated manifest. Do not pass any flags — the task requires no arguments.

    Step 4 — Diff verification: Run git diff priv/public_api.json. Inspect the output carefully:
      - Expected: exactly 8 new + lines inside the Elixir.Rendro.Recipes.functions array ("branded_invoice/2", "certificate/1", "certificate/2", "invoice/2", "receipt/1", "receipt/2", "statement/1", "statement/2")
      - Expected: zero - lines (no removals)
      - Expected: no other module block touched in the diff
      If there are any - lines or other module changes, STOP and investigate before proceeding.

    Step 5 — Contract test: Run mix test test/docs_contract/public_api_contract_test.exs. All 5 assertions must pass (GREEN):
      - Assertion 1: Schema validation
      - Assertion 2: Byte-compare (fresh manifest == checked-in manifest after regen)
      - Assertion 3: Hidden internals
      - Assertion 4: Tier-tag exactly-one
      - Assertion 5: Stable-tier @spec coverage for all 10 Rendro.Recipes functions

    Step 6 — Full suite: Run mix test to confirm the complete test suite exits 0.
  </action>
  <verify>
    <automated>mix rendro.api.gen && git diff priv/public_api.json | grep -E "^\+" | grep -v "^+++" | wc -l && mix test test/docs_contract/public_api_contract_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `mix rendro.api.gen` exits 0 without error
    - `git diff priv/public_api.json` shows exactly 8 new + lines in the Elixir.Rendro.Recipes.functions array: "branded_invoice/2", "certificate/1", "certificate/2", "invoice/2", "receipt/1", "receipt/2", "statement/1", "statement/2" — in that alpha-sorted order
    - `git diff priv/public_api.json` shows zero - lines (no removals from any module)
    - `git diff priv/public_api.json` shows no changes to any module other than Elixir.Rendro.Recipes
    - `mix test test/docs_contract/public_api_contract_test.exs` exits 0 (all 5 assertions pass)
    - `mix test` exits 0 (full suite green — includes drift test, opts-threading regression, contract test, all existing recipe tests)
    - priv/public_api.json Elixir.Rendro.Recipes.functions array contains exactly: ["branded_invoice/1", "branded_invoice/2", "certificate/1", "certificate/2", "invoice/1", "invoice/2", "receipt/1", "receipt/2", "statement/1", "statement/2"] (10 entries, alpha-sorted)
    - priv/public_api.json was NOT hand-edited — only modified via mix rendro.api.gen
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add facade-expansion note to guides/api_stability.md (D-13)</name>
  <files>guides/api_stability.md</files>
  <read_first>
    - guides/api_stability.md — read the existing tiering model and any section describing the Recipes facade; match the doc's voice and find the right insertion point. Do NOT add per-function entries (Tier-1 covers facades by module).
    - .planning/phases/93-recipes-facade-dx-closure/93-CONTEXT.md — D-13 (one-line note only; optional polish, no per-function edits required)
  </read_first>
  <action>
    Add a single concise line to guides/api_stability.md noting that the Rendro.Recipes facade now exposes statement, receipt, and certificate (in addition to invoice and branded_invoice), plus arity-2 `opts` forms for all five recipes. Keep it to one sentence — Tier-1 already covers facades by module, so no per-function stability entries are added. Place it near the existing facade/Tier-1 discussion so it reads naturally.
  </action>
  <verify>
    <automated>grep -iE "statement.*receipt.*certificate|arity-2|opts form" guides/api_stability.md</automated>
  </verify>
  <acceptance_criteria>
    - guides/api_stability.md contains a one-line note mentioning the Rendro.Recipes facade now exposes statement, receipt, and certificate plus arity-2 opts forms
    - No per-function stability tier entries were added for the new facade functions (Tier-1 by-module coverage is unchanged)
    - The added line reads as a single sentence consistent with the surrounding doc voice
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix rendro.api.gen → priv/public_api.json | Generator reads BEAM metadata (Code.fetch_docs/1) and writes a deterministic JSON file; no network, no user input |
| priv/public_api.json → public_api_contract_test.exs | The committed golden file is byte-compared against a fresh generation; any discrepancy is a test failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-06 | Tampering | priv/public_api.json (hand-edited) | Low | mitigate | Never hand-edit the manifest. The byte-compare in public_api_contract_test.exs (Assertion 2) catches any hand-edit or generator divergence immediately. The additive-only diff check (Signal D) verifies no Tier-1 symbol was removed. |
| T-93-07 | Information Disclosure | public surface expansion | None | accept | Adding 8 new @spec'd functions to the stable public surface is intentional and reviewed. No secrets exposed. The functions are thin delegation wrappers over already-public recipe modules. |
| T-93-SC | Tampering | npm/pip/cargo installs | N/A | accept | No new packages installed. mix rendro.api.gen uses existing Jason dependency already present in mix.exs. |

**Summary:** This plan runs a deterministic code generator and verifies a byte-compare assertion in an existing test. No network surface, no auth, no user input, no new dependencies. The byte-compare contract test is the mitigation for the only meaningful risk (accidental hand-edit or non-additive change). The additive-only diff (Signal D) guards that no Tier-1 symbol is replaced. Severity: none/low.
</threat_model>

<verification>
Phase 93 gate — all four signals green:

Signal A: `mix test test/rendro/recipes_facade_drift_test.exs` exits 0
Signal B: opts-threading regression tests in drift file pass (covered by Signal A)
Signal C: `mix test test/docs_contract/public_api_contract_test.exs` exits 0
Signal D: `git diff priv/public_api.json` shows only + lines in Elixir.Rendro.Recipes.functions (8 additions), zero - lines, no other module touched

Full phase gate: `mix test` exits 0
</verification>

<success_criteria>
- priv/public_api.json regenerated via mix rendro.api.gen (never hand-edited)
- Elixir.Rendro.Recipes.functions: 2 entries → 10 entries, additive-only diff (8 + lines, 0 - lines)
- public_api_contract_test.exs all 5 assertions pass
- Full mix test suite green (zero failing tests)
- Phase 93 all 4 success criteria satisfied:
  SC#1: 10 @spec'd functions on Rendro.Recipes (5 recipes × arity-1 + arity-2)
  SC#2: opts-drop footgun fixed (README corrected, opts thread through)
  SC#3: drift test GREEN (reachability + no-extra-fns + struct byte-identity + orphan sweep)
  SC#4: priv/public_api.json updated, contract test passes (additive, no Tier-1 replaced)
</success_criteria>

<output>
Create .planning/phases/93-recipes-facade-dx-closure/93-03-SUMMARY.md when done.
</output>
