---
phase: 93-recipes-facade-dx-closure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/rendro/recipes_facade_drift_test.exs
  - README.md
autonomous: true
requirements:
  - DX-01
  - DX-02

must_haves:
  truths:
    - "A drift test file exists at test/rendro/recipes_facade_drift_test.exs and is runnable"
    - "The drift test starts RED (all assertions fail because facade/2 functions do not yet exist)"
    - "The test encodes the @recipes SOT table for 5 recipes (invoice, branded_invoice, statement, receipt, certificate)"
    - "README.md line ~135 no longer claims invoice delegates to document/1 dropping opts"
  artifacts:
    - path: "test/rendro/recipes_facade_drift_test.exs"
      provides: "Drift detection — reachability, no-extra-fns, struct byte-identity, orphan sweep, opts-threading regression"
      exports: ["Rendro.RecipesFacadeDriftTest"]
    - path: "README.md"
      provides: "Corrected opts-threading description at line ~135"
  key_links:
    - from: "test/rendro/recipes_facade_drift_test.exs"
      to: "Rendro.Recipes"
      via: "function_exported?/3 + __info__(:functions)"
      pattern: "function_exported\\?\\(Rendro\\.Recipes"
    - from: "test/rendro/recipes_facade_drift_test.exs"
      to: ":application.get_key(:rendro, :modules)"
      via: "auto-discovery orphan sweep"
      pattern: ":application\\.get_key\\(:rendro"
---

<objective>
Author the RED drift test and fix the README opts-drop line.

Purpose: Create the Nyquist Wave 0 test file that will start RED (all arity-2 facade functions do not yet exist) and go GREEN after Plan 02 expands the facade. Fix the inaccurate README prose in the same wave so the correction is not forgotten.

Output:
- test/rendro/recipes_facade_drift_test.exs — 4 drift assertions (reachability/1+2, no-extra-functions MapSet equality, struct byte-identity, auto-discovery orphan sweep) plus a facade-level opts-threading regression block (per D-07–D-10). Contains inline defp fixture_for/1 helpers for all 5 recipes.
- README.md line ~135 — corrected to reflect opts-threading through document/2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-recipes-facade-dx-closure/93-CONTEXT.md
@.planning/phases/93-recipes-facade-dx-closure/93-RESEARCH.md
@.planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md
@.planning/phases/93-recipes-facade-dx-closure/93-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from PATTERNS.md + RESEARCH.md. -->

From lib/rendro/recipes.ex (CURRENT STATE — has only 2 fns, both arity-1 calling document/1):
```elixir
defmodule Rendro.Recipes do
  @moduledoc tags: [:stable]
  @spec invoice(map()) :: Rendro.Document.t()
  def invoice(data), do: Rendro.Recipes.Invoice.document(data)       # FOOTGUN: opts dropped
  @spec branded_invoice(map()) :: Rendro.Document.t()
  def branded_invoice(data), do: Rendro.Recipes.BrandedInvoice.document(data)   # FOOTGUN
end
```

From test/rendro/recipes/invoice_opts_threading_test.exs (module header + async pattern):
```elixir
defmodule Rendro.Recipes.InvoiceOptsThreadingTest do
  use ExUnit.Case, async: true
  alias Rendro.Recipes.Invoice
  # defp sample_data/0 → %{id:, date:, items: [...]}
end
```

From test/docs_contract/public_api_contract_test.exs (reflection idioms):
```elixir
{:ok, all_modules} = :application.get_key(:rendro, :modules)
MapSet.new(...)
MapSet.difference(set_a, set_b)
```

Fixture shapes (from PATTERNS.md — inline these as defp fixture_for/1 in the drift test):
- invoice:      %{id: "INV-001", date: ~D[2026-01-01], items: []}
- branded_invoice: %{id: "INV-DRIFT-01", date: ~D[2026-01-01], items: [], brand: %{font_name: :brand_heading, logo_name: :company_logo}}
- statement:    %{period: %{from: ~D[2026-01-01], to: ~D[2026-01-31]}, account: %{name: "Drift Test Co"}, opening_balance: Decimal.new("0.00"), lines: []}
- receipt:      %{title: "Receipt", date: ~D[2026-01-01], customer: %{name: "Drift Test Co"}, lines: [], totals: %{subtotal: Decimal.new("0.00"), total: Decimal.new("0.00")}}
- certificate:  %{title: "Certificate of Completion", recipient: "Drift Test", body: "Completed.", date: ~D[2026-01-01], seal_line: "Signed"}

Sentinel opts per recipe (from PATTERNS.md):
- Statement:    labels: %{balance: "Saldo"}   — changes column header via Pagination.label_resolver/1
- Certificate:  border: true                  — adds :frame region, changes template geometry
- Receipt:      formatters: %{amount: fn v -> "EUR #{v}" end}
- Invoice/BrandedInvoice: pass-through equality only (invoice(data, []) == Invoice.document(data, []))
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Author RED drift test (test/rendro/recipes_facade_drift_test.exs)</name>
  <files>test/rendro/recipes_facade_drift_test.exs</files>
  <read_first>
    - test/rendro/recipes/invoice_opts_threading_test.exs — module header, async: true, defp sample_data, test/describe structure (read lines 1–40)
    - test/docs_contract/public_api_contract_test.exs — :application.get_key, MapSet.new, MapSet.difference idioms (read lines 1–80)
    - test/rendro/recipes/statement_test.exs — fixture_data/2 shape for Statement (read lines 1–50)
    - test/rendro/recipes/receipt_test.exs — fixture_data/2 shape for Receipt (read lines 1–45)
    - test/rendro/recipes/certificate_test.exs — fixture_data/1 shape for Certificate (read lines 1–25)
    - lib/rendro/recipes.ex — current state (full file, 32 lines) to understand what CURRENTLY exists
    - .planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md — Assertion 1–4 code blocks + fixture_for/1 shapes + opts-threading regression block
    - .planning/phases/93-recipes-facade-dx-closure/93-VALIDATION.md — Wave 0 requirements and RED-before/GREEN-after table
  </read_first>
  <action>
    Create test/rendro/recipes_facade_drift_test.exs as a new file (does NOT exist yet).

    Module name: Rendro.RecipesFacadeDriftTest
    Use: ExUnit.Case, async: true (NOT async: false — this test only reads BEAM metadata, no global compile state)

    Place the @recipes module attribute immediately after "use ExUnit.Case, async: true" (per D-07). The SOT table is the canonical list; adding a recipe forces a deliberate edit here:
      @recipes [
        {:invoice, Rendro.Recipes.Invoice},
        {:branded_invoice, Rendro.Recipes.BrandedInvoice},
        {:statement, Rendro.Recipes.Statement},
        {:receipt, Rendro.Recipes.Receipt},
        {:certificate, Rendro.Recipes.Certificate}
      ]

    Write four test blocks (per D-08, D-09):

    Test "each recipe is reachable as name/1 and name/2 on Rendro.Recipes":
      - Iterate @recipes with for {name, _module} <- @recipes
      - Assert function_exported?(Rendro.Recipes, name, 1) with message "Expected Rendro.Recipes.#{name}/1 to be exported"
      - Assert function_exported?(Rendro.Recipes, name, 2) with message "Expected Rendro.Recipes.#{name}/2 to be exported"

    Test "Rendro.Recipes exposes exactly the expected 10 functions":
      - Build expected MapSet from @recipes: for {name, _} <- @recipes, arity <- [1, 2] do {name, arity} end |> MapSet.new()
      - Build actual MapSet from Rendro.Recipes.__info__(:functions) |> MapSet.new()
      - Assert actual == expected (exact — no extras, no missing)

    Test "Rendro.Recipes delegates produce struct-identical result to recipe modules":
      - Iterate @recipes with for {name, module} <- @recipes
      - data = fixture_for(name)
      - facade_result = apply(Rendro.Recipes, name, [data])
      - direct_result = module.document(data)
      - Assert facade_result == direct_result with clear message naming the recipe

    Test "no orphan recipe modules missing facade wrapper" (per D-09):
      - {:ok, all_modules} = :application.get_key(:rendro, :modules)
      - Filter to modules where: Atom.to_string(mod) starts with "Elixir.Rendro.Recipes.", function_exported?(mod, :document, 2), and mod != Rendro.Recipes.Pagination
      - Build MapSet from filtered list; build expected_modules MapSet from Enum.map(@recipes, fn {_, mod} -> mod end)
      - Assert recipe_modules_with_document2 == expected_modules with a diff message using MapSet.difference for both directions

    Write a describe "facade opts-threading regression" block (per D-10) containing:
      - test "statement/2 with sentinel opts produces struct-identical result to Statement.document/2": data = fixture_for(:statement), opts = [labels: %{balance: "Saldo"}], assert Rendro.Recipes.statement(data, opts) == Rendro.Recipes.Statement.document(data, opts)
      - test "statement/2 with sentinel opts changes result vs no-opts": same data, assert Rendro.Recipes.statement(data, opts) != Rendro.Recipes.statement(data)
      - test "certificate/2 with border: true produces struct-identical result to Certificate.document/2": data = fixture_for(:certificate), opts = [border: true], assert Rendro.Recipes.certificate(data, opts) == Rendro.Recipes.Certificate.document(data, opts)
      - test "receipt/1 with empty opts returns same as receipt/2 with []": data = fixture_for(:receipt), assert Rendro.Recipes.receipt(data, []) == Rendro.Recipes.receipt(data)
      - test "invoice/2 with empty opts is struct-identical to Invoice.document/2 with []": data = fixture_for(:invoice), assert Rendro.Recipes.invoice(data, []) == Rendro.Recipes.Invoice.document(data, [])

    Write private defp fixture_for/1 helpers at the bottom of the module (these CANNOT be imported from other test files per RESEARCH.md Pitfall 5):
      fixture_for(:invoice)         -> %{id: "INV-001", date: ~D[2026-01-01], items: []}
      fixture_for(:branded_invoice) -> %{id: "INV-DRIFT-01", date: ~D[2026-01-01], items: [], brand: %{font_name: :brand_heading, logo_name: :company_logo}}
      fixture_for(:statement)       -> %{period: %{from: ~D[2026-01-01], to: ~D[2026-01-31]}, account: %{name: "Drift Test Co"}, opening_balance: Decimal.new("0.00"), lines: []}
      fixture_for(:receipt)         -> %{title: "Receipt", date: ~D[2026-01-01], customer: %{name: "Drift Test Co"}, lines: [], totals: %{subtotal: Decimal.new("0.00"), total: Decimal.new("0.00")}}
      fixture_for(:certificate)     -> %{title: "Certificate of Completion", recipient: "Drift Test", body: "Completed.", date: ~D[2026-01-01], seal_line: "Signed"}

    IMPORTANT — this test must compile successfully and run, but all assertions should FAIL before Plan 02 expands the facade (facade/2 functions are not yet defined). Confirm RED before proceeding.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes_facade_drift_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File test/rendro/recipes_facade_drift_test.exs exists and compiles without errors
    - Running `mix test test/rendro/recipes_facade_drift_test.exs` produces test failures (RED), NOT a compile error
    - The failure messages include "Expected Rendro.Recipes.statement/1 to be exported" (or equivalent) confirming the facade/2 functions are missing
    - The @recipes module attribute lists exactly 5 entries: invoice, branded_invoice, statement, receipt, certificate
    - The no-extra-functions test fails because actual set is {invoice/1, branded_invoice/1} but expected has 10 entries
    - The auto-discovery orphan sweep test fails because Statement/Receipt/Certificate have document/2 but no facade wrapper
    - The opts-threading regression tests fail because invoice/2, statement/2, certificate/2, receipt/2 are not yet defined
    - `mix test test/rendro/recipes/ test/docs_contract/` (existing tests) continues to exit 0 (no regressions)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix README opts-drop line (~L135) (per D-06)</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 130–145 — read the inaccurate line 135 and surrounding context to understand the minimal correction needed
    - .planning/phases/93-recipes-facade-dx-closure/93-PATTERNS.md — README section with the exact current text and required replacement
  </read_first>
  <action>
    Edit README.md line ~135. The current inaccurate text is:
      "The delegating alias `Rendro.Recipes.invoice/1` calls `Rendro.Recipes.Invoice.document/1` for convenience."

    Replace with the minimal corrected version (per D-06 — exact wording at Claude's discretion, keep it short):
      "`Rendro.Recipes.invoice/1` (and `/2`) delegates to `Rendro.Recipes.Invoice.document/2`, threading opts through."

    Or equivalently:
      "`Rendro.Recipes` delegates to the recipe module's `document/2`, threading opts through. `invoice/1` is equivalent to `invoice(data, [])`."

    Constraint: This is a one-line prose correction. Do NOT rewrite the surrounding paragraph or add a 5-pair docs table — CONTEXT.md explicitly notes the full rewrite is optional polish only (Claude's Discretion). Correct the inaccurate line and move on.
  </action>
  <verify>
    <automated>grep -n "document/2\|threading opts" README.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - README.md line ~135 no longer contains the text "document/1" in the context of the invoice delegation description
    - README.md line ~135 now references opts threading through document/2
    - `mix test` exit code is 0 (README changes do not affect tests)
    - The surrounding README paragraphs are unchanged (no paragraph rewrites, no new sections)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/rendro/recipes_facade_drift_test.exs → Rendro.Recipes | Test reads BEAM-loaded module metadata via reflection; no network, no I/O, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-01 | Tampering | @recipes SOT table | None | accept | The @recipes table is the reviewed-edit gate; any drift requires a deliberate source change. No injection surface: it is a compile-time module attribute, not runtime input. |
| T-93-02 | Information Disclosure | test fixture data | None | accept | Fixtures contain no PII, secrets, or credentials — minimal synthetic maps for struct equality assertions only. |
| T-93-SC | Tampering | npm/pip/cargo installs | N/A | accept | No new packages installed in this plan. Pure Elixir source changes only. Package legitimacy audit confirmed: no new deps. |

**Summary:** This plan authors a test file and edits a README line. There is no network surface, no auth, no user input, no persistence, no new dependencies. All additive `@spec`'d delegation changes have transparent opts pass-through; recipe modules remain the single opts authority via `Keyword.get/3`, tolerant of unknown keys — no injection surface. The only meaningful risk surface is the contract test (Signal C), which guards against breaking stable-tier Tier-1 symbols. Severity: none/low.
</threat_model>

<verification>
Run after both tasks complete:

1. `mix test test/rendro/recipes_facade_drift_test.exs` — must exit with failures (RED is correct at this wave; facade arity-2 functions do not exist yet)
2. `mix test test/rendro/recipes/ test/docs_contract/` — must exit 0 (existing suite unaffected)
3. `grep -n "document/2\|threading opts" README.md | head -5` — must show the corrected line
4. `mix test` — full suite must exit 0 (no regressions from README edit or new test file that compiles but fails expected assertions)

Note: The drift test will fail until Plan 02 is complete. That is intentional. The important check is that it COMPILES cleanly and fails with assertion errors (not compile errors).
</verification>

<success_criteria>
- test/rendro/recipes_facade_drift_test.exs exists, compiles, and exits with test failures (RED) on all 9 drift/regression assertions
- All 5 fixture_for/1 helpers are defined inline (not imported)
- @recipes SOT table has exactly 5 entries covering all shipped recipes
- README.md line ~135 corrected to reference document/2 + opts threading
- Full test suite (mix test) exits 0 — only the new drift test file fails, and it fails with assertion errors, not compile/syntax errors
</success_criteria>

<output>
Create .planning/phases/93-recipes-facade-dx-closure/93-01-SUMMARY.md when done.
</output>
