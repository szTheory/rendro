---
phase: 82-1-0-0-consolidation-and-publish
plan: 03
type: execute
wave: 3
depends_on:
  - 82-02
files_modified: []
autonomous: false
requirements:
  - REL-06
must_haves:
  truths:
    - Operator manually cuts and pushes the v1.0.0 tag
    - Hex package is published via CI
    - Post-publish verification confirms rendro 1.0.0 is live
  artifacts: []
  key_links:
    - from: Tag Push
      to: CI Hex Publish
      via: GitHub Actions
---

<objective>
Execute the irreversible 1.0.0 publish sequence via operator manual action.

Purpose: Hex publish is irreversible and must be operator-gated. The agent prepares the commands, and the operator runs them.
Output: Published Hex package `rendro 1.0.0`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator Publish Checkpoint</name>
  <action>The project has been prepared for the 1.0.0 release. The Hex publish is triggered via CI tag. The agent CANNOT run the `git push` autonomously since it is an irreversible cut.</action>
  <verify>
    Execute the following commands manually in your terminal:
    1. Ensure your working tree is completely clean and all Phase 82 plans are committed (`git status`).
    2. Cut the tag: `git tag v1.0.0`
    3. Push the tag to trigger CI: `git push origin v1.0.0`
    4. Open GitHub Actions and wait for the `release.yml` workflow to complete and publish to Hex.
    5. Verify the package is live: `mix hex.info rendro` (ensure version is 1.0.0).
  </verify>
  <done>Operator responds with "approved" once the Hex package is verified live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Hex | Operator asserts final human judgment before irreversible release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03 | Elevation of Privilege | Publish Process | mitigate | Require explicit human checkpoint for `git push` of the tag, preventing automated unverified publish. |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | No installs. |
</threat_model>

<verification>
- Operator responds with "approved".
</verification>

<success_criteria>
- `rendro 1.0.0` is published to Hex and permanently live.
</success_criteria>

<output>
Create `.planning/phases/82-1-0-0-consolidation-and-publish/82-03-SUMMARY.md` when done
</output>