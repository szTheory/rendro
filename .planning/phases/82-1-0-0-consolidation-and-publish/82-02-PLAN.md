---
phase: 82-1-0-0-consolidation-and-publish
plan: 02
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - mix.exs
  - CHANGELOG.md
autonomous: true
requirements:
  - REL-04
  - REL-06
must_haves:
  truths:
    - Version is set to 1.0.0
    - Changelog has a consolidated 1.0.0 section with today's date
  artifacts:
    - path: mix.exs
      provides: 1.0.0 version configuration
    - path: CHANGELOG.md
      provides: 1.0.0 consolidated release notes
  key_links:
    - from: CHANGELOG.md
      to: guides/upgrading_to_1.0.md
      via: Stability subsection reference
---

<objective>
Consolidate the `CHANGELOG.md` entry for the 1.0.0 release and bump the project version in `mix.exs`.

Purpose: Provide a clean, unified release note encompassing v2.3, v2.4, and the API stability work, marking the first formal SemVer commitment.
Output: Updated `CHANGELOG.md` and `mix.exs` set to 1.0.0.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mix.exs
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version & Changelog Consolidation</name>
  <files>mix.exs, CHANGELOG.md</files>
  <action>
    Modify `mix.exs` to set `@version "1.0.0"`.
    Modify `CHANGELOG.md`:
    1. Replace `## [0.3.1] - Unreleased` with `## [1.0.0] - {CURRENT_DATE}` (use `date +%Y-%m-%d` to get the date).
    2. Consolidate the uncatalogued v2.4 features and the Phase 78-80 stability/cleanup work into this entry under standard headers (`### Added`, `### Changed`, etc.).
    3. Add a "Stability" subsection pointing to `guides/upgrading_to_1.0.md`.
  </action>
  <verify>
    <automated>mix release.preflight</automated>
  </verify>
  <done>Project version is 1.0.0, changelog is dated and consolidated, and preflight passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> User | The changelog must accurately reflect the shipped 1.0.0 code to maintain developer trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-SC | Tampering | npm/pip/cargo installs | accept | No installs. |
</threat_model>

<verification>
- `mix release.preflight` passes cleanly against the dated 1.0.0 changelog.
- `mix.exs` version is exactly `1.0.0`.
</verification>

<success_criteria>
- The project is configured and validated for the `v1.0.0` cut.
</success_criteria>

<output>
Create `.planning/phases/82-1-0-0-consolidation-and-publish/82-02-SUMMARY.md` when done
</output>