---
phase: 82-1-0-0-consolidation-and-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/release/preflight.ex
  - mix.exs
autonomous: true
requirements:
  - REL-02
  - REL-03
must_haves:
  truths:
    - Preflight task verifies hex package has no evidence/operator files
    - Preflight task accepts dated changelog headers, not just Unreleased
    - Preflight task runs mix hex.audit and mix deps.audit
  artifacts:
    - path: lib/mix/tasks/release/preflight.ex
      provides: "Hardened preflight checks"
    - path: mix.exs
      provides: "mix_audit dependency"
  key_links:
    - from: lib/mix/tasks/release/preflight.ex
      to: "mix deps.audit"
      via: "@phase_2_checks"
---

<objective>
Fix the critical release-hardening gaps (REL-02, REL-03) that were missed in Phase 81, ensuring the Hex tarball is clean and the preflight task won't block the 1.0.0 publish on the dated changelog header.

Purpose: Prevents internal evidence files from leaking into the published Hex package and allows the final CHANGELOG to bear a real date instead of "Unreleased".
Output: Updated `preflight.ex` task with absence-checks, source_ref parity, and regex changelog matching, plus `mix_audit` added to `mix.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mix/tasks/release/preflight.ex
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight Artifact & Parity Checks</name>
  <files>lib/mix/tasks/release/preflight.ex</files>
  <action>
    Modify `lib/mix/tasks/release/preflight.ex`:
    1. In `check_hex_artifacts/2`, add a check asserting that `File.exists?` returns false for the forbidden paths: `priv/support_matrix.json`, `priv/viewer_evidence/`, `priv/guardrails/`, `scripts/`, `test/`. Use the `fail/2` pattern if any are found.
    2. In `check_changelog_release_tail/1`, replace the hardcoded `"## [#{version}] - Unreleased"` check with `Regex.match?(~r/## \[#{version}\] - (\d{4}-\d{2}-\d{2}|Unreleased)/, changelog)`. Remove the brittle check for the exact string ``render_to_artifact -> Protect.password -> store/deliver``.
    3. Add a new `check_source_ref_parity(context, version)` function asserting `docs[:source_ref]` matches `"v#{version}"`, and append it to the `phase_1_results` pipeline (e.g., around `check_package_metadata`).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Preflight task handles artifact absence, source_ref parity, and dated changelog headers without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Dependency Audit Wiring</name>
  <files>mix.exs, lib/mix/tasks/release/preflight.ex</files>
  <action>
    Modify `mix.exs` to add `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}` to the `deps/0` list.
    Modify `lib/mix/tasks/release/preflight.ex` to append `{"Hex Audit", ["hex.audit"]}` and `{"Deps Audit", ["deps.audit"]}` to the `@phase_2_checks` list.
  </action>
  <verify>
    <automated>mix deps.get && mix deps.audit</automated>
  </verify>
  <done>Audit dependencies are installed and wired into the preflight execution pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> Hex Registry | The preflight step prevents sensitive or bloat artifacts from being packaged into the published tarball. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Information Disclosure | preflight.ex | mitigate | Ensure explicit absence checking for internal priv/ directories and test/ artifacts before Hex build is approved. |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | mix_audit added as a known trusted tooling dependency. |
</threat_model>

<verification>
- `mix compile` succeeds.
- `mix release.preflight` dry-run executes without failing on the changelog or missing audits.
</verification>

<success_criteria>
- The gaps from Phase 81 are closed, enabling the final 1.0.0 publish phase.
- Operator files are strictly barred from the Hex tarball.
</success_criteria>

<output>
Create `.planning/phases/82-1-0-0-consolidation-and-publish/82-01-SUMMARY.md` when done
</output>