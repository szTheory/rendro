---
phase: 76-reference-phoenix-app-ci-and-documentation-closure
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex
  - examples/phoenix_example/lib/phoenix_example_web/router.ex
  - examples/phoenix_example/lib/phoenix_example_web/controllers/page_controller.ex
  - examples/phoenix_example/test/phoenix_example_web/controllers/pdf_controller_test.exs
autonomous: true
requirements: [REF-02]
must_haves:
  truths:
    - "GET /statement/download, /receipt/download, /certificate/download each return 200 + application/pdf + a %PDF- body via Rendro.Adapters.Phoenix"
    - "Each new recipe also has a /preview inline route"
    - "The index chooser page lists download + preview links for all three new recipes"
    - "Statement & Receipt documents expose [:header, :body, :footer] regions; Certificate exposes exactly [:body]"
  artifacts:
    - path: "examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex"
      provides: "Three @demo_* fixtures + 6 download/preview actions calling the adapter"
      contains: "Rendro.Recipes.Statement.document"
    - path: "examples/phoenix_example/lib/phoenix_example_web/router.ex"
      provides: "6 new :api-scope routes mirroring /branded/*"
      contains: "/statement/download"
    - path: "examples/phoenix_example/test/phoenix_example_web/controllers/pdf_controller_test.exs"
      provides: "HTTP + magic-byte + structural assertions per new recipe"
      contains: "%PDF-"
  key_links:
    - from: "examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex"
      to: "Rendro.Adapters.Phoenix.render_pdf/3"
      via: "RendroPhoenix.render_pdf(conn, doc, filename)"
      pattern: "RendroPhoenix\\.(render_pdf|preview_pdf)"
    - from: "examples/phoenix_example/lib/phoenix_example_web/router.ex"
      to: "PDFController recipe actions"
      via: "get \"/statement/download\", PDFController, :statement_download"
      pattern: "PDFController, :(statement|receipt|certificate)_(download|preview)"
---

<objective>
Demonstrate all three new shipped recipes (Statement, Receipt, Certificate) through `Rendro.Adapters.Phoenix` by extending the existing plain dead-controller pattern: add inline `@demo_*` fixtures, one `download` + one `preview` action per recipe, six routes, six chooser links, and per-recipe ConnCase + structural tests. Implements REF-02 (decisions D-05, D-06, D-07, D-08).

Purpose: REF-02 requires the reference app to demonstrate all five recipes through the adapter. Invoice and BrandedInvoice already exist; this plan clones that idiom verbatim for the three new recipes (D-05: no LiveView, no shared SampleData module).
Output: Extended controller, router, page chooser, and test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-CONTEXT.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-RESEARCH.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md
@lib/rendro/adapters/phoenix.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 fixtures + 6 actions, 6 routes, 6 chooser links (D-05, D-06, D-07)</name>
  <files>examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex, examples/phoenix_example/lib/phoenix_example_web/router.ex, examples/phoenix_example/lib/phoenix_example_web/controllers/page_controller.ex</files>
  <read_first>
    - examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex (file being modified — Invoice/Branded action pairs + `@demo_*` attrs; `alias Rendro.Adapters.Phoenix, as: RendroPhoenix` already at line 3)
    - examples/phoenix_example/lib/phoenix_example_web/router.ex (file being modified — `:api` scope with `/branded/*` precedent)
    - examples/phoenix_example/lib/phoenix_example_web/controllers/page_controller.ex (file being modified — `@chooser_html` `<ul>` with existing `<li><a>` entries)
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md (verified Decimal-typed fixtures + action/route/chooser excerpts)
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-RESEARCH.md ("Verified Recipe Data Contracts" — required/optional keys per recipe)
  </read_first>
  <action>
    In `pdf_controller.ex`: add three inline module attributes using the EXACT verified Decimal-typed shapes from 76-PATTERNS.md/76-RESEARCH.md (all `amount`/balance keys are `Decimal.new("...")` — a Float raises `ArgumentError` in `validate_data!/1`): `@demo_statement` (`:period` with `%Date{}` from/to, `:account` `%{name}`, `:opening_balance` Decimal, `:lines` with date/description/amount; do NOT add a per-line `:balance` key — the recipe computes it); `@demo_receipt` (`:title`, `:date`, `:customer` `%{name}`, `:lines` with description/amount, optional `:totals` `%{subtotal, total}` with values consistent to the line sum); `@demo_certificate` (`:title`, `:recipient`, `:date`, optional `:body` ≤2000 bytes, `:seal_line`; no `:brand` for the base demo). Add six actions cloning the Invoice pair 1:1: `statement_download/2`+`statement_preview/2`, `receipt_download/2`+`receipt_preview/2`, `certificate_download/2`+`certificate_preview/2` — each calls `Rendro.Recipes.{Statement|Receipt|Certificate}.document(@demo_*)` then `RendroPhoenix.render_pdf(conn, doc, "<recipe>.pdf")` (download) or `RendroPhoenix.preview_pdf(conn, doc)` (preview). Filenames: `"statement.pdf"`, `"receipt.pdf"`, `"certificate.pdf"`. Never hand-roll `Rendro.render` + header juggling — the adapter owns content-type/disposition.
    In `router.ex`: add six routes inside the existing `:api` scope mirroring `/branded/*`: `get "/statement/download"`, `/statement/preview`, `/receipt/download`, `/receipt/preview`, `/certificate/download`, `/certificate/preview`, each pointing at the matching `PDFController` action atom.
    In `page_controller.ex`: extend the `@chooser_html` `<ul>` with six new `<li><a href="...">` entries (download + preview per new recipe), mirroring the existing `/branded/*` entries. The `index/2` action is unchanged.
  </action>
  <verify>
    <automated>cd examples/phoenix_example && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/phoenix_example && mix compile --warnings-as-errors` exits 0.
    - `pdf_controller.ex` defines `statement_download`, `statement_preview`, `receipt_download`, `receipt_preview`, `certificate_download`, `certificate_preview` (grep all six action names present).
    - All `amount`/`opening_balance`/`subtotal`/`total` values in the three fixtures use `Decimal.new(`; no bare float literals on those keys; `@demo_statement` lines have NO `:balance` key.
    - `router.ex` contains all six route paths (`/statement/download`, `/statement/preview`, `/receipt/download`, `/receipt/preview`, `/certificate/download`, `/certificate/preview`) inside the `:api` scope.
    - `page_controller.ex` `@chooser_html` contains six new `href` entries for the three recipes.
  </acceptance_criteria>
  <done>Three recipes are demonstrated through the adapter with attachment + inline routes and chooser links; app compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-recipe ConnCase + structural tests (D-08)</name>
  <files>examples/phoenix_example/test/phoenix_example_web/controllers/pdf_controller_test.exs</files>
  <read_first>
    - examples/phoenix_example/test/phoenix_example_web/controllers/pdf_controller_test.exs (file being modified — existing Invoice/Branded describe blocks; the assertion idiom to mirror)
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md (three assertion kinds; the CRITICAL Certificate single-`:body`-region block vs Statement/Receipt three-region block)
    - lib/rendro/recipes/certificate.ex (confirm `page_template/1` builds a SINGLE `:body` region around lines 89-100 — do not assume header/footer)
  </read_first>
  <behavior>
    - Statement: GET /statement/download returns status 200, content-type =~ "application/pdf", body magic bytes == "%PDF-".
    - Receipt: GET /receipt/download returns 200 + application/pdf + "%PDF-".
    - Certificate: GET /certificate/download returns 200 + application/pdf + "%PDF-".
    - Statement structural: `Rendro.Recipes.Statement.document/1` returns `%Rendro.Document{page_template: :statement}` with regions == [:header, :body, :footer] and non-empty sections.
    - Receipt structural: returns `%Rendro.Document{page_template: :receipt}` with regions including :header, :body, :footer.
    - Certificate structural: returns `%Rendro.Document{page_template: :certificate}` with region names == [:body] EXACTLY (single body-only region — NOT three regions).
  </behavior>
  <action>
    Add three describe/test blocks to `pdf_controller_test.exs` mirroring the existing Invoice block. Declare matching `@statement_data`/`@receipt_data`/`@certificate_data` module attrs equal to the controller's `@demo_*` shapes. Per recipe assert: (1) HTTP — `get(conn, "/<recipe>/download")` then `conn.status == 200`, `get_resp_header(conn, "content-type") |> hd() =~ "application/pdf"`, `binary_part(conn.resp_body, 0, 5) == "%PDF-"`; (2) Structural — re-run `document/1`, assert `%Rendro.Document{}` and the expected `page_template` atom. For Statement & Receipt assert `:header`, `:body`, `:footer` are all in the region-name set and `doc.sections != []`. For Certificate assert `region_names == [:body]` EXACTLY — do NOT copy the three-region assertion (Certificate's `page_template/1` builds a single body-only region; reusing the 3-region block fails with a MatchError/region-name mismatch, RESEARCH Pitfall 1). Optionally extend the existing source-level describe block to assert the controller references the three new `Rendro.Recipes.*.document` calls.
  </action>
  <verify>
    <automated>cd examples/phoenix_example && mix test test/phoenix_example_web/controllers/pdf_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/phoenix_example && mix test test/phoenix_example_web/controllers/pdf_controller_test.exs` exits 0 with all new tests green.
    - Test file asserts `binary_part(conn.resp_body, 0, 5) == "%PDF-"` for statement, receipt, and certificate download routes.
    - The Certificate structural test asserts `region_names == [:body]` (exactly one body region), NOT a three-region set.
    - The Statement and Receipt structural tests assert `:header`, `:body`, AND `:footer` regions.
    - No fixture uses a Float for any `amount`/balance key (would raise `ArgumentError` and fail the test before assertions).
  </acceptance_criteria>
  <done>All three recipes pass HTTP, magic-byte, and structural assertions; Certificate single-region trap avoided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Phoenix `:api` controller | Inbound GET requests; controllers ignore params and serve fixed demo data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Tampering (input) | PDFController recipe actions | accept | Actions bind `_params` and serve only fixed inline `@demo_*` fixtures; no user input reaches `document/2`. Recipes still validate data maps via `validate_data!/1` (defense-in-depth). |
| T-76-04 | Information Disclosure | rendered PDF responses | accept | Demo PDFs contain only fictitious fixture data (Acme Corp, Jane Smith); no secrets/PII. |
| T-76-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs (Elixir/Hex only; no new packages). |
</threat_model>

<verification>
- `cd examples/phoenix_example && mix compile --warnings-as-errors` exits 0.
- `cd examples/phoenix_example && mix test test/phoenix_example_web/controllers/pdf_controller_test.exs` exits 0.
- `cd examples/phoenix_example && mix test` (full example-app suite) exits 0.
</verification>

<success_criteria>
All five recipes are demonstrated through `Rendro.Adapters.Phoenix` with attachment + inline routes, chooser links, and passing tests (REF-02). Certificate single-`:body`-region assertion is correct.
</success_criteria>

<output>
Create `.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-02-SUMMARY.md` when done.
</output>
