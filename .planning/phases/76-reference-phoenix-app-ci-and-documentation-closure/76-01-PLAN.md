---
phase: 76-reference-phoenix-app-ci-and-documentation-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_example/mix.exs
  - examples/phoenix_example/lib/phoenix_example_web/controllers/error_json.ex
  - examples/phoenix_example/README.md
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "examples/phoenix_example compiles clean with --warnings-as-errors on the new dep floors"
    - "The app boots via mix phx.server without a missing-module crash (ErrorJSON exists)"
    - "A README documents setup and each demonstrated recipe"
  artifacts:
    - path: "examples/phoenix_example/mix.exs"
      provides: "Non-stale dep constraints (Phoenix ~> 1.8, plug ~> 1.18, jason ~> 1.4, elixir ~> 1.19)"
      contains: "~> 1.8"
    - path: "examples/phoenix_example/lib/phoenix_example_web/controllers/error_json.ex"
      provides: "PhoenixExampleWeb.ErrorJSON render-errors handler (D-03 load-bearing fix)"
      contains: "PhoenixExampleWeb.ErrorJSON"
    - path: "examples/phoenix_example/README.md"
      provides: "Setup + per-recipe documentation"
      contains: "mix phx.server"
  key_links:
    - from: "examples/phoenix_example/config/config.exs"
      to: "PhoenixExampleWeb.ErrorJSON"
      via: "render_errors formats: [json: PhoenixExampleWeb.ErrorJSON]"
      pattern: "PhoenixExampleWeb.ErrorJSON"
---

<objective>
Modernize the reference Phoenix app to non-stale dependency constraints, fix the load-bearing missing `PhoenixExampleWeb.ErrorJSON` module so the app boots clean, and author a README documenting setup + each demonstrated recipe. Implements REF-01 (decisions D-01, D-02, D-03, D-04).

Purpose: REF-01 requires `examples/phoenix_example` to be `mix`-runnable on current constraints with a README. The app is a deliberately generator-free Phoenix 1.8-line app; per D-01 we upgrade it in place (NOT `mix phx.new`) so no omitted boilerplate is re-added.
Output: Updated `mix.exs` dep floors, a new `error_json.ex` module, and a new `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-CONTEXT.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-RESEARCH.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump example-app dep constraints + add ErrorJSON module (D-02, D-03)</name>
  <files>examples/phoenix_example/mix.exs, examples/phoenix_example/lib/phoenix_example_web/controllers/error_json.ex</files>
  <read_first>
    - examples/phoenix_example/mix.exs (file being modified — current `elixir: "~> 1.15"` at line 8, `deps/0` lines 25-33)
    - examples/phoenix_example/config/config.exs (confirms `render_errors` registers `formats: [json: PhoenixExampleWeb.ErrorJSON]` — the missing module that breaks boot)
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md (mix.exs edit block + ErrorJSON Pattern 4 / "No Analog Found" fallback)
  </read_first>
  <action>
    In `examples/phoenix_example/mix.exs`: set `elixir: "~> 1.19"` (was `~> 1.15`) in `project/0`. In `deps/0` set the floors per D-02: `{:phoenix, "~> 1.8"}` (was `~> 1.7`), `{:plug, "~> 1.18"}` (was `~> 1.14` — bandit 1.10 already floors plug at 1.18, mix.lock resolves 1.19.1), `{:jason, "~> 1.4"}` (was `~> 1.2`). Leave `{:bandit, "~> 1.0"}` and `{:rendro, path: "../.."}` unchanged. These are floor bumps only — `mix.lock` already resolves above all of them, so no lock churn beyond a clean re-resolve.
    Create new module `PhoenixExampleWeb.ErrorJSON` in `examples/phoenix_example/lib/phoenix_example_web/controllers/error_json.ex` using the Phoenix 1.7+/1.8 generator-default convention (D-03): a single `render/2` clause taking a template name plus assigns and returning `%{errors: %{detail: Phoenix.Controller.status_message_from_template(template)}}`. This satisfies the `render_errors` handler `config/config.exs` already references so the app boots without a missing-module crash. Do NOT add Ecto/assets/LiveView/gettext (D-01 minimal mandate).
  </action>
  <verify>
    <automated>cd examples/phoenix_example && mix deps.get && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/phoenix_example && mix deps.get && mix compile --warnings-as-errors` exits 0 (no missing-module/undefined-function warnings).
    - `examples/phoenix_example/mix.exs` contains `{:phoenix, "~> 1.8"}`, `{:plug, "~> 1.18"}`, `{:jason, "~> 1.4"}`, and `elixir: "~> 1.19"`; still contains `{:bandit, "~> 1.0"}` and `{:rendro, path: "../.."}`.
    - `examples/phoenix_example/lib/phoenix_example_web/controllers/error_json.ex` defines `defmodule PhoenixExampleWeb.ErrorJSON` with a `render/2` returning `%{errors: %{detail: Phoenix.Controller.status_message_from_template(...)}}`.
    - `grep -c "Ecto\|LiveView\|gettext" examples/phoenix_example/lib/phoenix_example_web/controllers/error_json.ex` returns 0 (minimal mandate preserved).
  </acceptance_criteria>
  <done>App re-resolves deps and compiles clean with warnings-as-errors; ErrorJSON module exists so render-error handling is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Author examples/phoenix_example/README.md (D-04)</name>
  <files>examples/phoenix_example/README.md</files>
  <read_first>
    - examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex (current demonstrated routes /download, /preview, /branded/* — the recipes already shown; the new recipe routes land in plan 76-02 but the README should describe the full intended surface)
    - examples/phoenix_example/lib/phoenix_example_web/router.ex (route names to document)
    - guides/branding.md (repo tone/structure reference — minimal, arriving-engineer north star per CONTEXT §specifics)
  </read_first>
  <action>
    Create `examples/phoenix_example/README.md`. Document: (1) Setup — `cd examples/phoenix_example && mix deps.get`; (2) Boot — `mix phx.server` (serves at `http://localhost:4000`); (3) one short section per demonstrated recipe (Invoice, BrandedInvoice, Statement, Receipt/Report, Certificate) naming its `/download` (attachment) and `/preview` (inline) routes and noting each flows through `Rendro.Adapters.Phoenix`. Include the operator note from RESEARCH Runtime State Inventory: the `example-phoenix` CI job is advisory and must NOT be added as a required branch-protection check (REF-03). Keep prose minimal — it should read like the plain controller integration a real Phoenix engineer would copy, not a framework showcase. Statement/Receipt/Certificate routes are wired in plan 76-02; describe them here as the intended surface (the README is documentation, not a compile dependency).
  </action>
  <verify>
    <automated>test -f examples/phoenix_example/README.md && grep -q "mix phx.server" examples/phoenix_example/README.md && grep -qi "not required" examples/phoenix_example/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `examples/phoenix_example/README.md` exists.
    - Contains `mix deps.get` and `mix phx.server` setup/boot commands.
    - Names all five recipes (Invoice, BrandedInvoice, Statement, Receipt/Report, Certificate) and mentions `Rendro.Adapters.Phoenix`.
    - Contains the operator note that `example-phoenix` must NOT be a required branch-protection check (case-insensitive "not required").
  </acceptance_criteria>
  <done>README documents setup, boot, each recipe surface, and the advisory-CI operator caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry → example-app build | Dependency version-floor bumps resolved from Hex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering (supply chain) | `mix.exs` dep floor bumps | accept | No new packages added; only floor bumps to already-locked high-trust Hex packages (Phoenix/Plug/Jason). `mix.lock` already resolves above the new floors; `mix deps.get` re-resolves without introducing new transitive deps. |
| T-76-02 | Information Disclosure | `error_json.ex` render path | accept | Error body returns only the generic status message via `status_message_from_template/1`; no stack traces, secrets, or PII leak. Example app serves fixed demo data with no auth/user input. |
| T-76-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan (Elixir/Hex only; no new packages — RESEARCH Package Legitimacy Audit: gate not required). |
</threat_model>

<verification>
- `cd examples/phoenix_example && mix deps.get && mix compile --warnings-as-errors` exits 0.
- `cd examples/phoenix_example && mix phx.server` boots without a missing-module crash (manual boot gate, D-04).
- README present with setup/boot/recipe sections + advisory-CI operator note.
</verification>

<success_criteria>
App is `mix`-runnable on non-stale constraints (REF-01); ErrorJSON load-bearing bug closed; README documents the surface.
</success_criteria>

<output>
Create `.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-01-SUMMARY.md` when done.
</output>
