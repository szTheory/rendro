---
phase: 76-reference-phoenix-app-ci-and-documentation-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/page_primitive.md
  - guides/recipes.md
  - mix.exs
  - scripts/verify_docs.exs
  - test/docs_contract/page_primitive_claims_test.exs
  - test/docs_contract/recipes_claims_test.exs
  - test/docs_contract/recipes_contract_test.exs
autonomous: true
requirements: [CONTRACT-02]
must_haves:
  truths:
    - "D-14: guides/page_primitive.md and guides/recipes.md exist and are wired into root mix.exs docs/0 extras + groups_for_extras under a Recipes & Primitives group (skip_undefined_reference_warnings_on as needed)"
    - "Both guides' `elixir` fences carry # docs-contract: ids, are self-contained, assertion-bearing, and free of placeholders"
    - "Claims tests assert each guide claim maps to a matrix-supported capability and refute out-of-matrix claims (full_pdf_compliance, digital_signatures)"
    - "Each cited support-matrix row's evidence path exists on disk"
    - "verify_docs.exs registers two new claims lanes (10 total)"
  artifacts:
    - path: "guides/page_primitive.md"
      provides: "PAGE primitive guide bounded to page_numbering matrix row"
      contains: "Page X of Y"
    - path: "guides/recipes.md"
      provides: "Consolidated recipes guide (statement/receipt/certificate sections + branding pointer)"
      min_lines: 30
    - path: "test/docs_contract/recipes_claims_test.exs"
      provides: "Semantic-claims cross-check for statement/receipt_report/certificate rows + evidence existence"
      contains: "support_matrix.json"
    - path: "test/docs_contract/recipes_contract_test.exs"
      provides: "Fence verification over both guides"
      contains: "verified_fences"
  key_links:
    - from: "mix.exs"
      to: "guides/page_primitive.md, guides/recipes.md"
      via: "docs/0 extras + groups_for_extras Recipes & Primitives"
      pattern: "guides/(page_primitive|recipes)\\.md"
    - from: "test/docs_contract/recipes_claims_test.exs"
      to: "priv/support_matrix.json"
      via: "Jason.decode! + capability lookup + File.exists?(evidence)"
      pattern: "support_matrix\\.json"
    - from: "scripts/verify_docs.exs"
      to: "test/docs_contract/recipes_claims_test.exs"
      via: "lanes list tuple"
      pattern: "recipes_claims_test"
---

<objective>
Author two HexDocs guides (`guides/page_primitive.md` for the PAGE primitive, `guides/recipes.md` consolidating the canonical recipes), wire them into ExDoc, and add three docs-contract tests + two verify_docs lanes that bound every claim to `priv/support_matrix.json` + on-disk proof evidence. Implements CONTRACT-02 (decisions D-13, D-14, D-15, D-16).

Purpose: CONTRACT-02 requires the PAGE primitive and each recipe documented in HexDocs guides, with docs-contract tests rejecting any claim beyond what the support matrix and proof lanes cover — reusing the project's two existing harnesses (fence + semantic-claims), inventing nothing new.
Output: Two guides, root mix.exs docs wiring, two new claims tests, one new fence-contract test, two new verify_docs lanes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-CONTEXT.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-RESEARCH.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md
@guides/branding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both guides + wire into ExDoc (D-13, D-14)</name>
  <files>guides/page_primitive.md, guides/recipes.md, mix.exs</files>
  <read_first>
    - guides/branding.md (tone/structure/fence reference; the single source of truth for Invoice/BrandedInvoice — recipes.md points here, makes no new branding claims)
    - guides/viewer_evidence.md (tone reference + the extras insertion point precedent)
    - test/support/docs_contract.ex (fence harness — verified_fences/1 filters lang == "elixir"; `elixir-schematic` fences are skipped; evaluate!/2 runs Code.eval_string with import ExUnit.Assertions)
    - mix.exs (root, file being modified — docs/0 extras, groups_for_extras, skip_undefined_reference_warnings_on approximately lines 93-171)
    - priv/support_matrix.json (the four rows + capabilities that bound the guides: page_numbering, statement, receipt_report, certificate; and the unsupported array)
  </read_first>
  <action>
    Create `guides/page_primitive.md` documenting the PAGE primitive bounded to the `page_numbering` matrix row's supported capabilities: single-pass "Page X of Y" substitution, deterministic output, and first-page suppression. Create `guides/recipes.md` with one `##` section per canonical recipe: Statement and Receipt/Report (multi-page table continuation + running footer page numbers), Certificate (geometry-derived layout + multiple page sizes + optional branded output). The Invoice/BrandedInvoice section is a POINTER to `guides/branding.md` (D-13 — the matrix has no invoice/branded_invoice rows, so make no new claims there; preserve branding.md as the single source of truth). Never claim `full_pdf_compliance` or "digital signatures" (matrix `unsupported` array — the claims tests refute these). Any runnable `elixir` fence MUST carry a `# docs-contract: <id>` line, be self-contained + assertion-bearing, and be free of `...` / `%{...}` placeholders (use the verified Decimal-typed recipe fixtures from 76-RESEARCH.md). Use `elixir-schematic` for any illustrative-only snippet (silently skipped by the fence harness). Choose distinct, descriptive fence ids (Claude's discretion per CONTEXT).
    In root `mix.exs` `docs/0`: add `"guides/page_primitive.md"` and `"guides/recipes.md"` to `extras:` (after `guides/viewer_evidence.md`); add a `groups_for_extras:` group `"Recipes & Primitives": ["guides/page_primitive.md", "guides/recipes.md"]`; if `mix docs` emits undefined-reference warnings, add both guide paths to `skip_undefined_reference_warnings_on:` (mirror branding/integrations). Prefer fully-qualified real module/function references first.
  </action>
  <verify>
    <automated>test -f guides/page_primitive.md && test -f guides/recipes.md && grep -q "guides/page_primitive.md" mix.exs && grep -q "guides/recipes.md" mix.exs && grep -q "Recipes & Primitives" mix.exs && mix docs 2>&1 | grep -iv "warning: redefining\|Generating\|View\|^$" | grep -i "undefined" && echo "UNDEFINED-REFS-FOUND" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `guides/page_primitive.md` and `guides/recipes.md` exist; both contain "Page X of Y" / running-footer language for the primitive/statement/receipt and geometry-derived language for certificate.
    - Neither guide contains "digital signatures" or full-PDF-compliance claims.
    - recipes.md's Invoice/BrandedInvoice coverage is a pointer to guides/branding.md (contains a reference to branding), not new branding claims.
    - mix.exs `extras:` includes both guide paths; `groups_for_extras:` includes a "Recipes & Primitives" group listing both (D-14 — wiring into root mix.exs docs/0; add to skip_undefined_reference_warnings_on if needed).
    - `mix docs` completes with no undefined-reference warnings (or both guides added to skip list and docs build succeeds).
  </acceptance_criteria>
  <done>Both guides authored and wired into HexDocs; mix docs builds clean; claims bounded to the matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three docs-contract tests + two verify_docs lanes (D-15, D-16)</name>
  <files>test/docs_contract/page_primitive_claims_test.exs, test/docs_contract/recipes_claims_test.exs, test/docs_contract/recipes_contract_test.exs, scripts/verify_docs.exs</files>
  <read_first>
    - test/docs_contract/viewer_evidence_claims_test.exs (semantic-claims analog — File.read! guide + assert/refute + matrix lookup + evidence path)
    - test/docs_contract/branding_contract_test.exs (fence-contract analog — verified_fences + placeholder-free + evaluate! over the guide)
    - scripts/verify_docs.exs (file being modified — the `lanes` list at lines 7-16, currently 8 entries; the tuple regex shape `{"...", ["test", "test/docs_contract/...exs"]}`)
    - priv/support_matrix.json (capability keys + evidence paths to cross-check; unsupported array)
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md (verified matrix rows + evidence-path table; claims-test and fence-test pattern excerpts)
  </read_first>
  <behavior>
    - page_primitive_claims_test: reads guides/page_primitive.md + priv/support_matrix.json; asserts a backed phrase present (e.g. "Page X of Y"); asserts page_numbering capabilities single_pass_substitution and deterministic_output == "supported"; refutes "digital signatures"; asserts File.exists?(matrix["page_numbering"]["evidence"]) (test/rendro/pipeline/paginate_test.exs).
    - recipes_claims_test: reads guides/recipes.md + matrix; for statement, receipt_report, certificate asserts each cited capability == "supported", refutes out-of-matrix claims (full_pdf_compliance / digital signatures), and asserts File.exists? for each row's evidence path (statement_test.exs, receipt_test.exs, certificate_test.exs).
    - recipes_contract_test: for guides/recipes.md AND guides/page_primitive.md, verified_fences returns fences each free of "..." and "%{...}" and each evaluable via DocsContract.evaluate!.
  </behavior>
  <action>
    Create `test/docs_contract/page_primitive_claims_test.exs` cloning the viewer_evidence_claims_test pattern, cross-checking ONLY the `page_numbering` matrix row (capabilities single_pass_substitution + deterministic_output supported; evidence path `test/rendro/pipeline/paginate_test.exs` exists; refute "digital signatures").
    Create `test/docs_contract/recipes_claims_test.exs` cross-checking the `statement`, `receipt_report`, and `certificate` rows — for each, assert its cited capabilities are "supported", assert `File.exists?(matrix[row]["evidence"])` (statement_test.exs / receipt_test.exs / certificate_test.exs), and refute out-of-matrix claims (the unsupported array: full_pdf_compliance, digital_signatures).
    Create `test/docs_contract/recipes_contract_test.exs` cloning branding_contract_test: for each of guides/recipes.md and guides/page_primitive.md, call `Rendro.Test.DocsContract.verified_fences/1`, assert every fence body is free of "..." and "%{...}", and run `DocsContract.evaluate!(code, path)`. This test runs in the NORMAL suite (not a verify_docs lane).
    In `scripts/verify_docs.exs`: append exactly two CLAIMS lanes to the `lanes` list, keeping the exact tuple shape `{"<label>", ["test", "test/docs_contract/<file>.exs"]}`: a Recipes semantic-claims lane pointing at `recipes_claims_test.exs` and a Page-primitive semantic-claims lane pointing at `page_primitive_claims_test.exs`. Do NOT add the fence-contract test as a lane. This brings the lane count to 10 (coupled with plan 76-03's `== 10` assertion).
  </action>
  <verify>
    <automated>mix test test/docs_contract/recipes_claims_test.exs test/docs_contract/page_primitive_claims_test.exs test/docs_contract/recipes_contract_test.exs && grep -c 'test/docs_contract/.*\.exs"\]}' scripts/verify_docs.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/docs_contract/recipes_claims_test.exs test/docs_contract/page_primitive_claims_test.exs test/docs_contract/recipes_contract_test.exs` exits 0.
    - page_primitive_claims_test cross-checks ONLY page_numbering; recipes_claims_test cross-checks statement + receipt_report + certificate and asserts File.exists? on all three evidence paths.
    - Both claims tests `refute guide =~ "digital signatures"` (and avoid full-PDF-compliance language).
    - recipes_contract_test calls `DocsContract.verified_fences/1` and `evaluate!/2` over BOTH guides and refutes "..." / "%{...}".
    - `scripts/verify_docs.exs` `lanes` list has exactly 10 entries matching the tuple-shape regex `{"<label>", ["test", "test/docs_contract/<file>.exs"]}` (grep count == 10), including the two new claims lanes; the fence-contract test is NOT registered as a lane.
    - `mix run scripts/verify_docs.exs` runs all 10 lanes green.
  </acceptance_criteria>
  <done>Two guides are bounded by matrix-cross-checking claims tests + a fence-contract test; ten docs-contract lanes registered; all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose → published HexDocs | Documentation claims reach adopters; must not overstate capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-07 | Spoofing / trust erosion (overclaim) | guides/page_primitive.md, guides/recipes.md | mitigate | Semantic-claims tests assert every guide claim maps to a matrix-`supported` capability AND `refute` out-of-matrix language (`full_pdf_compliance`, `digital_signatures`); each cited row's `evidence:` path must exist on disk. Guides cannot ship claims beyond proof. |
| T-76-08 | Tampering (illustrative code masquerading as proven) | `elixir` fences in guides | mitigate | Fence harness runs every `elixir` fence via `evaluate!/2`; illustrative-only code must use `elixir-schematic` (skipped). recipes_contract_test refutes `...`/`%{...}` placeholders so no fence can fake verification. |
| T-76-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan (Markdown + Elixir test/config edits only; no new packages). |
</threat_model>

<verification>
- `mix test test/docs_contract/recipes_claims_test.exs test/docs_contract/page_primitive_claims_test.exs test/docs_contract/recipes_contract_test.exs` exits 0.
- `mix run scripts/verify_docs.exs` runs 10 lanes green.
- `mix docs` builds clean (no undefined-reference warnings).
- Wave-merge gate: full `mix test` + `mix run scripts/verify_docs.exs` green (the 76-03 lane-count `== 10` assertion now passes).
</verification>

<success_criteria>
PAGE primitive + each recipe documented in HexDocs guides; docs-contract tests reject any claim beyond the support matrix + proof lanes; two new lanes registered (CONTRACT-02).
</success_criteria>

<output>
Create `.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-04-SUMMARY.md` when done.
</output>
