---
phase: 76-reference-phoenix-app-ci-and-documentation-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - priv/guardrails/required_status_checks.json
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
requirements: [REF-03]
must_haves:
  truths:
    - "An example-phoenix CI job runs `mix deps.get && mix test` against examples/phoenix_example"
    - "The example-phoenix job is graph-disconnected (needs: []) so it neither gates nor is gated by test/signing-live-proof/long-lived-live-proof/release-proof"
    - "example-phoenix is recorded as advisory (not required) in the guardrail manifest"
    - "The four engine lanes remain in required_contexts unchanged"
    - "The required test job no longer runs the Phoenix example compile step"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "example-phoenix advisory job; removed Verify Phoenix Example step"
      contains: "example-phoenix:"
    - path: "priv/guardrails/required_status_checks.json"
      provides: "Second advisory_contexts entry for example-phoenix"
      contains: "example-phoenix"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Enum.find advisory lookup + example-phoenix assertion + lane count 10"
      contains: "Enum.find"
  key_links:
    - from: "priv/guardrails/required_status_checks.json"
      to: ".github/workflows/ci.yml"
      via: "advisory_contexts[].ci_job == example-phoenix job name"
      pattern: "example-phoenix"
    - from: "test/guardrails/required_checks_contract_test.exs"
      to: "priv/guardrails/required_status_checks.json"
      via: "Enum.find(advisory_contexts, name == example-phoenix)"
      pattern: "Enum.find"
---

<objective>
Add a graph-disconnected, non-required `example-phoenix` CI job that runs `mix test` against the reference app, remove the now-redundant "Verify Phoenix Example" step from the required `test` job, record the new job as advisory in the guardrail manifest, and update the guardrail contract test (the single-element advisory destructure and the hardcoded lane count both break silently if missed). Implements REF-03 (decisions D-09, D-10, D-11, D-12).

Purpose: REF-03 requires the example to be CI-exercised via `mix test` in an isolated job that never blocks the engine-critical lanes. The job must be visible (no `continue-on-error`) but never gate-coupled.
Output: Updated `ci.yml`, manifest, and contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-CONTEXT.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-RESEARCH.md
@.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add example-phoenix job, remove redundant step, record advisory manifest (D-09, D-10, D-11, D-12)</name>
  <files>.github/workflows/ci.yml, priv/guardrails/required_status_checks.json</files>
  <read_first>
    - .github/workflows/ci.yml (file being modified — the required `test` job's "Verify Phoenix Example" step to remove approximately lines 31-35; the `viewer-evidence-live-proof` job approximately lines 37-89 as the non-required ENCODING precedent ONLY; the setup-beam block otp-version 28 / elixir-version 1.19.5)
    - priv/guardrails/required_status_checks.json (file being modified — policy additive_only, required_contexts, contexts, and the single existing advisory_contexts[0] viewer-evidence entry)
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md (ci.yml Edit 1 / Edit 2 + manifest append block)
  </read_first>
  <action>
    In ci.yml: REMOVE the entire "Verify Phoenix Example" step from the required `test` job (the step that runs cd examples/phoenix_example then mix deps.get and mix compile) per D-11, so Phoenix deps no longer run in a required lane. ADD a new top-level job named example-phoenix with runs-on ubuntu-latest and NO needs key — graph-disconnected per D-09. Do NOT use needs: test; the viewer-evidence-live-proof precedent's needs: test is wrong here because a red engine test would suppress the example signal. Steps: actions/checkout@v4; erlef/setup-beam@v1 with otp-version 28 and elixir-version 1.19.5 matching the other jobs; a run step with working-directory examples/phoenix_example executing mix deps.get then mix test. Do NOT add continue-on-error per D-10 — that would mask red as green; failure must be visible-but-non-blocking.
    In required_status_checks.json: APPEND a second entry to advisory_contexts for the new job with name example-phoenix, ci_job example-phoenix, command "mix deps.get && mix test", and a notes string containing both the substring "not required" and a "REF-03" reference (the contract test asserts both). Do NOT touch required_contexts or contexts — the four engine lanes (long-lived-live-proof, release-proof, signing-live-proof, test) stay required per the additive_only policy.
  </action>
  <verify>
    <automated>node -e "const j=require('./priv/guardrails/required_status_checks.json'); const a=j.advisory_contexts.find(x=>x.name==='example-phoenix'); if(!a){console.error('missing advisory');process.exit(1)} if(!/not required/.test(a.notes)||!/REF-03/.test(a.notes)){console.error('notes');process.exit(1)} if(j.required_contexts.includes('example-phoenix')){console.error('leaked into required');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a top-level job `example-phoenix:` with NO `needs:` key and NO `continue-on-error:`.
    - The example-phoenix job runs `mix deps.get` and `mix test` with `working-directory: examples/phoenix_example` and setup-beam otp-version '28' / elixir-version '1.19.5'.
    - ci.yml no longer contains a "Verify Phoenix Example" step in the `test` job (`grep -c "Verify Phoenix Example" .github/workflows/ci.yml` returns 0).
    - required_status_checks.json `advisory_contexts` has an `example-phoenix` entry whose `notes` matches both "not required" and "REF-03"; `required_contexts` does NOT contain "example-phoenix".
    - The node verify command above prints OK and exits 0.
  </acceptance_criteria>
  <done>Isolated advisory example-phoenix job exists with no gate coupling; required test job no longer runs Phoenix deps; manifest records the new advisory check.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor guardrail contract test — Enum.find advisory + example-phoenix assert + lane count 8→10 (D-12, Pitfalls 2/3/5)</name>
  <files>test/guardrails/required_checks_contract_test.exs</files>
  <read_first>
    - test/guardrails/required_checks_contract_test.exs (file being modified — line 33 `[advisory] = baseline["advisory_contexts"]` single-element destructure; line 44 ci.yml job-name loop iterating `@required_contexts ++ ["viewer-evidence-live-proof"]`; line 95 `assert length(lane_entries) == 8`; existing viewer assertions reference "not required" and "D-32")
    - .planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-PATTERNS.md (the three coupled edits with exact replacement snippets)
  </read_first>
  <action>
    Edit 1 (Pitfall 2): Replace the single-element destructure `[advisory] = baseline["advisory_contexts"]` with an `Enum.find/2` lookup for the existing viewer entry — `viewer = Enum.find(baseline["advisory_contexts"], &(&1["name"] == "viewer-evidence-live-proof"))` — keeping its existing `notes =~ "not required"` and `notes =~ "D-32"` assertions retargeted at `viewer`. Then ADD a new lookup-and-assert block: `example = Enum.find(baseline["advisory_contexts"], &(&1["name"] == "example-phoenix"))`, assert `example` is truthy, assert `example["notes"] =~ "not required"` AND `example["notes"] =~ "REF-03"`, and `refute "example-phoenix" in baseline["required_contexts"]`.
    Edit 2 (Pitfall 3): In the ci.yml job-name loop, extend the iterated advisory list to `@required_contexts ++ ["viewer-evidence-live-proof", "example-phoenix"]` so the loop asserts `ci =~ "  example-phoenix:"`. Do NOT add example-phoenix to `@required_contexts` (line 8) — it stays advisory-only.
    Edit 3 (Pitfall 5): Bump the docs-contract lane-count assertion `assert length(lane_entries) == 8` to `== 10` (the two new lanes are registered in plan 76-04's `verify_docs.exs` edit). NOTE: this assertion only passes once 76-04 has registered both lanes; the wave-merge gate runs the full suite after both wave-1 plans land.
    Leave the `mix ci` alias assertion, the behavioral-wiring tests, and the fork-safe GITHUB_TOKEN/gh-api refute block untouched.
  </action>
  <verify>
    <automated>grep -q "Enum.find" test/guardrails/required_checks_contract_test.exs && grep -q "REF-03" test/guardrails/required_checks_contract_test.exs && grep -q "== 10" test/guardrails/required_checks_contract_test.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The test file uses `Enum.find(baseline["advisory_contexts"], ...)` instead of `[advisory] = ...` (the bare single-element destructure is gone).
    - The file asserts an `example-phoenix` advisory entry exists with `notes` matching "not required" and "REF-03", and `refute "example-phoenix" in baseline["required_contexts"]`.
    - The ci.yml job-name loop iterates a list that includes `"example-phoenix"` on the advisory side; `@required_contexts` is unchanged (still `~w(long-lived-live-proof release-proof signing-live-proof test)`).
    - The lane-count assertion reads `== 10` (not `== 8`).
    - After plan 76-04 lands, `mix test test/guardrails/required_checks_contract_test.exs` exits 0 (verified at the wave-merge gate).
  </acceptance_criteria>
  <done>Contract test reflects two advisory contexts and ten docs-contract lanes; engine lanes asserted unchanged; both silent-drift traps closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo checkout | CI job executes repo code; no secrets injected into the example job |
| In-repo manifest → live branch-protection | Manifest is the in-repo source of truth; live required-set reconciled externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05 | Information Disclosure | example-phoenix CI job | mitigate | Job runs only `mix deps.get && mix test` with no `secrets.*`, no `GITHUB_TOKEN`, no `gh api`. The guardrail contract test's existing fork-safe block already `refute`s token/`gh api` references in ci.yml — left intact. |
| T-76-06 | Elevation of Privilege / gate bypass | required_status_checks manifest | mitigate | `additive_only` policy enforced: new job lands only in `advisory_contexts`; contract test `refute`s `example-phoenix` in `required_contexts` and asserts the four engine lanes unchanged, so a Phoenix failure can never gate an engine merge. |
| T-76-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan (CI/YAML/JSON + Elixir test edits only; no new packages). |
</threat_model>

<verification>
- `node -e` manifest check (Task 1) prints OK.
- `grep` checks (Task 2) confirm Enum.find / REF-03 / == 10 present.
- Wave-merge gate: `mix test test/guardrails/required_checks_contract_test.exs` exits 0 once 76-04's lane registration lands.
- `grep -c "Verify Phoenix Example" .github/workflows/ci.yml` returns 0.
</verification>

<success_criteria>
Isolated, visible, non-required `example-phoenix` job exists with no gate coupling (REF-03); guardrail manifest + contract test updated; engine lanes provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/76-reference-phoenix-app-ci-and-documentation-closure/76-03-SUMMARY.md` when done.
</output>
