---
phase: 27-fallback-chains-and-honest-i18n-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/font_registry.ex
  - lib/rendro/i18n/analyzer.ex
  - test/rendro/font_registry_test.exs
  - test/rendro/i18n/analyzer_test.exs
autonomous: true
requirements: [FONT-04, I18N-02]
must_haves:
  truths:
    - "Font registry accepts fallback logical names during registration."
    - "I18n analyzer correctly identifies RTL and complex shaping scripts."
  artifacts:
    - path: "lib/rendro/font_registry.ex"
      provides: "Fallback chain configuration and retrieval"
    - path: "lib/rendro/i18n/analyzer.ex"
      provides: "Text scanning for unsupported Unicode blocks"
  key_links:
    - from: "lib/rendro/font_registry.ex"
      to: "lib/rendro/font_registry.ex"
      via: "preflight validation of fallback targets"
      pattern: "fetch_descriptor"
---

<objective>
Introduce font fallback chain registration and I18n script analysis capabilities.

Purpose: Provide the foundational rules for missing glyphs and unsupported script scenarios, allowing Rendro to explicitly manage typography boundaries instead of implicitly failing.
Output: Updated `FontRegistry` with fallback lists and a new `Rendro.I18n.Analyzer` module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/rendro/font_registry.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fallbacks to FontRegistry</name>
  <files>lib/rendro/font_registry.ex, test/rendro/font_registry_test.exs</files>
  <behavior>
    - Test 1: `register_embedded/4` and `register/3` accept `fallbacks: [:other_logical_name]`.
    - Test 2: `preflight/1` verifies that all fallback logical names actually exist in the registry.
    - Test 3: Introduce `resolve_pdf_font_chain/3` which returns `{:ok, [Rendro.PDF.Font.t()]}` including the primary font and its fallbacks.
  </behavior>
  <action>
    Modify `Rendro.FontRegistry` descriptor type to include `optional(:fallbacks) => [logical_name()]`. Update registration functions to accept and store the `fallbacks` list from opts. Update `preflight/1` to return an error if a fallback points to an unregistered logical name. Create `resolve_pdf_font_chain(registry, font_ref, default_font)` that mirrors `resolve_pdf_font/3` but recursively resolves and returns the primary font and all fallback PDF fonts in order.
  </action>
  <verify>
    <automated>mix test test/rendro/font_registry_test.exs</automated>
  </verify>
  <done>Fallback chains can be registered and resolved into lists of PDF font structs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create I18n Analyzer for script boundaries</name>
  <files>lib/rendro/i18n/analyzer.ex, test/rendro/i18n/analyzer_test.exs</files>
  <behavior>
    - Test 1: Ascii and basic Latin returns no diagnostics.
    - Test 2: Arabic/Hebrew codepoints return `%{type: :unsupported_script, reason: :rtl_required}`.
    - Test 3: Devanagari/Khmer return `%{type: :unsupported_script, reason: :complex_shaping_required}`.
  </behavior>
  <action>
    Create `Rendro.I18n.Analyzer` module with `analyze(text) :: list(diagnostic_map)`.
    Use Elixir's native binary pattern matching or `String.to_charlist` to scan the text once.
    Define Unicode ranges for RTL (Arabic, Hebrew) and Complex Shaping (Devanagari, Thai, Khmer, etc).
    If any character falls into these ranges, return the corresponding diagnostic maps. Do not spam: return maximum one of each diagnostic type per analysis call.
  </action>
  <verify>
    <automated>mix test test/rendro/i18n/analyzer_test.exs</automated>
  </verify>
  <done>Analyzer accurately flags strings containing RTL or complex text shaping characters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API → Registry | User-provided fallback lists could contain cycles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Denial of Service | `FontRegistry` | mitigate | Ensure `resolve_pdf_font_chain` limits fallback depth or handles cycles to prevent infinite loops during resolution. |
</threat_model>

<verification>
- `mix test test/rendro/font_registry_test.exs` passes.
- `mix test test/rendro/i18n/analyzer_test.exs` passes.
- Registry cleanly rejects invalid fallback targets at preflight.
</verification>

<success_criteria>
Rendro can declare fallback fonts, resolve them into a chain of `Rendro.PDF.Font.t()` structs, and identify text that falls outside the supported shaping matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/27-fallback-chains-and-honest-i18n-diagnostics/27-01-SUMMARY.md`
</output>