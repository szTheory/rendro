---
phase: 27-fallback-chains-and-honest-i18n-diagnostics
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
  - test/rendro/i18n_test.exs
autonomous: true
requirements: [FONT-04, I18N-01, I18N-02]
must_haves:
  truths:
    - "PDF Writer emits font change instructions per text run."
    - "System tests verify the end-to-end honest Unicode/i18n support matrix traps unsupported text."
  artifacts:
    - path: "lib/rendro/pdf/writer.ex"
      provides: "Multi-font PDF text stream emission"
    - path: "test/rendro/i18n_test.exs"
      provides: "Narrow support matrix regression proofs"
  key_links:
    - from: "lib/rendro/pdf/writer.ex"
      to: "lib/rendro/pipeline/measured_text.ex"
      via: "run iteration"
      pattern: "%MeasuredText{lines: lines}"
---

<objective>
Update the PDF Writer to output multi-font text runs and establish the end-to-end verification of the honest I18n matrix.

Purpose: Turn the internal measurement run data into actual PDF font switching, and lock down the promised "honest i18n" behavior (trapping and halting on unsupported text) at a system level.
Output: Modified `Rendro.PDF.Writer` handling inline `/F#` switches and `test/rendro/i18n_test.exs` ensuring explicit error emission.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/rendro/pdf/writer.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit text runs in PDF Writer</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <behavior>
    - Test 1: A `MeasuredText` with multiple runs in a line outputs `/<font_name> <size> Tf` followed by the run text.
    - Test 2: Position adjustments between runs are correctly emitted.
  </behavior>
  <action>
    Update `Rendro.PDF.Writer` where it processes `%MeasuredText{}`.
    Currently, it likely emits the font instruction once for the block and iterates lines.
    Modify it so that it iterates over the lines, and for each line, iterates over the runs.
    For each run, emit `/#{run.font.name} #{text.size} Tf` followed by the text string emission.
    Ensure text positioning handles the widths of the preceding runs if not using absolute positioning per run.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Writer correctly translates font fallback runs into PDF stream instructions.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end I18n matrix verification</name>
  <files>test/rendro/i18n_test.exs, lib/rendro/document.ex</files>
  <action>
    Create `test/rendro/i18n_test.exs` as a system test.
    Build documents with complex shaping text (e.g. Arabic) and text with completely missing glyphs (not covered by fallbacks).
    Assert that `Rendro.render/1` cleanly fails and returns `{:error, %Rendro.Error{stage: :measure, reason: {:unsupported_glyph, char}}}` when missing glyphs are encountered, and `{:error, %Rendro.Error{stage: :measure, reason: {:unsupported_script, reason}}}` when RTL or complex shaping is detected.
    Ensure the structured `Error` contains the actionable `next` message.
    Update `Rendro.Document` docs to explain the I18n diagnostic and strict failure contract.
  </action>
  <verify>
    <automated>mix test test/rendro/i18n_test.exs</automated>
  </verify>
  <done>System tests prove explicit error emission instead of silent failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Run Data → PDF Output | Font names injected into PDF commands must be valid PDF names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | `Writer` | mitigate | Trust the `font.name` from `FontRegistry` (which already sanitized logical names) but ensure formatting is correct `/#{font.name}`. |
</threat_model>

<verification>
- `mix test test/rendro/pdf/writer_test.exs` passes.
- `mix test test/rendro/i18n_test.exs` passes.
</verification>

<success_criteria>
The phase goal is met: typography support boundaries are explicit product behavior. Complex shaping and unmapped glyphs produce typed pipeline errors with actionable contexts, and font fallbacks correctly emit multi-font PDF runs.
</success_criteria>

<output>
After completion, create `.planning/phases/27-fallback-chains-and-honest-i18n-diagnostics/27-03-SUMMARY.md`
</output>
