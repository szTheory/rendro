---
phase: 70
plan: 03
type: execute
wave: 3
depends_on:
  - 70-02
files_modified:
  - priv/support_matrix.json
  - guides/api_stability.md
  - CHANGELOG.md
  - priv/schemas/support_matrix.schema.json
  - test/docs_contract/viewer_evidence_claims_test.exs
  - test/docs_contract/forms_claims_test.exs
  - guides/viewer_evidence.md
autonomous: true
requirements:
  - VIEWER-01
must_haves:
  truths:
    - All five legacy `supported` rows have `evidence`, `recorded_at`, `viewer_kind: "manual"` matching their evidence frontmatter.
    - `forms.viewers.chrome_pdfium` row unchanged (Phase 69 promotion-complete cell).
    - `embedded_files.viewers.apple_preview` remains `unverified` (D-07 independence).
    - `guides/api_stability.md` mirrors all five evidence paths via STACK template; phase-summary phrasing removed.
    - Forms section carries **two** mirrors: Preview manual (line 48) + chrome_pdfium (line 50 unchanged).
    - Tier-B JSON Schema requires promotion keys on all `supported` rows.
    - `mix rendro.viewer_evidence validate` exits 0 with **zero** legacy warnings.
    - `mix docs.contract` passes 8/8 lanes.
    - **Merge unit (D-19):** This plan executes in the same PR as 70-02 evidence files — closure Tasks 1–7 plus all Wave 2 artifacts merge atomically; 70-01 may merge independently.
  artifacts:
    - path: priv/support_matrix.json
      provides: "Five legacy row promotion-complete pointers (additive only)"
    - path: guides/api_stability.md
      provides: "STACK mirrors for all consolidated viewer evidence paths"
    - path: CHANGELOG.md
      provides: "Five per-row Changed re-home bullets under Viewer Evidence (v2.3)"
    - path: priv/schemas/support_matrix.schema.json
      provides: "Tier-B supported → required evidence/recorded_at/viewer_kind branch"
  key_links:
    - from: priv/support_matrix.json
      to: priv/viewer_evidence/forms/apple_preview.md
      via: "forms.viewers.apple_preview.evidence pointer"
    - from: guides/api_stability.md
      to: priv/viewer_evidence/protection/apple_preview.md
      via: "Protection STACK mirror sentence (line 128 replacement)"
    - from: test/docs_contract/viewer_evidence_claims_test.exs
      to: priv/schemas/support_matrix.schema.json
      via: "production validate_promotion_complete/1 + validate_matrix_structure/1"
---

<objective>
Close VIEWER-01 in one atomic public-contract wave — matrix pointers, api_stability mirrors, CHANGELOG, Tier-B schema flip, docs-contract asserts, zero legacy warnings.

Purpose: Deliver D-19 single-PR closure — evidence files from 70-02 become promotion-complete only when matrix, prose mirror, CHANGELOG, and schema enforcement land together. No matrix-first / prose-later split.
Output: Six-of-six promotion-complete supported rows; legacy warnings 5→0; full docs-contract green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-consolidate-already-validated-surfaces/70-CONTEXT.md
@.planning/phases/70-consolidate-already-validated-surfaces/70-RESEARCH.md
@.planning/phases/70-consolidate-already-validated-surfaces/70-PATTERNS.md
@.planning/phases/70-consolidate-already-validated-surfaces/70-VALIDATION.md
@guides/api_stability.md
@CHANGELOG.md
@priv/support_matrix.json
@priv/schemas/support_matrix.schema.json
@priv/viewer_evidence/forms/apple_preview.md
@priv/viewer_evidence/embedded_files/adobe_acrobat_reader.md
@priv/viewer_evidence/links/adobe_acrobat_reader.md
@priv/viewer_evidence/links/apple_preview.md
@priv/viewer_evidence/protection/apple_preview.md
@test/docs_contract/viewer_evidence_claims_test.exs
@test/docs_contract/forms_claims_test.exs
@test/docs_contract/embedded_artifact_claims_test.exs
@test/docs_contract/protection_claims_test.exs
</context>

<interfaces>
Five matrix rows to promote (additive keys only — `status` and `proof[]` unchanged):

| Matrix path | Evidence path | `viewer_kind` |
|-------------|---------------|---------------|
| `forms.viewers.apple_preview` | `priv/viewer_evidence/forms/apple_preview.md` | `"manual"` |
| `embedded_files.viewers.adobe_acrobat_reader` | `priv/viewer_evidence/embedded_files/adobe_acrobat_reader.md` | `"manual"` |
| `links.viewers.adobe_acrobat_reader` | `priv/viewer_evidence/links/adobe_acrobat_reader.md` | `"manual"` |
| `links.viewers.apple_preview` | `priv/viewer_evidence/links/apple_preview.md` | `"manual"` |
| `protection.viewers.apple_preview` | `priv/viewer_evidence/protection/apple_preview.md` | `"manual"` |

STACK.md mirror template (`.planning/research/STACK.md` line 218):
```markdown
{Viewer} is `supported` for `{surface}` based on the recorded viewer checklist for version **{viewer_version}** on **{platform}** (`priv/viewer_evidence/{surface}/{viewer}.md`). That proof confirms `{proof_ids...}` for the representative {surface} fixture.
```

Tier-B schema addition (`priv/schemas/support_matrix.schema.json` `$defs/viewer_row/allOf` third branch):
```json
{
  "if": {
    "properties": { "status": { "const": "supported" } },
    "required": ["status"]
  },
  "then": {
    "required": ["evidence", "recorded_at", "viewer_kind"]
  }
}
```

CHANGELOG five bullets (70-CONTEXT D-20) under existing `#### Viewer Evidence (v2.3)` → `##### Changed`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Promote five matrix rows (additive keys only)</name>
  <files>priv/support_matrix.json</files>
  <read_first>
    - priv/support_matrix.json (five legacy rows — forms apple_preview lines 38–46, embedded_files adobe_acrobat_reader 182–189, links adobe_acrobat_reader 210–216, links apple_preview 217–223, protection apple_preview 260–268)
    - priv/viewer_evidence/forms/apple_preview.md (recorded_at value)
    - priv/viewer_evidence/embedded_files/adobe_acrobat_reader.md
    - priv/viewer_evidence/links/adobe_acrobat_reader.md
    - priv/viewer_evidence/links/apple_preview.md
    - priv/viewer_evidence/protection/apple_preview.md
    - .planning/phases/70-consolidate-already-validated-surfaces/70-CONTEXT.md D-02, D-15
  </read_first>
  <acceptance_criteria>
    - Each of the five rows gains `"evidence"`, `"recorded_at"`, `"viewer_kind": "manual"` with `recorded_at` **exactly equal** to matching evidence frontmatter.
    - `status` and `proof[]` unchanged on all five rows.
    - `forms.viewers.chrome_pdfium` row **unchanged** (evidence, recorded_at, viewer_kind pdfium-cli intact).
    - `embedded_files.viewers.apple_preview.status` remains `"unverified"`.
    - `git diff priv/support_matrix.json` touches only the five legacy viewer rows (no other surfaces/viewers).
  </acceptance_criteria>
  <action>
    Add three additive keys per row to the five matrix paths listed in interfaces.
    Copy `recorded_at` from each evidence file frontmatter — do not invent dates.
  </action>
  <verify>
    <automated>mix run -e 'matrix = Rendro.ViewerEvidence.Matrix.load!(); for {surface, viewer} <- [{"forms","apple_preview"},{"embedded_files","adobe_acrobat_reader"},{"links","adobe_acrobat_reader"},{"links","apple_preview"},{"protection","apple_preview"}], do: row = get_in(matrix, [surface, "viewers", viewer]); assert row["evidence"]; assert row["recorded_at"]; assert row["viewer_kind"] == "manual"; end'</automated>
  </verify>
  <done>Five matrix rows promotion-complete; chrome_pdfium and embedded_files Preview unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace api_stability.md viewer mirrors (atomic unit edit)</name>
  <files>guides/api_stability.md</files>
  <read_first>
    - guides/api_stability.md (lines 48, 50, 100–106, 128)
    - priv/viewer_evidence/forms/apple_preview.md (viewer_version, platform)
    - priv/viewer_evidence/embedded_files/adobe_acrobat_reader.md
    - priv/viewer_evidence/links/adobe_acrobat_reader.md
    - priv/viewer_evidence/links/apple_preview.md
    - priv/viewer_evidence/protection/apple_preview.md
    - test/docs_contract/embedded_artifact_claims_test.exs (lines 102–106 positive guards)
    - .planning/phases/70-consolidate-already-validated-surfaces/70-CONTEXT.md D-18, D-21, D-22
  </read_first>
  <acceptance_criteria>
    - **Line 48:** Old Phase 47 sentence removed. Replaced with STACK mirror for forms × Preview sourcing `{viewer_version}` and `{platform}` from `priv/viewer_evidence/forms/apple_preview.md` frontmatter; path `priv/viewer_evidence/forms/apple_preview.md`; proof IDs `open`, `default_state_visible`, `edit_or_toggle`, `save`.
    - **Line 50:** chrome_pdfium STACK mirror **unchanged** (second forms mirror, D-18).
    - **Line 49:** Adobe Acrobat Reader forms `unverified` sentence **unchanged**.
    - **Lines 100–106 (Embedded Artifact Posture):** Edited as **one unit** — intro replaces "phase validation record" with `priv/viewer_evidence/` canonical language; Acrobat sentence gains STACK mirrors for embedded_files and links evidence paths; Preview sentence gains STACK mirror for links path; high-level facts preserved: `Adobe Acrobat Reader is \`supported\` for both \`embedded_files\` and \`links\`.` and `Apple Preview is \`supported\` for \`links\` and \`unverified\` for \`embedded_files\`.`
    - **Line 128:** Phase 54 hardcoded version sentence replaced with STACK mirror from `priv/viewer_evidence/protection/apple_preview.md` frontmatter; five protection proof IDs listed.
    - **Line 130:** Adobe Acrobat Reader protection `unverified` sentence **unchanged**.
    - `grep -q 'Phase 47 viewer checklist' guides/api_stability.md` fails (phrase removed).
    - `grep -q 'phase validation record' guides/api_stability.md` fails (phrase removed).
    - `grep -q 'Phase 54 checklist' guides/api_stability.md` fails (phrase removed).
  </acceptance_criteria>
  <action>
    Read `viewer_version` and `platform` from each of the five evidence frontmatter files.
    Replace line 48, lines 100–106 block, and line 128 per 70-RESEARCH §2.5 target shapes.
    Preserve embedded_artifact_claims_test.exs positive substring guards.
  </action>
  <verify>
    <automated>grep -q 'priv/viewer_evidence/forms/apple_preview.md' guides/api_stability.md && grep -q 'priv/viewer_evidence/embedded_files/adobe_acrobat_reader.md' guides/api_stability.md && grep -q 'priv/viewer_evidence/links/adobe_acrobat_reader.md' guides/api_stability.md && grep -q 'priv/viewer_evidence/links/apple_preview.md' guides/api_stability.md && grep -q 'priv/viewer_evidence/protection/apple_preview.md' guides/api_stability.md && ! grep -q 'phase validation record' guides/api_stability.md</automated>
  </verify>
  <done>api_stability mirrors all five evidence paths; phase-summary phrasing removed.</done>
</task>

<task type="auto">
  <name>Task 3: Append five CHANGELOG re-home bullets</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (`[0.3.0] - Unreleased` → `#### Viewer Evidence (v2.3)` → `##### Changed` — line 22–24)
    - .planning/phases/70-consolidate-already-validated-surfaces/70-CONTEXT.md D-20
    - .planning/phases/70-consolidate-already-validated-surfaces/70-PATTERNS.md §2.9
  </read_first>
  <acceptance_criteria>
    - Under `##### Changed` in `#### Viewer Evidence (v2.3)`, exactly **five** new bullets appended (after existing discipline bullet):
      1. forms × Apple Preview → `priv/viewer_evidence/forms/apple_preview.md` (**support status unchanged** since v1.8 Phase 47)
      2. embedded_files × Adobe Acrobat Reader → `priv/viewer_evidence/embedded_files/adobe_acrobat_reader.md` (**support status unchanged** since v1.9 Phase 50)
      3. links × Adobe Acrobat Reader → `priv/viewer_evidence/links/adobe_acrobat_reader.md` (**support status unchanged** since v1.9 Phase 50)
      4. links × Apple Preview → `priv/viewer_evidence/links/apple_preview.md` (**support status unchanged** since v1.9 Phase 50)
      5. protection × Apple Preview → `priv/viewer_evidence/protection/apple_preview.md` (**support status unchanged** since v1.10 Phase 54)
    - Bullets use **Changed** heading (not Added).
    - `grep -c 'support status unchanged' CHANGELOG.md` returns **≥ 5** (five new bullets each contain phrase).
    - Existing Phase 69 discipline + chrome_pdfium Added bullets unchanged.
  </acceptance_criteria>
  <action>
    Append five per-row Changed bullets per 70-RESEARCH §3.5 exact wording under existing Viewer Evidence (v2.3) Changed subsection.
  </action>
  <verify>
    <automated>grep -q 'priv/viewer_evidence/forms/apple_preview.md' CHANGELOG.md && grep -q 'priv/viewer_evidence/protection/apple_preview.md' CHANGELOG.md && grep -c 'Re-home' CHANGELOG.md | awk '{exit ($1 >= 5) ? 0 : 1}'</automated>
  </verify>
  <done>CHANGELOG documents all five re-homes with provenance and unchanged-status notes.</done>
</task>

<task type="auto">
  <name>Task 4: Flip Tier-B JSON Schema supported branch</name>
  <files>priv/schemas/support_matrix.schema.json</files>
  <read_first>
    - priv/schemas/support_matrix.schema.json (`$defs/viewer_row/allOf` lines 119–151)
    - .planning/phases/70-consolidate-already-validated-surfaces/70-CONTEXT.md D-24
    - .planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-PATTERNS.md §2.6
  </read_first>
  <acceptance_criteria>
    - Third `allOf` branch added to `$defs/viewer_row/allOf` after `unverified` branch:
      ```json
      {
        "if": {
          "properties": { "status": { "const": "supported" } },
          "required": ["status"]
        },
        "then": {
          "required": ["evidence", "recorded_at", "viewer_kind"]
        }
      }
      ```
    - Production matrix still passes `Validator.validate_matrix_structure/1` after Task 1 pointers land.
    - A synthetic `supported` row without `evidence` fails structural validation (tier-B fixture subtest pattern).
    - Flip lands in **same plan/commit** as matrix pointers (D-24) — not before.
  </acceptance_criteria>
  <action>
    Insert supported if/then branch as third element in `$defs/viewer_row/allOf` array.
    Do not modify explicit_deferral or unverified branches.
  </action>
  <verify>
    <automated>grep -q '"const": "supported"' priv/schemas/support_matrix.schema.json && mix run -e ':ok = Rendro.ViewerEvidence.Validator.validate_matrix_structure(Rendro.ViewerEvidence.Matrix.load!())'</automated>
  </verify>
  <done>Tier-B schema enforces promotion-complete keys on all supported rows.</done>
</task>

<task type="auto">
  <name>Task 5: Extend docs-contract lane and fix forms_claims drift</name>
  <files>test/docs_contract/viewer_evidence_claims_test.exs, test/docs_contract/forms_claims_test.exs</files>
  <read_first>
    - test/docs_contract/viewer_evidence_claims_test.exs (production tier-A describe block lines 8–47)
    - test/docs_contract/forms_claims_test.exs (line 23 chrome_pdfium drift)
    - .planning/phases/70-consolidate-already-validated-surfaces/70-CONTEXT.md D-23, D-25
    - .planning/phases/70-consolidate-already-validated-surfaces/70-RESEARCH.md §6
  </read_first>
  <acceptance_criteria>
    - New test `"production support matrix is promotion-complete for all supported rows"` calls `Validator.validate_promotion_complete(matrix)` on production matrix — returns `:ok`.
    - New test `"api stability guide mirrors all consolidated viewer evidence paths"` asserts all five paths in `guides/api_stability.md`.
    - New test `"viewer claim sentences do not reference phase summaries instead of evidence files"` refutes `Phase 47 viewer checklist`, `Phase 54 checklist`, `phase validation record`.
    - Update `run_full succeeds on production matrix` test to assert legacy warnings list is **empty** (or refute `"missing promotion-complete"` substring).
    - Fix `forms_claims_test.exs` line 23: `chrome_pdfium` status assert changes from `"unverified"` to `"supported"`.
    - All `refute` guards in `forms_claims_test.exs` wording test remain intact.
    - `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes.
    - `mix test test/docs_contract/forms_claims_test.exs` passes.
  </acceptance_criteria>
  <action>
    Add three production tier-A tests and update run_full expectation in `viewer_evidence_claims_test.exs`.
    Change line 23 in `forms_claims_test.exs` from `unverified` to `supported` for chrome_pdfium regex.
  </action>
  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs && mix test test/docs_contract/forms_claims_test.exs</automated>
  </verify>
  <done>Docs-contract lane enforces promotion-complete production matrix and mirror paths.</done>
</task>

<task type="auto">
  <name>Task 6: Expand viewer_evidence.md Appendix A checklists (optional but recommended)</name>
  <files>guides/viewer_evidence.md</files>
  <read_first>
    - guides/viewer_evidence.md (Appendix A lines 136–149 — forms-only today)
    - .planning/phases/70-consolidate-already-validated-surfaces/70-RESEARCH.md §10 open question #2
    - .planning/phases/70-consolidate-already-validated-surfaces/70-PATTERNS.md §2.1, §2.2 (regen one-liners)
  </read_first>
  <acceptance_criteria>
    - Appendix A includes checklist tables for **embedded_files**, **links**, and **protection** with behavior IDs matching matrix `proof[]`.
    - Each new surface subsection includes fixture regen one-liner:
      - embedded/links: `MIX_ENV=test mix run -e 'Rendro.Test.EmbeddedArtifactSupportFixture.write_fixture("test/fixtures/embedded_artifact_support_fixture.pdf")'`
      - protection: `mix run scripts/protected_viewer_proof_fixture.exs --output test/fixtures/protection_support_fixture.pdf`
    - Protection subsection notes non-deterministic regen + password-in-viewer-only discipline.
    - Existing forms checklist and eight-step quick-start unchanged.
  </acceptance_criteria>
  <action>
    Extend Appendix A with embedded_files, links, and protection checklist tables per 70-RESEARCH §5 operator checklists.
    Do not duplicate observation values from canonical evidence files (skeleton/checklist only).
  </action>
  <verify>
    <automated>grep -q 'embedded_artifact_support_fixture.pdf' guides/viewer_evidence.md && grep -q 'protection_support_fixture.pdf' guides/viewer_evidence.md && grep -q 'discoverable' guides/viewer_evidence.md && grep -q 'opens_with_open_password' guides/viewer_evidence.md</automated>
  </verify>
  <done>Operator guide documents all Phase 70 surface checklists and regen commands.</done>
</task>

<task type="auto">
  <name>Task 7: Full closure verification — zero legacy warnings, 8/8 docs-contract</name>
  <files>priv/support_matrix.json, guides/api_stability.md, CHANGELOG.md, priv/schemas/support_matrix.schema.json</files>
  <read_first>
    - test/docs_contract/embedded_artifact_claims_test.exs
    - test/docs_contract/protection_claims_test.exs
    - scripts/verify_docs.exs
    - .planning/phases/70-consolidate-already-validated-surfaces/70-VALIDATION.md (70-03-06)
  </read_first>
  <acceptance_criteria>
    - `mix rendro.viewer_evidence validate` exit 0; stderr/stdout contains **zero** `"missing promotion-complete"` warnings.
    - `mix rendro.viewer_evidence list` shows `supported=6` with empty notes column for all six rows.
    - `mix test test/docs_contract/embedded_artifact_claims_test.exs` passes — matrix regex and guide posture guards unchanged.
    - `mix test test/docs_contract/protection_claims_test.exs` passes.
    - `mix docs.contract` exits 0 (8/8 lanes).
    - `mix docs` builds without error.
    - Orphan scan: `Validator.list_orphan_evidence()` returns `[]`.
  </acceptance_criteria>
  <action>
    Run full verification sequence after all Task 1–6 edits land in one atomic commit/PR that includes all five Wave 2 evidence files from 70-02.
    Fix any broken assertions before marking plan complete.
  </action>
  <verify>
    <automated>mix rendro.viewer_evidence validate 2>&1 | grep -c 'missing promotion-complete' | grep -q '^0$' && mix test test/docs_contract/viewer_evidence_claims_test.exs && mix test test/docs_contract/forms_claims_test.exs && mix test test/docs_contract/embedded_artifact_claims_test.exs && mix test test/docs_contract/protection_claims_test.exs && mix docs.contract</automated>
  </verify>
  <done>VIEWER-01 closed: six promotion-complete rows, zero legacy warnings, all docs-contract lanes green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Public Contract Closure

| Boundary | Description |
|----------|-------------|
| Evidence facts → api_stability mirror | Version/platform copied from evidence frontmatter only — no invented viewer claims. |
| Contract changes → CHANGELOG | Every re-home recorded; support status explicitly unchanged. |
| Schema → CI | Tier-B flip rejects future pointerless supported rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-03-01 | Tampering | api_stability mirrors | High | mitigate | Copy `viewer_version`/`platform` from evidence frontmatter; preserve Acrobat unverified sentences; embedded_artifact_claims + forms_claims refute guards block overclaim. |
| T-70-03-02 | Elevation of Privilege | Support boundary widening | High | mitigate | Additive matrix keys only; `status`/`proof[]` unchanged; embedded_files Preview stays `unverified`; CHANGELOG bullets state **support status unchanged**. |
| T-70-03-03 | Tampering | Partial api_stability edit | Medium | mitigate | Edit Embedded Artifact Posture as one unit (D-22); phase-summary refute tests in docs-contract. |
| T-70-03-04 | Denial of Service | Schema flip timing | Medium | mitigate | Flip only after all five matrix pointers land in same wave (D-24); production matrix passes JSV before merge. |
| T-70-03-05 | Repudiation | CHANGELOG omission | Medium | mitigate | Five per-row Changed bullets under Viewer Evidence (v2.3); discipline rule already documented (Phase 69). |

**Block gate:** T-70-03-01 and T-70-03-02 are HIGH — mitigated by frontmatter-sourced mirrors + unchanged status discipline + docs-contract guards; no merge if family claim tests fail or legacy warnings remain.
</threat_model>

<success_criteria>
- Five matrix rows have `evidence`, `recorded_at`, `viewer_kind: "manual"` matching frontmatter.
- `guides/api_stability.md` references all five evidence paths; no phase-summary phrasing.
- Five CHANGELOG re-home bullets under Viewer Evidence (v2.3) Changed.
- Tier-B `supported` schema branch requires promotion keys.
- `mix rendro.viewer_evidence validate` — zero legacy warnings.
- `mix docs.contract` — 8/8 lanes green.
</success_criteria>

<output>
After completion, create `.planning/phases/70-consolidate-already-validated-surfaces/70-03-SUMMARY.md` via orchestration.
</output>
