---
phase: 88-launch-execution-demand-instrumentation
plan: 05
type: execute
wave: 3
depends_on:
  - 88-02
  - 88-03
  - 88-04
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md
  - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md
  - .github/ISSUE_TEMPLATE/config.yml
  - ADOPTION.md
requirements:
  - LNCH-01
  - LNCH-02
  - LNCH-03
autonomous: false
must_haves:
  truths:
    - "Launch publication is blocked until CMP-03 is reconciled, required commits/docs are pushed or published, and public GitHub/HexDocs/Livebook links are live."
    - "ElixirForum announcement is the canonical hub and shorter channels link to it."
    - "ElixirStatus, awesome-elixir PR, demand-thread replies, and mobile follow-up are published in the locked order."
    - "Final launch copy keeps the allowed core claim and refutes forbidden claims."
    - "ADOPTION.md launch snapshot records launch date, forum URL, and Hex download values before download growth is counted."
    - "Final verification runs docs-contract, CI, public URL checks, viewer-evidence checks, and GitHub intake checks."
  artifacts:
    - path: ".planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md"
      provides: "Final readiness status and external publication URL ledger"
      contains: "Ready"
    - path: ".planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md"
      provides: "Final external copy drafts and link budgets"
      contains: "Disclosure: I maintain Rendro."
    - path: "ADOPTION.md"
      provides: "Launch snapshot and post-launch review basis"
      contains: "Launch Snapshot"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Final GitHub contact-link routing to the canonical ElixirForum hub"
      contains: "blank_issues_enabled: false"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CMP-03 traceability reconciliation when public Livebook readiness is verified"
      contains: "CMP-03"
    - path: "origin/main and HexDocs"
      provides: "Public availability for GitHub raw URLs and HexDocs proof links"
      contains: "README.md, comparison.md, first_invoice.livemd, ADOPTION.md"
  key_links:
    - from: ".planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md"
      to: "ElixirForum hub URL"
      via: "Canonical launch hub field filled after publication"
      pattern: "ElixirForum announcement"
    - from: "ADOPTION.md"
      to: "https://hex.pm/api/packages/rendro"
      via: "Launch Snapshot Hex downloads.all and downloads.week values"
      pattern: "Hex downloads.all"
    - from: ".planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md"
      to: "ADOPTION.md"
      via: "Feedback and unsupported-document routing"
      pattern: "ADOPTION.md"
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "ElixirForum hub URL"
      via: "Plan 05 replaces the stable Announcing-category contact link with the final launch thread URL after publication"
      pattern: "ElixirForum announcement"
    - from: "local main"
      to: "origin/main and HexDocs"
      via: "Public sync/publish step before URL readiness checks"
      pattern: "git push origin main; mix hex.publish docs"
---

<objective>
Execute the gated public launch and record the launch snapshot.

Purpose: make Rendro visible to the Elixir community only after proof links and adoption intake are public, then preserve the URLs and launch-date data needed for post-launch demand review.
Output: reconciled launch readiness, finalized copy, published external posts/PR, ADOPTION.md launch snapshot, and final verification evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-CONTEXT.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-RESEARCH.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-PATTERNS.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-UI-SPEC.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-VALIDATION.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md
@.planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md
@ADOPTION.md
@README.md
@guides/comparison.md
@guides/livebook/first_invoice.livemd
@guides/api_stability.md
@CHANGELOG.md
@priv/support_matrix.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Sync public proof artifacts and verify CMP-03 readiness before any posting</name>
  <read_first>
    - .planning/REQUIREMENTS.md - file to inspect and, only after verified, reconcile CMP-03 status if still stale.
    - .planning/STATE.md - source for Phase 87 completion state.
    - .planning/ROADMAP.md - source for Phase 87/88 status and Phase 88 success criteria.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md - file to update with gate statuses.
    - ADOPTION.md - file to inspect for ledger readiness.
    - README.md - public GitHub readiness source.
    - guides/comparison.md - public GitHub/HexDocs readiness source.
    - guides/livebook/first_invoice.livemd - public GitHub/HexDocs readiness source.
    - guides/api_stability.md - mobile evidence public support-boundary source.
    - priv/support_matrix.json - mobile terminal row source.
    - .github/ISSUE_TEMPLATE/config.yml - source for contact-link routing to verify before publication.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-CONTEXT.md - D-03 and D-01 publication order.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-VALIDATION.md - public URL and launch gate checks.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md</files>
  <action>
    This task is blocking and must complete before any public post, PR, or demand-thread reply.

    Verify local traceability first:
    - HYG-01 through HYG-05 are complete in `.planning/REQUIREMENTS.md`.
    - GAL-01 through GAL-03 are complete in `.planning/REQUIREMENTS.md`.
    - CMP-01 and CMP-02 are complete in `.planning/REQUIREMENTS.md`.
    - CMP-03 local prerequisites are satisfied: if `.planning/REQUIREMENTS.md` still shows `[ ] **CMP-03**`, verify only local facts at this point: `guides/livebook/first_invoice.livemd` exists, ExDoc registers it, `mix rendro.livebook.check` passes, and the advisory CI lane exists outside required status checks. Do not require public Livebook URLs before the sync/publish step below. If any local prerequisite fails, mark `Comparison guide and Livebook are live` as `Blocked` and stop before public posting.

    Sync or publish public proof artifacts before URL verification:
    - Run `git status --short --branch` and confirm the working tree has only expected committed phase artifacts before publishing. If local `main` is ahead of `origin/main`, push the required committed artifacts with `git push origin main`. If the branch is not `main` or contains unrelated uncommitted work, stop and record `Blocked` in `88-LAUNCH-CHECKLIST.md` instead of publishing from an ambiguous state.
    - Build docs/package proof locally with `mix docs.contract`, `mix rendro.livebook.check`, and `mix hex.build`. If HexDocs pages are missing or stale after the GitHub push, publish docs with `mix hex.publish docs` using the maintainer's Hex credentials. If the package itself must be republished because HexDocs cannot expose the current extras for the already-published version, stop for maintainer confirmation before any `mix hex.publish package` action and record the chosen command/output in `88-LAUNCH-CHECKLIST.md`.
    - Rerun the public URL checks below after GitHub and HexDocs propagation. Do not mark the gate `Ready` until public GitHub raw URLs and HexDocs URLs pass from a clean command run.

    Verify public readiness:
    - `https://raw.githubusercontent.com/szTheory/rendro/main/README.md` returns HTTP 200 and contains `Rendered Recipe Gallery`.
    - `https://raw.githubusercontent.com/szTheory/rendro/main/guides/comparison.md` returns HTTP 200 and contains `Generating PDFs in Elixir without Chrome`.
    - `https://raw.githubusercontent.com/szTheory/rendro/main/guides/livebook/first_invoice.livemd` returns HTTP 200 and contains the first-invoice Livebook content.
    - `https://raw.githubusercontent.com/szTheory/rendro/main/ADOPTION.md` returns HTTP 200 and contains `# Adoption Signals`.
    - `https://hexdocs.pm/rendro/readme.html`, `https://hexdocs.pm/rendro/comparison.html`, and `https://hexdocs.pm/rendro/first_invoice.html` return HTTP 200.
    - `curl -fsSL https://hex.pm/api/packages/rendro | jq '.downloads'` returns `all` and `week` values.

    Reconcile CMP-03 only after public verification passes: update CMP-03 in `.planning/REQUIREMENTS.md` from unchecked to checked and add a note in `88-LAUNCH-CHECKLIST.md` naming the local checks, `git push origin main` output when used, HexDocs publish command/output when used, and the passing public Livebook URL check. If a public URL still fails after sync/publish, keep CMP-03 unchecked, mark `Comparison guide and Livebook are live` as `Blocked`, and stop before posting.

    Verify repository intake and evidence readiness:
    - `gh api repos/szTheory/rendro --jq '.has_discussions'` returns `true`.
    - GitHub's discussion category API returns the exact pair `Use cases|use-cases`.
    - D-34 labels exist.
    - `mix rendro.viewer_evidence validate` passes.
    - `mix test test/docs_contract/launch_execution_claims_test.exs test/docs_contract/adoption_claims_test.exs test/docs_contract/github_intake_claims_test.exs` passes.

    Update `88-LAUNCH-CHECKLIST.md` gate rows with `Ready`, `Blocked`, or `Deferred with reason` and the exact command output summary for each gate. Do not publish while any required gate is `Blocked`.
  </action>
  <verify>
    <automated>git status --short --branch && mix docs.contract && mix rendro.livebook.check && mix hex.build && mix test test/docs_contract/launch_execution_claims_test.exs test/docs_contract/adoption_claims_test.exs test/docs_contract/github_intake_claims_test.exs && mix rendro.viewer_evidence validate && test "$(gh api repos/szTheory/rendro --jq '.has_discussions')" = "true" && gh api graphql -f owner=szTheory -f repo=rendro -f query='query($owner:String!,$repo:String!){repository(owner:$owner,name:$repo){discussionCategories(first:20){nodes{name slug}}}}' --jq '.data.repository.discussionCategories.nodes[] | "\(.name)|\(.slug)"' | grep -Fx "Use cases|use-cases" >/dev/null && curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/README.md | grep -F "Rendered Recipe Gallery" >/dev/null && curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/guides/comparison.md | grep -F "Generating PDFs in Elixir without Chrome" >/dev/null && curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/guides/livebook/first_invoice.livemd | grep -F "First Invoice" >/dev/null && curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/ADOPTION.md | grep -F "# Adoption Signals" >/dev/null && curl -fsSL -o /dev/null https://hexdocs.pm/rendro/readme.html && curl -fsSL -o /dev/null https://hexdocs.pm/rendro/comparison.html && curl -fsSL -o /dev/null https://hexdocs.pm/rendro/first_invoice.html && curl -fsSL https://hex.pm/api/packages/rendro | jq '.downloads'</automated>
  </verify>
  <done>
    CMP-03 is either checked in `.planning/REQUIREMENTS.md` with verification notes or the plan is blocked before posting, required GitHub/HexDocs artifacts have been pushed or published with command output recorded, all public proof URLs return 200, and the launch checklist shows no required `Blocked` gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize launch copy with public URLs, link budgets, and banned-claim guards</name>
  <read_first>
    - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md - file to modify with final public URLs and copy.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md - source for verified public URLs.
    - ADOPTION.md - link target for feedback routing.
    - README.md - source of public repo/HexDocs positioning.
    - guides/comparison.md - comparison link and decision-guide language.
    - guides/livebook/first_invoice.livemd - first-invoice try path.
    - guides/api_stability.md - support boundary and mobile evidence link target.
    - test/docs_contract/launch_execution_claims_test.exs - launch copy test to keep active.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-CONTEXT.md - D-01 through D-14 and D-21.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-UI-SPEC.md - launch hub, channel contracts, link budgets, and copywriting contract.
  </read_first>
  <files>.planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md, .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md</files>
  <action>
    Replace draft-only link fields in `88-LAUNCH-COPY.md` with verified public URLs from Task 1.

    Ensure the ElixirForum hub copy uses:
    - title `Rendro: Elixir-native PDF layout without Chrome`.
    - first-mention sentence `Rendro is an open-source, Elixir-native PDF layout library for Phoenix teams that need reliable PDFs without Chrome.`
    - structure: what Rendro is, why it exists, what works today, proof links, honest boundaries, feedback request.
    - proof links for rendered gallery/manual SHA, comparison guide, first-invoice Livebook, and support matrix or API stability guide.
    - no benchmark victory language.

    Explicit decision coverage for launch hub copy:
    - D-02: Treat the ElixirForum announcement as the canonical launch hub and make shorter channels link back to it.
    - D-04: Use title `Rendro: Elixir-native PDF layout without Chrome`, category `News > Announcing`, and tags `library` and `pdf` when available.
    - D-05: Keep the announcement structure to what Rendro is, why it exists, what works today, proof links, honest boundaries, and feedback request; lead with proof links and support boundaries, not benchmark victory language.
    - D-07: Keep ElixirStatus short, publish it immediately after the forum thread, and link to the forum announcement, HexDocs, and Livebook.
    - D-08: Use the awesome-elixir PDF entry wording `Rendro - Elixir-native PDF layout library with deterministic pagination and no browser runtime.` and place it alphabetically under `PDF`.
    - D-09: Keep Show HN optional and later; do not create a required Show HN publication task.

    Ensure demand-thread replies:
    - appear only after the forum hub section.
    - include `for future readers` and `Disclosure: I maintain Rendro.` near the top.
    - use D-12 decision-guide language.
    - contain no more than three links each: public repo or HexDocs, comparison guide, and first-invoice Livebook.
    - route concrete unsupported documents to GitHub Discussions/issues and ADOPTION.md.

    Explicit decision coverage for demand-thread copy:
    - D-10: Reply to `PDF generation without Chromium dependency` first, then `Looking for a Prawn-Like PDF Generation Library in Elixir`, and only after the canonical announcement is live.
    - D-13: Limit each demand-thread reply to at most three links: public repo or HexDocs, comparison guide, and first-invoice Livebook; do not include raw benchmark tables.

    Ensure mobile follow-up copy:
    - uses title `What happens when a Rendro PDF reaches a phone?`.
    - says simple AcroForm rows can be manually proven per viewer and signed PDFs need a real validation surface.
    - links to support matrix or API stability guide and does not say `mobile PDF support`.

    Keep Show HN as a deferred non-blocking note only. Do not add a required Show HN publication step.
  </action>
  <verify>
    <automated>mix test test/docs_contract/launch_execution_claims_test.exs && ! grep -R -E "mobile PDF support|Prawn equivalent|PDF/A compliant|PDF/UA compliant|works in every viewer" .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md</automated>
  </verify>
  <done>
    Final copy has verified public URLs, required title/first mention/disclosure/mobile title, correct link budgets, and no forbidden launch claims.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Publish external launch surfaces in locked order and record final URLs</name>
  <read_first>
    - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md - file to modify with final external URLs and statuses.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-COPY.md - source copy for manual publication.
    - ADOPTION.md - file to modify with Launch Snapshot date, forum URL, Hex downloads.all, Hex downloads.week, and review baseline.
    - .github/ISSUE_TEMPLATE/config.yml - file to modify so the ElixirForum contact link points at the final launch hub URL.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-CONTEXT.md - D-01 through D-14, D-21, D-30, and D-35.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-UI-SPEC.md - channel contracts and manual publication behavior.
    - .planning/phases/88-launch-execution-demand-instrumentation/88-VALIDATION.md - manual-only verification instructions.
  </read_first>
  <files>.planning/phases/88-launch-execution-demand-instrumentation/88-LAUNCH-CHECKLIST.md, ADOPTION.md, .github/ISSUE_TEMPLATE/config.yml</files>
  <action>
    Run Task 1 readiness verification again immediately before posting.

    The maintainer publishes in this exact order:
    1. ElixirForum `News > Announcing` announcement using title `Rendro: Elixir-native PDF layout without Chrome`; use tags `library` and `pdf` if available.
    2. ElixirStatus short post linking to the forum announcement, HexDocs, and Livebook.
    3. awesome-elixir PR under `PDF`, alphabetically sorted with wording `Rendro - Elixir-native PDF layout library with deterministic pagination and no browser runtime.`
    4. ElixirForum demand-thread reply in `PDF generation without Chromium dependency`.
    5. ElixirForum demand-thread reply in `Looking for a Prawn-Like PDF Generation Library in Elixir`.
    6. Mobile evidence follow-up titled `What happens when a Rendro PDF reaches a phone?`.

    The maintainer returns the final URLs for each publication. The executor records them in `88-LAUNCH-CHECKLIST.md`.

    Replace the ElixirForum contact link in `.github/ISSUE_TEMPLATE/config.yml` from the stable Announcing category URL to the final ElixirForum launch hub URL returned by the maintainer. Keep the Discussions and ADOPTION.md contact links intact. Run the GitHub intake docs-contract test after this update so the public routing no longer points at a placeholder or pre-launch category target.

    Update `ADOPTION.md` `## Launch Snapshot` with:
    - launch date `L` as the ElixirForum hub publication date.
    - launch thread URL.
    - Hex `downloads.all` from `curl -fsSL https://hex.pm/api/packages/rendro | jq '.downloads.all'`.
    - Hex `downloads.week` from `curl -fsSL https://hex.pm/api/packages/rendro | jq '.downloads.week'`.
    - note that download growth is measured only after this launch snapshot.

    Do not post Show HN in this task.

    After the task commit for the checklist, ADOPTION.md launch snapshot, and config hub-link update exists, push the committed final launch state to `origin/main` with `git push origin main`. Verify the raw GitHub `ADOPTION.md` and `.github/ISSUE_TEMPLATE/config.yml` contents expose the final launch thread URL. If HexDocs needs to show the launch snapshot or new public guide links, republish docs with `mix hex.publish docs` and rerun the relevant public URL checks.
  </action>
  <verify>
    <automated>mix ci && mix docs.contract && mix rendro.viewer_evidence validate && mix test test/docs_contract/github_intake_claims_test.exs && curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/ADOPTION.md | grep -F "Launch Snapshot" >/dev/null && curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/.github/ISSUE_TEMPLATE/config.yml | grep -F "elixirforum.com/t/" >/dev/null && curl -fsSL https://hex.pm/api/packages/rendro | jq '.downloads' && gh pr list --repo h4cc/awesome-elixir --search "rendro" --state all --json url,title,author</automated>
  </verify>
  <done>
    All required launch surfaces have final URLs recorded in the checklist, `.github/ISSUE_TEMPLATE/config.yml` routes the ElixirForum contact link to the final launch hub URL on public GitHub, ADOPTION.md has a launch snapshot with L and Hex download values on public GitHub, demand-thread replies disclose maintainer status, mobile follow-up is published after demand replies, and final automated verification commands pass.
  </done>
</task>

</tasks>

<verification>
- `mix ci`
- `mix docs.contract`
- `mix rendro.viewer_evidence validate`
- `mix rendro.viewer_evidence list`
- `test "$(gh api repos/szTheory/rendro --jq '.has_discussions')" = "true"`
- `gh api graphql -f owner=szTheory -f repo=rendro -f query='query($owner:String!,$repo:String!){repository(owner:$owner,name:$repo){discussionCategories(first:20){nodes{name slug}}}}' --jq '.data.repository.discussionCategories.nodes[] | "\(.name)|\(.slug)"' | grep -Fx "Use cases|use-cases" >/dev/null`
- `mix test test/docs_contract/launch_execution_claims_test.exs test/docs_contract/adoption_claims_test.exs test/docs_contract/github_intake_claims_test.exs test/docs_contract/viewer_evidence_claims_test.exs test/docs_contract/forms_claims_test.exs test/docs_contract/signing_claims_test.exs test/docs_contract/raster_claims_test.exs`
- `curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/README.md | grep -F "Rendered Recipe Gallery" >/dev/null`
- `curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/guides/comparison.md | grep -F "Generating PDFs in Elixir without Chrome" >/dev/null`
- `curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/guides/livebook/first_invoice.livemd | grep -F "First Invoice" >/dev/null`
- `curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/ADOPTION.md | grep -F "# Adoption Signals" >/dev/null`
- `curl -fsSL https://raw.githubusercontent.com/szTheory/rendro/main/.github/ISSUE_TEMPLATE/config.yml | grep -F "elixirforum.com/t/" >/dev/null`
- `curl -fsSL -o /dev/null https://hexdocs.pm/rendro/readme.html`
- `curl -fsSL -o /dev/null https://hexdocs.pm/rendro/comparison.html`
- `curl -fsSL -o /dev/null https://hexdocs.pm/rendro/first_invoice.html`
- `curl -fsSL https://hex.pm/api/packages/rendro | jq '.downloads'`
- `gh issue list --state all --label "adoption:signal" --json number,title,author,createdAt,url,labels`
- `gh issue list --state all --label "area:text-shaping" --json number,title,author,createdAt,url,labels`
- `gh pr list --state merged --search "merged:>=$LAUNCH_DATE -author:szTheory -author:dependabot[bot]" --json number,title,author,mergedAt,url | jq '[.[] | select(((.author.is_bot // false) | not) and .author.login != "dependabot[bot]") ]'` plus manual rejection of typo-only PRs and maintainer alternate accounts listed in `ADOPTION.md`.
</verification>

<success_criteria>
- CMP-03 is reconciled before public posting.
- Required artifacts are pushed or published before URL readiness is marked Ready.
- Public GitHub and HexDocs URLs for README, comparison guide, Livebook, and ADOPTION.md are live before the first external post.
- ElixirForum, ElixirStatus, awesome-elixir PR, two demand-thread replies, and mobile follow-up are published in the locked order.
- `ADOPTION.md` records the launch snapshot and does not count download growth before that snapshot.
- Final checks pass and no required CI lane depends on manual mobile evidence.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local repo state to public ecosystem posts | Local artifacts must be public before external readers click links. |
| Maintainer manual posting to public community surfaces | Human account actions publish final copy and URLs. |
| Launch snapshot to demand-gate review | Hex/GitHub counters become review inputs only after L is recorded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-88-17 | Tampering | CMP-03 reconciliation | mitigate | Task 1 only checks CMP-03 after local and public Livebook proof pass; otherwise posting stops. |
| T-88-18 | Repudiation | Public launch URLs | mitigate | Task 3 records final URLs in checklist and forum URL in ADOPTION.md launch snapshot. |
| T-88-19 | Information Disclosure | External posts and adoption snapshot | mitigate | Posts use public proof links only; ADOPTION.md records aggregate Hex counts and public URLs, not private adopter details. |
| T-88-20 | Denial of Service | Maintainer response load | mitigate | Elixir-first hub launch centralizes discussion, avoids same-day broad blitz, and keeps Show HN deferred. |
| T-88-21 | Spoofing | Demand-thread posture | mitigate | Replies disclose `Disclosure: I maintain Rendro.` and do not present maintainer copy as neutral third-party advice. |
</threat_model>

<output>
Create `.planning/phases/88-launch-execution-demand-instrumentation/88-05-SUMMARY.md` when this plan completes.
</output>
