---
phase: 83
plan: 05
type: execute
wave: 4
depends_on:
  - 83-01
  - 83-04
files_modified:
  - priv/support_matrix.json
  - test/docs_contract/script_support_claims_test.exs
  - test/docs_contract/public_api_contract_test.exs
  - guides/api_stability.md
  - mix.exs
  - priv/public_api.json
autonomous: true
requirements:
  - HYG-05
  - HYG-01

must_haves:
  truths:
    - "priv/support_matrix.json has a text_shaping top-level key with arabic, hebrew_rtl, devanagari, and thai rows, each with status: explicit_deferral and evidence_deferred string >= 40 chars"
    - "D-10: the matrix records explicit_deferral rows for exactly the four named script families (Arabic, Hebrew/RTL, Devanagari, Thai); the runtime gate being broader than the matrix is the honest direction of mismatch"
    - "test/docs_contract/script_support_claims_test.exs exists and asserts all four explicit_deferral entries"
    - "Rendro.Text.Shaper is no longer in the hidden_modules list in public_api_contract_test.exs"
    - "mix rendro.api.gen runs successfully and priv/public_api.json byte-matches the regenerated output"
    - "guides/api_stability.md lists Rendro.Text.Shaper and Rendro.Text.Shaper.Simple in Tier-1 Stable"
    - "mix test test/docs_contract/ passes including the new script_support_claims_test.exs"
  artifacts:
    - path: "priv/support_matrix.json"
      provides: "text_shaping section with four explicit_deferral rows"
      contains: "text_shaping"
    - path: "test/docs_contract/script_support_claims_test.exs"
      provides: "Docs-contract test for script support claims"
      contains: "explicit_deferral"
    - path: "priv/public_api.json"
      provides: "Updated public API manifest"
      contains: "Rendro.Text.Shaper"
    - path: "guides/api_stability.md"
      provides: "Updated tier listing"
      contains: "Rendro.Text.Shaper.Simple"
  key_links:
    - from: "test/docs_contract/script_support_claims_test.exs"
      to: "priv/support_matrix.json"
      via: "File.read! assertion"
      pattern: "support_matrix.json"
    - from: "test/docs_contract/public_api_contract_test.exs"
      to: "priv/public_api.json"
      via: "byte-comparison assertion"
      pattern: "public_api.json"
---

<objective>
Finalize the Phase 83 docs-contract and public API manifest work: (1) add the `text_shaping` section to `priv/support_matrix.json` with four `explicit_deferral` rows for Arabic, Hebrew/RTL, Devanagari, and Thai; (2) create `test/docs_contract/script_support_claims_test.exs` asserting those rows; (3) remove `Rendro.Text.Shaper` from the `hidden_modules` list in the public API contract test; (4) run `mix rendro.api.gen` to regenerate `priv/public_api.json`; (5) update `guides/api_stability.md` tier listings for the two new stable-tier modules and the HarfBuzz adapter.

Purpose: Delivers HYG-05 (matrix rows + docs-contract guard) and completes D-06 (full mechanical churn path for promoting Shaper to public). The "pure Elixir core" claim is now structurally true AND bounded by docs-contract tests.

Output: Updated support_matrix.json, new script_support_claims_test.exs, updated public_api_contract_test.exs, updated public_api.json, updated api_stability.md.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-claim-accuracy-shaping-hygiene/83-CONTEXT.md
@.planning/phases/83-claim-accuracy-shaping-hygiene/83-RESEARCH.md
@.planning/phases/83-claim-accuracy-shaping-hygiene/83-PATTERNS.md
@.planning/phases/83-claim-accuracy-shaping-hygiene/83-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From priv/support_matrix.json (existing explicit_deferral vocabulary to match):
```json
"pdfjs": {
  "status": "explicit_deferral",
  "evidence_deferred": "PDF.js failed the forms four-check save-and-reopen round-trip..."
}
```
Required fields: "status" + "evidence_deferred" (minLength: 40). Forbidden: "evidence", "recorded_at", "viewer_kind".

Target text_shaping section (from PATTERNS.md §support_matrix.json):
```json
"text_shaping": {
  "latin_and_cjk": { "status": "supported", "capabilities": [...], "engine": "..." },
  "arabic": { "status": "explicit_deferral", "evidence_deferred": "Arabic shaping requires contextual glyph substitution, joining forms, and right-to-left reordering that Shaper.Simple does not implement; full shaping is demand-gated at LNCH-03. Use harfbuzz_ex optional dep with config :rendro, shaper: Rendro.Adapters.HarfBuzz." },
  "hebrew_rtl": { "status": "explicit_deferral", "evidence_deferred": "Hebrew rendering requires UAX #9 bidi reordering which is not implemented in Rendro.Text.Bidi; visual reordering and RTL line presentation are deferred to v2.7 behind the LNCH-03 demand gate." },
  "devanagari": { "status": "explicit_deferral", "evidence_deferred": "Devanagari and other Indic scripts require complex glyph reordering, conjunct formation, and matra positioning that Shaper.Simple does not implement; deferred to v2.7 behind the LNCH-03 demand gate." },
  "thai": { "status": "explicit_deferral", "evidence_deferred": "Thai and other SEA scripts require cluster-aware line breaking, vowel positioning, and tone mark handling that Shaper.Simple does not implement; deferred to v2.7 behind the LNCH-03 demand gate." }
}
```

From test/docs_contract/public_api_contract_test.exs (CURRENT hidden_modules at line ~85–92):
```elixir
hidden_modules = [
  Rendro.PDF.CidFont,
  Rendro.PDF.FontSubsetter,
  Rendro.Text.Bidi,
  Rendro.Text.Shaper,     # line 89 — REMOVE this entry
  Rendro.Format,
  Rendro.Audit
]
```

From test/docs_contract/signing_claims_test.exs (template for new test — lane self-registration pattern):
```elixir
test "docs verification script includes the signing claims lane" do
  script = File.read!("scripts/verify_docs.exs")
  assert script =~ ~s|{"Signing semantic-claims lane", ["test", ...]}|
end
```

From guides/api_stability.md (additions needed):
```markdown
# Tier-1 Stable section: add line:
**Text shaping behaviour:** `Rendro.Text.Shaper` (behaviour) and `Rendro.Text.Shaper.Simple` (pure-Elixir default implementation)

# Tier-2 Evolving / Adapters section: add HarfBuzz:
**Adapter modules:** `Rendro.Adapters.PyHanko`, `Rendro.Adapters.Qpdf`, `Rendro.Adapters.HarfBuzz`, and all other `Rendro.Adapters.*` modules.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add text_shaping section to support_matrix.json + create script_support_claims_test.exs</name>
  <files>priv/support_matrix.json, test/docs_contract/script_support_claims_test.exs</files>
  <read_first>
    - priv/support_matrix.json — full file: read the entire JSON structure to find the correct insertion point for the text_shaping key (must be a top-level key in the root JSON object); verify the current pdfjs explicit_deferral row for exact vocabulary
    - priv/schemas/support_matrix.schema.json — read to verify additionalProperties: true at root (Pitfall 5 in RESEARCH.md); confirm the schema does not reject the new text_shaping section
    - test/docs_contract/signing_claims_test.exs — full file: use as the structural template for the new test
    - scripts/verify_docs.exs — read to understand the lane registration format (needed for the second test in the new file)
    - .planning/phases/83-claim-accuracy-shaping-hygiene/83-PATTERNS.md — §support_matrix.json and §script_support_claims_test.exs sections (exact JSON and test code)
  </read_first>
  <behavior>
    - priv/support_matrix.json contains a "text_shaping" top-level key
    - "arabic", "hebrew_rtl", "devanagari", "thai" entries each have status "explicit_deferral" and evidence_deferred string >= 40 chars
    - "latin_and_cjk" entry has status "supported"
    - Schema validation passes: `mix run -e "Rendro.SupportMatrix.validate!()"` or equivalent
    - New test file: `Rendro.DocsContract.ScriptSupportClaimsTest` with tests asserting the four explicit_deferral entries
    - New test file: second test asserts the lane is registered in scripts/verify_docs.exs
    - `mix test test/docs_contract/script_support_claims_test.exs` passes
  </behavior>
  <action>
    Edit priv/support_matrix.json: add the "text_shaping" object as a new top-level key in the root JSON object. Use the exact structure from PATTERNS.md §support_matrix.json. Insert it after the last existing top-level key (maintain valid JSON — ensure trailing comma handling is correct before the closing brace). Each of the four explicit_deferral entries must have evidence_deferred strings that are >= 40 characters (already true per the examples in PATTERNS.md). The latin_and_cjk entry uses "status": "supported" with "capabilities" and "engine" fields — these are NOT viewer rows and do not use the restricted viewer_row vocabulary.

    Create test/docs_contract/script_support_claims_test.exs: use signing_claims_test.exs as the structural template. Module name: `Rendro.DocsContract.ScriptSupportClaimsTest`. First test: "support matrix has text_shaping section with four explicit_deferral entries" — assert the matrix JSON contains "text_shaping", "arabic", "hebrew_rtl", "devanagari", "thai"; assert each of the four has "explicit_deferral" status via regex; assert evidence_deferred strings are non-empty; assert latin_and_cjk has "supported" status; refute any false overclaims. Second test: "docs verification script includes the script support claims lane" — assert `scripts/verify_docs.exs` contains the string `{"Script support claims lane", ["test", "test/docs_contract/script_support_claims_test.exs"]}`. Also register the new lane in scripts/verify_docs.exs by adding the lane tuple in the correct location.
  </action>
  <verify>
    <automated>mix test test/docs_contract/script_support_claims_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>mix test test/docs_contract/script_support_claims_test.exs passes; priv/support_matrix.json valid JSON with text_shaping key; scripts/verify_docs.exs contains the lane registration string</done>
</task>

<task type="auto">
  <name>Task 2: Promote Shaper to public — remove from hidden_modules, regenerate API manifest, update api_stability.md</name>
  <files>test/docs_contract/public_api_contract_test.exs, priv/public_api.json, guides/api_stability.md, mix.exs</files>
  <read_first>
    - test/docs_contract/public_api_contract_test.exs — full file: find lines 85–92 (hidden_modules list), understand Assertion 3 structure and the false-pass guard (lines 98–101 must NOT be changed — it catches renames/deletions)
    - guides/api_stability.md — full file: find the Tier-1 Stable section and Tier-2 Evolving/Adapters section to locate exact insertion points; verify the test at test/docs_contract/api_stability_claims_test.exs line ~5–26 to understand which exact header strings must be preserved verbatim
    - test/docs_contract/api_stability_claims_test.exs — read lines 1–30 to find assert guide =~ "## Tier-1 Stable" and similar guards that must still pass after editing
    - lib/rendro/public_api.ex (current state) — confirm @adapter_files already includes harfbuzz.ex (Plan 01 Task 2)
    - .planning/phases/83-claim-accuracy-shaping-hygiene/83-PATTERNS.md — §public_api_contract_test.exs (hidden_modules target list) and §guides/api_stability.md (tier list additions)
    - .planning/phases/83-claim-accuracy-shaping-hygiene/83-CONTEXT.md — D-06 (mechanical churn path checklist)
    - mix.exs — lines 137–180 (groups_for_modules) — verify Rendro.Text.Shaper and Rendro.Text.Shaper.Simple are already in the "Core Builder API" group (from Plan 01 Task 2); if not, add them now
  </read_first>
  <action>
    Edit test/docs_contract/public_api_contract_test.exs: remove `Rendro.Text.Shaper` from the `hidden_modules` list at line ~89. Do NOT change any other entry in the list. Do NOT change the false-pass guard (lines 98–101). Save.

    Edit guides/api_stability.md: in the Tier-1 Stable section, add a new bullet: "**Text shaping behaviour:** `Rendro.Text.Shaper` (behaviour) and `Rendro.Text.Shaper.Simple` (pure-Elixir default implementation)". In the Tier-2 Evolving (Adapters) section, add `Rendro.Adapters.HarfBuzz` to the list of named adapter modules. Preserve all existing header strings (## Tier-1 Stable, ## Tier-2 Evolving, etc.) verbatim — api_stability_claims_test.exs asserts these exact strings.

    Verify mix.exs groups_for_modules: confirm `Rendro.Text.Shaper` and `Rendro.Text.Shaper.Simple` are present in "Core Builder API" and `Rendro.Adapters.HarfBuzz` is present in "Ecosystem Adapters" (from Plan 01 Task 2). If either is missing, add now.

    Run `mix rendro.api.gen` to regenerate priv/public_api.json. This will compile with the optional-dep compile gate (harfbuzz_ex may or may not be loaded during generation — verify the manifest contains `Rendro.Text.Shaper` and `Rendro.Text.Shaper.Simple` entries with `:stable` tier tags). Run `mix test test/docs_contract/public_api_contract_test.exs` — it must pass. If it fails because the JSON byte-comparison is wrong, run `mix rendro.api.gen` again to ensure the manifest is current, then re-run the test.
  </action>
  <verify>
    <automated>mix rendro.api.gen && mix test test/docs_contract/public_api_contract_test.exs test/docs_contract/api_stability_claims_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>public_api_contract_test.exs passes (hidden_modules no longer includes Rendro.Text.Shaper, manifest byte-matches); api_stability_claims_test.exs passes; priv/public_api.json contains Rendro.Text.Shaper with tier: stable; guides/api_stability.md lists both new stable-tier modules and the HarfBuzz adapter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support_matrix.json writes | JSON file is machine-written and CI-validated; no user input flows through this file |
| public_api.json regeneration | mix rendro.api.gen reads source code introspectively — no external network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-11 | Repudiation | Overclaiming script support in docs | mitigate | script_support_claims_test.exs asserts explicit_deferral status for all four gated scripts; CI will fail if claims drift |
| T-83-12 | Repudiation | "pure Elixir core" claim accuracy | mitigate | HYG-01 (Plan 01) makes the claim structurally true; this plan adds docs-contract CI enforcement via public_api_contract_test.exs showing Shaper as stable-tier public module |
| T-83-13 | Tampering | priv/public_api.json stale manifest | mitigate | public_api_contract_test.exs byte-compares the manifest against a fresh `mix rendro.api.gen` run; drift fails CI |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `mix test test/docs_contract/` — all docs-contract tests pass including new script_support_claims_test.exs
2. `grep "Rendro.Text.Shaper" test/docs_contract/public_api_contract_test.exs` — no match in hidden_modules list
3. `grep "text_shaping" priv/support_matrix.json` — shows the text_shaping key
4. `grep "explicit_deferral" priv/support_matrix.json | wc -l` — at least 4 entries (arabic, hebrew_rtl, devanagari, thai plus any pre-existing ones)
5. `grep "Rendro.Text.Shaper" priv/public_api.json` — shows entries with stable tier
6. `grep "Rendro.Adapters.HarfBuzz" guides/api_stability.md` — shows entry in Tier-2
7. `mix test` — full suite passes
</verification>

<success_criteria>
- priv/support_matrix.json: text_shaping section with five entries (latin_and_cjk supported + four explicit_deferral)
- test/docs_contract/script_support_claims_test.exs: new file, all tests pass
- public_api_contract_test.exs: Rendro.Text.Shaper removed from hidden_modules; test passes
- priv/public_api.json: regenerated, byte-matches, contains Rendro.Text.Shaper with stable tier
- guides/api_stability.md: Tier-1 lists Rendro.Text.Shaper + Simple; Tier-2 lists Rendro.Adapters.HarfBuzz
- mix test test/docs_contract/ passes
- mix test (full suite) passes — all five phase requirements met
</success_criteria>

<output>
Create `.planning/phases/83-claim-accuracy-shaping-hygiene/83-05-SUMMARY.md` when done.
</output>
