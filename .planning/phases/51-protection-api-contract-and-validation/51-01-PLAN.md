---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: ["50-03"]
files_modified:
  - lib/rendro/protect.ex
  - lib/rendro/protect/adapter.ex
  - lib/rendro/adapters/qpdf.ex
  - lib/rendro/error.ex
  - test/rendro/protect_test.exs
  - test/rendro/adapters/qpdf_test.exs
  - test/rendro/error_test.exs
autonomous: true
requirements:
  - PROTECT-01
  - PROTECT-02
  - PROTECT-03
must_haves:
  truths:
    - `Rendro.Protect.password/2` remains the canonical public boundary, and `Rendro.render_protected/3` remains thin sugar over artifact-first composition.
    - The public protection surface accepts only `:aes_256`, requires both passwords, and rejects malformed options before any adapter invocation.
    - qpdf remains an optional runtime executable with typed `:protect`-stage failures rather than a hard dependency or hidden global configuration.
  artifacts:
    - path: lib/rendro/protect.ex
      provides: "Public protection API, boundary validation, and redacted failure details"
    - path: lib/rendro/adapters/qpdf.ex
      provides: "Runtime qpdf adapter with predictable execution and cleanup behavior"
    - path: lib/rendro/error.ex
      provides: "Typed `:protect` error envelope and operator guidance"
  key_links:
    - from: lib/rendro.ex
      to: lib/rendro/protect.ex
      via: "`Rendro.render_protected/3` composes render-to-artifact with the canonical protection boundary"
    - from: lib/rendro/protect.ex
      to: lib/rendro/adapters/qpdf.ex
      via: "validated normalized options cross the optional adapter seam only after public checks pass"
    - from: test/rendro/protect_test.exs
      to: lib/rendro/error.ex
      via: "public failures remain typed and redacted under the `:protect` stage"
---

<objective>
Stabilize the public protection contract and the first-party qpdf runtime seam.

Purpose: turn the already-landed protection surface into a fully locked Phase 51 contract rather than leaving edge cases and operational gaps to later phases.
Output: hardened public API semantics, typed failure coverage, and qpdf execution behavior that remains optional-runtime and password-safe.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-protection-api-contract-and-validation/51-CONTEXT.md
@.planning/phases/51-protection-api-contract-and-validation/51-RESEARCH.md
@.planning/phases/51-protection-api-contract-and-validation/51-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@guides/api_stability.md
</execution_context>

<context>
@lib/rendro.ex
@lib/rendro/protect.ex
@lib/rendro/protect/adapter.ex
@lib/rendro/adapters/qpdf.ex
@lib/rendro/error.ex
@test/rendro/protect_test.exs
@test/rendro/adapters/qpdf_test.exs
@test/rendro/error_test.exs

<interfaces>
From `lib/rendro/protect.ex`:
```elixir
@spec password(Artifact.t(), options()) :: {:ok, Artifact.t()} | {:error, Error.t()}
def password(%Artifact{} = artifact, opts) when is_list(opts) do
  with {:ok, normalized} <- normalize_opts(opts),
       {:ok, protected_binary} <- normalized.adapter.protect(artifact, normalized) do
    ...
  end
end
```

From `lib/rendro/adapters/qpdf.ex`:
```elixir
@spec protect(Rendro.Artifact.t(), map()) :: {:ok, binary()} | {:error, term()}
def protect(%Rendro.Artifact{} = artifact, opts) when is_map(opts) do
  ...
end
```

From `lib/rendro/error.ex`:
```elixir
defp what(:protect, _reason), do: "PDF protection failed while wrapping the rendered artifact."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the public protection contract at the artifact boundary</name>
  <files>lib/rendro/protect.ex, lib/rendro/protect/adapter.ex, test/rendro/protect_test.exs, test/rendro/error_test.exs</files>
  <behavior>
    - `Rendro.Protect.password/2` remains the normative API and rejects malformed options before adapter execution.
    - `Rendro.render_protected/3` stays obvious delegation and does not grow separate semantics or hidden defaults.
    - Only `:aes_256` passes the public contract, both passwords remain required, and advisory permissions remain curated and explicit.
  </behavior>
  <action>
    Keep `Rendro.Protect.password/2` as the single canonical public seam per D-01 through D-03, and keep `Rendro.render_protected/3` as thin sugar only. If normalization stays inline in `Rendro.Protect`, make that code clearer and better-factored without changing the public shape; do not extract a helper module solely to match stale planning prose.
    Tighten option-boundary behavior per D-04 through D-09 and D-18 through D-20: require both non-empty binary passwords, accept only `algorithm: :aes_256`, keep adapter override explicit-per-call, and keep the advisory permission surface curated rather than qpdf-shaped.
    Extend `test/rendro/protect_test.exs` and `test/rendro/error_test.exs` so the phase proves invalid algorithms, invalid adapter values, invalid password values, and invalid advisory permission shapes never reach adapter execution and always surface as typed `Rendro.Error` values under stage `:protect`.
    Preserve the existing artifact-first story in docs and tests by ensuring `Protect.render_protected/3` and `Rendro.render_protected/3` remain composition wrappers, not a second product path with different metadata or error semantics.
  </action>
  <verify>
    <automated>mix test test/rendro/protect_test.exs test/rendro/error_test.exs</automated>
  </verify>
  <done>The public protection API is explicit, narrow, and fully enforced before any adapter call happens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden qpdf execution and the typed protect-stage failure envelope</name>
  <files>lib/rendro/adapters/qpdf.ex, lib/rendro/error.ex, test/rendro/adapters/qpdf_test.exs, test/rendro/protect_test.exs, test/rendro/error_test.exs</files>
  <behavior>
    - qpdf remains an optional runtime executable with typed missing-executable behavior.
    - Adapter execution failures remain redacted and actionable to callers.
    - Temporary files and directories created for qpdf runs are cleaned up predictably on both success and failure paths.
  </behavior>
  <action>
    Harden `Rendro.Adapters.Qpdf` around its operational seam: keep executable lookup and command execution injectable for tests, keep qpdf out of `mix.exs`, and improve temp-file lifecycle behavior so the adapter does not leave stray temp directories behind on non-success paths.
    Preserve the public error envelope by ensuring adapter failures continue to surface through `Rendro.Error.from_stage(:protect, ...)` with safe details only. Do not leak password values, raw argfile contents, or verbose process output into broad default metadata or error details.
    Extend adapter tests to cover failure-path cleanup and typed error behavior for missing executables, non-zero qpdf exits, and command-runner failures. Reuse the existing injectable runner/finder pattern rather than introducing mocks or heavier runtime plumbing.
    Keep the adapter behavior minimal (`protect/2`) and avoid widening into global adapter config, broad qpdf flag passthrough, or richer backend abstraction than this milestone needs.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/qpdf_test.exs test/rendro/protect_test.exs test/rendro/error_test.exs</automated>
  </verify>
  <done>The qpdf adapter is operationally hardened while preserving the narrow optional-runtime contract and typed public failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> `Rendro.Protect` | Untrusted authored passwords and options cross into the public protection contract here. |
| `Rendro.Protect` -> qpdf executable | Validated options cross from pure Elixir into an external process boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | `Rendro.Protect.password/2` | mitigate | Reject malformed adapter, algorithm, password, and advisory-permission input before any adapter invocation. |
| T-51-02 | Information disclosure | `Rendro.Error` / qpdf seam | mitigate | Redact password material from error details and never surface argfile contents or secrets in typed failures. |
| T-51-03 | Denial of service / residue | `Rendro.Adapters.Qpdf` | mitigate | Ensure temp paths are cleaned up on failure and success so repeated failures do not accumulate runtime scratch. |
</threat_model>

<verification>
mix test test/rendro/protect_test.exs test/rendro/adapters/qpdf_test.exs test/rendro/error_test.exs
</verification>

<success_criteria>
- The canonical public protection API remains artifact-first and explicit.
- Invalid protection input is rejected before adapter execution with typed `:protect` errors.
- qpdf remains runtime-optional and leaves no avoidable temp-dir residue on failure paths.
- No algorithm or permission surface wider than the locked Phase 51 contract is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/51-protection-api-contract-and-validation/51-01-SUMMARY.md`
</output>
