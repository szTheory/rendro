---
phase: 51
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - lib/rendro/protect.ex
  - lib/rendro/artifact.ex
  - lib/rendro/audit.ex
  - test/rendro/protect_test.exs
  - test/rendro/artifact_test.exs
  - test/rendro/audit_test.exs
  - test/docs_contract/protection_claims_test.exs
  - guides/api_stability.md
  - guides/integrations.md
  - priv/support_matrix.json
autonomous: true
requirements:
  - PROTECT-01
  - PROTECT-03
must_haves:
  truths:
    - Protected artifacts expose only a minimal, audit-safe `metadata.protection` shape and set `metadata.deterministic` truthfully to `false`.
    - Password material never leaks through artifact metadata, error details, audit metadata, or integration guidance.
    - Any docs or support-matrix adjustments remain narrow and truthful, preserving the artifact-first and advisory-permissions posture without widening into viewer or compliance claims.
  artifacts:
    - path: lib/rendro/protect.ex
      provides: "Stable narrow protection metadata shape and redacted details"
    - path: lib/rendro/audit.ex
      provides: "Verified password-safe metadata scrubbing across audit boundaries"
    - path: test/docs_contract/protection_claims_test.exs
      provides: "Regression lock that docs and support claims stay aligned with the code truth"
  key_links:
    - from: lib/rendro/protect.ex
      to: lib/rendro/artifact.ex
      via: "protected output remains a normal artifact with merged metadata rather than a second result container"
    - from: lib/rendro/protect.ex
      to: lib/rendro/audit.ex
      via: "the protection metadata shape must survive audit scrubbing without password leakage"
    - from: test/docs_contract/protection_claims_test.exs
      to: guides/api_stability.md
      via: "public wording remains locked to the same narrow support boundary the code implements"
---

<objective>
Lock artifact metadata, redaction behavior, and regression proof around the protection contract.

Purpose: finish Phase 51 by proving that protection remains auditable and truthful without leaking secrets or widening the public support story.
Output: stable protection metadata, audit-redaction coverage, and regression tests/docs aligned with the current artifact-first boundary.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-protection-api-contract-and-validation/51-CONTEXT.md
@.planning/phases/51-protection-api-contract-and-validation/51-RESEARCH.md
@.planning/phases/51-protection-api-contract-and-validation/51-PATTERNS.md
@guides/api_stability.md
@guides/integrations.md
@priv/support_matrix.json
</execution_context>

<context>
@lib/rendro/protect.ex
@lib/rendro/artifact.ex
@lib/rendro/audit.ex
@test/rendro/protect_test.exs
@test/rendro/artifact_test.exs
@test/docs_contract/protection_claims_test.exs

<interfaces>
From `lib/rendro/protect.ex`:
```elixir
Artifact.wrap(
  protected_binary,
  artifact,
  %{deterministic: false, protection: protection_metadata(normalized)}
)
```

From `lib/rendro/audit.ex`:
```elixir
def scrub_metadata(metadata) when is_map(metadata) do
  metadata
  |> Map.drop([:password, :open_password, :owner_password, "password", "open_password", "owner_password"])
  ...
end
```

From `guides/integrations.md`:
```markdown
The worker also does **not** accept password or protection fields in job args.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock minimal protected-artifact metadata and regression coverage</name>
  <files>lib/rendro/protect.ex, lib/rendro/artifact.ex, test/rendro/protect_test.exs, test/rendro/artifact_test.exs</files>
  <behavior>
    - Protected artifacts remain ordinary `%Rendro.Artifact{}` values with merged metadata rather than a second result shape.
    - `metadata.deterministic` flips to `false` truthfully for protected output.
    - `metadata.protection` stays minimal and stable: presence, algorithm, curated advisory permissions, and safe booleans only.
  </behavior>
  <action>
    Finalize the protected-artifact metadata shape in `Rendro.Protect` and, if useful, document it in `Rendro.Artifact` moduledoc or tests. Preserve the narrow shape from D-14 through D-17 and do not add passwords, tool version, stderr/stdout, or richer policy detail to default metadata.
    Extend regression tests so they prove the exact metadata posture of protected artifacts, including `deterministic: false`, retained base artifact fields such as `page_count`, and absence of password material.
    Keep artifact wrapping on `Rendro.Artifact.wrap/3` and do not introduce a separate protected-artifact struct or a document-authored protection state. Any code cleanup here should serve contract clarity only.
  </action>
  <verify>
    <automated>mix test test/rendro/protect_test.exs test/rendro/artifact_test.exs</automated>
  </verify>
  <done>Protected output has one small, explicit, password-safe metadata contract backed by regression tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove audit redaction and keep support wording truthful</name>
  <files>lib/rendro/audit.ex, test/rendro/audit_test.exs, test/docs_contract/protection_claims_test.exs, guides/api_stability.md, guides/integrations.md, priv/support_matrix.json</files>
  <behavior>
    - Password-related keys are scrubbed before metadata crosses audit-facing boundaries.
    - Integration guidance remains explicit that background job payloads and delivery seams should not persist protection secrets.
    - Docs and support-matrix wording stay narrow: artifact-first, AES-256 only, advisory permissions are not a security guarantee, and viewer rows remain unverified unless separately proven.
  </behavior>
  <action>
    Add focused audit redaction tests that pass protection-shaped metadata through `Rendro.Audit.scrub_metadata/1` and prove password keys are removed recursively while safe booleans and non-secret metadata remain.
    Reconcile `guides/api_stability.md`, `guides/integrations.md`, `priv/support_matrix.json`, and `test/docs_contract/protection_claims_test.exs` only as needed to keep them aligned with the locked Phase 51 contract. Do not widen into native encryption support, compliance claims, or promoted viewer support.
    Preserve the current truthful distinction between password-to-open protection and advisory permissions, and preserve the explicit guidance that Oban/storage/delivery seams transport protected artifacts without learning the passwords themselves.
  </action>
  <verify>
    <automated>mix test test/rendro/audit_test.exs test/docs_contract/protection_claims_test.exs test/rendro/protect_test.exs test/rendro/artifact_test.exs</automated>
  </verify>
  <done>Password-safe audit and docs boundaries are proven by tests and remain aligned with the same narrow protection contract the code implements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protected artifact -> application storage/logging | Metadata can cross into app-owned persistence, telemetry, or logs here. |
| Support docs -> downstream teams | Public wording can widen product expectations if it overclaims support here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Information disclosure | `metadata.protection` / audit seams | mitigate | Keep metadata minimal, scrub password keys recursively, and prove the behavior with focused tests. |
| T-51-05 | Repudiation / truth drift | docs + support matrix | mitigate | Lock wording and support claims with docs-contract tests so code and public contract cannot diverge silently. |
| T-51-06 | Scope creep | integrations/docs | mitigate | Preserve explicit artifact-first language and reject global/password-persistence guidance in delivery seams. |
</threat_model>

<verification>
mix test test/rendro/protect_test.exs test/rendro/artifact_test.exs test/rendro/audit_test.exs test/docs_contract/protection_claims_test.exs
</verification>

<success_criteria>
- Protected artifacts expose only minimal safe metadata and truthfully mark non-deterministic output.
- Password material is absent from error details, artifact metadata, and scrubbed audit-facing maps.
- Integration/docs/support claims remain aligned with the narrow Phase 51 boundary and do not overclaim viewer, compliance, or native-encryption support.
</success_criteria>

<output>
After completion, create `.planning/phases/51-protection-api-contract-and-validation/51-02-SUMMARY.md`
</output>
