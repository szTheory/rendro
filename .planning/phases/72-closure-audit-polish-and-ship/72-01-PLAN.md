---
phase: 72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/guardrails/required_status_checks.json
  - test/guardrails/required_checks_contract_test.exs
  - scripts/audit_branch_protection.exs
autonomous: true
requirements:
  - GUARDRAIL-02
must_haves:
  truths:
    - Committed `priv/guardrails/required_status_checks.json` lists exactly four sorted required contexts on `main` with `strict: true` and `policy: additive_only`.
    - Offline contract test proves `ci.yml` job names, behavioral command strings, and eight docs-contract lanes match the baseline — no GitHub API in `mix ci`.
    - `scripts/audit_branch_protection.exs` normalizes live protection and fails when baseline contexts are missing or `strict` is false.
    - Baseline documents `viewer-evidence-live-proof` as advisory only (Phase 68 D-18 / D-32); `supersedes_planning_refs` corrects PITFALLS §7 drift (D-07).
    - Pre-v2.3 and v2.3-close required context **sets are identical**; semantics unchanged for all four required jobs.
  artifacts:
    - path: priv/guardrails/required_status_checks.json
      provides: "GUARDRAIL-02 durable baseline — branch, strict, policy, required_contexts, per-context semantic_class + command mapping, advisory_contexts"
    - path: test/guardrails/required_checks_contract_test.exs
      provides: "Fork-safe offline wiring proof (ci.yml + verify_docs.exs + JSON integrity)"
    - path: scripts/audit_branch_protection.exs
      provides: "Close-ritual live GitHub audit comparing live protection ⊇ baseline"
  key_links:
    - from: priv/guardrails/required_status_checks.json
      to: .github/workflows/ci.yml
      via: "required_checks_contract_test.exs asserts each contexts[].ci_job exists"
    - from: scripts/audit_branch_protection.exs
      to: priv/guardrails/required_status_checks.json
      via: "reads required_contexts at runtime for live diff"
    - from: test/guardrails/required_checks_contract_test.exs
      to: scripts/verify_docs.exs
      via: "asserts exactly eight lanes; lane 8 is viewer_evidence_claims_test.exs"
---

<objective>
Land the GUARDRAIL-02 durable baseline — committed required-check contract, offline CI wiring test, and live branch-protection audit script.

Purpose: Close the v2.2 operational gap (artifact closure ≠ operational closure) per D-01 B-lite pattern. Makes engine-level lane preservation auditable in-repo without GitHub API in default `mix ci` (D-04).
Output: `priv/guardrails/required_status_checks.json`, `test/guardrails/required_checks_contract_test.exs`, `scripts/audit_branch_protection.exs`.

**CONTEXT decisions covered:** D-01, D-02, D-03, D-04, D-05, D-06 (audit script only — VERIFICATION capture in 72-02), D-07, D-31.
**Explicitly deferred:** D-32 promotion of `viewer-evidence-live-proof` to required — documented as advisory in baseline JSON only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md
@.planning/phases/72-closure-audit-polish-and-ship/72-RESEARCH.md
@.planning/phases/72-closure-audit-polish-and-ship/72-PATTERNS.md
@.planning/phases/72-closure-audit-polish-and-ship/72-VALIDATION.md
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-CONTEXT.md
@.planning/milestones/v2.2-MILESTONE-AUDIT.md
@.github/workflows/ci.yml
@scripts/verify_docs.exs
@test/mix/tasks/ci_alias_contract_test.exs
@test/docs_contract/viewer_evidence_claims_test.exs
@mix.exs
</context>

<interfaces>
v2.3 close baseline (D-03) — four required GitHub contexts on `main`:

| Context | Semantic class | CI job | Primary command |
|---------|----------------|--------|-----------------|
| `test` | `deterministic` | `test` | `mix ci` |
| `signing-live-proof` | `behavioral_live_proof` | `signing-live-proof` | `mix test --include live_signing test/rendro/adapters/signing_live_test.exs` |
| `long-lived-live-proof` | `behavioral_live_proof` | `long-lived-live-proof` | `mix test --include live_pdf_tools test/rendro/adapters/signing_live_test.exs` |
| `release-proof` | `release` | `release-proof` | `mix run scripts/release_preflight_proof.exs --current-version-tag --worktree <isolated>` |

Advisory (not required per D-18/D-32): `viewer-evidence-live-proof`.

Folded into `test` / `mix ci` (not separate GitHub contexts): eight docs-contract lanes via `mix test` including lane 8 `viewer_evidence_claims_test.exs`; structural validation; format; hex.build; credo; dialyzer.

Recommended JSON shape (RESEARCH §2.6 — executor may adjust field order but must preserve semantics):

```json
{
  "schema_version": 1,
  "branch": "main",
  "strict": true,
  "policy": "additive_only",
  "since_milestone": "v2.3",
  "required_contexts": [
    "long-lived-live-proof",
    "release-proof",
    "signing-live-proof",
    "test"
  ],
  "contexts": [ "... four required entries with semantic_class, ci_job, command, notes ..." ],
  "advisory_contexts": [ "... viewer-evidence-live-proof ..." ],
  "supersedes_planning_refs": {
    "pitfalls_7_viewer_evidence_schema_required": false,
    "rationale": "Structural viewer-evidence enforcement folds into test job; no fourth required GitHub context."
  }
}
```

Live audit normalization (D-05):

```elixir
live_contexts =
  protection
  |> get_in(["required_status_checks", "contexts"])
  |> Enum.map(& &1["context"])
  |> Enum.sort()

missing = baseline_required -- live_contexts
# System.halt(1) if missing != [] or live_strict != true
```

Repo identity: parse `szTheory/rendro` from `mix.exs` `@source_url` (`https://github.com/szTheory/rendro`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create required-check baseline JSON</name>
  <files>priv/guardrails/required_status_checks.json</files>
  <read_first>
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md (D-01–D-03, D-07, D-31)
    - .planning/phases/72-closure-audit-polish-and-ship/72-RESEARCH.md §2.6
    - .github/workflows/ci.yml (job names and run commands lines 11–178)
    - .planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-CONTEXT.md (D-18 folded into test)
  </read_first>
  <acceptance_criteria>
    - File `priv/guardrails/required_status_checks.json` exists and parses as valid JSON.
    - Top-level fields: `schema_version` (1), `branch` (`"main"`), `strict` (`true`), `policy` (`"additive_only"`), `since_milestone` (`"v2.3"`).
    - `required_contexts` equals exactly `["long-lived-live-proof", "release-proof", "signing-live-proof", "test"]` (sorted alphabetically).
    - `contexts` array has four entries; each has `name`, `semantic_class`, `ci_job`, `command` matching live `ci.yml` step commands.
    - `test` context `notes` states viewer-evidence schema/lint folded into `test` job per Phase 68 D-18.
    - `advisory_contexts` includes `viewer-evidence-live-proof` with note that it is not required on `main` (D-32).
    - `supersedes_planning_refs.pitfalls_7_viewer_evidence_schema_required` is `false`.
    - `signing-live-proof` command contains `live_signing` and `signing_live_test.exs`.
    - `long-lived-live-proof` command contains `live_pdf_tools` and `signing_live_test.exs`.
  </acceptance_criteria>
  <action>
    Create directory `priv/guardrails/` if absent.
    Write `required_status_checks.json` per RESEARCH §2.6 recommended shape, copying exact command strings from `.github/workflows/ci.yml` lines 29, 123, 158, 178, and 81–89 for advisory job.
    Map `test` → `mix ci` with notes listing folded docs-contract lanes (eight total, lane 8 = viewer evidence).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('priv/guardrails/required_status_checks.json')); assert d['strict']==True; assert d['required_contexts']==sorted(['test','signing-live-proof','long-lived-live-proof','release-proof']); assert len(d['contexts'])==4"</automated>
  </verify>
  <done>Committed baseline JSON is the single source for offline test and live audit script.</done>
</task>

<task type="auto">
  <name>Task 2: Add offline required-checks contract test</name>
  <files>test/guardrails/required_checks_contract_test.exs</files>
  <read_first>
    - priv/guardrails/required_status_checks.json (from Task 1)
    - test/mix/tasks/ci_alias_contract_test.exs (alias contract pattern)
    - test/docs_contract/viewer_evidence_claims_test.exs describe "docs-contract lane registration" (lines 243–276)
    - scripts/verify_docs.exs (eight-lane registry lines 7–16)
    - .github/workflows/ci.yml
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-04
  </read_first>
  <acceptance_criteria>
    - File `test/guardrails/required_checks_contract_test.exs` exists.
    - `mix test test/guardrails/required_checks_contract_test.exs` exits 0.
    - Test asserts JSON `required_contexts` is sorted; `strict == true`; `policy == "additive_only"`.
    - Test asserts `.github/workflows/ci.yml` contains job keys: `test`, `signing-live-proof`, `long-lived-live-proof`, `release-proof`, `viewer-evidence-live-proof`.
    - Test asserts each `required_contexts` entry has matching `jobs.<name>:` block in `ci.yml`.
    - Test asserts `signing-live-proof` step contains `live_signing` and `signing_live_test.exs`.
    - Test asserts `long-lived-live-proof` step contains `live_pdf_tools` and `signing_live_test.exs`.
    - Test asserts `scripts/verify_docs.exs` registers exactly eight lanes; lane 8 path is `test/docs_contract/viewer_evidence_claims_test.exs`.
    - Test asserts `mix.exs` `:ci` alias includes structural validation steps (`mix test`, `mix format --check-formatted`, `mix hex.build`, `mix credo`, `mix dialyzer`) — mirror `ci_alias_contract_test.exs` pattern; structural validation is folded into `test` context per D-18, not a separate required GitHub context.
    - Test asserts each `contexts[].ci_job` from JSON exists in `ci.yml`.
    - Test file contains **no** `Req`, `HTTPoison`, `gh api`, or `GITHUB_TOKEN` references (fork-safe offline only).
  </acceptance_criteria>
  <action>
    Create `test/guardrails/required_checks_contract_test.exs` with describe blocks per RESEARCH §2.8:
    (1) baseline JSON integrity, (2) ci.yml job names, (3) behavioral command wiring, (4) eight docs-contract lanes, (5) JSON ci_job ↔ ci.yml alignment.
    Read JSON and workflow files as strings; use `assert script =~` pattern from existing contract tests.
  </action>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs</automated>
  </verify>
  <done>GUARDRAIL-02 offline wiring proof runs in default `mix test` / `mix ci` without network.</done>
</task>

<task type="auto">
  <name>Task 3: Implement live branch-protection audit script</name>
  <files>scripts/audit_branch_protection.exs</files>
  <read_first>
    - priv/guardrails/required_status_checks.json
    - scripts/release_preflight_proof.exs (script exit semantics, System.halt pattern)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-05, D-06
    - .planning/phases/72-closure-audit-polish-and-ship/72-RESEARCH.md §2.7
    - mix.exs (@source_url for repo slug)
  </read_first>
  <acceptance_criteria>
    - File `scripts/audit_branch_protection.exs` exists and is executable via `mix run scripts/audit_branch_protection.exs`.
    - Script reads `priv/guardrails/required_status_checks.json` for `branch`, `required_contexts`, `strict`.
    - Script fetches `GET /repos/{owner}/{repo}/branches/{branch}/protection` using `:req` (dev/test dep) with `Authorization: Bearer #{token}` from `GITHUB_TOKEN` env.
    - On success: prints normalized JSON to stdout: `{"strict": true, "contexts": ["long-lived-live-proof", "release-proof", "signing-live-proof", "test"]}` (sorted).
    - On gap: prints missing contexts to stderr and exits 1 (`System.halt(1)`).
    - Fails with exit 1 when `strict` is false on live protection.
    - Fails with clear error when `GITHUB_TOKEN` is unset (exit 1, message to stderr).
    - Script is **not** referenced from `mix.exs` `:ci` alias or `mix ci` steps.
    - Moduledoc documents close-ritual usage and token scope (`repo` admin read).
  </acceptance_criteria>
  <action>
    Implement `scripts/audit_branch_protection.exs` as a standalone Elixir script module.
    Parse owner/repo from `Mix.Project.config()[:source_url]` or `mix.exs` `@source_url`.
    Normalize GitHub API response per RESEARCH §2.7.
    Do not log token value; redact Authorization header from any debug output.
  </action>
  <verify>
    <automated>mix compile && mix run -e 'Code.ensure_loaded!(Rendro.ReleasePreflightProof); true' 2>/dev/null; elixir -e 'content=File.read!("scripts/audit_branch_protection.exs"); refute content =~ "mix ci"; assert content =~ "required_status_checks.json"'</automated>
  </verify>
  <done>Live audit script ready for close ritual; VERIFICATION capture deferred to 72-02 Task 3.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Branch Protection Contract

| Boundary | Description |
|----------|-------------|
| Repo → GitHub API | Close-ritual only; token never in committed files or default CI. |
| Baseline JSON → CI | Offline test is authoritative for wiring; live audit confirms operational state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01-01 | Information Disclosure | `GITHUB_TOKEN` in audit script logs | High | mitigate | Never print token; redact Authorization; document env-only usage; script not in `mix ci`. |
| T-72-01-02 | Tampering | Stale or wrong baseline JSON | High | mitigate | Contract test asserts exact four contexts + behavioral command substrings from live `ci.yml`. |
| T-72-01-03 | Spoofing | Audit script against wrong repo | Medium | mitigate | Parse repo slug from `mix.exs` `@source_url`; fail on API 404. |
| T-72-01-04 | Tampering | Required-check list shrinks or behavioral lanes diluted | High | mitigate | `policy: additive_only` in JSON; contract test locks `live_signing` / `live_pdf_tools` commands; live audit verifies baseline ⊂ live. |
| T-72-01-05 | Denial of Service | GitHub API in fork PR CI | Medium | accept | Script excluded from `mix ci` per D-04; close ritual only. |
| T-72-01-06 | Elevation of Privilege | Token write scope requested | High | mitigate | Document read-only admin scope; script uses GET protection endpoint only. |

**Block gate:** T-72-01-01, T-72-01-02, T-72-01-04, T-72-01-06 are HIGH — must not merge without mitigations above.
</threat_model>

<verification_criteria>
- `mix test test/guardrails/required_checks_contract_test.exs` — exit 0
- `priv/guardrails/required_status_checks.json` parses; four required contexts; advisory `viewer-evidence-live-proof` documented
- `scripts/audit_branch_protection.exs` compiles; references baseline JSON; absent from `mix ci` alias
- No edits to `priv/support_matrix.json`, `.github/workflows/ci.yml` required job semantics, or branch protection UI
</verification_criteria>

<success_criteria>
- GUARDRAIL-02 durable baseline committed with offline contract test green.
- Live audit script implemented and documented for close ritual (execution with token in 72-02).
- PITFALLS §7 drift corrected via `supersedes_planning_refs` (D-07).
</success_criteria>

<output>
After completion, create `.planning/phases/72-closure-audit-polish-and-ship/72-01-SUMMARY.md` via orchestration.
</output>
