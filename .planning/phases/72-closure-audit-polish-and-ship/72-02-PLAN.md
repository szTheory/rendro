---
phase: 72
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - lib/mix/tasks/rendro/viewer_evidence.ex
  - test/mix/tasks/viewer_evidence_task_test.exs
  - test/rendro/viewer_evidence/validator_test.exs
  - .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md
  - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VERIFICATION.md
  - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VERIFICATION.md
autonomous: true
requirements:
  - GUARDRAIL-02
must_haves:
  truths:
    - `mix rendro.viewer_evidence validate --strict` exits 1 when any `supported` row has `recorded_at` older than 180 days; exits 0 on production matrix at ship time.
    - Default `mix rendro.viewer_evidence validate` keeps staleness advisory (Phase 68 D-17); `--strict` is **not** wired into `mix ci`, docs-contract lane 8, or branch protection (D-09).
    - `72-VERIFICATION.md` draft captures machine `list --json` ledger, both validate outputs, guardrails test result, and live audit snapshot (or `gaps_found` with explicit gap per D-06).
    - Matrix terminal state unchanged: `supported=17`, `explicit_deferral=9`, `unverified=0`; `missing` exit 0.
    - Trust-sensitive spot-check table (~8–12 rows) present per D-19; no hand-maintained 26-row duplicate (D-17).
  artifacts:
    - path: lib/mix/tasks/rendro/viewer_evidence.ex
      provides: "validate --strict staleness gate (operator/release only)"
    - path: .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md
      provides: "B+C hybrid closure ledger — must-haves, JSON export, spot-check, GUARDRAIL-02 audit table"
    - path: .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VERIFICATION.md
      provides: "Lightweight PASS backfill for milestone audit (D-21)"
    - path: .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VERIFICATION.md
      provides: "Lightweight PASS backfill for milestone audit (D-21)"
  key_links:
    - from: lib/mix/tasks/rendro/viewer_evidence.ex
      to: lib/rendro/viewer_evidence/validator.ex
      via: "staleness_warnings/1 reclassified fatal when --strict"
    - from: 72-VERIFICATION.md
      to: priv/guardrails/required_status_checks.json
      via: "GUARDRAIL-02 mapping table test → mix ci + 8 docs-contract lanes"
    - from: 72-VERIFICATION.md
      to: scripts/audit_branch_protection.exs
      via: "live audit normalized JSON snapshot (D-06)"
---

<objective>
Implement `validate --strict` staleness gate and draft the Phase 72 verification ledger with CLI outputs and GUARDRAIL-02 audit evidence.

Purpose: Deliver D-08–D-11 operator/release staleness discipline and D-17–D-21 verification packet without new matrix recording. Backfill missing 69/71 VERIFICATION stubs so `/gsd-audit-milestone v2.3` is unblocked (D-21).
Output: `--strict` on Mix task + tests; `72-VERIFICATION.md` draft; `69-VERIFICATION.md` and `71-VERIFICATION.md` lightweight PASS artifacts.

**CONTEXT decisions covered:** D-08, D-09, D-10 (Appendix D deferred to 72-03 guide edit), D-11, D-17, D-18, D-19, D-20, D-21 (partial — milestone audit regeneration still post-Phase 72 per D-15).
**Explicitly deferred:** D-15 `/gsd-complete-milestone` and `/gsd-audit-milestone` execution — post Phase 72 execute per ship ordering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md
@.planning/phases/72-closure-audit-polish-and-ship/72-RESEARCH.md
@.planning/phases/72-closure-audit-polish-and-ship/72-PATTERNS.md
@.planning/phases/72-closure-audit-polish-and-ship/72-VALIDATION.md
@.planning/phases/70-consolidate-already-validated-surfaces/70-VERIFICATION.md
@lib/mix/tasks/rendro/viewer_evidence.ex
@lib/rendro/viewer_evidence/validator.ex
@test/mix/tasks/viewer_evidence_task_test.exs
@priv/guardrails/required_status_checks.json
@scripts/audit_branch_protection.exs
@priv/support_matrix.json
</context>

<interfaces>
`--strict` implementation (minimal diff per RESEARCH §2.5):

1. Add `pop_strict_flag/1` analogous to `pop_json_flag/1`.
2. Accept `mix rendro.viewer_evidence validate --strict` (flag after `validate`).
3. When `strict?`, reclassify warnings containing `"is older than"` from advisory to fatal in `partition_warnings/2`.
4. Update moduledoc subcommands and exit-code table for `validate --strict`.
5. **Do not** add `rendro.viewer_evidence` to `mix ci` alias.

Staleness source (`validator.ex`):

```elixir
@staleness_days 180
# Warning format: "#{path}: recorded_at #{date} is older than #{@staleness_days} days"
```

Production matrix at ship (2026-05-29): all `supported` rows `recorded_at` 2026-05-28 or 2026-05-29 — both `validate` and `validate --strict` exit 0.

`72-VERIFICATION.md` structure (follow `70-VERIFICATION.md` + D-17 extensions):

```markdown
---
status: passed | gaps_found
phase: 72-closure-audit-polish-and-ship
verified: <ISO timestamp>
requirements: [GUARDRAIL-02]
score: N/N
---
## Must-Haves Verified
## Matrix Ledger (machine export)
## Trust-Sensitive Spot-Check
## GUARDRAIL-02 Required-Check Audit
## Automated Checks Run
## Gaps
```

Trust-sensitive spot-check rows (D-19 — minimum 8):

| surface | viewer | status | spot-check note |
|---------|--------|--------|-----------------|
| signature_widget | pdfjs | explicit_deferral | mozilla/pdf.js#4202 in matrix + api_stability |
| signed_artifact | apple_preview | explicit_deferral | /Sig validation gap |
| signing_preparation | apple_preview | supported | inherits signature_widget pointer |
| signing_preparation | chrome_pdfium | supported | inherits signature_widget pointer |
| long_lived_signed_artifact | apple_preview | explicit_deferral | LTV indicators |
| long_lived_signed_artifact | chrome_pdfium | explicit_deferral | LTV indicators |
| forms | adobe_acrobat_reader | supported | evidence path + viewer_kind |
| signature_widget | chrome_pdfium | supported | viewer_kind pdfium-cli honesty |

Ledger command:

```bash
mix rendro.viewer_evidence list --json
# Expected summary: {"total":26,"supported":17,"unverified":0,"explicit_deferral":9}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement validate --strict on Mix task</name>
  <files>lib/mix/tasks/rendro/viewer_evidence.ex</files>
  <read_first>
    - lib/mix/tasks/rendro/viewer_evidence.ex (parse_args!, run_validate, partition_warnings, moduledoc lines 54–66)
    - lib/rendro/viewer_evidence/validator.ex (staleness_warnings/1, @staleness_days 180)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-08, D-09, D-11
    - .planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-CONTEXT.md D-17
    - mix.exs (:ci alias — confirm no rendro.viewer_evidence)
  </read_first>
  <acceptance_criteria>
    - `mix rendro.viewer_evidence validate --strict` is accepted by `parse_args!` (exit 0 on production matrix).
    - Moduledoc lists `validate [--strict]` subcommand and exit code: **1** on staleness when `--strict`; **0** on staleness without flag.
    - When `--strict` and staleness warnings exist, task exits `{:shutdown, 1}` with message "Viewer evidence validation failed."
    - When `--strict` and no staleness warnings, stdout contains "Viewer evidence validation passed."
    - Default `mix rendro.viewer_evidence validate` (no flag) still exits 0 with only staleness warnings (advisory on stderr).
    - `mix.exs` `:ci` alias does **not** include `rendro.viewer_evidence` (grep confirms).
    - Scope limited to staleness (`"is older than"`); legacy `"missing promotion-complete"` warnings remain zero on production matrix.
  </acceptance_criteria>
  <action>
    Add `pop_strict_flag/1`; thread `strict?` through `run_validate/1`.
    Extend `partition_warnings/2` to accept `strict?: true` and treat staleness warnings as fatal.
    Update moduledoc subcommands section (line ~25) and exit codes section.
    Do not modify `Validator.run_full/3` unless task-level reclassification is insufficient.
  </action>
  <verify>
    <automated>mix rendro.viewer_evidence validate --strict 2>&1 | grep -q 'Viewer evidence validation passed' && mix rendro.viewer_evidence validate 2>&1 | grep -q 'Viewer evidence validation passed'</automated>
  </verify>
  <done>Staleness strict gate implemented; default validate behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add --strict exit-code tests</name>
  <files>test/mix/tasks/viewer_evidence_task_test.exs, test/rendro/viewer_evidence/validator_test.exs</files>
  <read_first>
    - test/mix/tasks/viewer_evidence_task_test.exs (capture_shell_messages, describe "validate/1")
    - test/rendro/viewer_evidence/validator_test.exs (existing staleness tests if any)
    - lib/rendro/viewer_evidence/validator.ex (staleness_warnings/1)
    - .planning/phases/72-closure-audit-polish-and-ship/72-RESEARCH.md §2.5 test strategy
  </read_first>
  <acceptance_criteria>
    - `mix test test/mix/tasks/viewer_evidence_task_test.exs` exits 0.
    - Test: `validate --strict` on production matrix returns `:ok` (dates inside 180-day window).
    - Test: `validate --strict` with synthetic backdated `recorded_at` (fixture matrix in test/support or inline temp file) exits `{:shutdown, 1}`.
    - Test: default `validate` with same backdated fixture exits `:ok` (staleness advisory only).
    - Unit test in `validator_test.exs`: `staleness_warnings/1` returns warning when `recorded_at` > 180 days ago on `supported` row.
    - Test confirms `mix ci` alias still excludes `rendro.viewer_evidence`.
  </acceptance_criteria>
  <action>
    Add describe `"validate --strict"` to viewer_evidence_task_test.exs.
    Create minimal matrix fixture with `recorded_at` set to `Date.add(Date.utc_today(), -181) |> Date.to_iso8601()` for strict-failure test; use `Matrix` / `Validator` directly or temp file path.
    Add staleness unit test to validator_test.exs if not present.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/viewer_evidence_task_test.exs test/rendro/viewer_evidence/validator_test.exs --only staleness 2>/dev/null || mix test test/mix/tasks/viewer_evidence_task_test.exs test/rendro/viewer_evidence/validator_test.exs</automated>
  </verify>
  <done>Strict staleness behavior covered by automated tests; production matrix green on both validate modes.</done>
</task>

<task type="auto">
  <name>Task 3: Draft 72-VERIFICATION.md with CLI and audit outputs</name>
  <files>.planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/70-consolidate-already-validated-surfaces/70-VERIFICATION.md (format precedent)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-06, D-17–D-20, D-23
    - priv/guardrails/required_status_checks.json (from 72-01)
    - scripts/audit_branch_protection.exs (from 72-01)
    - guides/api_stability.md (spot-check cross-ref for deferral rows)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md` exists with YAML frontmatter: `status`, `phase`, `verified` (ISO date), `requirements: [GUARDRAIL-02]`, `score`.
    - Section "Matrix Ledger" contains fenced output of `mix rendro.viewer_evidence list --json` with summary `supported=17`, `unverified=0`, `explicit_deferral=9`.
    - Section "Trust-Sensitive Spot-Check" has ≥8 rows from D-19 list including `signature_widget × pdfjs` (#4202) and `signed_artifact × apple_preview`.
    - Section "GUARDRAIL-02 Required-Check Audit" table has columns: context, pre-v2.3, v2.3 close, semantics changed (all four required = **no**).
    - Mapping table documents `test` → `mix ci` + eight docs-contract lanes (folded per D-18).
    - Section "Automated Checks Run" lists commands with PASS/FAIL annotations for: `missing`, `validate`, `validate --strict`, `mix test test/guardrails/required_checks_contract_test.exs`.
    - Section "Live audit snapshot" (or "GUARDRAIL-02 Required-Check Audit" subsection) records: command run (`mix run scripts/audit_branch_protection.exs`), ISO-8601 timestamp, and fenced normalized JSON output from the audit script when `GITHUB_TOKEN` is set and audit passes.
    - When live audit fails or token is unavailable: frontmatter `status: gaps_found` and "Gaps" section lists explicit gap (e.g. missing context name, `strict: false`, or `GITHUB_TOKEN unset — live audit pending before tag`); never silent pass when audit was attempted and failed (D-06).
  </acceptance_criteria>
  <action>
    Run closure commands and capture outputs:
    ```bash
    mix rendro.viewer_evidence missing
    mix rendro.viewer_evidence list --json
    mix rendro.viewer_evidence validate
    mix rendro.viewer_evidence validate --strict
    mix test test/guardrails/required_checks_contract_test.exs
    ```
    If `GITHUB_TOKEN` is available, run `mix run scripts/audit_branch_protection.exs` and paste normalized JSON under "Live audit snapshot"; set `status: gaps_found` with explicit gap list if audit fails (D-06). If token unavailable, note `status: passed` with automated checks green and live audit pending operator run before tag.
    Do **not** hand-maintain full 26-row cell table.
  </action>
  <verify>
    <automated>test -f .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md && grep -q 'GUARDRAIL-02' .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md && grep -q 'supported=17' .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md</automated>
  </verify>
  <done>Verification ledger draft committed; live audit snapshot captured or gap explicitly documented.</done>
</task>

<task type="auto">
  <name>Task 4: Backfill 69 and 71 VERIFICATION stubs</name>
  <files>.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VERIFICATION.md, .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/70-consolidate-already-validated-surfaces/70-VERIFICATION.md (stub format)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-21
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/ (SUMMARY files if present)
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/ (SUMMARY files if present)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VERIFICATION.md` exists with `status: passed`, `requirements: [RECIPE-01, RECIPE-03, RECIPE-05]`, and cites `guides/viewer_evidence.md` + forms × Apple Preview canonical cell.
    - File `.planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VERIFICATION.md` exists with `status: passed`, `requirements: [VIEWER-02, VIEWER-03, VIEWER-04, VIEWER-05, VIEWER-06, VIEWER-07]`, and cites terminal matrix counts `supported=17`, `explicit_deferral=9`, `unverified=0`.
    - Both stubs reference existing plan SUMMARY metrics; no Nyquist replay required (lightweight PASS per D-21).
    - Section "Gaps" in each file is `None.` or documents only known deferrals to `/gsd-audit-milestone`.
  </acceptance_criteria>
  <action>
    Create lightweight PASS verification artifacts modeled on `70-VERIFICATION.md` frontmatter + must-haves table + automated checks citing `mix rendro.viewer_evidence list` / `mix docs.contract` results from Phase 71 close state.
    Do not regenerate full evidence files or re-run manual GUI recording.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VERIFICATION.md && test -f .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VERIFICATION.md</automated>
  </verify>
  <done>Milestone audit blocker for missing 69/71 VERIFICATION cleared.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Staleness Gate & Verification Ledger

| Boundary | Description |
|----------|-------------|
| Operator CLI → CI | `--strict` must not leak into merge-blocking paths (calendar bomb). |
| VERIFICATION → Matrix | Machine export is canonical; spot-check is sampling only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-02-01 | Denial of Service | `--strict` wired into `mix ci` | High | mitigate | Acceptance criteria grep `mix.exs` `:ci`; D-09 explicit; task test asserts alias unchanged. |
| T-72-02-02 | Repudiation | Silent pass when live audit fails | High | mitigate | D-06: VERIFICATION `gaps_found` + explicit gap list; never omit failed audit. |
| T-72-02-03 | Tampering | Hand-maintained 26-row ledger drifts from matrix | Medium | mitigate | D-17: `list --json` only; spot-check ≤12 rows. |
| T-72-02-04 | Information Disclosure | Full JSON ledger exposes operator handles | Low | accept | Evidence frontmatter optional handle; same data already in repo matrix. |
| T-72-02-05 | Tampering | `--strict` scope creep to legacy warnings | Medium | mitigate | Strict reclassifies only `"is older than"` warnings per D-08. |

**Block gate:** T-72-02-01 and T-72-02-02 are HIGH — must not merge if `--strict` appears in `mix ci` or live audit failure is unrecorded.
</threat_model>

<verification_criteria>
- `mix rendro.viewer_evidence validate` — exit 0
- `mix rendro.viewer_evidence validate --strict` — exit 0 on production matrix
- `mix rendro.viewer_evidence missing` — exit 0
- `mix test test/mix/tasks/viewer_evidence_task_test.exs test/rendro/viewer_evidence/validator_test.exs` — exit 0
- `72-VERIFICATION.md` exists with JSON ledger and GUARDRAIL-02 audit section
- `69-VERIFICATION.md` and `71-VERIFICATION.md` exist
</verification_criteria>

<success_criteria>
- `--strict` staleness gate implemented and tested; default validate unchanged.
- Phase 72 verification ledger drafted with machine export and trust-sensitive spot-check.
- Milestone audit backfill stubs for phases 69 and 71 committed.
</success_criteria>

<output>
After completion, create `.planning/phases/72-closure-audit-polish-and-ship/72-02-SUMMARY.md` via orchestration.
</output>
