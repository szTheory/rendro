---
phase: 72
plan: 03
type: execute
wave: 3
depends_on:
  - 72-02
files_modified:
  - guides/viewer_evidence.md
  - guides/api_stability.md
  - test/docs_contract/viewer_evidence_claims_test.exs
  - test/docs_contract/branding_claims_test.exs
  - CHANGELOG.md
  - mix.exs
  - .github/workflows/release.yml
  - .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md
autonomous: true
requirements:
  - GUARDRAIL-02
must_haves:
  truths:
    - `guides/viewer_evidence.md` documents Phase 71 automated path, fixes stale manual steps (missing exit 0), and adds Appendix D `--strict` row (D-24, D-10).
    - `test/docs_contract/viewer_evidence_claims_test.exs` asserts `forms/chrome_pdfium.md` and `signature_widget/chrome_pdfium.md` in api_stability mirror; optional deferral substring test passes (D-26).
    - `CHANGELOG.md` splits `0.3.0` (historical pre–v2.3 viewer) from `0.3.1` (v2.3 Viewer Evidence bullets); `mix.exs` `@version` is `"0.3.1"` (D-12, D-13).
    - `mix release.preflight` green at `0.3.1`; `release.yml` runs `mix release.preflight` before `hex.publish` (D-14).
    - Negative `hex.build` test refutes `priv/viewer_evidence/` and `priv/support_matrix.json` in tarball (D-30).
    - `mix docs.contract` 8/8 lanes green; `mix ci` passes; no matrix cell status edits.
    - `72-VERIFICATION.md` finalized with ship ritual outputs and `status: passed` (or `gaps_found` with explicit gaps).
  artifacts:
    - path: CHANGELOG.md
      provides: "0.3.0 freeze + 0.3.1 v2.3 viewer-evidence public contract"
    - path: mix.exs
      provides: "@version 0.3.1; files: whitelist unchanged (operator priv repo-only per D-29)"
    - path: guides/viewer_evidence.md
      provides: "Appendix D --strict; Phase 71 trust_sensitive live-test path"
    - path: test/docs_contract/branding_claims_test.exs
      provides: "Negative hex tarball operator-priv exclusion"
  key_links:
    - from: CHANGELOG.md
      to: guides/api_stability.md
      provides: "Adopter-visible viewer promotion/deferral discipline per RECIPE-05"
    - from: test/docs_contract/viewer_evidence_claims_test.exs
      to: guides/api_stability.md
      via: "supported path + deferral substring mirror asserts"
    - from: .github/workflows/release.yml
      to: lib/mix/tasks/release/preflight.ex
      via: "mix release.preflight before hex.publish"
---

<objective>
Polish guides and docs-contract, reconcile CHANGELOG/`mix.exs` for `v0.3.1`, harden release workflow, and finalize Phase 72 verification for Hex ship.

Purpose: Close adopter contract gap (`0.3.0` on Hex predates v2.3 viewer work) and satisfy D-22–D-27 surgical guide polish without matrix recording or CI topology changes.
Output: Guide fixes, docs-contract hardening, `0.3.1` release artifacts, optional negative hex test, finalized `72-VERIFICATION.md`.

**CONTEXT decisions covered:** D-10 (Appendix D in guide), D-12, D-13, D-14, D-16 (no release-please), D-22, D-23, D-24, D-25, D-26, D-27, D-30.
**Explicitly deferred (with rationale):**
- D-15 `/gsd-audit-milestone` + `/gsd-complete-milestone` — execute **after** Hex `v0.3.1` publish per ship ordering.
- D-28 `mix rendro.viewer_evidence init` — v2.4 operator DX backlog.
- D-29 Hex `files:` expansion — violates documentation honesty; negative test documents omission instead.
- D-32 promote `viewer-evidence-live-proof` — separate future policy; documented in 72-01 baseline only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md
@.planning/phases/72-closure-audit-polish-and-ship/72-RESEARCH.md
@.planning/phases/72-closure-audit-polish-and-ship/72-PATTERNS.md
@.planning/phases/72-closure-audit-polish-and-ship/72-VALIDATION.md
@.planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md
@guides/viewer_evidence.md
@guides/api_stability.md
@test/docs_contract/viewer_evidence_claims_test.exs
@test/docs_contract/branding_claims_test.exs
@CHANGELOG.md
@mix.exs
@.github/workflows/release.yml
@lib/mix/tasks/release/preflight.ex
@scripts/release_preflight_proof.exs
</context>

<interfaces>
CHANGELOG split procedure (D-13):

```bash
git show v0.3.0:CHANGELOG.md   # historical 0.3.0 content at tag ba023c9
```

Target structure:

```markdown
## [0.3.1] - <ship-date>
#### Viewer Evidence (v2.3)
... all v2.3 bullets moved from 0.3.0 draft ...

## [0.3.0] - 2026-05-08
... content actually published at v0.3.0 tag only ...
```

`mix.exs` version bump only — `files:` whitelist unchanged:

```elixir
@version "0.3.1"
files: ~w(lib priv/branded guides .formatter.exs mix.exs README.md LICENSE NOTICE CHANGELOG.md)
```

`release.yml` hardening (D-14):

```yaml
- name: Run release preflight
  run: mix release.preflight

- name: Publish to Hex
  run: mix hex.publish --yes
```

Guide fixes (`viewer_evidence.md`):

| Location | Current (stale) | Target |
|----------|-----------------|--------|
| Manual step 1 | "Exit code **1** when unverified cells exist (expected today)" | Exit **0** at v2.3 close |
| Manual step 6 | Legacy promotion warnings note | Remove — tier-B complete |
| Automated path | Phase 70 rows only | Add Phase 71 `record` commands + link `trust_sensitive_viewer_evidence_live_test.exs` |
| Appendix D | No `--strict` row | Add `validate --strict` row per D-10 |

Docs-contract path asserts to add (D-26):

```elixir
"priv/viewer_evidence/forms/chrome_pdfium.md",
"priv/viewer_evidence/signature_widget/chrome_pdfium.md",
```

Deferral mirror test (optional but recommended):

```elixir
# For each explicit_deferral row: assert guide =~ String.slice(reason, 0, 40)
```

Negative hex.build test (D-30):

```elixir
refute contents =~ "priv/viewer_evidence/"
refute contents =~ "priv/support_matrix.json"
```

Pre-tag gate:

```bash
mix run scripts/release_preflight_proof.exs --current-version-tag --worktree /tmp/rendro-preflight-72
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Surgical guide polish (viewer_evidence + api_stability drift-fix)</name>
  <files>guides/viewer_evidence.md, guides/api_stability.md</files>
  <read_first>
    - guides/viewer_evidence.md (lines 29–67 automated path, 77–129 manual steps, Appendix D)
    - guides/api_stability.md (supported STACK mirrors, deferral prose)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-22–D-27
    - test/rendro/adapters/trust_sensitive_viewer_evidence_live_test.exs
    - .github/workflows/ci.yml viewer-evidence-live-proof file list (lines 82–89)
  </read_first>
  <acceptance_criteria>
    - Manual step 1 (`missing`): documents exit **0** when no unverified cells (not "expected today" exit 1).
    - Manual step 6 (`validate`): removes legacy promotion-warning note; states tier-B promotion-complete.
    - Automated path section lists Phase 71 `record` commands for signature_widget, signed_artifact, signing_preparation, long_lived_signed_artifact surfaces.
    - Automated path links `test/rendro/adapters/trust_sensitive_viewer_evidence_live_test.exs` and seven CI live-test files.
    - Appendix D table includes row for `validate --strict` with exit 1 on staleness; notes **not** merge-blocking CI (D-09/D-10).
    - Prerequisites section unchanged: Hex omits `priv/viewer_evidence/` and `priv/support_matrix.json` (D-29 honesty).
    - `guides/api_stability.md`: drift-fix only — no SemVer section rewrites; every `explicit_deferral` reason from matrix still appears in prose (grep spot-check).
    - **No** re-copy of evidence frontmatter into guides (D-27).
  </acceptance_criteria>
  <action>
    Edit `guides/viewer_evidence.md` per RESEARCH §2.10: fix stale manual steps, extend automated path with Phase 71 trust-sensitive `record` examples mirroring moduledoc lines 38–44 of `viewer_evidence.ex`, add Appendix D `--strict` row.
    Edit `guides/api_stability.md` only where deferral substring or supported path drift is found — prefer docs-contract test to drive minimal fixes in Task 2.
  </action>
  <verify>
    <automated>grep -q 'validate --strict' guides/viewer_evidence.md && grep -q 'trust_sensitive_viewer_evidence_live_test' guides/viewer_evidence.md && ! grep -q 'expected today' guides/viewer_evidence.md</automated>
  </verify>
  <done>Operator recipe current for v2.3 close; matrix/observation split preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Harden viewer_evidence docs-contract lane</name>
  <files>test/docs_contract/viewer_evidence_claims_test.exs, guides/api_stability.md</files>
  <read_first>
    - test/docs_contract/viewer_evidence_claims_test.exs (describe "api stability guide mirrors...", lines 32–51)
    - priv/support_matrix.json (explicit_deferral evidence_deferred strings)
    - lib/rendro/viewer_evidence/matrix.ex (enumerate_viewer_cells if needed)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-26
  </read_first>
  <acceptance_criteria>
    - Path assert loop includes `priv/viewer_evidence/forms/chrome_pdfium.md` and `priv/viewer_evidence/signature_widget/chrome_pdfium.md`.
    - New test (or extended test): for each `explicit_deferral` matrix row, `guides/api_stability.md` contains `String.slice(evidence_deferred, 0, 40)`.
    - All nine deferral rows pass substring mirror (forms×pdfjs, signature_widget×pdfjs, signing_preparation×pdfjs, signed_artifact×apple_preview, signed_artifact×pdfjs, long_lived×apple_preview, long_lived×chrome_pdfium, long_lived×pdfjs, embedded_files×apple_preview).
    - `mix test test/docs_contract/viewer_evidence_claims_test.exs` exits 0.
    - `mix docs.contract` reports 8/8 lanes PASS.
    - Lane registration tests (describe "docs-contract lane registration") still pass unchanged.
  </acceptance_criteria>
  <action>
    Add two chrome_pdfium paths to the existing for-loop in `"api stability guide mirrors all consolidated viewer evidence paths"`.
    Add test `"api stability guide contains deferral reason substrings from matrix"` using `Matrix.load!()` + `enumerate_viewer_cells/1` filtered to `explicit_deferral`.
    Fix `guides/api_stability.md` only if new asserts fail — minimal substring additions, no full prose rewrite.
  </action>
  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs && mix docs.contract</automated>
  </verify>
  <done>Docs-contract lane 8 enforces full supported-path mirror and deferral honesty.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile CHANGELOG and bump mix.exs to 0.3.1</name>
  <files>CHANGELOG.md, mix.exs</files>
  <read_first>
    - CHANGELOG.md (current Unreleased / 0.3.0 draft with Viewer Evidence section)
    - mix.exs (@version, package files: whitelist)
    - git show v0.3.0:CHANGELOG.md (historical published content)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-12, D-13, D-16
    - lib/mix/tasks/release/preflight.ex (check_changelog_release_tail/1 expects `## [version] - Unreleased` at tag time)
  </read_first>
  <acceptance_criteria>
    - `mix.exs` `@version` is `"0.3.1"`.
    - `mix.exs` `package/0` `files:` list is **unchanged** — no `priv/viewer_evidence/` or `priv/support_matrix.json` (D-29).
    - `CHANGELOG.md` contains `## [0.3.0] - 2026-05-08` with content matching what `v0.3.0` tag actually shipped (pre–v2.3 viewer scope).
    - `CHANGELOG.md` contains `## [0.3.1] - Unreleased` (or dated at tag) with entire `#### Viewer Evidence (v2.3)` section moved from 0.3.0 draft.
    - No attempt to re-tag or re-publish `v0.3.0`.
    - `grep '@version' mix.exs` shows `0.3.1`.
  </acceptance_criteria>
  <action>
    Run `git show v0.3.0:CHANGELOG.md` to capture historical 0.3.0 bullets.
    Freeze `## [0.3.0] - 2026-05-08` section with tag-accurate content.
    Create `## [0.3.1] - Unreleased` and move all v2.3 Viewer Evidence bullets from the conflated 0.3.0 draft.
    Bump `@version` to `"0.3.1"` in `mix.exs`.
    Keep `## [Unreleased]` or top-level future section per project convention if present.
  </action>
  <verify>
    <automated>grep -q '0.3.1' mix.exs && grep -q '## \[0.3.1\]' CHANGELOG.md && grep -q '## \[0.3.0\] - 2026-05-08' CHANGELOG.md</automated>
  </verify>
  <done>Semver patch reconciled; adopter contract gap identified in D-12 addressed in changelog.</done>
</task>

<task type="auto">
  <name>Task 4: Negative hex.build test and release.yml preflight hardening</name>
  <files>test/docs_contract/branding_claims_test.exs, .github/workflows/release.yml</files>
  <read_first>
    - test/docs_contract/branding_claims_test.exs describe "hex tarball contents" (lines 41–56)
    - mix.exs package files: whitelist
    - .github/workflows/release.yml
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-14, D-30
    - lib/mix/tasks/release/preflight.ex
  </read_first>
  <acceptance_criteria>
    - New test `"built tarball excludes operator-only priv paths"` in `branding_claims_test.exs` (or sibling describe).
    - Test runs `mix hex.build`, lists tarball contents, `refute contents =~ "priv/viewer_evidence/"` and `refute contents =~ "priv/support_matrix.json"`.
    - `mix test test/docs_contract/branding_claims_test.exs` exits 0.
    - `.github/workflows/release.yml` includes step `mix release.preflight` after `mix deps.get` and before `mix hex.publish`.
    - Existing `mix ci` step retained in release workflow (preflight is additive per D-14).
  </acceptance_criteria>
  <action>
    Add negative hex.build test mirroring positive tarball test pattern in `branding_claims_test.exs`.
    Insert `- run: mix release.preflight` in `release.yml` between dependency install and publish step; rename prior step to clarify CI vs preflight if needed.
  </action>
  <verify>
    <automated>mix test test/docs_contract/branding_claims_test.exs && grep -q 'mix release.preflight' .github/workflows/release.yml</automated>
  </verify>
  <done>Hex packaging honesty locked by test; tag publish path runs full release preflight.</done>
</task>

<task type="auto">
  <name>Task 5: Finalize verification and run ship gate</name>
  <files>.planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/72-closure-audit-polish-and-ship/72-VERIFICATION.md (draft from 72-02)
    - .planning/phases/72-closure-audit-polish-and-ship/72-CONTEXT.md D-23, D-15
    - scripts/release_preflight_proof.exs
    - scripts/audit_branch_protection.exs
  </read_first>
  <acceptance_criteria>
    - `mix ci` exits 0 at `0.3.1` version.
    - `mix docs.contract` exits 0 (8/8 lanes).
    - `mix rendro.viewer_evidence missing` exits 0; `validate` and `validate --strict` exit 0.
    - `mix run scripts/release_preflight_proof.exs --current-version-tag --worktree /tmp/rendro-preflight-72` exits 0 (or documents synthetic-tag workflow if HEAD not yet tagged).
    - `mix release.preflight` exits 0 at `0.3.1` (CHANGELOG tail and version checks pass).
    - `72-VERIFICATION.md` updated with final automated checks block including `mix ci`, `mix docs.contract`, `mix release.preflight` result.
    - If `GITHUB_TOKEN` set: live audit pasted and `status: passed`; else document operator action before `v0.3.1` tag push.
    - Score reflects all D-23 gates; "Gaps" section is `None.` or lists only unresolved live-audit/token gaps.
    - **No** `v0.3.1` git tag created in this task unless operator explicitly requests — document tag command in VERIFICATION.
  </acceptance_criteria>
  <action>
    Run full closure gate:
    ```bash
    mix ci
    mix docs.contract
    mix rendro.viewer_evidence missing
    mix rendro.viewer_evidence validate
    mix rendro.viewer_evidence validate --strict
    mix test test/guardrails/
    mix test test/docs_contract/
    mix run scripts/release_preflight_proof.exs --current-version-tag --worktree /tmp/rendro-preflight-72
    mix release.preflight
    ```
    Update `72-VERIFICATION.md` must-haves table with PASS rows for guide polish, CHANGELOG 0.3.1, docs-contract, hex negative test, release.yml hardening.
    Document post-execute operator sequence: merge → tag `v0.3.1` → Hex publish via `release.yml` → `/gsd-audit-milestone v2.3` → `/gsd-complete-milestone v2.3` (D-15).
  </action>
  <verify>
    <automated>mix ci && mix docs.contract && mix rendro.viewer_evidence missing && mix rendro.viewer_evidence validate --strict && mix release.preflight</automated>
  </verify>
  <done>Phase 72 closure gate green; VERIFICATION finalized; ship sequence documented for operator.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Publication Closure

| Boundary | Description |
|----------|-------------|
| Hex tarball → Adopter | Operator-only priv paths must not ship; claims must match published version. |
| CHANGELOG → hex.pm | Version narrative must not conflate pre–v2.3 and v2.3 viewer work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-03-01 | Tampering | CHANGELOG attributes v2.3 work to 0.3.0 | High | mitigate | `git show v0.3.0:CHANGELOG.md` diff; split sections per D-13; do not re-tag 0.3.0. |
| T-72-03-02 | Information Disclosure | Operator matrix/evidence in Hex package | High | mitigate | Negative hex.build test; `files:` unchanged; guide prerequisites unchanged. |
| T-72-03-03 | Tampering | release.yml publishes without preflight | High | mitigate | Add `mix release.preflight` step per D-14; release-proof CI job unchanged. |
| T-72-03-04 | Elevation of Privilege | Hex `files:` expanded mid-closure | High | mitigate | Task acceptance: `files:` grep unchanged; negative test fails if paths added. |
| T-72-03-05 | Repudiation | Guide claims contradict matrix | Medium | mitigate | Docs-contract deferral substring + path asserts (D-26). |
| T-72-03-06 | Denial of Service | release-please adoption mid-close | Low | accept | D-16 explicit out of scope. |

**Block gate:** T-72-03-01, T-72-03-02, T-72-03-03, T-72-03-04 are HIGH — must not merge without CHANGELOG split, negative hex test, release preflight step, and unchanged `files:` whitelist.
</threat_model>

<verification_criteria>
- `mix ci` — exit 0
- `mix docs.contract` — 8/8 lanes PASS
- `mix test test/docs_contract/` — exit 0 including new path/deferral/hex tests
- `mix.exs` `@version` — `"0.3.1"`
- `CHANGELOG.md` — `0.3.0` frozen + `0.3.1` section present
- `guides/viewer_evidence.md` — Appendix D `--strict` documented
- `.github/workflows/release.yml` — contains `mix release.preflight`
- `72-VERIFICATION.md` — finalized with all D-23 gates
- No edits to `priv/support_matrix.json` cell statuses
</verification_criteria>

<success_criteria>
- Guides and docs-contract aligned with terminal matrix state.
- `v0.3.1` release artifacts ready; Hex publish path hardened.
- Phase 72 verification finalized; operator ship sequence documented.
- GUARDRAIL-02 closure complete across all three waves.
</success_criteria>

<output>
After completion, create `.planning/phases/72-closure-audit-polish-and-ship/72-03-SUMMARY.md` via orchestration.

**Post-execute operator sequence (D-15 — not automated in this plan):**
1. Merge Phase 72 PR to `main`
2. `git tag v0.3.1 && git push origin v0.3.1` → `release.yml` publishes to Hex
3. `/gsd-audit-milestone v2.3`
4. `/gsd-complete-milestone v2.3`
</output>
