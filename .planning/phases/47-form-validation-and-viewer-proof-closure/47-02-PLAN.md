---
phase: 47
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - priv/support_matrix.json
  - guides/api_stability.md
  - test/docs_contract/forms_claims_test.exs
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - M4-FORMS
must_haves:
  truths:
    - Rendro exposes a machine-readable forms support matrix with separate widget, behavior, and viewer facets.
    - Public docs name the only proof-eligible viewers narrowly while keeping them unverified until manual proof is recorded.
    - Structural validation, authored semantic validation, and viewer-support claims stay clearly separated.
  artifacts:
    - path: priv/support_matrix.json
      provides: "Machine-readable forms support boundary contract"
    - path: guides/api_stability.md
      provides: "Public wording for truthful forms support posture"
    - path: test/docs_contract/forms_claims_test.exs
      provides: "Executable lock on support-matrix shape and public wording"
  key_links:
    - from: guides/api_stability.md
      to: priv/support_matrix.json
      via: "named viewer posture mirrors the machine-readable matrix instead of drifting before proof exists"
    - from: scripts/verify_docs.exs
      to: test/docs_contract/forms_claims_test.exs
      via: "forms support claims enter the canonical docs verification lane"
---

<objective>
Publish the supported forms contract as a small machine-readable matrix plus truthful public wording that matches it.

Purpose: Close the documentation and support-boundary side of the milestone without widening claims beyond named, proven viewers and explicit unsupported/unverified surfaces.
Output: Nested forms support metadata, docs wording, and docs-contract automation that keeps them aligned.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.8-ROADMAP.md
@.planning/milestones/v1.8-CONTEXT.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-CONTEXT.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-RESEARCH.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-PATTERNS.md
@.planning/phases/44-validator-backed-trust-surfaces/44-02-SUMMARY.md
</execution_context>

<context>
@guides/api_stability.md
@priv/support_matrix.json
@test/docs_contract/integrations_claims_test.exs
@test/support/docs_contract.ex
@scripts/verify_docs.exs

<interfaces>
From `priv/support_matrix.json`:
```json
{
  "validators": {
    "pdfinfo": {
      "version": "22+",
      "validates": ["structural_integrity"]
    }
  },
  "surfaces": {
    "validated": ["structural_integrity"],
    "experimental": []
  },
  "unsupported": ["full_pdf_compliance"]
}
```

From `scripts/verify_docs.exs`:
```elixir
lanes = [
  {"README doctest lane", ["test", "test/docs_contract/readme_doctest_test.exs"]},
  {"Integration contract lane", ["test", "test/docs_contract/integrations_contract_test.exs"]},
  {"Integration semantic-claims lane", ["test", "test/docs_contract/integrations_claims_test.exs"]}
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape the support matrix into stable forms facets</name>
  <files>priv/support_matrix.json, test/docs_contract/forms_claims_test.exs</files>
  <behavior>
    - `priv/support_matrix.json` exposes `forms.widgets`, `forms.behaviors`, and `forms.viewers` without becoming a full compatibility matrix.
    - Supported widgets remain `text`, `checkbox`, and `radio`; unsupported items like signatures, XFA, and hierarchical field names are explicit.
    - Adobe Acrobat Reader and Apple Preview stay `unverified` until Plan 03 records proof; other unchecked viewers also remain `unverified`.
  </behavior>
  <action>
    Replace the coarse `surfaces` shape in `priv/support_matrix.json` per D-18, D-19, D-20, and D-21 with a nested forms contract that keeps `validators.pdfinfo` as the structural lane while adding `forms.widgets`, `forms.behaviors`, and `forms.viewers`.
    Encode the Phase 47 posture directly in the matrix per D-14, D-15, and D-23: `text`, `checkbox`, and `radio` are supported; `signature`, `xfa`, `hierarchical_field_names`, and `need_appearances` remain unsupported; Adobe Acrobat Reader and Apple Preview are present as the only proof-eligible named viewers but must publish `status: "unverified"` in this plan because D-14 and D-16 require proof before any `supported` claim. Any extra viewer entries also remain `unverified`.
    Add `test/docs_contract/forms_claims_test.exs` as a focused contract test that parses the JSON, asserts the exact nested paths and provisional viewer statuses, and prevents drift back to the old flat `surfaces` contract or premature `supported` claims before Plan 03 completes proof.
    Keep the matrix small and versionable per D-20 and D-24; do not add browser-style compatibility sprawl or prose-only exceptions.
  </action>
  <verify>
    <automated>mix test test/docs_contract/forms_claims_test.exs</automated>
  </verify>
  <done>The support matrix exposes stable nested forms facets with explicit supported, unsupported, and unverified boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock public wording and docs verification to the new support contract</name>
  <files>guides/api_stability.md, test/docs_contract/forms_claims_test.exs, scripts/verify_docs.exs</files>
  <behavior>
    - Public docs avoid “standard viewers” wording and name only the proof-eligible viewers backed by the matrix.
    - Docs explicitly separate authored semantic validation, Poppler structural validation, and viewer-interaction proof.
    - The canonical docs verification script runs the forms claims lane automatically.
  </behavior>
  <action>
    Update `guides/api_stability.md` per D-12, D-13, D-14, D-16, D-17, D-22, and D-23 with narrow support-boundary language for interactive forms. The wording must say that Rendro supports authored AcroForm text fields, checkboxes, and radio groups; Adobe Acrobat Reader and Apple Preview are the only named viewers eligible for support in this phase, but they remain `unverified` until the Phase 47 viewer checklist is completed; other viewers are not part of the supported contract unless the matrix later records proof-backed support.
    Extend `test/docs_contract/forms_claims_test.exs` to assert that provisional wording exactly, to refute broad phrases such as “standard PDF viewers”, and to fail if Acrobat/Preview are described as supported before Plan 03 updates the contract from recorded proof.
    Update `scripts/verify_docs.exs` to include the new forms claims lane in the canonical docs gate so support-matrix and wording regressions fail together.
    Keep one authoritative contract story per D-24: no new guide sprawl, no broad viewer marketing, and no conflation of Poppler validation with viewer proof.
  </action>
  <verify>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>Human-facing wording and machine-readable claims are tied together under one automated docs-contract lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal support contract -> public docs | Capability statements leave code/tests and become external compatibility claims here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Repudiation | `priv/support_matrix.json` + docs | mitigate | Keep a machine-readable matrix plus executable docs-claims tests so supported/unverified/unsupported statements are auditable and versioned. |
| T-47-04 | Information Integrity | `guides/api_stability.md` | mitigate | Refute vague “standard viewers” wording and require named viewers with proof-backed status in both docs and tests. |
</threat_model>

<verification>
mix test test/docs_contract/forms_claims_test.exs
mix run scripts/verify_docs.exs
</verification>

<success_criteria>
- `priv/support_matrix.json` publishes stable nested forms facets with explicit unsupported boundaries and provisional `unverified` viewer statuses until proof exists.
- Public support wording stays narrow, names only the proof-eligible viewers, and remains consistent with the provisional matrix state.
- Docs verification fails if the matrix or wording drifts from the Phase 47 support contract.
</success_criteria>

<output>
After completion, create `.planning/phases/47-form-validation-and-viewer-proof-closure/47-02-SUMMARY.md`
</output>
