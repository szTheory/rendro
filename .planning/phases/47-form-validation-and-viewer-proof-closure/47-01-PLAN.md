---
phase: 47
plan: 01
type: execute
wave: 1
depends_on: ["46-02"]
files_modified:
  - lib/rendro/rules/check_form_fields.ex
  - lib/rendro/form_field.ex
  - test/rendro/rules/check_form_fields_test.exs
  - test/rendro/pipeline/validate_test.exs
autonomous: true
requirements:
  - M4-FORMS
must_haves:
  truths:
    - Unsupported or ambiguous authored form states fail during `Rendro.Pipeline.Validate` before PDF serialization.
    - Supported form widgets stay limited to text fields, checkboxes, and radio groups with explicit non-coercive semantics.
    - Duplicate field identity, dotted names, invalid text values, and contradictory radio-group defaults are reported as typed validation errors.
  artifacts:
    - path: lib/rendro/rules/check_form_fields.ex
      provides: "Core semantic validation for the supported AcroForm surface"
    - path: test/rendro/rules/check_form_fields_test.exs
      provides: "Raw rule-tuple regression coverage for authored form semantics"
    - path: test/rendro/pipeline/validate_test.exs
      provides: "Validate-stage aggregation coverage for form semantic failures"
  key_links:
    - from: lib/rendro/pipeline/validate.ex
      to: lib/rendro/rules/check_form_fields.ex
      via: "single-pass rule aggregation through `details.errors`"
    - from: lib/rendro/form_field.ex
      to: lib/rendro/rules/check_form_fields.ex
      via: "supported authored attribute contract matches the enforced validation surface"
---

<objective>
Tighten the core authored-boundary validation contract for Rendro’s supported form widgets.

Purpose: Close the gap between what the writer can serialize and what Rendro truthfully supports by rejecting unsupported or ambiguous authored state before render.
Output: Expanded semantic validation in `CheckFormFields` plus focused rule/pipeline regression coverage.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.8-ROADMAP.md
@.planning/milestones/v1.8-CONTEXT.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-CONTEXT.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-RESEARCH.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-PATTERNS.md
@.planning/phases/45-01-SUMMARY.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-01-SUMMARY.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-02-SUMMARY.md
</execution_context>

<context>
@lib/rendro/pipeline/validate.ex
@lib/rendro/rules/check_form_fields.ex
@lib/rendro/form_field.ex
@test/rendro/rules/check_form_fields_test.exs
@test/rendro/pipeline/validate_test.exs

<interfaces>
From `lib/rendro/pipeline/validate.ex`:
```elixir
@default_rules [CheckReferences, CheckBounds, CheckRequiredKeys, CheckFormFields]

case walk(doc, doc, rules) do
  [] ->
    {:ok, doc}

  errors ->
    {:error,
     Rendro.Error.from_stage(:validate, :structural_corruption, %{details: %{errors: errors}})}
end
```

From `lib/rendro/rules/check_form_fields.ex`:
```elixir
def check(%Document{} = doc, _root_doc) do
  case duplicate_checked_radio_groups(doc) do
    [] -> :ok
    groups -> {:errors, Enum.map(groups, &{:radio_group_multiple_checked_defaults, &1})}
  end
end

def check(%FormField{name: name}, _doc) when not (is_binary(name) and byte_size(name) > 0),
  do: {:error, {:missing_required_key, :name}}
```

From `lib/rendro/form_field.ex`:
```elixir
@type field_type :: :text | :checkbox | :radio

defstruct [
  :name,
  type: :text,
  value: "",
  font: "Helvetica",
  size: 12,
  checked: false,
  group: nil,
  export_value: "Yes"
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce field-local supported-surface semantics</name>
  <files>lib/rendro/rules/check_form_fields.ex, lib/rendro/form_field.ex, test/rendro/rules/check_form_fields_test.exs</files>
  <behavior>
    - Text fields reject non-binary values, unsupported dotted names, unsupported editing-font inputs, and non-positive sizes.
    - Checkbox and radio widgets reject empty or non-binary export values instead of relying on writer behavior.
    - Validation remains explicit and non-coercive for names, values, booleans, fonts, and sizes.
  </behavior>
  <action>
    Extend `lib/rendro/rules/check_form_fields.ex` per D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, and D-11 using the existing clause-per-invariant style rather than adding a second traversal path or writer-side guards.
    Add explicit tuple errors for the new invariants and keep them small and machine-readable; use the researcher’s recommended contract unless the code proves a tighter existing convention: `{:unsupported_form_field_name, name}`, `{:invalid_form_field_value, value}`, `{:invalid_form_field_font, font}`, `{:invalid_form_field_size, size}`, and `{:invalid_form_field_export_value, export_value}`.
    Reject hierarchical or dotted names for all supported widget families, keep `checked` boolean-only, keep supported widget families fixed at `:text`, `:checkbox`, and `:radio`, and do not coerce atoms/strings/booleans into accepted values.
    Update `lib/rendro/form_field.ex` only as needed to make the supported authored attribute contract explicit in docs/typespecs; do not widen the struct into new widget families or signatures.
    Add raw rule-tuple tests covering every new local invariant in `test/rendro/rules/check_form_fields_test.exs`.
  </action>
  <verify>
    <automated>mix test test/rendro/rules/check_form_fields_test.exs</automated>
  </verify>
  <done>Local authored-form semantics are enforced in core with typed errors and no coercive fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add document-wide identity and radio-group integrity checks</name>
  <files>lib/rendro/rules/check_form_fields.ex, test/rendro/rules/check_form_fields_test.exs, test/rendro/pipeline/validate_test.exs</files>
  <behavior>
    - Duplicate field identity fails before render, including collisions that make the logical AcroForm surface ambiguous.
    - Radio groups reject duplicate export values and multiple checked defaults within one group.
    - `Validate.run/1` aggregates the new semantic failures through the existing `%Rendro.Error{details: %{errors: errors}}` envelope.
  </behavior>
  <action>
    Add document-level scans in `lib/rendro/rules/check_form_fields.ex` per D-06, D-10, and D-11 using the existing table-aware collectors instead of a second document walk.
    Implement one narrow truthful identity rule set: standalone field names must be unique, radio widget names must be unique, radio-group names must not collide with any existing logical field identity, and radio export values must be unique within each group. Reuse `{:duplicate_form_field_name, identity}` for logical-name collisions and `{:duplicate_radio_export_value, group, export_value}` for per-group export collisions.
    Preserve the existing `{:radio_group_multiple_checked_defaults, group}` contract and add pipeline-level assertions in `test/rendro/pipeline/validate_test.exs` so the new tuples appear in aggregated validate-stage errors, not only raw rule tests.
    Keep the recommendation set cohesive per D-23 and D-24: one rule module, one authored-boundary validation surface, no scope widening into hierarchical naming or additional widget families.
  </action>
  <verify>
    <automated>mix test test/rendro/rules/check_form_fields_test.exs test/rendro/pipeline/validate_test.exs</automated>
  </verify>
  <done>Document-wide form identity and radio semantics are enforced before serialization and proven at both rule and pipeline layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User API -> Core validation | Developer-authored form metadata enters the supported contract boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | `Rendro.Rules.CheckFormFields` | mitigate | Reject unsupported/dotted names, ambiguous identities, and contradictory radio semantics before writer code can guess intent. |
| T-47-02 | Integrity | `Rendro.FormField` authored attributes | mitigate | Keep accepted value/font/size/export shapes explicit and typed instead of coercing developer input into plausible-but-unsupported states. |
</threat_model>

<verification>
mix test test/rendro/rules/check_form_fields_test.exs test/rendro/pipeline/validate_test.exs
</verification>

<success_criteria>
- `Rendro.Pipeline.Validate` rejects unsupported form semantics with typed tuples collected under `details.errors`.
- Supported surface remains limited to text, checkbox, and radio widgets with explicit non-coercive authored inputs.
- Duplicate identity, dotted names, invalid text-editing attributes, duplicate radio exports, and contradictory defaults all have focused regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/47-form-validation-and-viewer-proof-closure/47-01-SUMMARY.md`
</output>
