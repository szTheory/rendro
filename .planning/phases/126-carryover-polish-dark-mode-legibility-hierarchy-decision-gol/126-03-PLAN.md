---
phase: 126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol
plan: 03
type: execute
wave: 2
depends_on: [126-01, 126-02]
files_modified:
  - .github/workflows/ci.yml
  - test/guardrails/required_checks_contract_test.exs
  - priv/raster_refs/presets/swiss/light.sha256
  - priv/raster_refs/presets/corporate_classic/dark.sha256
  - priv/raster_refs/presets/editorial/dark.sha256
  - priv/raster_refs/presets/minimal_mono/dark.sha256
  - priv/raster_refs/presets/humanist/dark.sha256
  - priv/raster_refs/presets/brutalist/dark.sha256
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05]
must_haves:
  truths:
    - "D-13/D-14: focused and full deterministic suites pass before affected pinned-PDFium hashes are regenerated, and the two verification lanes remain separate."
    - "The pinned blessing runs from the push of the exact tested workflow commit to a unique guarded `gsd/phase-126-raster-bless-*` ref and returns a manifest-bound artifact; no default-branch merge, local blessing, or local-main push is authorized."
    - "Exactly the six preset rows whose recipes changed are reviewed for mechanical byte impact: Swiss Invoice light, Corporate-Classic Invoice dark, Editorial/Minimal-Mono Ticket dark, and Humanist/Brutalist Payslip dark; unrelated reference hashes remain unchanged."
    - "D-15: one full-size PNG per affected row is written to a caller-provided temporary directory with stable row-ID filenames suitable for sequential review, never as only a contact sheet."
    - "The advisory job runs the existing adapter raster harness compare-only and the preset raster harness separately; only a `push` whose `github.ref_name` matches the dedicated Phase 126 blessing prefix may bless preset hashes, and the required test job never receives blessing state."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Guarded isolated-ref push route for pinned-PDFium preset blessing that preserves ordinary CI triggers, the adapter comparison harness, and a bounded provenance artifact"
    - path: test/guardrails/required_checks_contract_test.exs
      provides: "Executable workflow contract for the blessing branch filter, exact push/ref guard, both raster commands, lane isolation, review directory, payload source, and exact-commit manifest"
    - path: priv/raster_refs/presets/swiss/light.sha256
      provides: "Pinned Swiss Invoice light page-one hash"
    - path: priv/raster_refs/presets/corporate_classic/dark.sha256
      provides: "Pinned Corporate-Classic Invoice dark page-one hash"
    - path: priv/raster_refs/presets/editorial/dark.sha256
      provides: "Pinned Editorial Ticket dark page-one hash"
    - path: priv/raster_refs/presets/minimal_mono/dark.sha256
      provides: "Pinned Minimal-Mono Ticket dark page-one hash"
    - path: priv/raster_refs/presets/humanist/dark.sha256
      provides: "Pinned Humanist Payslip dark page-one hash"
    - path: priv/raster_refs/presets/brutalist/dark.sha256
      provides: "Pinned Brutalist Payslip dark page-one hash"
  key_links:
    - from: .github/workflows/ci.yml
      to: test/rendro/theme/preset_raster_snapshot_test.exs
      via: "a dedicated advisory step passes push/ref-guarded MIX_RASTER_BLESS and RENDRO_PRESET_RASTER_REVIEW_DIR to the sole producer of preset hashes and stable-row PNGs"
      pattern: "github.event_name == 'push'|startsWith|RENDRO_PRESET_RASTER_REVIEW_DIR"
    - from: test/rendro/theme/preset_raster_snapshot_test.exs
      to: priv/pdfium_pin.json
      via: "version and executable-SHA provenance check before compare/bless"
      pattern: "assert_pinned_pdfium"
    - from: test/support/preset_render_matrix.ex
      to: priv/raster_refs/presets
      via: "stable row ID maps recipe/genre/mode to its page-one reference"
      pattern: "invoice|ticket|payslip"
  prohibitions:
    - statement: "Raster hashes must not be generated or blessed by an unpinned host renderer or merged into the required deterministic CI lane."
      status: unverified
      flagged: true
    - statement: "Unchanged recipe rows must not receive reference edits merely because the harness renders the full fixed matrix."
      status: unverified
      flagged: true
---

<objective>
Produce provenance-bound advisory raster evidence for only the preset rows mechanically affected by the three repairs.

Purpose: prepare readable, row-addressable images for honest human judgment after deterministic behavior is already proven.
Output: a successful exact-commit pinned-CI run, its provenance manifest, six refreshed affected page-one hashes, and six full-size review PNGs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/WINDOWS.md
@.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-CONTEXT.md
@.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-RESEARCH.md
@.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-VALIDATION.md
@.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-01-SUMMARY.md
@.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Push the guarded pinned-CI blessing ref and import the six affected raster artifacts</name>
  <read_first>
    - .planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-CONTEXT.md
    - .planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-VALIDATION.md
    - .github/workflows/ci.yml
    - test/guardrails/required_checks_contract_test.exs
    - test/support/preset_render_matrix.ex
    - test/rendro/theme/preset_render_matrix_test.exs
    - test/rendro/theme/preset_raster_snapshot_test.exs
    - priv/pdfium_pin.json
    - priv/raster_refs/presets/swiss/light.sha256
    - priv/raster_refs/presets/corporate_classic/dark.sha256
    - priv/raster_refs/presets/editorial/dark.sha256
    - priv/raster_refs/presets/minimal_mono/dark.sha256
    - priv/raster_refs/presets/humanist/dark.sha256
    - priv/raster_refs/presets/brutalist/dark.sha256
  </read_first>
  <files>.github/workflows/ci.yml, test/guardrails/required_checks_contract_test.exs, priv/raster_refs/presets/swiss/light.sha256, priv/raster_refs/presets/corporate_classic/dark.sha256, priv/raster_refs/presets/editorial/dark.sha256, priv/raster_refs/presets/minimal_mono/dark.sha256, priv/raster_refs/presets/humanist/dark.sha256, priv/raster_refs/presets/brutalist/dark.sha256</files>
  <precondition>`gh auth status` succeeds with `repo` and `workflow` scopes and `git push origin` may create an isolated Phase-126 CI ref so GitHub Actions can execute the exact Plan 01/02 commit without pushing the local main branch.</precondition>
  <action>First implement the advisory route that this task will consume. Extend top-level `on.push.branches` with the quoted filter `'gsd/phase-126-raster-bless-*'` while preserving the existing `main` push, `pull_request`, and `schedule` triggers exactly; do not add `workflow_dispatch` and do not require a merge to the default branch. Preserve the existing adapter harness as its own compare-only step with literal `MIX_RASTER_BLESS: "false"` and exact command `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs`. Add a separate preset step whose exact command is `mix test --include raster_snapshot test/rendro/theme/preset_raster_snapshot_test.exs`, with `MIX_RASTER_BLESS: ${{ github.event_name == 'push' &amp;&amp; startsWith(github.ref_name, 'gsd/phase-126-raster-bless-') &amp;&amp; 'true' || 'false' }}` and `RENDRO_PRESET_RASTER_REVIEW_DIR: ${{ runner.temp }}/rendro_phase126_review`; this exact event/ref conjunction is the sole condition that may enable blessing, so ordinary CI, adapter raster tests, pull requests, schedules, `main`, and all other refs remain compare-only. On the same `github.event_name == 'push' &amp;&amp; startsWith(github.ref_name, 'gsd/phase-126-raster-bless-')` predicate, create `${{ runner.temp }}/phase126_preset_raster_bless`, then stage these exact hash sources while preserving their repository-relative paths: `priv/raster_refs/presets/swiss/light.sha256`, `priv/raster_refs/presets/corporate_classic/dark.sha256`, `priv/raster_refs/presets/editorial/dark.sha256`, `priv/raster_refs/presets/minimal_mono/dark.sha256`, `priv/raster_refs/presets/humanist/dark.sha256`, and `priv/raster_refs/presets/brutalist/dark.sha256`. Stage these exact preset-harness outputs under artifact-relative `review/`: `${{ runner.temp }}/rendro_phase126_review/swiss_invoice_light_page_1.png`, `corporate_classic_invoice_dark_page_1.png`, `editorial_ticket_dark_page_1.png`, `minimal_mono_ticket_dark_page_1.png`, `humanist_payslip_dark_page_1.png`, and `brutalist_payslip_dark_page_1.png`. Generate artifact-root `manifest.txt` from actual CI values with `GITHUB_SHA=${GITHUB_SHA}` and `GITHUB_RUN_ID=${GITHUB_RUN_ID}`, the version and digest read from `priv/pdfium_pin.json`, and the sorted twelve artifact-relative payload paths. Upload `${{ runner.temp }}/phase126_preset_raster_bless` as `phase-126-preset-raster-bless` using `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` with `if-no-files-found: error` and the identical push/ref guard. Extend `test/guardrails/required_checks_contract_test.exs` to parse the workflow and advisory block and assert the preserved ordinary triggers plus exact blessing branch filter, both exact harness commands, adapter `MIX_RASTER_BLESS: "false"`, exact preset event/ref expression, actual review-directory env, guarded staging/upload, all twelve exact source-to-artifact mappings, manifest SHA/run/pin fields, pinned upload action/name/path, graph disconnection, and absence of blessing/review/upload fragments from the required `test` block.

Per D-13/D-14, run that guardrail plus the focused Invoice/Ticket/Payslip deterministic tests, all seven typography modules, the accent golden, and the deterministic preset matrix. After they pass, commit the workflow/guardrail changes so the tested route is in the exact commit GitHub will execute, capture `HEAD_SHA=$(git rev-parse HEAD)`, derive the unique `CI_REF=gsd/phase-126-raster-bless-${HEAD_SHA:0:12}`, and push that exact commit as the tip with `git push origin "$HEAD_SHA:refs/heads/$CI_REF"`; the push itself triggers `ci.yml` from that commit, so do not issue a separate manual-dispatch command. Poll for at most 60 seconds using the exact selector `gh run list --workflow ci.yml --branch "$CI_REF" --event push --limit 20 --json databaseId,headSha --jq ".[] | select(.headSha == \"$HEAD_SHA\") | .databaseId"`, assign its single result to `RUN_ID`, and fail closed if no run appears or more than one exact-SHA result is returned. Confirm `gh run view "$RUN_ID" --json event,headBranch,headSha --jq ".event == \"push\" and .headBranch == \"$CI_REF\" and .headSha == \"$HEAD_SHA\""` returns `true`, then require `gh run watch "$RUN_ID" --exit-status` to succeed. Only then download `phase-126-preset-raster-bless` with `gh run download "$RUN_ID" -n phase-126-preset-raster-bless -D tmp/phase126_ci_raster_artifact`. Validate the artifact manifest before copying anything: `GITHUB_SHA` equals `HEAD_SHA`, `GITHUB_RUN_ID` equals `RUN_ID`, the PDFium version/digest equal `priv/pdfium_pin.json`, and the sorted payload list contains exactly the six declared hash paths plus the six stable-row full-size PNGs. Copy those six hashes to their declared repository paths and the six PNGs to `tmp/rendro_phase126_review`; never run local blessing, never set `GITHUB_ACTIONS` locally, never push local main, never merge the workflow prerequisite to `main`, and never import any other matrix hash. Finish with an exact six-file diff fence and a compare-only audit of artifact hashes/PNG presence. Record `CI_REF`, `HEAD_SHA`, and the run URL in the Summary. After the artifact is safely downloaded and validated, retain the remote ref until the human provenance and full-size sequential review checkpoints complete; then request explicit approval before deleting it, because remote-ref deletion is a separate destructive cleanup action.</action>
  <acceptance_criteria>
    - Focused deterministic commands exit 0 before the isolated-ref push, and the selected `push` run's workflow, branch, and `headSha` are respectively `ci.yml`, `CI_REF`, and the exact tested/pushed `HEAD_SHA`.
    - The advisory job executes the adapter harness with blessing forced false and executes `test/rendro/theme/preset_raster_snapshot_test.exs` separately with exact push/ref-controlled blessing plus `${{ runner.temp }}/rendro_phase126_review`; ordinary triggers and the required test job contain no enabled blessing path.
    - The workflow guardrail proves the six hashes and six stable-row PNGs are staged from the preset harness outputs and that manifest commit/run/pin provenance is populated from CI, not prose constants.
    - The successful GitHub Actions artifact manifest records the expected run ID, commit SHA, PDFium version, and executable SHA from `priv/pdfium_pin.json`; no local or alternate renderer is accepted.
    - Only the six declared raster references are imported, each containing one lowercase 64-character SHA-256 line; unrelated matrix references are untouched.
    - Exactly six full-size PNGs named by stable row ID exist in `tmp/rendro_phase126_review` for the later sequential review.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted test/guardrails/required_checks_contract_test.exs &amp;&amp; mix test test/guardrails/required_checks_contract_test.exs test/rendro/recipes/invoice*_test.exs test/rendro/recipes/ticket*_test.exs test/rendro/recipes/payslip*_test.exs test/rendro/recipes/*_typography_test.exs test/rendro/theme/preset_accent_golden_test.exs test/rendro/theme/preset_render_matrix_test.exs --max-failures 1 &amp;&amp; test -f tmp/phase126_ci_raster_artifact/manifest.txt &amp;&amp; test "$(git diff --name-only -- priv/raster_refs/presets | grep -vE '^(priv/raster_refs/presets/(swiss/light|corporate_classic/dark|editorial/dark|minimal_mono/dark|humanist/dark|brutalist/dark)\.sha256)$' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(find tmp/rendro_phase126_review -maxdepth 1 -type f -name '*_page_1.png' | rg '(swiss_invoice_light|corporate_classic_invoice_dark|editorial_ticket_dark|minimal_mono_ticket_dark|humanist_payslip_dark|brutalist_payslip_dark)' | wc -l | tr -d ' ')" = "6"</automated>
  </verify>
  <done>All six affected rows have deterministic proof and an exact-commit, pinned-CI artifact handoff imported through a fail-closed six-file fence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the pinned CI provenance handoff before visual review</name>
  <read_first>
    - .planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-CONTEXT.md
    - .github/workflows/ci.yml
    - priv/pdfium_pin.json
    - tmp/phase126_ci_raster_artifact/manifest.txt
  </read_first>
  <files>None (read-only review of the GitHub Actions run and downloaded manifest)</files>
  <what-built>An isolated guarded-ref push caused GitHub Actions to run the exact tested workflow commit with SHA-verified PDFium v0.11.0, then handed back only the six affected hashes and six full-size row PNGs through a named artifact.</what-built>
  <how-to-verify>
    1. Open the GitHub Actions run URL reported by Task 1 and confirm event `push`, head branch equals the unique `gsd/phase-126-raster-bless-<short-sha>` ref, conclusion `success`, and head SHA equals the local tested commit.
    2. Confirm the `advisory-checks` log shows the pinned download SHA check and `MIX_RASTER_BLESS=true`; confirm the required `test` job remained deterministic and did not bless.
    3. Compare `tmp/phase126_ci_raster_artifact/manifest.txt` with `priv/pdfium_pin.json` and confirm the payload lists exactly six declared hashes plus six named PNGs.
    4. Confirm the local raster-reference diff contains no path outside the six declared files. Approve the provenance handoff only; Plan 126-04 separately reviews image quality full-size.
  </how-to-verify>
  <acceptance_criteria>
    - The reviewer confirms the external run, tested commit SHA, pinned renderer digest/version, and exact artifact payload agree.
    - The reviewer confirms blessing occurred only in the advisory job and no unpinned local blessing was used.
  </acceptance_criteria>
  <verify>
    <automated>test -f tmp/phase126_ci_raster_artifact/manifest.txt &amp;&amp; test "$(git diff --name-only -- priv/raster_refs/presets | grep -vE '^(priv/raster_refs/presets/(swiss/light|corporate_classic/dark|editorial/dark|minimal_mono/dark|humanist/dark|brutalist/dark)\.sha256)$' | wc -l | tr -d ' ')" = "0"</automated>
    <human-check>Approve only when the exact-commit GitHub Actions run and artifact manifest establish pinned CI provenance and the six-file import fence.</human-check>
  </verify>
  <resume-signal>Type "approved" or identify the mismatched run SHA, pin field, payload path, or job behavior.</resume-signal>
  <done>The external pinned-CI blessing and artifact retrieval path is explicitly accepted before visual judgment begins.</done>
</task>

</tasks>

## Artifacts this phase produces

Files:
- `.github/workflows/ci.yml`
- `test/guardrails/required_checks_contract_test.exs`
- `priv/raster_refs/presets/swiss/light.sha256`
- `priv/raster_refs/presets/corporate_classic/dark.sha256`
- `priv/raster_refs/presets/editorial/dark.sha256`
- `priv/raster_refs/presets/minimal_mono/dark.sha256`
- `priv/raster_refs/presets/humanist/dark.sha256`
- `priv/raster_refs/presets/brutalist/dark.sha256`
- Ephemeral `tmp/phase126_ci_raster_artifact/manifest.txt` (downloaded CI provenance and payload manifest; not committed)
- Ephemeral `tmp/rendro_phase126_review/<row-id>_page_1.png` review images (not committed)

New symbols: none.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| deterministic PDFs → external PDFium process | Optional rasterization crosses a tool provenance boundary. |
| raster bytes → committed hashes/human review | Advisory evidence may be stale or falsely blessed if row identity/provenance is not fixed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-09 | Tampering | PDFium executable/reference blessing | high | mitigate | Verify pinned version and executable SHA; bless only through the established CI/container guard. |
| T-126-10 | Repudiation | row-to-hash provenance | medium | mitigate | Use stable matrix IDs, exact six-file diff fence, and one named full-size PNG per row. |
| T-126-11 | Denial of service | raster generation | low | accept | Fixed twelve-row/page-one harness and six-row review set bound runtime and disk use. |
</threat_model>

<verification>
Deterministic suites run first. The exact tested workflow commit is then pushed to a unique guarded Phase 126 ref, and that push directly starts the pinned advisory workflow without a default-branch merge; exact run selection, manifest validation, the blocking provenance checkpoint, and the final diff fence prove the artifact came from that commit and no unrelated reference row changed.
</verification>

<success_criteria>
- Six affected rows have current pinned hashes and readable full-size PNGs.
- No unpinned renderer or unrelated reference change enters the phase.
- D-13..D-15 evidence ordering and lane separation are preserved.
</success_criteria>

<output>
Create `.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-03-SUMMARY.md` when done.
</output>
