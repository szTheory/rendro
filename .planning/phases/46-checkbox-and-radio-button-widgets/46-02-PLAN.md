---
phase: 46
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
autonomous: true
requirements:
  - M4-FORMS
must_haves:
  truths:
    - Rendered PDFs serialize checkbox and radio widgets as valid button fields.
    - Checkbox and radio widgets expose deterministic default visual state without relying on `NeedAppearances`.
    - Radio groups preserve authored exclusivity semantics through explicit PDF structure.
  artifacts:
    - path: lib/rendro/pdf/writer.ex
      provides: "Button-widget allocation, dictionaries, and appearance serialization"
  key_links:
    - from: lib/rendro/pdf/writer.ex
      to: "AcroForm field tree"
      via: "checkbox widgets and radio parent/child groups are allocated into deterministic objects"
    - from: lib/rendro/pdf/writer.ex
      to: "Page annotations"
      via: "button widgets are attached to the correct page `Annots` arrays"
---

<objective>
Extend the PDF writer so checkbox and radio widgets render as deterministic AcroForm button fields using the existing object-allocation and page-annotation pipeline.

Purpose: This plan turns the typed widget data from Plan 01 into real interactive PDF button widgets with trustworthy default appearances and grouping semantics.
Output: `/FT /Btn` widgets, deterministic on/off appearance dictionaries, and grouped radio serialization in generated PDFs.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-ROADMAP.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-CONTEXT.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-RESEARCH.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-PATTERNS.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-VALIDATION.md
</execution_context>

<interfaces>
From `lib/rendro/pdf/writer.ex` (exact analog allocation seam):
```elixir
defp allocate_form_field_nums(form_fields, start_num) do
  Enum.map_reduce(form_fields, start_num, fn form_field, num ->
    allocation = %{
      block: form_field.block,
      field: form_field.field,
      page_index: form_field.page_index,
      widget_obj_num: num,
      appearance_obj_num: num + 1
    }

    {allocation, num + 2}
  end)
end
```

From `lib/rendro/pdf/writer.ex` (exact analog page annotation seam):
```elixir
{annot_refs, form_objects} =
  build_form_field_objects(
    page,
    page_num,
    Enum.filter(form_field_allocations, &(&1.page_index == page_index)),
    opts
  )
```

From `test/rendro/pdf/writer_test.exs` (exact analog proof style):
```elixir
assert pdf =~ "/Subtype /Widget"
assert pdf =~ "/FT /Tx"
assert pdf =~ "/AP <<"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allocate and serialize checkbox widgets as button fields</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <behavior>
    - Checkbox widgets serialize as `/FT /Btn`.
    - Each checkbox has deterministic checked and unchecked appearance states and exposes the correct default `/AS`.
    - The writer keeps using page-local annotation attachment and deterministic object ordering.
  </behavior>
  <action>
    Extend `lib/rendro/pdf/writer.ex` using the existing helper-per-responsibility seams from `46-PATTERNS.md`: keep allocation, widget dictionary assembly, page annotation wiring, and appearance serialization in dedicated helpers.
    Add button-specific writer helpers rather than mutating the text-field helper into a giant mixed branch. Checkbox output should include `/FT /Btn`, on/off state dictionaries, and explicit `/AS` aligned with authored `checked` state.
    Add focused PDF substring assertions in `test/rendro/pdf/writer_test.exs` following the existing text-field proof style, covering `/FT /Btn`, `/AS`, and deterministic checkbox appearance serialization.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Rendered PDFs contain deterministic checkbox widgets with valid button-field dictionaries and visible default state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize grouped radio widgets with explicit exclusivity semantics</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <behavior>
    - Radio widgets sharing one authored group serialize as one logical group, not independent checkboxes.
    - Group structure preserves mutual exclusivity semantics and deterministic default selection.
    - Radio widget annotations still land on the correct page and preserve authored geometry.
  </behavior>
  <action>
    Expand the writer allocation phase to group radio widgets before low-level serialization, following the deterministic-object-numbering pattern from `46-PATTERNS.md`.
    Serialize radios as an explicit parent button field with child widget annotations and authored export values. Keep the current page-local `Annots` attachment flow, but ensure each child widget references the correct group structure.
    Add writer tests proving grouped radio serialization, deterministic default selection, and correct annotation placement, again using the existing substring/assertion style rather than introducing brittle binary parsing.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Rendered PDFs encode grouped radio widgets with deterministic exclusivity semantics and correct page-local annotations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core widget data -> PDF dictionaries | Authored widget metadata is converted into executable PDF button semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Integrity | `Writer` | mitigate | Keep logical value, appearance state, and object allocation in one deterministic path so widgets cannot open in contradictory visual/logical states. |
| T-46-04 | Tampering | radio grouping | mitigate | Serialize explicit parent/child group relationships from authored `group` data instead of guessing group structure from layout or page order. |
</threat_model>

<success_criteria>
- Checkboxes serialize as `/FT /Btn` with deterministic default state and appearance data.
- Radio groups serialize with explicit grouping semantics rather than independent widgets.
- No `NeedAppearances` fallback is introduced.
</success_criteria>
