---
phase: 46
plan: 01
type: execute
wave: 1
depends_on: ["45-02"]
files_modified:
  - lib/rendro/form_field.ex
  - lib/rendro.ex
  - lib/rendro/rules/check_form_fields.ex
  - lib/rendro/pipeline/measure.ex
  - test/rendro/rules/check_form_fields_test.exs
  - test/rendro_builders_test.exs
autonomous: true
requirements:
  - M4-FORMS
must_haves:
  truths:
    - Developers can author checkbox and radio button widgets through `Rendro.form_field/3`.
    - Invalid radio widget definitions fail validation before render.
    - Button widgets retain deterministic layout sizing through the existing form-field measurement path.
  artifacts:
    - path: lib/rendro/form_field.ex
      provides: "Extended form-field domain contract for button widgets"
    - path: lib/rendro/rules/check_form_fields.ex
      provides: "Type-aware and document-aware widget validation"
  key_links:
    - from: lib/rendro.ex
      to: lib/rendro/form_field.ex
      via: "`Rendro.form_field/3` constructs typed button widgets"
    - from: lib/rendro/pipeline/validate.ex
      to: lib/rendro/rules/check_form_fields.ex
      via: "button widget invariants are enforced in the default validation pipeline"
---

<objective>
Extend the public/domain/validation surface so Rendro can express checkbox and radio button widgets safely before any PDF writer changes land.

Purpose: This plan broadens the existing Phase 45 form-field contract to cover button widgets while preserving one DSL surface and one measurement path.
Output: Typed, validated checkbox/radio widget data flowing through the existing build/measure/validate pipeline.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-ROADMAP.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-CONTEXT.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-RESEARCH.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-PATTERNS.md
@.planning/phases/46-checkbox-and-radio-button-widgets/46-VALIDATION.md
</execution_context>

<interfaces>
From `lib/rendro/form_field.ex` (exact analog):
```elixir
@enforce_keys [:name]
defstruct [
  :name,
  value: "",
  font: "Helvetica",
  size: 12
]
```

From `lib/rendro.ex` (exact analog):
```elixir
{field_attrs, block_attrs} = Keyword.split(attrs, [:font, :size])
field = struct!(FormField, Keyword.merge(field_attrs, name: name, value: value))
struct!(Block, Keyword.put(block_attrs, :content, field))
```

From `lib/rendro/pipeline/measure.ex` (exact analog):
```elixir
defp measure_block(
       _doc,
       %Rendro.Block{content: %Rendro.FormField{}} = block,
       _container_width
     ) do
  {:ok, %{block | width: block.width || 150.0, height: block.height || 20.0}}
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend form-field authoring contract for button widgets</name>
  <files>lib/rendro/form_field.ex, lib/rendro.ex, test/rendro_builders_test.exs</files>
  <behavior>
    - `Rendro.FormField` can represent `:text`, `:checkbox`, and `:radio` widget types.
    - Button widgets carry authored fields needed later by the writer, including `checked`, `group`, and `export_value`.
    - `Rendro.form_field/3` continues to be the only public widget builder and accepts the new typed attrs cleanly.
  </behavior>
  <action>
    Extend `lib/rendro/form_field.ex` in place, following the existing struct-extension pattern from `46-PATTERNS.md`, rather than creating separate checkbox/radio structs.
    Update `Rendro.form_field/3` in `lib/rendro.ex` using the same `Keyword.split/2` builder pattern already present, expanding the allowed field attrs to include `:type`, `:checked`, `:group`, and `:export_value`.
    Add/expand builder tests in `test/rendro_builders_test.exs` to prove checkbox/radio widgets are authored through `Rendro.form_field/3` and preserve explicit block sizing.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>
  <done>Typed button-widget authoring works through the existing DSL surface with deterministic block structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add type-aware and document-aware validation for button widgets</name>
  <files>lib/rendro/rules/check_form_fields.ex, lib/rendro/pipeline/measure.ex, test/rendro/rules/check_form_fields_test.exs</files>
  <behavior>
    - Checkbox widgets validate with required name and sane boolean/export defaults.
    - Radio widgets require explicit group and export-value semantics.
    - Contradictory default radio selections in one group fail deterministically before render.
    - Measurement stays on the existing form-field clause, refining per-type defaults only if needed.
  </behavior>
  <action>
    Extend `lib/rendro/rules/check_form_fields.ex` using the current clause-based rule style from `46-PATTERNS.md`; keep the rule module focused but add type-specific clauses plus a document-level scan for contradictory radio defaults.
    Keep `lib/rendro/pipeline/measure.ex` on the existing `%Rendro.FormField{}` measurement seam; if checkbox/radio defaults differ from text fields, implement that by branching inside the current dedicated clause rather than adding a parallel pipeline path.
    Add failing-then-passing tests in `test/rendro/rules/check_form_fields_test.exs` for missing radio group, missing export value, and duplicate checked defaults within the same radio group.
  </action>
  <verify>
    <automated>mix test test/rendro/rules/check_form_fields_test.exs</automated>
  </verify>
  <done>Button-widget authored state is validated truthfully before writer serialization begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User API -> Core | Developers provide widget type, group identity, export value, and default state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | `Rendro.FormField` | mitigate | Keep all widget semantics in one typed struct so writer logic never guesses hidden defaults from layout position alone. |
| T-46-02 | Integrity | `CheckFormFields` | mitigate | Reject incomplete radio metadata and contradictory default selections before render. |
</threat_model>

<success_criteria>
- Developers can author checkboxes and radios through `Rendro.form_field/3`.
- Validation rejects structurally invalid button-widget state before writer execution.
- The measurement seam remains one `%Rendro.FormField{}` path, not a second widget pipeline.
</success_criteria>
