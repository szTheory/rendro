---
phase: 28-asset-registry-and-deterministic-image-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/image_parser.ex
  - test/rendro/image_parser_test.exs
  - lib/rendro/asset_registry.ex
  - test/rendro/asset_registry_test.exs
  - lib/rendro/document.ex
  - test/rendro/document_test.exs
autonomous: true
requirements:
  - ASSET-01
must_haves:
  truths:
    - "System can parse image binaries to extract intrinsic dimensions"
    - "Assets can be registered on a document struct with a logical name"
    - "Invalid or unsupported images fail deterministically at registration"
  artifacts:
    - path: "lib/rendro/image_parser.ex"
      provides: "Binary extraction of width/height from image files"
    - path: "lib/rendro/asset_registry.ex"
      provides: "State container for registered assets and their metadata"
  key_links:
    - from: "lib/rendro/document.ex"
      to: "lib/rendro/asset_registry.ex"
      via: "Rendro.Document.register_image/3 delegates to registry"
---

<objective>
Establish the first-class Asset Registry and image parsing utilities on the document boundary.

Purpose: To allow authoring APIs to reference assets logically by name, while ensuring all payloads and intrinsic dimensions are captured deterministically upfront on the `%Rendro.Document{}` struct without side-effects during measurement.
Output: Image parser for bounds extraction, asset registry state container, and the public document registration API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-asset-registry-and-deterministic-image-rendering/28-RESEARCH.md
@.planning/phases/28-asset-registry-and-deterministic-image-rendering/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ImageParser for intrinsic dimensions</name>
  <files>lib/rendro/image_parser.ex, test/rendro/image_parser_test.exs</files>
  <behavior>
    - Test 1: Given valid PNG bytes, extracts `{width, height}` and `mime: "image/png"`
    - Test 2: Given valid JPEG bytes, extracts `{width, height}` and `mime: "image/jpeg"`
    - Test 3: Given invalid/unsupported bytes, returns `{:error, :unsupported_image_format}`
  </behavior>
  <action>Create `Rendro.ImageParser` utilizing pure Elixir binary pattern matching to read PNG (IHDR chunk) and JPEG (SOF markers) headers to extract dimensions. Avoid pulling in heavy NIF-based libraries; this should be pure functional parsing for bounds and format. Analog reference: Follow the parsing approach in `lib/rendro/pdf/font_parser.ex`. Like the font parser which reads binary chunks recursively (e.g., `defp parse_table(<<tag::binary-4, ...>>)`), use explicit binary pattern matching on standard image chunk signatures and process headers functionally.</action>
  <verify>
    <automated>mix test test/rendro/image_parser_test.exs</automated>
  </verify>
  <done>Parser handles basic PNG and JPEG formats and returns deterministic intrinsic bounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AssetRegistry and Document Integration</name>
  <files>lib/rendro/asset_registry.ex, test/rendro/asset_registry_test.exs, lib/rendro/document.ex, test/rendro/document_test.exs</files>
  <behavior>
    - Test 1: Registry accepts valid image source (binary or path) and stores binary + parsed bounds.
    - Test 2: Registry raises custom InvalidAssetError if the image cannot be parsed.
    - Test 3: Document struct initializes with empty `asset_registry` and exposes `register_image(doc, logical_name, source)` that updates the registry.
  </behavior>
  <action>
    Create `Rendro.AssetRegistry` modeled strictly after `Rendro.FontRegistry`.
    It should store registered assets map: `%{logical_name => %{binary: bytes, width: w, height: h, mime: m}}`.
    Implement `register_image` in both `AssetRegistry` and `Rendro.Document`. Analog reference: The integration in `lib/rendro/document.ex` must follow the exact same pattern as the `font_registry` integration. Specifically, update the struct fields identically and delegate `Rendro.Document.register_image/3` directly to `AssetRegistry` analogous to how `register_font/3` is handled. Ensure local paths are read and converted to binaries *immediately* during registration, so downstream pipeline stages never do file I/O.
  </action>
  <verify>
    <automated>mix test test/rendro/asset_registry_test.exs test/rendro/document_test.exs</automated>
  </verify>
  <done>Assets can be registered on the document, storing fully resolved binaries and intrinsic dimensions.</done>
</task>

</tasks>