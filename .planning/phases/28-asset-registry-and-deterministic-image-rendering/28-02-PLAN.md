---
phase: 28-asset-registry-and-deterministic-image-rendering
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - lib/rendro/image.ex
  - lib/rendro/component.ex
  - test/rendro/component_test.exs
  - lib/rendro/pipeline/measure.ex
  - test/rendro/pipeline/measure_test.exs
autonomous: true
requirements:
  - ASSET-02
must_haves:
  truths:
    - "Authoring API allows declaring images with logical names and bounding constraints"
    - "Measurement pipeline looks up intrinsic dimensions from the asset registry"
    - "Measurement deterministically calculates missing dimensions using aspect ratio"
  artifacts:
    - path: "lib/rendro/image.ex"
      provides: "AST representation for an authored image"
    - path: "lib/rendro/component.ex"
      provides: "Rendro.image/2 helper function"
  key_links:
    - from: "lib/rendro/pipeline/measure.ex"
      to: "lib/rendro/asset_registry.ex"
      via: "Looks up logical_name in asset registry to determine aspect ratio"
---

<objective>
Introduce the image layout component and connect it to the measurement pipeline.

Purpose: Allow layout authors to declare images semantically, specifying either a width, height, or fit boundary. The layout engine will resolve this against the intrinsic dimensions extracted in Phase 28-01.
Output: `Rendro.Image` AST struct, `Rendro.image/2` helper, and bounds calculation in the Measure pipeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-asset-registry-and-deterministic-image-rendering/28-RESEARCH.md
@.planning/phases/28-asset-registry-and-deterministic-image-rendering/28-PATTERNS.md
</context>

<interfaces>
From lib/rendro/asset_registry.ex (created in 28-01):
```elixir
# Expected interface to pull from 28-01
def fetch(registry, logical_name) :: {:ok, %{width: w, height: h, mime: m, binary: b}} | :error
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Image struct and Component helper</name>
  <files>lib/rendro/image.ex, lib/rendro/component.ex, test/rendro/component_test.exs</files>
  <behavior>
    - Test 1: `Rendro.image(:logo, width: 100)` returns a `%Rendro.Block{content: %Rendro.Image{logical_name: :logo}, width: 100}`
    - Test 2: Fails/rejects missing constraint (must provide width, height, or fit).
  </behavior>
  <action>
    Create `%Rendro.Image{}` struct holding `:logical_name`. Analog reference: Follow the structure of `lib/rendro/text.ex` from PATTERNS.md for creating the AST representation.
    Add `image/2` to `Rendro.Component` (similar to how texts are wrapped in blocks).
    Ensure the user provides constraint dimensions (e.g., width, height, or a fit bounding box) so layout is deterministic. The helper should return a standard `Rendro.Block` with the `Rendro.Image` inside.
  </action>
  <verify>
    <automated>mix test test/rendro/component_test.exs</automated>
  </verify>
  <done>Image AST is well-typed and component builder enforces required constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constraint-based Image Measurement</name>
  <files>lib/rendro/pipeline/measure.ex, test/rendro/pipeline/measure_test.exs</files>
  <behavior>
    - Test 1: If image constraint is `width: 200` and intrinsic aspect ratio is 2:1, resulting block height is 100.
    - Test 2: If image constraint is `height: 100` and intrinsic is 2:1, resulting block width is 200.
    - Test 3: Fails measurement if image logical_name is not found in AssetRegistry.
  </behavior>
  <action>
    Update `Rendro.Pipeline.Measure` to handle `%Rendro.Block{content: %Rendro.Image{}}`.
    It must query the `doc.asset_registry` for the intrinsic bounds.
    Calculate the missing layout dimension (width or height) using the intrinsic aspect ratio.
    Return the modified block with fully resolved layout `width` and `height`.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Measurement pipeline correctly sizes images based on registry data and constraints.</done>
</task>

</tasks>