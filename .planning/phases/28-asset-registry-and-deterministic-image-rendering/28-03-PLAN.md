---
phase: 28-asset-registry-and-deterministic-image-rendering
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
  - lib/rendro/pipeline/render.ex
  - test/rendro/pipeline/render_test.exs
autonomous: true
requirements:
  - ASSET-03
must_haves:
  truths:
    - "Measured image blocks are embedded as Image XObjects in the PDF"
    - "Render pipeline translates image layout bounds to PDF matrix scale/translate"
  artifacts:
    - path: "lib/rendro/pdf/writer.ex"
      provides: "PDF Image stream generation and XObject mapping"
    - path: "lib/rendro/pipeline/render.ex"
      provides: "Rendering command emission for image blocks"
  key_links:
    - from: "lib/rendro/pipeline/render.ex"
      to: "lib/rendro/pdf/writer.ex"
      via: "Passes image payload and geometry to Writer"
---

<objective>
Render the measured images into the final PDF payload.

Purpose: To fulfill the visual representation by converting the abstract measured image blocks into PDF Image XObjects and executing the correct stream drawing operations.
Output: Updates to PDF writer and Render pipeline to support drawing images.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-asset-registry-and-deterministic-image-rendering/28-RESEARCH.md
</context>

<interfaces>
From lib/rendro/image.ex (created in 28-02):
```elixir
# Image AST representation
%Rendro.Image{logical_name: atom()}
```
From lib/rendro/asset_registry.ex:
```elixir
# Metadata provided from registry
%{width: intrinsic_w, height: intrinsic_h, mime: mime_type, binary: bytes}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PDF Image XObject Support to Writer</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <behavior>
    - Test 1: Writer correctly builds Image XObject dictionaries and streams for PNGs and JPEGs.
    - Test 2: Emits the `Do` (Draw Object) operator with appropriate transformation matrix (cm) for sizing.
  </behavior>
  <action>
    Update `Rendro.PDF.Writer` (or equivalent PDF object generation module) to support image payloads.
    Create XObjects for each unique image (pulled from AssetRegistry) and map them in the Resources dictionary.
    Expose drawing functions to emit the `cm` and `Do` operations that scale the 1x1 image space to the measured block bounds.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>PDF internals correctly support image streams and drawing commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Pipeline Integration</name>
  <files>lib/rendro/pipeline/render.ex, test/rendro/pipeline/render_test.exs</files>
  <behavior>
    - Test 1: When visiting a `%Rendro.Block{content: %Rendro.Image{}}`, it queries AssetRegistry and invokes PDF Writer image drawing.
  </behavior>
  <action>
    Update `Rendro.Pipeline.Render` block visitor logic to handle `%Rendro.Image{}` content.
    For each image block, fetch the binary/metadata from the registry and output the required positioning and drawing operations to the layout stream.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/render_test.exs</automated>
  </verify>
  <done>Render pipeline successfully translates measured image blocks into PDF rendering commands.</done>
</task>

</tasks>
