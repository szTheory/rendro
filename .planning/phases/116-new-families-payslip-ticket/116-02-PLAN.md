---
phase: 116-new-families-payslip-ticket
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - lib/rendro/recipes/payslip.ex
  - test/rendro/recipes/payslip_test.exs
  - test/rendro/recipes/payslip_byte_identity_test.exs
autonomous: true
requirements: [FAM-01, FAM-03]
must_haves:
  truths:
    - "Rendro.Recipes.Payslip.document/2 (3-rung pattern: document/2, page_template/1, sections/2) renders {:ok, pdf} for a well-formed fictional payslip"
    - "Net pay is the single largest/heaviest text element on the rendered page (D-11, rubric hierarchy=5) -- verified by comparing Rendro.Text sizes across sections/2's returned content, not just eyeballing the PDF"
    - "Earnings/deductions render as ONE combined ledger table (6 columns: Earnings|Current|YTD|Deductions|Current|YTD) with money right-aligned via cell_align, not two separate fixed side-by-side tables (D-12, Pitfall 4)"
    - "The combined ledger paginates natively across pages via Pagination.chunk_rows_into_pages/2 when line count is large, with the gross-to-net reconciliation block kept with the last table page (D-13)"
    - "net_pay always equals gross earnings minus total deductions, asserted via Decimal.equal?/2 -- never ==; a mismatch raises an instructive four-part ArgumentError (D-13)"
    - "Float anywhere in earnings/deductions amounts, ytd fields, net_pay, or optional totals raises an instructive ArgumentError instead of producing inexact financial output"
    - "Jurisdiction-specific line content (e.g. 'PAYE Income Tax' vs 'Federal Income Tax') lives in caller-supplied :description strings, never a library-enumerated type -- chrome labels come from :labels instead; proven by a negative test asserting an arbitrary non-English/unrelated :description string round-trips into the rendered PDF unchanged with no jurisdiction-keyword allowlist rejecting it (D-17)"
    - "Every color read in Payslip's section builders resolves a role from palette(opts) (S1) -- no inlined {0,0,0} or raw {r,g,b} literal anywhere in the file"
    - "@default_labels ships a complete jurisdiction-neutral English label set so Payslip.document(fixture) with zero :labels/:formatters opts renders correctly with no FunctionClauseError"
    - "page_template/1 implements the D-14 4-region template (:header/:summary/:body/:footer); the test fixture's employee/employer identifiers are masked (e.g. an id ending '... 4321' style, never a real-looking full SSN/NI/bank number), both business/employee are fictional, and masking is test-enforced via ExUnit regex assertions (masking token present + unmasked full-length numeric-id pattern refuted), not just prose narration (D-14 PII masking, FAM-01)"
    - "validate_data!/1 enforces the D-15 data map contract exactly: employer.name/employee.name/period.from,to/pay_date/earnings(non-empty)/deductions/net_pay required, totals/payment_method optional"
    - "No jurisdiction profile or named :profile atom exists anywhere -- :palette/:labels/:formatters are three orthogonal override seams merged over recipe-shipped defaults, the same override-map convention as the S1 palette seam (D-16)"
  artifacts:
    - lib/rendro/recipes/payslip.ex
    - test/rendro/recipes/payslip_test.exs
    - test/rendro/recipes/payslip_byte_identity_test.exs
  key_links:
    - "Payslip.document/2 -> page_template/1 (geometry-derived, 4 functional regions: header/summary/body/footer) -> sections/2 -> body_section/2 -> Rendro.Recipes.Pagination.chunk_rows_into_pages/2 for the combined ledger + a kept-with-last reconciliation block"
    - "Payslip.sections/2 calls Rendro.Recipes.Pagination.validate_labels!/2 and validate_formatters!/2 (from 116-01) before building any section content"
---

<objective>
Build `Rendro.Recipes.Payslip` end to end: geometry-derived 4-region template, the `palette(opts)` color seam (S1), full errors-as-product `validate_data!/1` (including the D-13 gross-to-net `Decimal.equal?/2` reconciliation assertion), the D-11 net-pay visual anchor, the D-12 combined ledger table with native multi-page pagination, and the D-13 kept-with-last reconciliation block. Jurisdiction differences (PAYE/NI vs FICA/401k) never enter `lib/` — they are caller `:description` data plus the recipe's own `@default_labels`/`:labels`/`:formatters` override seams (D-16-D-18).

Purpose: Deliver FAM-01 — the milestone's first new document family, a production-grade payslip whose reader-first fact (net pay) is unmistakably the visual anchor.
Output: `lib/rendro/recipes/payslip.ex` (adapter tier), `test/rendro/recipes/payslip_test.exs`, `test/rendro/recipes/payslip_byte_identity_test.exs`.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `Rendro.Recipes.Payslip` — new module, `@moduledoc tags: [:adapter]`
- `Rendro.Recipes.Payslip.page_template/1` — public, geometry-derived 4-region (`:header`/`:summary`/`:body`/`:footer`) template (D-14)
- `Rendro.Recipes.Payslip.sections/2` — public
- `Rendro.Recipes.Payslip.document/2` — public
- `Rendro.Recipes.Payslip.@default_labels` — module attribute, 13 jurisdiction-neutral English label keys including `:net_pay` (D-18)
- Private: `palette/1` (S1, verbatim from Invoice), `validate_data!/1`, `header_section/2`, `summary_section/2` (the D-11 net-pay anchor), `body_section/2` (D-12 combined ledger), `footer_section/2`, `validate_reconciliation!/1` (D-13, `Decimal.equal?/2`)

Registered in `priv/public_api.json` (adapter tier) and `priv/support_matrix.json` (surface `payslip`) by 116-04, not this plan.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-new-families-payslip-ticket/116-CONTEXT.md
@.planning/phases/116-new-families-payslip-ticket/116-RESEARCH.md
@.planning/phases/116-new-families-payslip-ticket/116-PATTERNS.md
@.planning/phases/116-new-families-payslip-ticket/116-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Geometry, palette, @default_labels, and validate_data!/1 shape/type checks</name>
  <files>lib/rendro/recipes/payslip.ex, test/rendro/recipes/payslip_test.exs</files>
  <read_first>
    lib/rendro/recipes/invoice.ex (`defp palette(opts)` at lines 371-386 — copy verbatim, same 7 roles/defaults; `page_template/1` `Keyword.take` whitelist at lines 119-131; `validate_data!/1` dispatch-on-non-map shape at lines 392-413; `maybe_validate_totals_types!/1` at lines 597-618 as the Decimal-vs-Float type-guard pattern to reuse per line-item field)
    lib/rendro/recipes/certificate.ex (`page_template/1` at lines 88-145 — `PageSize.resolve/2`-derived geometry, `ml/mr/mt/mb` margin pattern; Payslip must derive geometry the same way, NOT Invoice's fixed `@margin`/`@content_width` module attributes, so both A4 and US Letter work per D-14)
    lib/rendro/page_size.ex (`resolve/2` — full 19-line file; `:a4`/`:us_letter`/`{w,h}` tuple, `:portrait`/`:landscape`)
    lib/rendro/recipes/pagination.ex (from 116-01: `label_resolver/2`, `validate_labels!/2`, `validate_formatters!/2`, `type_name/1` — all now available)
  </read_first>
  <behavior>
    - `Payslip.page_template/1` (no opts) returns a `%Rendro.PageTemplate{}` with exactly 4 region names: `:header`, `:summary`, `:body`, `:footer` (region names, not necessarily physical region count — see Task 2 for a nuance on `:summary`'s implementation).
    - `Payslip.page_template(page_size: :us_letter)` returns different `width`/`height` than the `:a4` default (proves geometry is `PageSize.resolve/2`-derived, not hardcoded).
    - `validate_data!` raises an instructive `ArgumentError` (message contains `"What:"`, `"Where:"`, `"Why:"`, `"Next:"`) for: missing `:net_pay`; `:earnings` as `[]` (must be non-empty); a `Float` in an earnings line's `:amount`; a `Float` in a deductions line's `:ytd`; a non-`%Date{}` `:pay_date`; `:employer` missing `:name`.
    - `validate_data!` does NOT raise for a well-formed minimal payload: required top-level keys present, one earnings line, `:deductions` as `[]` (empty deductions list is valid — D-15 only requires `:earnings` non-empty).
    - `fixture_data()`'s masked identifier fields (`employee.id`, `payment_method` when present) each contain the masking token `"·"` (U+00B7 middot, e.g. `"E-·····4821"` / `"···· 4321"` style) via `String.contains?/2`, AND each masked field `refute`s a match against an unmasked full-length numeric-id pattern -- `Regex.match?(~r/^\d{9}$/, field)` (SSN-style, 9 consecutive digits) and `Regex.match?(~r/^\d{6,}$/, field)` (generic bank/NI-style, 6+ consecutive digits with no masking separator) both return `false` (D-14 PII masking, FAM-01 -- test-enforced, not prose-only).
  </behavior>
  <action>
    Create `lib/rendro/recipes/payslip.ex`, `@moduledoc tags: [:adapter]`. Module doc mirrors Invoice's 3-rung-pattern shape and documents the D-15 data contract exactly: `%{employer: %{name (req), address}, employee: %{name (req), id, tax_code}, period: %{from, to} (req, Date.t()), pay_date (req, Date.t()), earnings: [%{description, amount, ytd}] (req, non-empty list), deductions: [%{description, amount, ytd}] (req key, list -- MAY be empty), net_pay: Decimal (req), totals: %{gross, deductions, net, gross_ytd, deductions_ytd, net_ytd} (optional caller assertions), payment_method (optional, masked)}`. Per-line `:amount` is required Decimal; per-line `:ytd` is optional Decimal (both reject Float instructively when present).

    `page_template(opts \\ [])`: derive `{pw, ph}` via `Rendro.PageSize.resolve(page_size, :portrait)` (default `page_size: :a4` — Payslip is portrait, unlike Certificate's landscape default) plus `ml/mr/mt/mb` margin opts (default 72, mirror `certificate.ex:88-100`). Build region geometry top-down: `header_h` ~64pt (identity: employer/employee/period/pay_date is denser than Invoice's id/date pair), `summary_h` ~54pt (the net-pay band), `footer_h` ~24pt, with `:body` consuming the remainder down to the bottom margin. Manually compute each `:top`/`:bottom` region's `y` the way Invoice computes `@body_y`/`@footer_y` (region `y` is NOT auto-stacked by the engine for non-body regions — every recipe that isn't purely `:flow` derives `y` itself). Apply the SAME `Keyword.take` whitelist as `invoice.ex:119-131` so `:palette`/`:labels`/`:formatters` never reach `struct!/2`.

    Region list for `page_template/1` (four FUNCTIONAL zones map to a small implementation detail resolved in Task 2 — see that task's `<action>` for why the physical region count may be 5, not 4): `:header` (`role: :header`, `anchor: :top`), `:summary` (`role: :custom`, `anchor: :top`, directly below `:header`), `:body` (`role: :body`, `anchor: :flow`), `:footer` (`role: :footer`, `anchor: :bottom`). Do not implement the `:summary` band's fill/rule visual yet — Task 2 owns that; this task only needs the region's geometry (`x`/`y`/`width`/`height`) to exist and be correct.

    `defp palette(opts)` — copy verbatim from `invoice.ex:371-386` (identical 7-role map and defaults).

    `@default_labels` module attribute (D-18), exactly these 13 keys with jurisdiction-neutral English defaults: `earnings: "Earnings"`, `deductions: "Deductions"`, `description: "Description"`, `amount: "Current"`, `ytd_amount: "YTD"`, `gross_pay: "Gross Pay"`, `total_deductions: "Total Deductions"`, `net_pay: "NET PAY"`, `year_to_date: "Year to Date"`, `pay_period: "Pay Period"`, `pay_date: "Pay Date"`, `employer: "Employer"`, `employee: "Employee"`. EVERY label key referenced anywhere in Payslip's section builders (this task and Tasks 2-3) MUST be a key here — per 116-01's verified gotcha, `Rendro.Format.label/1` has no fallback clause and raises `FunctionClauseError` for an unrecognized atom.

    `validate_data!(data)`: non-map dispatch clause first (four-part `ArgumentError`, mirror `invoice.ex:392-401`). Then, in a private pipeline mirroring `invoice.ex:403-413`'s shape: required top-level keys `[:employer, :employee, :period, :pay_date, :earnings, :deductions, :net_pay]` (missing-key check mirrors `invoice.ex:415-431`); `:employer`/`:employee` must be maps with a required binary `:name`; `:period` must be a map with `:from`/`:to` as `%Date{}`; `:pay_date` must be `%Date{}`; `:earnings` must be a non-empty list of maps, each with binary `:description`, `%Decimal{}` `:amount` (reject Float instructively, mirror `invoice.ex:608-618`'s message shape), and optional `%Decimal{}` `:ytd` (same Float rejection when present); `:deductions` must be a list (may be `[]`) of the same per-line shape. `:net_pay` must be `%Decimal{}` (reject Float). Do NOT implement the D-13 `Decimal.equal?/2` gross/deductions/net_pay reconciliation assertion in this task — Task 3 owns both halves of D-13 (validate + render) together, since reviewing them apart from the reconciliation block they protect is harder to reason about correctly.

    Create `test/rendro/recipes/payslip_test.exs` with a `defp fixture_data(opts \\ [])` helper mirroring `statement_test.exs`/`certificate_test.exs` (fictional-only: employer "Aurora Textiles Co.", employee "Jordan Rivera", masked employee id like `"E-·····4821"`, masked bank-style `payment_method` if included — PII masking is mandatory per D-14, this fixture is the ONLY payslip data this phase ships, matching RESEARCH's Open Question 1 recommendation to keep fixtures test-local rather than adding to schema-mismatched `priv/examples/`). Add a dedicated `test "PII masking is test-enforced (D-14, FAM-01)"` in this file asserting both masked fields contain the `"·"` masking token and `refute` a match against `~r/^\d{9}$/` and `~r/^\d{6,}$/` (see `<behavior>` above) — this makes PII masking proof-backed, not prose-only.
  </action>
  <acceptance_criteria>
    All six `<behavior>` cases pass. Additionally: `Payslip.page_template/1` compiles and returns a `%Rendro.PageTemplate{}`; `@default_labels` has all 13 keys listed above (assert via a test that iterates the expected key list and confirms `Map.has_key?/2` for each); the D-14 PII-masking assertion passes -- `fixture_data()`'s `employee.id` and `payment_method` (when present) each contain the masking token `"·"` and each `refute`s a match against `~r/^\d{9}$/` and `~r/^\d{6,}$/` (FAM-01, D-14 -- test-enforced, not prose-only).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_test.exs</automated>
  </verify>
  <done>lib/rendro/recipes/payslip.ex exists with page_template/1 (geometry-derived), palette/1 (S1, verbatim), @default_labels (13 keys), and validate_data!/1 (shape/type checks only, no reconciliation assert yet); compiles clean; all Task 1 tests pass, including the D-14 PII-masking regex assertion (FAM-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sections/2, document/2, header/summary/footer content, and the net-pay visual anchor (D-11)</name>
  <files>lib/rendro/recipes/payslip.ex, test/rendro/recipes/payslip_test.exs</files>
  <read_first>
    lib/rendro/pipeline/paginate.ex (`anchor_region_blocks/3` at lines 1092-1109 — VERIFIED engine mechanic: within any non-body region, blocks stack sequentially; each block's `y` is unconditionally overwritten with a running cursor (`Map.put(:y, current_y)`), and the cursor advances by `next_y = current_y + (block.height || 0)`. A block declared with an EXPLICIT `height: 0` does not advance the cursor, so the NEXT block in that region's content list starts at the SAME y -- this is the only mechanism for layering multiple visual elements within one region, since a block's own `y` field is otherwise always ignored. `block.x` IS respected as an offset from the region's left edge (`Map.put(:x, start_x + block.x)`), so horizontal placement within a region is directly controllable)
    lib/rendro/path.ex (full file already read this session — `Rendro.Path` op union is `{:move, x, y}` / `{:line, x, y}` / `{:curve, ...}` / `{:rect, x, y, w, h}` / `{:rounded_rect, x, y, w, h, radius}` / `:close`; `fill`/`stroke` accept a bare `{r,g,b}` or a map with `:color`/`:width`/`:dash`/`:cap`/`:join`)
    lib/rendro.ex (`Rendro.path/2` at line 243 — the ergonomic helper that returns a `%Rendro.Block{}` wrapping a `%Rendro.Path{}`; splits `attrs` into block-level keys (`:x,:y,:width,:height,:keep_together,:keep_with_next,:break_before,:break_after`) vs path-level keys (`:fill,:stroke`) — prefer this over hand-building `%Rendro.Block{content: %Rendro.Path{...}}}`)
    lib/rendro/recipes/statement.ex (label_resolver/formatter call idiom at lines 270-274 — `Pagination.formatter(opts, :amount, &Rendro.Format.money/1)`, adapt `Pagination.label_resolver(opts)` to `Pagination.label_resolver(opts, @default_labels)` per D-18/116-01)
  </read_first>
  <behavior>
    - `Payslip.sections(fixture_data())` returns a list of `%Rendro.Section{}` structs whose regions include `:header` and `:footer` (body/summary content covered by later assertions).
    - `Payslip.document(fixture_data())` returns a `%Rendro.Document{}`; `Rendro.render(doc)` returns `{:ok, pdf}` starting with `"%PDF-"`.
    - The maximum `size:` among every `%Rendro.Text{}` block anywhere in `Payslip.sections(fixture_data())`'s content occurs within the net-pay anchor's content (not the header, not the footer) -- this is the automatable stand-in for "net pay is the single largest element on the page" (D-11, rubric hierarchy=5). Assert this by recursively collecting `%Rendro.Text{size: s}` structs per section/region and confirming the global max is attributable to the summary/anchor content.
    - `Payslip.document(fixture_data(), labels: %{net_pay: "TAKE HOME"})` renders without raising, and the rendered PDF text stream contains `"TAKE HOME"` (opts labels override works end to end).
  </behavior>
  <action>
    Implement `sections(data, opts \\ [])`: call `validate_data!(data)`, then `Rendro.Recipes.Pagination.validate_labels!(opts, "Rendro.Recipes.Payslip.document/2")` and `Rendro.Recipes.Pagination.validate_formatters!(opts, "Rendro.Recipes.Payslip.document/2")` (from 116-01) BEFORE building any section content — mirrors how Invoice validates data up front, extended per D-19. Return `[header_section(data, opts), summary_section(data, opts), body_section(data, opts), footer_section(data, opts)]` (`body_section/2` is a stub returning an empty-content `Rendro.section(name: :payslip_body, region: :body, content: [])` in THIS task — Task 3 replaces it with the real ledger + reconciliation).

    Implement `document(data, opts \\ [])` mirroring `invoice.ex:172-186` exactly (validate, build template, build sections, `Rendro.Document.new() |> add_template |> set_template |> Enum.reduce(secs, ..., &add_section/2)`).

    `header_section/2`: employer name/address, employee name/id (masked)/tax_code, `lbl.(:pay_period)` + formatted `period.from`-`period.to`, `lbl.(:pay_date)` + formatted `pay_date` — all colors from `palette(opts).ink`/`.muted`, all via `fmt_date = Pagination.formatter(opts, :date, &Rendro.Format.date/1)` and `lbl = Pagination.label_resolver(opts, @default_labels)`.

    `summary_section/2` — the D-11 net-pay anchor. Per the VERIFIED engine mechanic above, achieve the "tinted band behind the label+value text" effect with a SINGLE region's content list ordered as: (1) a backdrop `Rendro.path/2` block — `[{:rect, 0, 0, band_w, band_h}]`, `fill: colors.surface`, `stroke: %{color: colors.rule, width: 0.75}` (hairline top rule per D-11's grayscale-safe requirement) — given `width: band_w, height: 0` (the block's OWN stacking height is explicitly 0 so it does not push subsequent blocks down; the drawn rectangle's real height comes entirely from the `{:rect,...}` op's own `h` argument, which IS `band_h`, independent of the wrapper block's stacking height); (2) the `"NET PAY"` label block, `Rendro.text(lbl.(:net_pay), size: 10, color: colors.muted)` — small/caps-style per D-11; (3) the value block, `Rendro.text(fmt_amount.(data.net_pay), size: 27, color: colors.ink)` (26-28pt range per D-11 guidance — this size MUST be strictly greater than any other text size in the document, including the header's largest text, so pick a value comfortably above whatever size you chose for header/body text). Confirm this composition renders correctly (band visible behind both text blocks, not pushing them down) via a quick manual check before moving on — this specific 3-block-with-zero-height-backdrop composition has no direct precedent elsewhere in the codebase, so treat the first render as a spike: if the backdrop visually fails to appear behind the text (e.g. renders as a separate stacked block instead), the documented fallback is two coincident regions (`:summary` and a decorative `:summary_band`) with the DECORATIVE region listed BEFORE `:summary` in `page_template/1`'s `regions:` list — `lib/rendro/pipeline/paginate.ex:909-921`'s `apply_page_template/5` proves non-body region paint order follows `template.regions` list order (`Enum.reject(&(&1.name == :body)) |> Enum.flat_map(...)`, then `anchored_blocks ++ page.blocks`), so an EARLIER-listed region always paints UNDER a LATER-listed one.

    `footer_section/2`: masked `payment_method` (when present) + `Rendro.page_number/1`, colors from `palette(opts)`.
  </action>
  <acceptance_criteria>All four `<behavior>` cases pass. `mix test test/rendro/recipes/payslip_test.exs` is green (no `content_overflow`, no unresolved `{{...}}` tokens in the rendered PDF).</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_test.exs</automated>
  </verify>
  <done>Payslip.document/2 renders {:ok, pdf}; the net-pay band is the single largest text element on the page (test-asserted, not eyeballed); :labels opts override renders end to end; body_section is still a stub (Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Combined ledger table, pagination, D-13 reconciliation (validate + render), byte-identity</name>
  <files>lib/rendro/recipes/payslip.ex, test/rendro/recipes/payslip_test.exs, test/rendro/recipes/payslip_byte_identity_test.exs</files>
  <read_first>
    lib/rendro/recipes/invoice.ex (`body_section/2` at lines 218-273 — `Rendro.measure_rows/4`, `Pagination.chunk_rows_into_pages/2`, `break_before: idx > 0` per-page table blocks, totals appended after the LAST table block only; `totals_reserved_height/1` at lines 277-286 (reserve-on-every-page-so-the-last-page-has-room idiom); `maybe_validate_totals!/1` at lines 640-687 (`Decimal.equal?/2`, NEVER `==`) — retarget this exact idiom for gross/deductions/net_pay)
    lib/rendro.ex (`cell_align` validated at line 475 — accepts a map of `0-based column index => :right`; `def table/2` at line 325; `@valid_border_atoms` includes `:columns` for the mid vertical rule between the Earnings and Deductions column-groups)
    lib/rendro/cell.ex, lib/rendro/row.ex (a table `row()` is `[Rendro.Block.t() | String.t()] | Rendro.Row.t()` — for the bold-emphasis subtotal row, wrap each of the 6 cell values in `Rendro.block(Rendro.text(str, size: ..., color: colors.ink))` rather than plain strings; Rendro ships no font-weight/bold attribute, so emphasis is approximated via color/size, sourced from palette(opts), never inlined)
    test/rendro/recipes/invoice_byte_identity_test.exs (byte-identity test shape to mirror — two `deterministic: true` renders equal, plus a frozen sha256 golden computed by actually running the render, never hand-typed)
  </read_first>
  <behavior>
    - `Payslip.document(fixture_data())` with 1 earnings + 1 deductions line renders `{:ok, pdf}` and the PDF text stream contains both line `:description` strings and their right-aligned formatted amounts.
    - `Payslip.document(fixture_data(earnings: <30 lines>))` (enough rows to overflow one A4 page's body capacity) renders `{:ok, pdf}` with 2+ pages, each ledger page repeats the 6-column header, and the reconciliation block appears ONLY on the last page.
    - `Payslip.document(fixture_data(net_pay: Decimal.new("1.00")))` (deliberately wrong, does not equal gross-deductions) raises an `ArgumentError` naming the mismatch (contains `"net_pay"`, the supplied value, and the derived value) -- never a silent wrong render.
    - `Payslip.document(fixture_data(earnings: [%{description: "Base", amount: 100.0}]))` (Float, not Decimal) raises an instructive `ArgumentError` naming the offending field -- never `ArithmeticError`.
    - Two `deterministic: true` renders of the same fixture produce byte-identical PDF output; a frozen sha256 golden (computed by actually running the render via `mix run`, not hand-typed) matches on a fresh render.
    - `Payslip.document(fixture_data(earnings: [%{description: "Impôt sur le revenu (PAYE)", amount: Decimal.new("100.00")}, %{description: "Xyzzy Plugh Quux Nonstandard Label", amount: Decimal.new("50.00")}]))` renders `{:ok, pdf}` and the PDF text stream contains BOTH literal `:description` strings unchanged (byte-for-byte, including the non-ASCII "Impôt sur le revenu" string) -- proving no jurisdiction-keyword allowlist/rejection exists in `validate_data!/1` or `body_section/2` (D-17: statutory line content is caller `:description` data, never a library-enumerated type).
  </behavior>
  <action>
    Replace the Task-2 `body_section/2` stub with the real D-12 combined ledger: zip `data.earnings`/`data.deductions` to equal length (blank-pad the shorter list with `%{description: "", amount: nil, ytd: nil}`), formatted rows `[earn.description, fmt_amount_or_blank(earn.amount), fmt_amount_or_blank(earn.ytd), ded.description, fmt_amount_or_blank(ded.amount), fmt_amount_or_blank(ded.ytd)]` (blank amount/ytd render as `""`, never `"nil"` or a crash). `table_opts = [header: [lbl.(:earnings), lbl.(:amount), lbl.(:ytd_amount), lbl.(:deductions), lbl.(:amount), lbl.(:ytd_amount)], columns: <6 columns, e.g. 2x {:share,2}+{:fixed,X} pattern>, borders: :columns, cell_align: %{1 => :right, 2 => :right, 4 => :right, 5 => :right}]`. Measure via `Rendro.measure_rows/4` exactly like `invoice.ex:236-237`, chunk via `Rendro.Recipes.Pagination.chunk_rows_into_pages/2` with `effective_capacity = capacity - header_h - reconciliation_reserved_height(data) - @row_epsilon` (mirror `invoice.ex:246`'s `@totals_line_height`-per-page reservation idiom, renamed for Payslip's reconciliation block), one table block per page with `break_before: idx > 0`.

    Append a bold-emphasis subtotal row as the table's LAST data row (not a separate block) reading "Gross Pay" / gross total / "" / "Total Deductions" / deductions total / "" — cells wrapped in `Rendro.block(Rendro.text(str, size: <body_size+1 or 2>, color: colors.ink))` per the emphasis convention noted in `<read_first>`.

    Implement the D-13 reconciliation, BOTH halves together in this task:
    - Validate half (extends `validate_data!/1` from Task 1): `defp validate_reconciliation!(%{earnings: earnings, deductions: deductions, net_pay: net_pay})` — fold `gross = Enum.reduce(earnings, Decimal.new(0), fn %{amount: a}, acc -> Decimal.add(acc, a) end)`, `total_deductions` the same fold over `deductions`, `expected_net = Decimal.sub(gross, total_deductions)`, `unless Decimal.equal?(net_pay, expected_net) do raise ArgumentError, <four-part message naming supplied vs derived net_pay> end` — copy the exact idiom from `invoice.ex:640-687`, NEVER `==`. If `data.totals` is supplied, additionally assert each of `totals.gross`/`totals.deductions`/`totals.net`/`totals.gross_ytd`/`totals.deductions_ytd`/`totals.net_ytd` (whichever are present) against the corresponding derived value via `Decimal.equal?/2`, mirroring `invoice.ex`'s optional-totals-assertion pattern (each check independently gated on `Map.has_key?/2`, so callers can supply a subset).
    - Render half: a trailing block (or blocks) rendering `"#{lbl.(:gross_pay)} #{fmt.(gross)} − #{lbl.(:total_deductions)} #{fmt.(total_deductions)} = #{lbl.(:net_pay)} #{fmt.(net_pay)}"` plus, when `data.totals` includes YTD fields, a compact `"#{lbl.(:gross_pay)} #{lbl.(:year_to_date)}: ... | ..."` trio line. Append this block ONLY after the LAST ledger table block (never wrapped in `keep_together` — mirror Invoice's explicit anti-pattern warning).

    Create `test/rendro/recipes/payslip_byte_identity_test.exs` mirroring `invoice_byte_identity_test.exs` exactly: a fixed, deterministic minimal `fixture_data()` (no anatomy variance), two `deterministic: true` renders asserted equal, and a `@toy_golden_sha256` computed by actually running the render (via `mix run` or an ad-hoc `iex -S mix` session), never hand-typed.

    Add a dedicated `test "arbitrary non-English/unrelated :description round-trips unchanged (D-17)"` in `payslip_test.exs` asserting the rendered PDF text stream contains both literal description strings verbatim (see the last `<behavior>` case above), with no jurisdiction-keyword filtering anywhere in `validate_data!/1`/`body_section/2` -- this proves the "engine never learns a jurisdiction" boundary is code-enforced, not just documented.
  </action>
  <acceptance_criteria>All six `<behavior>` cases pass, including the D-17 negative test (arbitrary non-English/unrelated `:description` round-trips unchanged into the rendered PDF, no jurisdiction-keyword allowlist rejects it). Source-assertion (region-scoped, NOT a blanket `{0, 0, 0}` grep -- `palette/1`'s own default map legitimately contains four such literals by design, copied verbatim from Invoice): every OTHER function in the file passes a literal color tuple only via a `colors.<role>` variable reference, never via the engine's `color:`/`fill:` option keys directly -- `grep -E -c 'color: \{[0-9]|fill: \{[0-9]' lib/rendro/recipes/payslip.ex` (excluding comment lines via `grep -v '^\s*#'` first) returns `0` outside `palette/1`'s own map (whose keys are `ink:`/`muted:`/`accent:`/`on_accent:`/`background:`/`surface:`/`rule:`, never the literal token `color:`/`fill:`, so this pattern cannot false-positive against it).</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_test.exs test/rendro/recipes/payslip_byte_identity_test.exs</automated>
  </verify>
  <done>Combined ledger paginates natively; reconciliation is validated via Decimal.equal?/2 (never ==) and rendered kept-with-last; Float rejection works; byte-identity holds across two renders and matches a frozen golden; D-17's caller-`:description` passthrough is test-enforced (arbitrary non-English/unrelated strings round-trip unchanged, no jurisdiction-keyword allowlist).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recipe caller → `data` map | Any Elixir caller of `Payslip.document/2`/`sections/2` supplies payroll data — untrusted shape/type/arithmetic-consistency until `validate_data!/1` runs |
| Fixture author → committed test fixture | The single test-local Payslip fixture this phase ships is the ONLY payslip example data in the repo; it sets the PII-safety precedent for every future payslip fixture (Phase 118) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-02-01 | Tampering | `earnings`/`deductions`/`net_pay`/`totals` money fields | high | mitigate | Every money field is type-checked Decimal-only; a Float raises an instructive `ArgumentError` before any `Decimal.add/sub` arithmetic runs (mirrors `invoice.ex:608-618`) — Float arithmetic on payroll data is a financial-correctness bug class, not a cosmetic one |
| T-116-02-02 | Tampering / Repudiation | `net_pay` vs derived gross-deductions | high | mitigate | `Decimal.equal?/2` caller assertion (never `==`) in `validate_data!/1` raises an instructive `ArgumentError` naming both the supplied and derived value — a mismatched net_pay can never silently render as the visual anchor |
| T-116-02-03 | Information Disclosure | Committed test fixture (employer/employee identity, ids, payment method) | medium | mitigate | D-14's masking convention is enforced by construction in the fixture helper (fictional names, masked ids like `E-·····4821`) AND proof-backed by a dedicated ExUnit test (Task 1, "PII masking is test-enforced (D-14, FAM-01)") asserting the masking token `"·"` is present in each identifier field and `refute`ing a match against `~r/^\d{9}$/` (SSN-style) and `~r/^\d{6,}$/` (bank/NI-style) unmasked full-length numeric-id patterns -- not prose-only |
| T-116-02-04 | DoS | Pathological un-splittable ledger content (e.g. a single row taller than the entire body capacity) | low | accept | Already mitigated at the engine level — `Pagination.chunk_rows_into_pages/2` + the core pipeline's `validate_region_fit!`/`validate_page_fit!` surface this as the typed `:content_overflow` `Rendro.Error`, never a crash or silent truncation; no recipe-level work needed beyond not swallowing the error |

</threat_model>

<verification>
`mix test test/rendro/recipes/payslip_test.exs test/rendro/recipes/payslip_byte_identity_test.exs` after every task. Full suite (`mix test`) before this plan's SUMMARY is written, to catch any regression in `statement_test.exs` from the 116-01 dependency.
</verification>

<success_criteria>
- `Rendro.Recipes.Payslip.document/2` renders a production-grade payslip: net-pay anchor visually dominant (size-asserted), combined ledger with native pagination, kept-with-last reconciliation, all colors via `palette(opts)`.
- `validate_data!/1` is fully errors-as-product: every required-field, type, and reconciliation violation raises an instructive four-part `ArgumentError`, never a leaked `BadMapError`/`ArithmeticError`.
- Byte-identity holds across two deterministic renders of the same fixture.
- PII masking (D-14, FAM-01) and jurisdiction-neutral `:description` passthrough (D-17) are test-enforced via concrete ExUnit assertions (regex/token match on masked fields; verbatim round-trip of arbitrary non-English/unrelated description strings) -- never prose-only or eyeballed.
</success_criteria>

<output>
Create `.planning/phases/116-new-families-payslip-ticket/116-02-SUMMARY.md` when done
</output>
