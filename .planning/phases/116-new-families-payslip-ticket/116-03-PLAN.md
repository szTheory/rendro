---
phase: 116-new-families-payslip-ticket
plan: 03
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - lib/rendro/recipes/ticket.ex
  - test/rendro/recipes/ticket_test.exs
  - test/rendro/recipes/ticket_byte_identity_test.exs
autonomous: true
requirements: [FAM-02, FAM-03]
must_haves:
  truths:
    - "Rendro.Recipes.Ticket.document/2 (3-rung pattern) renders {:ok, pdf} for a well-formed fictional event-ticket fixture (Aurora Live), with zero hardcoded A4 numerics -- all geometry derived from PageSize.resolve/2 (D-03)"
    - "The anchor is an ordered 1-4 cell placement grid whose values render in the largest type on the page (D-02); the SAME recipe code renders a boarding-pass shape (Gate/Seat/Group) given different data+labels, with no archetype branching in lib/ (D-01)"
    - "A bordered code box always renders in the stub region, >= ~100x100pt (D-05); the human-readable code.reference ALWAYS renders, even when a PNG is supplied (D-06); a caller-supplied PNG renders fit-contain/centered under the fixed logical name :ticket_code, never stretched (D-08); with no PNG, the box shows the centered reference and NEVER a faux barcode/QR stripe pattern (D-07)"
    - "A dashed perforation line separates :main from :stub (D-09)"
    - "Bad/corrupt/non-image bytes in data.code.image raise an instructive ArgumentError naming data.code.image -- Rendro.AssetRegistry.InvalidAssetError never leaks (D-10); an oversized-but-valid image raises no error (fit-contain scales down deterministically)"
    - "Content that cannot fit a fixed region (:main/:stub/:terms) after validate_data!/1's byte guards surfaces as the pipeline's typed :content_overflow Rendro.Error -- never silent truncation, never a raw crash (D-04)"
    - "data.code.image = nil renders byte-identical to omitting :image entirely (D-08)"
    - "Every color read in Ticket's section builders resolves a role from palette(opts) (S1) -- no inlined {0,0,0} or raw {r,g,b} literal anywhere in the file"
  artifacts:
    - lib/rendro/recipes/ticket.ex
    - test/rendro/recipes/ticket_test.exs
    - test/rendro/recipes/ticket_byte_identity_test.exs
  key_links:
    - "Ticket.document/2 -> page_template/1 (band derived as a ratio of content width, :main/:stub/:terms regions from PageSize.resolve/2) -> sections/2 -> validate_data!/1 pre-validates data.code.image via the pure Rendro.ImageParser.parse/1 BEFORE document/2 ever calls Rendro.Document.register_image/3 under the fixed logical name :ticket_code"
    - "Ticket.sections/2 calls Rendro.Recipes.Pagination.validate_labels!/2 and validate_formatters!/2 (from 116-01) before building any section content"
---

<objective>
Build `Rendro.Recipes.Ticket` end to end: geometry-derived fixed landscape band (D-03), the D-02 placement-grid anchor, the D-05/D-06/D-07/D-08/D-09 stub composition (bordered code box, always-on human-readable reference, optional caller-supplied PNG, dashed perforation), and D-10's genuinely-new caller-image pre-validation plumbing (no prior art in this codebase — the first recipe to register a caller-supplied, not library-shipped, image asset).

Purpose: Deliver FAM-02 — a single, archetype-agnostic Ticket family (event admission by default, boarding-pass/transit reachable purely via data+labels) whose "this is a ticket" signal comes from information design, never a fake scannable code.
Output: `lib/rendro/recipes/ticket.ex` (adapter tier), `test/rendro/recipes/ticket_test.exs`, `test/rendro/recipes/ticket_byte_identity_test.exs`.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `Rendro.Recipes.Ticket` — new module, `@moduledoc tags: [:adapter]`
- `Rendro.Recipes.Ticket.page_template/1` — public, geometry-derived fixed band (D-03), regions `:main`/`:stub`/`:terms`
- `Rendro.Recipes.Ticket.sections/2` — public
- `Rendro.Recipes.Ticket.document/2` — public
- `Rendro.Recipes.Ticket.@default_labels` — module attribute, 7 label keys (`:admit`, `:seat`, `:gate`, `:section`, `:row`, `:reference`, `:present_code`, D-18)
- The `:ticket_code` logical image name — fixed, recipe-owned `Rendro.AssetRegistry` key; callers never choose their own name (D-08)
- Private: `palette/1` (S1, verbatim from Invoice), `validate_data!/1` (D-04 byte guards + D-10 image pre-validation), `main_section/2` (D-02 placement grid), `stub_section/2` (D-05/D-06/D-07/D-08/D-09), `terms_section/2`

Registered in `priv/public_api.json` (adapter tier) and `priv/support_matrix.json` (surface `ticket`) by 116-04, not this plan.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-new-families-payslip-ticket/116-CONTEXT.md
@.planning/phases/116-new-families-payslip-ticket/116-RESEARCH.md
@.planning/phases/116-new-families-payslip-ticket/116-PATTERNS.md
@.planning/phases/116-new-families-payslip-ticket/116-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Geometry, palette, @default_labels, and validate_data!/1 (byte guards + D-10 image pre-validation)</name>
  <files>lib/rendro/recipes/ticket.ex, test/rendro/recipes/ticket_test.exs</files>
  <read_first>
    lib/rendro/recipes/certificate.ex (`page_template/1` at lines 88-145 — `PageSize.resolve/2`-derived geometry, `ml/mr/mt/mb` pattern; `validate_body!/1` byte-guard idiom at lines 460-483 — clone this shape for Ticket's multiple free-text fields; `validate_data!/1` dispatch shape at lines 420-445)
    lib/rendro/region.ex (`role :: :header | :body | :footer | :sidebar | :custom` — a closed 5-atom union; Ticket's `:main`/`:stub` regions must use `role: :custom` exactly like `certificate.ex:121`'s `:frame` region; `:terms` may use `role: :body` since it is genuinely flowing fine-print text)
    lib/rendro/page_size.ex (already read this session — `resolve/2`)
    lib/rendro/image_parser.ex (full file already read this session — pure `parse/1`, returns `{:ok, %{width:,height:,mime:}}` or `{:error, :unsupported_image_format}`)
    lib/rendro/asset_registry.ex (`register_image/3` at lines 37-63 — its EXACT source-resolution logic to mirror in `validate_data!/1`: `case source do {:binary, bytes} when is_binary(bytes) -> bytes; {:path, path} when is_binary(path) -> File.read!(path) end`, then `Rendro.ImageParser.parse/1` on the resulting binary. `File.read!/1` itself can raise on a missing/unreadable path — fold THAT failure into the same instructive `ArgumentError` too, since a bad path is just as much a caller data error as malformed bytes)
    lib/rendro/recipes/pagination.ex (from 116-01: `label_resolver/2`, `validate_labels!/2`, `validate_formatters!/2`, `type_name/1`)
  </read_first>
  <behavior>
    - `Ticket.page_template/1` (no opts) returns a `%Rendro.PageTemplate{}` whose regions include `:main`, `:stub`, and `:terms`, all with `role: :custom` or `role: :body` (never `:header`/`:footer`/`:sidebar`), and `:main`/`:stub` are `anchor: :fixed`.
    - `Ticket.page_template(page_size: :us_letter)` returns different `width`/`height` than the `:a4` default (geometry is `PageSize.resolve/2`-derived).
    - `validate_data!` raises an instructive `ArgumentError` for: missing `:title`; `:placement` as `[]` (must be 1-4 entries) or as a 5-entry list (over the D-02 cap); a `:placement` entry missing `:label` or `:value`; missing/blank `code.reference`; a `title` exceeding its byte guard; `data.code.image = {:binary, "not a real image"}` (raises naming `data.code.image`, message does NOT contain `"InvalidAssetError"`).
    - `validate_data!` does NOT raise for: a well-formed minimal payload with `code.image: nil`; the same payload with `code.image` omitted entirely (both are valid — nil and absent are equivalent, per D-08's byte-identity requirement carried into Task 3's render behavior).
    - `validate_data!` does NOT raise for a valid PNG binary in `data.code.image` (construct a minimal valid 1x1 PNG byte sequence for the test, matching `image_parser.ex`'s PNG signature branch).
  </behavior>
  <action>
    Create `lib/rendro/recipes/ticket.ex`, `@moduledoc tags: [:adapter]`. Module doc documents the archetype-agnostic data contract (D-01/D-02/D-06): `%{issuer: %{name (req), venue}, title: String.t() (req), subtitle: String.t()|nil, placement: [%{label, value}] (req, 1-4 entries, ordered), code: %{reference (req, non-blank), label (optional, default via labels), image: {:path, Path.t()} | {:binary, binary()} | nil (optional)}, terms: String.t()|nil}`. Document explicitly that the SAME shape renders an event ticket (`placement: [%{label:"Section",value:"GA"},%{label:"Row",value:"H"},%{label:"Seat",value:"24"}]`) or a boarding pass (`placement: [%{label:"Gate",value:"B12"},%{label:"Seat",value:"14C"},%{label:"Group",value:"2"}]`) purely via data — zero branching in this file (D-01).

    `page_template(opts \\ [])`: derive `{pw, ph}` via `Rendro.PageSize.resolve(page_size, :portrait)` (default `:a4`) plus `ml/mr/mt/mb` margin opts (default 72, mirror `certificate.ex:88-100`). Per D-03: `content_w = pw - ml - mr`; `band_w = content_w`; `band_h = band_w / 2.4` (dimensionless ratio, NOT a fixed-point constant — this is the load-bearing reason A4 and US Letter both fall out correctly with zero hardcoded numerics); `stub_split = band_w * 0.68` (the x-offset, relative to the band's own left edge, where `:main` ends and `:stub` begins). Regions: `:main` (`role: :custom`, `anchor: :fixed`, `x: ml, y: mt, width: stub_split, height: band_h`), `:stub` (`role: :custom`, `anchor: :fixed`, `x: ml + stub_split, y: mt, width: band_w - stub_split, height: band_h`), `:terms` (`role: :body`, `anchor: :flow`, `x: ml, y: mt + band_h + <gap, e.g. 16>, width: content_w, height: <remaining space down to bottom margin>`). Apply the SAME `Keyword.take` whitelist as `invoice.ex:119-131`.

    `defp palette(opts)` — copy verbatim from `invoice.ex:371-386`.

    `@default_labels` (D-18), exactly these 7 keys: `admit: "Admit One"`, `seat: "Seat"`, `gate: "Gate"`, `section: "Section"`, `row: "Row"`, `reference: "Reference"`, `present_code: "Present this reference at entry."`. `:reference` and `:present_code` are consumed directly by this recipe's own chrome text (Task 3); `:seat`/`:gate`/`:section`/`:row`/`:admit` are shipped so callers building their OWN `:placement` list (or a small optional "ADMIT ONE"-style accent badge, Claude's Discretion per CONTEXT.md) have a consistent, overridable vocabulary — they need not all be referenced inside `ticket.ex` itself to satisfy D-18, but every key `lbl.(:key)` this file DOES call must exist here (same `Rendro.Format.label/1` fallback-crash gotcha as 116-01/116-02).

    `validate_data!(data)`: non-map dispatch first (four-part `ArgumentError`). Then: required top-level keys `[:issuer, :title, :placement, :code]`; `:issuer` a map with required binary `:name`; `:title` a binary, byte-guarded (`byte_size(title) > 200`, mirror `certificate.ex:460-470`'s exact message shape); `:subtitle`/`:terms`, when present, binary and byte-guarded (subtitle ≤200 bytes, terms ≤600 bytes); `:placement` a list of 1 to 4 maps, each with binary `:label` (≤40 bytes) and binary `:value` (≤40 bytes) — reject `[]` (empty) and reject a 5th+ entry, both with instructive messages naming the D-02 1-4 cap; `:code` a map with required non-blank binary `:reference` (≤80 bytes, reject `""`), optional binary `:label`.

    `:code.image` — D-10, the genuinely-new plumbing (no prior-art copy source in this codebase per RESEARCH Finding 6). When present and not `nil`: resolve to raw bytes using the IDENTICAL source-matching logic as `asset_registry.ex:38-42` (`{:binary, bytes}` used directly; `{:path, path}` read via `File.read!/1`), wrapped so ANY failure — a bad path (`File.read!/1` raising) OR malformed bytes (`Rendro.ImageParser.parse/1` returning `{:error, reason}`) — raises the SAME four-part `ArgumentError` naming `data.code.image` in its `Why:` line, with `Next:` suggesting a valid PNG/JPEG path or binary. On successful parse, do nothing further here (this is validation-only; `document/2`, Task 2, will separately call `Rendro.Document.register_image/3`, which re-parses — a small, harmless redundancy, since `ImageParser.parse/1` only inspects header/signature bytes, no full pixel decode). This pre-validation is what guarantees `Rendro.AssetRegistry.InvalidAssetError` never leaks past `validate_data!/1` (D-10's hard requirement).

    Create `test/rendro/recipes/ticket_test.exs` with a `defp fixture_data(opts \\ [])` helper: issuer "Aurora Live" (the milestone's canonical live-events fixture business per CONTEXT.md), title "Indie Night: The Lumen Set", a 3-cell event placement grid (`Section`/`Row`/`Seat`), `code: %{reference: "AUR-88213-GA"}` (no image by default).
  </action>
  <acceptance_criteria>All six `<behavior>` cases pass, plus the two additional no-raise cases (nil image, valid PNG image).</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/ticket_test.exs</automated>
  </verify>
  <done>lib/rendro/recipes/ticket.ex exists with page_template/1 (geometry-derived band, :main/:stub/:terms), palette/1 (S1), @default_labels (7 keys), and validate_data!/1 (byte guards + D-10 image pre-validation); compiles clean; all Task 1 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sections/2, document/2, and the D-02 placement-grid anchor (via Rendro.table, not manual block stacking)</name>
  <files>lib/rendro/recipes/ticket.ex, test/rendro/recipes/ticket_test.exs</files>
  <read_first>
    lib/rendro/recipes/certificate.ex (`document/2` at lines 224-269 — the `base_doc = if brand = Map.get(data, :brand) do ... register_image ... else base_doc end` conditional-registration idiom; adapt for Ticket's `code.image`, registering under the FIXED logical name `:ticket_code`, never a caller-chosen name)
    lib/rendro.ex (`def table/2` at line 325 — `@type row :: [Rendro.Block.t() | String.t()] | Rendro.Row.t()`; a table's `header:` accepts the SAME `row()` type, meaning header cells CAN be `Rendro.Block.t()`-wrapped `Rendro.text/2` with custom size/color, not just plain strings — this is the key insight this task relies on: the D-02 placement grid (small caps label ABOVE large bold value, repeated N times side by side) is a `Rendro.table/2` with ONE data row and Block-styled header cells, NOT a hand-rolled multi-column block-stacking composition. Reusing the table primitive sidesteps a subtlety in how `lib/rendro/pipeline/paginate.ex`'s `anchor_region_blocks/3` assigns a SINGLE shared Y-cursor to every block in a region's content list regardless of each block's own `x` offset — a hand-stacked multi-column layout would require fragile height-zeroing tricks across cells; a table's own column-layout logic already handles this correctly and is tested elsewhere)
  </read_first>
  <behavior>
    - `Ticket.sections(fixture_data())` returns a list of `%Rendro.Section{}` structs including regions `:main` and `:stub` (stub content covered by Task 3; this task's acceptance is on `:main` only).
    - `Ticket.document(fixture_data())` returns a `%Rendro.Document{}`; `Rendro.render(doc)` returns `{:ok, pdf}`.
    - The rendered PDF text stream contains every `placement[].value` string (e.g. `"GA"`, `"H"`, `"24"`) AND every `placement[].label` string (e.g. `"SECTION"`/`"Section"` depending on casing choice), proving the grid actually renders, not just compiles.
    - `Ticket.document(fixture_data(placement: [%{label: "Gate", value: "B12"}, %{label: "Seat", value: "14C"}, %{label: "Group", value: "2"}]))` renders `{:ok, pdf}` — the SAME code path renders a boarding-pass-shaped anchor with zero `lib/` changes (D-01 proof).
    - `Ticket.document(Map.put(fixture_data(), :code, %{reference: "AUR-1", image: {:binary, <valid 1x1 PNG bytes>}}))` renders `{:ok, pdf}` and `doc.asset_registry.assets` (via the returned `%Rendro.Document{}` before render, or asserted through `Rendro.Document.register_image/3`'s effect) has an entry keyed `:ticket_code` — never a caller-chosen name.
  </behavior>
  <action>
    Implement `sections(data, opts \\ [])`: `validate_data!(data)`, then `Rendro.Recipes.Pagination.validate_labels!(opts, "Rendro.Recipes.Ticket.document/2")` and `validate_formatters!(opts, "Rendro.Recipes.Ticket.document/2")` (from 116-01), BEFORE building content. Return `[main_section(data, opts), stub_section(data, opts), terms_section(data, opts)]` (`stub_section/2` and `terms_section/2` are stubs returning empty-content sections in THIS task — Task 3 replaces them).

    Implement `document(data, opts \\ [])` mirroring `certificate.ex:224-269`'s conditional-registration idiom: `base_doc = if image = get_in(data, [:code, :image]) do Rendro.Document.new() |> Rendro.Document.register_image(:ticket_code, image) else Rendro.Document.new() end`, then `add_template |> set_template |> Enum.reduce(secs, ..., &add_section/2)`. The registration call happens ONLY after `validate_data!/1` (called inside `sections/2`, which `document/2` also calls) has already pre-validated the image bytes via D-10 — so by the time `register_image/3` runs, `Rendro.AssetRegistry.InvalidAssetError` should never actually trigger for caller-supplied data that passed validation (register_image re-parses, but the bytes are already known-good).

    Implement `main_section/2` — the D-02 anchor, via `Rendro.table/2`: `header: Enum.map(data.placement, fn %{label: l} -> Rendro.block(Rendro.text(String.upcase(l), size: 8, color: colors.muted)) end)`, single data row `[Enum.map(data.placement, fn %{value: v} -> Rendro.block(Rendro.text(v, size: 22, color: colors.ink)) end)]`, `columns: List.duplicate({:share, 1}, length(data.placement))`, `borders: :none` (purely typographic, no visible grid lines — a bordered spreadsheet-look would contradict the ticket motif). Wrap in `Rendro.block(table)` and place as the `:main` region's content, alongside `data.issuer.name`/`data.title`/`data.subtitle` (smaller supporting text, natural stacking above or below the grid table block — Claude's Discretion on exact order, but the placement-grid VALUES must remain the single largest text anywhere in `:main`'s content, matching D-02's "single dominant element" requirement — verify via the same max-size-assertion technique used in 116-02's net-pay test).
  </action>
  <acceptance_criteria>All five `<behavior>` cases pass. A max-size assertion (mirroring 116-02 Task 2's technique) confirms the placement-grid VALUE cells (22pt) are the largest text anywhere in `Ticket.sections(fixture_data())`'s `:main` content.</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/ticket_test.exs</automated>
  </verify>
  <done>Ticket.document/2 renders {:ok, pdf}; the placement grid renders correctly for BOTH an event-shaped and a boarding-pass-shaped :placement list with zero lib/ branching; caller-supplied images register under the fixed :ticket_code name; stub/terms are still stubs (Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stub composition (code box, reference, perforation, PNG fit-contain), overflow, byte-identity</name>
  <files>lib/rendro/recipes/ticket.ex, test/rendro/recipes/ticket_test.exs, test/rendro/recipes/ticket_byte_identity_test.exs</files>
  <read_first>
    lib/rendro/pipeline/measure.ex (`measure_block/3` Path clause at lines 141-155 — VERIFIED: when a `%Rendro.Path{}` block has BOTH `width` and `height` explicitly set (not `nil`), measure.ex takes the `{w, h} when not is_nil(w) and not is_nil(h) -> {w, h}` branch and respects those values LITERALLY, never recomputing from the ops — this is what makes the zero-height Z-layering technique below deterministic and safe, not a guess. Also note the `%Rendro.Image{}` clause at lines 101-139: `Component.image(name, fit: {w,h})` (no explicit `:width`/`:height`) gets a REAL computed height from the `{nil,nil,{fit_w,fit_h}}` aspect-preserving-contain branch — it is NOT zero-height, so it DOES consume real stacking space after the box backdrop)
    lib/rendro/pipeline/paginate.ex (`anchor_region_blocks/3` at lines 1092-1109 — the same verified mechanic cited in 116-02 Task 2: a block with explicit `height: 0` does not advance the shared per-region Y cursor, so the NEXT block starts at the same y, producing a visual overlay; `block.x` IS respected as an offset from the region's left edge)
    lib/rendro/path.ex (op union: `{:move, x, y}` / `{:line, x, y}` — use these two ops for the perforation's straight dashed line; this RESOLVES RESEARCH.md's open Assumption A2, which speculated `{:move_to}`/`{:line_to}` — the actual names are `:move`/`:line`)
    lib/rendro.ex (`Rendro.path/2` at line 243 — prefer over hand-building `%Rendro.Block{content: %Rendro.Path{...}}}`; `Rendro.Component.image/2` at `component.ex:20`)
    test/rendro/flow_test.exs (line ~124-134 — the exact shape of a `:content_overflow` assertion: `{:error, %Rendro.Error{} = error} = Rendro.render(doc); error.stage == :paginate; error.reason == :content_overflow`)
    test/rendro/recipes/invoice_byte_identity_test.exs (byte-identity test shape — mirror exactly, including a frozen sha256 golden computed by actually running the render, never hand-typed)
  </read_first>
  <behavior>
    - No-image fixture: `Ticket.document(fixture_data())` renders `{:ok, pdf}`; the PDF text stream contains `code.reference` (upper-cased) and the caption `lbl.(:reference)`; it does NOT contain any drawing pattern resembling repeated thin parallel stripes at fixed intervals (the closest automatable proxy for "no faux barcode": assert the stub region's Path ops list contains exactly the perforation line + the one code-box `{:rounded_rect,...}` — no additional `{:rect,...}`/`{:line,...}` sequence of 3+ evenly-spaced parallel strokes).
    - PNG-supplied fixture: renders `{:ok, pdf}`; `code.reference` STILL appears in the text stream (D-06, always-on); the image is registered under `:ticket_code`, fit-contain sized (never equal to a naive stretch-to-box size unless the source PNG's aspect ratio happens to match the box).
    - `code.image: nil` and `code.image` key omitted entirely produce BYTE-IDENTICAL `Rendro.render(doc, deterministic: true)` output (D-08) — same fixture otherwise.
    - A `:placement` value using a long unbroken (no-whitespace) token that fits Task 1's ≤40-byte guard but cannot fit the grid table's narrow per-column share width at 22pt raises `{:error, %Rendro.Error{stage: :paginate, reason: :content_overflow}}` from `Rendro.render/1` — never a crash, never silent truncation.
    - Two `deterministic: true` renders of the same fixture are byte-identical; a frozen sha256 golden (computed by actually running the render) matches a fresh render.
  </behavior>
  <action>
    Replace the Task-2 `stub_section/2` and `terms_section/2` stubs.

    `stub_section/2` — build the region's content list in this exact order, relying on the VERIFIED zero-height overlay mechanic:
    1. Perforation: `Rendro.path([{:move, 0, 0}, {:line, 0, band_h}], stroke: %{color: colors.rule, width: 0.75, dash: [3, 3]}, width: 1, height: 0)` (D-09) — height:0 so it never displaces later content; drawn at the stub's own left edge (x=0 relative to the region = the boundary with `:main`).
    2. Code box backdrop: `Rendro.path([{:rounded_rect, box_x, 0, box_size, box_size, 6.0}], stroke: %{color: colors.rule, width: 1.0}, width: box_x + box_size, height: 0)` (D-05) — `box_size = min(stub_width, band_h) - 2 * <pad, e.g. 12>`; assert in a test (using the A4-default geometry) that this naturally computes to `>= 100`, satisfying D-05's minimum WITHOUT hardcoding it. `height: 0` so the box's OWN drawn rectangle (real height = `box_size`, from the `{:rounded_rect,...}` op's own `h` argument) does not push subsequent content down — the next block(s) overlay it, per the verified mechanic.
    3a. No image (`get_in(data, [:code, :image])` is `nil` or absent): the reference block(s) render starting at the SAME y as the box backdrop (since it was height:0) — a small muted caption `Rendro.text(lbl.(:reference), size: 8, color: colors.muted)` then the large upper-cased reference `Rendro.text(String.upcase(data.code.reference), size: 15, color: colors.ink)`, both `x`-offset to sit roughly within the box's horizontal span (Claude's Discretion on exact centering math — the requirement is "visibly inside the box," not pixel-perfect centering). Optionally append the 1-line caption `lbl.(:present_code)` below (D-07).
    3b. Image supplied: place `Rendro.Component.image(:ticket_code, fit: {box_size, box_size})` (positioned via `Map.put(:x, box_x)` on the returned block, since `Component.image/2` does not accept an `:x` opt) IMMEDIATELY after the box backdrop — its measured height (real, per the `<read_first>` note) naturally advances the cursor past the box, so the ALWAYS-VISIBLE reference (small caption + value, same styling as 3a) renders AFTER it, i.e. below the box rather than overlaid on the image.

    `terms_section/2`: when `data.terms` present, a single muted fine-print text block (`size: 8, color: colors.muted`); when absent, empty content (`[]`) — an empty-content section is harmless (mirrors how Invoice's optional anatomy blocks simply don't append when the data key is absent).

    Create `test/rendro/recipes/ticket_byte_identity_test.exs` mirroring `invoice_byte_identity_test.exs` exactly (fixed fixture, two `deterministic: true` renders equal, a frozen sha256 golden computed by actually running the render). Add a SECOND describe block in the same file (or in `ticket_test.exs`, executor's choice) asserting `code.image: nil` and `code.image` omitted produce byte-identical output (D-08) — this is a DIFFERENT invariant than the standard byte-identity check and must be tested explicitly, not assumed from it.
  </action>
  <acceptance_criteria>All five `<behavior>` cases pass. Source-assertion (region-scoped, NOT a blanket `{0, 0, 0}` grep -- `palette/1`'s own default map legitimately contains four such literals by design, copied verbatim from Invoice): `grep -E -c 'color: \{[0-9]|fill: \{[0-9]' lib/rendro/recipes/ticket.ex` (excluding comment lines via `grep -v '^\s*#'` first) returns `0` -- confirms every color reference outside `palette/1` goes through a `colors.<role>` variable, never the engine's `color:`/`fill:` option keys with a literal tuple.</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/ticket_test.exs test/rendro/recipes/ticket_byte_identity_test.exs</automated>
  </verify>
  <done>Stub region renders a bordered code box with either the always-visible reference or a fit-contain image + reference below it; perforation renders as a dashed line; content_overflow surfaces as a typed error; image:nil is byte-identical to no-image; byte-identity holds across two renders and matches a frozen golden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recipe caller → `data.code.image` bytes | The FIRST caller-supplied (not library-shipped) image asset in any Rendro recipe — a genuinely new trust boundary (RESEARCH Finding 6) |
| Recipe caller → `data` free-text fields (title/subtitle/terms/placement label+value/code.reference) | Untrusted length/content until `validate_data!/1`'s byte guards run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-03-01 | Tampering / DoS | `data.code.image` (malformed/corrupt bytes) | high | mitigate | D-10: pre-validate via the PURE `Rendro.ImageParser.parse/1` inside `validate_data!/1`, before ANY call to `Rendro.Document.register_image/3` — raises an instructive four-part `ArgumentError` naming `data.code.image`; `Rendro.AssetRegistry.InvalidAssetError` (raised by `register_image/3` itself on bad bytes) is never allowed to reach the caller, since validation already rejected bad bytes upstream |
| T-116-03-02 | DoS | Oversized-but-valid image dimensions | low | accept | `ImageParser.parse/1` only inspects signature/header bytes (confirmed: no full pixel-data decode in `image_parser.ex`'s clauses), so a large declared width/height cannot trigger unbounded memory use through parsing; `Component.image(fit: ...)`'s deterministic aspect-preserving contain only ever scales the RENDERED box size, never the source binary — already mitigated at the engine level, no additional recipe work needed |
| T-116-03-03 | Tampering | Path traversal via a caller-supplied `{:path, p}` image source | medium | accept | `Rendro.AssetRegistry.register_image/3` calls `File.read!(path)` with no path sandboxing — this EXTENDS an existing pattern (Certificate/BrandedInvoice already call `File.read!` on a path) to a new trust boundary where the path may be caller-supplied rather than library-fixed. D-08 locks the accepted shapes as `{:path,p}` \| `{:binary,b}` \| `nil` with no explicit sandboxing requirement; Rendro is a library, not a network service — the calling application, not Rendro, owns the trust boundary for what path it passes. Flagged here for awareness; out of scope for this phase per D-08's locked scope; a candidate for a future hardening phase if real-world misuse surfaces |
| T-116-03-04 | Spoofing | No-PNG fallback rendering a fake scannable-looking pattern | medium | mitigate | D-07 explicitly forbids drawing a faux barcode/QR; the no-PNG fallback renders ONLY the bordered box + centered text (caption + reference), never a striped/patterned fill — enforced by this plan's Task 3 acceptance criteria (a source/ops-count assertion that no repeated-stripe pattern is drawn) |
| T-116-03-05 | Tampering / DoS | Unbounded free-text (title/subtitle/terms/placement label+value/code.reference) | medium | mitigate | D-04: `validate_data!/1` byte-guards every free-text field before render (mirrors Certificate's 2000-byte `:body` guard, scaled to each field's much smaller physical space); genuine overflow that survives the guard (e.g. a long unbroken token) surfaces as the pipeline's typed `:content_overflow`, never silent truncation |

</threat_model>

<verification>
`mix test test/rendro/recipes/ticket_test.exs test/rendro/recipes/ticket_byte_identity_test.exs` after every task. Full suite (`mix test`) before this plan's SUMMARY is written.
</verification>

<success_criteria>
- `Rendro.Recipes.Ticket.document/2` renders a production-grade, archetype-agnostic ticket: placement-grid anchor visually dominant, bordered code box always present, human-readable reference always present, dashed perforation, all colors via `palette(opts)`.
- D-10's caller-image pre-validation guarantees `InvalidAssetError` never leaks; D-07's honesty guard guarantees no faux barcode is ever drawn.
- `code.image: nil` is byte-identical to omitting `:image`; byte-identity holds across two deterministic renders.
</success_criteria>

<output>
Create `.planning/phases/116-new-families-payslip-ticket/116-03-SUMMARY.md` when done
</output>
