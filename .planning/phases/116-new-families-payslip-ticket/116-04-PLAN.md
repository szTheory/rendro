---
phase: 116-new-families-payslip-ticket
plan: 04
type: execute
wave: 3
depends_on: ["116-02", "116-03"]
files_modified:
  - lib/mix/tasks/rendro/api.gen.ex
  - priv/public_api.json
  - priv/support_matrix.json
  - test/docs_contract/recipes_claims_test.exs
autonomous: true
requirements: [FAM-03]
must_haves:
  truths:
    - "priv/public_api.json lists Rendro.Recipes.Payslip and Rendro.Recipes.Ticket at the adapter tier, generated by mix rendro.api.gen (never hand-authored)"
    - "test/docs_contract/public_api_contract_test.exs passes -- the freshly-generated manifest is byte-identical to the checked-in priv/public_api.json"
    - "priv/support_matrix.json has proof-backed payslip and ticket rows whose evidence paths exist on disk and whose capability claims are individually asserted in recipes_claims_test.exs, mirroring the statement/receipt_report/certificate row pattern"
  artifacts:
    - priv/public_api.json
    - priv/support_matrix.json
    - test/docs_contract/recipes_claims_test.exs
  key_links:
    - "lib/mix/tasks/rendro/api.gen.ex's @public_modules allowlist -> mix rendro.api.gen -> priv/public_api.json -> test/docs_contract/public_api_contract_test.exs byte-comparison"
    - "priv/support_matrix.json payslip/ticket rows -> test/docs_contract/recipes_claims_test.exs new describe blocks -> evidence paths (test/rendro/recipes/payslip_test.exs, test/rendro/recipes/ticket_test.exs)"
---

<objective>
Close the loop on FAM-03's registration requirement: add `Rendro.Recipes.Payslip` and `Rendro.Recipes.Ticket` to the hardcoded `@public_modules` allowlist that `mix rendro.api.gen` introspects (RESEARCH Finding 1 — `@moduledoc tags: [:adapter]` alone produces NO manifest change without this edit), regenerate `priv/public_api.json`, and add proof-backed `payslip`/`ticket` rows to `priv/support_matrix.json` with matching contract-test assertions.

Purpose: Without this plan, Payslip and Ticket exist as working code but are invisible to every downstream consumer of `priv/public_api.json`/`priv/support_matrix.json` (docs generation, the Phase-79 public-API contract lane, Milestone C's future catalog) — FAM-03 is explicitly unmet until this registration lands.
Output: Updated `@public_modules`, regenerated `priv/public_api.json`, new `payslip`/`ticket` rows in `priv/support_matrix.json`, matching `describe` blocks in `test/docs_contract/recipes_claims_test.exs`.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `priv/public_api.json` — two new top-level module entries: `Rendro.Recipes.Payslip`, `Rendro.Recipes.Ticket` (adapter tier), generated via `mix rendro.api.gen`
- `lib/mix/tasks/rendro/api.gen.ex` `@public_modules` — extended with both new recipe modules, alphabetically
- `priv/support_matrix.json` — two new surfaces: `payslip` (capabilities: `net_pay_visual_anchor`, `multi_page_ledger_continuation`, `jurisdiction_as_data`, `deterministic_output`), `ticket` (capabilities: `geometry_derived_layout`, `caller_supplied_code_image`, `no_faux_barcode`, `deterministic_output`)
- `test/docs_contract/recipes_claims_test.exs` — two new `describe` blocks proof-backing the above capability claims
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-new-families-payslip-ticket/116-CONTEXT.md
@.planning/phases/116-new-families-payslip-ticket/116-RESEARCH.md
@.planning/phases/116-new-families-payslip-ticket/116-02-SUMMARY.md
@.planning/phases/116-new-families-payslip-ticket/116-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register both recipes in @public_modules and regenerate priv/public_api.json</name>
  <files>lib/mix/tasks/rendro/api.gen.ex, priv/public_api.json</files>
  <read_first>
    lib/mix/tasks/rendro/api.gen.ex (`@public_modules` list, lines 44-102; the existing `Rendro.Recipes.*` entries sit alphabetically at lines 89-93: `BrandedInvoice, Certificate, Invoice, Receipt, Statement`)
    test/docs_contract/public_api_contract_test.exs (Assertion 2, "manifest surface equality" — byte-compares a freshly-generated manifest against the checked-in `priv/public_api.json`; this is what proves the regeneration was done correctly, not eyeballed)
  </read_first>
  <action>
    In `lib/mix/tasks/rendro/api.gen.ex`, insert `Rendro.Recipes.Payslip,` and `Rendro.Recipes.Ticket,` into the `@public_modules` list, ALPHABETICALLY among the existing `Rendro.Recipes.*` entries: `Payslip` goes between `Rendro.Recipes.Invoice,` and `Rendro.Recipes.Receipt,`; `Ticket` goes after `Rendro.Recipes.Statement,`. Confirm both `Rendro.Recipes.Payslip` and `Rendro.Recipes.Ticket` already carry `@moduledoc tags: [:adapter]` (set in 116-02/116-03 Task 1) — this allowlist edit is necessary but not sufficient without that tag, and vice versa (RESEARCH Finding 1/Pitfall 1).

    Run `mix rendro.api.gen` to regenerate `priv/public_api.json`. Do NOT hand-edit `priv/public_api.json` — it is a generated, deterministically-sorted artifact; any manual edit will be overwritten by (or drift from) the next regeneration. `git diff priv/public_api.json` after regeneration should show ONLY two new module entries (`Rendro.Recipes.Payslip`, `Rendro.Recipes.Ticket`), inserted in alphabetically-sorted position, with no unrelated drift to any existing entry.
  </action>
  <acceptance_criteria>
    - `git diff priv/public_api.json` shows exactly two new top-level module keys added, no other changes
    - `mix test test/docs_contract/public_api_contract_test.exs` passes (both the schema-validation and the byte-identity "manifest surface equality" assertions)
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.api.gen && mix test test/docs_contract/public_api_contract_test.exs</automated>
  </verify>
  <done>@public_modules includes both new recipes alphabetically; priv/public_api.json is regenerated (not hand-edited) and the docs-contract lane passes.</done>
</task>

<task type="auto">
  <name>Task 2: Proof-backed payslip/ticket rows in priv/support_matrix.json + recipes_claims_test.exs</name>
  <files>priv/support_matrix.json, test/docs_contract/recipes_claims_test.exs</files>
  <read_first>
    priv/support_matrix.json (the `statement` row at line ~440, `receipt_report` at ~451, `certificate` at ~462 — exact shape to mirror: `{"surface": <name>, "status": "supported", "evidence": <test file path>, "recorded_at": "<date>", "capabilities": {<key>: "supported", ...}}`)
    test/docs_contract/recipes_claims_test.exs (existing `describe "statement row claim backing"` / `"receipt_report row claim backing"` / `"certificate row claim backing"` blocks, lines 13-98 — the exact pattern: one test per capability key asserting `matrix["<surface>"]["capabilities"]["<key>"] == "supported"`, one test asserting the evidence path exists on disk via `File.exists?/1` or similar, one test asserting `status == "supported"`)
    priv/schemas/support_matrix.schema.json (confirm `additionalProperties: true` for top-level surface keys — new `payslip`/`ticket` keys are unlisted `additionalProperties`, same as every existing recipe row, so no schema edit is needed; RESEARCH Assumption A3)
  </read_first>
  <action>
    Add a `"payslip"` row to `priv/support_matrix.json` (JSON object, inserted near the other recipe rows for readability, exact position does not matter to the schema): `surface: "payslip"`, `status: "supported"`, `evidence: "test/rendro/recipes/payslip_test.exs"`, `recorded_at: <today's date, ISO 8601>`, `capabilities: {net_pay_visual_anchor: "supported", multi_page_ledger_continuation: "supported", jurisdiction_as_data: "supported", deterministic_output: "supported"}`.

    Add a `"ticket"` row: `surface: "ticket"`, `status: "supported"`, `evidence: "test/rendro/recipes/ticket_test.exs"`, `recorded_at: <today's date>`, `capabilities: {geometry_derived_layout: "supported", caller_supplied_code_image: "supported", no_faux_barcode: "supported", deterministic_output: "supported"}`.

    Add two new `describe` blocks to `test/docs_contract/recipes_claims_test.exs`, mirroring the exact shape of the existing `"certificate row claim backing"` block (lines 70-99): `"payslip row claim backing"` with one test per capability key (4 tests) + one evidence-path-exists test + one status test; `"ticket row claim backing"` with the same shape for its 4 capability keys. Every capability KEY name used in the JSON row must have a matching assertion in this file — an unasserted capability claim is exactly the kind of "unproven claim" this docs-contract lane exists to catch (SHOW-04, downstream).
  </action>
  <acceptance_criteria>
    - `priv/support_matrix.json` validates against `priv/schemas/support_matrix.schema.json` (no schema edit required, per `<read_first>`)
    - Both evidence paths (`test/rendro/recipes/payslip_test.exs`, `test/rendro/recipes/ticket_test.exs`) exist on disk
    - Every capability key in both new rows has a matching assertion in `recipes_claims_test.exs`
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/</automated>
  </verify>
  <done>payslip/ticket rows exist in priv/support_matrix.json, validate against the schema, and every capability claim is individually asserted (proof-backed, not just declared) in recipes_claims_test.exs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated-artifact integrity | `priv/public_api.json` is a checked-in, generated file; a hand-edit that drifts from `mix rendro.api.gen`'s deterministic output is a supply-chain-adjacent integrity concern for a library whose whole value proposition is a byte-comparable, auditable public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-04-01 | Tampering | `priv/public_api.json` (checked-in generated artifact) | low | mitigate | Task 1 explicitly forbids hand-editing the manifest; `test/docs_contract/public_api_contract_test.exs`'s byte-identity assertion (already existing, unmodified by this plan) is the automated enforcement that catches any future drift, including an accidental hand-edit |
| T-116-04-02 | Repudiation | `priv/support_matrix.json` capability claims without backing proof | low | mitigate | Every capability key added in Task 2 has a corresponding `recipes_claims_test.exs` assertion — an unproven claim in the matrix fails CI, mirroring the existing statement/receipt_report/certificate precedent |

No new runtime attack surface — this plan touches only build tooling and checked-in JSON/test artifacts; no user-facing code path changes.

</threat_model>

<verification>
`mix rendro.api.gen && mix test test/docs_contract/` after Task 1; `mix test test/docs_contract/` after Task 2. Full suite (`mix test`) before this plan's — and the phase's — SUMMARY is written, since this is the phase's final wave.
</verification>

<success_criteria>
- Both new recipes appear in `priv/public_api.json` at the adapter tier, generated (not hand-authored), passing the byte-identity contract test.
- Both new recipes have proof-backed `priv/support_matrix.json` rows with every capability claim individually tested.
- Full `mix test` suite is green — FAM-01, FAM-02, and FAM-03 are all fully satisfied; Phase 116 is complete.
</success_criteria>

<output>
Create `.planning/phases/116-new-families-payslip-ticket/116-04-SUMMARY.md` when done
</output>
