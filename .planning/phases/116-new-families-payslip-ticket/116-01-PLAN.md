---
phase: 116-new-families-payslip-ticket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes/pagination.ex
  - test/rendro/recipes/pagination_test.exs
autonomous: true
requirements: [FAM-03]
must_haves:
  truths:
    - "Statement's existing arity-1 label_resolver call sites (statement.ex:274, statement.ex:294) compile unmodified and Statement's full test suite stays green after the arity-2 generalization"
    - "label_resolver/2 merge order is opts[:labels] -> default_labels -> Rendro.Format.label/1 (D-18)"
    - "Malformed :labels (non-map, or map with a non-binary/empty-string value) and malformed :formatters (non-keyword, or keyword with a non-arity-1-function value) each raise an instructive four-part ArgumentError -- never BadMapError, FunctionClauseError, or BadArityError (D-19)"
  artifacts:
    - lib/rendro/recipes/pagination.ex
    - test/rendro/recipes/pagination_test.exs
  key_links:
    - "Payslip.sections/2 and Ticket.sections/2 (116-02/116-03) will call Pagination.label_resolver(opts, @default_labels), Pagination.validate_labels!/2, and Pagination.validate_formatters!/2 -- this plan is the load-bearing dependency both recipe plans build on"
---

<objective>
Generalize the shared, `@moduledoc false` `Rendro.Recipes.Pagination` helper with the two pieces of net-new plumbing both Payslip and Ticket require: an additive arity-2 `label_resolver/2` (recipe-owned default labels, D-18) and D-19's opts-shape validators (`validate_labels!/2`, `validate_formatters!/2`). Neither change touches `priv/public_api.json` (`Pagination` is `@moduledoc false`, confirmed RESEARCH Finding 4).

Purpose: Land the one true shared seam before either recipe plan starts, so 116-02 (Payslip) and 116-03 (Ticket) can run in parallel against a stable, tested `Pagination` API instead of each inventing (and potentially diverging on) the same validators.
Output: `label_resolver/2`, `validate_labels!/2`, `validate_formatters!/2` in `lib/rendro/recipes/pagination.ex`; a new `test/rendro/recipes/pagination_test.exs` covering all three; zero regression in `test/rendro/recipes/statement_test.exs`.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `Rendro.Recipes.Pagination.label_resolver/2` — new arity, additive over the existing `label_resolver/1` (D-18)
- `Rendro.Recipes.Pagination.validate_labels!/2` — new (D-19)
- `Rendro.Recipes.Pagination.validate_formatters!/2` — new (D-19)

`Rendro.Recipes.Pagination` stays `@moduledoc false`; none of the above touch `priv/public_api.json` (RESEARCH Finding 4).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-new-families-payslip-ticket/116-CONTEXT.md
@.planning/phases/116-new-families-payslip-ticket/116-RESEARCH.md
@.planning/phases/116-new-families-payslip-ticket/116-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize label_resolver to arity-2 (additive, D-18)</name>
  <files>lib/rendro/recipes/pagination.ex, test/rendro/recipes/pagination_test.exs</files>
  <read_first>
    lib/rendro/recipes/pagination.ex (current arity-1 `label_resolver/1` at lines 64-73; `type_name/1` at lines 76-82, do not modify)
    lib/rendro/recipes/statement.ex (the only two call sites: line 274 inside `header_section/2` and line 294 inside `body_section/2`, both `Rendro.Recipes.Pagination.label_resolver(opts)` — must keep compiling with zero call-site edits)
    lib/rendro/format.ex (`label/1` at line 108 — `def label(key) when is_map_key(@labels, key), do: Map.fetch!(@labels, key)`. There is NO fallback clause. Calling `Rendro.Format.label/1` with any atom outside its 5-key `@labels` map — `:balance`, `:brought_forward`, `:carried_forward`, `:opening_balance`, `:closing_balance` — raises `FunctionClauseError`, not a humanized fallback string.)
  </read_first>
  <behavior>
    - `label_resolver(opts, default_labels \\ %{})` with `opts = [labels: %{net_pay: "NETTO"}]`, `default_labels = %{net_pay: "Net Pay"}` → resolver.(:net_pay) returns `"NETTO"` (opts wins).
    - `label_resolver([], %{net_pay: "Net Pay"})` → resolver.(:net_pay) returns `"Net Pay"` (default_labels wins when opts absent).
    - `label_resolver([], %{})` → resolver.(:opening_balance) returns `"Opening balance"` (falls through to `Rendro.Format.label/1`, byte-identical to today's arity-1 behavior).
    - `label_resolver(opts)` (arity-1 call, e.g. from `statement.ex:274`) still compiles and resolves identically to before this change (default `default_labels \\ %{}` collapses the merge order to `opts[:labels] -> Rendro.Format.label/1`, exactly today's two-step lookup).
  </behavior>
  <action>
    Per D-18, change `def label_resolver(opts)` (`pagination.ex:64`) to `def label_resolver(opts, default_labels \\ %{})`. Keep the existing `user_labels = Keyword.get(opts, :labels, %{})` line and the returned closure's shape, but insert a new intermediate `Map.fetch(default_labels, key)` lookup between the existing `user_labels` lookup and the existing `Rendro.Format.label(key)` fallback. This is an additive default-argument change (mirror the exact target shown in RESEARCH.md's "Code Examples" section) — do NOT remove or restructure the arity-1 form; Elixir's default-argument sugar means `def label_resolver(opts, default_labels \\ %{})` still answers calls made as `label_resolver(opts)`, so `statement.ex:274`/`statement.ex:294` require zero edits.

    Add a code comment directly above `label_resolver/2` documenting the `Rendro.Format.label/1` gotcha verified above: every label key any recipe resolves via the returned closure MUST be present in either `opts[:labels]` or that recipe's own `default_labels` map — there is no silent/humanized fallback for an unrecognized atom, it is a `FunctionClauseError` crash. This comment is the load-bearing warning 116-02/116-03 rely on when building their `@default_labels` maps.
  </action>
  <acceptance_criteria>
    - `Rendro.Recipes.Pagination.label_resolver/2` exists with signature `label_resolver(opts, default_labels \\ %{})`
    - `test/rendro/recipes/statement_test.exs` passes unmodified (zero edits to that file)
    - New `test/rendro/recipes/pagination_test.exs` (create) has a `describe "label_resolver/2 (D-18)"` block covering all four cases in `<behavior>` above
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/pagination_test.exs test/rendro/recipes/statement_test.exs</automated>
  </verify>
  <done>label_resolver/2 lands additively; Statement's two arity-1 call sites are untouched and its full test suite is green; the four new merge-order tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-19 opts-shape validators (validate_labels!/2, validate_formatters!/2)</name>
  <files>lib/rendro/recipes/pagination.ex, test/rendro/recipes/pagination_test.exs</files>
  <read_first>
    lib/rendro/recipes/pagination.ex (`type_name/1` at lines 76-82 — reuse verbatim in the new validators' `Why:` lines, do not reimplement)
    lib/rendro/recipes/invoice.ex (`validate_totals_field_type!/2` at lines 608-629 and the four-part What/Where/Why/Next `ArgumentError` idiom used throughout — the exact message shape to mirror; note the raised message's first line always names the caller's public entry point, e.g. `"Rendro.Recipes.Invoice.document/2 — ..."`)
  </read_first>
  <behavior>
    - `validate_labels!([], "Rendro.Recipes.Payslip.document/2")` → `:ok` (key absent is always valid).
    - `validate_labels!([labels: %{net_pay: "Net Pay"}], "...")` → `:ok`.
    - `validate_labels!([labels: "not a map"], "...")` → raises `ArgumentError` whose message contains `"What:"`, `"Where:"`, `"Why:"`, `"Next:"`, and `"String"` (the `type_name/1` result for the bad value) — NOT `BadMapError`.
    - `validate_labels!([labels: %{net_pay: ""}], "...")` → raises `ArgumentError` (empty-string label value rejected — an empty label would blank the anchor, the exact D-18 footgun this guards against).
    - `validate_formatters!([], "...")` → `:ok`.
    - `validate_formatters!([formatters: [amount: &Rendro.Format.money/1]], "...")` → `:ok`.
    - `validate_formatters!([formatters: %{amount: & &1}], "...")` → raises `ArgumentError` (not a keyword list) — not `BadArityError`.
    - `validate_formatters!([formatters: [amount: fn _a, _b -> "x" end]], "...")` → raises `ArgumentError` (arity-2 function rejected, `is_function(f, 1)` check) — not `BadArityError`.
  </behavior>
  <action>
    Add two new public functions to `Rendro.Recipes.Pagination` (module stays `@moduledoc false`; confirmed this never touches `priv/public_api.json`):

    `validate_labels!(opts, recipe_mfa)` — `recipe_mfa` is a plain string like `"Rendro.Recipes.Payslip.document/2"`, used verbatim as the error message's first line and in the `Where:` field (mirror Invoice's exact convention of naming the public entry point, not the private validator). If `Keyword.get(opts, :labels)` is not `nil`: raise the four-part `ArgumentError` if the value is not a `Map` (use `Pagination.type_name/1` in `Why:`); if it is a Map, raise if ANY value is not a non-empty `String.t()` (an empty string `""` is invalid — it would silently blank a chrome/anchor label). Returns `:ok` when the key is absent or the value is fully valid.

    `validate_formatters!(opts, recipe_mfa)` — same `recipe_mfa` convention. If `Keyword.get(opts, :formatters)` is not `nil`: raise if the value is not `Keyword.keyword?/1`; if it is a keyword list, raise if ANY value is not `is_function(fun, 1)`. Returns `:ok` when the key is absent or fully valid.

    Neither validator may let a `BadMapError`, `FunctionClauseError`, or `BadArityError` escape — every type/shape check happens inside these two functions, before any caller code (116-02/116-03) touches the raw opts value.
  </action>
  <acceptance_criteria>All eight `<behavior>` cases pass as new ExUnit tests in `test/rendro/recipes/pagination_test.exs` under a `describe "validate_labels!/2 and validate_formatters!/2 (D-19)"` block.</acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/pagination_test.exs</automated>
  </verify>
  <done>validate_labels!/2 and validate_formatters!/2 exist, follow the four-part ArgumentError idiom, reuse type_name/1, and all eight acceptance-criteria cases pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recipe caller → `opts` keyword | Any Elixir caller of `Payslip.document/2`/`Ticket.document/2` (and, indirectly, `Pagination`) supplies `:labels`/`:formatters` — untrusted shape/type until validated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-01-01 | Tampering / DoS | `Pagination.label_resolver/2` consumers | medium | mitigate | `validate_labels!/2` rejects a non-map or non-binary/empty-string `:labels` value with an instructive `ArgumentError` before any downstream `Map.fetch/2` call can raise a raw `BadMapError` deep inside a section builder |
| T-116-01-02 | Tampering / DoS | `Pagination.formatter/3` consumers | medium | mitigate | `validate_formatters!/2` rejects a non-keyword or non-arity-1-function `:formatters` value with an instructive `ArgumentError` before render-time invocation could raise `BadArityError` or a cryptic function-call crash |
| T-116-01-03 | Information Disclosure | Error messages from T-116-01-01/02 | low | accept | Error messages `inspect/1` the caller-supplied value; this is standard Elixir library practice (identical to Invoice's existing `validate_totals_field_type!/2`) and contains no secrets — caller-authored opts values only |

</threat_model>

<verification>
Full recipe + Statement regression suite: `mix test test/rendro/recipes/pagination_test.exs test/rendro/recipes/statement_test.exs`. Full suite (`mix test`) must stay green before 116-02/116-03 start consuming this plan's output.
</verification>

<success_criteria>
- `label_resolver/2` is additive; `statement.ex` requires zero edits and its test suite is green.
- `validate_labels!/2` and `validate_formatters!/2` exist, are exported (not `defp`), and both are ready for 116-02/116-03 to call from `sections/2`.
- `pagination_test.exs` exists and covers all cases in both tasks' `<behavior>` blocks.
</success_criteria>

<output>
Create `.planning/phases/116-new-families-payslip-ticket/116-01-SUMMARY.md` when done
</output>
