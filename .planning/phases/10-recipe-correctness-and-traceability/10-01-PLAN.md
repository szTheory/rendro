---
phase: 10-recipe-correctness-and-traceability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/adapters/mailglass.ex
  - test/rendro/adapters/mailglass_test.exs
  - lib/rendro/adapters/accrue.ex
  - test/rendro/adapters/accrue_test.exs
  - guides/integrations.md
autonomous: true
requirements:
  - ADPT-05
must_haves:
  truths:
    - "Custom Mailglass-style wrappers attach PDFs without crashing and remain wrapped in their own struct module"
    - "Accrue invoices fail with typed errors on invalid nested line items instead of raising"
    - "Accrue invoice headers render issued dates as user-facing YYYY-MM-DD values instead of Elixir inspect syntax"
    - "Public integration docs state the exact supported adapter shapes and exact failure tuples"
  artifacts:
    - path: lib/rendro/adapters/mailglass.ex
      provides: "Mailglass wrapper dispatch that re-wraps through the input struct module"
    - path: test/rendro/adapters/mailglass_test.exs
      provides: "Regression coverage for custom wrapper success and unsupported wrapper failure"
    - path: lib/rendro/adapters/accrue.ex
      provides: "Typed nested line-item validation and issued_at normalization"
    - path: test/rendro/adapters/accrue_test.exs
      provides: "Regression coverage for invalid line_items and issued_at formatting"
    - path: guides/integrations.md
      provides: "Truthful Mailglass and Accrue contract documentation"
  key_links:
    - from: lib/rendro/adapters/mailglass.ex
      to: test/rendro/adapters/mailglass_test.exs
      via: "custom wrapper success regression"
      pattern: "Mailglass\\.\\* wrapper with :swoosh and own update_swoosh/2"
    - from: lib/rendro/adapters/accrue.ex
      to: test/rendro/adapters/accrue_test.exs
      via: "typed invalid invoice regression"
      pattern: "invalid_line_item|invalid_invoice"
    - from: guides/integrations.md
      to: lib/rendro/adapters/mailglass.ex
      via: "exact supported wrapper and error tuple wording"
      pattern: "update_swoosh/2|invalid_line_item|Issued: 2026-04-26"
---

<objective>
Fix the remaining recipe contract defects in Mailglass and Accrue, then restate the public integration guide to match the corrected behavior exactly.

Purpose: Close the Phase 10 adapter correctness gaps without widening scope beyond the optional recipe layer.
Output: Hardened Mailglass and Accrue adapters, focused regression tests, and truthful integration-guide contract docs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-recipe-correctness-and-traceability/10-CONTEXT.md
@.planning/phases/10-recipe-correctness-and-traceability/10-RESEARCH.md
@.planning/phases/10-recipe-correctness-and-traceability/10-PATTERNS.md
@.planning/phases/05-CONTEXT.md
@.planning/phases/05-early-ecosystem-recipes/05-REVIEW.md
@.planning/phases/05-early-ecosystem-recipes/05-VERIFICATION.md
@.planning/phases/05-early-ecosystem-recipes/05-HUMAN-UAT.md
@lib/rendro/adapters/mailglass.ex
@lib/rendro/adapters/accrue.ex
@test/rendro/adapters/mailglass_test.exs
@test/rendro/adapters/accrue_test.exs
@guides/integrations.md
@test/support/mocks.ex

<interfaces>
From lib/rendro/adapters/mailglass.ex:
```elixir
@spec attach_pdf(term(), Rendro.Document.t(), String.t()) ::
        term()
        | {:error, Rendro.Error.t()}
        | {:error, {:unrecognized_message_shape, atom() | term()}}
```

```elixir
defp mailglass_message?(value) when is_struct(value) do
  mod = value.__struct__

  mod
  |> Atom.to_string()
  |> String.ends_with?(".Message") and
    function_exported?(mod, :update_swoosh, 2)
end
```

From lib/rendro/adapters/accrue.ex:
```elixir
@spec recipe(term()) ::
        {:ok, Rendro.Document.t()} | {:error, {:invalid_invoice, term()}}
```

```elixir
defp build_header(%Accrue.Invoice{id: id, issued_at: issued_at, customer: customer}) do
  [
    Rendro.block(Rendro.text("INVOICE ##{id}", size: 18)),
    Rendro.block(Rendro.text("Issued: #{inspect(issued_at)}", size: 10)),
    Rendro.block(Rendro.text("Bill to: #{customer_name(customer)}", size: 10))
  ]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Mailglass custom-wrapper dispatch and prove the admitted wrapper contract</name>
  <files>lib/rendro/adapters/mailglass.ex, test/rendro/adapters/mailglass_test.exs</files>
  <read_first>
    lib/rendro/adapters/mailglass.ex
    test/rendro/adapters/mailglass_test.exs
    test/support/mocks.ex
    .planning/phases/10-recipe-correctness-and-traceability/10-CONTEXT.md
    .planning/phases/05-early-ecosystem-recipes/05-REVIEW.md
    .planning/phases/05-early-ecosystem-recipes/05-HUMAN-UAT.md
  </read_first>
  <behavior>
    - A struct whose module name ends in `.Message`, exports `update_swoosh/2`, and carries `%Swoosh.Email{}` in `:swoosh` or `:email` returns the same wrapper type with the PDF attachment added.
    - An admitted wrapper that lacks a `%Swoosh.Email{}` in `:swoosh` or `:email` returns `{:error, {:unrecognized_message_shape, module}}` instead of raising or downgrading to bare `%Swoosh.Email{}`.
  </behavior>
  <action>
    In `lib/rendro/adapters/mailglass.ex`, update `put_swoosh/2` per D-02, D-03, and D-04 so it only accepts struct inputs, binds `mod = message.__struct__`, dispatches `apply(mod, :update_swoosh, [message, swoosh_email])` when `function_exported?(mod, :update_swoosh, 2)` is true, falls back only to `%{message | swoosh: swoosh_email}` or `%{message | email: swoosh_email}` when those exact fields exist, and otherwise returns `{:error, {:unrecognized_message_shape, mod}}`. Delete the current hardcoded `Mailglass.Message.update_swoosh/2` dispatch and delete the terminal `true -> swoosh_email` fallback so unsupported wrappers are never silently downgraded.
    In `test/rendro/adapters/mailglass_test.exs`, change `Mailglass.Wrapper.Message` to `defstruct [:id, :payload, :swoosh]`, make `update_swoosh/2` return `%{msg | swoosh: swoosh}`, and add a regression test named exactly `Mailglass.* wrapper with :swoosh and own update_swoosh/2 is re-wrapped via the wrapper's module` that asserts `Adapter.attach_pdf/3` returns `%Mailglass.Wrapper.Message{}` with exactly one `invoice.pdf` attachment on `result.swoosh.attachments`.
  </action>
  <acceptance_criteria>
    - `lib/rendro/adapters/mailglass.ex` contains `function_exported?(mod, :update_swoosh, 2)` inside `put_swoosh/2`.
    - `lib/rendro/adapters/mailglass.ex` contains `apply(mod, :update_swoosh, [message, swoosh_email])`.
    - `lib/rendro/adapters/mailglass.ex` contains `{:error, {:unrecognized_message_shape, mod}}`.
    - `lib/rendro/adapters/mailglass.ex` does not contain `Mailglass.Message.update_swoosh(message, swoosh_email)`.
    - `lib/rendro/adapters/mailglass.ex` does not contain a `true ->` arm that returns `swoosh_email`.
    - `test/rendro/adapters/mailglass_test.exs` contains the exact test name `Mailglass.* wrapper with :swoosh and own update_swoosh/2 is re-wrapped via the wrapper's module`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/adapters/mailglass_test.exs</automated>
  </verify>
  <done>Custom Mailglass-style wrappers admitted by the documented contract no longer raise `FunctionClauseError`, and unsupported shapes still return typed error tuples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate Accrue line_items at the adapter boundary and normalize issued_at output</name>
  <files>lib/rendro/adapters/accrue.ex, test/rendro/adapters/accrue_test.exs</files>
  <read_first>
    lib/rendro/adapters/accrue.ex
    test/rendro/adapters/accrue_test.exs
    .planning/phases/10-recipe-correctness-and-traceability/10-CONTEXT.md
    .planning/phases/05-early-ecosystem-recipes/05-REVIEW.md
    .planning/phases/10-recipe-correctness-and-traceability/10-PATTERNS.md
  </read_first>
  <behavior>
    - `recipe/1` returns `{:error, {:invalid_invoice, {:invalid_line_item, {index, bad_item}}}}` on the first non-`%Accrue.LineItem{}` entry and never raises from `Enum.map/2`.
    - `Issued:` renders `YYYY-MM-DD` for `Date`, `NaiveDateTime`, and `DateTime` inputs and does not emit `~D[` or inspected datetime syntax in user-facing document text.
  </behavior>
  <action>
    In `lib/rendro/adapters/accrue.ex`, implement nested validation per D-06 through D-09 before building the document. Use a dedicated validator that walks `line_items` with indexes, returns `:ok` when every entry matches `%Accrue.LineItem{}`, and returns `{:error, {:invalid_invoice, {:invalid_line_item, {index, bad_item}}}}` on the first invalid entry. Keep the public `@spec recipe(term()) :: {:ok, Rendro.Document.t()} | {:error, {:invalid_invoice, term()}}`, but ensure the `%Accrue.Invoice{}` path never raises on bad nested input.
    Replace the `inspect(issued_at)` header rendering per D-10 through D-13 with a `format_issued_at/1` helper that returns `Date.to_iso8601(date)` for `%Date{}`, `NaiveDateTime.to_date() |> Date.to_iso8601()` for `%NaiveDateTime{}`, `DateTime.to_date() |> Date.to_iso8601()` for `%DateTime{}`, `nil` for `nil`, and `to_string(value)` for any other already-stringifiable compatibility input. Build the header so `Issued: <value>` appears only when `format_issued_at/1` returns a non-nil string; do not use `inspect/1` anywhere in the invoice header path.
    In `test/rendro/adapters/accrue_test.exs`, add one regression asserting an invoice with an invalid nested line item returns `{:error, {:invalid_invoice, {:invalid_line_item, {0, %{description: "bad"}}}}}` (or equivalent first-index payload), one regression asserting a `NaiveDateTime` `issued_at` renders `Issued: 2026-04-26`, and one assertion that the inspected document string does not contain `~D[` after `Adapter.recipe/1`.
  </action>
  <acceptance_criteria>
    - `lib/rendro/adapters/accrue.ex` contains `{:invalid_line_item, {index, bad_item}}` or an equivalent first-invalid-entry payload under `{:error, {:invalid_invoice, ...}}`.
    - `lib/rendro/adapters/accrue.ex` contains `Date.to_iso8601(`, `NaiveDateTime.to_date()`, and `DateTime.to_date()`.
    - `lib/rendro/adapters/accrue.ex` does not contain `inspect(issued_at)`.
    - `test/rendro/adapters/accrue_test.exs` contains assertions for `invalid_line_item` and `Issued: 2026-04-26`.
    - `test/rendro/adapters/accrue_test.exs` contains an assertion that the document inspection does not match `~D[`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/adapters/accrue_test.exs</automated>
  </verify>
  <done>Accrue recipe input failures are tuple-returning and deterministic, and invoice output shows normalized user-facing dates.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the integration guide to match the corrected Mailglass and Accrue contracts exactly</name>
  <files>guides/integrations.md</files>
  <read_first>
    guides/integrations.md
    lib/rendro/adapters/mailglass.ex
    lib/rendro/adapters/accrue.ex
    test/rendro/adapters/mailglass_test.exs
    test/rendro/adapters/accrue_test.exs
    .planning/phases/10-recipe-correctness-and-traceability/10-CONTEXT.md
  </read_first>
  <action>
    In the Mailglass section of `guides/integrations.md`, update the verification and failure-diagnostics text per D-05 and D-15 so the supported custom-wrapper contract is spelled out exactly: the struct module name must end in `.Message`, it must export `update_swoosh/2`, and it must carry the wrapped `%Swoosh.Email{}` in `:swoosh` or `:email`. State that unsupported shapes return `{:error, {:unrecognized_message_shape, struct_module}}` and are not downgraded to raw `%Swoosh.Email{}`.
    In the Accrue section, update the recipe-contract and failure-diagnostics tables so they state the exact accepted `issued_at` temporal inputs (`Date`, `NaiveDateTime`, `DateTime`), state that those inputs render as `YYYY-MM-DD`, state that non-`%Accrue.LineItem{}` entries fail the whole recipe with `{:error, {:invalid_invoice, {:invalid_line_item, {index, bad_item}}}}`, and keep the existing "minimum useful mapping" guidance intact.
  </action>
  <acceptance_criteria>
    - `guides/integrations.md` contains `.Message`, `update_swoosh/2`, and ``:swoosh` or `:email`` in the Mailglass failure contract.
    - `guides/integrations.md` contains the exact type names `Date`, `NaiveDateTime`, and `DateTime` in the Accrue contract.
    - `guides/integrations.md` contains `{:invalid_invoice, {:invalid_line_item, {index, bad_item}}}` or the exact payload chosen in Task 2.
    - `guides/integrations.md` contains `YYYY-MM-DD`.
    - `guides/integrations.md` still contains the sentence `The recipe is the **minimum useful** mapping.`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "\.Message|update_swoosh/2|:swoosh|:email|NaiveDateTime|DateTime|YYYY-MM-DD|minimum useful|invalid_line_item" guides/integrations.md</automated>
  </verify>
  <done>The public integration guide matches the implemented adapter contract and no longer overpromises or handwaves failure behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter caller -> `Rendro.Adapters.Mailglass` | Caller-controlled message wrappers cross into attachment mutation logic. |
| Adapter caller -> `Rendro.Adapters.Accrue` | Caller-controlled invoice and line-item data cross into document generation logic. |
| Public docs -> adopters | Maintainers and integrators rely on guide text as a contract for supported shapes and failure modes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | D | `lib/rendro/adapters/mailglass.ex` | mitigate | Validate wrapper shape per D-02/D-04 and return `{:error, {:unrecognized_message_shape, mod}}` instead of allowing `FunctionClauseError` on custom wrappers. |
| T-10-02 | T | `lib/rendro/adapters/accrue.ex` | mitigate | Validate every `line_items` entry before rendering and fail with `{:error, {:invalid_invoice, {:invalid_line_item, {index, bad_item}}}}` so incorrect billing rows never silently disappear. |
| T-10-03 | T | `lib/rendro/adapters/accrue.ex` | mitigate | Normalize accepted temporal inputs to `YYYY-MM-DD` and remove `inspect/1` from user-facing invoice text to prevent debug-format leakage. |
| T-10-04 | R | `guides/integrations.md` | mitigate | Update the guide in the same plan as the code/tests so documented wrapper and invoice contracts stay evidence-backed. |
</threat_model>

<verification>
- `mix test test/rendro/adapters/mailglass_test.exs test/rendro/adapters/accrue_test.exs`
- `rg -n "\.Message|update_swoosh/2|invalid_line_item|YYYY-MM-DD|NaiveDateTime|DateTime" guides/integrations.md`
</verification>

<success_criteria>
1. Mailglass custom-wrapper dispatch succeeds for the exact admitted wrapper shape and never raises on unsupported shapes.
2. Accrue invalid nested line items return typed errors and issued dates render without Elixir inspect syntax.
3. `guides/integrations.md` states the exact supported shapes and failure tuples for both adapters.
</success_criteria>

<output>
After completion, create `.planning/phases/10-recipe-correctness-and-traceability/10-01-SUMMARY.md`
</output>
