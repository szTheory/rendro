---
phase: 49
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
  - test/rendro/deterministic_test.exs
autonomous: true
requirements:
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - Each paginated linked block fragment emits one rectangular `/Subtype /Link` annotation through the existing page `/Annots` seam.
    - Linked content still renders through the normal writer path while separate `/Link` annotations are emitted for the same paginated fragments.
    - External links serialize authored URI bytes into `/A << /S /URI /URI (...) >>` without canonicalization or extra action types.
    - Internal links serialize direct same-document `/Dest [page_ref /Fit]` arrays and remain deterministic across repeated renders.
  artifacts:
    - path: lib/rendro/pdf/writer.ex
      provides: "Link annotation collection, allocation, and `/Annots` serialization through the existing writer seam"
    - path: test/rendro/pdf/writer_test.exs
      provides: "Structural proof for `/Link`, `/A`, `/URI`, and direct `/Dest` writer output"
  key_links:
    - from: lib/rendro/link.ex
      to: lib/rendro/pdf/writer.ex
      via: "writer unwraps `%Rendro.Link{content: inner}` for visible content rendering while separately emitting link annotations"
    - from: lib/rendro/fragmentable.ex
      to: lib/rendro/pdf/writer.ex
      via: "paginated linked fragments become per-page rectangular link annotations"
    - from: lib/rendro/pdf/writer.ex
      to: test/rendro/pdf/writer_test.exs
      via: "writer tests lock the narrow `/Annots` link structure as contract"
    - from: lib/rendro/pdf/writer.ex
      to: test/rendro/deterministic_test.exs
      via: "deterministic proof asserts stable link annotation output"
---

<objective>
Serialize validated, measured links into deterministic PDF annotations without widening Rendro into a generic annotation or action system.

Purpose: Reuse the narrow `/Annots` writer seam to turn paginated linked blocks into structural PDF link annotations.
Output: Link annotation collection/allocation in the writer plus proof tests for URI actions, page destinations, and deterministic output.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.9-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/49-curated-link-annotation-surface/49-CONTEXT.md
@.planning/phases/49-curated-link-annotation-surface/49-RESEARCH.md
@.planning/phases/49-curated-link-annotation-surface/49-PATTERNS.md
@.planning/phases/48-embedded-file-core-surface/48-PATTERNS.md
@.planning/phases/48-embedded-file-core-surface/48-VERIFICATION.md
</execution_context>

<context>
@lib/rendro/pdf/writer.ex
@lib/rendro/page.ex
@test/rendro/pdf/writer_test.exs
@test/rendro/deterministic_test.exs

<interfaces>
From `lib/rendro/pdf/writer.ex`:
```elixir
{annot_refs, form_objects} =
  build_form_field_objects(page, page_num, form_field_allocations, page_index, opts)

page_dict =
  {:dict, maybe_add_annots_entry(pages_num, content_num, media_box, resources, annot_refs)}
```

From `lib/rendro/pdf/writer.ex`:
```elixir
defp maybe_add_annots_entry(pages_num, content_num, media_box, resources, annot_refs) do
  base_page_entries(pages_num, content_num, media_box, resources) ++
    [{"Annots", {:array, annot_refs}}]
end
```

From `lib/rendro/pdf/writer.ex`:
```elixir
defp form_field_rect(page, block) do
  x = block.x + page.margin_left
  y = page.height - (block.y + block.height) - page.margin_top
  [x, y, x + block.width, y + block.height]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse the page `/Annots` seam for deterministic link annotations</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <behavior>
    - Every linked paginated block fragment emits one rectangular `/Subtype /Link` annotation on its page.
    - `%Rendro.Link{content: inner}` delegates visible rendering through the existing writer path for `inner`, so linked text/table content still appears unchanged.
    - External links serialize `/A << /S /URI /URI (...) >>` with the authored URI bytes preserved.
    - Internal links serialize direct same-document `/Dest [page_ref /Fit]` arrays without named destinations or generic actions.
  </behavior>
  <action>
    Extend `lib/rendro/pdf/writer.ex` to collect linked blocks in page order and thread them through the same `build_page_objects` and `maybe_add_annots_entry` seam already used for widgets, per D-14 and the Phase 48 `/Annots` precedent.
    Add explicit writer render-path support for `%Rendro.Link{content: inner}` by delegating normal content rendering to the existing `inner` writer clauses before or alongside annotation collection; do not let the wrapper become annotation-only, because Phase 49 must preserve visible linked text/table output while adding interactivity.
    Reuse block geometry to derive each annotation rectangle per D-04 and D-14, emit one annotation per paginated fragment per D-15, and accept that those rectangular fragment boxes may include whitespace per D-18; do not add manual overlay APIs, `QuadPoints`, or non-rectangular hit regions per D-16.
    Serialize URI links with inline `/A << /S /URI /URI (...) >>` and preserve authored URI bytes exactly per D-13. Serialize page links with direct `/Dest [page_ref /Fit]` arrays per D-05 while keeping named destinations, arbitrary actions, and generic annotation dictionaries out of scope per D-03 and D-06.
    Add writer tests proving `/Subtype /Link`, URI action dictionaries, direct `/Dest` arrays, fragment-per-page annotation emission, and the absence of `QuadPoints`, `/Subtype /FileAttachment`, named destinations, or broader action dictionaries.
    Add proof cases showing linked text and linked table content still serialize their visible content unchanged while separate `/Link` annotations are emitted, so the wrapper preserves both render output and interactive output.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>The writer emits only the narrow Phase 49 link annotations through the existing `/Annots` seam, preserves authored target semantics, and keeps linked content rendering through the normal writer path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock deterministic proof for link annotation output</name>
  <files>test/rendro/pdf/writer_test.exs, test/rendro/deterministic_test.exs</files>
  <behavior>
    - Deterministic renders of the same linked document remain byte-identical.
    - Link annotations do not reorder or rewrite authored targets across repeated renders.
    - Adding a link wrapper does not remove or rewrite the visible rendered content bytes for linked text/table cases.
    - Structural proof stays limited to Phase 49 output and does not widen into Phase 50 docs/support-matrix work.
  </behavior>
  <action>
    Extend deterministic proof coverage with focused link cases in `test/rendro/deterministic_test.exs`, proving stable bytes for the same linked document and stable ordering when multiple curated links exist on a page.
    Include at least one linked text or table fixture whose rendered content bytes are asserted alongside the annotation structure so deterministic proof also covers the render-path delegation contract.
    Keep the proof narrow to structural writer output for this phase: use PDF substring/order assertions and deterministic render equality, but do not add support-matrix updates, viewer-behavior claims, or other Phase 50 artifacts.
    Where tests need narrow structural helpers, add them only inside the test files and keep them specific to Phase 49 proof needs.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs test/rendro/deterministic_test.exs</automated>
  </verify>
  <done>Link annotation output is structurally proven and deterministic without widening Phase 49 into support-boundary documentation work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paginated blocks -> PDF annotations | Final block geometry and validated targets cross into serialized annotation objects here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | Integrity | `Rendro.PDF.Writer` | mitigate | Derive rectangles from paginated block geometry only and emit one annotation per fragment so writer output matches validated layout state per D-04, D-14, and D-15. |
| T-49-08 | Information disclosure | `Rendro.PDF.Writer` | mitigate | Serialize only the validated URI schemes and direct same-document page destinations; do not emit file/custom-scheme actions, base URIs, or named destinations per D-08 and D-09. |
| T-49-09 | Scope creep | `Rendro.PDF.Writer` | mitigate | Reuse `/Annots` narrowly for `/Link` annotations and avoid generic annotation dictionaries, raw actions, or other Phase 50 trust-surface work per D-03 and roadmap scope. |
</threat_model>

<verification>
mix test test/rendro/pdf/writer_test.exs test/rendro/deterministic_test.exs
</verification>

<success_criteria>
- Each linked block fragment emits one rectangular `/Link` annotation through the existing page annotation seam.
- URI links preserve authored bytes and page links serialize direct page destinations.
- Deterministic renders remain byte-identical and Phase 49 proof stays structural and narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/49-curated-link-annotation-surface/49-03-SUMMARY.md`
</output>
