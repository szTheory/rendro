---
phase: 49
plan: 01
type: execute
wave: 1
depends_on: ["48-02"]
files_modified:
  - lib/rendro/link.ex
  - lib/rendro.ex
  - lib/rendro/block.ex
  - lib/rendro/rules/check_links.ex
  - lib/rendro/pipeline/validate.ex
  - test/rendro_builders_test.exs
  - test/rendro/rules/check_links_test.exs
  - test/rendro/pipeline/validate_test.exs
autonomous: true
requirements:
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - Developers can author links through one explicit `Rendro.link/2` builder instead of hidden attrs or raw annotation dictionaries.
    - External links only accept authored absolute `http` and `https` URIs, and internal links only accept authored page-number destinations.
    - Invalid link targets fail in `Rendro.Pipeline.Validate` with typed tuples before writer serialization begins.
  artifacts:
    - path: lib/rendro/link.ex
      provides: "Curated authored link node carrying wrapped content plus a distinct URI or page target"
    - path: lib/rendro/rules/check_links.ex
      provides: "Validate-stage link semantics for URI policy and page destination resolution"
  key_links:
    - from: lib/rendro.ex
      to: lib/rendro/link.ex
      via: "`Rendro.link/2` wraps one authored block into a narrow link node"
    - from: lib/rendro/pipeline/validate.ex
      to: lib/rendro/rules/check_links.ex
      via: "link invariants join the default validate-stage rule set"
    - from: test/rendro/pipeline/validate_test.exs
      to: lib/rendro/rules/check_links.ex
      via: "aggregate validation errors preserve typed link failure tuples"
---

<objective>
Add the authored link contract and validate-stage enforcement before any measurement or writer work lands.

Purpose: Lock the public API and boundary-validation semantics to the narrow Phase 49 contract.
Output: Explicit link builder, internal link node, and validate-stage rejection of malformed URI or page destinations.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/milestones/v1.9-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/49-curated-link-annotation-surface/49-CONTEXT.md
@.planning/phases/49-curated-link-annotation-surface/49-RESEARCH.md
@.planning/phases/49-curated-link-annotation-surface/49-PATTERNS.md
@.planning/phases/48-embedded-file-core-surface/48-PATTERNS.md
@.planning/phases/48-embedded-file-core-surface/48-VERIFICATION.md
</execution_context>

<context>
@lib/rendro.ex
@lib/rendro/block.ex
@lib/rendro/page.ex
@lib/rendro/pipeline/validate.ex
@test/rendro_builders_test.exs
@test/rendro/pipeline/validate_test.exs

<interfaces>
From `lib/rendro.ex`:
```elixir
@spec block(Text.t() | term(), keyword()) :: Block.t()
def block(content, attrs \\ []) do
  struct!(Block, Keyword.put(attrs, :content, content))
end

@spec form_field(String.t(), String.t(), keyword()) :: Block.t()
def form_field(name, value \\ "", attrs \\ []) do
  ...
end
```

From `lib/rendro/block.ex`:
```elixir
defstruct [
  :content,
  x: 0,
  y: 0,
  width: nil,
  height: nil,
  ...
]
```

From `lib/rendro/pipeline/validate.ex`:
```elixir
@default_rules [CheckReferences, CheckBounds, CheckRequiredKeys, CheckFormFields, CheckEmbeddedFiles]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the explicit curated link builder and node contract</name>
  <files>lib/rendro/link.ex, lib/rendro.ex, lib/rendro/block.ex, test/rendro_builders_test.exs</files>
  <behavior>
    - `Rendro.link/2` accepts exactly one authored `%Rendro.Block{}` and exactly one curated target variant.
    - URI and page destinations are distinct authored variants, not one overloaded `to` string.
    - The returned block preserves caller-owned geometry and layout flags while replacing only `block.content` with the link wrapper.
  </behavior>
  <action>
    Add `lib/rendro/link.ex` as a narrow explicit authored node per D-01, D-02, D-03, and D-10, with a target shape equivalent to `{:uri, binary}` or `{:page, pos_integer}` and no raw action or annotation escape hatch.
    Add `Rendro.link/2` in `lib/rendro.ex` so callers compose links the same way they compose `Rendro.form_field/3`, and reject unsupported option shapes at the builder boundary rather than guessing intent from strings per D-10 and D-17.
    Preserve block-owned geometry per D-04 by requiring the wrapped value to be a `%Rendro.Block{}` and by leaving `x`, `y`, `width`, `height`, and pagination flags on the outer block unchanged; update `Rendro.Block` types to acknowledge the new content struct.
    Add builder tests proving explicit URI/page variants, geometry preservation, and rejection of missing/multiple/unsupported target keys. Do not add hidden `link:` attrs to `Rendro.text/2` or `Rendro.block/2` per D-01 and D-03.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>
  <done>The public API exposes one explicit link builder with distinct URI and page target variants and no generic annotation escape hatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce link semantics in the validate stage</name>
  <files>lib/rendro/rules/check_links.ex, lib/rendro/pipeline/validate.ex, test/rendro/rules/check_links_test.exs, test/rendro/pipeline/validate_test.exs</files>
  <behavior>
    - Absolute `http` and `https` URIs pass unchanged, while malformed, relative, hostless, or unsupported-scheme URIs fail with typed tuples.
    - Page destinations pass only when they target an existing paginated page number.
    - Links wrapping `%Rendro.FormField{}` fail validation so widget and link annotations do not compete on the same authored block.
    - Validate-stage aggregation preserves the exact link error tuples under `%Rendro.Error{details: %{errors: errors}}`.
  </behavior>
  <action>
    Add `Rendro.Rules.CheckLinks` and register it in `Rendro.Pipeline.Validate` per D-11 and D-12, keeping the existing traversal and aggregate error envelope unchanged.
    Validate external links with `URI.new/1` semantics per research and enforce the exact small contract from D-08, D-09, and D-13: allow only absolute `http`/`https` URIs with hosts, preserve authored URI bytes after validation, and reject canonicalization, scheme rewriting, base-URI handling, or viewer-policy-dependent schemes.
    Validate internal links per D-05, D-06, and D-07 by accepting only authored page-number destinations that resolve against `length(doc.pages)` after pagination; reject named destinations, inferred anchors, block IDs, and any implicit target derivation.
    Reject `%Rendro.FormField{}` as wrapped link content in `CheckLinks` so Phase 49 does not claim mixed widget/link hit semantics on the shared `/Annots` seam; cover that policy with both raw rule tests and aggregate validation tests.
    Add raw rule tests plus aggregate validation tests proving typed tuples for unsupported schemes, malformed URIs, out-of-range page destinations, and `%Rendro.FormField{}`-wrapped links without pushing any of those failures into writer code.
  </action>
  <verify>
    <automated>mix test test/rendro/rules/check_links_test.exs test/rendro/pipeline/validate_test.exs</automated>
  </verify>
  <done>Malformed or out-of-scope link targets, including `%Rendro.FormField{}`-wrapped links, fail at the validate stage with typed tuples and without mutating authored target bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User API -> Core | Developer-authored link targets cross into Rendro's trusted authored contract here. |
| Validate -> Writer | Only validated link nodes should reach PDF serialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | `Rendro.link/2` | mitigate | Keep the builder explicit and reject hidden attrs, multiple target variants, and raw action dictionaries per D-01, D-03, and D-10. |
| T-49-02 | Information disclosure | `Rendro.Rules.CheckLinks` | mitigate | Reject `file:`, relative, scheme-relative, and custom-scheme URIs so Rendro does not serialize viewer- or OS-dependent link behaviors per D-08 and D-09. |
| T-49-03 | Integrity | `Rendro.Pipeline.Validate` | mitigate | Resolve page destinations against final paginated page count and fail unresolved targets before writer code per D-05, D-11, and D-12. |
</threat_model>

<verification>
mix test test/rendro_builders_test.exs test/rendro/rules/check_links_test.exs test/rendro/pipeline/validate_test.exs
</verification>

<success_criteria>
- Developers can author explicit URI and page links through `Rendro.link/2`.
- Validate-stage rules reject unsupported URI shapes and invalid page targets with typed tuples.
- Links wrapping `%Rendro.FormField{}` are rejected before render.
- No hidden attrs, generic annotation dictionaries, or inferred destination systems are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/49-curated-link-annotation-surface/49-01-SUMMARY.md`
</output>
