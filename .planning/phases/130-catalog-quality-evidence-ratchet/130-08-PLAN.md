---
phase: 130-catalog-quality-evidence-ratchet
plan: 08
type: execute
wave: 4
depends_on: [130-07]
files_modified:
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/invoice.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/statement.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/receipt_report.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/certificate.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/payslip.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/ticket.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/invoice_dark.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/certificate_dark.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/ticket_dark.png
  - tmp/phase130-launch-reconcile/assets/rendro/gallery/invoice_brand.png
  - tmp/phase130-launch-reconcile/assets/rendro/artifacts.json
  - tmp/phase130-launch-reconcile/assets/rendro/manual.pdf
  - tmp/phase130-launch-reconcile/README.md
  - tmp/phase130-launch-reconcile/guides/recipes.md
  - tmp/phase130-launch-reconcile/guides/theming.md
autonomous: false
requirements: [CATALOG-06, CATALOG-07, CATALOG-08]
must_haves:
  truths:
    - "The sibling launch family is generated and checked with PDFium v0.11.0 / executable SHA b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a using the same absolute binary for both commands."
    - "Exactly ten launch gallery PNGs change, branded_invoice.png is byte-stable, and artifacts.json, manual.pdf, generated README/guides/recipes blocks, and the separate guides/theming.md hash table are synchronized in staging."
    - "The six changed light legacy evidence images invoice, statement, receipt_report, certificate, payslip, and ticket are reviewed sequentially at full size before their six passed records can be rebound or published."
    - "Dark and brand launch rows receive deterministic hash/source synchronization only; they are not described as passed reader-quality evidence."
    - "D-20/D-21: this launch/golden reconciliation never invokes or counts as `mix rendro.catalog.gen`; the later exact twelve-image catalog review and single final canonical catalog generation stay separate."
  artifacts:
    - path: tmp/phase130-launch-reconcile/assets/rendro/artifacts.json
      provides: "Pinned-renderer candidate launch manifest with current source-PDF and PNG identities"
    - path: tmp/phase130-launch-reconcile/assets/rendro/gallery
      provides: "Ten changed launch PNGs plus byte-stable branded_invoice.png"
    - path: .planning/phases/130-catalog-quality-evidence-ratchet/130-08-SUMMARY.md
      provides: "Six complete launch-review records keyed to staged current hashes and bounded deterministic-only rows"
  key_links:
    - from: lib/mix/tasks/rendro/launch_artifacts/gen.ex
      to: tmp/phase130-launch-reconcile/assets/rendro/artifacts.json
      via: "generation inside the detached worktree with one absolute pinned PDFium binary"
      pattern: "launch_artifacts.gen|--pdfium"
    - from: tmp/phase130-launch-reconcile/assets/rendro/artifacts.json
      to: priv/quality/rubric_scores.json
      via: "six light evidence_ref paths and current PNG/source-PDF hashes presented for human reauthorization; no write until Plan 06"
      pattern: "invoice|statement|receipt_report|certificate|payslip|ticket"
    - from: tmp/phase130-launch-reconcile/assets/rendro/artifacts.json
      to: tmp/phase130-launch-reconcile/guides/theming.md
      via: "separately synchronized hard-coded hash table enforced by ThemingClaimsTest"
      pattern: "png_sha256"
  prohibitions:
    - statement: "Generation success MUST NOT be treated as human reauthorization of any legacy passed score."
      status: unverified
      flagged: true
    - statement: "Dark or brand launch rows MUST NOT become passed quality evidence through this reconciliation."
      status: unverified
      flagged: true
    - statement: "Launch reconciliation MUST NOT invoke catalog generation or enter the twelve-image catalog review payload."
      status: unverified
      flagged: true
---

<objective>
Generate, verify, and human-review the downstream launch family while all tracked writes remain staged.

Purpose: make the inherited launch drift reproducible and evidence-safe before catalog candidate generation begins.
Output: one pinned, checked launch candidate and six explicit full-size light-image reauthorization records; no canonical publication.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-catalog-quality-evidence-ratchet/130-07-SUMMARY.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-RESEARCH.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-PATTERNS.md
@lib/rendro/launch_artifacts.ex
@lib/mix/tasks/rendro/launch_artifacts/gen.ex
@lib/mix/tasks/rendro/launch_artifacts/check.ex
@test/rendro/launch_artifacts_test.exs
@test/docs_contract/launch_artifacts_claims_test.exs
@test/docs_contract/theming_claims_test.exs
@test/docs_contract/rubric_manifest_contract_test.exs
@priv/pdfium_pin.json
@priv/quality/rubric_scores.json
@priv/quality/SIGN-OFF.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Supply or trigger the exact pinned-PDFium launch batch</name>
  <precondition>130-07-SUMMARY.md records authorized and assert-green staged golden hashes, and `tmp/phase130-launch-reconcile` remains at its recorded full HEAD SHA.</precondition>
  <files>tmp/phase130-launch-reconcile/assets/rendro/gallery, tmp/phase130-launch-reconcile/assets/rendro/artifacts.json, tmp/phase130-launch-reconcile/assets/rendro/manual.pdf, tmp/phase130-launch-reconcile/README.md, tmp/phase130-launch-reconcile/guides/recipes.md, tmp/phase130-launch-reconcile/guides/theming.md</files>
  <action>Probe for an absolute local `pdfium-cli`, run its version and SHA-256 checks, and accept it only when it is v0.11.0 with SHA `b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a`. If unavailable, stop here for the human to provide that exact binary path or authorize/trigger the exact-full-SHA CI artifact route; record the CI full commit SHA, ref, run/job ID, download identity, pin version, and pin SHA before resuming. Never download an unverified binary or substitute another renderer. With the accepted absolute path, run inside the detached worktree exactly `mix rendro.launch_artifacts.gen --pdfium &lt;absolute-pinned-path&gt;` followed by `mix rendro.launch_artifacts.check --pdfium &lt;same-absolute-pinned-path&gt;`. Manually synchronize only the generated hash table in `guides/theming.md` from the staged `assets/rendro/artifacts.json`, then rerun the check and theming contract.</action>
  <instructions>Provide the absolute local pinned binary path, or authorize the exact-SHA CI run printed by the executor. Return `launch blocked: &lt;reason&gt;` if neither exact route is available.</instructions>
  <what-built>The exact-HEAD detached worktree with two authorized staged golden refs and a bounded launch-generation target.</what-built>
  <how-to-verify>
    1. Confirm PDFium version `v0.11.0` and executable SHA `b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a`.
    2. Confirm generation and check use the identical absolute binary path and exact staged worktree SHA.
    3. If CI is used, confirm the full SHA/ref/run/job and downloaded artifact identity before local staging.
    4. Return `launch blocked: &lt;reason&gt;` on any mismatch or unavailable exact evidence.
  </how-to-verify>
  <resume-signal>Provide `pinned pdfium: &lt;absolute path&gt;`, `authorize exact-SHA launch CI`, or `launch blocked: &lt;reason&gt;`.</resume-signal>
  <verification>The exact PDFium version/SHA and worktree or CI full-SHA provenance are recorded; launch generation/check and focused contracts pass against the same binary.</verification>
  <verify><automated>cd tmp/phase130-launch-reconcile &amp;&amp; mix rendro.launch_artifacts.check --pdfium "$RENDRO_PHASE130_PDFIUM" &amp;&amp; mix test test/rendro/launch_artifacts_test.exs test/docs_contract/launch_artifacts_claims_test.exs test/docs_contract/theming_claims_test.exs --max-failures 1</automated><human-check>Confirm the exact binary or exact-SHA CI provenance; no alternate renderer is acceptable.</human-check></verify>
  <done>One exact-SHA/pin-bound launch generation and check are green inside staging, or the plan stops with a precise unavailable-evidence reason.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the complete staged mechanical batch and evidence fence</name>
  <files>None (read-only staged diff and identity audit)</files>
  <action>Compare the staged worktree to its `.phase130-baseline.json`. Require exactly ten changed gallery PNGs: invoice, statement, receipt_report, certificate, payslip, ticket, invoice_dark, certificate_dark, ticket_dark, and invoice_brand. Require `branded_invoice.png` byte equality. Require current source-PDF and PNG identities for the ten changed rows, current renderer/pin metadata, manual.pdf, artifacts.json, README generated block, guides/recipes generated block, and the separate guides/theming hash table. Reject missing/extra paths, unsafe paths, partial generated blocks, stale hashes, or any diff in source code, catalog assets, rubric/SIGN-OFF, public API, dependencies, recipes, presets, or catalog membership. Record an exact binary diff inventory and rollback command; do not publish or commit the staged batch.</action>
  <verify><automated>cd tmp/phase130-launch-reconcile &amp;&amp; mix rendro.launch_artifacts.check --pdfium "$RENDRO_PHASE130_PDFIUM" &amp;&amp; mix test test/rendro/launch_artifacts_test.exs test/docs_contract/launch_artifacts_claims_test.exs test/docs_contract/theming_claims_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1 &amp;&amp; test "$(git diff --name-only -- assets/rendro/gallery | wc -l | tr -d ' ')" = "10" &amp;&amp; git diff --quiet -- assets/rendro/gallery/branded_invoice.png &amp;&amp; git diff --exit-code -- priv/quality/rubric_scores.json priv/quality/SIGN-OFF.md assets/rendro/catalog.json assets/rendro/catalog mix.exs priv/public_api.json</automated></verify>
  <done>The staged mechanical batch is complete, pinned, exactly scoped, reproducible, and still separated from all human/catalog evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Reauthorize the six changed light launch images at full size</name>
  <precondition>Task 2 records a green pinned launch check, exactly ten changed PNGs, and byte-stable branded_invoice.png with no canonical/evidence publication.</precondition>
  <what-built>Six changed light launch images in the staged worktree, each bound by `assets/rendro/artifacts.json` to current PNG SHA, complete source-PDF SHA, renderer version, and pin SHA.</what-built>
  <how-to-verify>
    1. Inspect at full size, sequentially: invoice.png, statement.png, receipt_report.png, certificate.png, payslip.png, ticket.png.
    2. For each image, record approve/reject, bounded observation, reviewer, date, staged PNG SHA, complete source-PDF SHA, renderer v0.11.0, pin SHA, and exact worktree/CI provenance.
    3. Do not infer approval from generation success or prior `passed: true`; every changed light image needs a new explicit decision.
    4. Treat invoice_dark, certificate_dark, ticket_dark, invoice_brand, and all other non-light rows as deterministic-only synchronization, not passed quality evidence.
    5. Return `launch review blocked: &lt;reason&gt;` for missing/full-size-unavailable/stale/mismatched evidence. Any rejected light row must remain non-passed until separately resolved; automation may not improve it.
  </how-to-verify>
  <resume-signal>Return six complete hash-bound review records, or `launch review blocked: &lt;reason&gt;`.</resume-signal>
  <verify><automated>cd tmp/phase130-launch-reconcile &amp;&amp; test "$(jq '[.gallery[] | select(.id == "invoice" or .id == "statement" or .id == "receipt_report" or .id == "certificate" or .id == "payslip" or .id == "ticket")] | length' assets/rendro/artifacts.json)" = "6" &amp;&amp; mix test test/docs_contract/launch_artifacts_claims_test.exs test/docs_contract/theming_claims_test.exs --max-failures 1</automated><human-check>Six separate full-size decisions with complete current identities are mandatory.</human-check></verify>
  <done>130-08-SUMMARY.md contains exactly six explicit current-image review records; no score, SIGN-OFF, or canonical asset has yet been changed.</done>
</task>

</tasks>

## Mechanical batch isolation justification

The ten changed PNGs, manifest, manual, and generated documentation blocks are one generator-owned sibling family. They are staged in an exact-HEAD detached worktree so the generator cannot leave the main checkout half-updated while human evidence is pending. `branded_invoice.png` is the negative-control byte-stable row. Plan 06 alone publishes the reviewed batch and binds the six legacy records.

## Artifacts this phase produces

Ephemeral staged launch artifacts and six hash-bound human review records in the summary. No public API/dependency/preset/recipe/catalog cell is added. No catalog generator runs. No canonical launch/evidence file is published.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| external PDFium -> staged launch bytes | Renderer version/SHA and exact binary path must match the project pin. |
| generated launch bytes -> legacy passed evidence | Six changed light images require explicit current human reauthorization. |
| staged worktree -> canonical checkout | Publication is deferred until both generation and human evidence are complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-130-08A | Spoofing / Tampering | PDFium binary or CI artifact | high | mitigate | Exact v0.11.0 executable SHA, identical absolute path for gen/check, and full-SHA run provenance. |
| T-130-08B | Tampering | generated mechanical batch | high | mitigate | Exact ten-change set, branded_invoice byte-stable control, synchronized manifest/docs/manual, and full staged check. |
| T-130-08C | Repudiation / Elevation of privilege | legacy passed evidence | high | mitigate | Six independent full-size hash-bound decisions; automation cannot infer or normalize approval. |
| T-130-08D | Tampering | catalog/evidence separation | high | mitigate | Detached staging, main diff fence, no catalog command, and Plan 06-only publication. |
</threat_model>

<verification>
Pinned launch gen/check and launch/docs/rubric contracts pass in staging; the exact staged diff is bounded; six changed light images receive explicit full-size review.
</verification>

<success_criteria>
- Pinned v0.11.0/SHA launch generation and check are green for the exact staged commit.
- Exactly ten launch PNGs change and branded_invoice.png is byte-stable.
- Six changed light images have explicit current full-size records; dark/brand rows remain deterministic-only.
- Main canonical/evidence files and the catalog family remain untouched.
</success_criteria>

<output>
Create `.planning/phases/130-catalog-quality-evidence-ratchet/130-08-SUMMARY.md` with pin/run/diff provenance and six complete review records when done.
</output>
