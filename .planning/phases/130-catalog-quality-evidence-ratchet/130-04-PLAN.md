---
phase: 130-catalog-quality-evidence-ratchet
plan: 04
type: execute
wave: 3
depends_on: [130-03]
files_modified: []
autonomous: false
requirements: [CATALOG-09]
must_haves:
  truths:
    - "D-21: exactly one pinned-PDFium final payload is accepted only from the exact candidate SHA and pin, with twelve full-size page-one images and complete identity manifest."
    - "D-22/D-26: review is sequential at full size with exact instruction `Final human review — inspect this exact light/dark pair; record observed evidence, not an assumed pass.`"
    - "D-23: light passed values use exact integer arithmetic—content_hierarchy 5, every other dimension at least 4, both gates true—and require current hashes, signer/date, supersedes_evidence_ref, and resolution_ref."
    - "D-24: all dark cells remain needs_work while print_safety is false, including improved Humanist dark Receipt."
    - "D-25: missing/incomplete images, stale/mismatched hashes, unavailable/wrong renderer, incomplete scores, or threshold misses fail closed and never become zero or inferred pass."
    - "CATALOG-09 boundary: exact threshold edges pass only when all gates/provenance close; every one-step miss remains needs_work."
    - "CATALOG-09 adjacency: every verdict binds only to its exact catalog ID/mode/PNG/PDF/renderer identity with no cross-cell or cross-pair leakage."
    - "CATALOG-09 empty: absent or incomplete current review evidence fails closed and preserves the prior non-promoted disposition."
    - "CATALOG-09 ordering: reviewer inspects exactly twelve images in canonical family order, light immediately followed by dark."
    - "CATALOG-09 precision: scores are integers, gates booleans, hashes current, signer/date and closure refs concrete; no rounding, averaging, quota, or inferred pass."
    - "UI E2 empty/loading/error/populated/overflow/long-text: review starts only with the complete current twelve-image payload; images are full-size and every identity/scope field stays complete."
    - "UI E3 overflow/long-text: returned records retain full IDs, hashes, renderer identity, bounded justifications, and boundary disclosure without clipping or ambiguous abbreviation."
  artifacts:
    - path: tmp/phase130-review/final
      provides: "Exactly twelve canonical full-size page-one pinned rasters and ordered identity manifest"
    - path: tmp/phase130-review/multipage
      provides: "Separate bounded Invoice/Statement first/final proof"
    - path: .planning/phases/130-catalog-quality-evidence-ratchet/130-04-SUMMARY.md
      provides: "Twelve complete transcribable reviewer records and exact external provenance"
  key_links:
    - from: tmp/phase130-review/final/identity-manifest.json
      to: assets/rendro/catalog.json
      via: "exact ID/path/PNG/PDF identity equality"
      pattern: "png_sha256|source_pdf_sha256"
    - from: .planning/phases/130-catalog-quality-evidence-ratchet/130-04-SUMMARY.md
      to: priv/quality/rubric_scores.json
      via: "Plan 05 exact transcription only after complete human records"
      pattern: "catalog_id"
  prohibitions:
    - statement: "The reviewer MUST NOT optimize scores toward a desired green count, negotiate a miss upward, or treat improvement as automatic promotion."
      status: unverified
      flagged: true
    - statement: "A dark screen-readable result MUST NOT be promoted while print_safety is false or described as print/accessibility/compliance evidence."
      status: unverified
      flagged: true
    - statement: "A partial, stale, mismatched, zoomed-out, or differently rendered payload MUST NOT be accepted as current human evidence."
      status: unverified
      flagged: true
---

<objective>
Obtain one exact pinned payload and one bounded human review for the twelve repaired cells.

Purpose: external rendering and aesthetic judgment are evidence inputs that deterministic automation cannot fabricate.
Output: exact-SHA/pin provenance plus twelve complete hash-bound human score records in the plan summary; no repo evidence edit yet.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-CONTEXT.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-UI-SPEC.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-VALIDATION.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md
@assets/rendro/catalog.json
@priv/quality/rubric_scores.json
@priv/quality/SIGN-OFF.md
@priv/pdfium_pin.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authorize and obtain the exact-SHA pinned-PDFium payload</name>
  <read_first>
    - .planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md — candidate commit and exact 32-cell identities
    - .github/workflows/ci.yml — phase-130 exact-SHA branch route
    - priv/pdfium_pin.json — expected version/SHA
    - assets/rendro/catalog.json — current candidate identities
  </read_first>
  <files>None (external exact-SHA branch/CI action and ephemeral artifact download only)</files>
  <action>Because the pinned Linux PDFium binary is unavailable locally, obtain explicit approval before pushing the narrow `gsd/phase-130-catalog-review-{12-char-sha}` ref. After approval, the executor may push that exact candidate commit, monitor the isolated advisory job, accept only a successful evidence-producing job for the same full SHA, validate the pinned version/executable SHA and identity manifest, download exactly the final twelve-image directory plus separate multipage directory to `tmp/phase130-review`, and record run/job/ref/full-SHA provenance in the summary. Stop on auth, aggregate/job ambiguity, mismatch, absent file, extra final image, or failed evidence step; do not infer success from the overall workflow or change any score.</action>
  <instructions>Authorize the executor to push only the exact immutable candidate ref named above and run/download the existing isolated pinned-PDFium workflow. If repository authentication is unavailable, authenticate in the current CLI session and then resume; do not substitute a different renderer or moving branch.</instructions>
  <verification>The evidence-producing job succeeds for the recorded full SHA and pin, and the downloaded final directory/identity manifest passes the automated cardinality and hash checks below.</verification>
  <what-built>A deterministic current candidate plus an exact CI contract for a single pinned advisory payload.</what-built>
  <how-to-verify>
    1. Approve the exact ref and candidate full SHA named from 130-03-SUMMARY.md; do not approve a moving branch.
    2. Confirm the accepted advisory job itself succeeded and names that same full SHA, run ID, PDFium version, and executable SHA.
    3. Confirm `tmp/phase130-review/final` has twelve PNGs plus one identity manifest in canonical order and `tmp/phase130-review/multipage` is separate.
    4. If any identity is unavailable or mismatched, respond with the exact ID/field and leave review blocked.
  </how-to-verify>
  <acceptance_criteria>
    - One and only one final payload is accepted from the exact candidate SHA and pin.
    - Final payload has exactly twelve page-one PNGs; multipage evidence is outside it.
    - Every manifest entry matches current catalog identity and all provenance is recorded for later cleanup.
  </acceptance_criteria>
  <verify>
    <automated>test "$(find tmp/phase130-review/final -maxdepth 1 -type f -name '*.png' | wc -l | tr -d ' ')" = "12" &amp;&amp; jq -e '.images | length == 12' tmp/phase130-review/final/identity-manifest.json &gt;/dev/null &amp;&amp; mix rendro.catalog.check</automated>
    <human-check>Confirm the exact external branch push/run is authorized, or return `review blocked: external payload not authorized/available`.</human-check>
  </verify>
  <resume-signal>Type `authorize exact-SHA payload` or `review blocked: <reason>`.</resume-signal>
  <done>The exact candidate has one validated pinned payload locally, with external ref/run provenance retained through Plan 05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Review all six light-dark pairs and return exact observed records</name>
  <read_first>
    - tmp/phase130-review/final/identity-manifest.json — canonical identity/order authority
    - assets/rendro/catalog.json — current cell boundary disclosures and hashes
    - priv/quality/rubric_scores.json — frozen dimensions/gates/threshold shape and superseded records
    - priv/quality/SIGN-OFF.md — bounded evidence language
    - priv/examples/invoice/DOMAIN.md
    - priv/examples/statement/DOMAIN.md
    - priv/examples/receipt/DOMAIN.md
    - priv/examples/certificate/DOMAIN.md
    - priv/examples/payslip/DOMAIN.md
    - priv/examples/ticket/DOMAIN.md
  </read_first>
  <files>None (read-only full-size review; records are returned into 130-04-SUMMARY.md)</files>
  <action>Present every final PNG individually at 100% or larger in the exact D-21 order, never using a contact sheet as the inspection surface. Use the exact D-26 final-review instruction and current-identity label. For each artifact record six integer dimension scores, reading_order and print_safety booleans, concise observed justifications, reviewer/date, exact PNG/PDF hashes, renderer identity, and the prior evidence plus concrete resolution references. Mechanically derive light eligibility using the frozen D-23 rule with no rounding or quota. Retain every threshold miss as needs_work and every dark record as needs_work while print_safety is false. Inspect the separate multipage proof only as a bounded pagination observation, not part of the twelve score records.</action>
  <what-built>Six repaired recipe pairs, deterministic current 32-cell state, and one exact pinned twelve-image payload.</what-built>
  <how-to-verify>
    1. Inspect Corporate-Classic Invoice light/dark, Minimal-Mono Statement light/dark, Humanist Receipt light/dark, Editorial Certificate light/dark, Swiss Payslip light/dark, and Brutalist Ticket light/dark at full readable size.
    2. Confirm the locked family hierarchy and Humanist dark Receipt treatment while preserving complete text, money, geometry, and dark boundary disclosure.
    3. Return all required scalar fields for each exact manifest identity; do not omit a failed dimension or replace it with prose.
    4. Recompute passed exactly; every one-step miss and every dark false print-safety gate remains needs_work.
    5. Return `review blocked: <exact IDs/fields>` if the set, identity, renderer, or evidence is incomplete.
  </how-to-verify>
  <acceptance_criteria>
    - Exactly twelve complete records are returned in canonical order and bind one-to-one to the manifest.
    - Every record has exact integer/boolean/current-identity/provenance/justification fields; no inferred or rounded value exists.
    - Light promotion candidates satisfy every frozen threshold and closure field; all misses and all dark cells remain needs_work.
    - Review claims remain exact-artifact, full-size, screen-oriented, and non-compliance-bounded.
  </acceptance_criteria>
  <verify>
    <automated>test "$(find tmp/phase130-review/final -maxdepth 1 -type f -name '*.png' | wc -l | tr -d ' ')" = "12" &amp;&amp; jq -e '.images | length == 12 and ([.images[].catalog_id] | unique | length == 12)' tmp/phase130-review/final/identity-manifest.json &gt;/dev/null</automated>
    <human-check>Return twelve complete observed score records and the bounded multipage verdict, or exact blocking IDs/reasons.</human-check>
  </verify>
  <resume-signal>Provide all twelve structured records and the separate multipage verdict; use `review blocked: <reason>` on any stop condition.</resume-signal>
  <done>130-04-SUMMARY.md contains twelve complete transcribable current-identity records, final renderer/ref provenance, and no assumption that improvement equals pass.</done>
</task>

</tasks>

## Artifacts this phase produces

Committed files, public symbols, API fields, schema fields, and flags: none.

Ephemeral artifacts: `tmp/phase130-review/final/{12 PNGs,identity-manifest.json}` and separate `tmp/phase130-review/multipage/` proof. Summary fields: exact ref/SHA/run/job/pin provenance, twelve human score records, and bounded multipage verdict.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local candidate → external CI/ref | External execution must bind to one immutable full SHA and pinned renderer. |
| pinned images → human judgment | Reproducible bytes still require bounded full-size interpretation. |
| human response → durable evidence | Plan 05 may transcribe only complete exact-ID records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-130-04A | Tampering | external artifact identity | high | mitigate | Exact ref/full SHA/run/job/pin plus per-image PNG/PDF joins; mismatch blocks. |
| T-130-04B | Repudiation | human score provenance | high | mitigate | Reviewer/date, exact artifact identities, six integers, two booleans, justifications, and closure refs are mandatory. |
| T-130-04C | Elevation of privilege | automation/human authority | high | mitigate | CI produces bytes only; checkpoint supplies observation; Plan 05 alone transcribes and derives projection. |
</threat_model>

<verification>
Automated checks prove exact input cardinality/identity; human review supplies the intentionally non-automated judgments. Neither substitutes for the other.
</verification>

<success_criteria>
- One exact pinned payload is provenance-valid and locally available.
- All twelve cells receive complete current human observations without score inflation or dark promotion.
- Any stop condition blocks Plan 05 and routes back to the affected evidence checkpoint.
</success_criteria>

<output>
Create `.planning/phases/130-catalog-quality-evidence-ratchet/130-04-SUMMARY.md` only with complete provenance and all twelve records.
</output>
