---
phase: 130-catalog-quality-evidence-ratchet
plan: 04
type: execute
wave: 3
depends_on: [130-03]
files_modified:
  - dev/rendro/catalog.ex
  - dev/mix/tasks/rendro/catalog/candidate.ex
  - test/rendro/catalog_test.exs
  - test/docs_contract/catalog_quality_contract_test.exs
autonomous: false
requirements: [CATALOG-08, CATALOG-09]
must_haves:
  truths:
    - "D-20/D-21: one candidate-only 32-cell bundle is rendered after all six recipe suites stabilize, outside assets/rendro and without invoking canonical quality projection."
    - "The candidate manifest binds literal order, current PNG/PDF hashes, dimensions/page counts, renderer version/pin SHA, baseline commit SHA, and an actual-diff partition of changed-scored, changed-unscored, and byte-stable IDs."
    - "Stale scored bindings are represented as review_required in candidate metadata only; their old scores/sign-off are neither copied as current nor mutated."
    - "Actual changed unscored rows are enumerated but remain untouched until final transcription; byte-stable rows are never rebound."
    - "Candidate generation writes only under tmp/phase130-candidate, stages atomically, cleans the exact staging/target paths on failure, and leaves canonical PNGs/catalog.json/rubric/SIGN-OFF byte-identical; canonical generate/1 also stages all PNGs until quality joins succeed."
    - "D-25/D-26: missing baseline identity, duplicate/missing cell, unsafe path, renderer mismatch, partial write, or source drift fails closed and emits the evidence-failure state."
    - "CATALOG-08: the fixed 32 IDs/order, artifact hashes, schema-relevant shape, dimensions, page counts, safe paths, and coverage are proven before review."
  artifacts:
    - path: dev/rendro/catalog.ex
      provides: "Private dev-only candidate generation and validation seam separate from canonical generate/1"
    - path: dev/mix/tasks/rendro/catalog/candidate.ex
      provides: "Narrow `mix rendro.catalog.candidate` entry point with fixed candidate target"
    - path: tmp/phase130-candidate/candidate-manifest.json
      provides: "Ephemeral current 32-cell identity bundle and actual-diff classification"
  key_links:
    - from: dev/mix/tasks/rendro/catalog/candidate.ex
      to: dev/rendro/catalog.ex
      via: "candidate-only function; no force/bypass option"
      pattern: "generate_candidate"
    - from: tmp/phase130-candidate/candidate-manifest.json
      to: assets/rendro/catalog.json
      via: "ID-aligned baseline diff without canonical writes"
      pattern: "changed_scored|changed_unscored|byte_stable"
    - from: tmp/phase130-candidate/candidate-manifest.json
      to: priv/quality/rubric_scores.json
      via: "classification only; score values are never projected into candidate cells"
      pattern: "review_required|review_status"
  prohibitions:
    - statement: "Candidate generation MUST NOT expose a public API, mutate canonical assets/evidence, project stale scores as current, or promote a candidate."
      status: unverified
      flagged: true
    - statement: "A broad force flag, generic validation bypass, partial output, or arbitrary output root MUST NOT be introduced."
      status: unverified
      flagged: true
---

<objective>
Create and execute the private candidate-only generation seam as one isolated 32-cell mechanical batch.

Purpose: break the stale-scored-binding deadlock without weakening canonical generation or evidence authority.
Output: one validated ephemeral candidate bundle, exact actual-diff inventory, and a deterministic pre-review gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-CONTEXT.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-RESEARCH.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md
@dev/rendro/catalog.ex
@dev/mix/tasks/rendro/catalog/gen.ex
@dev/mix/tasks/rendro/catalog/check.ex
@assets/rendro/catalog.json
@priv/quality/rubric_scores.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate one candidate pair without touching canonical or reviewer-owned state</name>
  <reversibility rating="costly">The dev-only candidate contract controls evidence identity for this review, but can be removed after the phase without runtime migration.</reversibility>
  <read_first>
    - dev/rendro/catalog.ex — generate/1, build_cells/1, build_manifest/2, apply_quality_projections/2, static/artifact checks
    - dev/mix/tasks/rendro/catalog/gen.ex — strict option parsing and writer behavior
    - test/rendro/catalog_test.exs — literal registry, path, artifact, and read-only contracts
    - test/docs_contract/catalog_quality_contract_test.exs — stale scored/unscored evidence behavior
  </read_first>
  <files>dev/rendro/catalog.ex, dev/mix/tasks/rendro/catalog/candidate.ex, test/rendro/catalog_test.exs, test/docs_contract/catalog_quality_contract_test.exs</files>
  <behavior>
    - "The Corporate-Classic Invoice pair renders into a staging directory and its candidate records carry exact current PNG/PDF/renderer identity with no quality projection."
    - "Changed scored rows are marked review_required with prior and candidate hashes but no inherited current score/verdict."
    - "A failed render/identity check removes the exact staging and candidate roots and leaves canonical/evidence bytes unchanged."
    - "Expansion uses the same path for all 32 literal cells and writes the manifest only after validation succeeds."
  </behavior>
  <action>Write candidate and failed-canonical-write contract tests red first. Refactor the private cell builder to accept an explicit internal artifact root. Make canonical generate/1 render all 32 PNGs into an exact staging root, run apply_quality_projections/2 and all pre-publication identity checks there, then atomically publish PNGs/catalog.json only after the quality join succeeds; stale rubric failure must leave committed canonical bytes untouched. Add dev-only `generate_candidate/1` behind `mix rendro.catalog.candidate`, fixed to `tmp/phase130-candidate`, with an exact sibling staging directory and atomic final rename. Candidate cells never call apply_quality_projections/2. Derive an ordered diff against the committed canonical manifest and classify IDs by actual hash equality plus existing review_status: changed scored becomes review_required metadata, changed unscored becomes changed_unscored metadata, and stable rows remain byte_stable; omit dimension scores, passed, sign-off, and public quality from candidate cells. Require a valid literal 32-cell baseline, bind renderer kind/version/pin SHA and commit SHA, validate paths/count/order/hashes/dimensions/page counts before publish, and clean only exact staging/target roots on failure. The candidate task accepts only the existing optional `--pdfium` argument; add no force, destination, skip, or bypass switch.</action>
  <verify><automated>mix test test/rendro/catalog_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted dev/rendro/catalog.ex dev/mix/tasks/rendro/catalog/candidate.ex test/rendro/catalog_test.exs test/docs_contract/catalog_quality_contract_test.exs</automated></verify>
  <done>The dev-only seam produces a validated current candidate, and both candidate failure and canonical stale-join failure leave canonical/reviewer-owned bytes unchanged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Authorize the exact-SHA pinned 32-cell candidate batch</name>
  <read_first>
    - .planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md — exact route/ref grammar
    - .github/workflows/ci.yml — candidate generation and upload job
    - priv/pdfium_pin.json — required pin
  </read_first>
  <files>None (external exact-SHA ref/CI action and ephemeral artifact download)</files>
  <action>Because pinned Linux PDFium is unavailable locally, obtain approval before pushing exactly `gsd/phase-130-catalog-review-&lt;full 40-lowercase-hex candidate commit SHA&gt;`. After approval, push only that exact ref, monitor the evidence-producing job, require ref suffix equality to GITHUB_SHA and the pinned executable SHA, then download the single candidate bundle to `tmp/phase130-candidate` and the separated review outputs to `tmp/phase130-review`. Stop on auth, SHA/ref mismatch, aggregate/job ambiguity, failed generation, missing/extra file, or pin mismatch.</action>
  <instructions>Authorize only the exact full-SHA ref printed by the executor. If repository authentication is unavailable, authenticate in the current CLI session and resume; do not substitute another renderer, abbreviated SHA, or moving branch.</instructions>
  <verification>The accepted evidence-producing job succeeds for the recorded full SHA and pin, and the downloaded candidate/final/multipage directories satisfy the automated count and identity checks.</verification>
  <what-built>A tested candidate-only generator and full-SHA-bound isolated advisory route.</what-built>
  <how-to-verify>
    1. Approve only the full 40-character SHA ref printed by the executor.
    2. Confirm the accepted job names the same full SHA, run ID, renderer version, and executable pin SHA.
    3. Confirm the downloaded candidate contains one manifest and exactly 32 candidate PNGs; final review contains exactly twelve PNGs and multipage proof is separate.
    4. Return `review blocked: &lt;reason&gt;` for any mismatch or unavailable evidence.
  </how-to-verify>
  <resume-signal>Type `authorize exact-SHA candidate` or `review blocked: &lt;reason&gt;`.</resume-signal>
  <verify>
    <automated>test "$(find tmp/phase130-candidate/catalog -type f -name '*.png' | wc -l | tr -d ' ')" = "32" &amp;&amp; jq -e '.cells | length == 32 and .diff.changed_scored and .diff.changed_unscored and .diff.byte_stable' tmp/phase130-candidate/candidate-manifest.json &gt;/dev/null &amp;&amp; test "$(find tmp/phase130-review/final -maxdepth 1 -type f -name '*.png' | wc -l | tr -d ' ')" = "12"</automated>
    <human-check>Authorize the exact ref or report the exact blocking reason.</human-check>
  </verify>
  <done>One and only one full-SHA/pin-bound 32-cell candidate batch and its separated review payload are locally available.</done>
</task>

<task type="auto">
  <name>Task 3: Gate the candidate and record the authoritative actual diff</name>
  <read_first>
    - tmp/phase130-candidate/candidate-manifest.json — current identity/diff authority
    - assets/rendro/catalog.json — unchanged canonical baseline
    - priv/quality/rubric_scores.json — unchanged reviewer-owned baseline
    - priv/quality/SIGN-OFF.md — unchanged prior evidence
  </read_first>
  <files>None (read-only candidate gate and summary evidence)</files>
  <action>Run all six focused family suites and candidate validation. Prove exact 32 count/order, safe candidate-relative paths, current lowercase SHA-256 values, renderer/pin/commit identity, page/dimension coverage, exact diff partition, unchanged no-theme identities, and byte-for-byte unchanged canonical catalog PNGs/catalog.json/rubric/SIGN-OFF from the pre-run snapshot. Enumerate every changed scored and changed unscored ID from the actual manifest; do not use the research prediction. Record `Candidate catalog state — deterministic checks passed; advisory review pending.` only after all checks pass. On failure remove the exact candidate/review temp roots and leave canonical/evidence state untouched.</action>
  <verify><automated>mix test test/rendro/recipes/{receipt,invoice,statement,certificate,payslip,ticket}_test.exs test/rendro/recipes/{receipt,invoice,statement,certificate,payslip,ticket}_typography_test.exs test/rendro/recipes/{receipt,invoice,statement,certificate,payslip,ticket}_byte_identity_test.exs --max-failures 1 &amp;&amp; mix test test/rendro/catalog_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1 &amp;&amp; jq -e '.cells | length == 32 and ((.diff.changed_scored + .diff.changed_unscored + .diff.byte_stable) | length == 32)' tmp/phase130-candidate/candidate-manifest.json &gt;/dev/null</automated></verify>
  <done>The candidate is deterministic and reviewable, with stale scored bindings explicit, actual unscored changes enumerated, and canonical state untouched.</done>
</task>

</tasks>

## Mechanical batch isolation justification

The 32 PNGs are one mechanically generated, ephemeral atomic bundle under a single fixed temp root; they are not hand-edited files and cannot be split without invalidating the literal-order/hash-set contract. The executor must review `candidate-manifest.json`, verify exactly 32 paths and the three-way diff partition, run `git diff --exit-code -- assets/rendro/catalog.json assets/rendro/catalog priv/quality/rubric_scores.json priv/quality/SIGN-OFF.md`, and reject any canonical/evidence diff before review.

## Artifacts this phase produces

New public/runtime symbols, dependencies, recipes, presets, catalog entries, or schema fields: none. New dev-only task: `mix rendro.catalog.candidate`. Ephemeral: one fixed-root 32-PNG candidate bundle, its candidate-only manifest, twelve-image review payload, and separate multipage proof.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| candidate renderer → temp filesystem | All writes must remain in the exact candidate root and publish atomically. |
| candidate identities → stale rubric | Diff classification may describe staleness but has no score authority. |
| exact git ref → external pinned job | Full SHA and renderer pin bind the mechanical batch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-130-04A | Tampering / Information disclosure | candidate paths | high | mitigate | Fixed safe-relative root, staging, atomic rename, exact-root cleanup, and tests rejecting traversal. |
| T-130-04B | Elevation of privilege | stale scored projection | high | mitigate | Candidate cells omit quality; changed scored rows are review_required metadata only. |
| T-130-04C | Repudiation | actual-diff classification | high | mitigate | Ordered ID-aligned baseline/candidate hash comparison and complete three-way partition. |
| T-130-04D | Tampering | canonical/evidence state | high | mitigate | Before/after snapshots plus staged canonical publication prove candidate execution and stale quality joins cannot partially overwrite canonical or reviewer-owned files. |
</threat_model>

<verification>
Task 1 proves the private seam, Task 2 obtains the pinned mechanical batch, and Task 3 verifies the whole atomic bundle and unchanged canonical state.
</verification>

<success_criteria>
- Exactly one current candidate bundle exists outside canonical assets.
- Stale scored evidence is explicit but never projected or mutated.
- Actual changed unscored IDs are authoritative and byte-stable rows remain untouched.
- Failure cleanup is deterministic and canonical/reviewer-owned state is unchanged.
</success_criteria>

<output>
Create `.planning/phases/130-catalog-quality-evidence-ratchet/130-04-SUMMARY.md` with exact candidate/ref/run/pin/diff evidence when done.
</output>
