---
phase: 130-catalog-quality-evidence-ratchet
plan: 03
type: execute
wave: 2
depends_on: [130-01, 130-02]
files_modified:
  - test/rendro/catalog_raster_review_test.exs
  - test/guardrails/required_checks_contract_test.exs
  - .github/workflows/ci.yml
  - assets/rendro/catalog.json
  - assets/rendro/catalog/invoice
  - assets/rendro/catalog/statement
  - assets/rendro/catalog/receipt
  - assets/rendro/catalog/certificate
  - assets/rendro/catalog/payslip
  - assets/rendro/catalog/ticket
  - priv/quality/rubric_scores.json
  - test/rendro/catalog_test.exs
  - test/docs_contract/catalog_quality_contract_test.exs
  - test/docs_contract/rubric_manifest_contract_test.exs
autonomous: true
requirements: [CATALOG-08, CATALOG-09]
must_haves:
  truths:
    - "D-19: all six family suites are green before artifact generation; no interim check has edited scores, SIGN-OFF, passed, dispositions, or public quality projection."
    - "D-20: one complete generation produces the literal ordered 32-cell catalog, and only actually changed unscored cells receive an explicit current PNG/PDF hash/date/reason rebind without scores."
    - "D-21: the final advisory route is a separately named exact twelve-image page-one payload in canonical light-then-dark family order with renderer version/SHA, commit/run identity, catalog ID, PNG SHA, and complete source-PDF SHA."
    - "D-21: Invoice/Statement first/final multipage proof remains in a separate directory/artifact and cannot enter or satisfy the twelve-image review payload."
    - "D-25/D-26: deterministic failure, missing pair, stale hash, or renderer mismatch stops before review and exposes exact candidate/evidence-failure copy rather than changing a verdict."
    - "CATALOG-08 fixed-32 contract: exact artifact paths, literal ordering, SHA-256 identities, schema, dimensions, page counts, quality join, and coverage are current after generation; missing or extra cells fail."
    - statement: "CATALOG-08 unclassified: the fixed 32-cell artifact/hash/schema/coverage behavior is manually covered by literal registry, generation-diff, catalog checker, and mutation contracts; verifier must confirm this exhausts the unclassified probe."
      verification: backstop
    - "CATALOG-09 ordering: final review payload order is exactly Corporate-Classic Invoice light/dark, Minimal-Mono Statement light/dark, Humanist Receipt light/dark, Editorial Certificate light/dark, Swiss Payslip light/dark, Brutalist Ticket light/dark."
    - "UI E2 empty: an absent image or incomplete twelve-image identity set stops fail-closed and cannot promote a disposition."
    - "UI E2 loading: there is no partially reviewable state; review begins only after the complete pinned payload and identity manifest exist."
    - "UI E2 error: missing, changed, stale, unavailable, or renderer-mismatched evidence emits the exact failure state and retains non-promoted dispositions."
    - "UI E2 populated: exactly twelve full-size page-one rasters appear sequentially in canonical light-then-dark family order; a contact sheet is index-only."
    - "UI E2 overflow: full-size images are preserved; scaled sheets cannot substitute for inspection."
    - "UI E2 long-text: IDs, renderer version/SHA, PNG/PDF hashes, justifications, and boundary disclosures remain complete and directly traceable."
    - "UI E3 overflow: identity records expand/wrap without clipping machine- or human-readable fields."
    - "UI E3 long-text: identity, justification, and scope-limiting disclosure are never truncated into ambiguity."
  assumptions:
    - statement: "CATALOG-08 unclassified probe remains explicitly flagged until the verifier confirms the fixed-32 artifact/hash/schema/coverage contracts exhaust it."
      status: unverified
      flagged: true
  artifacts:
    - path: test/rendro/catalog_raster_review_test.exs
      provides: "Separated exact twelve-image final payload and bounded multipage proof contracts"
    - path: .github/workflows/ci.yml
      provides: "Narrow phase-130 exact-SHA pinned-PDFium staging route, separate from required deterministic CI"
    - path: assets/rendro/catalog.json
      provides: "Current literal ordered 32-cell identities after the single complete artifact generation"
    - path: priv/quality/rubric_scores.json
      provides: "Current reasoned-unscored rebinds only for actually changed unscored cells"
  key_links:
    - from: test/rendro/catalog_raster_review_test.exs
      to: assets/rendro/catalog.json
      via: "literal canonical IDs and exact PNG/source-PDF SHA assertions"
      pattern: "@flagship_ids|png_sha256|source_pdf_sha256"
    - from: priv/quality/rubric_scores.json
      to: assets/rendro/catalog.json
      via: "one-to-one current identity join checked by Rendro.Catalog"
      pattern: "catalog_dispositions"
    - from: .github/workflows/ci.yml
      to: priv/pdfium_pin.json
      via: "pinned version and executable SHA captured in identity manifest"
      pattern: "pdfium"
  prohibitions:
    - statement: "Generation or CI automation MUST NOT smuggle a human-quality approval, edit observed scores, or reinterpret an identity rebind as a visual verdict."
      status: unverified
      flagged: true
    - statement: "A contact sheet, partial directory, unpinned renderer, stale hash, or mixed multipage payload MUST NOT be presented as the final review surface."
      status: unverified
      flagged: true
    - statement: "Artifact regeneration MUST NOT widen the catalog, public API, package payload, recipe/preset set, or dark-output claims."
      status: unverified
      flagged: true
---

<objective>
Stabilize the exact evidence payload, regenerate the fixed catalog once, and establish a current deterministic candidate state.

Purpose: no reviewer should see stale, partial, mixed, or automation-approved evidence.
Output: separated payload contracts, current 32-cell artifacts, explicit actual-diff unscored rebinds, and a green deterministic gate ready for the external pinned run.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-CONTEXT.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-UI-SPEC.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-RESEARCH.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-PATTERNS.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-VALIDATION.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-01-SUMMARY.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-02-SUMMARY.md
@test/rendro/catalog_raster_review_test.exs
@dev/rendro/catalog.ex
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one canonical review pair through the separated pinned payload</name>
  <reversibility rating="costly">The CI artifact contract is consumed by the final human review, but remains isolated and can be changed without public/runtime migration.</reversibility>
  <read_first>
    - test/rendro/catalog_raster_review_test.exs — @flagship_ids, current mixed 16-file writer, per-ID hash assertions
    - .github/workflows/ci.yml — phase-127 branch gate, pinned install, staging, upload, required catalog check
    - test/guardrails/required_checks_contract_test.exs — deterministic/advisory topology guards
    - priv/pdfium_pin.json — exact renderer version and SHA
    - assets/rendro/catalog.json — canonical current cell identities/order
  </read_first>
  <files>test/rendro/catalog_raster_review_test.exs, test/guardrails/required_checks_contract_test.exs, .github/workflows/ci.yml</files>
  <behavior>
    - "The first Corporate-Classic Invoice light/dark pair is rerendered by the pin, hash-matched to catalog PNG/source PDF identities, and staged in literal order."
    - "Expansion to the remaining five pairs yields exactly twelve page-one images and a complete ordered identity manifest."
    - "The four Invoice/Statement first/final proof files are emitted under a distinct multipage path and are excluded from final-review cardinality."
    - "The advisory job remains graph-disconnected from required deterministic authority and accepts only an immutable-SHA phase-130 branch pattern."
  </behavior>
  <action>Split the current raster test into a final-review directory containing only the literal twelve D-21 page-one IDs and a separately named multipage proof directory/test. Start with the Invoice light/dark end-to-end assertion, then expand the literal list; write a machine-readable manifest in review order with catalog ID, mode, image path, PNG SHA, complete source-PDF SHA, renderer version/SHA, and available commit/run identity. Add guards for exact counts/order, directory separation, safe relative paths, hash equality, missing/extra files, and D-25 renderer mismatch. Narrow the CI route to phase-130 immutable-SHA refs, keep required mix rendro.catalog.check independent, and stage the D-26 candidate/failure copy without any score mutation.</action>
  <acceptance_criteria>
    - Review output contains exactly twelve canonical page-one PNGs plus one complete ordered identity manifest; multipage proof is elsewhere.
    - Every entry binds the exact catalog ID/mode/path/PNG/PDF/renderer identity and fails on absence, addition, reorder, unsafe path, or mismatch.
    - CI topology proves advisory output cannot become required deterministic merge authority or edit quality records.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted test/rendro/catalog_raster_review_test.exs test/guardrails/required_checks_contract_test.exs &amp;&amp; mix test test/guardrails/required_checks_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The final advisory contract is exact, identity-bound, separate from multipage proof, and ready for one pinned execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate the fixed 32 cells once and rebind only actual changed unscored identities</name>
  <reversibility rating="costly">D-20 refreshes committed artifact hashes consumed by documentation and Phase 131, but old identities remain recoverable from git.</reversibility>
  <read_first>
    - dev/rendro/catalog.ex — @catalog_specs, generate/1, check/1, quality/disposition errors, projection arithmetic
    - dev/mix/tasks/rendro/catalog/gen.ex — sole writer command
    - dev/mix/tasks/rendro/catalog/check.ex — read-only checker
    - assets/rendro/catalog.json — pre-generation identity baseline
    - priv/quality/rubric_scores.json — scored and reasoned-unscored bindings
    - test/rendro/catalog_test.exs — exact count/order/hash coverage
    - test/docs_contract/catalog_quality_contract_test.exs — stale/unscored contracts
    - test/docs_contract/rubric_manifest_contract_test.exs — threshold/schema mutation contracts
  </read_first>
  <files>assets/rendro/catalog.json, assets/rendro/catalog/invoice, assets/rendro/catalog/statement, assets/rendro/catalog/receipt, assets/rendro/catalog/certificate, assets/rendro/catalog/payslip, assets/rendro/catalog/ticket, priv/quality/rubric_scores.json, test/rendro/catalog_test.exs, test/docs_contract/catalog_quality_contract_test.exs, test/docs_contract/rubric_manifest_contract_test.exs</files>
  <precondition>Both 130-01-SUMMARY.md and 130-02-SUMMARY.md record all six focused three-file family suites green with no rubric_scores.json, SIGN-OFF.md, or catalog artifact edit.</precondition>
  <behavior>
    - "One mix rendro.catalog.gen invocation produces exactly 32 cells in unchanged literal order."
    - "The executor computes the before/after identity diff and enumerates exact changed scored and unscored IDs; it does not assume the research-predicted set."
    - "Only actually changed unscored rows receive current evidence_ref, PNG/PDF SHA, date, and concrete mechanical-rebind reason while staying review_status unscored."
    - "All twelve scored target rows remain non-promoted and review-pending until Plan 04 supplies current human evidence."
  </behavior>
  <action>Capture the pre-generation 32-cell ID/path/PNG/PDF identity table, run the sole D-20 full catalog generation exactly once, and compute the exact diff. Add or strengthen the reasoned-unscored contract only if needed, then update each and only each mechanically changed unscored disposition with current hashes/date and a concrete recipe-change reason while retaining unscored status. Do not mass-rebind byte-stable rows, change scores/passed/SIGN-OFF, alter registry/order/schema/thresholds, or generate a review verdict. Preserve safe-relative artifact paths and record the exact changed-ID sets in the summary.</action>
  <acceptance_criteria>
    - Generated catalog has the same 32 IDs/order/paths/page geometries and current SHA-256 identities.
    - Summary contains an actual-diff table separating changed scored, changed unscored, and byte-stable IDs.
    - Every changed unscored row is current and reasoned; no byte-stable or scored row receives invented evidence or a status change.
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.catalog.check &amp;&amp; mix test test/rendro/catalog_test.exs test/docs_contract/catalog_quality_contract_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The fixed catalog is current after one full generation and every non-human identity update is explicit, minimal, and non-promotional.</done>
</task>

<task type="auto">
  <name>Task 3: Gate the candidate state before any pinned or human review</name>
  <reversibility rating="reversible">This task performs read-only verification and records command evidence; rerunning or replacing the checks has no migration cost.</reversibility>
  <read_first>
    - assets/rendro/catalog.json — generated identities and quality projection
    - priv/quality/rubric_scores.json — current one-to-one bindings
    - priv/quality/SIGN-OFF.md — must still contain prior evidence only
    - .planning/phases/130-catalog-quality-evidence-ratchet/130-01-SUMMARY.md — first three family checkpoints
    - .planning/phases/130-catalog-quality-evidence-ratchet/130-02-SUMMARY.md — remaining family checkpoints
    - .planning/phases/130-catalog-quality-evidence-ratchet/130-VALIDATION.md — required deterministic map
  </read_first>
  <files>None (read-only deterministic candidate gate)</files>
  <action>Run the six families' unit/typography/byte suites, exact catalog checker, catalog/schema/docs contracts, and full deterministic suite. Assert literal 32 count/order, safe-relative paths, current lowercase SHA-256 values, exact one-to-one disposition joins, unchanged threshold arithmetic, dark print_safety false/boundary disclosure, unchanged no-theme identities, and no new public API/dependency/recipe/preset/catalog entry. Record the exact D-26 label `Candidate catalog state — deterministic checks passed; advisory review pending.` only after all commands pass; on any miss emit the approved evidence-failure copy and return to the owning family or Task 2 without editing review evidence.</action>
  <acceptance_criteria>
    - Focused, catalog, schema, docs-contract, and full deterministic commands pass against the same candidate identities.
    - SIGN-OFF and scored verdicts remain prior evidence; candidate state is explicitly pending advisory review.
    - No stale/missing/mismatched identity, cardinality drift, public-surface drift, or unsupported claim remains.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes/{receipt,invoice,statement,certificate,payslip,ticket}_test.exs test/rendro/recipes/{receipt,invoice,statement,certificate,payslip,ticket}_typography_test.exs test/rendro/recipes/{receipt,invoice,statement,certificate,payslip,ticket}_byte_identity_test.exs --max-failures 1 &amp;&amp; mix rendro.catalog.check &amp;&amp; mix test test/rendro/catalog_test.exs test/docs_contract/catalog_quality_contract_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1 &amp;&amp; mix test --exclude quarantine --slowest 10</automated>
  </verify>
  <done>A deterministic, current, review-pending candidate state is proven before external evidence begins.</done>
</task>

</tasks>

## Artifacts this phase produces

New public symbols, APIs, dependencies, recipes, presets, catalog entries, schema fields, and runtime flags: none.

New/changed test and CI contract: separate final-review and multipage output paths; an ephemeral ordered identity manifest containing existing identity fields plus CI commit/run and pinned renderer provenance; a narrow phase-130 immutable-SHA CI branch selector.

Generated committed artifacts: `assets/rendro/catalog.json` and the existing 32 PNG paths under the six listed family directories. Reviewer source: current reasoned-unscored rows in `priv/quality/rubric_scores.json` only where the actual diff requires rebind.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| literal registry → fixture/artifact filesystem | Existing safe-relative paths constrain reads/writes to intended roots. |
| generated PDF/PNG → manifest SHA-256 | Current bytes must bind to exact paths and IDs without stale inheritance. |
| deterministic candidate → pinned advisory job | Renderer/commit/run identity is external evidence and cannot replace merge authority. |
| generator → reviewer-owned rubric | Mechanical identity refresh has no authority to author human scores. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-130-03A | Tampering / Information disclosure | artifact path resolution | high | mitigate | Preserve literal registry and Path.safe_relative/1; tests reject unsafe/missing/extra paths. |
| T-130-03B | Tampering / Repudiation | PNG/PDF/renderer identity | high | mitigate | Join exact catalog ID, both SHA-256 hashes, pin version/SHA, commit/run, and fail closed on mismatch. |
| T-130-03C | Repudiation | automated quality projection | high | mitigate | Generator only derives projections; scored fields/SIGN-OFF excluded from generation task and checked unchanged. |
| T-130-03D | Denial of service | fixed payload/cardinality | low | accept | Deliberately fixed 32 catalog cells and 12 review images keep work bounded; missing targets halt rather than shrink scope. |
</threat_model>

<verification>
The final Task 3 command is the deterministic merge-authority gate. No pinned-PDFium or human result is inferred from it.
</verification>

<success_criteria>
- The one complete artifact generation and actual-diff rebind are auditable.
- The current candidate passes deterministic artifact/hash/schema/coverage checks.
- The exact twelve-image advisory payload contract is separate from multipage proof and from human authority.
</success_criteria>

<output>
Create `.planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md` with the exact diff and candidate identities when done.
</output>
