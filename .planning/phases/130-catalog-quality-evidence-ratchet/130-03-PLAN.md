---
phase: 130-catalog-quality-evidence-ratchet
plan: 03
type: execute
wave: 2
depends_on: [130-01, 130-02]
files_modified:
  - dev/rendro/catalog_review_payload.ex
  - test/rendro/catalog_review_payload_contract_test.exs
  - test/rendro/catalog_raster_review_test.exs
  - test/guardrails/required_checks_contract_test.exs
  - .github/workflows/ci.yml
autonomous: true
requirements: [CATALOG-09]
must_haves:
  truths:
    - "D-21: the advisory route consumes one candidate-only 32-cell manifest and emits exactly twelve full-size page-one images in canonical light/dark order; the four multipage proof images remain separate."
    - "D-21/D-25: every review image is bound to its candidate catalog ID, mode, PNG SHA, complete source-PDF SHA, renderer version/SHA, and exact commit/run identity; missing or stale identity fails closed."
    - "D-26: the route emits candidate and evidence-failure copy without writing scores, SIGN-OFF, passed values, canonical assets, or public projections."
    - "The phase-130 ref grammar is exactly gsd/phase-130-catalog-review-<40 lowercase hex>, and CI requires the suffix to equal GITHUB_SHA; normal PR behavior and deterministic merge authority remain unchanged."
    - "UI E2 empty/loading/error/populated/overflow/long-text and E3 overflow/long-text: no partial/contact-sheet substitute is reviewable, and all identities remain complete."
  artifacts:
    - path: dev/rendro/catalog_review_payload.ex
      provides: "Project-owned pure candidate-manifest classification and review identity construction seam"
    - path: test/rendro/catalog_review_payload_contract_test.exs
      provides: "Untagged pure ExUnit contract for exact candidate/final/multipage classification and fail-closed identity validation"
    - path: test/rendro/catalog_raster_review_test.exs
      provides: "Tagged pinned-PDFium realization of the already-proven payload contract"
    - path: .github/workflows/ci.yml
      provides: "Phase-narrow full-SHA-bound advisory route"
  key_links:
    - from: test/rendro/catalog_review_payload_contract_test.exs
      to: dev/rendro/catalog_review_payload.ex
      via: "Rendro.CatalogReviewPayload.classify/2 over synthetic candidate and multipage metadata without PDFium"
      pattern: "CatalogReviewPayload|classify"
    - from: test/rendro/catalog_raster_review_test.exs
      to: tmp/phase130-candidate/candidate-manifest.json
      via: "tagged rendering consumes the pure classifier's literal targets and exact candidate PNG/PDF/renderer identity checks"
      pattern: "candidate-manifest|png_sha256|source_pdf_sha256"
    - from: .github/workflows/ci.yml
      to: priv/pdfium_pin.json
      via: "pinned version/executable SHA plus exact GITHUB_SHA/ref equality"
      pattern: "phase-130-catalog-review|GITHUB_SHA|pdfium"
  prohibitions:
    - statement: "The advisory route MUST NOT read stale canonical hashes as the repaired candidate or mutate reviewer-owned evidence."
      status: unverified
      flagged: true
    - statement: "A contact sheet, partial directory, multipage proof, moving ref, or unpinned renderer MUST NOT satisfy final review."
      status: unverified
      flagged: true
---

<objective>
Define the exact candidate payload and immutable-SHA-bound CI route before any artifact generation.

Purpose: give the later candidate batch a deterministic consumer without coupling advisory evidence to canonical generation or human authority.
Output: separated raster payload contracts and a phase-narrow CI route; no generated catalog or evidence mutation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-CONTEXT.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-UI-SPEC.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-RESEARCH.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-PATTERNS.md
@test/rendro/catalog_raster_review_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one canonical pair from the candidate manifest into the separated payload</name>
  <reversibility rating="costly">The exact payload contract is consumed by the only Phase-130 human review, but it remains dev/test-only.</reversibility>
  <read_first>
    - test/rendro/catalog_review_payload_contract_test.exs — create this untagged Rendro.CatalogReviewPayloadContractTest as the pure candidate/final/multipage contract owner
    - test/rendro/catalog_raster_review_test.exs — retain Rendro.CatalogRasterReviewTest as the tagged pinned-PDFium executor only
    - dev/rendro/catalog.ex — literal catalog specs and candidate-manifest identity shape reused by the classifier
    - assets/rendro/catalog.json — baseline identity shape only; repaired candidate identity must not come from here
    - priv/pdfium_pin.json — renderer identity fields
  </read_first>
  <files>dev/rendro/catalog_review_payload.ex, test/rendro/catalog_review_payload_contract_test.exs, test/rendro/catalog_raster_review_test.exs</files>
  <behavior>
    - "The untagged pure contract passes a synthetic candidate manifest through Rendro.CatalogReviewPayload.classify/2 and receives exactly twelve page-one entries in the literal D-21 light-then-dark family order."
    - "The same result classifies exactly four Invoice/Statement first/final proof entries as multipage membership separate from the twelve-entry final payload."
    - "Every final identity carries catalog ID, mode, renderer version and executable SHA, PNG SHA, complete source-PDF SHA, commit SHA, and run ID."
    - "Duplicate, missing, extra, reordered, unsafe-path, renderer-mismatched, malformed, or non-candidate input returns an error before review."
    - "Classification neither accepts nor returns dimension scores, passed values, dispositions, SIGN-OFF content, or any other quality mutation."
  </behavior>
  <action>Write untagged `Rendro.CatalogReviewPayloadContractTest` red first in `test/rendro/catalog_review_payload_contract_test.exs`. Add the dev-only pure module `Rendro.CatalogReviewPayload` in `dev/rendro/catalog_review_payload.ex` with `classify/2` as the project-owned seam: it consumes decoded candidate-manifest data plus four already-described multipage proof entries and returns a validated twelve-entry final identity payload and a separate four-entry multipage classification, without reading files or invoking PDFium. Keep the literal D-21 ID order in this module and reject wrong cardinality/order/mode, duplicates, missing/extra IDs, unsafe relative paths, renderer version/SHA mismatch, malformed SHA/commit/run fields, incomplete source-PDF identity, and any score/disposition fields. Prove exact output order and membership, every required identity field, all rejection cases, and deep equality of the input before/after classification. Refactor tagged `Rendro.CatalogRasterReviewTest` to require `tmp/phase130-candidate/candidate-manifest.json`, delegate classification/identity-manifest construction to `Rendro.CatalogReviewPayload.classify/2`, and reserve PDFium rendering plus writes to `tmp/phase130-review/final` and `tmp/phase130-review/multipage` for the later pinned run. Keep D-26 copy diagnostic and never write canonical assets or reviewer fields.</action>
  <acceptance_criteria>
    - `Rendro.CatalogReviewPayloadContractTest` is untagged and directly exercises `Rendro.CatalogReviewPayload.classify/2` without PDFium or a candidate directory.
    - The pure contract proves literal twelve-entry light-then-dark order, exact four-entry separate multipage membership, and complete catalog ID/mode/renderer version+SHA/PNG SHA/source-PDF SHA/commit/run identity.
    - Duplicate, missing, extra, reordered, unsafe-path, renderer-mismatched, malformed, or quality-bearing input fails closed, and classification does not mutate its input or any disposition/score state.
    - `Rendro.CatalogRasterReviewTest` remains tagged `raster_snapshot` and is not executed by this task's automated command.
  </acceptance_criteria>
  <verify><automated>mix format --check-formatted dev/rendro/catalog_review_payload.ex test/rendro/catalog_review_payload_contract_test.exs test/rendro/catalog_raster_review_test.exs &amp;&amp; mix test test/rendro/catalog_review_payload_contract_test.exs --max-failures 1</automated></verify>
  <done>The review contract accepts exactly one complete candidate bundle and cannot confuse multipage or canonical baseline artifacts with the twelve-image payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the advisory route to the exact full commit SHA</name>
  <read_first>
    - .github/workflows/ci.yml — existing phase-127 advisory route and normal deterministic jobs
    - test/guardrails/required_checks_contract_test.exs — workflow topology assertions
  </read_first>
  <files>.github/workflows/ci.yml, test/guardrails/required_checks_contract_test.exs</files>
  <behavior>
    - "Only refs matching `gsd/phase-130-catalog-review-[0-9a-f]{40}` enter the route, and the 40-hex suffix must equal GITHUB_SHA."
    - "The route invokes the candidate-only task, then the separated raster payload, and uploads distinct candidate/final/multipage artifacts."
    - "Normal PR jobs and required mix rendro.catalog.check stay graph-disconnected from advisory success."
  </behavior>
  <action>Add guardrail tests first, then add the narrow phase-130 route with exact full-SHA equality, pinned PDFium verification, candidate generation, review staging, and separately named uploads. Reject abbreviated or mismatched suffixes. Do not broaden pull_request triggers, make advisory output required, or add a generic bypass/force option.</action>
  <verify><automated>mix test test/guardrails/required_checks_contract_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted test/guardrails/required_checks_contract_test.exs</automated></verify>
  <done>The only new advisory route is phase-narrow, full-SHA-bound, pinned, and unable to become deterministic merge authority.</done>
</task>

</tasks>

## Artifacts this phase produces

New public/runtime symbols, APIs, dependencies, recipes, presets, catalog entries, schema fields, and runtime flags: none. New dev-only symbol: `Rendro.CatalogReviewPayload.classify/2`. New untagged contract: `test/rendro/catalog_review_payload_contract_test.exs`. The existing `test/rendro/catalog_raster_review_test.exs` remains tagged for pinned execution. This plan generates no candidate or canonical artifacts.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| candidate manifest → review staging | Only exact candidate identities may enter the final payload. |
| git ref → advisory CI | Ref grammar and suffix/full-SHA equality bind the external run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-130-03A | Tampering / Repudiation | candidate payload identity | high | mitigate | Exact ID/path/PNG/PDF/renderer joins and fixed cardinality/order reject stale or mixed input. |
| T-130-03B | Spoofing / Tampering | phase-130 CI ref | high | mitigate | Full 40-hex grammar plus exact suffix equality to GITHUB_SHA; abbreviated/moving refs fail. |
| T-130-03C | Elevation of privilege | advisory CI topology | high | mitigate | Guardrails keep required deterministic checks independent and forbid evidence writes. |
</threat_model>

<verification>
Run the untagged pure payload contract and the workflow guardrail suite. Do not run the tagged PDFium test until Plan 04 has the fixed candidate and pinned renderer.
</verification>

<success_criteria>
- Exact candidate-only input, twelve-image output, and separate multipage output are deterministic contracts.
- The external route is full-SHA-bound and does not broaden normal CI.
- No canonical artifact or human score is touched.
</success_criteria>

<output>
Create `.planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md` when done.
</output>
