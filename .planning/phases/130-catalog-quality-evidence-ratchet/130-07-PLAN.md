---
phase: 130-catalog-quality-evidence-ratchet
plan: 07
type: execute
wave: 3
depends_on: [130-03]
files_modified:
  - tmp/phase130-launch-reconcile
  - priv/goldens/statement/dark.sha256
  - priv/goldens/certificate/dark.sha256
autonomous: false
requirements: [CATALOG-06, CATALOG-07, CATALOG-08]
must_haves:
  truths:
    - "D-19/D-26: the downstream statement/dark and certificate/dark byte changes are treated as engineering artifacts from completed Plans 01/02, never as catalog or human-quality evidence."
    - "The exact statement golden transition aca31620062efd25c21d74c855d9ba50e65777a35068afc86387efe415063ec9 -> a971a8a7395ebbb3e1af9c54e236483c969a90b222af1a6fdedfccead587eab6 and certificate transition df9703ee72be0fa78d2fab8f064ca93eb70b4699801372ff9c24513aa5c4cdb1 -> acb99d40fc68d365d1c6b158e2cf335325563307e1828c90f39bd2a95aff07d1 require explicit human authorization before blessing."
    - "A detached exact-HEAD worktree under tmp/phase130-launch-reconcile contains all blessing and launch-generation writes until the six changed light gallery images receive a separate full-size review."
    - "No canonical launch asset, legacy score record, SIGN-OFF entry, catalog cell, recipe, preset, dependency, public API, or no-theme byte changes before publication in Plan 06."
  artifacts:
    - path: tmp/phase130-launch-reconcile
      provides: "Fail-closed detached worktree for the golden and launch reconciliation batch"
    - path: priv/goldens/statement/dark.sha256
      provides: "Human-authorized deterministic Statement dark PDF hash, staged only in the detached worktree"
    - path: priv/goldens/certificate/dark.sha256
      provides: "Human-authorized deterministic Certificate dark PDF hash, staged only in the detached worktree"
  key_links:
    - from: test/rendro/recipes/theme_mode_background_golden_test.exs
      to: priv/goldens/{statement,certificate}/dark.sha256
      via: "Rendro.Test.Golden assert-only default and explicit MIX_GOLDEN_BLESS=true authorization"
      pattern: "assert_or_bless|MIX_GOLDEN_BLESS"
    - from: tmp/phase130-launch-reconcile
      to: HEAD
      via: "detached worktree HEAD equality and exact-path cleanup fence"
      pattern: "git worktree|rev-parse HEAD"
  prohibitions:
    - statement: "Golden hashes MUST NOT be blessed before the exact old/new pairs are approved."
      status: unverified
      flagged: true
    - statement: "The staging worktree MUST NOT publish canonical launch assets or alter legacy score evidence."
      status: unverified
      flagged: true
---

<objective>
Stage and authorize the deterministic golden reconciliation without exposing a half-updated launch/evidence state.

Purpose: clear the two known byte-golden failures while establishing a recoverable, exact-HEAD boundary for the sibling launch family.
Output: an exact detached staging worktree and two explicitly authorized golden hashes; main-worktree canonical assets/evidence remain untouched.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-catalog-quality-evidence-ratchet/130-01-SUMMARY.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-02-SUMMARY.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-03-SUMMARY.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-RESEARCH.md
@test/rendro/recipes/theme_mode_background_golden_test.exs
@test/support/golden.ex
@priv/goldens/statement/dark.sha256
@priv/goldens/certificate/dark.sha256
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the exact-HEAD fail-closed reconciliation worktree</name>
  <files>tmp/phase130-launch-reconcile</files>
  <action>Assert the main worktree has no uncommitted changes overlapping Plans 03-08 ownership. Create the exact path `tmp/phase130-launch-reconcile` as a detached worktree at the current full `HEAD`; refuse a pre-existing non-empty target. Record the full SHA, current two committed golden hashes, canonical launch manifest/gallery/manual/docs hashes, `priv/quality/rubric_scores.json`, and `priv/quality/SIGN-OFF.md` in `tmp/phase130-launch-reconcile/.phase130-baseline.json`. The baseline is diagnostic metadata inside the ignored worktree only. Prove the worktree commit equals main `HEAD` and that no tracked main-worktree file changed. Cleanup is limited to `git worktree remove tmp/phase130-launch-reconcile` after Plan 06 publishes or on a pre-generation failure; never use recursive cleanup against an unresolved path.</action>
  <verify><automated>test "$(git rev-parse HEAD)" = "$(git -C tmp/phase130-launch-reconcile rev-parse HEAD)" &amp;&amp; jq -e '.head_sha and .goldens.statement_dark and .goldens.certificate_dark and .launch_manifest_sha256 and .rubric_sha256 and .sign_off_sha256' tmp/phase130-launch-reconcile/.phase130-baseline.json &gt;/dev/null &amp;&amp; git diff --exit-code -- priv/goldens assets/rendro/gallery assets/rendro/artifacts.json assets/rendro/manual.pdf README.md guides/recipes.md guides/theming.md priv/quality/rubric_scores.json priv/quality/SIGN-OFF.md</automated></verify>
  <done>One exact-HEAD staging worktree exists with a complete pre-change identity fence and main canonical/evidence bytes untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize the exact two golden hash transitions</name>
  <decision>Authorize only the two deterministic dark golden transitions caused by completed Plans 01/02.</decision>
  <context>Statement changes from `aca31620062efd25c21d74c855d9ba50e65777a35068afc86387efe415063ec9` to `a971a8a7395ebbb3e1af9c54e236483c969a90b222af1a6fdedfccead587eab6`; Certificate changes from `df9703ee72be0fa78d2fab8f064ca93eb70b4699801372ff9c24513aa5c4cdb1` to `acb99d40fc68d365d1c6b158e2cf335325563307e1828c90f39bd2a95aff07d1`. This authorization is deterministic byte-baseline approval only and cannot authorize launch quality evidence or catalog scores.</context>
  <options>
    <option id="authorize-exact-two"><name>Authorize exact two</name><pros>Allows only the recorded Statement and Certificate hashes to be blessed in staging.</pros><cons>Accepts the intentional downstream bytes from completed recipe repairs.</cons></option>
    <option id="reject-or-investigate"><name>Reject or investigate</name><pros>Preserves both committed hashes and stops publication.</pros><cons>The three known full-suite contracts remain red until the mismatch is resolved.</cons></option>
  </options>
  <resume-signal>Select `authorize-exact-two` or `reject-or-investigate`.</resume-signal>
  <verify><automated>test "$(cat priv/goldens/statement/dark.sha256)" = "aca31620062efd25c21d74c855d9ba50e65777a35068afc86387efe415063ec9" &amp;&amp; test "$(cat priv/goldens/certificate/dark.sha256)" = "df9703ee72be0fa78d2fab8f064ca93eb70b4699801372ff9c24513aa5c4cdb1"</automated></verify>
  <done>The summary records the selected option and exact old/new values; rejection stops the plan without any blessing.</done>
</task>

<task type="auto">
  <name>Task 3: Bless only the authorized golden pair inside staging</name>
  <precondition>Task 2 records `authorize-exact-two` for the exact four hashes printed in the checkpoint.</precondition>
  <files>tmp/phase130-launch-reconcile/priv/goldens/statement/dark.sha256, tmp/phase130-launch-reconcile/priv/goldens/certificate/dark.sha256</files>
  <action>Inside the detached worktree run exactly `MIX_GOLDEN_BLESS=true mix test test/rendro/recipes/theme_mode_background_golden_test.exs --max-failures 1`, then rerun the identical test without `MIX_GOLDEN_BLESS`. Assert the staged files equal the authorized new hashes, the diff contains exactly those two one-line files, and main-worktree goldens still equal the old hashes. Do not copy or commit the staged hashes yet; Plan 06 publishes them together with the reviewed launch/evidence batch. On any mismatch, preserve the worktree for diagnosis and stop.</action>
  <verify><automated>cd tmp/phase130-launch-reconcile &amp;&amp; test "$(cat priv/goldens/statement/dark.sha256)" = "a971a8a7395ebbb3e1af9c54e236483c969a90b222af1a6fdedfccead587eab6" &amp;&amp; test "$(cat priv/goldens/certificate/dark.sha256)" = "acb99d40fc68d365d1c6b158e2cf335325563307e1828c90f39bd2a95aff07d1" &amp;&amp; mix test test/rendro/recipes/theme_mode_background_golden_test.exs --max-failures 1 &amp;&amp; test "$(git diff --name-only | sort)" = "$(printf '%s\n' priv/goldens/certificate/dark.sha256 priv/goldens/statement/dark.sha256 | sort)"</automated></verify>
  <done>Only the two authorized golden hashes are green in staging, with the main worktree still byte-identical to its pre-reconciliation state.</done>
</task>

</tasks>

## Artifacts this phase produces

Ephemeral only: `tmp/phase130-launch-reconcile` and its baseline identity record. Staged tracked changes are exactly the two authorized golden hash files. No public/runtime symbol, dependency, recipe, preset, catalog cell, launch artifact, score, or sign-off is published by this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| completed recipe bytes -> blessed golden | Human authorization must bind exact old/new hashes. |
| main worktree -> detached staging worktree | Generated writes must remain recoverable and unpublished until evidence is complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-130-07A | Tampering / Repudiation | golden blessing | high | mitigate | Exact four hashes are printed and approved before the bless command; the post-bless diff is exactly two files. |
| T-130-07B | Tampering | staging boundary | high | mitigate | Detached exact-HEAD worktree, baseline hashes, main diff fence, and exact-path cleanup prevent half-publication. |
| T-130-07C | Elevation of privilege | evidence authority | high | mitigate | Golden approval is explicitly deterministic-only and cannot authorize launch or catalog quality records. |
</threat_model>

<verification>
The focused golden suite passes assert-only in staging, the staged diff is exactly two authorized hashes, and main canonical/evidence state remains unchanged.
</verification>

<success_criteria>
- Exact human authorization exists for both old/new golden pairs.
- Only the two staged golden refs change and both pass assert-only verification.
- No launch, legacy evidence, catalog, public API, dependency, preset, or no-theme state is published.
</success_criteria>

<output>
Create `.planning/phases/130-catalog-quality-evidence-ratchet/130-07-SUMMARY.md` with exact authorization and staged hashes when done.
</output>
