---
phase: 11-reconstruct-phase-1-4-artifacts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-core-deterministic-foundation/01-PLAN.md
  - .planning/phases/01-core-deterministic-foundation/01-SUMMARY.md
  - .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md
  - .planning/phases/02-layout-and-pagination-engine/02-PLAN.md
  - .planning/phases/02-layout-and-pagination-engine/02-SUMMARY.md
  - .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md
  - .planning/phases/03-adapter-and-ops-integration/03-PLAN.md
  - .planning/phases/03-adapter-and-ops-integration/03-SUMMARY.md
  - .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md
  - .planning/phases/04-quality-and-release-hardening/04-PLAN.md
  - .planning/phases/04-quality-and-release-hardening/04-SUMMARY.md
  - .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - test/rendro/adapters/phoenix_test.exs
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - LAY-01
  - LAY-02
  - LAY-03
  - LAY-04
  - LAY-05
  - ADPT-01
  - ADPT-02
  - ADPT-03
  - ADPT-04
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
must_haves:
  truths:
    - "Phase 1 reconstructed artifacts map CORE-01, CORE-02, CORE-05, OBS-01, and OBS-03 to primary executable proof at the current public boundary per D-01 through D-05."
    - "Phase 2 reconstructed artifacts map CORE-03, CORE-04, and LAY-01 through LAY-05 to executable proof from the live layout/pagination surfaces, with read-only evidence mapping by default per D-06 through D-10."
    - "Phase 3 reconstructed artifacts prove ADPT-01 through ADPT-04 and the Phase 3 observability slices against current adapter boundaries, using a narrowly scoped Phoenix boundary test as the default adapter proof surface for ADPT-01 and ADPT-02 per D-04 and D-07."
    - "Phase 4 reconstructed artifacts evaluate QUAL-01 through QUAL-05 against runnable commands and workflow-level evidence, recording Partial or Blocked verdicts truthfully where current proof does not fully close QUAL-03 or QUAL-04 per D-09 and D-10."
    - "`.planning/REQUIREMENTS.md` updates immediately after each reconstructed phase verification closes: Phase 1 rows after `01-VERIFICATION.md`, Phase 2 rows after `02-VERIFICATION.md`, Phase 3 rows after `03-VERIFICATION.md`, and Phase 4 rows plus final totals after `04-VERIFICATION.md`, using only `Pending`, `Done`, `Partial`, and `Blocked` per D-15 through D-18."
  artifacts:
    - path: .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md
      provides: "Requirement-first proof and verdicts for the reconstructed Phase 1 requirements"
    - path: .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md
      provides: "Requirement-first proof and verdicts for the reconstructed Phase 2 requirements"
    - path: .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md
      provides: "Requirement-first proof and verdicts for the reconstructed Phase 3 requirements"
    - path: .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md
      provides: "Requirement-first proof and verdicts for the reconstructed Phase 4 requirements"
    - path: .planning/REQUIREMENTS.md
      provides: "Central traceability rows synchronized from the four finished verification artifacts only"
  key_links:
    - from: .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md
      to: .planning/REQUIREMENTS.md
      via: "immediate sync of CORE-01, CORE-02, CORE-05, OBS-01, and OBS-03 verdicts after Phase 1 closes"
      pattern: "CORE-01|CORE-02|CORE-05|OBS-01|OBS-03"
    - from: .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md
      to: .planning/REQUIREMENTS.md
      via: "immediate sync of CORE-03, CORE-04, and LAY-01 through LAY-05 verdicts after Phase 2 closes"
      pattern: "CORE-03|CORE-04|LAY-01|LAY-02|LAY-03|LAY-04|LAY-05"
    - from: .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md
      to: .planning/REQUIREMENTS.md
      via: "immediate sync of ADPT-01 through ADPT-04 and OBS-02/OBS-04 verdicts after Phase 3 closes"
      pattern: "ADPT-01|ADPT-02|ADPT-03|ADPT-04|OBS-02|OBS-04"
    - from: .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md
      to: .planning/REQUIREMENTS.md
      via: "Phase 4 closeout sync of QUAL-01 through QUAL-05 verdicts plus final coverage totals"
      pattern: "QUAL-01|QUAL-02|QUAL-03|QUAL-04|QUAL-05"
---

<objective>
Reconstruct the missing Phase 1 through Phase 4 GSD artifact triads against the live fixed codebase, then reconcile the central requirements traceability table from those finished verification verdicts only.

Purpose: Close the formal proof debt identified by the milestone audit without changing runtime behavior or overstating what the current executable evidence can support.
Output: Reconstructed zero-padded `PLAN.md`, `SUMMARY.md`, and `VERIFICATION.md` artifacts for Phases 1, 2, 3, and 4, plus a truth-synced `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-reconstruct-phase-1-4-artifacts/11-CONTEXT.md
@.planning/phases/11-reconstruct-phase-1-4-artifacts/11-RESEARCH.md
@.planning/phases/11-reconstruct-phase-1-4-artifacts/11-PATTERNS.md
@.planning/phases/11-reconstruct-phase-1-4-artifacts/11-VALIDATION.md
@.planning/phases/05-early-ecosystem-recipes/05-VERIFICATION.md
@.planning/phases/06-pipeline-telemetry-contract/06-VERIFICATION.md
@.planning/phases/10-recipe-correctness-and-traceability/10-01-PLAN.md
@.planning/phases/10-recipe-correctness-and-traceability/10-02-PLAN.md

<interfaces>
Requirement ownership to preserve during reconstruction:
```text
Phase 1 -> CORE-01, CORE-02, CORE-05, OBS-01, OBS-03
Phase 2 -> CORE-03, CORE-04, LAY-01, LAY-02, LAY-03, LAY-04, LAY-05
Phase 3 -> ADPT-01, ADPT-02, ADPT-03, ADPT-04, OBS-02, OBS-04
Phase 4 -> QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05
```

Locked status vocabulary for the central traceability sync:
```text
Pending | Done | Partial | Blocked
```

Nyquist validation artifact already present for this phase:
```text
.planning/phases/11-reconstruct-phase-1-4-artifacts/11-VALIDATION.md
```

Verification shape required by D-11 through D-14:
```text
1. Short success-criteria / goal-achievement summary first
2. Requirement-first body with one primary executable proof per requirement
3. Artifact appendix last
4. Cross-reference repeated evidence instead of duplicating it
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct the Phase 1 triad from live core, deterministic, telemetry, and error proofs, then sync Phase 1 traceability rows</name>
  <files>.planning/phases/01-core-deterministic-foundation/01-PLAN.md, .planning/phases/01-core-deterministic-foundation/01-SUMMARY.md, .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/v1.0-MILESTONE-AUDIT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-CONTEXT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-RESEARCH.md
    .planning/phases/06-pipeline-telemetry-contract/06-VERIFICATION.md
    lib/rendro.ex
    lib/rendro/document.ex
    lib/rendro/pipeline.ex
    lib/rendro/error.ex
    lib/rendro/pdf/writer.ex
    test/rendro_builders_test.exs
    test/rendro/integration_test.exs
    test/rendro/pipeline_test.exs
    test/rendro_test.exs
    test/rendro/deterministic_test.exs
    test/rendro/telemetry_test.exs
    test/rendro/error_test.exs
  </read_first>
  <action>
    Create the reconstructed Phase 1 triad using the Phase 10 plan structure and the Phase 05/06 evidence-forward verification style. Apply D-01 through D-05 exactly: the `01-VERIFICATION.md` body must be requirement-first and must name one primary executable proof for each of `CORE-01`, `CORE-02`, `CORE-05`, `OBS-01`, and `OBS-03` against the live fixed codebase, not against historical intent. Use existing ExUnit and integration proof surfaces as the default evidence path per D-04, with Phase 06 verification only as supporting evidence for the repaired telemetry contract.
    Keep this slice read-only per D-06 through D-10: do not change runtime modules, public APIs, telemetry semantics, or docs claims. If any Phase 1 requirement still lacks boundary-level proof after reviewing the current tests and commands, mark that requirement `Partial` or `Blocked` in `01-VERIFICATION.md` with a concise gap statement instead of forcing a `Done` claim. After the verification verdicts are stable, derive `01-SUMMARY.md` from those actual findings and reconstruct `01-PLAN.md` as an evidence-based record of what Phase 1 delivered, preserving the zero-padded artifact names exactly. Then immediately update the owned Phase 1 rows in `.planning/REQUIREMENTS.md` from `01-VERIFICATION.md` per D-15 and D-16; do not defer those row changes to a later task.
  </action>
  <acceptance_criteria>
    - `.planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md` exists and contains requirement sections for `CORE-01`, `CORE-02`, `CORE-05`, `OBS-01`, and `OBS-03`.
    - `01-VERIFICATION.md` uses the hybrid structure required by D-11 through D-14: goal summary first, requirement-first body second, artifact appendix last.
    - `01-SUMMARY.md` and `01-PLAN.md` both reference the current reconstructed Phase 1 artifacts rather than original implementation intent.
    - The `CORE-01`, `CORE-02`, `CORE-05`, `OBS-01`, and `OBS-03` rows in `.planning/REQUIREMENTS.md` match the final verdicts in `01-VERIFICATION.md` immediately after this task closes.
    - None of the Phase 1 runtime source files are modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; vf=.planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md; reqs=.planning/REQUIREMENTS.md; python_cmd=${PYTHON:-python3}; mix test test/rendro_builders_test.exs test/rendro/integration_test.exs test/rendro/pipeline_test.exs test/rendro_test.exs test/rendro/deterministic_test.exs test/rendro/telemetry_test.exs test/rendro/error_test.exs; rg -n "Goal Achievement|Requirements Coverage|Required Artifacts" "$vf" >/dev/null; for req in CORE-01 CORE-02 CORE-05 OBS-01 OBS-03; do rg -n "^## Requirement: ${req}$" "$vf" >/dev/null; awk -v req="$req" '\''BEGIN{ok=0} $0=="## Requirement: " req {inreq=1; next} /^## Requirement: / && inreq {exit} inreq && /^\*\*Status:\*\* (Done|Partial|Blocked)/ {status=1} inreq && /^\*\*Primary proof:\*\*/ {proof=1} END{exit (status && proof) ? 0 : 1}'\'' "$vf"; done; rg -n "01-VERIFICATION.md|01-SUMMARY.md|01-PLAN.md" .planning/phases/01-core-deterministic-foundation/01-SUMMARY.md .planning/phases/01-core-deterministic-foundation/01-PLAN.md >/dev/null; "$python_cmd" - <<'\''PY'\'' "$vf" "$reqs"\nimport re, sys\nvf, reqs = sys.argv[1:]\nowned = [\"CORE-01\", \"CORE-02\", \"CORE-05\", \"OBS-01\", \"OBS-03\"]\ntext = open(vf, encoding=\"utf-8\").read()\nactual = open(reqs, encoding=\"utf-8\").read().splitlines()\nexpected = {}\nparts = re.split(r\"^## Requirement: \", text, flags=re.M)\nfor part in parts[1:]:\n    header, *rest = part.splitlines()\n    req = header.strip()\n    body = \"\\n\".join(rest)\n    m = re.search(r\"^\\*\\*Status:\\*\\* (Done|Partial|Blocked|Pending)$\", body, flags=re.M)\n    if req in owned and m:\n        expected[req] = m.group(1)\nrows = {}\nfor line in actual:\n    m = re.match(r\"^\\| ([A-Z0-9-]+) \\| .* \\| (Pending|Done|Partial|Blocked) \\|$\", line)\n    if m and m.group(1) in owned:\n        rows[m.group(1)] = m.group(2)\nfor req in owned:\n    if rows.get(req) != expected.get(req):\n        raise SystemExit(f\"traceability mismatch for {req}: requirements={rows.get(req)!r} verification={expected.get(req)!r}\")\nPY'</automated>
  </verify>
  <done>Phase 1 has a truthful reconstructed triad tied to executable proof for its five owned requirements, with any proof gaps recorded as `Partial` or `Blocked` instead of inferred away, and the owned Phase 1 rows in `.planning/REQUIREMENTS.md` are synchronized immediately from `01-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruct the Phase 2 triad from live fixed-position, flow, pagination, and overflow proofs, then sync Phase 2 traceability rows</name>
  <files>.planning/phases/02-layout-and-pagination-engine/02-PLAN.md, .planning/phases/02-layout-and-pagination-engine/02-SUMMARY.md, .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/v1.0-MILESTONE-AUDIT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-CONTEXT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-RESEARCH.md
    lib/rendro.ex
    lib/rendro/page.ex
    lib/rendro/block.ex
    lib/rendro/table.ex
    lib/rendro/pipeline.ex
    test/rendro_builders_test.exs
    test/rendro/flow_test.exs
    test/rendro/integration_test.exs
    test/rendro/metadata_test.exs
    test/rendro/error_test.exs
    test/rendro/pipeline/paginate_test.exs
  </read_first>
  <action>
    Create the missing `.planning/phases/02-layout-and-pagination-engine/` directory if needed, then reconstruct the zero-padded Phase 2 triad. Per D-01 through D-05 and the Phase 11 research, treat this as predominantly read-only evidence mapping: `02-VERIFICATION.md` must map `CORE-03`, `CORE-04`, and `LAY-01` through `LAY-05` to current public-boundary proof from `Rendro.fixed/2`, `Rendro.flow/2`, pagination tests, table/header behavior tests, metadata coverage, and overflow diagnostics. Use source files only as supporting links; source presence alone is never enough to close a requirement.
    Do not broaden scope into layout-engine remediation. If a requirement lacks a current primary proof surface, record the honest verdict in `02-VERIFICATION.md` and carry that exact verdict into `02-SUMMARY.md` and `02-PLAN.md`. The reconstructed plan should read as a formal record of what the current fixed code proves, not as a promise that every originally intended Phase 2 behavior landed perfectly. Then immediately update the owned Phase 2 rows in `.planning/REQUIREMENTS.md` from `02-VERIFICATION.md` per D-15 and D-16; do not defer those row changes to a later task.
  </action>
  <acceptance_criteria>
    - `.planning/phases/02-layout-and-pagination-engine/` exists and contains `02-PLAN.md`, `02-SUMMARY.md`, and `02-VERIFICATION.md`.
    - `02-VERIFICATION.md` contains requirement-first coverage for `CORE-03`, `CORE-04`, and `LAY-01` through `LAY-05`.
    - Phase 2 proof is anchored to executable tests or commands for fixed-position, flow, pagination, table header repetition, header/footer placement, and overflow diagnostics.
    - The `CORE-03`, `CORE-04`, and `LAY-01` through `LAY-05` rows in `.planning/REQUIREMENTS.md` match the final verdicts in `02-VERIFICATION.md` immediately after this task closes.
    - None of the Phase 2 runtime source files are modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; vf=.planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md; reqs=.planning/REQUIREMENTS.md; python_cmd=${PYTHON:-python3}; mix test test/rendro_builders_test.exs test/rendro/flow_test.exs test/rendro/integration_test.exs test/rendro/metadata_test.exs test/rendro/error_test.exs test/rendro/pipeline/paginate_test.exs; rg -n "Goal Achievement|Requirements Coverage|Required Artifacts" "$vf" >/dev/null; for req in CORE-03 CORE-04 LAY-01 LAY-02 LAY-03 LAY-04 LAY-05; do rg -n "^## Requirement: ${req}$" "$vf" >/dev/null; awk -v req="$req" '\''BEGIN{ok=0} $0=="## Requirement: " req {inreq=1; next} /^## Requirement: / && inreq {exit} inreq && /^\*\*Status:\*\* (Done|Partial|Blocked)/ {status=1} inreq && /^\*\*Primary proof:\*\*/ {proof=1} END{exit (status && proof) ? 0 : 1}'\'' "$vf"; done; rg -n "02-VERIFICATION.md|02-SUMMARY.md|02-PLAN.md" .planning/phases/02-layout-and-pagination-engine/02-SUMMARY.md .planning/phases/02-layout-and-pagination-engine/02-PLAN.md >/dev/null; "$python_cmd" - <<'\''PY'\'' "$vf" "$reqs"\nimport re, sys\nvf, reqs = sys.argv[1:]\nowned = [\"CORE-03\", \"CORE-04\", \"LAY-01\", \"LAY-02\", \"LAY-03\", \"LAY-04\", \"LAY-05\"]\ntext = open(vf, encoding=\"utf-8\").read()\nactual = open(reqs, encoding=\"utf-8\").read().splitlines()\nexpected = {}\nparts = re.split(r\"^## Requirement: \", text, flags=re.M)\nfor part in parts[1:]:\n    header, *rest = part.splitlines()\n    req = header.strip()\n    body = \"\\n\".join(rest)\n    m = re.search(r\"^\\*\\*Status:\\*\\* (Done|Partial|Blocked|Pending)$\", body, flags=re.M)\n    if req in owned and m:\n        expected[req] = m.group(1)\nrows = {}\nfor line in actual:\n    m = re.match(r\"^\\| ([A-Z0-9-]+) \\| .* \\| (Pending|Done|Partial|Blocked) \\|$\", line)\n    if m and m.group(1) in owned:\n        rows[m.group(1)] = m.group(2)\nfor req in owned:\n    if rows.get(req) != expected.get(req):\n        raise SystemExit(f\"traceability mismatch for {req}: requirements={rows.get(req)!r} verification={expected.get(req)!r}\")\nPY'</automated>
  </verify>
  <done>Phase 2 has a complete reconstructed triad, every owned requirement is tied to an explicit current proof surface or an explicit `Partial`/`Blocked` verdict, and the owned Phase 2 rows in `.planning/REQUIREMENTS.md` are synchronized immediately from `02-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Reconstruct the Phase 3 triad, add the Phoenix boundary proof, and sync Phase 3 traceability rows</name>
  <files>.planning/phases/03-adapter-and-ops-integration/03-PLAN.md, .planning/phases/03-adapter-and-ops-integration/03-SUMMARY.md, .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md, .planning/REQUIREMENTS.md, test/rendro/adapters/phoenix_test.exs</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/v1.0-MILESTONE-AUDIT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-CONTEXT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-RESEARCH.md
    lib/rendro/adapters/phoenix.ex
    lib/rendro/adapters/oban/render_worker.ex
    lib/rendro/adapters/threadline.ex
    test/rendro/adapters/oban/render_worker_test.exs
    test/rendro/adapters/threadline_test.exs
    test/rendro/pipeline_test.exs
    test/rendro/policy_test.exs
    test/rendro/telemetry_test.exs
    examples/phoenix_example/mix.exs
  </read_first>
  <action>
    Create the missing `.planning/phases/03-adapter-and-ops-integration/` directory if needed, then reconstruct the Phase 3 triad around the current adapter and ops boundaries. `03-VERIFICATION.md` must cover `ADPT-01`, `ADPT-02`, `ADPT-03`, `ADPT-04`, `OBS-02`, and `OBS-04` with one primary executable proof each per D-01 through D-05. For `ADPT-03`, use optional-dependency proof such as `mix compile --no-optional-deps --warnings-as-errors` plus the guarded adapter surfaces. For `ADPT-04` and `OBS-04`, use the current Oban worker, policy, and telemetry tests against the fixed codebase.
    For `ADPT-01` and `ADPT-02`, follow D-04 exactly and make the boundary proof explicit rather than conditional: add one narrowly scoped proving test in `test/rendro/adapters/phoenix_test.exs` that exercises the Phoenix adapter boundary without changing runtime behavior or adapter semantics. This keeps the proof surface deterministic for execution and avoids branching verify logic. Do not modify adapter runtime modules in this phase. If the boundary still cannot be proven truthfully even with the proving test, record `Partial` or `Blocked` in `03-VERIFICATION.md` and carry that verdict into `03-SUMMARY.md` and `03-PLAN.md`. Then immediately update the owned Phase 3 rows in `.planning/REQUIREMENTS.md` from `03-VERIFICATION.md` per D-15 and D-16; do not defer those row changes to a later task.
  </action>
  <acceptance_criteria>
    - `.planning/phases/03-adapter-and-ops-integration/` exists and contains `03-PLAN.md`, `03-SUMMARY.md`, and `03-VERIFICATION.md`.
    - `03-VERIFICATION.md` contains requirement-first coverage for `ADPT-01`, `ADPT-02`, `ADPT-03`, `ADPT-04`, `OBS-02`, and `OBS-04`.
    - `ADPT-03` proof includes an optional-dependency compile command, not only source inspection.
    - The `ADPT-01` through `ADPT-04` and `OBS-02` / `OBS-04` rows in `.planning/REQUIREMENTS.md` match the final verdicts in `03-VERIFICATION.md` immediately after this task closes.
    - `test/rendro/adapters/phoenix_test.exs` exists as a Phase 11 proving artifact for `ADPT-01` and `ADPT-02`, and no Phoenix adapter runtime code is changed.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; vf=.planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md; reqs=.planning/REQUIREMENTS.md; python_cmd=${PYTHON:-python3}; test -f test/rendro/adapters/phoenix_test.exs; mix compile --no-optional-deps --warnings-as-errors; mix test test/rendro/adapters/phoenix_test.exs test/rendro/adapters/oban/render_worker_test.exs test/rendro/adapters/threadline_test.exs test/rendro/pipeline_test.exs test/rendro/policy_test.exs test/rendro/telemetry_test.exs; for req in ADPT-01 ADPT-02 ADPT-03 ADPT-04 OBS-02 OBS-04; do awk -v req="$req" '\''BEGIN{ok=0} $0=="## Requirement: " req {inreq=1; next} /^## Requirement: / && inreq {exit} inreq && /^\*\*Status:\*\* (Done|Partial|Blocked)/ {status=1} inreq && /^\*\*Primary proof:\*\*/ {proof=1} END{exit (status && proof) ? 0 : 1}'\'' "$vf"; done; rg -n "mix compile --no-optional-deps --warnings-as-errors|phoenix_test\\.exs|pipeline_test\\.exs|Done|Partial|Blocked" "$vf" .planning/phases/03-adapter-and-ops-integration/03-PLAN.md .planning/phases/03-adapter-and-ops-integration/03-SUMMARY.md >/dev/null; "$python_cmd" - <<'\''PY'\'' "$vf" "$reqs"\nimport re, sys\nvf, reqs = sys.argv[1:]\nowned = [\"ADPT-01\", \"ADPT-02\", \"ADPT-03\", \"ADPT-04\", \"OBS-02\", \"OBS-04\"]\ntext = open(vf, encoding=\"utf-8\").read()\nactual = open(reqs, encoding=\"utf-8\").read().splitlines()\nexpected = {}\nparts = re.split(r\"^## Requirement: \", text, flags=re.M)\nfor part in parts[1:]:\n    header, *rest = part.splitlines()\n    req = header.strip()\n    body = \"\\n\".join(rest)\n    m = re.search(r\"^\\*\\*Status:\\*\\* (Done|Partial|Blocked|Pending)$\", body, flags=re.M)\n    if req in owned and m:\n        expected[req] = m.group(1)\nrows = {}\nfor line in actual:\n    m = re.match(r\"^\\| ([A-Z0-9-]+) \\| .* \\| (Pending|Done|Partial|Blocked) \\|$\", line)\n    if m and m.group(1) in owned:\n        rows[m.group(1)] = m.group(2)\nfor req in owned:\n    if rows.get(req) != expected.get(req):\n        raise SystemExit(f\"traceability mismatch for {req}: requirements={rows.get(req)!r} verification={expected.get(req)!r}\")\nPY'</automated>
  </verify>
  <done>Phase 3 has a reconstructed triad with truthful adapter-boundary proof, any new code added is limited to a proving test for Phoenix adapter boundaries, and the owned Phase 3 rows in `.planning/REQUIREMENTS.md` are synchronized immediately from `03-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 4: Reconstruct the Phase 4 triad, then sync the owned Phase 4 rows and final coverage totals</name>
  <files>.planning/phases/04-quality-and-release-hardening/04-PLAN.md, .planning/phases/04-quality-and-release-hardening/04-SUMMARY.md, .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/v1.0-MILESTONE-AUDIT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-CONTEXT.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-RESEARCH.md
    .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md
    .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md
    .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md
    .planning/phases/11-reconstruct-phase-1-4-artifacts/11-VALIDATION.md
    mix.exs
    .github/workflows/ci.yml
    lib/mix/tasks/verify.ex
    lib/mix/tasks/release/preflight.ex
    scripts/verify_docs.exs
    README.md
    guides/integrations.md
    examples/phoenix_example/mix.exs
  </read_first>
  <action>
    Create the missing `.planning/phases/04-quality-and-release-hardening/` directory if needed, then reconstruct the Phase 4 triad around executable verification surfaces rather than file presence. `04-VERIFICATION.md` must cover `QUAL-01` through `QUAL-05` using runnable commands or workflow-level proof per D-04: `mix ci`, docs-contract verification, example-app execution/compile evidence, `mix release.preflight`, and `mix verify` output. Use `11-VALIDATION.md` as the Nyquist execution contract for this phase so the reconstructed quality proof and the phase validation artifact stay aligned. Record the actual evidence ceiling truthfully. Per the research and user guidance, this task should explicitly allow mixed outcomes for `QUAL-03` and `QUAL-04` if the current executable proof cannot fully close the CI example-app path or the release happy path without a real tag.
    Keep Phase 11 read-mostly: do not change CI workflow semantics, release-task semantics, or runtime code here. Before assigning any verdict for `QUAL-01` or `QUAL-05`, the executor must establish a clean proof surface by creating a temporary clean checkout/worktree at the current `HEAD` and rerunning `mix ci` and `mix verify` there. If the clean rerun passes, use that result as the canonical proof. If the clean rerun fails, capture the clean-run failure as intrinsic evidence in `04-VERIFICATION.md`; only then may the owning requirement be marked `Partial` or `Blocked`. Dirty-worktree failures from unrelated edits in the active workspace must never be converted into canonical requirement verdicts. `04-SUMMARY.md` and `04-PLAN.md` must mirror those truthful mixed verdicts rather than flattening them into a false pass state.
    After `04-VERIFICATION.md` carries final verdicts, immediately sync the owned Phase 4 rows in `.planning/REQUIREMENTS.md` per D-15 and D-16. Use the completed `01-VERIFICATION.md`, `02-VERIFICATION.md`, and `03-VERIFICATION.md` only to recompute the final coverage totals at the bottom of `.planning/REQUIREMENTS.md`, since those earlier row updates should already have happened in Tasks 1 through 3. Do not infer status from source inspection, plan prose, summary prose, or command availability. Preserve `ADPT-05` as `Done`, preserve the existing ownership columns, and recompute the coverage totals at the bottom of `.planning/REQUIREMENTS.md` so they match the final row counts exactly.
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-quality-and-release-hardening/` exists and contains `04-PLAN.md`, `04-SUMMARY.md`, and `04-VERIFICATION.md`.
    - `04-VERIFICATION.md` contains requirement-first coverage for `QUAL-01`, `QUAL-02`, `QUAL-03`, `QUAL-04`, and `QUAL-05`.
    - `QUAL-01` and `QUAL-05` verdicts are derived only from a temporary clean checkout/worktree proof run at current `HEAD` or from a clean-run intrinsic failure, never from unrelated local drift in the current workspace.
    - `QUAL-03` and `QUAL-04` are explicitly allowed to end `Partial` or `Blocked` if current executable proof does not fully close them.
    - The `QUAL-01` through `QUAL-05` rows in `.planning/REQUIREMENTS.md` match the final verdicts in `04-VERIFICATION.md` immediately after this task closes, and the previously synced Phase 1 through Phase 3 rows remain in parity with their verification artifacts.
    - The coverage counts at the end of `.planning/REQUIREMENTS.md` match the final row totals exactly.
    - This task does not modify CI workflow files, release-task files, or example-app runtime code.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; vf=.planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md; reqs=.planning/REQUIREMENTS.md; python_cmd=${PYTHON:-python3}; section_check(){ local req="$1" mode="$2"; awk -v req="$req" -v mode="$mode" '\''$0=="## Requirement: " req {inreq=1; next} /^## Requirement: / && inreq {exit} inreq && mode=="status" && /^\*\*Status:\*\* (Partial|Blocked)/ {found=1} inreq && mode=="workflow" && (/\.github\/workflows\/ci\.yml/ || /workflow evidence/ || /CI workflow/) {found=1} inreq && mode=="clean" && (/temporary clean/ || /clean checkout/ || /clean worktree/ || /unrelated local drift/) {found=1} END{exit found ? 0 : 1}'\'' "$vf"; }; test -f .planning/phases/11-reconstruct-phase-1-4-artifacts/11-VALIDATION.md; tmpdir=$(mktemp -d /tmp/rendro-phase11-XXXXXX); cleanup(){ git worktree remove --force "$tmpdir" >/dev/null 2>&1 || true; rm -rf "$tmpdir"; }; trap cleanup EXIT; git worktree add --detach "$tmpdir" HEAD >/tmp/phase11-worktree-add.log 2>&1; set +e; (cd "$tmpdir" && mix ci) >/tmp/phase11-qual-ci.log 2>&1; ci=$?; (cd "$tmpdir" && mix run scripts/verify_docs.exs) >/tmp/phase11-qual-docs.log 2>&1; docs=$?; (cd "$tmpdir/examples/phoenix_example" && mix deps.get >/tmp/phase11-qual-example-deps.log 2>&1 && mix compile >/tmp/phase11-qual-example.log 2>&1); example=$?; (cd "$tmpdir" && mix verify) >/tmp/phase11-qual-verify.log 2>&1; verify=$?; (cd "$tmpdir" && mix release.preflight) >/tmp/phase11-qual-preflight.log 2>&1; preflight=$?; set -e; [ "$ci" -eq 0 ] || section_check QUAL-01 status; [ "$docs" -eq 0 ] || section_check QUAL-02 status; [ "$example" -eq 0 ] || section_check QUAL-03 status; [ "$verify" -eq 0 ] || section_check QUAL-05 status; [ "$preflight" -eq 0 ] || section_check QUAL-04 status; for req in QUAL-01 QUAL-02 QUAL-03 QUAL-04 QUAL-05; do awk -v req="$req" '\''BEGIN{ok=0} $0=="## Requirement: " req {inreq=1; next} /^## Requirement: / && inreq {exit} inreq && /^\*\*Status:\*\* (Done|Partial|Blocked)/ {status=1} inreq && /^\*\*Primary proof:\*\*/ {proof=1} END{exit (status && proof) ? 0 : 1}'\'' "$vf"; done; section_check QUAL-01 workflow; section_check QUAL-03 workflow; section_check QUAL-01 clean; section_check QUAL-05 clean; rg -n "mix ci|scripts/verify_docs\\.exs|mix verify|mix release\\.preflight|temporary clean|clean checkout|clean worktree|unrelated local drift|Done|Partial|Blocked" "$vf" .planning/phases/04-quality-and-release-hardening/04-PLAN.md .planning/phases/04-quality-and-release-hardening/04-SUMMARY.md >/dev/null; "$python_cmd" - <<'\''PY'\'' "$vf" "$reqs" .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md
import re, sys
vf4, reqs_path, vf1, vf2, vf3 = sys.argv[1:]
phase_vfs = [vf1, vf2, vf3, vf4]
expected = {}
for path in phase_vfs:
    text = open(path, encoding="utf-8").read()
    parts = re.split(r"^## Requirement: ", text, flags=re.M)
    for part in parts[1:]:
        header, *rest = part.splitlines()
        req = header.strip()
        body = "\n".join(rest)
        m = re.search(r"^\*\*Status:\*\* (Done|Partial|Blocked|Pending)$", body, flags=re.M)
        if m:
            expected[req] = m.group(1)
required = ["CORE-01","CORE-02","CORE-03","CORE-04","CORE-05","LAY-01","LAY-02","LAY-03","LAY-04","LAY-05","ADPT-01","ADPT-02","ADPT-03","ADPT-04","OBS-01","OBS-02","OBS-03","OBS-04","QUAL-01","QUAL-02","QUAL-03","QUAL-04","QUAL-05"]
missing = [r for r in required if r not in expected]
if missing:
    raise SystemExit(f"missing verification verdicts for: {missing}")
text = open(reqs_path, encoding="utf-8").read().splitlines()
actual = {}
for line in text:
    m = re.match(r"^\| ([A-Z0-9-]+) \| .* \| (Pending|Done|Partial|Blocked) \|$", line)
    if m:
        actual[m.group(1)] = m.group(2)
for req in required:
    if actual.get(req) != expected[req]:
        raise SystemExit(f"traceability mismatch for {req}: requirements={actual.get(req)!r} verification={expected[req]!r}")
coverage = {}
for line in text:
    m = re.match(r"^- (Verified \(Done\)|Pending verification|Partial verification|Blocked verification): (\d+)$", line)
    if m:
        coverage[m.group(1)] = int(m.group(2))
expected_counts = {
    "Verified (Done)": sum(1 for v in actual.values() if v == "Done"),
    "Pending verification": sum(1 for v in actual.values() if v == "Pending"),
    "Partial verification": sum(1 for v in actual.values() if v == "Partial"),
    "Blocked verification": sum(1 for v in actual.values() if v == "Blocked"),
}
for key, value in expected_counts.items():
    if coverage.get(key) != value:
        raise SystemExit(f"coverage mismatch for {key}: requirements={coverage.get(key)!r} expected={value!r}")
PY'</automated>
  </verify>
  <done>Phase 4 has a reconstructed triad that truthfully records the current quality/release proof surface, including mixed outcomes where the live evidence bar is not fully met, the owned Phase 4 rows in `.planning/REQUIREMENTS.md` are synchronized immediately from `04-VERIFICATION.md`, and the final coverage totals reflect all four reconstructed verification verdict sets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live code/test/command surfaces -> reconstructed verification artifacts | Evidence claims are only safe if they are grounded in executable proof at the public boundary. |
| Reconstructed verification artifacts -> `.planning/REQUIREMENTS.md` | Status changes become canonical project truth once written to the central traceability table. |
| Optional adapter / CI / release boundaries -> requirement verdicts | Compile-only or file-only signals can overstate Phoenix, CI, or release guarantees unless they are backed by the correct command or boundary test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | R | Reconstructed `01/02/03/04-VERIFICATION.md` files | mitigate | Enforce D-01 through D-05: every requirement section must cite one primary executable proof at the public boundary and treat source links as supporting evidence only. |
| T-11-02 | T | `.planning/REQUIREMENTS.md` | mitigate | Update owned rows immediately when each reconstructed phase verification closes, then recompute final totals during Phase 4 closeout per D-15 through D-18. |
| T-11-03 | E | Phase 3 Phoenix adapter proof | mitigate | Require conn- or endpoint-level proof for `ADPT-01` and `ADPT-02` where practical; if absent, allow only a narrow proving test in `test/rendro/adapters/phoenix_test.exs`, not runtime adapter edits. |
| T-11-04 | R | Phase 4 quality/release claims | mitigate | Treat `mix ci`, docs-contract checks, example-app proof, `mix verify`, and `mix release.preflight` as the decisive surfaces; record `Partial` or `Blocked` when those commands do not fully close `QUAL-03` or `QUAL-04`. |
| T-11-05 | T | Read-mostly reconstruction scope | mitigate | Keep runtime code changes out of scope per D-06 through D-09; targeted proof edits are allowed only when they prove an existing contract without changing behavior. |
</threat_model>

<verification>
- `mix test test/rendro_test.exs test/rendro/deterministic_test.exs test/rendro/telemetry_test.exs test/rendro/error_test.exs`
- `mix test test/rendro/flow_test.exs test/rendro/integration_test.exs test/rendro/metadata_test.exs test/rendro/pipeline/paginate_test.exs`
- `mix compile --no-optional-deps --warnings-as-errors`
- `mix test test/rendro/adapters/phoenix_test.exs test/rendro/adapters/oban/render_worker_test.exs test/rendro/adapters/threadline_test.exs test/rendro/policy_test.exs`
- `mix ci`
- `mix run scripts/verify_docs.exs`
- `cd examples/phoenix_example && mix deps.get && mix compile`
- `mix verify`
- `mix release.preflight`
- `rg -n "CORE-01|CORE-02|CORE-03|CORE-04|CORE-05|LAY-01|LAY-02|LAY-03|LAY-04|LAY-05|ADPT-01|ADPT-02|ADPT-03|ADPT-04|OBS-01|OBS-02|OBS-03|OBS-04|QUAL-01|QUAL-02|QUAL-03|QUAL-04|QUAL-05" .planning/phases/01-core-deterministic-foundation/01-VERIFICATION.md .planning/phases/02-layout-and-pagination-engine/02-VERIFICATION.md .planning/phases/03-adapter-and-ops-integration/03-VERIFICATION.md .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
1. Phase 1, Phase 2, Phase 3, and Phase 4 each have a zero-padded reconstructed `PLAN.md`, `SUMMARY.md`, and `VERIFICATION.md`.
2. Each reconstructed `VERIFICATION.md` is requirement-first and evidence-first, using primary executable proof at the current public boundary.
3. Phase 3 adds at most a narrowly scoped Phoenix boundary proving test; no runtime behavior changes are introduced by Phase 11.
4. Phase 4 records mixed `Done` / `Partial` / `Blocked` outcomes truthfully where the current CI or release proof surfaces do not fully close the requirement.
5. `.planning/REQUIREMENTS.md` is synchronized incrementally after each reconstructed phase verification closes, and the final coverage totals are recomputed during Phase 4 closeout from the completed verification verdicts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reconstruct-phase-1-4-artifacts/11-01-SUMMARY.md`
</output>
