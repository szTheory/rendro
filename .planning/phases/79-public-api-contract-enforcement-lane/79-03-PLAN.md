---
phase: 79-public-api-contract-enforcement-lane
plan: "03"
type: execute
wave: 2
depends_on:
  - "79-01"
files_modified:
  - scripts/verify_docs.exs
  - test/guardrails/required_checks_contract_test.exs
  - priv/guardrails/required_status_checks.json
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "scripts/verify_docs.exs lists 11 lanes total, with the public-api contract lane entry present as {\"Public API contract lane\", [\"test\", \"test/docs_contract/public_api_contract_test.exs\"]}"
    - "test/guardrails/required_checks_contract_test.exs asserts lane count == 11 (bumped from 10)"
    - "priv/guardrails/required_status_checks.json test context notes reads '11 docs-contract lanes' and includes 'Public-api contract lane added Phase 79 D-07'"
    - "mix test test/guardrails/required_checks_contract_test.exs exits 0 after the lockstep triple update"
    - "No new required_contexts[] entry added — the lane folds into the existing test context per D-07"
  artifacts:
    - path: "scripts/verify_docs.exs"
      provides: "11-lane registry including the public-api contract lane"
      contains: "Public API contract lane"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Lane count assertion updated to 11"
      contains: "== 11"
    - path: "priv/guardrails/required_status_checks.json"
      provides: "test context notes bumped to 11 lanes with Phase 79 D-07 attribution"
      contains: "Phase 79 D-07"
  key_links:
    - from: "scripts/verify_docs.exs"
      to: "test/guardrails/required_checks_contract_test.exs"
      via: "lane count regex assertion must match actual entry count"
      pattern: "lane_entries.*== 11"
    - from: "priv/guardrails/required_status_checks.json"
      to: "test/guardrails/required_checks_contract_test.exs"
      via: "notes string sub-assertions (Phase 68 D-18 and Viewer-evidence retained)"
      pattern: "Phase 68 D-18|Viewer-evidence"
---

<objective>
Update the guardrails lockstep triple — `scripts/verify_docs.exs`, `test/guardrails/required_checks_contract_test.exs`, and `priv/guardrails/required_status_checks.json` — so the public-api contract lane is registered and the lane-count assertions stay consistent (D-07). These three files MUST be updated together in the same commit; updating only some of them breaks the guardrail contract test.

Purpose: Wire the new lane into the existing `test` required status check context so any CI merge gate that checks the `test` context automatically includes the public-api contract lane. Per D-07, this is a notes-bump only — no new entry in `required_contexts[]`.

Output: Three existing files modified; all guardrail contract tests remain green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-CONTEXT.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-RESEARCH.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-PATTERNS.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the guardrails lockstep triple — verify_docs.exs, lane-count assertion, and JSON notes</name>
  <files>
    scripts/verify_docs.exs,
    test/guardrails/required_checks_contract_test.exs,
    priv/guardrails/required_status_checks.json
  </files>

  <read_first>
    - `scripts/verify_docs.exs` — MUST read the full file. Locate the `lanes = [` list (lines ~7-18). Note the current 10 entries. Identify the exact comma placement style of the last entry. The new lane entry is: `{"Public API contract lane", ["test", "test/docs_contract/public_api_contract_test.exs"]}`. Append it as the 11th entry.
    - `test/guardrails/required_checks_contract_test.exs` — MUST read the full file. Locate `describe "docs-contract lane count"` block (lines ~97-107). Note the current description string `"verify_docs.exs registers exactly ten lanes..."` and the assertion `assert length(lane_entries) == 10`. Also check the notes sub-assertions (lines ~25-27) which assert `=~ "Phase 68 D-18"` and `=~ "Viewer-evidence"` — these must still pass after the notes update.
    - `priv/guardrails/required_status_checks.json` — MUST read the full file. Locate the `"test"` context entry. Note the current `"notes"` string (currently `"Includes mix test (8 docs-contract lanes)..."` — stale; RESEARCH.md confirmed the live file has this stale count). The target notes string is: `"Includes mix test (11 docs-contract lanes), format, hex.build, compile --warnings-as-errors, docs, credo, dialyzer. Viewer-evidence schema/lint folded here per Phase 68 D-18 — not a separate required context. Public-api contract lane added Phase 79 D-07."` — this retains both "Phase 68 D-18" and "Viewer-evidence" substrings that the contract test asserts via `=~`.
  </read_first>

  <action>
    Make exactly three targeted edits — all in the same commit (CRITICAL: committing only one or two of the three breaks the guardrail contract test):

    EDIT 1 — `scripts/verify_docs.exs`: Append the new lane entry to the `lanes` list. The entry tuple shape MUST match the regex `~r/\{"[^"]+", \["test", "test\/docs_contract\/[^"]+"\]\}/` that the guardrail contract test uses to count lanes. Entry: `{"Public API contract lane", ["test", "test/docs_contract/public_api_contract_test.exs"]}`. Preserve existing formatting and comma style — add a comma after the preceding last entry (`"Page-primitive semantic-claims lane"` entry) and add the new entry as the 11th item. Result: 11 entries total.

    EDIT 2 — `test/guardrails/required_checks_contract_test.exs`: In the `describe "docs-contract lane count"` block:
    (a) Update the test description string from `"verify_docs.exs registers exactly ten lanes including the recipes and page-primitive lanes"` to `"verify_docs.exs registers exactly eleven lanes including the recipes, page-primitive, and public-api contract lanes"`.
    (b) Update `assert length(lane_entries) == 10` to `assert length(lane_entries) == 11`.
    No other changes to this file — the `=~ "Phase 68 D-18"` and `=~ "Viewer-evidence"` sub-assertions are not touched and will continue to pass.

    EDIT 3 — `priv/guardrails/required_status_checks.json`: Update only the `"notes"` value of the `"test"` context entry. Replace the existing notes string with: `"Includes mix test (11 docs-contract lanes), format, hex.build, compile --warnings-as-errors, docs, credo, dialyzer. Viewer-evidence schema/lint folded here per Phase 68 D-18 — not a separate required context. Public-api contract lane added Phase 79 D-07."`. Do NOT modify `required_contexts[]`, do NOT add a new entry to `contexts[]`, do NOT change any other field. The em dash in "D-18 —" must be preserved as a literal UTF-8 em dash character (not replaced with ASCII `--`).
  </action>

  <verify>
    <automated>
      grep -c "Public API contract lane" scripts/verify_docs.exs
    </automated>
    <automated>
      grep -c "== 11" test/guardrails/required_checks_contract_test.exs
    </automated>
    <automated>
      grep -c "Phase 79 D-07" priv/guardrails/required_status_checks.json
    </automated>
    <automated>
      grep -c "11 docs-contract lanes" priv/guardrails/required_status_checks.json
    </automated>
    <automated>
      mix test test/guardrails/required_checks_contract_test.exs
    </automated>
    <automated>
      mix test test/docs_contract/public_api_contract_test.exs
    </automated>
    <automated>
      mix ci
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Public API contract lane" scripts/verify_docs.exs` returns 1
    - `grep -c "== 11" test/guardrails/required_checks_contract_test.exs` returns 1 (the lane count assertion)
    - `grep -c "Phase 79 D-07" priv/guardrails/required_status_checks.json` returns 1
    - `grep -c "11 docs-contract lanes" priv/guardrails/required_status_checks.json` returns 1
    - `mix test test/guardrails/required_checks_contract_test.exs` exits 0 (lane count == 11 passes; notes sub-assertions for "Phase 68 D-18" and "Viewer-evidence" still pass)
    - `mix test test/docs_contract/public_api_contract_test.exs` exits 0 (lane itself still passes)
    - `mix ci` exits 0 — full phase gate green (format + compile --warnings-as-errors + test + docs + credo + dialyzer)
    - `priv/guardrails/required_status_checks.json` still has exactly 4 entries in `required_contexts[]` — no new context added (per D-07)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guardrails config → CI gate evaluation | The JSON file is read by the CI gate machinery; notes are informational only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | `priv/guardrails/required_status_checks.json` | accept | First-party checked-in config file; changes visible in git diff; the guardrail contract test (`required_checks_contract_test.exs`) asserts its invariants and would catch malicious edits |
| T-79-03-02 | Tampering | `scripts/verify_docs.exs` lane registry | accept | Script is a first-party file; lane count test provides a second layer of verification |
| T-79-03-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — three existing config/test files modified |

**Summary:** This plan modifies three config/test files to bump lane counts and notes strings. No new runtime surface, no auth/input/crypto surface. The guardrail contract test itself is a validation layer; the lockstep requirement (all three files in one commit) is a process control, not a security control.
</threat_model>

<verification>
After Plan 03 completes:
- `mix test test/guardrails/required_checks_contract_test.exs` exits 0
- `mix test test/docs_contract/public_api_contract_test.exs` exits 0
- `mix ci` exits 0 — full phase gate green
- `priv/guardrails/required_status_checks.json` `required_contexts` array still has 4 entries (no new entry added)
- `scripts/verify_docs.exs` has 11 lane entries
- All three edits are in a single commit
</verification>

<success_criteria>
- `scripts/verify_docs.exs` contains the public-api contract lane entry (11 lanes total)
- `test/guardrails/required_checks_contract_test.exs` asserts `length(lane_entries) == 11`
- `priv/guardrails/required_status_checks.json` test context notes contains "11 docs-contract lanes" and "Phase 79 D-07"
- `mix ci` exits 0 — this is the ROADMAP success criterion 4 (the lane gates merges via the existing `test` required context per D-07)
</success_criteria>

<output>
Create `.planning/phases/79-public-api-contract-enforcement-lane/79-03-SUMMARY.md` when done.
</output>
