---
phase: 79-public-api-contract-enforcement-lane
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/docs_contract/public_api_contract_test.exs
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "Adding a stray public function to a stable module fails the public-api lane with a two-list drift diff naming the new function under 'in code but NOT in manifest' and instructing 'mix rendro.api.gen'"
    - "The known internal modules (Rendro.PDF.CidFont, Rendro.PDF.FontSubsetter, Rendro.Text.Bidi, Rendro.Text.Shaper, Rendro.Format, Rendro.Audit) are asserted :hidden — if any gains a real @moduledoc the test fails"
    - "Every manifested module is asserted to carry exactly one tier tag (:stable xor :adapter) — a zero-tag or two-tag module fails the test"
    - "The @spec coverage assertion is present in the test file and starts RED (Rendro.Component has 0 specs) — the test file exists and runs, it just fails the @spec sub-assertion until Plan 02 lands"
    - "The on-disk priv/public_api.json validates against its JSON schema via Rendro.PublicApi.Validator"
  artifacts:
    - path: "test/docs_contract/public_api_contract_test.exs"
      provides: "Public API contract test covering all 4 API-04 sub-assertions"
      contains: "async: false"
    - path: "test/docs_contract/public_api_contract_test.exs"
      provides: "Manifest byte-equality assertion with two-list drift diff failure message (D-03)"
      contains: "mix rendro.api.gen"
    - path: "test/docs_contract/public_api_contract_test.exs"
      provides: "@spec coverage assertion using Code.Typespec.fetch_specs/1 (not Code.fetch_docs)"
      contains: "Code.Typespec.fetch_specs"
  key_links:
    - from: "test/docs_contract/public_api_contract_test.exs"
      to: "Rendro.PublicApi.build_manifest/1"
      via: "setup_all recompile + BEAM-availability filter"
      pattern: "recompile_conditional_adapters"
    - from: "test/docs_contract/public_api_contract_test.exs"
      to: "priv/public_api.json"
      via: "Rendro.PublicApi.Loader.load!() + byte-equality compare"
      pattern: "encode_manifest.*\\\\n"
---

<objective>
Create `test/docs_contract/public_api_contract_test.exs` — the 14th docs-contract lane — covering all four API-04 sub-assertions: (1) manifest surface equality with errors-as-product two-list drift diff (D-01/D-03), (2) schema validation via JSV (D-01), (3) known-internals :hidden assertion (D-05), (4) tier-tag exactly-one assertion (D-06), and (5) @spec coverage assertion for stable-tier functions (D-04).

Purpose: After this plan, the contract test exists and runs. Assertions 1-4 are GREEN. Assertion 5 (@spec) starts RED because `Rendro.Component` has zero specs — Plan 02 will make it green. The RED state is intentional: write the test first, backfill second (TDD order per CONTEXT.md Claude's Discretion).

Output: `test/docs_contract/public_api_contract_test.exs` (new file, ~120 lines)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-CONTEXT.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-RESEARCH.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public_api_contract_test.exs with all four API-04 assertions</name>
  <files>test/docs_contract/public_api_contract_test.exs</files>

  <read_first>
    - `test/rendro/public_api/manifest_test.exs` — PRIMARY ANALOG: the byte-equality pattern (lines ~85-106), setup_all with recompile_conditional_adapters (lines ~8-13), hidden-function assertion pattern (lines ~140-170), schema validation (lines ~16-21), tier coverage (lines ~58-73). Copy the exact BEAM-availability filter verbatim.
    - `test/docs_contract/recipes_contract_test.exs` — `async: false` declaration shape, ExUnit contract test module structure (no @moduledoc, no DocsContract alias needed here).
    - `lib/rendro/public_api.ex` — available functions: `tier_of/1`, `public_functions/1`, `public_types/1`, `build_manifest/1`, `recompile_conditional_adapters/0`.
    - `lib/rendro/public_api/loader.ex` — `Loader.load!/0` signature.
    - `lib/rendro/public_api/validator.ex` — `Validator.validate/1` signature.
    - `lib/mix/tasks/rendro/api.gen.ex` — `public_modules/0` and `encode_manifest/1` signatures; the `<> "\n"` trailing newline requirement.
    - `priv/public_api.json` — confirm the 45-module shape, "Elixir.Rendro.Component" key exists with 2 functions listed.
  </read_first>

  <action>
    Create `test/docs_contract/public_api_contract_test.exs` as `defmodule Rendro.DocsContract.PublicApiContractTest` with `use ExUnit.Case, async: false`. No @moduledoc (ExUnit codebase convention). Alias `Rendro.PublicApi`, `Rendro.PublicApi.Loader`, `Rendro.PublicApi.Validator`.

    `setup_all` block: call `PublicApi.recompile_conditional_adapters()` then `:ok` — mirrors `manifest_test.exs` pattern exactly.

    ASSERTION 1 — Schema validation (simplest, baseline green): `describe "schema validation"` with one test: `manifest = Loader.load!()` then `assert Validator.validate(manifest) == :ok`. Per D-01.

    ASSERTION 2 — Manifest surface equality (byte-equality + two-list drift diff per D-01/D-03):
    `describe "manifest surface equality (D-01/D-03)"` with one test. Per **D-02** (conditional-adapter footgun handled by the shared codepath, not re-solved): reuse the generator's `public_modules/0` registry and the BEAM-availability filter verbatim from `manifest_test.exs` lines ~85-89 — do NOT add a parallel filtering implementation in the test, and rely on the normal `mix test` env where optional adapter deps are loaded so the introspected surface matches the manifest. `Mix.Tasks.Rendro.Api.Gen.public_modules() |> Enum.filter(fn mod -> Code.ensure_loaded?(mod) and match?({:docs_v1, _, _, _, _, _, _}, Code.fetch_docs(mod)) end)`. Build `fresh_manifest` via `PublicApi.build_manifest(loaded_modules)`. Encode: `fresh_json = Mix.Tasks.Rendro.Api.Gen.encode_manifest(fresh_manifest) <> "\n"` — the `<> "\n"` is MANDATORY (generator appends it on write; omitting causes a one-byte mismatch). Read `checked_in = File.read!("priv/public_api.json")`.

    Produce the two-list diff for the failure message (D-03): derive `fresh_keys` and `on_disk_keys` from `Map.keys(fresh_manifest["modules"]) |> MapSet.new()` and the decoded on-disk manifest. Compute `in_code_not_manifested = MapSet.difference(fresh_keys, on_disk_keys) |> Enum.sort()` and `manifested_not_in_code = MapSet.difference(on_disk_keys, fresh_keys) |> Enum.sort()`. Use `flunk/1` if either list is non-empty with a heredoc message naming both lists and instructing `mix rendro.api.gen`. Then assert `fresh_json == checked_in` with a brief fallback message for within-module function drift (byte mismatch that the two-list check doesn't catch).

    ASSERTION 3 — Known internals are :hidden (D-05):
    `describe "known internal modules are :hidden (D-05)"` — two tests.

    Test A: for the 6 module atoms `[Rendro.PDF.CidFont, Rendro.PDF.FontSubsetter, Rendro.Text.Bidi, Rendro.Text.Shaper, Rendro.Format, Rendro.Audit]`, call `Code.fetch_docs(module)` and assert the result matches `{:docs_v1, _, _, _, :hidden, _, _}` or is `{:error, _}` (treat either as hidden). If the module_doc is NOT :hidden, fail with a message naming the module. Pattern from `manifest_test.exs` `redact helpers hidden` describe block.

    Test B: for hidden helper functions, assert `@doc false` on the `Rendro.Sign` helpers (`redact_opts`, `redact_prepare_opts`, `redact_sign_opts`, `redact_augment_opts`) and `Rendro.Protect` helpers (`redact_opts`). Use `Code.fetch_docs(module)` → `{:docs_v1, _, _, _, _, _, docs}`, filter by function name via pattern match `{{:function, ^name, _arity}, _, _, _, _}`, assert `doc == :hidden` for each matching entry. Mirror the exact pattern from `manifest_test.exs` lines ~140-170.

    ASSERTION 4 — Exactly one tier tag per module (D-06):
    `describe "tier-tag exactly one per module (D-06)"` with one test. Load manifest. For each module key, call `String.to_existing_atom(mod_key)` (NOT `inspect/1` — manifest keys are "Elixir.Rendro.X" form). Fetch docs, extract `tags` from `elem(5)` metadata via pattern `{:docs_v1, _, _, _, _, %{tags: tags}, _}` — default to `[]` if no match. Filter to tier tags: `tier_tags = Enum.filter(tags, &(&1 in [:stable, :adapter]))`. Collect violations where `length(tier_tags) != 1`. Assert `violations == []` with a joined list message per the error-message style pattern from PATTERNS.md.

    ASSERTION 5 — Stable-tier @spec coverage (D-04):
    `describe "stable-tier @spec coverage (D-04)"` with one test. Load manifest. Iterate modules where `entry["tier"] == "stable"`. For each: convert mod_key via `String.to_existing_atom/1`. Fetch specs via `Code.Typespec.fetch_specs(module)` — returns `{:ok, specs}` or `:error` (CRITICAL: this is NOT `Code.fetch_docs/1`; specs live in the abstract_code BEAM chunk, not the Docs chunk). Build `specced = Enum.map(specs, fn {{name, arity}, _} -> "#{name}/#{arity}" end)`. Cross-reference `entry["functions"]` against specced list. Collect unspecced as `"#{mod_key}.#{fn_str}"` strings. Assert `unspecced == []` with a message listing unspecced functions. This assertion will be RED until Plan 02 adds @specs to `Rendro.Component`.
  </action>

  <verify>
    <automated>
      mix test test/docs_contract/public_api_contract_test.exs --include pending 2>&1 | head -40
    </automated>
    <automated>
      grep -c "async: false" test/docs_contract/public_api_contract_test.exs
    </automated>
    <automated>
      grep -c "Code.Typespec.fetch_specs" test/docs_contract/public_api_contract_test.exs
    </automated>
    <automated>
      grep -c "mix rendro.api.gen" test/docs_contract/public_api_contract_test.exs
    </automated>
    <automated>
      grep -c "encode_manifest.*\\\\\\\\n\\\|encode_manifest.*<> .\\\\\\\\n" test/docs_contract/public_api_contract_test.exs
    </automated>
  </verify>

  <acceptance_criteria>
    - `test/docs_contract/public_api_contract_test.exs` exists
    - File contains `async: false` (required for contract tests, not claims tests)
    - File contains `Code.Typespec.fetch_specs` (NOT `Code.fetch_docs` for spec checking — critical pitfall)
    - File contains `mix rendro.api.gen` (in the drift-diff failure message per D-03)
    - File contains `encode_manifest` with `<> "\n"` trailing newline (byte-equality pitfall avoidance)
    - File contains `String.to_existing_atom` (for manifest key → module atom conversion; NOT `inspect/1`)
    - Running `mix test test/docs_contract/public_api_contract_test.exs` reports: schema, surface equality, hidden-internals, and tier-tag tests PASS; @spec coverage test FAILS with a message naming `"Elixir.Rendro.Component.image/2"` and `"Elixir.Rendro.Component.render_component/2"` (expected RED state)
    - `mix compile --warnings-as-errors` exits 0 (no new compiler warnings from the test file)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → priv/public_api.json | Test reads first-party checked-in file; no external input |
| manifest key → module atom | `String.to_existing_atom/1` used for key → atom conversion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | `priv/public_api.json` | accept | First-party checked-in file; any tampering is visible in git diff; no external write path in this plan |
| T-79-01-02 | Elevation of Privilege | `String.to_existing_atom/1` in test | accept | `String.to_existing_atom/1` is safe — only atoms already in the BEAM atom table can be created; manifest keys are "Elixir.*" form generated from first-party source; no user-controlled input path |
| T-79-01-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — pure Elixir test file, no new dependencies |

**Summary:** This plan is pure test-writing. No new runtime attack surface, no auth/input/crypto surface. ASVS L1 controls are not applicable here — the test reads only first-party checked-in files and uses stdlib introspection. The honest threat model is minimal.
</threat_model>

<verification>
After Plan 01 completes:
- `mix test test/docs_contract/public_api_contract_test.exs` — assertions 1-4 GREEN, assertion 5 (@spec) RED with message naming Rendro.Component functions
- `mix compile --warnings-as-errors` exits 0
- `grep -c "async: false" test/docs_contract/public_api_contract_test.exs` returns 1
</verification>

<success_criteria>
- `test/docs_contract/public_api_contract_test.exs` exists with all 4 API-04 sub-assertions coded
- Assertions 1-4 (schema, surface equality, hidden internals, tier-tag) pass
- Assertion 5 (@spec coverage) intentionally fails with a human-readable list naming `Elixir.Rendro.Component.image/2` and `Elixir.Rendro.Component.render_component/2`
- The drift-diff failure message implements D-03: two named lists + `mix rendro.api.gen` instruction
- `mix compile --warnings-as-errors` exits 0
</success_criteria>

<output>
Create `.planning/phases/79-public-api-contract-enforcement-lane/79-01-SUMMARY.md` when done.
</output>
