---
phase: 79-public-api-contract-enforcement-lane
plan: "02"
type: execute
wave: 1
depends_on:
  - "79-01"
files_modified:
  - lib/rendro/component.ex
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "The @spec coverage assertion in public_api_contract_test.exs turns GREEN — Rendro.Component.image/2 and Rendro.Component.render_component/2 now have @spec annotations"
    - "mix ci dialyzer passes — the two new @spec annotations are valid and dialyzer-verified"
    - "No other stable-tier module surfaces unspecced functions (all 12 other stable modules with public functions were already fully specced per RESEARCH.md audit)"
  artifacts:
    - path: "lib/rendro/component.ex"
      provides: "@spec render_component(module(), keyword()) :: term() before def render_component"
      contains: "@spec render_component("
    - path: "lib/rendro/component.ex"
      provides: "@spec image(atom(), keyword()) :: Rendro.Block.t() before def image"
      contains: "@spec image("
  key_links:
    - from: "lib/rendro/component.ex"
      to: "test/docs_contract/public_api_contract_test.exs"
      via: "Code.Typespec.fetch_specs(Rendro.Component) now returns both function specs"
      pattern: "@spec render_component|@spec image"
---

<objective>
Add the two missing `@spec` annotations to `lib/rendro/component.ex`, turning the @spec coverage assertion (Plan 01, Assertion 5) from RED to GREEN. This is the entire scope of the D-04 `@spec` backfill — RESEARCH.md verified that only `Rendro.Component` (2 public functions, 0 specs) is missing specs among the 27 stable-tier modules.

Purpose: Close the last gap in the stable-tier @spec surface so `mix ci` (which runs dialyzer) can validate the complete Tier-1 contract.

Output: `lib/rendro/component.ex` modified — 2 `@spec` annotations added, no other changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-CONTEXT.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-RESEARCH.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-PATTERNS.md
@.planning/phases/79-public-api-contract-enforcement-lane/79-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @spec annotations to Rendro.Component's two public functions</name>
  <files>lib/rendro/component.ex</files>

  <read_first>
    - `lib/rendro/component.ex` — MUST read the full file. Locate `def render_component(module, assigns \\ [])` and `def image(logical_name, opts \\ [])`. Note the exact `@doc` block position before each `def` — specs go between `@doc` block and `def`.
    - `lib/rendro/text.ex` — PRIMARY ANALOG for `@spec` placement idiom: `@doc` block, then `@spec` on the line immediately before `def`, no blank line between `@spec` and `def`. Read lines ~41-52 for the exact two-attribute pattern.
    - `lib/rendro/artifact.ex` — shows `@spec` with default-argument functions (lines ~23-25 and ~42-44): spec types match the non-default arity, `\\ []` in `def` is fine with single `@spec`.
    - `test/docs_contract/public_api_contract_test.exs` — confirm the @spec assertion is currently RED naming both component functions; this is the test this task makes GREEN.
  </read_first>

  <action>
    Add exactly two `@spec` annotations to `lib/rendro/component.ex`. Do not change any other content.

    For `render_component/2`: insert `@spec render_component(module(), keyword()) :: term()` on the line immediately before `def render_component(module, assigns \\ []) do`. Place it after the `@doc` block (if any blank lines exist between @doc and def, insert spec before the def, maintaining the `@doc → @spec → def` order from `text.ex`). Return type is `term()` because the function delegates to `module.render(assigns)` with no return type constraint on the callback.

    For `image/2`: insert `@spec image(atom(), keyword()) :: Rendro.Block.t()` on the line immediately before `def image(logical_name, opts \\ []) do`. Return type is `Rendro.Block.t()` — the implementation creates and returns a `%Rendro.Block{}` struct.

    Style: no blank line between `@spec` and `def` (matches `text.ex` and `artifact.ex` analogs). The `\\ []` default in the `def` head is compatible with a single `@spec` — do not add `@spec` overloads.
  </action>

  <verify>
    <automated>
      grep -c "@spec render_component(" lib/rendro/component.ex
    </automated>
    <automated>
      grep -c "@spec image(" lib/rendro/component.ex
    </automated>
    <automated>
      mix test test/docs_contract/public_api_contract_test.exs
    </automated>
    <automated>
      mix compile --warnings-as-errors
    </automated>
    <automated>
      mix ci
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "@spec render_component(" lib/rendro/component.ex` returns 1
    - `grep -c "@spec image(" lib/rendro/component.ex` returns 1
    - `mix test test/docs_contract/public_api_contract_test.exs` exits 0 — ALL assertions now GREEN including the @spec coverage assertion
    - `mix compile --warnings-as-errors` exits 0 — new @spec annotations produce no dialyzer warnings (dialyzer is run in `mix ci`)
    - `mix ci` exits 0 (format + compile --warnings-as-errors + test + docs + credo + dialyzer all green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source edit → compiled BEAM | @spec annotations are compile-time metadata; no runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | `lib/rendro/component.ex` @spec annotations | accept | @spec annotations are additive compile-time metadata with no runtime behavior; dialyzer validates correctness; no new attack surface |
| T-79-02-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs — purely additive @spec annotations on an existing Elixir module |

**Summary:** This plan adds two @spec annotations to an existing module. No new runtime surface, no auth/input/crypto surface. ASVS L1 controls are not applicable. Dialyzer (run in mix ci) validates type correctness of the new specs for free.
</threat_model>

<verification>
After Plan 02 completes:
- `mix test test/docs_contract/public_api_contract_test.exs` exits 0 — all 5 assertions GREEN
- `mix ci` exits 0 — dialyzer validates both new @spec annotations
- `grep "@spec render_component\|@spec image" lib/rendro/component.ex` returns 2 lines
</verification>

<success_criteria>
- `lib/rendro/component.ex` contains `@spec render_component(module(), keyword()) :: term()`
- `lib/rendro/component.ex` contains `@spec image(atom(), keyword()) :: Rendro.Block.t()`
- `mix test test/docs_contract/public_api_contract_test.exs` exits 0
- `mix ci` exits 0
</success_criteria>

<output>
Create `.planning/phases/79-public-api-contract-enforcement-lane/79-02-SUMMARY.md` when done.
</output>
