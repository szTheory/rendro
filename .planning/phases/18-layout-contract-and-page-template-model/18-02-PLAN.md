---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/rendro/pipeline/compose.ex
  - lib/rendro/pipeline/measure.ex
  - lib/rendro/pipeline/paginate.ex
  - lib/rendro/document.ex
  - test/rendro/pipeline/compose_test.exs
  - test/rendro/pipeline/measure_test.exs
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/flow_test.exs
autonomous: true
requirements: [LAY-07, LAY-08]

must_haves:
  truths:
    - "Flow documents are paginated from explicit page-template data, not from an implicit `%Rendro.Page{}` fallback."
    - "Sections and named regions normalize through the existing compose/measure/paginate pipeline."
    - "Headers and footers are anchored from template regions with deterministic reserved space semantics."
  artifacts:
    - path: "lib/rendro/pipeline/compose.ex"
      provides: "Normalization from authored sections/regions into internal block/page structures"
      min_lines: 25
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Template-backed page creation and anchored region pagination"
      min_lines: 40
  key_links:
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "test/rendro/flow_test.exs"
      via: "flow template pagination behavior"
      pattern: "page template"
---

<objective>
Normalize template-backed flow authoring into the current render core so sections and named regions become real engine inputs rather than options conventions.

Purpose: make page templates, body regions, and anchored header/footer behavior deterministic inside the pipeline before later text/table work builds on them.
Output: compose/measure/paginate changes and focused flow/pipeline regression proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-layout-contract-and-page-template-model/18-RESEARCH.md
@.planning/phases/18-layout-contract-and-page-template-model/18-PATTERNS.md
@.planning/phases/18-layout-contract-and-page-template-model/18-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize authored sections and regions during compose/measure</name>
  <files>lib/rendro/pipeline/compose.ex, lib/rendro/pipeline/measure.ex, test/rendro/pipeline/compose_test.exs, test/rendro/pipeline/measure_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/compose.ex
    - lib/rendro/pipeline/measure.ex
    - test/rendro/pipeline/compose_test.exs
    - test/rendro/pipeline/measure_test.exs
    - lib/rendro/document.ex
    - lib/rendro/page_template.ex
    - lib/rendro/region.ex
    - lib/rendro/section.ex
  </read_first>
  <action>
    Update `Compose` and `Measure` so authored `page_templates`, `sections`, and named regions normalize into one internal document shape before pagination. Make the body region explicit, preserve reusable section ordering deterministically, and reserve header/footer region dimensions from the template instead of deriving them only from raw block heights. Add unit tests that prove region/section normalization happens before pagination and that the measured body capacity reflects reserved template regions.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/compose.ex` contains normalization logic for sections or named regions.
    - `lib/rendro/pipeline/measure.ex` contains explicit handling for template/body/header/footer region dimensions.
    - `test/rendro/pipeline/compose_test.exs` contains a section/region normalization proof.
    - `mix test test/rendro/pipeline/compose_test.exs test/rendro/pipeline/measure_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/compose_test.exs test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Authored template data is normalized and measured deterministically before page assignment.</done>
</task>

<task type="auto">
  <name>Task 2: Paginate flow content from explicit page templates and anchored regions</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - test/rendro/pipeline/paginate_test.exs
    - test/rendro/flow_test.exs
    - lib/rendro/pipeline/compose.ex
    - lib/rendro/pipeline/measure.ex
    - .planning/phases/18-layout-contract-and-page-template-model/18-RESEARCH.md
  </read_first>
  <action>
    Replace the hard-coded flow `template = %Rendro.Page{}` path with pagination that materializes pages from the authored `Rendro.PageTemplate`. Anchor header and footer content from declared template regions, reserve body-region capacity deterministically, and keep section ordering stable across pages. Extend pipeline and flow tests to prove explicit template geometry is used, repeated header/footer regions stay predictable, and flow content no longer depends on an invisible default page contract.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/paginate.ex` no longer relies solely on `template = %Rendro.Page{}` for flow documents.
    - `test/rendro/pipeline/paginate_test.exs` contains a proof that page-template geometry drives pagination.
    - `test/rendro/flow_test.exs` contains a proof for anchored header/footer region behavior.
    - `mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>Flow pagination uses explicit template and region semantics through the core engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline normalization | Authored section/region data must not create a second rendering path or non-deterministic ordering. |
| Page-template pagination | Reserved region geometry must stay stable across identical inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | `Compose` / `Paginate` normalization | mitigate | Normalize all authored layout data into one internal shape before pagination and cover ordering with unit tests. |
| T-18-04 | Denial of service | Region-capacity math | mitigate | Keep deterministic body-capacity calculations explicit and fail early when regions leave no usable space. |
</threat_model>

<verification>
Run `mix test test/rendro/pipeline/compose_test.exs test/rendro/pipeline/measure_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs` to prove template-backed sections and anchored regions behave deterministically.
</verification>

<success_criteria>
- Compose/measure/paginate consume explicit page-template and region data.
- Header/footer reservation comes from real region geometry.
- Flow API tests prove explicit template-backed pagination instead of implicit defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/18-layout-contract-and-page-template-model/18-02-SUMMARY.md`
</output>
