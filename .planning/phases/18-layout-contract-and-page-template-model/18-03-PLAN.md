---
phase: 18
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/rendro/pipeline/paginate.ex
  - lib/rendro/error.ex
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/flow_test.exs
  - test/rendro/integration_test.exs
autonomous: true
requirements: [LAY-11]

must_haves:
  truths:
    - "Fixed-position pages fail truthfully when blocks exceed declared page or region bounds."
    - "Bounded flow regions fail with deterministic structured errors instead of silent clipping or late render surprises."
    - "Regression tests prove fit-validation behavior directly at the public `Rendro.render/1` boundary."
  artifacts:
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Truthful fixed-position and region-fit validation"
      min_lines: 35
    - path: "test/rendro/flow_test.exs"
      provides: "Public overflow regression proofs"
      min_lines: 20
  key_links:
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "test/rendro/flow_test.exs"
      via: "structured fit-validation errors"
      pattern: ":content_overflow"
---

<objective>
Close the truthful-fit requirement by adding deterministic overflow validation for fixed-position pages and bounded flow regions, with regression proof at the public render boundary.

Purpose: ensure Phase 18 does not ship a stronger layout contract while still allowing out-of-bounds authored content to pass silently.
Output: fit-validation logic, stable error details, and regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-layout-contract-and-page-template-model/18-RESEARCH.md
@.planning/phases/18-layout-contract-and-page-template-model/18-PATTERNS.md
@.planning/phases/18-layout-contract-and-page-template-model/18-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate fixed-position and bounded-region fit before render</name>
  <files>lib/rendro/pipeline/paginate.ex, lib/rendro/error.ex, test/rendro/pipeline/paginate_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - lib/rendro/error.ex
    - test/rendro/pipeline/paginate_test.exs
    - lib/rendro/page.ex
    - lib/rendro/page_template.ex
    - lib/rendro/region.ex
  </read_first>
  <action>
    Add explicit geometry validation for authored fixed-position pages and bounded template regions before render succeeds. Validate that each block's `x`, `y`, `width`, and `height` stays within the page or target region rectangle, and return deterministic `%Rendro.Error{stage: :paginate, reason: :content_overflow}` metadata that identifies whether the overflow came from a fixed page or a bounded region. Extend pipeline tests to prove fixed-page overflow is now rejected instead of passing through untouched.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/paginate.ex` contains explicit fit validation for fixed pages or bounded regions.
    - `test/rendro/pipeline/paginate_test.exs` contains a fixed-page overflow failure proof.
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Out-of-bounds authored content fails truthfully before render.</done>
</task>

<task type="auto">
  <name>Task 2: Prove truthful fit failures at the public render boundary</name>
  <files>test/rendro/flow_test.exs, test/rendro/integration_test.exs</files>
  <read_first>
    - test/rendro/flow_test.exs
    - test/rendro/integration_test.exs
    - lib/rendro/pipeline/paginate.ex
    - lib/rendro/error.ex
    - .planning/phases/18-layout-contract-and-page-template-model/18-RESEARCH.md
  </read_first>
  <action>
    Add regression tests at the public `Rendro.render/1` boundary covering both bounded flow-region overflow and fixed-position page overflow. Assert the returned error stage is `:paginate`, the reason is `:content_overflow`, and the `next` guidance stays truthful about reducing content size or expanding declared bounds instead of implying unsupported auto-fit behavior.
  </action>
  <acceptance_criteria>
    - `test/rendro/flow_test.exs` contains a bounded-region overflow proof.
    - `test/rendro/integration_test.exs` or `test/rendro/flow_test.exs` contains a fixed-position overflow proof at `Rendro.render/1`.
    - `mix test test/rendro/flow_test.exs test/rendro/integration_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/flow_test.exs test/rendro/integration_test.exs</automated>
  </verify>
  <done>The public render API now proves truthful fit validation for both fixed and flow layouts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pagination failure surface | Overflow errors are part of the product contract and must stay truthful. |
| Public render boundary | Users should never infer that Rendro silently clips or auto-fits unsupported layouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Repudiation | Overflow diagnostics | mitigate | Return explicit `:content_overflow` errors with deterministic stage and actionable guidance. |
| T-18-06 | Tampering | Fixed-page pass-through path | mitigate | Remove silent success for out-of-bounds fixed pages and cover the path with regression tests. |
</threat_model>

<verification>
Run `mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs test/rendro/integration_test.exs` to prove truthful fit validation at both internal and public boundaries.
</verification>

<success_criteria>
- Fixed-position overflow no longer passes through pagination silently.
- Flow-region overflow returns deterministic structured errors.
- Public render tests prove truthful guidance and stage/reason metadata for layout-fit failures.
</success_criteria>

<output>
After completion, create `.planning/phases/18-layout-contract-and-page-template-model/18-03-SUMMARY.md`
</output>
