---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro.ex
  - lib/rendro/document.ex
  - lib/rendro/page.ex
  - lib/rendro/page_template.ex
  - lib/rendro/region.ex
  - lib/rendro/section.ex
  - test/rendro/document_test.exs
  - test/rendro/page_test.exs
  - test/rendro_builders_test.exs
autonomous: true
requirements: [LAY-07, LAY-08]

must_haves:
  truths:
    - "Flow documents can name an explicit page template instead of relying on an implicit `%Rendro.Page{}` default."
    - "Sections and bounded regions exist as first-class authoring structs in the public API."
    - "Header and footer geometry are represented by real region data rather than only top-level block lists."
  artifacts:
    - path: "lib/rendro/page_template.ex"
      provides: "Explicit page template struct with page geometry and named regions"
      min_lines: 30
    - path: "lib/rendro/region.ex"
      provides: "Bounded region struct with anchor/role metadata"
      min_lines: 25
    - path: "lib/rendro/section.ex"
      provides: "Reusable flow section struct targeting named regions"
      min_lines: 20
  key_links:
    - from: "lib/rendro.ex"
      to: "test/rendro_builders_test.exs"
      via: "public builder contract"
      pattern: "Rendro.page_template"
---

<objective>
Introduce the public layout contract for Phase 18 by adding explicit page-template, region, and section structs plus builder APIs, and wire `Rendro.Document` so flow documents can carry this data without depending on hidden defaults.

Purpose: establish the authoring surface that later pipeline work will normalize and paginate.
Output: new domain structs, updated document/page types, and builder proofs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-layout-contract-and-page-template-model/18-RESEARCH.md
@.planning/phases/18-layout-contract-and-page-template-model/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit layout structs and defaults</name>
  <files>lib/rendro/page_template.ex, lib/rendro/region.ex, lib/rendro/section.ex, lib/rendro/document.ex, lib/rendro/page.ex</files>
  <read_first>
    - lib/rendro/document.ex
    - lib/rendro/page.ex
    - lib/rendro/block.ex
    - .planning/phases/18-layout-contract-and-page-template-model/18-RESEARCH.md
    - .planning/phases/18-layout-contract-and-page-template-model/18-PATTERNS.md
  </read_first>
  <action>
    Create `Rendro.PageTemplate`, `Rendro.Region`, and `Rendro.Section` as plain structs with explicit defaults and type specs. Update `Rendro.Document` so it can carry `page_templates`, `sections`, and a template reference for flow documents while keeping compatibility for existing fixed-page and flat-content inputs. Keep geometry defaults aligned with today's `%Rendro.Page{}` values (`width: 595.28`, `height: 841.89`, `margin_*: 72`) and represent header/footer as named regions on the template rather than hidden top-level-only semantics.
  </action>
  <acceptance_criteria>
    - `lib/rendro/page_template.ex` exists and contains `defmodule Rendro.PageTemplate`.
    - `lib/rendro/region.ex` exists and contains `defmodule Rendro.Region`.
    - `lib/rendro/section.ex` exists and contains `defmodule Rendro.Section`.
    - `lib/rendro/document.ex` contains `page_templates` and `sections` fields in the struct/type definition.
    - `mix test test/rendro/document_test.exs test/rendro/page_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/document_test.exs test/rendro/page_test.exs</automated>
  </verify>
  <done>Layout primitives and document shape exist as deterministic public data.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the new authoring surface through public builders</name>
  <files>lib/rendro.ex, test/rendro_builders_test.exs</files>
  <read_first>
    - lib/rendro.ex
    - test/rendro_builders_test.exs
    - lib/rendro/document.ex
    - lib/rendro/page_template.ex
    - lib/rendro/region.ex
    - lib/rendro/section.ex
  </read_first>
  <action>
    Add builder functions to `Rendro` for `page_template/1`, `region/1`, and `section/1`, and update `flow/2` or `document/1` helper expectations so authored flow documents can pass explicit template/section data. Extend `test/rendro_builders_test.exs` to prove the new builders are pure, carry deterministic defaults, and reject unknown keys through `struct!` just like the existing API.
  </action>
  <acceptance_criteria>
    - `lib/rendro.ex` contains `def page_template(attrs \\\\ [])`, `def region(attrs \\\\ [])`, and `def section(attrs \\\\ [])`.
    - `test/rendro_builders_test.exs` contains tests covering the new builders.
    - `mix test test/rendro_builders_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>
  <done>The public API exposes template and region authoring without hidden conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public authoring API | User-authored layout data must stay deterministic and reject unsupported keys or shapes. |
| Layout geometry contract | Template and region defaults become externally depended-on semantics once published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `Rendro.Document` / builders | mitigate | Keep layout structs pure and explicit; reject unknown keys with `struct!` and cover defaults in tests. |
| T-18-02 | Information disclosure | Public layout surface | mitigate | Do not expose internal pagination-only fields in the public contract; keep structs focused on authoring data. |
</threat_model>

<verification>
Run `mix test test/rendro/document_test.exs test/rendro/page_test.exs test/rendro_builders_test.exs` to prove the new public layout contract is stable and deterministic.
</verification>

<success_criteria>
- Explicit page-template, region, and section structs exist in `lib/rendro/`.
- `Rendro.Document` can represent template-backed flow authoring without losing existing fixed-page compatibility.
- Builder tests prove the new authoring surface is pure, deterministic, and key-checked.
</success_criteria>

<output>
After completion, create `.planning/phases/18-layout-contract-and-page-template-model/18-01-SUMMARY.md`
</output>
