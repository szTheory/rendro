---
phase: 55
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - guides/api_stability.md
  - priv/support_matrix.json
  - test/docs_contract/forms_claims_test.exs
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
must_haves:
  truths:
    - Phase 55 exposes unsigned signature-field authoring only; actual digital signatures remain unsupported.
    - The support matrix and API-stability wording distinguish the new authored helper contract from still-unsupported rendered signature-widget, digital-signature, compliance, and viewer-proof claims.
    - Docs-contract coverage fails if code, prose, and machine-readable support boundaries drift apart.
  artifacts:
    - path: priv/support_matrix.json
      provides: "Machine-readable separation between the authored helper contract and still-unsupported rendered/digital-signature claims"
    - path: guides/api_stability.md
      provides: "Human-readable explanation of the Phase 55 unsigned-only support boundary"
    - path: test/docs_contract/forms_claims_test.exs
      provides: "Executable lock tying forms wording and matrix claims to the same narrow contract"
  key_links:
    - from: priv/support_matrix.json
      to: guides/api_stability.md
      via: "machine-readable signature support posture must match the published unsigned-only wording"
      pattern: "signature|digital_signatures|unsupported|supported"
    - from: test/docs_contract/forms_claims_test.exs
      to: "priv/support_matrix.json"
      via: "docs-contract assertions must fail if signature support or digital-signature boundaries drift"
      pattern: "signature_field|unsigned|digital_signatures"
---

<objective>
Publish the truthful unsigned-signature support boundary and lock it with docs-contract coverage.

Purpose: satisfy the documentation/support side of `SIGN-01` and `SIGN-02` by updating the forms support contract to reflect unsigned authored signature placeholders while keeping actual digital signatures, compliance narratives, and viewer claims deferred.
Output: synchronized support-matrix rows, API-stability wording, and docs-contract assertions that keep Phase 55's contract small and explicit.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v2.0-ROADMAP.md
@.planning/phases/55-signature-field-authoring-contract/55-CONTEXT.md
@.planning/phases/55-signature-field-authoring-contract/55-RESEARCH.md
@.planning/phases/55-signature-field-authoring-contract/55-PATTERNS.md
@.planning/phases/47-form-validation-and-viewer-proof-closure/47-02-PLAN.md
@.planning/phases/51-protection-api-contract-and-validation/51-CONTEXT.md
</execution_context>

<context>
@guides/api_stability.md
@priv/support_matrix.json
@test/docs_contract/forms_claims_test.exs
@scripts/verify_docs.exs

<interfaces>
From `guides/api_stability.md`:
```md
## Interactive Forms Support Boundary

Rendro supports authored AcroForm text fields, checkboxes, and radio groups.
```

From `priv/support_matrix.json`:
```json
"forms": {
  "widgets": {
    "text": "supported",
    "checkbox": "supported",
    "radio": "supported",
    "signature": "unsupported"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update the forms support contract to publish unsigned signature-field support without widening into digital-signature claims</name>
  <read_first>
    - priv/support_matrix.json
    - guides/api_stability.md
    - test/docs_contract/forms_claims_test.exs
    - .planning/phases/55-signature-field-authoring-contract/55-CONTEXT.md
    - .planning/phases/55-signature-field-authoring-contract/55-RESEARCH.md
  </read_first>
  <files>priv/support_matrix.json, guides/api_stability.md, test/docs_contract/forms_claims_test.exs</files>
  <acceptance_criteria>
    - `priv/support_matrix.json` still says `"signature": "unsupported"` under `forms.widgets` until Phase 56 serialization exists
    - `priv/support_matrix.json` still says `"digital_signatures": "unsupported"` somewhere in the public support contract
    - `guides/api_stability.md` contains wording that distinguishes the new authored `Rendro.signature_field/2` contract from unsupported rendered signature widgets and digital signatures
    - `guides/api_stability.md` does not claim viewer support, tamper evidence, compliance, PAdES, LTV, TSA, OCSP, or CRL support for the Phase 55 signature surface
    - `test/docs_contract/forms_claims_test.exs` asserts that the helper is named while `forms.widgets.signature` and `digital_signatures` both remain unsupported in the public support contract
  </acceptance_criteria>
  <action>
    Keep `priv/support_matrix.json` conservative in Phase 55. The public `forms.widgets.signature` row should remain `unsupported` because actual signature-widget serialization and proof are deferred to later phases, and `digital_signatures` must remain unsupported as the broader trust boundary. If a matrix change is needed, make it a narrow authored-contract annotation that does not read as rendered/widget support; do not flip the widget row itself to supported in this phase.
    Update `guides/api_stability.md` to explain the new smallest truthful contract in one place: Rendro now exposes `Rendro.signature_field/2` as an authored unsigned placeholder API, but Phase 55 does not yet claim rendered signature-widget support, actual digital signatures, signer metadata support, cryptographic trust operations, compliance narratives, or viewer-promotion claims. Preserve the existing structural-vs-viewer distinction and make the authored-vs-rendered distinction explicit.
    Extend `test/docs_contract/forms_claims_test.exs` so it locks the same language across the guide and support matrix. The assertions should prove that the helper is named, that `forms.widgets.signature` and `digital_signatures` remain unsupported in the public support contract, and that wording stays narrow enough to reject blanket claims about rendered signature support, signature validity, compliance, or viewer behavior.
  </action>
  <verify>
    <automated>mix test test/docs_contract/forms_claims_test.exs</automated>
  </verify>
  <done>The published forms contract now names the authored helper explicitly while still keeping rendered signature support and digital-signature claims out of the public supported surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the docs verification lane aligned with the new signature-field contract</name>
  <read_first>
    - test/docs_contract/forms_claims_test.exs
    - scripts/verify_docs.exs
    - guides/api_stability.md
    - priv/support_matrix.json
  </read_first>
  <files>test/docs_contract/forms_claims_test.exs</files>
  <acceptance_criteria>
    - `mix run scripts/verify_docs.exs` is listed in this plan's verification section
    - `test/docs_contract/forms_claims_test.exs` contains at least one negative assertion against broad digital-signature claims
    - `test/docs_contract/forms_claims_test.exs` contains at least one assertion that the guide/support matrix distinguish unsigned signature fields from unsupported digital signatures
    - No task in this plan edits writer, adapter, or signing-preparation implementation files
  </acceptance_criteria>
  <action>
    Tighten `test/docs_contract/forms_claims_test.exs` so the canonical docs verification lane catches both kinds of Phase 55 drift: forgetting to mention the new `Rendro.signature_field/2` helper at all, and accidental widening into rendered signature-widget support or “digital signatures supported” claims. Reuse the existing docs-contract style of exact-string and regex assertions, and keep the lane focused on contract truth rather than implementation details.
    Do not open a second docs workflow or add a new guide. Reuse the existing forms claims lane and the existing `scripts/verify_docs.exs` path as the canonical contract gate, so future Phase 56 and 57 changes extend the same verification lane rather than fragmenting it.
  </action>
  <verify>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>The existing docs verification lane fails on either underclaiming or overclaiming the new unsigned signature-field contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal forms capability -> public support contract | Code-level authored support becomes external product claims here. |
| Unsigned field support -> digital-signature expectations | The boundary between placeholder authoring and cryptographic trust behavior is easiest to blur here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Spoofing / semantic confusion | `guides/api_stability.md` | mitigate | Explicitly distinguish unsigned signature fields from unsupported digital signatures and broader compliance narratives. |
| T-55-05 | Repudiation | `priv/support_matrix.json` + docs-contract tests | mitigate | Keep one machine-readable and one tested human-readable source of truth so support claims remain auditable. |
| T-55-06 | Scope drift | public docs | mitigate | Refute viewer, tamper-evidence, and compliance claims until later proof-backed phases land. |
</threat_model>

<verification>
mix test test/docs_contract/forms_claims_test.exs
mix run scripts/verify_docs.exs
</verification>

<success_criteria>
- Public docs and support metadata name the authored signature-field helper while keeping the rendered/widget support row conservative.
- `forms.widgets.signature`, `digital_signatures`, and related trust/compliance narratives remain explicitly unsupported.
- Docs-contract tests fail if the Phase 55 contract either overclaims rendered/signing support or omits the new authored-helper boundary.
- No signing-preparation API, writer serialization work, or viewer-proof promotion is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/55-signature-field-authoring-contract/55-02-SUMMARY.md`
</output>
