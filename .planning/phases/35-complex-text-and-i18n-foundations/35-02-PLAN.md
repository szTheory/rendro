---
phase: 35
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified: ["lib/rendro/pdf/font_subsetter.ex", "test/rendro/pdf/font_subsetter_test.exs"]
autonomous: true
requirements: ["Typography"]
must_haves:
  truths:
    - "PDF artifact sizes remain within reasonable bounds despite using complex custom fonts, proving advanced font subsetting works."
    - "Resulting TrueType subsets render correctly in standard PDF viewers."
  artifacts:
    - path: "lib/rendro/pdf/font_subsetter.ex"
  key_links:
    - from: "lib/rendro/pdf/font_subsetter.ex"
      to: "PDF Pipeline"
      via: "glyph table stripping"
---

<objective>
Implement the pure Elixir TrueType binary subsetter.

Purpose: Strip unused glyphs from massive TrueType fonts (e.g. CJK) to prevent bloating the final PDF output.
Output: `Rendro.PDF.FontSubsetter` module handling TrueType subset generation.
</objective>

<context>
@.planning/phases/35-complex-text-and-i18n-foundations/35-RESEARCH.md
@.planning/phases/35-complex-text-and-i18n-foundations/35-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure Elixir TrueType Subsetter</name>
  <files>lib/rendro/pdf/font_subsetter.ex, test/rendro/pdf/font_subsetter_test.exs</files>
  <behavior>
    - Strips unused glyphs and updates `loca` / `glyf` tables correctly.
    - Recalculates whole-font checksum logic rigorously.
  </behavior>
  <action>
    Implement `Rendro.PDF.FontSubsetter` to process TTF binaries at write-time, stripping `glyf`/`loca` tables of unused glyphs. Keep the core generation pipeline native and aligned with Elixir's strengths in binary pattern matching. Avoid Harfbuzz NIF subsetting; stay pure Elixir here.
    **Pattern Reference:** Follow the exact Binary Processing Pattern modeled in `lib/rendro/pdf/font_parser.ex` (as detailed in PATTERNS.md). Use `with` clauses and precise binary matching to traverse the TTF tables. Ensure `loca` byte offsets, `maxp` counts, `hmtx` widths, and whole-font checksums are cleanly recalculated to avoid font corruption.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/font_subsetter_test.exs</automated>
  </verify>
  <done>PDF artifact sizes remain within reasonable bounds when subsetting complex CJK fonts.</done>
</task>

</tasks>
