---
phase: 35
plan: 04
type: execute
wave: 3
depends_on: ["35-01", "35-02", "35-03"]
files_modified: ["lib/rendro/pipeline/build.ex", "lib/rendro/pipeline/compose.ex", "lib/rendro/pipeline/measure.ex", "test/rendro/pipeline/measure_test.exs"]
autonomous: true
requirements: ["Typography", "i18n"]
must_haves:
  truths:
    - "Document generation pipeline successfully processes mixed character sets end-to-end without layout overlap."
    - "Pipeline accurately calculates bounding boxes based on HarfBuzz glyph measurements, not grapheme lengths."
  artifacts:
    - path: "lib/rendro/pipeline/measure.ex"
    - path: "lib/rendro/pipeline/compose.ex"
  key_links:
    - from: "lib/rendro/pipeline/measure.ex"
      to: "lib/rendro/text/shaper.ex"
      via: "fetches precise bounds"
---

<objective>
Align Build, Compose, and Measure pipelines to handle shaped text runs.

Purpose: Transition the layout calculation logic away from single-byte assumptions into rigorous shaped-glyph bounding boxes.
Output: Upgraded pipeline stages orchestrating BiDi splitting and HarfBuzz measurements.
</objective>

<context>
@.planning/phases/35-complex-text-and-i18n-foundations/35-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align Measure Pipeline</name>
  <files>lib/rendro/pipeline/measure.ex, test/rendro/pipeline/measure_test.exs</files>
  <behavior>
    - Measurements rely entirely on `Rendro.Text.Shaper` output instead of `String.length`.
  </behavior>
  <action>
    Update `Rendro.Pipeline.Measure` to incorporate `Rendro.Text.Bidi` run splitting and HarfBuzz shaping to compute exact text box widths/heights, accounting for complex script ligatures.
    **Pattern Reference:** Adhere to the Pipeline Mapping Pattern (e.g. `measure_pages`, `measure_content`) from `lib/rendro/pipeline/measure.ex` mapped in PATTERNS.md. Use the Collection Processing Pattern (`map_ok` enum reductions) to cleanly handle measuring variable-length shaped runs while short-circuiting on errors.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Pipeline handles multi-script text measurement accurately without overlaps.</done>
</task>

<task type="auto">
  <name>Task 2: Align Build and Compose</name>
  <files>lib/rendro/pipeline/build.ex, lib/rendro/pipeline/compose.ex</files>
  <action>
    Update `Rendro.Pipeline.Build` and `Rendro.Pipeline.Compose` to maintain structured shaped text properties. Ensure font subsets are gathered correctly across all used characters in the document before passing to the Writer.
    **Pattern Reference:** Follow the Pipeline Mapping Pattern from PATTERNS.md, extending the `with {:ok, ...} <- step_function` structure to compose CID fonts cleanly.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/build_test.exs test/rendro/pipeline/compose_test.exs</automated>
  </verify>
  <done>Pipeline correctly groups subsets and generates references for CID fonts.</done>
</task>

</tasks>
