---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["mix.exs", "lib/rendro/text/bidi.ex", "lib/rendro/text/shaper.ex", "test/rendro/text/bidi_test.exs", "test/rendro/text/shaper_test.exs"]
autonomous: true
requirements: ["Typography", "i18n"]
must_haves:
  truths:
    - "Users can input strings containing mixed character sets (Latin, Arabic, CJK)."
    - "Text correctly segments into single-direction, single-script runs before shaping."
    - "Missing glyphs emit structured warnings via Telemetry and fall back gracefully rather than crashing the render pipeline."
  artifacts:
    - path: "lib/rendro/text/bidi.ex"
    - path: "lib/rendro/text/shaper.ex"
  key_links:
    - from: "lib/rendro/text/bidi.ex"
      to: "unicode_data"
      via: "property lookups"
---

<objective>
Setup core dependencies and pure i18n APIs (BiDi run splitting and HarfBuzz shaping).

Purpose: Enable mixed character sets and properly prepare text runs before shaping.
Output: Dependencies installed, and new text modules available.
</objective>

<context>
@.planning/phases/35-complex-text-and-i18n-foundations/35-RESEARCH.md
@.planning/phases/35-complex-text-and-i18n-foundations/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typography dependencies</name>
  <files>mix.exs</files>
  <action>
    Add `harfbuzz_ex` (~> 1.2) and `unicode_data` (~> 0.8.0) to `mix.exs` dependencies.
    Run `mix deps.get`.
  </action>
  <verify>
    <automated>mix deps | grep -E "harfbuzz_ex|unicode_data"</automated>
  </verify>
  <done>Dependencies resolve correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BiDi run splitting</name>
  <files>lib/rendro/text/bidi.ex, test/rendro/text/bidi_test.exs</files>
  <behavior>
    - Splitting mixed Latin and Arabic string should yield distinct unidirectional runs.
    - Script properties correctly map to each run.
  </behavior>
  <action>
    Create `Rendro.Text.Bidi` to iterate strings and chunk them by script and direction boundaries using `unicode_data` properties. 
    Ensure we don't pass mixed-direction text directly to HarfBuzz (Pitfall 1).
  </action>
  <verify>
    <automated>mix test test/rendro/text/bidi_test.exs</automated>
  </verify>
  <done>Mixed strings correctly segment into uniform runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement HarfBuzz shaper</name>
  <files>lib/rendro/text/shaper.ex, test/rendro/text/shaper_test.exs</files>
  <behavior>
    - Shaping returns exact Glyph IDs and Bounding Box measurements.
    - Missing glyphs trigger specific Telemetry events instead of crashing.
  </behavior>
  <action>
    Create `Rendro.Text.Shaper` as a wrapper around `harfbuzz_ex` returning glyphs and bounding boxes.
    Detect missing glyph IDs from the shaping engine and emit structured Telemetry events instead of crashing.
    **Pattern Reference:** Follow the Binary Traversal Pattern from `lib/rendro/i18n/analyzer.ex` (as noted in PATTERNS.md) to parse and process the incoming runs. Use a fast `do_analyze` binary matching loop to optimize short-circuiting.
  </action>
  <verify>
    <automated>mix test test/rendro/text/shaper_test.exs</automated>
  </verify>
  <done>Runs accurately shape into glyph properties and fallback securely on miss.</done>
</task>

</tasks>
