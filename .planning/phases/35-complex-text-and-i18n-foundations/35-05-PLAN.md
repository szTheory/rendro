---
phase: 35
plan: 05
type: execute
wave: 4
depends_on: ["35-04"]
files_modified: ["lib/rendro/pipeline/paginate.ex", "lib/rendro/pipeline/render.ex", "lib/rendro/pipeline/validate.ex"]
autonomous: true
requirements: ["Typography", "i18n"]
must_haves:
  truths:
    - "Pagination breaks text naturally at proper run boundaries, preserving right-to-left layout constraints."
    - "Render output successfully positions text strings with accurate horizontal tracking adjustments for complex kerning."
  artifacts:
    - path: "lib/rendro/pipeline/render.ex"
    - path: "lib/rendro/pipeline/paginate.ex"
  key_links:
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "lib/rendro/text/shaper.ex"
      via: "reads word bounds"
---

<objective>
Align Paginate, Render, and Validate pipelines to respect new typography shapes.

Purpose: Finalize the rendering lifecycle so that complex text runs break correctly across pages and render identically to measured boundaries.
Output: Upgraded downstream pipeline stages.
</objective>

<context>
@.planning/phases/35-complex-text-and-i18n-foundations/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Paginate Pipeline</name>
  <files>lib/rendro/pipeline/paginate.ex</files>
  <action>
    Update `Rendro.Pipeline.Paginate` to wrap text correctly around exact HarfBuzz measurements. Prevent splitting strings in the middle of crucial right-to-left or Indic ligatures.
    **Pattern Reference:** Use the Collection Processing Pattern (from PATTERNS.md) to manage splitting arrays of glyph bounds across physical page height limits.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Pagination strictly respects text shaping boundaries for wrapping.</done>
</task>

<task type="auto">
  <name>Task 2: Align Render and Validate</name>
  <files>lib/rendro/pipeline/render.ex, lib/rendro/pipeline/validate.ex</files>
  <action>
    Update `Rendro.Pipeline.Render` and `Rendro.Pipeline.Validate` to map subsetted font objects and text runs to final drawing commands. Ensure validation policies assert the presence of correct CID font encodings if Arabic/CJK content exists.
    **Pattern Reference:** Follow the Pipeline Mapping Pattern (`with {:ok, ...} <- check`) to integrate layout-to-drawing translations.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/render_test.exs test/rendro/pipeline/validate_test.exs</automated>
  </verify>
  <done>Render output exactly mimics measured bounds and passes internal structural validation.</done>
</task>

</tasks>
