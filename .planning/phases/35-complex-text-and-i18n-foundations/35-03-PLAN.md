---
phase: 35
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified: ["lib/rendro/pdf/cid_font.ex", "lib/rendro/pdf/writer.ex", "test/rendro/pdf/cid_font_test.exs"]
autonomous: true
requirements: ["Typography", "i18n"]
must_haves:
  truths:
    - "PDF document internally defines Type0 / CIDFontType2 dictionaries for non-Latin fonts."
    - "PDF text streams use Hex Encoded Glyph IDs (Identity-H) instead of string literals."
  artifacts:
    - path: "lib/rendro/pdf/cid_font.ex"
    - path: "lib/rendro/pdf/writer.ex"
  key_links:
    - from: "lib/rendro/pdf/writer.ex"
      to: "lib/rendro/pdf/cid_font.ex"
      via: "generates dictionaries"
---

<objective>
Upgrade PDF writer to emit Type0 / CID-keyed fonts.

Purpose: PDF 1.4 requires Type0 fonts with Identity-H encoding to support character sets beyond the standard 256-glyph MacRoman table.
Output: Upgraded `Writer` capable of generating CIDFontType2 dictionaries and Hex Glyph ID strings.
</objective>

<context>
@.planning/phases/35-complex-text-and-i18n-foundations/35-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate Type0 Font Dictionaries</name>
  <files>lib/rendro/pdf/cid_font.ex, test/rendro/pdf/cid_font_test.exs</files>
  <behavior>
    - Emits correct PDF dictionary structures: Type0, Identity-H encoding, CIDFontType2 descendants.
  </behavior>
  <action>
    Create `Rendro.PDF.CidFont` to format correct PDF metadata blocks for CID-Keyed fonts. Ensure the output strictly conforms to the Type0 / `Identity-H` structure required for Glyph ID referencing.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/cid_font_test.exs</automated>
  </verify>
  <done>Generated PDF dicts contain Identity-H encoding keys and CID font references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade PDF Writer for Glyph IDs</name>
  <files>lib/rendro/pdf/writer.ex</files>
  <behavior>
    - Text operator `Tj` processes text as hex-encoded Glyph IDs (e.g., `<001B001C>`) when drawing complex scripts.
  </behavior>
  <action>
    Modify `Rendro.PDF.Writer` so that text strings are written using Hex encoded Glyph IDs instead of literal strings when the font is a CID font.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>PDF content streams successfully use Glyph IDs, rendering CJK/Arabic text properly without artifacts.</done>
</task>

</tasks>
