---
phase: 121-light-dark-background-fill-mechanism-all-7-recipes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/support_matrix.json
  - lib/rendro/theme.ex
  - test/docs_contract/theming_claims_test.exs
autonomous: true
requirements: [MODE-03]
must_haves:
  truths:
    - "priv/support_matrix.json gains a theming section: theming.light = supported; theming.dark = supported_screen_oriented with boundary keys print_recommended, accessibility_pdf_ua_claim, wcag_contrast_claim, gui_viewer_visual_fidelity_claim ALL set to unsupported (D-09)."
    - "Rendro.Theme.dark/1's @doc carries the explicit 'screen-oriented, not recommended for print' boundary sentence."
    - "test/docs_contract/theming_claims_test.exs asserts the boundary keys are unsupported, that no theming row carries a print/PDF-UA/WCAG SUPPORT term, and that the dark/1 @doc boundary sentence is present — plus non-vacuity (teeth) assertions mirroring accessibility_overclaim_test.exs so the guard can never go vacuous."
  artifacts:
    - test/docs_contract/theming_claims_test.exs
  key_links:
    - "The theming.dark boundaries block mirrors the signing_preparation boundaries idiom (support_matrix.json:132-138): a flat map of boundary key → \"unsupported\"."
    - "The overclaim tripwire mirrors accessibility_overclaim_test.exs term-list co-occurrence + non-vacuity teeth."
  prohibitions:
    - "MUST NOT create guides/theming.md — it belongs to Phase 123 (CONTRACT-02, D-09)."
    - "MUST NOT add any print-safety / PDF-UA / WCAG / accessibility SUPPORT claim to any theming row (dark is screen-oriented only, MODE-03)."
    - "MUST NOT ship a dark gallery PNG / dark demo (D-07)."
---

<objective>
Bound the dark-mode claim honestly (MODE-03): dark is a screen-oriented mode with an explicit non-print boundary, no print-safety / accessibility / PDF-UA / WCAG claim, and every shipped demo stays light.

Add a `theming` support-matrix stub row (light = supported; dark = supported_screen_oriented with all print/UA/WCAG/GUI-fidelity boundary keys `unsupported`, D-09), add the explicit boundary sentence to `Rendro.Theme.dark/1`'s `@doc`, and author `theming_claims_test.exs` binding every boundary claim to proof with the overclaim-tripwire teeth pattern.

Purpose: the honesty law — never ship a claim without proof; confident but never overclaim. This is the docs-contract lane that keeps a future dark visual from implying print/accessibility support.
Output: `theming` matrix rows, the `dark/1` @doc boundary sentence, and a self-defending `theming_claims_test.exs`. (This plan is independent of the recipe wiring — it declares the boundary; the mechanism is proven by 121-01/02/03.)

## Explicitly NOT in this plan
- `guides/theming.md` (Phase 123, CONTRACT-02).
- Any `(recipe × mode)` gallery row / dark PNG (Phase 123, DEFAULT-03; D-07 ships zero dark demos now).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-CONTEXT.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-RESEARCH.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the theming support-matrix rows and the dark/1 @doc boundary sentence</name>
  <files>priv/support_matrix.json, lib/rendro/theme.ex</files>
  <read_first>
    - priv/support_matrix.json (top-level sections use nested capabilities/behaviors/boundaries/viewers; the signing_preparation boundaries block ~:132-138 is the flat boundary-key → "unsupported" idiom to mirror)
    - lib/rendro/theme.ex:224-243 (existing `dark/1` @doc block to extend — no transcendental math note, `mode: :dark`)
    - .planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-PATTERNS.md (support_matrix.json + theme.ex sections)
  </read_first>
  <action>
    1. `priv/support_matrix.json`: add a top-level `theming` section with two entries mirroring the existing row shape (validate JSON parses after the edit):
       - `light`: status `supported`; capabilities include `no_background_rect`, `byte_identical_to_v2_10`, `deterministic_output`.
       - `dark`: status `supported_screen_oriented`; capabilities include `full_page_background_every_page`, `overflow_page_background`, `deterministic_output`; a `boundaries` map (mirror the signing_preparation flat-map idiom) with keys `print_recommended`, `accessibility_pdf_ua_claim`, `wcag_contrast_claim`, `gui_viewer_visual_fidelity_claim` each set to `"unsupported"`.
       Do NOT introduce any print-safety / accessibility / PDF-UA / WCAG SUPPORT term anywhere in the theming rows.
    2. `lib/rendro/theme.ex`: extend the existing `dark/1` `@doc` (theme.ex:224-243) with the explicit boundary sentence stating dark is screen-oriented and not recommended for print. Keep the sentence verbatim-greppable (the docs-contract test asserts its presence). Do not change `dark/1` behavior.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix run -e 'JSON.decode!(File.read!("priv/support_matrix.json")); IO.puts("json ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `priv/support_matrix.json` parses as valid JSON and contains a `theming` section with `light` and `dark` entries; `dark` is `supported_screen_oriented` with the four boundary keys each `"unsupported"`.
    - No theming row contains a print-safety / accessibility / PDF-UA / WCAG SUPPORT term (only the boundary keys, all `unsupported`).
    - `grep -q "screen-oriented" lib/rendro/theme.ex` and the sentence appears within the `dark/1` @doc block.
    - `mix compile --warnings-as-errors` succeeds (no @doc/compile regressions).
    - `test -f guides/theming.md` is FALSE (not created this phase).
  </acceptance_criteria>
  <done>The theming matrix rows exist with all boundary keys unsupported; the dark/1 @doc carries the non-print boundary sentence; JSON valid; no guides/theming.md created.</done>
</task>

<task type="auto">
  <name>Task 2: Author theming_claims_test.exs (boundary assertions + overclaim tripwire + non-vacuity teeth)</name>
  <files>test/docs_contract/theming_claims_test.exs</files>
  <read_first>
    - test/docs_contract/theming_claims_test.exs (NEW)
    - test/docs_contract/raster_claims_test.exs:7-14,20,48 (reads priv/support_matrix.json; asserts section + boundary keys + "unsupported"; parses via JSON.decode!/1)
    - test/docs_contract/accessibility_overclaim_test.exs (whole file — term-list co-occurrence predicate + the "tripwire integrity (non-vacuity / teeth)" describe block at ~:53-60 to mirror)
    - lib/rendro/theme.ex:224-243 (`dark/1` @doc — assertion target)
    - priv/support_matrix.json (the theming rows added in Task 1)
  </read_first>
  <action>
    Create `test/docs_contract/theming_claims_test.exs` (`Rendro.DocsContract.ThemingClaimsTest`). Parse `priv/support_matrix.json` via `JSON.decode!/1` and assert:
    - The `theming` section exists with `light` and `dark`; `dark` status == `supported_screen_oriented`.
    - Each boundary key (`print_recommended`, `accessibility_pdf_ua_claim`, `wcag_contrast_claim`, `gui_viewer_visual_fidelity_claim`) is present and == `"unsupported"`.
    - No `theming` row value carries a print/PDF-UA/WCAG SUPPORT term (define a small term list; assert none co-occur with a `supported*` status) — mirror the `accessibility_overclaim_test.exs` co-occurrence predicate.
    - `Rendro.Theme.dark/1`'s @doc contains the "screen-oriented, not recommended for print" boundary sentence — read the compiled doc via `Code.fetch_docs/1` (or scan the theme.ex source).
    - Non-vacuity TEETH (mirror accessibility_overclaim_test.exs:53-60): assert the boundary-key list and the print/UA/WCAG term list are both non-empty, so the guard can never silently pass on an empty set.
    Do NOT create `guides/theming.md` and do NOT assert on any gallery/dark-PNG artifact (Phase 123).
  </action>
  <verify>
    <automated>mix test test/docs_contract/theming_claims_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/docs_contract/theming_claims_test.exs` passes.
    - The test asserts all four boundary keys == `"unsupported"` AND that no theming row carries a print/UA/WCAG support term.
    - The test asserts the `dark/1` @doc boundary sentence is present (via `Code.fetch_docs/1` or source scan).
    - Non-vacuity teeth assertions are present (boundary-key list and term list both refuted-empty) so the guard cannot go vacuous.
    - `git status --porcelain guides/` shows no `theming.md` added.
  </acceptance_criteria>
  <done>theming_claims_test.exs binds every boundary claim to proof, trips on any print/UA/WCAG overclaim, and defends its own non-vacuity; guides/theming.md not created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project docs → consumer claims | Support-matrix + @doc are read as capability claims; the risk is over-claiming (asserting print/accessibility support Rendro does not provide), not an external-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-03 | Repudiation | dark-mode capability claim in docs | low | mitigate | `theming_claims_test.exs` binds every boundary key to `unsupported` and trips on any print/UA/WCAG support term — the honesty-law gate; non-vacuity teeth keep it from going silent. |

No package installs; zero new deps — no `T-121-SC` applies. No content-stream / render surface touched in this plan.
</threat_model>

<verification>
- `mix test test/docs_contract/theming_claims_test.exs` green.
- `priv/support_matrix.json` valid JSON with the theming rows; `dark/1` @doc boundary sentence present.
- No `guides/theming.md`; no dark PNG artifact.
</verification>

<success_criteria>
- Dark is documented screen-oriented with an explicit non-print boundary and a support-matrix row; no print/UA/WCAG/accessibility support claim exists.
- The docs-contract lane self-defends against overclaim and vacuity.
</success_criteria>

<output>
Create `.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-04-SUMMARY.md` when done.
</output>
