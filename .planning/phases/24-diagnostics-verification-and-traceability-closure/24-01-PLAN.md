---
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/document.ex
  - lib/rendro.ex
  - README.md
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/pipeline_test.exs
  - test/rendro/inspector_test.exs
  - .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md
  - .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md
  - .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md
autonomous: true
requirements: [OBS-05, QUAL-06]

must_haves:
  truths:
    - "The public diagnostics contract stays map-based and documented truthfully as stable common fields plus event-specific optional keys."
    - "Deterministic proof covers `Rendro.render_with_diagnostics/2`, `Rendro.Inspector.inspect/1`, focused pagination tests, and the README/docs-contract lane without introducing new verification machinery."
    - "Phase 21, Phase 22, and Phase 24 validation metadata are machine-discoverable through the same Nyquist structure already used by Phases 18-20."
  artifacts:
    - path: "lib/rendro/document.ex"
      provides: "Truthful diagnostics type/documentation surface"
      min_lines: 20
    - path: "README.md"
      provides: "Truthful public diagnostics wording for `render_with_diagnostics/2` and inspector usage"
      min_lines: 40
    - path: ".planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md"
      provides: "Phase 24 structured Nyquist validation contract"
      min_lines: 40
  key_links:
    - from: "lib/rendro/document.ex"
      to: "README.md"
      via: "shared map-based diagnostics contract wording"
      pattern: "diagnostics|level|type|message|page_index|keep_rule"
    - from: "lib/rendro.ex"
      to: "test/rendro/pipeline_test.exs"
      via: "public `render_with_diagnostics/2` proof"
      pattern: "render_with_diagnostics|diagnostics"
    - from: "test/rendro/pipeline/paginate_test.exs"
      to: "test/rendro/inspector_test.exs"
      via: "deterministic diagnostics and inspector evidence"
      pattern: "table_split|keep_rule_break|Diagnostics:"
    - from: ".planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md"
      to: ".planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md"
      via: "shared Nyquist normalization pattern"
      pattern: "nyquist_compliant|Per-Task Verification Map|Validation Sign-Off"
---

<objective>
Align the diagnostics contract, prove the already-shipped public surfaces, and normalize validation metadata without widening runtime scope.

Purpose: Phase 24 closes trust gaps around `OBS-05` and `QUAL-06` by making the existing map-based diagnostics contract explicit, attaching a small deterministic proof slice to the public APIs, and bringing adjacent validation artifacts onto the repo's machine-discoverable Nyquist pattern.
Output: truthful diagnostics wording/types, focused tests and docs-contract proof, normalized `21-VALIDATION.md` and `22-VALIDATION.md`, and a structured `24-VALIDATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/METHODOLOGY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/24-diagnostics-verification-and-traceability-closure/24-CONTEXT.md
@.planning/phases/24-diagnostics-verification-and-traceability-closure/24-RESEARCH.md
@.planning/phases/24-diagnostics-verification-and-traceability-closure/24-PATTERNS.md
@.planning/phases/21-break-diagnostics-and-pagination-proofs/21-CONTEXT.md
@.planning/phases/21-break-diagnostics-and-pagination-proofs/21-RESEARCH.md
@.planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md
@.planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md
@.planning/phases/18-layout-contract-and-page-template-model/18-VALIDATION.md
@.planning/phases/19-deterministic-text-flow-and-break-semantics/19-VALIDATION.md
@.planning/phases/20-table-layout-maturity/20-VALIDATION.md

<interfaces>
From `lib/rendro/document.ex`:
```elixir
@type t :: %__MODULE__{
  diagnostics: [map()],
  options: %{optional(atom()) => term()}
}
```

From `lib/rendro.ex`:
```elixir
@spec render_with_diagnostics(Document.t(), render_options()) ::
        {:ok, binary(), Document.t()} | {:error, Rendro.Error.t()}
```

From `lib/rendro/inspector.ex`:
```elixir
@spec inspect(Document.t()) :: String.t()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the public diagnostics contract to the shipped map-based surface</name>
  <files>lib/rendro/document.ex, lib/rendro.ex, README.md</files>
  <read_first>
    - lib/rendro/document.ex
    - lib/rendro.ex
    - README.md
    - .planning/phases/24-diagnostics-verification-and-traceability-closure/24-CONTEXT.md
    - .planning/phases/24-diagnostics-verification-and-traceability-closure/24-RESEARCH.md
  </read_first>
  <action>
    Tighten the public diagnostics contract per D-04 through D-07 without widening runtime behavior. Keep `final_doc.diagnostics` as a list of maps, not a new `%Rendro.Document.Diagnostic{}` struct, and do not redesign telemetry. Update the `Rendro.Document` type/doc surface and the `Rendro.render_with_diagnostics/2` + README wording so all three describe one truthful contract: stable common keys such as `:level` and `:type`, event-specific optional keys such as `:message`, `:page_index`, `:reason`, and `:keep_rule`, and additive future keys allowed.

    Preserve the Phase 21 separation of concerns from D-06: `doc.diagnostics` remains the developer-facing layout-debug surface while telemetry stays the operational span surface. Keep the wording recommendation-first per D-14 and D-15: present one cohesive supported contract, not option menus or speculative future structs/config knobs.
  </action>
  <acceptance_criteria>
    - `lib/rendro/document.ex` still types diagnostics as maps and documents the common-fields contract explicitly.
    - `lib/rendro.ex` docs for `render_with_diagnostics/2` describe diagnostics as user-inspectable structured maps.
    - `README.md` no longer claims `%Rendro.Document.Diagnostic{}`.
    - No new runtime diagnostics struct, telemetry schema, or speculative verification API is introduced.
    - The verification command proves both the removal of the forbidden `%Rendro.Document.Diagnostic{}` README claim and the presence of the required map-based contract wording in each target file.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "%Rendro\\.Document\\.Diagnostic\\{" README.md && rg -n "structured maps" README.md && rg -n "stable common keys" README.md && rg -n "event-specific optional" README.md && rg -n "diagnostics: \\[map\\(\\)\\]" lib/rendro/document.ex && rg -n "page_index" lib/rendro/document.ex && rg -n "keep_rule" lib/rendro/document.ex && rg -n "optional\\(atom\\(\\)\\) => term\\(\\)" lib/rendro/document.ex && rg -n "render_with_diagnostics" lib/rendro.ex && rg -n "structured maps" lib/rendro.ex && rg -n "developer-facing layout-debug surface" lib/rendro.ex && rg -n "telemetry" lib/rendro.ex</automated>
  </verify>
  <done>The docs and types now describe the exact shipped diagnostics contract and no longer imply unsupported runtime structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the focused diagnostics and pagination-proof surfaces with a deterministic small suite</name>
  <files>test/rendro/pipeline/paginate_test.exs, test/rendro/pipeline_test.exs, test/rendro/inspector_test.exs</files>
  <behavior>
    - `Rendro.render_with_diagnostics/2` returns the final document and exposes diagnostics through the public API boundary.
    - Pagination tests prove `:table_split` and keep-rule diagnostics through deterministic assertions, not broad new snapshot infrastructure.
    - `Rendro.Inspector.inspect/1` continues to render a deterministic ASCII proof that includes diagnostics in a reviewable format.
  </behavior>
  <read_first>
    - test/rendro/pipeline/paginate_test.exs
    - test/rendro/pipeline_test.exs
    - test/rendro/inspector_test.exs
    - lib/rendro.ex
    - lib/rendro/inspector.ex
    - README.md
  </read_first>
  <action>
    Strengthen the smallest truthful proof slice per D-08 through D-10. Keep verification deterministic and reviewable: extend the existing pagination, pipeline, and inspector tests so they prove the actual public surfaces together rather than adding a new verification framework. The suite should explicitly cover `Rendro.render_with_diagnostics/2`, `Rendro.Inspector.inspect/1`, and the current pagination diagnostics shapes relevant to `OBS-05` and `QUAL-06`.

    Keep scope narrow. Use the existing files only, stay inside the README/docs-contract lane already present in the repo, and do not add sprawling snapshots, property suites, or telemetry assertions unrelated to the shipped contract.
  </action>
  <acceptance_criteria>
    - `test/rendro/pipeline_test.exs` proves the public `render_with_diagnostics/2` boundary returns a final document with diagnostics access.
    - `test/rendro/pipeline/paginate_test.exs` contains focused assertions for diagnostics relevant to table splits and keep-rule-driven page movement.
    - `test/rendro/inspector_test.exs` continues to assert deterministic ASCII output including diagnostics.
    - The README/docs-contract lane remains green for the corrected diagnostics wording.
    - `mix test test/rendro/pipeline/paginate_test.exs test/rendro/pipeline_test.exs test/rendro/inspector_test.exs test/docs_contract/readme_doctest_test.exs && mix run scripts/verify_docs.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/pipeline_test.exs test/rendro/inspector_test.exs test/docs_contract/readme_doctest_test.exs && mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>The authoritative proof slice is small, deterministic, and tied directly to the public diagnostics and inspector surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize Phase 21, Phase 22, and Phase 24 validation metadata to the existing Nyquist pattern</name>
  <files>.planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md, .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md, .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md</files>
  <read_first>
    - .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md
    - .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md
    - .planning/phases/18-layout-contract-and-page-template-model/18-VALIDATION.md
    - .planning/phases/19-deterministic-text-flow-and-break-semantics/19-VALIDATION.md
    - .planning/phases/20-table-layout-maturity/20-VALIDATION.md
    - .planning/phases/24-diagnostics-verification-and-traceability-closure/24-CONTEXT.md
  </read_first>
  <action>
    Reuse the exact Nyquist-compliant validation structure from Phases 18-20 per D-11 through D-13. Normalize `21-VALIDATION.md` and `22-VALIDATION.md` into that machine-discoverable frontmatter/table format, then create `24-VALIDATION.md` in the same shape with a focused verification map for this closure phase. `24-VALIDATION.md` must stay deterministic and small: use the concentrated diagnostics suite (`render_with_diagnostics/2`, inspector, paginate, pipeline, README/docs-contract) rather than a broad milestone matrix.

    Carry the repo's recommendation-first posture from D-14 and D-15 into the Phase 24 validation language itself: one cohesive closure direction, no option menus, and no invented config knobs or secondary validation convention.
  </action>
  <acceptance_criteria>
    - All three validation files use frontmatter with `nyquist_compliant: true`.
    - `21-VALIDATION.md` and `22-VALIDATION.md` no longer remain prose-only.
    - `24-VALIDATION.md` includes the standard sections: Test Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, and Validation Sign-Off.
    - The Phase 24 quick run command stays focused on the deterministic diagnostics/docs-contract lane.
    - The verification command asserts Nyquist frontmatter and required sections in each validation file individually, so one matching file cannot mask another missing section.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^nyquist_compliant: true$" .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md && rg -n "^## Per-Task Verification Map$" .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md && rg -n "^## Validation Sign-Off$" .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md && rg -n "^nyquist_compliant: true$" .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md && rg -n "^## Per-Task Verification Map$" .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md && rg -n "^## Validation Sign-Off$" .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md && rg -n "^nyquist_compliant: true$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Test Infrastructure$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Sampling Rate$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Per-Task Verification Map$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Wave 0 Requirements$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Manual-Only Verifications$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Validation Sign-Off$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md</automated>
  </verify>
  <done>Nyquist metadata for the diagnostics closure arc is consistent, machine-discoverable, and aligned to the repo's established validation contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs/types -> runtime contract | Diagnostics wording must reflect the shipped map-based API exactly. |
| Tests/docs-contract -> closure claims | Phase 24 can only close `OBS-05` and `QUAL-06` if the focused public proof surfaces are real and green. |
| Validation metadata -> tooling | Nyquist tooling depends on structured validation files rather than prose-only notes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `README.md`, `lib/rendro/document.ex`, `lib/rendro.ex` | mitigate | Correct the contract to map-based diagnostics per D-04 through D-07 and block any new struct/telemetry redesign from sneaking into Phase 24. |
| T-24-02 | Repudiation | focused diagnostics proof slice | mitigate | Tie closure to explicit `render_with_diagnostics/2`, inspector, pagination, and docs-contract tests per D-08 through D-10. |
| T-24-03 | Repudiation | validation metadata | mitigate | Normalize Phases 21, 22, and 24 to the established Nyquist pattern so tooling can discover the proof chain consistently. |
| T-24-04 | Elevation of privilege | validation/process scope | mitigate | Keep Phase 24 recommendation-first and closure-focused per D-14 and D-15; do not invent a second validation convention or new runtime surface. |
</threat_model>

<verification>
Run the focused closure suite and the per-file validation-structure checks together: `mix test test/rendro/pipeline/paginate_test.exs test/rendro/pipeline_test.exs test/rendro/inspector_test.exs test/docs_contract/readme_doctest_test.exs && mix run scripts/verify_docs.exs && rg -n "^nyquist_compliant: true$" .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md && rg -n "^## Per-Task Verification Map$" .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md && rg -n "^## Validation Sign-Off$" .planning/phases/21-break-diagnostics-and-pagination-proofs/21-VALIDATION.md && rg -n "^nyquist_compliant: true$" .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md && rg -n "^## Per-Task Verification Map$" .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md && rg -n "^## Validation Sign-Off$" .planning/phases/22-authoring-ergonomics-and-canonical-recipes/22-VALIDATION.md && rg -n "^nyquist_compliant: true$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Test Infrastructure$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Sampling Rate$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Per-Task Verification Map$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Wave 0 Requirements$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Manual-Only Verifications$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md && rg -n "^## Validation Sign-Off$" .planning/phases/24-diagnostics-verification-and-traceability-closure/24-VALIDATION.md`.
</verification>

<success_criteria>
- The diagnostics contract is truthful, map-based, and aligned across types, public docs, and README.
- Deterministic public proof exists for `render_with_diagnostics/2`, inspector output, pagination diagnostics, and the README/docs-contract lane.
- Phase 21, Phase 22, and Phase 24 validation artifacts all follow the same Nyquist-compliant structure.
</success_criteria>

<output>
After completion, create `.planning/phases/24-diagnostics-verification-and-traceability-closure/24-01-SUMMARY.md`
</output>
