---
phase: 123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani
plan: 04
type: execute
wave: 4
depends_on: [123-03]
files_modified:
  - guides/theming.md
  - test/docs_contract/theming_contract_test.exs
  - test/docs_contract/theming_claims_test.exs
  - priv/support_matrix.json
  - mix.exs
autonomous: true
requirements: [CONTRACT-02, DEFAULT-01]
must_haves:
  truths:
    - "from_brand(accent: \"#0E7C76\") |> resolve yields colors.accent == {14,124,118} and on_accent == {255,255,255} (white); from_brand(accent: \"#E6B450\") yields on_accent == {16,24,39} (ink) — the contrast heuristic proven BOTH ways by executable guide fences (DEFAULT-01, D-04)."
    - "guides/theming.md exists with exactly 3 verified # docs-contract: fences in order (theming-accent-only, theming-accent-contrast-both-ways, theming-brand-orthogonal); each is in-memory and side-effect-free (no File.write/render-to-disk/System.cmd — DocsContract sandbox, Pitfall 4)."
    - "The BrandedInvoice orthogonality fence asserts font_registry.fonts[:brand_heading] AND asset_registry.assets[:company_logo] (assets via data.brand) AND Theme.resolve(theme).colors.accent == {14,124,118} (accent via theme:) AND from_brand(...) returns a bare %Theme{} with no registry side-effect (COLOR-02 orthogonality, D-04)."
    - "on_accent honest wording ('readable default, NOT a WCAG-AA/PDF-UA claim, overridable via on_accent:') sits next to every derivation claim; no theming boundary key pairs a print/pdf-ua/wcag term with a supported* status (no overclaim, CONTRACT-02)."
    - "The Hex tarball ships assets/rendro (the 4 new PNGs) + guides/theming.md, and EXCLUDES priv/quality/, priv/support_matrix.json, priv/schemas/rubric_scores.schema.json — package.files is NOT edited (Pitfall 3)."
  artifacts:
    - guides/theming.md
    - test/docs_contract/theming_contract_test.exs
  key_links:
    - "guides/theming.md ```elixir fence + # docs-contract: id -> DocsContract.verified_fences/evaluate! -> theming_contract_test.exs (the fence IS the E2E test, D-04)."
    - "guides/theming.md claim -> priv/support_matrix.json theming row / boundary key -> theming_claims_test.exs overclaim tripwire (every claim bound to proof)."
---

<objective>
CONTRACT-02 + DEFAULT-01 (from_brand E2E). Land `guides/theming.md` whose `# docs-contract:` fences
ARE the executable from_brand end-to-end test (D-04 — no separate standalone integration test), add
`test/docs_contract/theming_contract_test.exs` to run them, flip the `guides/theming.md`
non-existence guard, keep the theming claims proof-backed with no overclaim, and confirm the
Hex-tarball lane stays green (theme is pure code; the 4 new PNGs + the guide SHIP, but
`priv/quality`/`support_matrix`/rubric-schema stay excluded — GT-8, Pitfall 3).

Research GT-7 verified the exact assertions: `resolve(from_brand(accent:"#0E7C76")).colors.accent ==
{14,124,118}`, `on_accent == {255,255,255}` (teal luminance ≈0.16 → white), and `from_brand(accent:
"#E6B450")` → `on_accent == {16,24,39}` (amber luminance ≈0.50 → ink) — the heuristic proven both
ways. GT-8 verified the DocsContract sandbox blocks `File.write*`/`System.cmd`/`Mix.Task.run`, so all
fences must be pure in-memory struct/registry assertions; the *visual* accent is proven by the
`invoice_brand` gallery row from Plan 03, not by a fence render.

Purpose: bind every public theming claim to executable proof and prove from_brand/2 end-to-end via the docs, keeping all docs/tarball lanes green.
Output: guides/theming.md (3 fences + honest wording + 11-row SHA block); theming_contract_test.exs; flipped theming_claims guard; mix.exs docs wiring; tarball verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md
@guides/branding.md
@test/docs_contract/branding_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/theming.md (3 executable fences + honest on_accent wording + 11-row SHA block) and wire mix.exs docs</name>
  <files>guides/theming.md, mix.exs</files>
  <read_first>
    - guides/branding.md (the # docs-contract: fence + prose style to mirror: branding-register-assets etc.)
    - brand/copy/VOICE.md (microcopy voice; restraint-reads-as-confidence)
    - lib/rendro/theme.ex (from_brand/2 L266-299; @spec from_brand(keyword(), keyword()); coerce_color/hex_to_rgb L383-394; on_accent_for/2 L339)
    - lib/rendro/recipes/branded_invoice.ex (document/2 ~L166; data.brand asset registration ~L174; the doctest field paths for font_registry/asset_registry — confirm before authoring, A3)
    - assets/rendro/artifacts.json (the 11 blessed png_sha256 / source_pdf_sha256 for the gallery block)
    - mix.exs (docs extras L157-171; skip_undefined_reference_warnings_on L139-145; groups_for_extras L172+; package.files L110-127 — read only, do NOT edit)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§Code Examples for exact fence bodies; §GT-7/GT-8; Pitfall 4)
  </read_first>
  <action>
    Create guides/theming.md with three verified ```elixir fences, each carrying a
    `# docs-contract: <id>` marker in this order: theming-accent-only, theming-accent-contrast-both-ways,
    theming-brand-orthogonal. Fence 1 (accent-only, progressive-disclosure headline): resolve
    from_brand(accent: "#0E7C76") and assert colors.accent == {14,124,118} and on_accent ==
    {255,255,255}. Fence 2 (both-ways): resolve from_brand(accent: "#E6B450") and assert on_accent ==
    {16,24,39} (ink, because amber is light). Fence 3 (orthogonality via BrandedInvoice): build a
    minimal REAL evaluable branded-invoice data map (NO "..." / "%{...}" skeletons — the sandbox and
    theming_contract_test reject them), put :brand => %{font_name: :brand_heading, logo_name:
    :company_logo}, call BrandedInvoice.document(data, theme: from_brand(accent: "#0E7C76")), and
    assert font_registry.fonts has :brand_heading AND asset_registry.assets has :company_logo AND
    Theme.resolve(theme).colors.accent == {14,124,118} AND that from_brand(...) alone returns a bare
    %Theme{} whose resolution registers nothing. Keep every fence PURE in-memory (no render-to-disk,
    no System.cmd, no Mix.Task.run — Pitfall 4). In the surrounding prose, place the honest wording
    next to every derivation claim: on_accent is a "sensible readable default," NOT a WCAG-AA/PDF-UA
    guarantee, and is overridable via on_accent:; use the headline one-liner
    `Rendro.Recipes.Invoice.document(data, theme: Rendro.Theme.from_brand(accent: "#0E7C76"))`; show
    accent-only first, then the fuller from_brand(accent:, ink:, surface:) form; a clearly-third-party
    hex (e.g. #7A2E8F) appears ONLY in teaching prose ("your color here"), never in a grounded fence.
    Add an 11-row gallery block listing each row with its source_pdf_sha256 + png_sha256 copied from
    assets/rendro/artifacts.json (certificate_dark/ticket_dark live here). Wire mix.exs docs: add
    "guides/theming.md" to docs extras, groups_for_extras, and skip_undefined_reference_warnings_on
    as needed so `mix docs --warnings-as-errors` stays green. Do NOT edit package.files.
  </action>
  <acceptance_criteria>
    - guides/theming.md exists with exactly 3 ```elixir fences each carrying a `# docs-contract:` marker, in order: theming-accent-only, theming-accent-contrast-both-ways, theming-brand-orthogonal
    - No fence body contains "..." or "%{...}"; no fence calls File.write*/System.cmd/Mix.Task.run/render-to-disk
    - Prose contains the string "readable default" (or equivalent) AND explicitly states on_accent is NOT a WCAG-AA/PDF-UA guarantee AND is overridable
    - guides/theming.md contains each of the 11 png_sha256 values from assets/rendro/artifacts.json
    - `mix docs --warnings-as-errors` exits 0
    - `git diff mix.exs` shows NO change to `package.files` (only docs extras/groups/skip-warnings)
  </acceptance_criteria>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <done>guides/theming.md ships 3 in-memory executable fences, honest on_accent wording, and the 11-row SHA block; docs build clean; package.files untouched.</done>
  <reversibility rating="reversible">Guide + docs wiring are additive; from_brand/2 is already shipped/frozen (Phase 119).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add theming_contract_test.exs to execute the guide fences as the from_brand E2E test</name>
  <files>test/docs_contract/theming_contract_test.exs</files>
  <read_first>
    - test/docs_contract/branding_contract_test.exs (the exact mirror: assert fence-id list in order, length == N, refute "..."/"%{...}", DocsContract.evaluate! each)
    - test/support/docs_contract.ex (verified_fences/1 + evaluate!/2 sandbox contract)
    - guides/theming.md (the fence ids authored in Task 1)
  </read_first>
  <action>
    Create test/docs_contract/theming_contract_test.exs mirroring branding_contract_test.exs for
    guides/theming.md: one test asserting DocsContract.verified_fences("guides/theming.md") maps to
    exactly ["theming-accent-only", "theming-accent-contrast-both-ways", "theming-brand-orthogonal"]
    in order; one test asserting length == 3, no fence body contains "..." or "%{...}", and
    DocsContract.evaluate! runs each fence (a raising assertion inside a fence fails this test — that
    is the from_brand E2E gate). This is the ONLY E2E test for from_brand/2 (D-04: the guide fence IS
    the test; no separate standalone integration test).
  </action>
  <acceptance_criteria>
    - test/docs_contract/theming_contract_test.exs asserts the 3 fence ids in exact order and length == 3
    - The test calls DocsContract.evaluate! on each fence (executing the accent coercion + both-ways on_accent + BrandedInvoice orthogonality assertions)
    - `mix test test/docs_contract/theming_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/theming_contract_test.exs</automated>
  </verify>
  <done>The guide fences execute green as the from_brand/2 E2E proof (accent coerce + on_accent both-ways + orthogonality).</done>
  <reversibility rating="reversible">Additive test; mirrors an existing proven pattern.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Flip the guides/theming.md guard, bind theming claims to proof, verify tarball lane</name>
  <files>test/docs_contract/theming_claims_test.exs, priv/support_matrix.json</files>
  <read_first>
    - test/docs_contract/theming_claims_test.exs (L163-165 refute File.exists?("guides/theming.md") — must invert; the overclaim tripwire @boundary_keys/@overclaim_terms L16-56; dark @doc boundary sentence check L155)
    - priv/support_matrix.json (theming.light + theming.dark rows L607+; boundary keys already "unsupported")
    - test/docs_contract/branding_claims_test.exs (L41-86 tarball lane: includes lib/rendro + priv/branded assets + NOTICE; excludes priv/quality/, priv/support_matrix.json, priv/schemas/rubric_scores.schema.json, priv/goldens/, priv/raster_refs/)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§GT-8 tarball nuance "no new asset ships" is precise not literal; Pitfall 3)
  </read_first>
  <action>
    In test/docs_contract/theming_claims_test.exs, remove/invert the L163 guard so it asserts
    File.exists?("guides/theming.md") is true (drop the "deferred to Phase 123" wording). Add
    assertion(s) binding every NEW theming claim the guide makes to proof: keep the overclaim tripwire
    green (no theming boundary key may pair a print/pdf-ua/wcag/accessibility term with a supported*
    status), and if guides/theming.md asserts a from_brand / accent-theming capability not yet in
    priv/support_matrix.json theming.light, add a proof-backed capability key there (status
    "supported" only for genuinely-supported deterministic capabilities; boundaries stay
    "unsupported"). Add a drift guard that every png_sha256 in assets/rendro/artifacts.json appears in
    guides/theming.md (so the guide's SHA block cannot silently drift from the manifest). Confirm the
    Hex-tarball lane stays green WITHOUT editing package.files: run the tarball lane and verify the 4
    new gallery PNGs + guides/theming.md ship while priv/quality/, priv/support_matrix.json, and
    priv/schemas/rubric_scores.schema.json remain excluded (Pitfall 3 — do NOT try to keep the PNGs
    out of the tarball).
  </action>
  <acceptance_criteria>
    - test/docs_contract/theming_claims_test.exs asserts `File.exists?("guides/theming.md")` (the refute is gone)
    - The overclaim tripwire still passes: no theming boundary key with a print/pdf-ua/wcag term carries a supported* status
    - A drift guard asserts every artifacts.json png_sha256 is present in guides/theming.md
    - `mix test test/docs_contract/theming_claims_test.exs` exits 0
    - `mix test test/docs_contract/branding_claims_test.exs` exits 0 (tarball excludes priv/quality/support_matrix/rubric-schema; ships assets/rendro + guides)
    - `git diff mix.exs` shows no `package.files` change
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/theming_claims_test.exs test/docs_contract/branding_claims_test.exs</automated>
  </verify>
  <done>The guides/theming.md guard is flipped, every theming claim binds to proof with no overclaim, the SHA block cannot drift, and the tarball lane is green with the correct includes/excludes.</done>
  <reversibility rating="reversible">Test inversion + additive matrix capability keys; no engine or public-API change.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces:
- **New file** `guides/theming.md`: 3 `# docs-contract:` fences (`theming-accent-only`, `theming-accent-contrast-both-ways`, `theming-brand-orthogonal`) + honest on_accent wording + an 11-row SHA-256 gallery block.
- **New file** `test/docs_contract/theming_contract_test.exs`: the from_brand/2 E2E gate (fence ids in order + evaluate! each).
- **Modified** `test/docs_contract/theming_claims_test.exs` (guard flipped + proof bindings + SHA drift guard), `priv/support_matrix.json` (any missing proof-backed theming capability key), `mix.exs` (docs extras/groups/skip-warnings — NOT package.files).
- **New marker IDs**: `theming-accent-only`, `theming-accent-contrast-both-ways`, `theming-brand-orthogonal`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authoring boundary | from_brand accepts a hex string coerced by Base.decode16! + validated by Rendro.Color.validate/1 at resolve/1; malformed hex raises (errors-as-product). No new input surface. |

## STRIDE Threat Register (ASVS L1, block-on-high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-INPUT | Tampering | from_brand hex input (author-time) | low | accept | Minimal V5: hex is coerced + validated at the authoring boundary with no injection sink; docs-contract evaluate! sandbox blocks File.write*/System.cmd/Mix.Task.run — keep fences side-effect-free to preserve it. |
| T-123-NA | N/A | Docs/matrix/tarball assertions | low | accept | No auth/session/network/DB; sha256 is content-addressing. Honest N/A per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `mix test test/docs_contract/theming_contract_test.exs` green (from_brand E2E).
- `mix test test/docs_contract/theming_claims_test.exs` green (guard flipped, no overclaim, no SHA drift).
- `mix test test/docs_contract/branding_claims_test.exs` green (tarball includes/excludes correct).
- `mix docs --warnings-as-errors` green.
</verification>

<success_criteria>
from_brand/2 is proven end-to-end by executable guide fences (accent coerce + on_accent both-ways +
BrandedInvoice orthogonality), every public theming claim binds to proof with honest bounding and no
overclaim, and the docs + Hex-tarball lanes are green with the theme shipping as pure code.
</success_criteria>

<output>
Create `.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-04-SUMMARY.md` when done.
</output>
