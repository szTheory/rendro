---
phase: 123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani
plan: 05
type: execute
wave: 4
depends_on: [123-03]
files_modified:
  - priv/quality/rubric_scores.json
  - priv/quality/SIGN-OFF.md
  - priv/schemas/rubric_scores.schema.json
  - test/docs_contract/rubric_manifest_contract_test.exs
autonomous: false
requirements: [DEFAULT-02]
must_haves:
  truths:
    - "All 6 rubric demos are re-scored against the CURRENT themed default/0 bytes (not the 2026-07-19 native-scale rasters); frozen thresholds (hierarchy==5, core>=4, gates true) are UNTOUCHED; recorded passed == passed?/2 recomputation for every entry (DEFAULT-02, D-02)."
    - "Every passed==true record carries a non-empty signed_off_by, a signed_off_at (ISO date), and an evidence_ref that File.exists? AND is covered by the hash-checked artifacts.json gallery — enforced by schema if/then AND test-loop teeth, fail-loud both directions (D-02)."
    - "HONESTY TRAP (git-provable): the score-flip commit's diff touches ONLY paths under priv/quality/, priv/schemas/, and test/docs_contract/ — ZERO lib/*.ex, ZERO assets/, ZERO palette/token/colour code. A passed:true is recorded in a commit that changed no colours, pointing at bytes blessed in Commit 2 (D-05 Commit 3)."
    - "A passed:true is committed ONLY on an honest human clear; if the themed Certificate does not honestly earn content_hierarchy==5, it is recorded 4 / passed:false (an honest finding), never rubber-stamped (D-02, Pitfall 2)."
    - "priv/quality/SIGN-OFF.md exists in the brand/audit/SCORECARD.md house style (dated, per-demo, 'Honest not flattering'), each demo citing its measured themed key-fact glyph delta; an additive Signed-off-by git trailer is on the score commit."
  artifacts:
    - priv/quality/rubric_scores.json
    - priv/quality/SIGN-OFF.md
    - priv/schemas/rubric_scores.schema.json
  key_links:
    - "themed gallery PNG (Commit 2) -> evidence_ref in rubric_scores.json -> File.exists? + artifacts.json hash coverage -> rubric_manifest_contract_test teeth (a passed:true without a live hash-checked evidence_ref fails the build)."
    - "human judgment on the pre-computed deltas -> passed field -> passed?/2 recomputation (the verdict can never be asserted independently, SHOW-01 gate)."
---

<objective>
D-05 Commit 3 — the HONEST RE-SCORE (score-flip), the honesty culmination of the phase (DEFAULT-02).
Re-score all 6 rubric demos against the themed default/0 bytes blessed in Commit 2, add
machine-enforced human sign-off, and record it in a commit whose diff contains ZERO colour code — the
exact discipline that makes the honest order provable in git and avoids the named trap (a slick
palette + a flipped score).

Research GT-5 verified the graft points: `passed?/2` (rubric_manifest_contract_test.exs L33-45)
recomputes the verdict so it can never be asserted independently; the schema `$defs/score_entry`
(`additionalProperties: true`) accepts new fields non-breakingly; the L94-114 loop is where the
sign-off teeth graft. The Big Finding makes this commit's honesty real: the 2026-07-19 justifications
cite native-scale glyph heights (34px, 24px…) that no longer describe the themed rasters, and the
themed Certificate recipient/title ratio drops 1.70→1.27 — the #1 honesty risk the human must confront
(measured in Commit 2; judged here). If it does not honestly clear `==5`, record `4`/`passed:false`.

Purpose: flip the scores honestly — human judgment over pre-computed evidence, machine-enforced provenance, zero colour code — in an isolated, git-provable commit.
Output: sign-off fields on all 6 records; schema if/then + test teeth; SIGN-OFF.md; a Signed-off-by trailer; a git-isolation proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-03-SUMMARY.md
@brand/audit/SCORECARD.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human sign-off — judge the 6 themed rubric demos against pre-computed evidence</name>
  <action>Pause for human verification. Do NOT write any rubric score until the human returns a verdict. Present the 6 themed light gallery rasters + the pre-computed glyph-height deltas (123-03-SUMMARY.md) + the invoice before/after, and collect a per-demo honest verdict (especially the flagged Certificate 1.27 hierarchy risk). The verdict drives Task 2's score writes.</action>
  <what-built>
    The 6 themed default/0 (light) gallery rasters blessed in Commit 2 (Plan 03) —
    assets/rendro/gallery/{invoice,statement,receipt_report,certificate,payslip,ticket}.png — plus,
    per demo, a measured key-fact-vs-next-largest glyph-height delta computed from those themed bytes
    (recorded in 123-03-SUMMARY.md), and an invoice before/after confirming parties + totals present.
    All measurement is pre-computed: the human's job is judgment, not arithmetic (anti-rubber-stamp).
  </what-built>
  <how-to-verify>
    1. Open the 6 light gallery PNGs and read the 6 measured deltas in 123-03-SUMMARY.md.
    2. For each demo, confirm the key fact is the single dominant focal point (content_hierarchy == 5)
       and every other core dimension honestly clears >= 4 against the Milestone-A rubric — the
       thresholds are frozen; a green demo must clear the UNCHANGED bar.
    3. FLAGGED RISK — Certificate: the themed recipient/title glyph ratio is ~1.27 (was 1.70 native).
       Confirm honestly whether the recipient still reads as one unambiguous focal point. If NOT:
       either (a) the emphasis was already strengthened in Commit 2 and re-blessed (verify the raster),
       or (b) direct that certificate be recorded content_hierarchy: 4 / passed: false (an honest
       finding), or (c) send it back to Commit 2 (Plan 03) for an anchor fix — never resolve it by
       editing the score here.
    4. Confirm the invoice raster shows issuer + customer + totals (the Phase-115 DATA fix survived).
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 honestly clear (hierarchy==5, core>=4, gates true), or give per-demo verdicts (e.g. "certificate: hierarchy 4, passed false" or "certificate: return to Commit 2 for anchor fix").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the honest re-score — sign-off fields + schema if/then + test teeth + SIGN-OFF.md</name>
  <files>priv/quality/rubric_scores.json, priv/quality/SIGN-OFF.md, priv/schemas/rubric_scores.schema.json, test/docs_contract/rubric_manifest_contract_test.exs</files>
  <read_first>
    - priv/quality/rubric_scores.json (6 records, recorded_at 2026-07-19; dimension_scores/gate_results/justifications)
    - priv/schemas/rubric_scores.schema.json (L91-140 $defs/score_entry.required = [demo_id, domain, family, dimension_scores, gate_results, passed, recorded_at]; additionalProperties: true)
    - test/docs_contract/rubric_manifest_contract_test.exs (passed?/2 L33-45; "recorded passed matches recomputation" loop L94-114 — graft the new teeth here; schema-validation test L47-50)
    - brand/audit/SCORECARD.md (house style: dated, per-dimension, "Honest not flattering")
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-03-SUMMARY.md (the 6 pre-computed themed deltas + Certificate ratio)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§GT-5 two-layer enforcement; §Code Examples sign-off record shape)
  </read_first>
  <action>
    Per the human verdict from Task 1, update each of the 6 records in priv/quality/rubric_scores.json:
    (1) replace the native-scale justifications with the fresh themed glyph-height deltas from
    123-03-SUMMARY.md; (2) set dimension_scores/gate_results to the honestly-judged values (if the
    human recorded certificate content_hierarchy 4, set it to 4 so passed?/2 recomputes passed:false —
    do NOT force 5); (3) add signed_off_by (e.g. "qiksnare13"), signed_off_at (ISO date, e.g.
    "2026-07-28"), and evidence_ref (the demo's light themed gallery PNG path under
    assets/rendro/gallery/) to every record. Do NOT touch the frozen threshold arithmetic
    (hierarchy_min 5, core_min >= 4) and do NOT touch dimensions/gates structure. Update
    priv/schemas/rubric_scores.schema.json: add signed_off_by (string, minLength 1), signed_off_at
    (string, format date), evidence_ref (string, minLength 1) to score_entry properties, and a
    top-level if/then on score_entry — if passed == true then required includes
    [signed_off_by, signed_off_at, evidence_ref]. If JSV if/then proves finicky (A1), the test-loop
    teeth are the load-bearing guard — keep both. Add teeth to
    test/docs_contract/rubric_manifest_contract_test.exs: a new `for entry <- scores` loop asserting
    every passed==true entry has non-empty signed_off_by, a signed_off_at, and an evidence_ref that
    File.exists? AND is present in the hash-checked assets/rendro/artifacts.json gallery (match on
    png_path); fail loud both directions (a passed:true without a live hash-checked evidence_ref fails
    the build; a passed:false must not be blocked). Author priv/quality/SIGN-OFF.md in the
    brand/audit/SCORECARD.md house style (dated, per-demo, "Honest not flattering"), each demo citing
    its measured themed key-fact glyph height vs next-largest element + the rubric anchor language it
    satisfies (and, for any passed:false, the honest reason). Commit this as Commit 3 with an additive
    `Signed-off-by: qiksnare13 <qiksnare13@gmail.com>` git trailer. This commit MUST contain zero
    palette/token/colour code and no lib/ or assets/ change.
  </action>
  <acceptance_criteria>
    - Every record in priv/quality/rubric_scores.json has non-empty signed_off_by, a signed_off_at, and an evidence_ref pointing at an existing assets/rendro/gallery/*.png that is present in artifacts.json
    - Recorded `passed` equals the passed?/2 recomputation for every entry (no independently-asserted passed:true)
    - priv/schemas/rubric_scores.schema.json requires signed_off_by/signed_off_at/evidence_ref when passed==true (if/then) and validates the checked-in manifest
    - rubric_manifest_contract_test.exs has a new loop asserting evidence_ref exists AND is manifest-covered for every passed:true entry
    - priv/quality/SIGN-OFF.md exists, dated, per-demo, citing themed glyph deltas
    - `mix test test/docs_contract/rubric_manifest_contract_test.exs` exits 0
    - The commit carries a `Signed-off-by: qiksnare13 <qiksnare13@gmail.com>` trailer
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs</automated>
  </verify>
  <done>All 6 demos honestly re-scored against themed bytes with machine-enforced live-hash-checked sign-off, SIGN-OFF.md authored, and the recompute + teeth green.</done>
  <reversibility rating="reversible">rubric_scores.json is an appendable S5 manifest; a regretted pass reverts by rolling back this commit alone (the D-05 split guarantees it stands independently).</reversibility>
</task>

<task type="auto">
  <name>Task 3: Prove the score-flip commit isolation (zero colour code) + run the phase gate</name>
  <files>priv/quality/rubric_scores.json</files>
  <read_first>
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md (D-05 Commit 3: touches ONLY rubric_scores.json + SIGN-OFF.md + schema/test; zero palette/token/colour code)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-VALIDATION.md (phase gate: mix ci.fast + mix ci.advisory green before /gsd-verify-work)
  </read_first>
  <action>
    Prove the honesty-trap prohibition is satisfied and run the phase gate. Inspect the Commit-3 diff
    (`git show --stat` for the score commit) and assert every changed path is under priv/quality/,
    priv/schemas/, or test/docs_contract/ — ZERO lib/*.ex, ZERO assets/, ZERO palette/token/colour
    edit. This is what makes the honest order provable: passed:true was recorded in a commit that
    changed no colours, pointing at bytes blessed in Commit 2. Then run the full phase gate:
    `mix ci.fast` (includes the hex.build tarball lane + `mix docs --warnings-as-errors`) and
    `mix ci.advisory` (gallery hash + raster) must both be green.
  </action>
  <acceptance_criteria>
    - `git show --stat <score-commit>` lists ONLY paths under priv/quality/, priv/schemas/, test/docs_contract/ (grep for lib/ or assets/ in the changed-path list returns nothing)
    - `mix ci.fast` exits 0
    - `mix ci.advisory` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix ci.fast</automated>
  </verify>
  <done>The score-flip commit is provably colour-free and isolated, and both phase gates (ci.fast + ci.advisory) are green.</done>
  <reversibility rating="reversible">Verification-only; no product code touched.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (Commit 3) produces:
- **Modified** `priv/quality/rubric_scores.json`: each of 6 records gains `signed_off_by`, `signed_off_at`, `evidence_ref` + fresh themed justifications; `passed` honestly recomputed.
- **New file** `priv/quality/SIGN-OFF.md` (SCORECARD house style, dated, per-demo).
- **Modified** `priv/schemas/rubric_scores.schema.json`: new `signed_off_by`/`signed_off_at`/`evidence_ref` fields + if/then requiring them when `passed==true`.
- **Modified** `test/docs_contract/rubric_manifest_contract_test.exs`: sign-off teeth loop (evidence_ref exists AND manifest-covered).
- An additive `Signed-off-by:` git trailer on the score commit.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none | Manifest/schema/test edits over in-repo data; no auth/session/network/DB/user-input surface. |

## STRIDE Threat Register (ASVS L1, block-on-high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-NA | N/A | Rubric manifest + sign-off provenance | low | accept | The git Signed-off-by trailer + evidence_ref are provenance/integrity aids, not security controls; sha256 is content-addressing. No external input, no package install, no network/DB. Honest N/A per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `mix test test/docs_contract/rubric_manifest_contract_test.exs` green (recompute + sign-off teeth).
- The score-flip commit diff is provably colour-free (only priv/quality/, priv/schemas/, test/docs_contract/).
- `mix ci.fast` + `mix ci.advisory` green (phase gate).
</verification>

<success_criteria>
All 6 demos are honestly re-scored against the themed bytes with machine-enforced, human-signed,
live-hash-checked provenance; a passed:true exists only on an honest clear; and the score-flip is an
isolated, git-provable, zero-colour-code commit — the honesty trap is demonstrably avoided.
</success_criteria>

<output>
Create `.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-05-SUMMARY.md` when done.
</output>
