---
phase: 123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani
plan: 03
type: execute
wave: 3
depends_on: [123-02]
files_modified:
  - lib/rendro/launch_artifacts.ex
  - assets/rendro/artifacts.json
  - assets/rendro/gallery/invoice.png
  - assets/rendro/gallery/branded_invoice.png
  - assets/rendro/gallery/statement.png
  - assets/rendro/gallery/receipt_report.png
  - assets/rendro/gallery/certificate.png
  - assets/rendro/gallery/payslip.png
  - assets/rendro/gallery/ticket.png
  - assets/rendro/gallery/invoice_dark.png
  - assets/rendro/gallery/certificate_dark.png
  - assets/rendro/gallery/ticket_dark.png
  - assets/rendro/gallery/invoice_brand.png
  - README.md
  - guides/recipes.md
  - test/docs_contract/launch_artifacts_claims_test.exs
autonomous: true
requirements: [DEFAULT-03]
must_haves:
  truths:
    - "assets/rendro/artifacts.json has exactly 11 ordered gallery rows: 7 retagged theme:\"default\" + invoice_dark + certificate_dark + ticket_dark + invoice_brand; every row has preset == null (DEFAULT-03, D-03)."
    - "The 7 existing rows render via theme: Rendro.Theme.default() so the \"default\" tag is literally true; all 7 re-bless because the theme swaps each recipe's native type scale, not just leading (Big Finding, GT-3)."
    - "invoice_brand renders via from_brand(accent: \"#0E7C76\") accent-only (no assets); the 3 dark rows render via Theme.dark(Theme.default()); S6 tags theme/mode valid, preset null on all 11."
    - "No rubric_scores.json change in this commit (D-05 Commit 2 isolation); glyph-height deltas are pre-computed against the THEMED rasters for the honest re-score."
  artifacts:
    - assets/rendro/artifacts.json
    - assets/rendro/gallery/invoice_dark.png
    - assets/rendro/gallery/certificate_dark.png
    - assets/rendro/gallery/ticket_dark.png
    - assets/rendro/gallery/invoice_brand.png
  key_links:
    - "@gallery_specs entry -> build_source_document/1 clause -> pdfium raster -> png_sha256 in artifacts.json -> evidence_ref cited by the Commit-3 rubric sign-off."
    - "readme_hero flag on @gallery_specs -> emitted into artifacts.json by gen -> readme_block/1 filter -> README hero subset (7 light + theming strip)."
---

<objective>
D-05 Commit 2 (part b) — gallery closure (DEFAULT-03). Populate the S6 `theme`/`mode` tags with 11
curated, hash-checked rows (D-03): retag the 7 existing rows to `theme:"default"` (rendered via
`theme: Rendro.Theme.default()` so the tag is literally true), and append 3 dark
(`invoice_dark`/`certificate_dark`/`ticket_dark` via `Theme.dark(Theme.default())`) + 1 `from_brand`
showcase (`invoice_brand` via `from_brand(accent: "#0E7C76")`, accent-only, no assets) — the flagship
Invoice triptych (default / dark / brand). `preset` stays `null` on all 11.

Research Big Finding (GT-3): threading `theme: default()` swaps each recipe's **native** type scale
(e.g. Certificate display 34→21), so **all 7** retagged rows re-bless — NOT just the prose rows. D-03's
"non-prose rows stay byte-identical" sub-claim is corrected; D-02 already stated the theme "changed
every demo's pixels," so this does not break the decision. This commit re-blesses all 7 + adds 4 new
rasters and **pre-computes fresh glyph-height deltas against the themed bytes** for the honest re-score
— it must NOT touch `priv/quality/rubric_scores.json` (Commit-3 isolation).

Purpose: land the themed/dark/brand gallery + fresh measured deltas so the human sign-off (Plan 05) judges real numbers, not the stale 2026-07-19 native-scale figures.
Output: 4 new specs/clauses/dims + readme_hero filter; regenerated artifacts.json (11 rows) + 11 PNGs; count test 7→11; pre-computed themed deltas (esp. the Certificate hierarchy checkpoint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md
@lib/rendro/launch_artifacts.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 gallery specs + dims + build_source_document clauses + retag 7 + readme_hero filter</name>
  <files>lib/rendro/launch_artifacts.ex</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex (@gallery_optional_s6_keys L29; @expected_gallery_dimensions L30-40; @gallery_specs L47-125; build_source_document/1 clauses L313-381; readme_block/1 L217-246; recipes_block/1 L248-287; expected_ids L571; s6_seam_errors L673-687)
    - lib/rendro/theme.ex (default/0 L180; dark/1; from_brand/2 L266-299 — accent: "#0E7C76" coerces to {14,124,118})
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md (D-03: 11-row set, tags, readme_hero; D-04 accent seed)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§GT-6 exact new-clause shapes + dims; Open Q2 readme_hero placement)
  </read_first>
  <action>
    Extend @gallery_specs with 4 new entries after the 7 existing (order matters — expected_ids is
    order-sensitive at L620): invoice_dark, certificate_dark, ticket_dark, invoice_brand, each with
    id/title/module/png_path/asset_name/fit/alt/caption. Dark rows' alt/caption must echo the
    support-matrix screen-oriented boundary ("screen-oriented, not recommended for print"). Add a
    readme_hero boolean to every spec: true for the 7 light rows + invoice_dark + invoice_brand
    (the README theming strip), false for certificate_dark + ticket_dark (guide-only). Add the 4 ids
    to @expected_gallery_dimensions: invoice_dark {794,1123}, invoice_brand {794,1123},
    certificate_dark {1123,794}, ticket_dark {397,560}. Add build_source_document/1 clauses:
    "invoice_dark" -> Invoice.document(data, theme: Theme.dark(Theme.default())) |> apply_launch_table_style();
    "certificate_dark" -> Certificate.document(data, theme: Theme.dark(Theme.default()), border: true) |> apply_certificate_body_wrap();
    "ticket_dark" -> Ticket.document(data, theme: Theme.dark(Theme.default())) |> apply_ticket_terms_wrap();
    "invoice_brand" -> Invoice.document(data, theme: Theme.from_brand(accent: "#0E7C76")) |> apply_launch_table_style().
    Retag the 7 existing clauses to thread theme: Rendro.Theme.default() into each recipe document/2
    call (per-spec or via a shared themed-default helper — Claude's Discretion), so the "default" tag
    is literally true. Update readme_block/1 (L217) to filter the manifest gallery to entries whose
    readme_hero is true (D-03 hero subset), leaving recipes_block/1 rendering all 11. Emit readme_hero
    into artifacts.json via gen so readme_block/1 filters purely on the manifest.
  </action>
  <acceptance_criteria>
    - `Rendro.LaunchArtifacts.gallery_specs()` returns 11 entries in order: invoice, branded_invoice, statement, receipt_report, certificate, payslip, ticket, invoice_dark, certificate_dark, ticket_dark, invoice_brand
    - Each spec has a `readme_hero` boolean; certificate_dark and ticket_dark are false, the other 9 true
    - build_source_document/1 has clauses for all 4 new ids using Theme.dark(Theme.default()) (dark) and from_brand(accent: "#0E7C76") (brand); the 7 original clauses thread theme: Theme.default()
    - `@expected_gallery_dimensions` has entries for invoice_dark/certificate_dark/ticket_dark/invoice_brand
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>11 specs (7 themed-default + 3 dark + 1 brand) with readme_hero flags, dims, and build_source_document clauses compile clean.</done>
  <reversibility rating="reversible">Specs regenerate the gallery from data; dropping a row is a spec deletion + regen.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Regenerate the gallery (re-bless 7 + 4 new), update the count test 7->11, verify hashes</name>
  <files>assets/rendro/artifacts.json, assets/rendro/gallery/invoice.png, assets/rendro/gallery/branded_invoice.png, assets/rendro/gallery/statement.png, assets/rendro/gallery/receipt_report.png, assets/rendro/gallery/certificate.png, assets/rendro/gallery/payslip.png, assets/rendro/gallery/ticket.png, assets/rendro/gallery/invoice_dark.png, assets/rendro/gallery/certificate_dark.png, assets/rendro/gallery/ticket_dark.png, assets/rendro/gallery/invoice_brand.png, README.md, guides/recipes.md, test/docs_contract/launch_artifacts_claims_test.exs</files>
  <read_first>
    - test/docs_contract/launch_artifacts_claims_test.exs (L12-25 "manifest records exactly the seven recipe previews" — the hard-coded id list to expand to 11)
    - lib/mix/tasks/rendro/launch_artifacts/gen.ex + check.ex (regen/verify entry points)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§GT-6 regen/check flow; Pitfall 5 hard-coded count)
  </read_first>
  <action>
    Run `mix rendro.launch_artifacts.gen` to re-render all 11 rasters (the 7 re-bless because the
    themed type scale shifted per the Big Finding; the 4 new rows are net-new hashed rasters) and
    rewrite assets/rendro/artifacts.json + the README/guides/recipes.md doc blocks. Update
    test/docs_contract/launch_artifacts_claims_test.exs L12-25: change the assertion from the 7-id
    list to the 11 ordered ids (invoice, branded_invoice, statement, receipt_report, certificate,
    payslip, ticket, invoice_dark, certificate_dark, ticket_dark, invoice_brand) and update the test
    name from "seven recipe previews" to eleven. Confirm every artifacts.json row carries
    theme ("default" ×10, "brand" ×1), mode ("light"/"dark"), and preset == null. Then run
    `mix rendro.launch_artifacts.check` to verify manifest shape + hash integrity. This commit must
    NOT modify priv/quality/rubric_scores.json (D-05 Commit-2 isolation).
  </action>
  <acceptance_criteria>
    - artifacts.json gallery has 11 entries in the exact order above; every entry `preset` is null (JSON null)
    - theme values are "default" for 10 rows and "brand" for invoice_brand; mode is "dark" for the 3 dark rows and "light" otherwise
    - 4 new PNGs exist: assets/rendro/gallery/{invoice_dark,certificate_dark,ticket_dark,invoice_brand}.png
    - `mix test test/docs_contract/launch_artifacts_claims_test.exs` exits 0 (count is 11, not 7)
    - `mix rendro.launch_artifacts.check` exits 0
    - `git show --stat HEAD` for this commit contains NO `priv/quality/rubric_scores.json` path
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.launch_artifacts.check && mix test test/docs_contract/launch_artifacts_claims_test.exs</automated>
  </verify>
  <done>artifacts.json is 11 hash-checked rows (preset null on all), 4 new PNGs blessed, the count test is 11, and no rubric score changed.</done>
  <reversibility rating="reversible">Re-blessing gallery hashes within a version is the normal expected motion; regen reproduces from specs.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Pre-compute themed glyph-height deltas + confront the Certificate hierarchy checkpoint</name>
  <files>lib/rendro/launch_artifacts.ex</files>
  <read_first>
    - priv/quality/rubric_scores.json (existing 6 justifications cite NATIVE-scale glyph heights, e.g. certificate "Alex Rivera 34px vs title 26px", invoice "Total Due 24px vs title 19px" — these are now counterfactual)
    - lib/rendro/recipes/certificate.ex (display anchor / recipient run; the Phase-122 display anchor is the emphasis lever)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (Big Finding item 3 + Pitfall 2 + Open Q1: themed Certificate recipient/title ratio 34/20=1.70 -> 21/16.5=1.27; measure before scoring; strengthen anchor in Commit 2 or record 4/passed:false — NEVER flip in Commit 3)
  </read_first>
  <action>
    For each of the 6 rubric demos (invoice, statement, receipt, certificate, payslip, ticket),
    measure the key-fact glyph height vs the next-largest element on the THEMED default/0 raster
    (the same bytes blessed in Task 2) and record the measured delta as the honest-re-score evidence.
    Do NOT reuse the 2026-07-19 native-scale numbers (Pitfall 1). Record these six deltas in the
    123-03-SUMMARY.md so Plan 05's SIGN-OFF.md and rubric justifications cite real themed figures.
    Highest-risk case: the Certificate — measure the recipient (display) vs title glyph-height ratio
    on the themed raster (~1.27 predicted). If the recipient is not clearly the single dominant focal
    point (honest content_hierarchy == 5), strengthen the Phase-122 display anchor in certificate.ex
    within THIS commit (Commit 2) and re-run gen/check to re-bless — do NOT defer emphasis work to
    the score-flip commit. If it still does not honestly dominate, record the measured ratio and flag
    it for the Plan 05 human checkpoint to record content_hierarchy: 4 / passed: false (an honest
    finding, not a plan failure). The final judgment is the human's in Plan 05; this task supplies the
    measured numbers and does any emphasis fix in Commit 2.
  </action>
  <acceptance_criteria>
    - 123-03-SUMMARY.md records a per-demo measured key-fact-vs-next-largest glyph-height delta computed from the THEMED rasters for all 6 demos
    - The Certificate recipient/title themed ratio is measured and recorded (predicted ~1.27); if any emphasis fix was applied it lives in this commit and `mix rendro.launch_artifacts.check` still exits 0
    - No number in the recorded deltas is copied verbatim from the 2026-07-19 native-scale justifications
    - No change to priv/quality/rubric_scores.json in this commit
  </acceptance_criteria>
  <verify>
    <automated>mix rendro.launch_artifacts.check</automated>
  </verify>
  <done>Fresh themed glyph-height deltas for all 6 demos are recorded, and the Certificate hierarchy risk is measured (and any emphasis fix applied in Commit 2), ready for the human sign-off.</done>
  <reversibility rating="reversible">Measurement is data; any anchor strengthening reverts with the commit and a regen.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (Commit 2b) produces:
- **4 new `@gallery_specs` entries**: `invoice_dark`, `certificate_dark`, `ticket_dark`, `invoice_brand`; a new `readme_hero` key on every spec.
- **4 new `build_source_document/1` `defp` clauses** (dark ×3 via `Theme.dark(Theme.default())`, brand ×1 via `from_brand(accent: "#0E7C76")`); 7 retagged clauses threading `theme: Theme.default()`.
- **4 new gallery PNGs**: `assets/rendro/gallery/{invoice_dark,certificate_dark,ticket_dark,invoice_brand}.png`; 7 re-blessed PNGs.
- **Modified** `assets/rendro/artifacts.json` (11 rows), `README.md` + `guides/recipes.md` (regenerated blocks), `test/docs_contract/launch_artifacts_claims_test.exs` (count 7→11).
- **Recorded** (in 123-03-SUMMARY.md): 6 themed glyph-height deltas + the Certificate hierarchy ratio.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none | Deterministic PDF→pdfium PNG→sha256 pipeline over in-repo fixtures; no external input surface. |

## STRIDE Threat Register (ASVS L1, block-on-high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-NA | N/A | Gallery specs + hash-checked rasters | low | accept | sha256 here is content-addressing (manifest integrity), not a security control; no external input, no package install, no network/DB. Honest N/A per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` green.
- `mix rendro.launch_artifacts.check` green (11 rows, hashes match).
- `mix test test/docs_contract/launch_artifacts_claims_test.exs` green (count 11).
- Commit-2 diffs contain no `priv/quality/rubric_scores.json`.
</verification>

<success_criteria>
The gallery is 11 curated hash-checked rows (7 themed-default + 3 dark + 1 brand, `preset:null` on
all, flagship Invoice triptych), the 7 re-bless honestly reflects the themed type scale, and fresh
themed glyph-height deltas are recorded for the human sign-off — all with zero rubric-score change.
</success_criteria>

<output>
Create `.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-03-SUMMARY.md` when done (include the 6 measured themed glyph-height deltas + Certificate ratio).
</output>
