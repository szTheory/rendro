---
phase: "09"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [".github/workflows/ci.yml", "mix.exs", "scripts/verify_docs.exs"]
autonomous: true
requirements: ["QUAL-01", "QUAL-02", "QUAL-05"]

must_haves:
  truths:
    - "GitHub Actions workflow runs mix ci on push and PRs"
    - "mix ci command explicitly checks formatting, docs, and hex packaging"
    - "verify_docs.exs outputs a warning instead of silently skipping partial code blocks"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI Runner config"
    - path: "mix.exs"
      provides: "Mix aliases configuration"
    - path: "scripts/verify_docs.exs"
      provides: "Documentation code validation script"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix ci"
      via: "Workflow step command"
      pattern: "run: mix ci"
---

<objective>
Establish the CI pipeline on GitHub Actions and enforce stricter code quality checks in standard Mix commands.

Purpose: Automate code quality gates so that no untested or unformatted code merges to main, and ensure that documentation gaps are visible.
Output: A GitHub Actions workflow file, an expanded `mix ci` alias, and an updated docs verification script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mix.exs
@scripts/verify_docs.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup GitHub CI and expand mix ci alias</name>
  <files>.github/workflows/ci.yml, mix.exs</files>
  <action>
    Create `.github/workflows/ci.yml` configuring a GitHub Actions workflow to run on `push` and `pull_request` to `main`. Use `actions/checkout@v4` and `erlef/setup-beam@v1` with `otp-version: '28'` and `elixir-version: '1.19.5'`. Include steps to run `mix deps.get` and `mix ci`.
    In `mix.exs`, locate the `defp aliases` function and expand the `ci` alias to include `"format --check-formatted"`, `"docs"`, and `"hex.build"` in addition to the existing commands (`compile --warnings-as-errors`, `test`, `credo --strict`, `dialyzer`).
  </action>
  <verify>
    <automated>mix ci</automated>
  </verify>
  <done>Workflow file is created and `mix ci` executes all required quality gates.</done>
</task>

<task type="auto">
  <name>Task 2: Emit warning on skipped documentation code blocks</name>
  <files>scripts/verify_docs.exs</files>
  <action>
    Modify the `rescue` block in `scripts/verify_docs.exs`. When `code =~ "..."` or `code =~ "%{...}"`, change the shell output message from an `info` "OK" to a `warning` indicating the block was skipped. Use `Mix.shell().info("  - Warning: Code block (partial) skipped")` (or `Mix.shell().error` if appropriate). Ensure this continues to not halt the system for partial blocks, while bringing visibility to what isn't strictly verified.
  </action>
  <verify>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>verify_docs.exs prints a clear warning when partial code blocks are skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI System -> Codebase | GitHub Actions executes arbitrary project code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `.github/workflows/ci.yml` | accept | Controlled via GitHub branch protection rules in the external repository settings. |
</threat_model>

<verification>
- CI workflow file exists and is valid YAML.
- `mix ci` correctly cascades through format, compile, tests, docs, and hex.build.
- `verify_docs.exs` prints warnings when run against the current `README.md`.
</verification>

<success_criteria>
`mix ci` runs locally successfully and the GitHub Actions config file is placed at the correct relative path.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ci-and-release-hardening/09-01-SUMMARY.md`
</output>