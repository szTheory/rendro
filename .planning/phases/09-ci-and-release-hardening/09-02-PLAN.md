---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: ["lib/mix/tasks/verify.ex", "lib/mix/tasks/release/preflight.ex"]
autonomous: true
requirements: ["QUAL-03", "QUAL-04"]

must_haves:
  truths:
    - "mix verify reports advisory failures without crashing the VM"
    - "release.preflight blocks releases if git tags are missing or mismatched"
    - "release.preflight validates publishing with a hex dry-run"
  artifacts:
    - path: "lib/mix/tasks/verify.ex"
      provides: "Verification suite entrypoint"
    - path: "lib/mix/tasks/release/preflight.ex"
      provides: "Release safety guardrails"
  key_links:
    - from: "lib/mix/tasks/release/preflight.ex"
      to: "Git CLI"
      via: "System.cmd checking tags"
      pattern: "git.*describe.*--tags"
---

<objective>
Harden local verification and release preflight tasks to handle errors gracefully and enforce release parity.

Purpose: Prevent VM crashes during advisory verification failures and prevent broken or untagged releases from publishing to Hex.
Output: Updated `mix verify` and `mix release.preflight` tasks with safe error boundaries and system command checks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/mix/tasks/verify.ex
@lib/mix/tasks/release/preflight.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mix verify MatchError</name>
  <files>lib/mix/tasks/verify.ex</files>
  <action>
    In `lib/mix/tasks/verify.ex` within the `run_step("Phoenix Example", ...)` block, update the file execution context logic. First, add `System.cmd("mix", ["deps.get"])` before compiling. Next, replace the bare `{_, 0} = System.cmd("mix", ["compile"])` match with a `case` statement. If compilation returns a non-zero exit code, print the error output using `Mix.shell().error/1` and return `:error` rather than crashing the VM. Return `:ok` on success.
  </action>
  <verify>
    <automated>mix verify</automated>
  </verify>
  <done>mix verify completes its execution path cleanly without raising a MatchError, returning appropriate exit codes based on step success.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce Git Tag and Publish Parity on Release</name>
  <files>lib/mix/tasks/release/preflight.ex</files>
  <action>
    In `lib/mix/tasks/release/preflight.ex`, replace the mocked git tag version check. Use `System.cmd("git", ["describe", "--tags", "--exact-match"])` to get the current exact tag. If the command fails or if the trimmed tag output does not exactly match `"v" <> version` (where version is from `Mix.Project.config()[:version]`), invoke `Mix.raise/1` with an appropriate error message indicating the mismatch.
    Finally, add a step at the end of the script to run `System.cmd("mix", ["hex.publish", "--dry-run"])` in a `case` statement. If it fails with a non-zero exit code, use `Mix.raise/1` to abort the preflight.
  </action>
  <verify>
    <automated>mix release.preflight</automated>
  </verify>
  <done>The preflight task correctly queries git for exact tags, compares them to the mix.exs version, and successfully performs a hex publish dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preflight Task -> Hex.pm | Local environment pushing package releases to the public registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Spoofing | `release.preflight.ex` | mitigate | Validating exact git tag against mix version guarantees parity between the code repository release marker and the uploaded hex artifact. |
</threat_model>

<verification>
- `mix verify` handles compilation errors gracefully by returning non-zero from the process but printing a structured message.
- `mix release.preflight` fails when executed without an exact git tag matching the version string.
</verification>

<success_criteria>
`mix verify` successfully completes, and `mix release.preflight` attempts its checks and safely fails on tag mismatches rather than proceeding blindly.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ci-and-release-hardening/09-02-SUMMARY.md`
</output>