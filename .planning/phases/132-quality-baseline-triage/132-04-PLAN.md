---
phase: 132-quality-baseline-triage
plan: "04"
type: execute
wave: 3
depends_on:
  - 132-03
files_modified:
  - .planning/phases/132-quality-baseline-triage/132-VALIDATION.md
  - .planning/phases/132-quality-baseline-triage/132-02-SUMMARY.md
  - .planning/phases/132-quality-baseline-triage/132-UAT.md
  - .github/workflows/ci.yml
  - priv/guardrails/required_status_checks.json
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
gap_closure: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04]
must_haves:
  truths:
    - "D-01: through D-04: Phase 132 closure remains maintenance-only and does not move the human-first ledger into runtime, package, release, or ordinary regression state."
    - "D-10: through D-14: deterministic, proof, advisory, and human-review evidence authority stays separate; unavailable remote evidence is explicit and does not block an otherwise automated objective claim."
    - "D-15: through D-24: UAT, SUMMARY, and VALIDATION cite the exact automated matrix, authority, consumer, and metric-governance evidence without weakening qualitative disposition rules."
    - "Implementation-owned PLAN/SUMMARY/UAT/VALIDATION blockers are removed before staged governance is allowed to exempt the one exact verifier-owned stale report."
    - "132-VALIDATION.md includes Plans 03 and 04, has automated or explicit-deferral/advisory semantics, records all task rows green, and ends `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true` with no pending/manual-only sign-off."
    - "The four UAT items are automated passes and AUDIT-04 summary coverage has `human_judgment: false` only after Plan 03 contracts pass."
    - "An independent always-running `quality-governance` CI job gates `ci-success`; `ci-success` remains the sole branch-protection required context."
    - "After the standard verifier rewrites 132-VERIFICATION.md, unexceptioned `mix quality.governance` rejects any `human_needed` or human-verification entries and all final local gates pass."
  artifacts:
    - path: ".planning/phases/132-quality-baseline-triage/132-VALIDATION.md"
      provides: "Terminal Nyquist contract including Plans 03/04 and no blocking human semantics"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/132-quality-baseline-triage/132-UAT.md"
      provides: "Four automated terminal UAT passes"
      contains: "passed: 4"
    - path: ".github/workflows/ci.yml"
      provides: "Independent quality-governance job in strict ci-success roll-up"
      contains: "quality-governance:"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Parsed topology and always-running governance job contract"
      contains: "quality-governance"
  key_links:
    - from: ".planning/phases/132-quality-baseline-triage/132-VALIDATION.md"
      to: "scripts/quality_governance.cjs"
      via: "Plan 03/04 validation rows name automated governance and staged/full order"
      pattern: "quality\\.governance"
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "quality-governance job runs the single command"
      pattern: "mix quality\\.governance"
    - from: ".github/workflows/ci.yml"
      to: "priv/guardrails/required_status_checks.json"
      via: "registry and YAML test agree on ci-success-only branch protection and roll-up membership"
      pattern: "ci-success"
---

<objective>
Close implementation-owned Phase 132 planning evidence, install governance as required CI, and complete the staged-to-full verification handoff.

Purpose: Make Nyquist, UAT, summary, CI, and final verifier state agree that completion is automated while preserving exact evidence authority and the sole `ci-success` branch-protection contract.
Output: terminal VALIDATION/UAT/SUMMARY evidence, required governance CI topology, and an executable post-verifier full validation sequence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/QUALITY.md
@.planning/phases/132-quality-baseline-triage/132-CONTEXT.md
@.planning/phases/132-quality-baseline-triage/132-VALIDATION.md
@.planning/phases/132-quality-baseline-triage/132-VERIFICATION.md
@.planning/phases/132-quality-baseline-triage/132-UAT.md
@.planning/phases/132-quality-baseline-triage/132-02-SUMMARY.md
@.planning/phases/132-quality-baseline-triage/132-03-SUMMARY.md
@.github/workflows/ci.yml
@priv/guardrails/required_status_checks.json
@test/guardrails/required_checks_contract_test.exs
@mix.exs

<interfaces>
- Plan 03 provides `mix quality.governance`, `node --test scripts/quality_governance.cjs`, and the staged direct Node command.
- The only staging exception is path `.planning/phases/132-quality-baseline-triage/132-VERIFICATION.md`, SHA-256 `1ade3d2f9e772ff2871253c435662fb33b146d13f61c54d8422e2ec7d13b2dfd`, source commit `be640780df7852387b493b392b7eb148308ea01b`.
- `ci-success` currently uses `if: always()` and fails for failure, cancellation, or skipped results.
- `RequiredChecksContractTest` already parses YAML and exposes `ci_job_block!/2`; extend these seams rather than raw whole-file matching.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require governance through ci-success</name>
  <files>.github/workflows/ci.yml, priv/guardrails/required_status_checks.json, test/guardrails/required_checks_contract_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` — pinned actions/toolchains and strict ci-success aggregation.
    - `required_status_checks.json` — sole required context and current non-advisory registry.
    - `required_checks_contract_test.exs` — parsed workflow and exact alias assertions.
  </read_first>
  <behavior>
    - "Top-level quality-governance has no needs, job-level condition, path filter, continue-on-error, or advisory semantics; it uses pinned checkout/setup-beam/setup-node, OTP 28, Elixir 1.19.5, Node 22.14.0, Mix dependency install, and `mix quality.governance`."
    - "ci-success.needs includes quality-governance and retains `if: always()` plus failure/cancelled/skipped rejection."
    - "required_contexts remains exactly `[ci-success]`; quality-governance is a non-advisory roll-up context, never a second branch-protection context."
    - "Parsed-YAML mutations reject conditions, advisory escape, continue-on-error, missing dependency install, wrong toolchain/command, missing needs edge, or required-context inflation."
  </behavior>
  <action>Add the independent job using the workflow's exact existing action pins. Set `MIX_ENV: test`; checkout; setup OTP/Elixir; setup Node; run `mix deps.get`; run `mix quality.governance`. Do not add path filters, `needs`, a job condition, continue-on-error, or advisory text. Add the job to `ci-success.needs` and preserve strict aggregation unchanged.

Add quality-governance to the non-advisory contexts registry with command and roll-up purpose; leave required_contexts exactly one element. Extend `RequiredChecksContractTest` with parsed job-map and ci-success-needs assertions, weakening mutations, exact pins/toolchains/install/command, registry count, and the existing exact unchanged `ci.fast` list. Do not modify Mix aliases or any Plan 03-owned file.</action>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Governance is an always-running fail-closed member of the sole ci-success required context, with topology enforced locally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminalize VALIDATION, UAT, and summary after CI proof</name>
  <files>.planning/phases/132-quality-baseline-triage/132-VALIDATION.md, .planning/phases/132-quality-baseline-triage/132-02-SUMMARY.md, .planning/phases/132-quality-baseline-triage/132-UAT.md</files>
  <read_first>
    - `132-VALIDATION.md` — draft/pending/manual-only state and missing Plan 03/04 rows.
    - `132-UAT.md` — four pending human judgments to convert only after Plan 03 passes.
    - `132-02-SUMMARY.md` — AUDIT-04 `human_judgment: true` field.
    - `132-03-SUMMARY.md` — evidence that parser, fixtures, consumers, and plan contracts passed.
    - `132-VERIFICATION.md` — read-only stale verifier report; never edit in this task.
    - `.github/workflows/ci.yml`, `priv/guardrails/required_status_checks.json`, and `test/guardrails/required_checks_contract_test.exs` — Task 1 outputs whose topology proof must pass before its VALIDATION row becomes green.
  </read_first>
  <behavior>
    - "VALIDATION frontmatter is terminal: validated/true/true; every 132-01..04 task has a real automated command and green status."
    - "Plan 04 Task 1 is marked green only after `mix test test/guardrails/required_checks_contract_test.exs` passes against the committed CI/registry topology; no planned or inferred status is accepted."
    - "Manual-Only Verifications is replaced by Advisory and Explicit Deferral: optional remote/human evidence names its authority and cannot satisfy/block completion; a genuinely unautomatable objective claim is unsupported/deferred with reason and trigger."
    - "All four UAT items cite exact automated tests, have `result: passed`, no current/awaiting/pending item, and summary totals show four passed, zero pending/blocked/skipped."
    - "AUDIT-04 summary coverage becomes `human_judgment: false` and cites the matrix, Decision basis, authority, and metric-only mutations."
  </behavior>
  <action>First rerun Plan 03's focused contract and Node fixture suite, then run `mix test test/guardrails/required_checks_contract_test.exs` against Task 1's implemented workflow and registry. Stop without changing VALIDATION/UAT/SUMMARY if any command fails. Only after all three commands are green, update the owned artifacts.

In `132-VALIDATION.md`, retain existing historical task rows but mark their actual automated evidence green; add rows for both Plan 03 tasks and both Plan 04 tasks with commands, threats, and file status. Record Plan 04 Task 1 green specifically from the just-passed topology test, not from file presence or plan intent. Give Plan 04 Task 2 its exact focused-plus-staged automated command and complete the row as part of this terminal artifact update; the task-level `<verify>` immediately below must execute that command successfully before the task commit is accepted. Replace manual-only/pending prose and manual sign-off with advisory/explicit-deferral semantics. Set `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`; make sign-off terminal and automated with no pending checkbox or human-required completion statement.

Convert the four UAT records in place to automated passes with exact references: classification matrix/Decision basis, unavailable authority mutations, precise consumer inventory, and metric-only repair/closure mutations. Remove awaiting/current-test state and set terminal totals. In `132-02-SUMMARY.md`, change only AUDIT-04 coverage to `human_judgment: false`, remove the human-only rationale, and cite the same executable evidence. Do not change Phase 132 identities/dispositions, invent remote results, or edit `132-VERIFICATION.md`.

Then run the bounded staging command exactly: `node scripts/quality_governance.cjs --check-active --allow-stale-verification-sha 1ade3d2f9e772ff2871253c435662fb33b146d13f61c54d8422e2ec7d13b2dfd --allow-stale-verification-source-commit be640780df7852387b493b392b7eb148308ea01b`. It must pass only because PLAN/SUMMARY/UAT/VALIDATION and CI topology are now clean and the sole unchanged verifier report matches both immutable identities. If any other artifact is exempted or any owned blocker remains, stop.</action>
  <verify>
    <automated>mix quality.baseline &amp;&amp; node --test scripts/quality_governance.cjs &amp;&amp; mix test test/guardrails/required_checks_contract_test.exs &amp;&amp; node scripts/quality_governance.cjs --check-active --allow-stale-verification-sha 1ade3d2f9e772ff2871253c435662fb33b146d13f61c54d8422e2ec7d13b2dfd --allow-stale-verification-source-commit be640780df7852387b493b392b7eb148308ea01b</automated>
  </verify>
  <done>CI topology is proven before its row is marked green; every executor-owned planning artifact is terminal and automated, leaving only the exact verifier-owned report for standard regeneration.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-132-G06 | Repudiation | VALIDATION/UAT/SUMMARY closure | high | mitigate | Exact commands and terminal counts/statuses; staging rejects any remaining blocker. |
| T-132-G07 | Tampering | staged-to-full handoff | high | mitigate | Exact stale identities, verifier-owned rewrite, then unexceptioned full command. |
| T-132-G08 | Denial of Service / Tampering | quality-governance CI job | high | mitigate | Independent always-running job in strict ci-success needs; parsed mutation tests. |
| T-132-G09 | Spoofing | required-context registry | high | mitigate | required_contexts stays exactly ci-success; new job registered as roll-up member. |
| T-132-SC | Tampering | package installs | low | accept | No dependency additions; CI installs existing lock only. |
</threat_model>

<post_execution_verification>
After both tasks are committed, invoke the standard GSD phase verifier. It must regenerate `.planning/phases/132-quality-baseline-triage/132-VERIFICATION.md`; executor tasks must not edit it. Confirm the report bytes and Git attribution no longer match the staged stale marker, its status is terminal/automated, and it contains neither `human_needed` nor `human_verification` entries. Then run unexceptioned `mix quality.governance`. The stale flags are forbidden in this final step.
</post_execution_verification>

<verification>
Execute in order after verifier regeneration:

1. `mix quality.governance`
2. `node --test scripts/quality_governance.cjs`
3. `mix test test/guardrails/required_checks_contract_test.exs`
4. `mix ci.fast`
5. `mix ci.proofs`
6. `mix format --check-formatted`
7. `mix credo --strict`
8. `mix dialyzer`

Hash `.planning/quality/baselines/132-initial.json` before/after and require equality. Confirm no `lib/`, package list, dependency, public API manifest, rendered artifact, or catalog output changed. A real remote CI result is supplementary if available; no live GitHub access or nonexistent CI-monitor script is required.
</verification>

<success_criteria>
- VALIDATION is terminal Nyquist-compliant and covers all four plans with no pending/manual-only completion semantics.
- UAT has four automated passes and SUMMARY has no blocking judgment.
- Staged governance passes only with the exact old verifier marker after all executor-owned blockers are gone.
- Standard verifier rewrites the report; unexceptioned full governance passes with no human-needed/human-verification state.
- Always-running governance CI rolls into sole required ci-success and all local gates pass.
- No runtime/package/API/rendered-byte/catalog/dependency changes.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature / requirement | Status | Notes |
|---|---|---|---|---|
| GOAL | SC-1..SC-4 | Reproducibility, complete ledger, risk routing, compatibility freeze | COVERED | Terminal evidence and required governance. |
| REQ | AUDIT-01..04 | All Phase 132 audit requirements | COVERED | Automated UAT/SUMMARY/VALIDATION and CI. |
| RESEARCH | R-01..R-06 | Authority separation, existing stack, focused contracts, no hand-roll | COVERED | Preserved through staged/full validation and CI. |
| CONTEXT | D-01..D-24 | All locked decisions | COVERED | No disposition/scope change; automated closure only. |
| GAP | VALIDATION | Draft/manual/pending Nyquist state | COVERED | Terminal Plan 03/04 map and explicit advisory/deferral. |
| GAP | UAT/SUMMARY | Four pending judgments and human_judgment | COVERED | Automated exact evidence. |
| GAP | Bootstrap | Verifier rewrite ordering | COVERED | Staged exact exception then full no-exception gate. |
| GAP | CI | Independent mandatory governance | COVERED | ci-success roll-up, sole context. |
| EXCLUSION | — | Product/package/API/bytes/catalog/dependencies | EXCLUDED | No task changes them. |

<output>Create `.planning/phases/132-quality-baseline-triage/132-04-SUMMARY.md` when done.</output>
