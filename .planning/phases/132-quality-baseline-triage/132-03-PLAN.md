---
phase: 132-quality-baseline-triage
plan: "03"
type: execute
wave: 2
depends_on:
  - 132-01
  - 132-02
files_modified:
  - mix.exs
  - scripts/quality_governance.cjs
  - test/quality/fixtures/governance-violation.json
  - test/quality/fixtures/governance-clean.json
  - prompts/rendro-oss-dna.md
  - test/quality/baseline_ledger_contract_test.exs
  - .planning/QUALITY.md
  - .planning/phases/132-quality-baseline-triage/132-01-PLAN.md
  - .planning/phases/132-quality-baseline-triage/132-02-PLAN.md
  - AGENTS.md
autonomous: true
gap_closure: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04]
must_haves:
  truths:
    - "D-01: through D-04: one `mix quality.governance` maintenance command runs the default-excluded ledger contract and the Node governance bridge without changing `mix ci.fast`, package/runtime state, or ordinary tests."
    - "D-05: through D-09: bounded QL/NS record parsing pins the exact approved Phase 132 classification matrix and rejects malformed boundaries, duplicate identity, and misplaced signals."
    - "D-10: through D-14: fixture execution and staged governance are read-only, exact unavailable semantics prevent authority inflation, and `132-initial.json` remains immutable."
    - "D-15: through D-22: every record has a Decision basis enum, nonempty record-local EV/SIG sets, exact disposition facts, and no metric-only or unavailable observation can authorize repair or closure."
    - "D-23: and D-24: project guidance uses concrete deterministic, advisory, and explicit-deferral completion vocabulary with truthful limits and next actions."
    - "PROH-132-01, PROH-132-02, and PROH-132-03 each have flat Node-test descriptors and independently prove violation RED plus clean GREEN."
    - "Active phases 132 onward are scanned by artifact role; archived milestones remain immutable, operational ledger consumers are allowlisted precisely, and full governance has no stale-report exception."
  artifacts:
    - path: "scripts/quality_governance.cjs"
      provides: "Shell-free Node fixture bridge, active-artifact scanner, and bounded stale-verifier staging mode"
      contains: "spawnSync"
    - path: "test/quality/baseline_ledger_contract_test.exs"
      provides: "Bounded ledger matrix, evidence, authority, and consumer contract"
      contains: "Rendro.Quality.BaselineLedgerContractTest"
    - path: ".planning/QUALITY.md"
      provides: "Exact approved classifications with Decision basis on every QL/NS record"
      contains: "Decision basis"
    - path: "test/quality/fixtures/governance-violation.json"
      provides: "Closed prohibited-state cases for all three prohibitions"
      contains: "PROH-132-01"
    - path: "test/quality/fixtures/governance-clean.json"
      provides: "Closed allowed-state cases for all three prohibitions"
      contains: "PROH-132-01"
  key_links:
    - from: "mix.exs"
      to: "scripts/quality_governance.cjs"
      via: "`quality.governance` runs quality.baseline then the unexceptioned active scan"
      pattern: "quality\\.governance"
    - from: "scripts/quality_governance.cjs"
      to: "mix.exs"
      via: "fixture runner invokes `mix quality.baseline` with argv list and shell disabled"
      pattern: "quality\\.baseline"
    - from: ".planning/phases/132-quality-baseline-triage/132-01-PLAN.md"
      to: "scripts/quality_governance.cjs"
      via: "flat prohibition descriptors name the Node target and both fixtures"
      pattern: "check_kind: node-test"
---

<objective>
Build the deterministic governance engine that closes Phase 132's ledger, prohibition, consumer, and active-planning contract gaps.

Purpose: Convert WR-02, WR-03, and PROH-132-01..03 from broad or human-backed assertions into bounded executable contracts while preserving every D-01..D-24 ledger decision.
Output: the single governance command, Node bad/clean bridge, exact ledger parser/mutations, active-artifact scanner, executable prohibition descriptors, and project completion policy.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/QUALITY.md
@.planning/phases/132-quality-baseline-triage/132-CONTEXT.md
@.planning/phases/132-quality-baseline-triage/132-RESEARCH.md
@.planning/phases/132-quality-baseline-triage/132-REVIEW.md
@.planning/phases/132-quality-baseline-triage/132-VERIFICATION.md
@.planning/phases/132-quality-baseline-triage/132-UAT.md
@.planning/phases/132-quality-baseline-triage/132-VALIDATION.md
@.planning/phases/132-quality-baseline-triage/132-01-PLAN.md
@.planning/phases/132-quality-baseline-triage/132-02-PLAN.md
@.planning/quality/baselines/132-initial.json
@.planning/quality/schema/baseline-v1.schema.json
@test/quality/baseline_ledger_contract_test.exs
@mix.exs
@test/test_helper.exs

<interfaces>
- `mix quality.baseline` is the existing explicit tag-including maintenance alias; `:quality_ledger_contract` stays excluded by default.
- `mix ci.fast` is an exact ordered contract and is not extended by this plan.
- The pre-Plan-03 verifier-owned report is exactly `.planning/phases/132-quality-baseline-triage/132-VERIFICATION.md`, SHA-256 `1ade3d2f9e772ff2871253c435662fb33b146d13f61c54d8422e2ec7d13b2dfd`, last committed by `be640780df7852387b493b392b7eb148308ea01b`.
- The stale-report exception is available only to the direct Node staging command with both exact identities; `mix quality.governance` never supplies it.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace command to bad/clean governance exits</name>
  <reversibility rating="costly">The command and descriptor target become CI/GSD policy; renaming later requires coordinated planning and workflow edits but no runtime migration.</reversibility>
  <files>mix.exs, scripts/quality_governance.cjs, test/quality/fixtures/governance-violation.json, test/quality/fixtures/governance-clean.json, prompts/rendro-oss-dna.md</files>
  <read_first>
    - `mix.exs` — existing aliases and exact `ci.fast` ordering.
    - `test/test_helper.exs` — default quality tag exclusion, read-only in this plan.
    - `132-VERIFICATION.md` — verifier-owned stale input whose exact identities are pinned above.
    - `132-UAT.md`, `132-VALIDATION.md`, and `132-02-SUMMARY.md` — active artifact roles the scanner must reject until Plan 04 closes them.
  </read_first>
  <behavior>
    - "Each PROH case first runs `mix quality.baseline` via `spawnSync('mix', ['quality.baseline'], {shell: false, cwd, env})`, then validates its closed virtual artifacts; bad exits nonzero and paired clean exits zero."
    - "Real active scanning considers only direct `.planning/phases/<numeric>-*` children with phase >=132 and never scans or rewrites `.planning/milestones/**`."
    - "Role parsers reject `human_judgment: true`, `<human-check>`, human-verification checkpoints, unresolved blocking backstops, descriptor-less prohibitions, pending/awaiting UAT, `human_needed`, and pending/manual-only VALIDATION sign-off. Advisory feedback and explicit deferral are non-blocking."
    - "Staging accepts only the exact verifier path + SHA-256 + source commit in `<interfaces>`; any content/path/commit mismatch fails. All other artifacts are checked normally. Unexceptioned full mode rejects that stale report."
  </behavior>
  <action>Create `scripts/quality_governance.cjs` with Node built-ins only: `node:test`, strict assert, fs/path/url/crypto, and child-process `spawnSync`. Do not interpolate a shell command. Define closed bad/clean JSON manifests with keyed PROH-132-01/02/03 cases; reject absolute/traversal paths, unknown keys, duplicates, missing pairs, and snapshot write/override fields. Bad cases cover local/advisory authority inflation, an unapproved product/package/release/ordinary-test ledger consumer, and metric-only repair/closure authority; clean cases cover exact unavailable semantics, approved maintenance consumers, and evidence-gated qualitative decisions.

Implement `--check-active` and the staging-only pair `--allow-stale-verification-sha 1ade3d2f9e772ff2871253c435662fb33b146d13f61c54d8422e2ec7d13b2dfd --allow-stale-verification-source-commit be640780df7852387b493b392b7eb148308ea01b`. Validate that Git attributes the unchanged report to that commit and its bytes hash exactly; do not accept an arbitrary caller-provided hash, any other report, or more than one exception. Even in staging, reject every plan/summary/UAT/VALIDATION blocker. Without both flags, reject `human_needed` and `human_verification` normally. `mix quality.governance` runs `quality.baseline` and then `node scripts/quality_governance.cjs --check-active` with no exception. Add the test environment preference, keep `quality.baseline`, `ci.fast`, default exclusion, deps, package, and product files unchanged.

Update `prompts/rendro-oss-dna.md`: completion coverage is `deterministic | advisory | explicit_deferral`; optional human feedback can enrich evidence but cannot block; a genuinely unautomatable objective claim is explicitly unsupported/deferred with a reason. Preserve D-13 human-review as evidence authority, not completion coverage.</action>
  <verify>
    <automated>snapshot_hash=$(shasum -a 256 .planning/quality/baselines/132-initial.json | awk '{print $1}') &amp;&amp; node --test scripts/quality_governance.cjs &amp;&amp; mix quality.baseline &amp;&amp; test "$snapshot_hash" = "$(shasum -a 256 .planning/quality/baselines/132-initial.json | awk '{print $1}')"</automated>
  </verify>
  <done>The tracer proves shell-free Mix invocation and independent bad/clean exits, while full active governance remains fail-closed until Plan 04 removes implementation-owned blockers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact ledger, consumer, prohibition, and plan policy</name>
  <files>test/quality/baseline_ledger_contract_test.exs, .planning/QUALITY.md, .planning/phases/132-quality-baseline-triage/132-01-PLAN.md, .planning/phases/132-quality-baseline-triage/132-02-PLAN.md, AGENTS.md</files>
  <read_first>
    - `baseline_ledger_contract_test.exs` — confirmed WR-02 and WR-03 seams.
    - `QUALITY.md` — QL-001..004 and NS-001..007 records to preserve and pin.
    - `132-01-PLAN.md` — descriptor-less prohibitions to amend.
    - `132-02-PLAN.md` — blocking human check to replace with automated evidence.
    - `AGENTS.md` — project completion-policy convention.
  </read_first>
  <behavior>
    - "QL blocks start at `#### QL-NNN` and end at the next QL peer or explicit NS section; NS blocks start at `### NS-NNN` and end at the next NS peer or lifecycle section. Ambiguous, nested, duplicate, misplaced, or malformed structure fails closed."
    - "The exact QL-001..004 / NS-001..007 matrix pins each record's EVs, SIGs, priority, disposition, owner/status/trigger requirements, and Decision basis. Every EV/SIG set is nonempty; each EV exists; each signal's source EV belongs to that same record."
    - "Decision basis is `supported_contract_risk | bounded_maintenance_cost | diagnostic_signal_only | explicit_unavailability`; diagnostic/unavailability bases cannot authorize repair or closure."
    - "Unavailable means exactly unavailable status plus reason and rerun trigger, never primary-CI authority or a pass/fail/zero surrogate; non-unavailable records cannot carry unavailable-only fields."
    - "Consumer inventory parses known Mix alias, workflow job, ExUnit maintenance, product/package/release/ordinary-test roles and accepts only the explicit maintenance command, Node bridge, governance CI job, and focused contract."
  </behavior>
  <action>Replace global signal scanning and vacuous evidence loops with bounded record helpers. Add in-memory mutations for authority inflation, consumer leak, metric-only authority, misplaced/duplicate signal, empty evidence, unknown evidence, and a globally resolvable EV absent from its record. Pin the exact approved matrix and add `Decision basis` to each QL/NS record without changing identity, evidence, disposition, owner, or D-05..D-22 lifecycle/routing. Keep prose human-first per D-01..D-04 and D-23/D-24; do not build a Markdown database.

Implement the precise consumer allowlist without a raw repository-wide substring ban. Amend only the approved historical seams: in `132-01-PLAN.md`, set each PROH status resolved with flat `verification: test`, `check_kind: node-test`, `check_target: scripts/quality_governance.cjs`, `check_violation_fixture: test/quality/fixtures/governance-violation.json`, and `check_clean_fixture: test/quality/fixtures/governance-clean.json`; remove null/flagged metadata. In `132-02-PLAN.md`, replace `<human-check>` and blocking manual completion language with the named mutations, leaving optional human feedback advisory. Update `AGENTS.md` to match the deterministic/advisory/explicit-deferral completion contract. Do not edit SUMMARY, UAT, VALIDATION, VERIFICATION, CI, or guardrail files; Plan 04 owns those.</action>
  <verify>
    <automated>mix quality.baseline &amp;&amp; node --test scripts/quality_governance.cjs &amp;&amp; mix format --check-formatted mix.exs test/quality/baseline_ledger_contract_test.exs</automated>
  </verify>
  <done>WR-02/03, all prohibitions, ledger authority, consumer scope, and plan-level blocking backstops are deterministically governed; Plan 04 can close the remaining active artifacts.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-132-G01 | Tampering / Elevation | Node fixture/process bridge | high | mitigate | Closed manifests, list-form argv, shell false, bounded cwd/env, snapshot hashing, and path rejection. |
| T-132-G02 | Tampering | stale verifier exception | high | mitigate | One exact path/hash/source-commit triple, staging CLI only; full Mix command carries no exception. |
| T-132-G03 | Spoofing | evidence authority / Decision basis | high | mitigate | Exact matrix, unavailable combinations, basis eligibility, and authority/metric mutations. |
| T-132-G04 | Elevation | ledger consumers | high | mitigate | Role-scoped inventory and exact maintenance allowlist. |
| T-132-G05 | Tampering | Markdown/planning parsing | high | mitigate | Peer-boundary parsing and malformed/misplaced/duplicate mutations. |
| T-132-SC | Tampering | package installs | low | accept | No installs or dependency changes; Node built-ins and existing Elixir deps only. |
</threat_model>

<verification>
- `mix quality.baseline`
- `node --test scripts/quality_governance.cjs`
- Snapshot SHA-256 identical before/after.
- Confirm `mix ci.fast`, default tag exclusions, deps, package list, `lib/`, public API, rendered artifacts, and catalog are unchanged.
- Do not run full `mix quality.governance` yet: its correct result remains nonzero until Plan 04 and post-execution verifier regeneration complete.
</verification>

<success_criteria>
- One command exists and full mode is unexceptioned.
- Staging exception is immutable, singular, verifier-only, and still rejects every implementation-owned blocker.
- Exact QL/NS matrix, Decision basis, EV/SIG resolution, unavailable semantics, consumers, and mutations pass.
- All three prohibitions have executable flat bad/clean descriptors.
- No runtime/package/API/rendered-byte/catalog/dependency change.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature / requirement | Status | Notes |
|---|---|---|---|---|
| GOAL | SC-1..SC-4 | Reproducible ledger, complete findings, risk routing, compatibility freeze | COVERED | Exact parser, mutations, consumers, immutable snapshot. |
| REQ | AUDIT-01..04 | All Phase 132 audit requirements | COVERED | Command, matrix, evidence and disposition governance. |
| RESEARCH | R-01..R-06 | Human ledger + normalized facts, existing stack, authority lanes, no hand-roll, routes, focused mutation contract | COVERED | Uses current seams and no new dependency. |
| CONTEXT | D-01..D-24 | All locked ledger/evidence/governance/JTBD decisions | COVERED | Cited in truths and task actions; no decision changed. |
| GAP | WR-02, WR-03 | Record-bound signals and non-vacuous evidence | COVERED | Bounded parser and mutations. |
| GAP | PROH-132-01..03 | Authority, consumer, metric-only prohibitions | COVERED | Flat Node bad/clean descriptors. |
| GAP | Bootstrap | Stale verifier ordering | COVERED | Exact staging exception; full mode unexceptioned. |
| EXCLUSION | — | Product/package/API/bytes/catalog/dependencies | EXCLUDED | No task changes them. |

<output>Create `.planning/phases/132-quality-baseline-triage/132-03-SUMMARY.md` when done.</output>
