---
phase: 69
plan: 02
type: execute
wave: 2
depends_on:
  - 69-01
files_modified:
  - test/fixtures/forms_support_fixture.pdf
  - priv/viewer_evidence/forms/apple_preview.md
  - priv/support_matrix.json
autonomous: false
requirements:
  - RECIPE-01
must_haves:
  truths:
    - Committed fixture `test/fixtures/forms_support_fixture.pdf` exists and is referenced by evidence frontmatter `fixture:` key.
    - Canonical evidence file `priv/viewer_evidence/forms/apple_preview.md` validates via `Validator.validate_evidence_file/3` and `mix rendro.viewer_evidence validate`.
    - Matrix row `forms.viewers.apple_preview` gains additive `evidence`, `recorded_at`, `viewer_kind: "manual"` — `status` and `proof[]` unchanged.
    - `recorded_at` in matrix equals `recorded_at` in evidence frontmatter (re-validation date, not Phase 47 backdate).
    - All four `proof[]` behavior IDs present in evidence `behaviors[]` with substantive widget-specific notes.
    - `mix rendro.viewer_evidence list` shows `forms/apple_preview` without legacy "missing promotion-complete" note.
  artifacts:
    - path: test/fixtures/forms_support_fixture.pdf
      provides: "Deterministic forms viewer proof fixture (committed binary)"
    - path: priv/viewer_evidence/forms/apple_preview.md
      provides: "Canonical evidence for forms × Apple Preview (RECIPE-01)"
    - path: priv/support_matrix.json
      provides: "Additive promotion keys on forms.viewers.apple_preview only"
  key_links:
    - from: priv/support_matrix.json
      to: priv/viewer_evidence/forms/apple_preview.md
      via: "evidence: priv/viewer_evidence/forms/apple_preview.md"
    - from: priv/viewer_evidence/forms/apple_preview.md
      to: test/fixtures/forms_support_fixture.pdf
      via: "frontmatter fixture: test/fixtures/forms_support_fixture.pdf"
---

<objective>
Walk the first real cell — **forms × Apple Preview** — through fixture → evidence file → matrix promotion.

Purpose: Deliver RECIPE-01 — canonical evidence home with schema-valid frontmatter and prose body, smoke-testing the operator recipe from 69-01 on a re-attestation consolidation (v1.8 Phase 47, status unchanged).
Output: Committed fixture PDF, evidence file, matrix promotion fields; docs-contract `run_full` validates referenced evidence; forms row legacy warning cleared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-RESEARCH.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-PATTERNS.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VALIDATION.md
@guides/viewer_evidence.md
@test/support/form_support_fixture.ex
@priv/viewer_evidence/_template.md
@priv/support_matrix.json
@lib/rendro/viewer_evidence/validator.ex
</context>

<interfaces>
From `Rendro.Test.FormSupportFixture` (widget inventory):
```elixir
# email: text, prefilled "jon@example.test"
# terms: checkbox, checked: true
# contact_email / contact_phone: radio group "contact", email selected
Rendro.Test.FormSupportFixture.write_fixture("test/fixtures/forms_support_fixture.pdf")
```

Target matrix row after promotion (69-CONTEXT D-13):
```json
"apple_preview": {
  "status": "supported",
  "proof": ["open", "default_state_visible", "edit_or_toggle", "save"],
  "evidence": "priv/viewer_evidence/forms/apple_preview.md",
  "recorded_at": "YYYY-MM-DD",
  "viewer_kind": "manual"
}
```

Manual checklist (matrix proof[] → operator actions):
| Behavior ID | Operator action |
|-------------|-----------------|
| `open` | Open fixture without error dialog |
| `default_state_visible` | `email` shows `jon@example.test`; `terms` checked; `contact_email` selected |
| `edit_or_toggle` | Change email; toggle terms; switch radio to phone |
| `save` | Save As; reopen; edited state persists |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit forms support fixture PDF</name>
  <files>test/fixtures/forms_support_fixture.pdf</files>
  <read_first>
    - test/support/form_support_fixture.ex
    - test/support/embedded_artifact_support_fixture.ex (committed-PDF precedent)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-09
  </read_first>
  <acceptance_criteria>
    - `test/fixtures/forms_support_fixture.pdf` exists and starts with `%PDF` magic bytes.
    - Regeneration command documented for evidence body: `Rendro.Test.FormSupportFixture.write_fixture("test/fixtures/forms_support_fixture.pdf")`.
    - File is deterministic (same bytes on re-run from clean compile).
    - `file test/fixtures/forms_support_fixture.pdf` reports PDF document data.
  </acceptance_criteria>
  <action>
    Run `mix run -e 'Rendro.Test.FormSupportFixture.write_fixture("test/fixtures/forms_support_fixture.pdf")'`.
    Commit the generated binary to `test/fixtures/forms_support_fixture.pdf`.
    Do not use `fixture_sha256` alone for the canonical first cell (D-09).
  </action>
  <verify>
    <automated>test -f test/fixtures/forms_support_fixture.pdf && head -c 4 test/fixtures/forms_support_fixture.pdf | grep -q '%PDF'</automated>
  </verify>
  <done>Committed forms fixture PDF available for manual viewer checklist.</done>
</task>

<task type="checkpoint" gate="human">
  <name>Task 2: Manual Apple Preview spot-check (re-attestation)</name>
  <files>test/fixtures/forms_support_fixture.pdf</files>
  <read_first>
    - guides/viewer_evidence.md (Quick-start step 4 + Appendix A forms checklist)
    - priv/support_matrix.json (`forms.viewers.apple_preview.proof[]`)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-07, D-08, D-10, D-12
  </read_first>
  <acceptance_criteria>
    - All four behaviors pass in Apple Preview against `test/fixtures/forms_support_fixture.pdf`.
    - `viewer_version` captured from Preview → About (not copied from protection row or other evidence).
    - `platform` captured as macOS version string at observation time (e.g. `"macOS 15.x (arm64)"`).
    - `recorded_at` set to **today's re-validation date** (ISO `YYYY-MM-DD`) — not backdated to Phase 47 date `2026-05-05`.
    - Each behavior has a substantive note referencing `email`, `terms`, `contact_email`/`contact_phone` widgets (no template stubs).
  </acceptance_criteria>
  <action>
    Open `test/fixtures/forms_support_fixture.pdf` in Apple Preview on macOS.
    Execute checklist: open → default_state_visible → edit_or_toggle → save (Save As to new path, reopen, confirm persistence).
    Record `viewer_version`, `platform`, `recorded_at`, and four behavior notes for Task 3 frontmatter.
    Phase 47 original attestation date `2026-05-05` goes in evidence **body prose only** (D-08).
  </action>
  <verify>
    <automated>echo "Manual checkpoint — operator confirms four behaviors pass and observation values captured"</automated>
  </verify>
  <done>Spot-check complete; observation values ready for evidence frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Create canonical evidence file</name>
  <files>priv/viewer_evidence/forms/apple_preview.md</files>
  <read_first>
    - priv/viewer_evidence/_template.md
    - priv/schemas/viewer_evidence.schema.json
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-10, D-11, D-14
    - lib/rendro/viewer_evidence/lint.ex (body rejection rules)
  </read_first>
  <acceptance_criteria>
    - File `priv/viewer_evidence/forms/apple_preview.md` exists with YAML frontmatter: `schema_version: 1`, `surface: forms`, `viewer: apple_preview`, `viewer_version`, `platform`, `recorded_at`, `fixture: "test/fixtures/forms_support_fixture.pdf"`.
    - `behaviors[]` contains exactly four entries with `behavior` IDs `open`, `default_state_visible`, `edit_or_toggle`, `save`; each has `result: pass` and `note` minLength ≥ 1 with widget-specific prose.
    - Body includes: (1) provenance paragraph citing v1.8 Phase 47 original date **2026-05-05**, (2) regen command for `FormSupportFixture.write_fixture/1`, (3) boundary note (Poppler ≠ viewer proof; does not promote other viewers/surfaces).
    - No forbidden content: no `-----BEGIN`, no `![`, no `/Users/`, no `passphrase:` assignments.
    - `grep -c 'behavior: open' priv/viewer_evidence/forms/apple_preview.md` returns **1**.
    - File byte size ≤ 65536.
  </acceptance_criteria>
  <action>
    Create directory `priv/viewer_evidence/forms/` if needed.
    Copy from `_template.md`; set `viewer: apple_preview`, observation values from Task 2 checkpoint.
    Write substantive behavior notes referencing `email` prefilled `jon@example.test`, `terms` checkbox, `contact_email`/`contact_phone` radio group.
    Body prose per D-11; do not put `status` or `viewer_kind` in frontmatter.
  </action>
  <verify>
    <automated>test -f priv/viewer_evidence/forms/apple_preview.md && mix run -e 'proof = ["open","default_state_visible","edit_or_toggle","save"]; :ok = Rendro.ViewerEvidence.Validator.validate_evidence_file("priv/viewer_evidence/forms/apple_preview.md", proof)'</automated>
  </verify>
  <done>Evidence file validates against schema, path alignment, and body lint.</done>
</task>

<task type="auto">
  <name>Task 4: Promote matrix row (additive fields only)</name>
  <files>priv/support_matrix.json</files>
  <read_first>
    - priv/support_matrix.json (forms.viewers.apple_preview — lines 38–46)
    - priv/viewer_evidence/forms/apple_preview.md (recorded_at value)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-13
    - test/docs_contract/forms_claims_test.exs (matrix regex guards)
  </read_first>
  <acceptance_criteria>
    - `forms.viewers.apple_preview` gains `"evidence": "priv/viewer_evidence/forms/apple_preview.md"`, `"recorded_at": "&lt;same ISO date as evidence frontmatter&gt;"`, `"viewer_kind": "manual"`.
    - `status` remains `"supported"`; `proof[]` unchanged as four IDs.
    - No other matrix rows modified (`git diff priv/support_matrix.json` touches only `forms.viewers.apple_preview`).
    - `mix rendro.viewer_evidence list` output for `forms/apple_preview` has empty notes column (no legacy warning).
    - `mix rendro.viewer_evidence validate` exit 0; stderr still shows **4** legacy warnings (embedded_files × acrobat, links × acrobat, links × apple_preview, protection × apple_preview) — not forms/apple_preview.
    - `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes including `run_full succeeds on production matrix`.
  </acceptance_criteria>
  <action>
    Add three additive keys to `forms.viewers.apple_preview` in `priv/support_matrix.json`.
    Ensure `recorded_at` matches evidence frontmatter exactly.
    Do not modify `status`, `proof[]`, or any other viewer row.
  </action>
  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs && mix rendro.viewer_evidence validate 2>&1 | grep -c 'forms.viewers.apple_preview' | grep -q '^0$'</automated>
  </verify>
  <done>Matrix promotion complete; forms row legacy warning cleared; docs-contract green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Evidence Recording

| Boundary | Description |
|----------|-------------|
| Operator observation → Evidence file | Text-only Markdown; fixtures by repo path; no inline binaries or secrets. |
| Evidence file → CI validator | Lint scans body for PEM, home paths, images, operational-secret tokens before merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-02-01 | Information Disclosure | Evidence body/frontmatter | High | mitigate | `Lint.evidence_body/1` rejects `-----BEGIN`, `/Users/`, `passphrase:`, `private_key:`; use fictional email domain only; no Save As absolute paths in notes. |
| T-69-02-02 | Tampering | Matrix promotion | Medium | mitigate | Additive keys only; `status` and `proof[]` unchanged; docs-contract `run_full` validates referenced evidence file. |
| T-69-02-03 | Repudiation | Re-attestation dates | Medium | mitigate | `recorded_at` = re-validation date in both matrix and frontmatter; Phase 47 date body-only (no backdating). |
| T-69-02-04 | Denial of Service | Evidence file size | Low | accept | Byte budget 65536 enforced by validator lint. |

**Block gate:** T-69-02-01 is HIGH — mitigated by existing lint module + operator discipline; no merge if lint fails.
</threat_model>

<success_criteria>
- `test/fixtures/forms_support_fixture.pdf` committed and referenced.
- `priv/viewer_evidence/forms/apple_preview.md` validates via `mix rendro.viewer_evidence validate`.
- `forms.viewers.apple_preview` has `evidence`, `recorded_at`, `viewer_kind: "manual"`; status unchanged.
- `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes.
- Only `forms.viewers.apple_preview` modified in `priv/support_matrix.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-02-SUMMARY.md` via orchestration.
</output>
