---
phase: 69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - guides/viewer_evidence.md
  - lib/mix/tasks/rendro/viewer_evidence.ex
  - test/docs_contract/viewer_evidence_claims_test.exs
autonomous: true
requirements:
  - RECIPE-03
must_haves:
  truths:
    - `guides/viewer_evidence.md` exists and is registered under HexDocs **Policies** beside `guides/api_stability.md`.
    - Guide quick-start has 8 numbered steps, each ending with an observable check; appendices A–F present.
    - Guide states full repo checkout required for recording (Hex package omits `priv/support_matrix.json`, `priv/schemas/`, `priv/viewer_evidence/`).
    - Guide shows frontmatter skeleton only — no observation values duplicated from canonical evidence file.
    - `Mix.Tasks.Rendro.ViewerEvidence` `@moduledoc` and guide cross-link bidirectionally.
  artifacts:
    - path: guides/viewer_evidence.md
      provides: "Operator entry point — hybrid quick-start + appendices (RECIPE-03)"
    - path: mix.exs
      provides: "Policies-group registration for viewer_evidence guide"
  key_links:
    - from: guides/viewer_evidence.md
      to: lib/mix/tasks/rendro/viewer_evidence.ex
      via: "Appendix D mix task reference + quick-start commands"
    - from: lib/mix/tasks/rendro/viewer_evidence.ex
      to: guides/viewer_evidence.md
      via: "@moduledoc Human workflow guide link"
---

<objective>
Publish `guides/viewer_evidence.md` as the operator entry point and register it in HexDocs **Policies** group.

Purpose: Deliver RECIPE-03 — a second operator can follow the guide start-to-finish without asking questions. This plan ships the human mirror only; the worked cell (`forms × apple_preview`) lands in 69-02 and public-contract closure in 69-03.
Output: Guide file + `mix.exs` registration + optional docs-contract pointer asserts; `mix docs` builds with guide under Policies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-RESEARCH.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-PATTERNS.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VALIDATION.md
@guides/integrations.md
@guides/api_stability.md
@priv/viewer_evidence/_template.md
@lib/mix/tasks/rendro/viewer_evidence.ex
</context>

<interfaces>
From mix.exs (current Policies registration):
```elixir
extras: [
  "README.md",
  "guides/integrations.md",
  "guides/branding.md",
  "guides/api_stability.md"
],
groups_for_extras: [
  Guides: ["guides/branding.md", "guides/integrations.md"],
  Policies: ["guides/api_stability.md"]
]
```

From mix.exs `@source_url`:
```elixir
@source_url "https://github.com/szTheory/rendro"
```

Quick-start spine (69-CONTEXT D-04):
```
missing → confirm proof[] → prepare fixture → manual checklist →
create evidence from _template.md → validate → promote matrix row →
verify (list + docs-contract)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register guide in mix.exs Policies group</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (defp docs/0 — extras and groups_for_extras)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-PATTERNS.md §2.1
  </read_first>
  <acceptance_criteria>
    - `mix.exs` `extras:` includes `"guides/viewer_evidence.md"` after `"guides/api_stability.md"`.
    - `groups_for_extras` **Policies** list is `["guides/api_stability.md", "guides/viewer_evidence.md"]` (Guides group unchanged).
    - `grep -c 'guides/viewer_evidence.md' mix.exs` returns **2** (extras + Policies group).
    - No changes to `package/0` `files:`, `:ci` alias, or `skip_undefined_reference_warnings_on`.
  </acceptance_criteria>
  <action>
    Add `"guides/viewer_evidence.md"` to `extras:` and to the **Policies** key in `groups_for_extras` per 69-PATTERNS §2.1.
    Do not add to Guides group or modify Hex `files:` list.
  </action>
  <verify>
    <automated>grep -q 'guides/viewer_evidence.md' mix.exs && grep -c 'guides/viewer_evidence.md' mix.exs | grep -q '^2$'</automated>
  </verify>
  <done>Guide path registered in both extras and Policies group.</done>
</task>

<task type="auto">
  <name>Task 2: Create operator guide (hybrid quick-start + appendices)</name>
  <files>guides/viewer_evidence.md</files>
  <read_first>
    - guides/integrations.md (imperative tone)
    - guides/api_stability.md (honest boundary prose)
    - priv/viewer_evidence/_template.md (frontmatter field names)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-01–D-06, D-14–D-18
    - lib/mix/tasks/rendro/viewer_evidence.ex (subcommand exit codes)
  </read_first>
  <acceptance_criteria>
    - File `guides/viewer_evidence.md` exists with sections: **Purpose**, **Prerequisites**, **Status vocabulary**, **Quick-start** (8 numbered steps), **Worked example — forms × Apple Preview**, **Appendix A** through **Appendix F**.
    - Prerequisites states: full **repo checkout** required; Hex package omits `priv/support_matrix.json`, `priv/schemas/`, `priv/viewer_evidence/`; HexDocs is read-only documentation.
    - Quick-start step 1 command is `mix rendro.viewer_evidence missing`; step 6 is `mix rendro.viewer_evidence validate`; step 8 includes `mix test test/docs_contract/viewer_evidence_claims_test.exs`.
    - Quick-start step 5 names copy source `priv/viewer_evidence/_template.md` and target path rule `priv/viewer_evidence/&lt;surface&gt;/&lt;viewer&gt;.md`.
    - Worked example links canonical file via GitHub URL `https://github.com/szTheory/rendro/blob/main/priv/viewer_evidence/forms/apple_preview.md` (not relative `priv/` link).
    - Guide shows annotated frontmatter **skeleton** with field names (`schema_version`, `surface`, `viewer`, `viewer_version`, `platform`, `recorded_at`, `fixture`, `behaviors[]`) — no real observation values from the canonical evidence file.
    - Appendix B includes synthetic **hypothetical** `explicit_deferral` JSON for `signed_artifact × apple_preview` labeled non-production; forbidden vocabulary list includes `TBD`, `not yet`, `deferred for later`, empty string.
    - Appendix B contrast table covers `supported` vs `explicit_deferral` vs `unverified` matrix keys.
    - Appendix A forms checklist table lists four behavior IDs: `open`, `default_state_visible`, `edit_or_toggle`, `save`.
    - Appendix F states Poppler structural proof ≠ viewer proof; one cell ≠ other surfaces/viewers.
    - Quick-start body ≤ ~40% of total guide length (appendices carry checklist/deferral detail).
    - `grep -c '^## Appendix' guides/viewer_evidence.md` returns **6** (A–F).
  </acceptance_criteria>
  <action>
    Create `guides/viewer_evidence.md` following Option C hybrid (69-CONTEXT D-01).
    Voice: imperative operator tone matching `integrations.md` / `api_stability.md` ("Run…", "Record…", "Do not promote if…").
    Quick-start 8 steps with observable check after each:
      1. `mix rendro.viewer_evidence missing` — identify target cell.
      2. Read matrix `proof[]` for target cell in `priv/support_matrix.json`.
      3. Prepare/commit fixture (`Rendro.Test.FormSupportFixture.write_fixture("test/fixtures/forms_support_fixture.pdf")` for forms).
      4. Manual checklist in viewer (per-behavior pass/fail + notes).
      5. `cp priv/viewer_evidence/_template.md priv/viewer_evidence/&lt;surface&gt;/&lt;viewer&gt;.md` — fill frontmatter.
      6. `mix rendro.viewer_evidence validate`.
      7. Add `evidence`, `recorded_at`, `viewer_kind` to matrix row; update `guides/api_stability.md` + `CHANGELOG.md`.
      8. `mix rendro.viewer_evidence list` + `mix test test/docs_contract/viewer_evidence_claims_test.exs`.
    Worked example: GitHub link to `priv/viewer_evidence/forms/apple_preview.md`; optional short behaviors excerpt labeled "canonical file wins".
    Appendix C: byte budget 65536, forbidden frontmatter keys (`status`, `viewer_kind`), fixture path-or-hash rule.
    Appendix D: `list`/`missing`/`validate` exit codes; link to `Mix.Tasks.Rendro.ViewerEvidence`.
    Appendix E: docs-contract = structural; signing-live-proof / long-lived-live-proof lanes unchanged.
  </action>
  <verify>
    <automated>test -f guides/viewer_evidence.md && grep -q 'mix rendro.viewer_evidence missing' guides/viewer_evidence.md && grep -q 'priv/viewer_evidence/_template.md' guides/viewer_evidence.md && grep -q 'github.com/szTheory/rendro/blob/main/priv/viewer_evidence/forms/apple_preview.md' guides/viewer_evidence.md && grep -c '^## Appendix' guides/viewer_evidence.md | grep -q '^6$'</automated>
  </verify>
  <done>Full hybrid guide exists with 8-step quick-start, six appendices, skeleton frontmatter, GitHub canonical link.</done>
</task>

<task type="auto">
  <name>Task 3: Mix task back-link and optional docs-contract pointer test</name>
  <files>lib/mix/tasks/rendro/viewer_evidence.ex, test/docs_contract/viewer_evidence_claims_test.exs</files>
  <read_first>
    - lib/mix/tasks/rendro/viewer_evidence.ex (@moduledoc line 49)
    - test/docs_contract/viewer_evidence_claims_test.exs (production tier-A describe block)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-18
  </read_first>
  <acceptance_criteria>
    - `@moduledoc` contains `guides/viewer_evidence.md` without "(Phase 69)" stub suffix — final link text only.
    - Guide Appendix D references `Mix.Tasks.Rendro.ViewerEvidence` by module name.
    - New test `"viewer evidence guide references canonical template and worked example paths"` in `viewer_evidence_claims_test.exs` asserts guide contains `priv/viewer_evidence/_template.md` and `priv/viewer_evidence/forms/apple_preview.md`.
    - `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes (production matrix unchanged — no evidence file yet).
    - `mix docs` completes without error (guide builds under Policies).
  </acceptance_criteria>
  <action>
    Update `@moduledoc` line 49 from `Human workflow guide: \`guides/viewer_evidence.md\` (Phase 69).` to `Human workflow guide: \`guides/viewer_evidence.md\`.`.
    Add docs-contract test per 69-CONTEXT D-18 in the `production tier-A artifacts` describe block.
  </action>
  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs && mix docs --quiet</automated>
  </verify>
  <done>Bidirectional guide ↔ mix task links verified; pointer test green; ExDoc build succeeds.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Operator Guide Publication

| Boundary | Description |
|----------|-------------|
| Guide author → Repo | Markdown guide only; no secrets, credentials, or home-directory paths in examples. |
| Guide → HexDocs reader | Read-only documentation; prerequisites block Hex-only recording attempts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01-01 | Information Disclosure | Guide examples | Medium | mitigate | Use fictional fixture email domain (`jon@example.test`); no PEM blocks, passphrases, or `/Users/` paths in guide prose or skeleton frontmatter. |
| T-69-01-02 | Tampering | mix.exs registration | Low | mitigate | Register guide in Policies group only; do not expand `package/0` `files:` to ship `priv/` artifacts without explicit release decision. |
| T-69-01-03 | Spoofing | Worked example link | Low | accept | GitHub `source_url` link to canonical evidence path; relative `priv/` links forbidden (404 on HexDocs). |
| T-69-01-04 | Denial of Service | ExDoc build | Low | accept | Guide is text-only Markdown; no embedded binaries or oversized assets. |

**Block gate:** No HIGH severity threats without mitigation. T-69-01-01 mitigated by lint-aligned example discipline.
</threat_model>

<success_criteria>
- `grep -c 'guides/viewer_evidence.md' mix.exs` returns 2.
- `guides/viewer_evidence.md` exists with 8 quick-start steps and appendices A–F.
- `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes.
- `mix docs` builds; guide appears under Policies group.
</success_criteria>

<output>
After completion, create `.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-01-SUMMARY.md` via orchestration.
</output>
