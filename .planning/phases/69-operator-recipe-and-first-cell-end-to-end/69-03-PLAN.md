---
phase: 69
plan: 03
type: execute
wave: 3
depends_on:
  - 69-02
files_modified:
  - guides/api_stability.md
  - CHANGELOG.md
autonomous: true
requirements:
  - RECIPE-05
must_haves:
  truths:
    - `guides/api_stability.md` contains `## Viewer Evidence and CHANGELOG Discipline` section documenting promotion/deferral/re-home CHANGELOG obligations.
    - Interactive Forms Apple Preview sentence uses STACK.md template with `viewer_version`, `platform`, evidence path, and four proof behavior IDs from canonical evidence file.
    - Adobe Acrobat Reader `unverified` sentence preserved; all `forms_claims_test.exs` refute guards still pass.
    - `CHANGELOG.md` under `[0.3.0] - Unreleased` has `#### Viewer Evidence (v2.3)` with two **Changed** bullets (discipline rule + forms re-home, status unchanged).
    - Full docs-contract suite (`mix docs.contract`) passes 8/8 lanes.
  artifacts:
    - path: guides/api_stability.md
      provides: "Public-contract mirror + CHANGELOG discipline rule (RECIPE-05)"
    - path: CHANGELOG.md
      provides: "Demonstrated Viewer Evidence subsection for worked cell"
  key_links:
    - from: guides/api_stability.md
      to: priv/viewer_evidence/forms/apple_preview.md
      via: "Interactive Forms Apple Preview sentence evidence path pointer"
    - from: guides/api_stability.md
      to: guides/viewer_evidence.md
      via: "Discipline section operator recipe pointer"
    - from: CHANGELOG.md
      to: guides/api_stability.md
      via: "Changed bullet documenting discipline rule adoption"
---

<objective>
Close the public-contract loop for the worked cell — `api_stability.md` mirror + CHANGELOG discipline.

Purpose: Deliver RECIPE-05 — every cell promotion and explicit deferral lands in CHANGELOG; rule documented in `guides/api_stability.md` so future surfaces inherit the discipline. Uses actual `viewer_version` and `platform` from 69-02 evidence frontmatter.
Output: Discipline section, updated forms × Apple Preview sentence, CHANGELOG entry; all docs-contract lanes green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-RESEARCH.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-PATTERNS.md
@.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-VALIDATION.md
@guides/api_stability.md
@guides/viewer_evidence.md
@priv/viewer_evidence/forms/apple_preview.md
@CHANGELOG.md
@test/docs_contract/forms_claims_test.exs
</context>

<interfaces>
Current forms sentence to replace (api_stability.md line 42):
```markdown
For text fields, checkboxes, and radio groups, Apple Preview is supported for this phase based on the recorded Phase 47 viewer checklist. Adobe Acrobat Reader remains `unverified` until the same checklist records passing open, visible default state, edit/toggle, and save behavior.
```

Target sentence template (69-CONTEXT D-21, STACK.md):
```markdown
Apple Preview is `supported` for `forms` based on the recorded viewer checklist for version **{viewer_version}** on **{platform}** (`priv/viewer_evidence/forms/apple_preview.md`). That proof confirms `open`, `default_state_visible`, `edit_or_toggle`, and `save` for the representative forms fixture.
```

Discipline section (69-CONTEXT D-20):
```markdown
## Viewer Evidence and CHANGELOG Discipline

Promotions (`unverified` → `supported`), new `explicit_deferral` rows, and legacy `supported` re-homes into `priv/viewer_evidence/` are public-contract changes requiring CHANGELOG entries. Re-validations that refresh `recorded_at` are also recorded.

See `guides/viewer_evidence.md` for the operator recording recipe.
```

CHANGELOG entry (69-CONTEXT D-22):
```markdown
#### Viewer Evidence (v2.3)

##### Changed

- Document viewer-evidence CHANGELOG discipline in `guides/api_stability.md` — promotions, explicit deferrals, and legacy re-homes require CHANGELOG entries; re-validations refresh `recorded_at` in the log.
- Re-home forms × Apple Preview viewer evidence to `priv/viewer_evidence/forms/apple_preview.md` with matrix `evidence:` pointer (**support status unchanged** since v1.8 Phase 47).
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Viewer Evidence and CHANGELOG Discipline section</name>
  <files>guides/api_stability.md</files>
  <read_first>
    - guides/api_stability.md (SemVer section through Interactive Forms — lines 1–44)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-19, D-20
    - guides/viewer_evidence.md (operator recipe pointer target)
  </read_first>
  <acceptance_criteria>
    - `guides/api_stability.md` contains heading `## Viewer Evidence and CHANGELOG Discipline`.
    - Section states promotions (`unverified` → `supported`), new `explicit_deferral`, and legacy `supported` re-homes require CHANGELOG entries.
    - Section states re-validations refreshing `recorded_at` are also recorded.
    - Section links to `guides/viewer_evidence.md` as operator recipe.
    - Section placed after `## Deprecation Policy` and before `## Interactive Forms Support Boundary` (policy preamble, discoverable).
    - Existing sections `## Semantic Versioning Expectations`, `## Core API vs Adapters`, `## Deprecation Policy` text unchanged.
  </acceptance_criteria>
  <action>
    Insert new `## Viewer Evidence and CHANGELOG Discipline` section after Deprecation Policy block (after line 24) and before Interactive Forms Support Boundary.
    Use exact prose from 69-CONTEXT D-20; link `guides/viewer_evidence.md`.
  </action>
  <verify>
    <automated>grep -q '## Viewer Evidence and CHANGELOG Discipline' guides/api_stability.md && grep -q 'guides/viewer_evidence.md' guides/api_stability.md</automated>
  </verify>
  <done>Discipline section present with CHANGELOG obligations and recipe pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Replace forms × Apple Preview mirror sentence</name>
  <files>guides/api_stability.md</files>
  <read_first>
    - priv/viewer_evidence/forms/apple_preview.md (viewer_version, platform frontmatter values)
    - guides/api_stability.md (Interactive Forms section — line 42)
    - test/docs_contract/forms_claims_test.exs (refute guards lines 37–67)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-21, D-23
  </read_first>
  <acceptance_criteria>
    - Old Phase 47 sentence (`based on the recorded Phase 47 viewer checklist`) removed from Interactive Forms section.
    - New sentence matches STACK template: `Apple Preview is \`supported\` for \`forms\` based on the recorded viewer checklist for version **{viewer_version}** on **{platform}** (\`priv/viewer_evidence/forms/apple_preview.md\`). That proof confirms \`open\`, \`default_state_visible\`, \`edit_or_toggle\`, and \`save\` for the representative forms fixture.`
    - `{viewer_version}` and `{platform}` copied from `priv/viewer_evidence/forms/apple_preview.md` frontmatter (not hardcoded placeholders).
    - Adobe Acrobat Reader `unverified` sentence preserved verbatim on following line.
    - `grep -q 'priv/viewer_evidence/forms/apple_preview.md' guides/api_stability.md` succeeds.
    - `grep -q 'Adobe Acrobat Reader remains \`unverified\`' guides/api_stability.md` succeeds.
    - No prose updates for embedded_files, links, or protection legacy rows (deferred to Phase 70).
  </acceptance_criteria>
  <action>
    Read `viewer_version` and `platform` from evidence frontmatter.
    Replace line 42 Apple Preview sentence with STACK.md template using actual observation values.
    Preserve Adobe Acrobat Reader unverified sentence and "Other viewers are not part…" paragraph unchanged.
  </action>
  <verify>
    <automated>grep -q 'priv/viewer_evidence/forms/apple_preview.md' guides/api_stability.md && grep -q 'default_state_visible' guides/api_stability.md && ! grep -q 'Phase 47 viewer checklist' guides/api_stability.md</automated>
  </verify>
  <done>Forms mirror sentence points at canonical evidence with version + platform from frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG Viewer Evidence subsection</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (`[0.3.0] - Unreleased` section)
    - .planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-CONTEXT.md D-22
    - .planning/phases/54-*/ (protection CHANGELOG pattern — if present)
  </read_first>
  <acceptance_criteria>
    - Under `## [0.3.0] - Unreleased`, new subsection `#### Viewer Evidence (v2.3)` exists.
    - Subsection contains `##### Changed` with exactly two bullets:
      1. Discipline rule adoption in `guides/api_stability.md`.
      2. Forms × Apple Preview re-home to `priv/viewer_evidence/forms/apple_preview.md` with **support status unchanged** note.
    - Bullets use **Changed** heading (not Added) — re-home pattern per D-22.
    - `grep -q 'Viewer Evidence (v2.3)' CHANGELOG.md` succeeds.
    - `grep -q 'support status unchanged' CHANGELOG.md` succeeds.
  </acceptance_criteria>
  <action>
    Insert `#### Viewer Evidence (v2.3)` subsection after existing `[0.3.0]` Added blocks (before next version header if any).
    Add two Changed bullets per 69-CONTEXT D-22 exact wording.
  </action>
  <verify>
    <automated>grep -q 'Viewer Evidence (v2.3)' CHANGELOG.md && grep -q 'support status unchanged' CHANGELOG.md && grep -q 'guides/api_stability.md' CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG demonstrates discipline rule + worked-cell re-home entry.</done>
</task>

<task type="auto">
  <name>Task 4: Verify docs-contract regression suite</name>
  <files>guides/api_stability.md, CHANGELOG.md</files>
  <read_first>
    - test/docs_contract/forms_claims_test.exs (full file — refute guards)
    - test/docs_contract/viewer_evidence_claims_test.exs
    - scripts/verify_docs.exs (lane registration)
  </read_first>
  <acceptance_criteria>
    - `mix test test/docs_contract/forms_claims_test.exs` passes — all refute guards intact (`refute guide =~ "Adobe Acrobat Reader is supported"`, `refute guide =~ "digital signatures are supported"`, etc.).
    - `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes.
    - `mix docs.contract` exits 0 (8/8 lanes).
    - `mix docs` builds without error.
    - Guide quick-start step 7 cross-reference (`guides/api_stability.md` + `CHANGELOG.md`) now satisfied by this plan's edits.
  </acceptance_criteria>
  <action>
    Run full docs-contract verification after api_stability and CHANGELOG edits.
    Fix any broken assertions before marking plan complete.
  </action>
  <verify>
    <automated>mix test test/docs_contract/forms_claims_test.exs && mix test test/docs_contract/viewer_evidence_claims_test.exs && mix docs.contract</automated>
  </verify>
  <done>All docs-contract lanes green; forms refute guards preserved; ExDoc build succeeds.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Public Contract Documentation

| Boundary | Description |
|----------|-------------|
| Evidence facts → api_stability mirror | Version/platform copied from evidence frontmatter only — no invented viewer claims. |
| Contract changes → CHANGELOG | Every promotion/deferral/re-home recorded for audit trail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-03-01 | Tampering | api_stability mirror | High | mitigate | Copy `viewer_version`/`platform` from evidence frontmatter; preserve Adobe Acrobat `unverified`; `forms_claims_test.exs` refute guards block overclaim. |
| T-69-03-02 | Repudiation | CHANGELOG omission | Medium | mitigate | Discipline section documents obligation; demonstrated Changed entry for worked cell; release-readiness pattern from Phase 54. |
| T-69-03-03 | Information Disclosure | api_stability prose | Low | accept | Mirror cites evidence path only; no operational secrets in public docs. |
| T-69-03-04 | Elevation of Privilege | Support boundary widening | Medium | mitigate | Re-home note explicitly states **support status unchanged**; no new viewers or surfaces promoted. |

**Block gate:** T-69-03-01 is HIGH — mitigated by frontmatter-sourced values + docs-contract refute guards; no merge if forms_claims_test fails.
</threat_model>

<success_criteria>
- `grep -q 'Viewer Evidence and CHANGELOG Discipline' guides/api_stability.md`.
- Forms Apple Preview sentence references `priv/viewer_evidence/forms/apple_preview.md` with evidence frontmatter version/platform.
- `grep -q 'Viewer Evidence (v2.3)' CHANGELOG.md`.
- `mix docs.contract` passes 8/8 lanes.
- `mix test test/docs_contract/forms_claims_test.exs` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/69-operator-recipe-and-first-cell-end-to-end/69-03-SUMMARY.md` via orchestration.
</output>
