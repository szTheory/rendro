---
phase: 56
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - lib/rendro/sign.ex
  - lib/rendro/sign/adapter.ex
  - lib/rendro/error.ex
  - test/rendro/sign_test.exs
  - test/rendro/error_test.exs
autonomous: true
requirements:
  - PREP-01
  - PREP-02
  - PREP-03
must_haves:
  truths:
    - `Rendro.Sign.prepare/2` accepts a rendered `%Rendro.Artifact{}` and returns another `%Rendro.Artifact{}` with a narrow nested signing-preparation manifest for external signing handoff.
    - The preparation seam consumes final artifact bytes only and does not change `Rendro.render/2`, `Rendro.render_to_artifact/2`, or the authored document model.
    - Rendro core still does not own cryptographic signing, key custody, certificate trust, or a first-party signer integration path.
  artifacts:
    - path: lib/rendro/sign.ex
      provides: "Canonical artifact-first `Rendro.Sign.prepare/2` API"
    - path: lib/rendro/sign/adapter.ex
      provides: "Optional-adapter preparation boundary without a bundled signer implementation"
    - path: test/rendro/sign_test.exs
      provides: "Prepared-artifact manifest and typed prepare-boundary coverage"
    - path: test/rendro/error_test.exs
      provides: "Prepare-stage error guidance stays actionable and free of signer secrets if error wording changes"
  key_links:
    - from: lib/rendro/sign.ex
      to: lib/rendro/artifact.ex
      via: "`Rendro.Sign.prepare/2` must wrap final prepared bytes back into a normal artifact with nested `metadata.signing_preparation`"
      pattern: "Artifact.wrap|signing_preparation|reserved_bytes"
    - from: lib/rendro/sign.ex
      to: lib/rendro/sign/adapter.ex
      via: "the core prep seam must stop at a narrow optional-adapter contract rather than a bundled signer implementation"
      pattern: "@callback prepare|adapter|prepare"
    - from: test/rendro/sign_test.exs
      to: lib/rendro/pdf/writer.ex
      via: "prepare must target the deterministic unsigned signature-widget output from 56-01 rather than authored document state"
      pattern: "signature|artifact|prepare"
---

<objective>
Add the artifact-first external-signing preparation seam on top of the deterministic unsigned writer output.

Purpose: satisfy `PREP-01`, `PREP-02`, and `PREP-03` by introducing `Rendro.Sign.prepare/2` as the canonical post-render handoff boundary over `%Rendro.Artifact{}`.
Output: one focused preparation API, one optional behavior contract for future signer integrations, and tests that prove prepared artifacts expose only a narrow generic manifest without widening into digital-signature execution.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/milestones/v2.0-ROADMAP.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-CONTEXT.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-RESEARCH.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-PATTERNS.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-01-PLAN.md
@.planning/phases/51-protection-api-contract-and-validation/51-01-PLAN.md
</execution_context>

<context>
@lib/rendro/artifact.ex
@lib/rendro/protect.ex
@lib/rendro/protect/adapter.ex
@lib/rendro/error.ex
@test/rendro/error_test.exs

<interfaces>
From `lib/rendro/artifact.ex`:
```elixir
@spec wrap(binary(), t(), map()) :: t()
def wrap(pdf_binary, %__MODULE__{} = source, metadata_updates \\ %{}) do
  %__MODULE__{
    binary: pdf_binary,
    hash: hash_binary(pdf_binary),
    diagnostics: source.diagnostics,
    metadata: Map.merge(source.metadata, metadata_updates)
  }
end
```

From `lib/rendro/protect.ex`:
```elixir
@spec password(Artifact.t(), options()) :: {:ok, Artifact.t()} | {:error, Error.t()}
def password(%Artifact{} = artifact, opts) when is_list(opts) do
  with {:ok, normalized} <- normalize_opts(opts),
       {:ok, protected_binary} <- normalized.adapter.protect(artifact, normalized) do
    {:ok, Artifact.wrap(protected_binary, artifact, %{...})}
  end
end
```

From `lib/rendro/protect/adapter.ex`:
```elixir
@callback protect(Rendro.Artifact.t(), map()) ::
            {:ok, binary()} | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `Rendro.Sign.prepare/2` as a narrow artifact-first preparation API over final rendered bytes</name>
  <read_first>
    - lib/rendro/artifact.ex
    - lib/rendro/protect.ex
    - lib/rendro/error.ex
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-CONTEXT.md
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-RESEARCH.md
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-01-PLAN.md
  </read_first>
  <files>lib/rendro/sign.ex, lib/rendro/error.ex, test/rendro/sign_test.exs, test/rendro/error_test.exs</files>
  <acceptance_criteria>
    - `lib/rendro/sign.ex` defines `@spec prepare(Rendro.Artifact.t(), options()) :: {:ok, Rendro.Artifact.t()} | {:error, Rendro.Error.t()}`
    - `Rendro.Sign.prepare/2` accepts only a rendered `%Rendro.Artifact{}` plus a narrow options list and leaves `Rendro.render/2`, `Rendro.render_to_artifact/2`, and `lib/rendro.ex` untouched per D-06 through D-09
    - prepared output records a nested `metadata.signing_preparation` manifest containing only adapter-neutral handoff facts: prepared status, selected field identity, reserved signature-content capacity, and byte-range/content placeholder coordinates or equivalent deterministic offsets per D-10 through D-14
    - the manifest does not include key material, certificates, signer identity, compliance labels, trust policy, or signer-specific blobs
    - `test/rendro/sign_test.exs` covers successful artifact wrapping, typed option validation, missing/non-signature field failures, and stable manifest shape
  </acceptance_criteria>
  <action>
    Add `lib/rendro/sign.ex` as the canonical D-06 artifact-first seam modeled after `Rendro.Protect.password/2`: validate options first, operate on final artifact bytes only, and wrap the resulting bytes back into a normal `%Rendro.Artifact{}` with `Rendro.Artifact.wrap/3`. Require callers to identify the authored signature field explicitly and require a deterministic reserved signature-content capacity option.
    Resolve the lookup strategy in the implementation contract now instead of leaving it open: `Rendro.Sign.prepare/2` should locate its target from Rendro-owned unsigned signature-widget bytes emitted by `56-01`, using a narrow byte-level lookup scoped strictly to that deterministic writer output. Do not add a new render-time metadata index or thread `%Rendro.Document{}` into the API in this phase.
    Keep the preparation contract small and generic. Record the handoff result under `artifact.metadata.signing_preparation` with facts external signing workflows actually need, such as `status: :prepared`, the selected field name, reserved content capacity, and deterministic placeholder coordinates or lengths for the byte-range and reserved signature-content span. Do not widen authored document state, do not create a second artifact carrier, and do not record cryptographic, compliance, signer-identity, or vendor-specific payloads in the shared core manifest.
    Add focused tests in `test/rendro/sign_test.exs` that prove `Rendro.Sign.prepare/2` composes strictly after render, preserves the artifact wrapper, and exposes only the narrow manifest promised by D-10 through D-17. If explicit prepare-stage wording is added to `lib/rendro/error.ex`, extend `test/rendro/error_test.exs` so callers receive actionable `what/why/next` guidance without any secret-bearing details.
  </action>
  <verify>
    <automated>mix test test/rendro/sign_test.exs test/rendro/error_test.exs</automated>
  </verify>
  <done>`Rendro.Sign.prepare/2` prepares final artifact bytes for external signing, returns a normal artifact with a narrow nested manifest, and keeps signer execution and cryptographic trust outside core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a minimal optional-adapter contract for future signer execution without bundling a signer path</name>
  <read_first>
    - lib/rendro/protect/adapter.ex
    - lib/rendro/sign.ex
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-CONTEXT.md
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-PATTERNS.md
  </read_first>
  <files>lib/rendro/sign/adapter.ex, lib/rendro/sign.ex, test/rendro/sign_test.exs</files>
  <acceptance_criteria>
    - `lib/rendro/sign/adapter.ex` defines a minimal optional-adapter contract for future signer integrations without shipping a first-party signer implementation
    - the behavior and tests keep signer execution, key custody, certificates, and trust-policy operations outside core per `PREP-03`
    - no task in this plan adds `Rendro.prepare_for_signing/2`, `Rendro.Sign.sign/2`, or any first-party cryptographic signing helper
    - no task in this plan edits `guides/api_stability.md`, `priv/support_matrix.json`, or docs-contract tests
  </acceptance_criteria>
  <action>
    Add `lib/rendro/sign/adapter.ex` only as a narrow optional boundary for future external signer implementations. The behavior should accept the prepared artifact or equivalent normalized facts and return prepared bytes plus namespaced adapter-local metadata, or an equivalently narrow result that keeps signer execution outside core.
    Keep the contract behavior-only in this phase. Do not ship a supported first-party signer integration, vendor-specific configuration bag, or root-level sugar. The core seam ends at prepared artifact output and an optional adapter contract that future phases or external packages may consume.
    Extend `test/rendro/sign_test.exs` with coverage that proves the adapter seam stays narrow and non-owning: the core manifest remains generic, and any future signer-specific additions must stay outside the shared contract.
  </action>
  <verify>
    <automated>mix test test/rendro/sign_test.exs</automated>
  </verify>
  <done>The repo has a minimal optional-adapter boundary for future signer execution, but Phase 56 still stops at prepared artifact output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered artifact -> signing-preparation seam | Final PDF bytes cross from deterministic render output into a trust-sensitive post-render transform here. |
| Core preparation seam -> optional adapter boundary | Future signer integrations must not drag signer-specific trust state back into core here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03 | Tampering / scope creep | `Rendro.Sign.prepare/2` inputs | mitigate | Require explicit field selection, deterministic reserve sizing, and typed prepare-boundary validation before any artifact mutation. |
| T-56-04 | Integrity | `lib/rendro/sign.ex` + `lib/rendro/artifact.ex` | mitigate | Keep preparation artifact-first and wrapper-preserving so render semantics stay unchanged and the handoff boundary remains auditable. |
| T-56-05 | Information disclosure / semantic drift | `metadata.signing_preparation` | mitigate | Limit the manifest to adapter-neutral placeholder coordinates and reserve sizing; exclude keys, certificates, signer identity, and compliance claims. |
| T-56-06 | Scope drift | optional adapter boundary | mitigate | Keep signer execution behavior-only and refuse first-party signer integration, root-level sugar, or in-core cryptographic helpers in this phase. |
</threat_model>

<verification>
mix test test/rendro/sign_test.exs
mix test test/rendro/sign_test.exs test/rendro/error_test.exs
</verification>

<success_criteria>
- `Rendro.Sign.prepare/2` consumes `%Rendro.Artifact{}` values only, returns a wrapped artifact with a narrow nested manifest, and leaves root render semantics unchanged.
- The signing-preparation seam operates on final artifact bytes and does not depend on widening the artifact shape or reintroducing authored document state.
- No root-level signing helper, in-core cryptographic signing path, bundled signer integration, or support-contract publication is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/56-writer-and-external-signing-preparation-seam/56-02-SUMMARY.md`
</output>
