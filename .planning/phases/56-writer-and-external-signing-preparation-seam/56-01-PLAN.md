---
phase: 56
plan: 01
type: execute
wave: 1
depends_on: ["55-02"]
files_modified:
  - lib/rendro/pdf/writer.ex
  - test/rendro/pdf/writer_test.exs
  - test/rendro/deterministic_test.exs
autonomous: true
requirements:
  - SIGN-03
must_haves:
  truths:
    - Identical authored signature-field inputs render the same visible unsigned `/Sig` widget structures through the existing AcroForm and page-annotation seams.
    - Ordinary `Rendro.render/2` and `Rendro.render_to_artifact/2` output stays unsigned: no `/V`, `/Contents`, `/ByteRange`, `/Lock`, `/SV`, `/Reference`, `/Filter`, or `/SubFilter` appear during base render.
    - Phase 56-01 is writer-only. It does not add post-render preparation APIs, docs-contract updates, or digital-signature claims.
  artifacts:
    - path: lib/rendro/pdf/writer.ex
      provides: "Deterministic unsigned signature-widget serialization on the existing writer path"
    - path: test/rendro/pdf/writer_test.exs
      provides: "Structural and negative regression coverage for unsigned `/Sig` widgets"
    - path: test/rendro/deterministic_test.exs
      provides: "Byte-for-byte deterministic proof for repeated unsigned signature renders"
  key_links:
    - from: lib/rendro/pdf/writer.ex
      to: test/rendro/pdf/writer_test.exs
      via: "writer assertions must prove `/FT /Sig` serialization and the absence of signing-value placeholders in ordinary render output"
      pattern: "/FT /Sig|/ByteRange|/Contents|/Lock|/SV"
    - from: lib/rendro/pdf/writer.ex
      to: test/rendro/deterministic_test.exs
      via: "deterministic repeated render proof must cover the signature widget branch explicitly"
      pattern: "deterministic|signature|render"
---

<objective>
Add the runtime writer seam for deterministic visible unsigned signature widgets.

Purpose: satisfy `SIGN-03` by extending the existing AcroForm/widget writer path for `%Rendro.FormField{type: :signature}` while keeping ordinary render output explicitly unsigned.
Output: one focused writer extension plus structural and deterministic regression coverage that proves the new unsigned `/Sig` widget contract and its negative guardrails.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/milestones/v2.0-ROADMAP.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-CONTEXT.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-RESEARCH.md
@.planning/phases/56-writer-and-external-signing-preparation-seam/56-PATTERNS.md
@.planning/phases/55-signature-field-authoring-contract/55-01-PLAN.md
@.planning/phases/48-embedded-file-core-surface/48-02-PLAN.md
</execution_context>

<context>
@lib/rendro/pdf/writer.ex
@test/rendro/pdf/writer_test.exs
@test/rendro/deterministic_test.exs

<interfaces>
From `lib/rendro/pdf/writer.ex`:
```elixir
defp allocate_standalone_form_field(form_field, num) do
  base = %{
    type: form_field.field.type,
    field_obj_num: num,
    block: form_field.block,
    field: form_field.field,
    page_index: form_field.page_index,
    widget_obj_num: num
  }
end
```

From `test/rendro/pdf/writer_test.exs`:
```elixir
assert pdf =~ "/Subtype /Widget"
refute pdf =~ "/NeedAppearances"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the existing standalone widget writer path for visible unsigned signature fields</name>
  <read_first>
    - lib/rendro/pdf/writer.ex
    - test/rendro/pdf/writer_test.exs
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-CONTEXT.md
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-PATTERNS.md
    - .planning/phases/55-signature-field-authoring-contract/55-01-PLAN.md
  </read_first>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <acceptance_criteria>
    - `lib/rendro/pdf/writer.ex` routes `%Rendro.FormField{type: :signature}` through the existing standalone field allocation path rather than a new writer subsystem
    - `test/rendro/pdf/writer_test.exs` contains a visible signature-field fixture that asserts `/Subtype /Widget`, `/FT /Sig`, `/T (...)`, `/Rect [...]`, `/AP <<`, and AcroForm `/Fields [` are present in deterministic render output
    - `test/rendro/pdf/writer_test.exs` contains explicit negative assertions that the rendered unsigned signature widget omits `/V`, `/Contents`, `/ByteRange`, `/Lock`, `/SV`, `/Reference`, `/Filter`, `/SubFilter`, and `/NeedAppearances`
    - ordinary render output remains free of signing placeholders and signer-policy dictionaries per D-02 through D-04
    - no task in this plan edits `lib/rendro/sign.ex`, `guides/api_stability.md`, `priv/support_matrix.json`, or docs-contract tests
  </acceptance_criteria>
  <action>
    Extend `lib/rendro/pdf/writer.ex` for D-01 through D-05 by treating `:signature` as another standalone widget family on the existing AcroForm path. Reuse `allocate_form_field_nums/2`, `allocate_standalone_form_field/2`, page `/Annots`, and catalog `/AcroForm` wiring instead of creating a signature-only writer path or any invisible-widget fallback.
    Add a dedicated signature-widget serializer that emits a visible unsigned field dictionary with `/Type /Annot`, `/Subtype /Widget`, `/FT /Sig`, `/Rect`, `/T`, `/P`, and a Rendro-owned `/AP` appearance stream. Keep ordinary render strictly unsigned: do not emit `/V`, do not reserve `/Contents` or `/ByteRange`, and do not introduce `/Lock`, `/SV`, `/Reference`, `/Filter`, `/SubFilter`, certification policy, or any other signer-owned dictionary content while rendering the authored document.
    Add focused writer regressions in `test/rendro/pdf/writer_test.exs` using the existing substring-assertion style. Cover both the positive structure for a visible unsigned signature widget and the negative guardrails that prevent signature-value or signing-policy placeholders from leaking into base output.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>The writer emits visible unsigned `/Sig` widgets through the existing AcroForm seam, and ordinary render output still makes no signing-completion claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove repeated deterministic renders of the same signature document produce identical bytes</name>
  <read_first>
    - test/rendro/deterministic_test.exs
    - test/rendro/pdf/writer_test.exs
    - lib/rendro/pdf/writer.ex
    - .planning/phases/56-writer-and-external-signing-preparation-seam/56-RESEARCH.md
  </read_first>
  <files>test/rendro/deterministic_test.exs, test/rendro/pdf/writer_test.exs</files>
  <acceptance_criteria>
    - `test/rendro/deterministic_test.exs` contains a signature-widget document slice rendered twice with `deterministic: true`
    - the deterministic regression asserts byte-for-byte equality for repeated renders of the same authored signature input
    - deterministic proof does not depend on any prepare-time placeholder reservation or post-render mutation
  </acceptance_criteria>
  <action>
    Add one deterministic regression lane that renders the same authored signature document twice with `deterministic: true` and proves the bytes are identical. Keep this proof at ordinary render time only: the point is to lock `SIGN-03` for the unsigned writer contract, not to prove post-render preparation behavior.
    Reuse the current deterministic-test style already present in the repo instead of building a new harness. Keep the fixture visibly aligned with the Phase 55 authored contract so later preparation work can rely on a stable unsigned writer output shape.
  </action>
  <verify>
    <automated>mix test test/rendro/deterministic_test.exs test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Repeated deterministic renders of the same unsigned signature document produce the same bytes without any prepare-time involvement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored signature field -> writer output | Unsupported or ambiguous signature semantics must not turn unsigned render output into a fake signing state here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Spoofing / semantic confusion | `lib/rendro/pdf/writer.ex` | mitigate | Emit visible unsigned `/Sig` widgets only and explicitly omit `/V`, `/Contents`, `/ByteRange`, `/Lock`, `/SV`, `/Reference`, `/Filter`, and `/SubFilter` in ordinary render output. |
| T-56-02 | Integrity | deterministic writer output | mitigate | Add repeated-render regression proof so the new signature branch cannot drift into nondeterministic object allocation or placeholder behavior. |
</threat_model>

<verification>
mix test test/rendro/pdf/writer_test.exs
mix test test/rendro/deterministic_test.exs test/rendro/pdf/writer_test.exs
</verification>

<success_criteria>
- Visible unsigned signature widgets serialize deterministically through the existing AcroForm and annotation seams.
- Ordinary render output never includes signature-value placeholders, signing-policy dictionaries, or signer-owned metadata.
- No preparation API, docs-contract update, or digital-signature support claim is introduced in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/56-writer-and-external-signing-preparation-seam/56-01-SUMMARY.md`
</output>
