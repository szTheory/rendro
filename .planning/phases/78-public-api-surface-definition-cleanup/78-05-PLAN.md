---
phase: 78-public-api-surface-definition-cleanup
plan: 05
type: execute
wave: 3
depends_on:
  - "78-04"
files_modified:
  - lib/mix/tasks/rendro/api.gen.ex
  - priv/public_api.json
  - test/rendro/public_api/manifest_test.exs
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "mix rendro.api.gen exits 0 and writes priv/public_api.json with all public modules listed and their tier, functions, and types"
    - "priv/public_api.json validates against priv/schemas/public_api.schema.json via Rendro.PublicApi.Validator.validate/1"
    - "priv/public_api.json lists no hidden modules (CidFont, FontSubsetter, Bidi, Shaper, Format, Audit absent)"
    - "priv/public_api.json lists all 22 stable modules and all 16 adapter modules with correct tier assignments"
    - "Conditional adapters (Threadline, Mailglass, Accrue, Phoenix, Oban) appear in priv/public_api.json — the generator recompiles them before introspecting"
    - "Running mix rendro.api.gen twice produces byte-identical priv/public_api.json (idempotent / deterministic)"
    - "All tests in test/rendro/public_api/manifest_test.exs pass (schema validation + idempotency + hidden-module assertions)"
    - "mix ci exits 0 (--warnings-as-errors clean; no unused variable warnings)"
  artifacts:
    - path: "lib/mix/tasks/rendro/api.gen.ex"
      provides: "mix rendro.api.gen task: compiles, recompiles adapters, introspects, writes manifest"
      contains: "use Mix.Task"
    - path: "priv/public_api.json"
      provides: "generated manifest of all public modules with tier + functions + types"
      contains: "\"modules\""
    - path: "test/rendro/public_api/manifest_test.exs"
      provides: "schema validation test, idempotency test, hidden-module exclusion test, tier-coverage test, byte-equality test"
  key_links:
    - from: "lib/mix/tasks/rendro/api.gen.ex"
      to: "lib/rendro/public_api.ex"
      via: "Rendro.PublicApi.recompile_conditional_adapters/0 + build_manifest/1"
      pattern: "Rendro\\.PublicApi\\."
    - from: "priv/public_api.json"
      to: "priv/schemas/public_api.schema.json"
      via: "Rendro.PublicApi.Validator.validate/1 in tests"
      pattern: "\"tier\""
    - from: "test/rendro/public_api/manifest_test.exs"
      to: "priv/public_api.json"
      via: "Rendro.PublicApi.Loader.load!/0"
      pattern: "Loader\\.load!"
---

<objective>
Create the `mix rendro.api.gen` task that walks all public Rendro modules via `Rendro.PublicApi.build_manifest/1`, recompiles conditional adapters first, and writes the canonical `priv/public_api.json`. Then commit the generated manifest. Write integration tests asserting schema validity, idempotency, hidden-module exclusions, tier coverage, and byte-equality of the freshly-generated manifest against the checked-in file.

Purpose: This completes the Phase 78 manifest pipeline. The checked-in `priv/public_api.json` is the canonical, schema-versioned source of truth for the public surface (API-01). Phase 79's contract test will regenerate this in-memory and assert equality — but the generator must be deterministic enough to survive that check. The tests here are the Phase 78 side of that guarantee, including the byte-equality idempotency assertion (the D-15 drift treadmill guard).

Output: Three new files. `priv/public_api.json` is the primary deliverable (checked in after generation). The mix task provides the regeneration command for future surface changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-CONTEXT.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-RESEARCH.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-PATTERNS.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mix rendro.api.gen task and generate priv/public_api.json</name>
  <files>
    lib/mix/tasks/rendro/api.gen.ex,
    priv/public_api.json
  </files>
  <read_first>
    - lib/mix/tasks/rendro/viewer_evidence.ex lines 1–10, 76–99, 118–128 — the existing mix task analog: use Mix.Task, @shortdoc, @moduledoc prose, @impl Mix.Task def run(args), Mix.Task.run("compile"), Mix.shell().info/error, exit({:shutdown, 1}) on error
    - lib/rendro/public_api.ex — the Rendro.PublicApi module created in Plan 04: recompile_conditional_adapters/0, build_manifest/1 signatures
    - RESEARCH.md Focus Area 6 — mix task structure: use Mix.Task, @shortdoc, @impl, Mix.Task.run("compile") first
    - RESEARCH.md Focus Area 5 — canonical JSON: JSON.encode! with string keys sorts alphabetically; pretty-print or compact (match support_matrix.json formatting)
    - PATTERNS.md §"lib/mix/tasks/rendro/api.gen.ex (NEW)" — complete module skeleton with @manifest_path, @impl run/1
    - CONTEXT.md D-15 — generator calls Rendro.PublicApi.build_manifest/1; never re-implements introspection inline
    - priv/support_matrix.json — check formatting (indentation style) to match in the generated output
    - PLANNING CONTEXT §"Key sequencing reality" — generator MUST call recompile_conditional_adapters before build_manifest; this is the chief footgun
  </read_first>
  <action>
    Step 1 — Create lib/mix/tasks/rendro/api.gen.ex:

    The file must be created under lib/mix/tasks/rendro/ (directory already exists — viewer_evidence.ex is there).

    Module: Mix.Tasks.Rendro.Api.Gen

    Structure (mirror viewer_evidence.ex):
    - use Mix.Task
    - @shortdoc "Generate priv/public_api.json from @moduledoc tags: in source"
    - @moduledoc with prose explaining: introspects all public Rendro modules for their tags: [:stable|:adapter] annotation and writes priv/public_api.json. Run after changing module tiers. Regenerates deterministically — running twice produces identical output.
    - @manifest_path "priv/public_api.json" module attribute
    - @public_modules module attribute listing all public Rendro modules in the correct tier sets (D-04 stable + D-05 adapter). This is the explicit module registry the generator uses — it does NOT do reflection over all loaded modules. List all 22 stable + 16 adapter modules by their full module atom. Conditional adapters are included in the list; recompile_conditional_adapters/0 ensures they are present.
    - @impl Mix.Task def run(_args):
        1. Mix.Task.run("compile")
        2. Rendro.PublicApi.recompile_conditional_adapters()
        3. manifest = Rendro.PublicApi.build_manifest(@public_modules)
        4. json = JSON.encode!(manifest, pretty: true) [or compact — match support_matrix.json formatting]
        5. File.write!(@manifest_path, json <> "\n")  [trailing newline for POSIX compliance]
        6. Mix.shell().info("Wrote #{@manifest_path}")
    - On File.write! or encode failure: catch, Mix.shell().error(message), exit({:shutdown, 1})

    Step 2 — Run the task to generate priv/public_api.json:
    Execute `mix rendro.api.gen` in the project root. This will produce priv/public_api.json with the actual module surface. The generated file is the checked-in artifact.

    Step 3 — Verify the generated file:
    - Confirm priv/public_api.json exists and is valid JSON
    - Confirm it contains "modules" key with entries for Rendro.Document, Rendro.Metadata, etc.
    - Confirm CidFont, FontSubsetter, Audit, Format are NOT present as module keys
    - Confirm tiers match D-04/D-05 assignments

    If any module's tier appears as "untagged" in the output, it means Plan 02 missed tagging it — fix the missing tag in the relevant source file before considering this task done.
  </action>
  <verify>
    <automated>
      mix rendro.api.gen 2>&amp;1 &amp;&amp; echo "EXIT 0"
    </automated>
  </verify>
  <acceptance_criteria>
    - lib/mix/tasks/rendro/api.gen.ex exists and contains "use Mix.Task", "@shortdoc", "@impl Mix.Task", "Rendro.PublicApi.recompile_conditional_adapters", "Rendro.PublicApi.build_manifest"
    - mix rendro.api.gen exits 0 and prints "Wrote priv/public_api.json"
    - priv/public_api.json exists after running the task
    - priv/public_api.json is valid JSON: File.read!("priv/public_api.json") |> JSON.decode! succeeds
    - priv/public_api.json has top-level "modules" key and no "schema_version" key
    - grep "CidFont\|FontSubsetter\|Rendro.Audit\|Rendro.Format\|Rendro.Text.Bidi\|Rendro.Text.Shaper" priv/public_api.json returns empty (hidden modules absent)
    - priv/public_api.json contains entries for "Elixir.Rendro" (tier: "stable") and "Elixir.Rendro.Recipes.Invoice" (tier: "adapter")
    - mix compile --warnings-as-errors exits 0 after adding the new task file
  </acceptance_criteria>
  <done>mix rendro.api.gen task exists and generates a valid priv/public_api.json with correct tiers; hidden modules are absent; CI is clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write integration tests for manifest schema validation, idempotency, and surface coverage</name>
  <files>
    test/rendro/public_api/manifest_test.exs
  </files>
  <read_first>
    - test/rendro/viewer_evidence/validator_test.exs — JSV validation test pattern to mirror for schema validation tests
    - priv/public_api.json — the generated manifest (produced in Task 1); read the actual content to know which modules are present and what to assert
    - lib/rendro/public_api/loader.ex — Rendro.PublicApi.Loader.load!/0 (reads and decodes priv/public_api.json)
    - lib/rendro/public_api/validator.ex — Rendro.PublicApi.Validator.validate/1
    - lib/rendro/public_api.ex — Rendro.PublicApi.recompile_conditional_adapters/0, build_manifest/1, tier_of/1
    - VALIDATION.md — validation dimensions to cover: schema-validates, idempotent, hidden modules absent, metadata visible, all manifested modules carry a tier
    - CONTEXT.md D-01 — the six hidden module targets (assert their absence from manifest)
    - CONTEXT.md D-04/D-05 — stable and adapter module lists (assert their presence with correct tier)
    - CONTEXT.md D-15 — drift treadmill guard: the checked-in priv/public_api.json must equal the freshly-generated manifest byte-for-byte; this is the Phase 78 half of what Phase 79 enforces
  </read_first>
  <behavior>
    - Test 1 (schema validation): Rendro.PublicApi.Loader.load!/0 |> Rendro.PublicApi.Validator.validate/1 returns :ok
    - Test 2 (hidden modules absent): The loaded manifest does not contain module keys for Rendro.PDF.CidFont, Rendro.PDF.FontSubsetter, Rendro.Text.Bidi, Rendro.Text.Shaper, Rendro.Format, Rendro.Audit
    - Test 3 (Metadata visible): The loaded manifest contains an entry for Elixir.Rendro.Metadata with tier "stable"
    - Test 4 (tier coverage): Every entry in the loaded manifest has a tier key with value "stable" or "adapter" — no "untagged" entries
    - Test 5 (idempotency + byte-equality): Rendro.PublicApi.recompile_conditional_adapters() + Rendro.PublicApi.build_manifest(modules) produces a map that, when JSON-encoded with the same options as the generator, byte-equals the checked-in priv/public_api.json
    - Test 6 (conditional adapters present): The loaded manifest contains entries for Rendro.Adapters.Threadline (or whatever the exact module name is) — confirm conditional adapters are present
    - Test 7 (redact helpers hidden): Code.fetch_docs(Rendro.Sign) docs list — entries for redact_opts/1, redact_prepare_opts/1, redact_sign_opts/1, redact_augment_opts/1 all have doc == :hidden
  </behavior>
  <action>
    Create test/rendro/public_api/manifest_test.exs:

    Use ExUnit.Case. At the top of the test module, call Rendro.PublicApi.recompile_conditional_adapters() in a setup_all block so conditional adapters are available for all tests.

    Test 1 — Schema validation:
    Load the manifest via Rendro.PublicApi.Loader.load!(); call Rendro.PublicApi.Validator.validate(manifest); assert result == :ok.

    Test 2 — Hidden module exclusion:
    Load manifest; extract Map.keys(manifest["modules"]); for each of the six hidden module targets (inspect form: "Elixir.Rendro.PDF.CidFont", "Elixir.Rendro.PDF.FontSubsetter", "Elixir.Rendro.Text.Bidi", "Elixir.Rendro.Text.Shaper", "Elixir.Rendro.Format", "Elixir.Rendro.Audit"), assert they are NOT in the keys list.

    Test 3 — Metadata visible:
    Load manifest; assert Map.has_key?(manifest["modules"], "Elixir.Rendro.Metadata"); assert manifest["modules"]["Elixir.Rendro.Metadata"]["tier"] == "stable".

    Test 4 — Tier coverage:
    Load manifest; for each {_module_key, entry} in manifest["modules"], assert entry["tier"] in ["stable", "adapter"] (no "untagged" values).

    Test 5 — Idempotency + byte-equality (D-15 drift treadmill guard):
    Call Rendro.PublicApi.recompile_conditional_adapters(); build a fresh manifest using Rendro.PublicApi.build_manifest with the same public module list as the generator (access via Mix.Tasks.Rendro.Api.Gen module attribute @public_modules if exposed, or define the same list inline — must match the generator exactly); encode to JSON using the same JSON.encode! options as the generator (pretty: true or compact — match Task 1 choice); add trailing newline; assert the resulting string byte-equals File.read!("priv/public_api.json"). This assertion guards the D-15 drift treadmill: if any module tag or function signature changes without regenerating the manifest, this test fails. It is the Phase 78 half of the Phase 79 contract enforcement.

    Test 6 — Conditional adapter presence:
    After recompile_conditional_adapters(), call Rendro.PublicApi.tier_of(Rendro.Adapters.Threadline) (or whatever module name the threadline adapter defines); assert result == :adapter (not :untagged).

    Test 7 — redact helpers hidden:
    {:docs_v1, _, _, _, _, _, docs} = Code.fetch_docs(Rendro.Sign)
    For each name in [:redact_opts, :redact_prepare_opts, :redact_sign_opts, :redact_augment_opts]:
      find the entry with matching function name; assert its doc element == :hidden.

    All tests live under ExUnit.Case, no special tags (they run in the standard mix test suite).
  </action>
  <verify>
    <automated>
      mix test test/rendro/public_api/manifest_test.exs 2>&amp;1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - mix test test/rendro/public_api/manifest_test.exs exits 0 with all 7+ tests passing
    - Schema validation test confirms :ok (manifest is schema-valid)
    - Hidden module exclusion test confirms CidFont etc. are absent from manifest keys
    - Tier coverage test confirms no "untagged" entries exist in the manifest
    - IDEMPOTENCY + BYTE-EQUALITY ASSERTION (D-15): the Test 5 idempotency assertion must compare JSON.encode!(fresh_manifest, [same opts as generator]) <> "\n" == File.read!("priv/public_api.json") — byte-for-byte string equality, not structural equality. This guards the drift treadmill: if the generator is run again without any tag changes, the output must be identical to the checked-in file. The test fails if encoding options differ, key ordering differs, or any whitespace differs between the fresh generation and the checked-in artifact.
    - mix test exits 0 (the new tests do not break the existing test suite)
    - mix compile --warnings-as-errors exits 0
  </acceptance_criteria>
  <done>Integration tests pass: manifest is schema-valid, hidden modules absent, no untagged tiers, generator is idempotent with byte-identical output matching the checked-in priv/public_api.json, conditional adapters appear, redact helpers are hidden</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix rendro.api.gen → priv/public_api.json | Writes to first-party priv/ directory; no external input; no user data |
| test → priv/public_api.json | Tests read a first-party static file; no network or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-05-SC | Tampering | No new package installs | accept | No new deps; JSON and JSV already in project |
| T-78-05-01 | Tampering | mix rendro.api.gen writes to priv/ | accept | Writes only to priv/public_api.json at a fixed, first-party path; no path traversal or user-controlled write target; File.write! overwrites in-place |
| T-78-05-02 | Information Disclosure | priv/public_api.json exposes module/function surface | accept | Intentional — the manifest is the public API declaration; it is checked in and distributed with the package as a stated surface |

No high-severity threats: the generator reads from BEAM chunks (first-party) and writes to a fixed first-party path. The manifest itself is intentionally public information.
</threat_model>

<verification>
After both tasks:

```
mix rendro.api.gen
mix test test/rendro/public_api/manifest_test.exs
mix ci
```

All three must exit 0. Final spot-checks:
- grep "CidFont" priv/public_api.json — returns empty
- grep "\"tier\": \"stable\"" priv/public_api.json — returns entries for stable modules
- grep "\"tier\": \"adapter\"" priv/public_api.json — returns entries for adapter modules
- grep "untagged" priv/public_api.json — returns empty
</verification>

<success_criteria>
- lib/mix/tasks/rendro/api.gen.ex exists with use Mix.Task, @shortdoc, @impl Mix.Task def run
- mix rendro.api.gen exits 0 and produces priv/public_api.json
- priv/public_api.json validates against priv/schemas/public_api.schema.json (Validator.validate returns :ok)
- priv/public_api.json contains no entries for CidFont, FontSubsetter, Bidi, Shaper, Format, Audit
- priv/public_api.json contains entry for Rendro.Metadata with tier "stable"
- mix test test/rendro/public_api/manifest_test.exs exits 0
- The idempotency test asserts byte-equality: JSON.encode!(fresh_manifest, [generator opts]) <> "\n" == File.read!("priv/public_api.json")
- mix ci exits 0
</success_criteria>

<output>
Create `.planning/phases/78-public-api-surface-definition-cleanup/78-05-SUMMARY.md` when done.
</output>
