---
phase: 78-public-api-surface-definition-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes/branded_invoice.ex
autonomous: true
requirements:
  - API-05

must_haves:
  truths:
    - "Rendro.Recipes.Invoice.sections/2 accepts opts as a named second argument (not _opts) and passes it to all section helpers"
    - "Rendro.Recipes.BrandedInvoice.sections/2 accepts opts as a named second argument and passes it to all section helpers"
    - "All section helper functions in Invoice and BrandedInvoice have arity-2 heads; unused opts in helper bodies is named _opts to satisfy --warnings-as-errors"
    - "Default rendered output is byte-identical before and after — no behavioral change for existing callers passing no opts"
    - "mix ci exits 0 (no unused variable warnings for opts parameter)"
  artifacts:
    - path: "lib/rendro/recipes/invoice.ex"
      provides: "sections/2 with named opts + arity-2 helper heads"
      contains: "def sections(data, opts \\\\ [])"
    - path: "lib/rendro/recipes/branded_invoice.ex"
      provides: "sections/2 with named opts + arity-2 helper heads"
      contains: "def sections(data, opts \\\\ [])"
  key_links:
    - from: "lib/rendro/recipes/invoice.ex sections/2"
      to: "invoice helper functions"
      via: "opts threaded into header_section/2, body_section/2, footer_section/2"
      pattern: "header_section\\(data, opts\\)"
    - from: "lib/rendro/recipes/branded_invoice.ex sections/2"
      to: "branded_invoice helper functions"
      via: "opts threaded into logo_section/2, header_section/2, body_section/2, footer_section/2"
      pattern: "header_section\\(data, opts\\)"
---

<objective>
Normalize `sections/2` opts handling in `Rendro.Recipes.Invoice` and `Rendro.Recipes.BrandedInvoice` to match the existing `Statement`/`Receipt`/`Certificate` pattern — flip `_opts` to `opts` in `sections/2` and add arity-2 heads to all section helpers. Default output stays byte-identical (D-11).

Purpose: All five recipes must handle opts uniformly (API-05 requirement). Invoice and BrandedInvoice currently silently ignore their second argument; after this plan, opts is threaded through so a `formatters:`/`labels:` path becomes available for future use without breaking existing callers.

Output: Two files modified, no new files, no new deps. A `mix ci` pass confirms no new warnings.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-CONTEXT.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize Rendro.Recipes.Invoice.sections/2 opts threading</name>
  <files>
    lib/rendro/recipes/invoice.ex
  </files>
  <read_first>
    - lib/rendro/recipes/invoice.ex — read entire file; locate sections/2 (~line 68, current: `def sections(data, _opts \\ [])`); locate all private section helpers (~lines 111–149) to identify their current arity-1 signatures
    - lib/rendro/recipes/statement.ex lines 203–212 — reference implementation: sections/2 pattern with named opts threaded to header_section/body_section/footer_section
    - lib/rendro/recipes/pagination.ex lines 57–73 — formatter/3 and label_resolver/1 helpers (available for threading real formatter support if trivial; use _opts in helper bodies if non-trivial per D-11)
    - CONTEXT.md D-10 — flip _opts → opts in sections/2; add arity-2 heads to helpers
    - CONTEXT.md D-11 — default output MUST stay byte-identical; if real formatter threading is non-trivial, scope to "accept and forward opts to helpers only" (arity-2 heads with _opts in body)
    - PATTERNS.md §"lib/rendro/recipes/invoice.ex + branded_invoice.ex (MODIFIED)" — exact before/after code pattern
  </read_first>
  <behavior>
    - Test 1: Rendro.Recipes.Invoice.sections(sample_data) returns a list of sections (existing callers unaffected)
    - Test 2: Rendro.Recipes.Invoice.sections(sample_data, []) returns the same result as sections/1 (byte-identical output for default opts)
    - Test 3: Rendro.Recipes.Invoice.sections(sample_data, [unknown_opt: :ignored]) does not crash and returns sections normally
    - Test 4: mix compile --warnings-as-errors exits 0 (no "variable opts is unused" warning)
  </behavior>
  <action>
    In lib/rendro/recipes/invoice.ex:

    1. In sections/2: replace `def sections(data, _opts \\ [])` with `def sections(data, opts \\ [])`.

    2. In the sections/2 body: pass `opts` to each helper call — change `header_section(data)` to `header_section(data, opts)`, `body_section(data)` to `body_section(data, opts)`, `footer_section(data)` to `footer_section(data, opts)`.

    3. For each private helper (header_section, body_section, footer_section, and any sub-helpers they call): add an arity-2 head that accepts `_opts` as the second parameter. The _opts name (underscore prefix) is MANDATORY to avoid --warnings-as-errors failures on unused variables. Keep the existing arity-1 head if callers remain, OR change all call sites consistently if arity-1 heads are no longer needed. Do not change the body of any helper — only the signature.

    Per D-11: if threading real formatter support (Pagination.formatter/3 / label_resolver/1) into invoice body rendering requires more than adding arity-2 heads, stop at arity-2 heads with `_opts`. Default output must be byte-identical.

    The @spec for sections/2 already declares `keyword()` as the second argument type — confirm it and leave it unchanged.

    Do NOT touch any other module in this file. Do NOT add new imports or aliases.
  </action>
  <verify>
    <automated>
      mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "def sections(data, opts" lib/rendro/recipes/invoice.ex` returns the updated signature (not `_opts`)
    - `grep "def sections(data, _opts" lib/rendro/recipes/invoice.ex` returns empty (old signature gone)
    - Every private section helper in invoice.ex has an arity-2 head with `_opts` as second param (grep for `defp.*data, _opts`)
    - `mix compile --warnings-as-errors` exits 0 — no "variable \"opts\" is unused" or "variable \"_opts\" is unused" warning in invoice.ex
    - Running existing invoice render tests (if any) passes unchanged
  </acceptance_criteria>
  <done>Invoice.sections/2 accepts named opts; all helpers have arity-2 heads with _opts; compilation is clean; no behavioral change</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize Rendro.Recipes.BrandedInvoice.sections/2 opts threading</name>
  <files>
    lib/rendro/recipes/branded_invoice.ex
  </files>
  <read_first>
    - lib/rendro/recipes/branded_invoice.ex — read entire file; locate sections/2 (~line 99, current: `def sections(data, _opts \\ [])`); locate all private section helpers (~lines 138–179) to identify arity-1 signatures; note the additional logo_section helper compared to Invoice
    - lib/rendro/recipes/statement.ex lines 203–212 — same reference pattern as Task 1
    - CONTEXT.md D-10/D-11 — same normalization rules; BrandedInvoice has 4 section helpers (logo, header, body, footer) vs Invoice's 3
    - PATTERNS.md §"Change for branded_invoice.ex" — includes logo_section in the threaded call list
  </read_first>
  <behavior>
    - Test 1: Rendro.Recipes.BrandedInvoice.sections(sample_data) returns a list of sections (existing callers unaffected)
    - Test 2: Rendro.Recipes.BrandedInvoice.sections(sample_data, []) returns the same result as sections/1 (byte-identical output)
    - Test 3: sections(sample_data, [unknown_opt: :ignored]) does not crash
    - Test 4: mix compile --warnings-as-errors exits 0 (no unused opts warning)
  </behavior>
  <action>
    In lib/rendro/recipes/branded_invoice.ex, apply the same transformation as Task 1 — but BrandedInvoice has four section helpers (logo_section, header_section, body_section, footer_section):

    1. Change `def sections(data, _opts \\ [])` to `def sections(data, opts \\ [])`.

    2. In sections/2 body: pass opts to all four helpers — logo_section(data, opts), header_section(data, opts), body_section(data, opts), footer_section(data, opts).

    3. For each private helper (logo_section, header_section, body_section, footer_section, and any sub-helpers they call): add an arity-2 head with `_opts` as the second parameter. Keep existing body unchanged.

    Same D-11 constraint: scope to arity-2 heads with _opts if real formatter threading is non-trivial. Default output byte-identical.

    Do NOT touch validate_data!/1 or any other function in the file.
  </action>
  <verify>
    <automated>
      mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "def sections(data, opts" lib/rendro/recipes/branded_invoice.ex` returns the updated signature
    - `grep "def sections(data, _opts" lib/rendro/recipes/branded_invoice.ex` returns empty
    - All four private section helpers (logo, header, body, footer) have arity-2 heads with _opts
    - `mix compile --warnings-as-errors` exits 0
    - Existing branded_invoice render tests (if any) pass unchanged
  </acceptance_criteria>
  <done>BrandedInvoice.sections/2 accepts named opts; all four helpers have arity-2 heads with _opts; compilation clean; no behavioral change</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recipe sections/2 opts keyword list | Caller-supplied keyword list; opts are keyword(), not validated — but D-13 defers NimbleOptions validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03-SC | Tampering | No new package installs | accept | Pure source edits; no dep changes |
| T-78-03-01 | Tampering | Unvalidated opts keyword list | accept | D-13 explicitly defers opts validation; opts are not evaluated as code; helpers receive opts but use _opts (unused) — no execution path changes in this plan. Future validation phase can add NimbleOptions. |

No high-severity threats: this plan threads an opts argument through private helpers; no new user-facing input surface beyond what already exists in sections/2.
</threat_model>

<verification>
After both tasks:

```
mix compile --warnings-as-errors
mix test 2>&1 | tail -5
```

Both must exit 0. Spot-check:
- grep "def sections(data, _opts" lib/rendro/recipes/invoice.ex — returns empty
- grep "def sections(data, _opts" lib/rendro/recipes/branded_invoice.ex — returns empty
- grep "def sections(data, opts" lib/rendro/recipes/invoice.ex — returns the updated line
</verification>

<success_criteria>
- mix compile --warnings-as-errors exits 0
- mix test exits 0 (no existing tests broken by the opts threading)
- Both invoice.ex and branded_invoice.ex have sections/2 with named opts (not _opts)
- All section helpers in both files have arity-2 heads with _opts suffix on unused second param
</success_criteria>

<output>
Create `.planning/phases/78-public-api-surface-definition-cleanup/78-03-SUMMARY.md` when done.
</output>
