---
phase: 78-public-api-surface-definition-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/pdf/cid_font.ex
  - lib/rendro/pdf/font_subsetter.ex
  - lib/rendro/text/bidi.ex
  - lib/rendro/text/shaper.ex
  - lib/rendro/audit.ex
  - lib/rendro/format.ex
  - lib/rendro/sign.ex
  - lib/rendro/protect.ex
  - lib/rendro/metadata.ex
autonomous: true
requirements:
  - API-02
  - API-03

must_haves:
  truths:
    - "HexDocs no longer renders Rendro.PDF.CidFont, Rendro.PDF.FontSubsetter, Rendro.Text.Bidi, Rendro.Text.Shaper, Rendro.Format, or Rendro.Audit (all are @moduledoc false)"
    - "Rendro.Sign redact_opts/1, redact_prepare_opts/1, redact_sign_opts/1, redact_augment_opts/1 are @doc false and do not appear in HexDocs"
    - "Rendro.Protect.redact_opts/2 is @doc false and does not appear in HexDocs"
    - "Rendro.Metadata is no longer an invisible type — it has a real @moduledoc with prose and @type t visible in HexDocs"
    - "mix ci exits 0 (--warnings-as-errors satisfied; no new compilation warnings introduced)"
  artifacts:
    - path: "lib/rendro/pdf/cid_font.ex"
      provides: "@moduledoc false at line 2"
      contains: "@moduledoc false"
    - path: "lib/rendro/pdf/font_subsetter.ex"
      provides: "@moduledoc false at line 2"
      contains: "@moduledoc false"
    - path: "lib/rendro/text/bidi.ex"
      provides: "@moduledoc false at line 2"
      contains: "@moduledoc false"
    - path: "lib/rendro/text/shaper.ex"
      provides: "@moduledoc false at line 2"
      contains: "@moduledoc false"
    - path: "lib/rendro/audit.ex"
      provides: "@moduledoc false replacing existing real moduledoc"
      contains: "@moduledoc false"
    - path: "lib/rendro/format.ex"
      provides: "@moduledoc false replacing existing real moduledoc"
      contains: "@moduledoc false"
    - path: "lib/rendro/sign.ex"
      provides: "@doc false on all four redact_* helpers"
    - path: "lib/rendro/protect.ex"
      provides: "@doc false on redact_opts/2"
    - path: "lib/rendro/metadata.ex"
      provides: "Real @moduledoc prose + tags: [:stable] + @type t visible"
      contains: "@moduledoc \"\"\""
  key_links:
    - from: "lib/rendro.ex:283"
      to: "lib/rendro/metadata.ex"
      via: "@spec metadata(keyword()) :: Metadata.t()"
      pattern: "Metadata\\.t\\(\\)"
    - from: "lib/rendro/section.ex:19"
      to: "lib/rendro/running_content.ex"
      via: "@type content references RunningContent.t()"
      pattern: "RunningContent\\.t\\(\\)"
    - from: "lib/rendro/document.ex:68"
      to: "lib/rendro/embedded_file_registry.ex"
      via: "@type t references EmbeddedFileRegistry.t()"
      pattern: "EmbeddedFileRegistry\\.t\\(\\)"
---

<objective>
Sweep the six accidentally-public engine internals to `@moduledoc false`, apply `@doc false` to the five `redact_*` helpers in Sign and Protect, and flip `Rendro.Metadata` from hidden to documented — closing the invisible-type gap that API-03 exists to fix.

Purpose: Every module left public at 1.0 becomes frozen SemVer surface. This plan excises the modules and helper functions that were never intended for public consumption, and surfaces the one struct (`Metadata`) that IS intended but was incorrectly hidden.

Output: Nine files modified. No new files. The hiding sweep is complete and the Metadata invisible-type gap is closed.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-CONTEXT.md
@.planning/phases/78-public-api-surface-definition-cleanup/78-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply @moduledoc false to six engine internals</name>
  <files>
    lib/rendro/pdf/cid_font.ex,
    lib/rendro/pdf/font_subsetter.ex,
    lib/rendro/text/bidi.ex,
    lib/rendro/text/shaper.ex,
    lib/rendro/audit.ex,
    lib/rendro/format.ex
  </files>
  <read_first>
    - lib/rendro/pdf/cid_font.ex — read current @moduledoc state (may already be @moduledoc false; confirm before editing)
    - lib/rendro/pdf/font_subsetter.ex — same
    - lib/rendro/text/bidi.ex — same
    - lib/rendro/text/shaper.ex — same
    - lib/rendro/audit.ex — currently has a real @moduledoc string (per PATTERNS.md); replace with @moduledoc false
    - lib/rendro/format.ex — currently has a real @moduledoc string; replace with @moduledoc false
    - PATTERNS.md §"Shared Patterns — @moduledoc false" — exact pattern to apply (one line: `@moduledoc false`)
    - CONTEXT.md D-01 — authoritative list of six hide targets
  </read_first>
  <action>
    For each of the six files, read the file first to confirm current @moduledoc state, then apply the change per D-01:

    Replace any existing @moduledoc value (whether `false`, a docstring, or absent) with exactly one line `@moduledoc false` at line 2 of each module (immediately after `defmodule ... do`). Do not change anything else in the module body.

    The six targets are:
    - lib/rendro/pdf/cid_font.ex — @moduledoc false (confirm/set)
    - lib/rendro/pdf/font_subsetter.ex — @moduledoc false (confirm/set)
    - lib/rendro/text/bidi.ex — @moduledoc false (confirm/set)
    - lib/rendro/text/shaper.ex — @moduledoc false (confirm/set)
    - lib/rendro/audit.ex — replace existing real moduledoc string with @moduledoc false
    - lib/rendro/format.ex — replace existing real moduledoc string with @moduledoc false

    Analog pattern: lib/rendro/viewer_evidence/matrix.ex line 2 (`@moduledoc false`).
    Do NOT touch any @type, @callback, @spec, @behaviour, or def in these files.
  </action>
  <verify>
    <automated>
      mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - Running `Code.fetch_docs(Rendro.PDF.CidFont) |> elem(4)` in `iex -S mix` returns `:hidden`
    - Same assertion holds for FontSubsetter, Bidi, Shaper, Format, Audit (all six return module_doc == `:hidden`)
    - `mix compile --warnings-as-errors` exits 0 with no new warnings after the edits
    - Grep confirmation: `grep -l "@moduledoc false" lib/rendro/pdf/cid_font.ex lib/rendro/pdf/font_subsetter.ex lib/rendro/text/bidi.ex lib/rendro/text/shaper.ex lib/rendro/audit.ex lib/rendro/format.ex` lists all six files
    - INVISIBLE-TYPE CROSS-REFERENCE CHECK (per D-02/D-03 reasoning — hiding must not introduce a new invisible-type gap): grep the non-hidden lib/ surface for type references to any of the six hidden modules. All of the following greps must return empty (no matches) in non-hidden files: `grep -rn "FontSubsetter\.t()\|CidFont\.t()\|Format\.t()\|Bidi\.t()\|Shaper\.t()\|Audit\.t()" lib/` — if any public module's @type or @spec references a hidden module's type, that file must also be updated (either hide it too, or extract the type) before this task is complete. Confirm each of the six hidden modules does NOT have its type referenced in any still-public module — mirroring the D-02 reasoning that kept RunningContent/EmbeddedFileRegistry public because their types are load-bearing in public @type declarations.
  </acceptance_criteria>
  <done>All six files contain @moduledoc false, compile clean under --warnings-as-errors, and no still-public module references their types</done>
</task>

<task type="auto">
  <name>Task 2: Apply @doc false to redact_* helpers in Sign and Protect</name>
  <files>
    lib/rendro/sign.ex,
    lib/rendro/protect.ex
  </files>
  <read_first>
    - lib/rendro/sign.ex — read lines 160–230 to locate all four redact_* helpers (redact_opts/1 ~line 174, redact_prepare_opts/1 ~line 177, redact_sign_opts/1 ~line 192, redact_augment_opts/1 ~line 216)
    - lib/rendro/protect.ex — read lines 70–90 to locate redact_opts/2 (~line 78)
    - PATTERNS.md §"@doc false (hide individual function)" — place @doc false immediately before each target def
    - CONTEXT.md D-01 — the exact five function targets
  </read_first>
  <action>
    In lib/rendro/sign.ex: add `@doc false` on the line immediately before each of the four def lines:
    - def redact_opts/1
    - def redact_prepare_opts/1
    - def redact_sign_opts/1
    - def redact_augment_opts/1

    In lib/rendro/protect.ex: add `@doc false` immediately before `def redact_opts/2`.

    Do NOT touch any other def, @doc, or @spec in either file. Do NOT add @doc false to the private (defp) helpers — only the public defs listed above.

    Analog: existing @doc false usages in the codebase (standard Elixir idiom). Pattern from PATTERNS.md §"@doc false (hide individual function)".
  </action>
  <verify>
    <automated>
      mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `Code.fetch_docs(Rendro.Sign)` — the docs list entry for each of {redact_opts/1, redact_prepare_opts/1, redact_sign_opts/1, redact_augment_opts/1} has doc field == `:hidden`
    - `Code.fetch_docs(Rendro.Protect)` — the docs list entry for redact_opts/2 has doc field == `:hidden`
    - `mix compile --warnings-as-errors` exits 0
    - All other public functions in sign.ex and protect.ex remain documented (doc != :hidden)
  </acceptance_criteria>
  <done>Five redact_* helpers carry @doc false; Sign and Protect compile clean; all other public docs intact</done>
</task>

<task type="auto">
  <name>Task 3: Expose Rendro.Metadata with real @moduledoc, tags: [:stable], and documented @type t</name>
  <files>
    lib/rendro/metadata.ex
  </files>
  <read_first>
    - lib/rendro/metadata.ex — read entire file to see current @moduledoc false, existing @type t (~line 14), existing defstruct fields
    - lib/rendro/error.ex — analog: real @moduledoc on a simple struct module (lines 1–7)
    - lib/rendro.ex lines 280–290 — the @spec metadata(keyword()) :: Metadata.t() that makes this module's type load-bearing
    - CONTEXT.md D-07 — the decision: flip @moduledoc false, add real prose, keep @type t unchanged
    - PATTERNS.md §"lib/rendro/metadata.ex (MODIFIED)" — exact before/after pattern including tags: [:stable]
  </read_first>
  <action>
    In lib/rendro/metadata.ex:
    1. Replace the existing `@moduledoc false` at line 2 with a real `@moduledoc` that includes prose describing the module's purpose and a `tags: [:stable]` annotation. The @moduledoc should explain: "Input metadata for a rendered PDF document. Passed to Rendro.metadata/1 to set title, author, creator, and custom key-value pairs in the PDF document info dictionary. All fields are optional. The `custom` field accepts an open map of atom() => term() pairs (additive contract)." Use the keyword-list form: `@moduledoc """...""", tags: [:stable]` (ExDoc 0.40 reads tags from the @moduledoc keyword options).
    2. Keep the existing `@type t` definition and `defstruct` fields COMPLETELY UNCHANGED — do not alter any field names, defaults, or type specs.
    3. Do not add any new fields or functions.

    The module after this edit must have: real @moduledoc prose + tags: [:stable] at the top, then the unchanged @type t, then the unchanged defstruct.

    Analog: lib/rendro/error.ex (real @moduledoc on a simple struct). Pattern in PATTERNS.md §"lib/rendro/metadata.ex (MODIFIED)".
  </action>
  <verify>
    <automated>
      mix compile --warnings-as-errors 2>&amp;1 | grep -E "warning|error" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `lib/rendro/metadata.ex` no longer contains `@moduledoc false` — grep for it must return empty
    - `Code.fetch_docs(Rendro.Metadata) |> elem(4)` returns a map with `"en"` key (module_doc != :hidden and != :none)
    - `Code.fetch_docs(Rendro.Metadata) |> elem(5)` (metadata map) contains `tags: [:stable]`
    - The @type t entry in Code.fetch_docs docs list is present and not hidden
    - `mix compile --warnings-as-errors` exits 0
    - lib/rendro.ex still compiles without modification (Metadata.t() reference resolves)
  </acceptance_criteria>
  <done>Rendro.Metadata has a real @moduledoc with tags: [:stable]; @type t is documented; the invisible-type gap for Rendro.metadata/1 is closed; CI is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → ExDoc compiler | @moduledoc / @doc attribute values are compiler-time strings authored by this project; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01-SC | Tampering | No new package installs in this plan | accept | Pure source edits (attribute changes); no dependency changes; slopcheck not applicable |
| T-78-01-01 | Information Disclosure | @moduledoc false targets | mitigate | Correct mitigation is the hiding itself — this plan performs it; after the sweep, CidFont/FontSubsetter/etc. emit :hidden from Code.fetch_docs/1 and are excluded from HexDocs |
| T-78-01-02 | Information Disclosure | redact_* helpers previously visible | mitigate | @doc false applied in Task 2; helpers still callable internally but excluded from published docs surface |

No high-severity threats: this plan makes only doc-attribute edits to library source. No user input is parsed, no network calls, no auth, no secrets.
</threat_model>

<verification>
Run after all three tasks complete:

```
mix compile --warnings-as-errors
```

Exit 0 required. Then run:

```
mix test --only api_sweep 2>/dev/null || mix test test/rendro/ 2>&1 | tail -5
```

Introspection spot-checks (in iex -S mix):
- elem(Code.fetch_docs(Rendro.PDF.CidFont), 4) == :hidden
- elem(Code.fetch_docs(Rendro.Audit), 4) == :hidden
- elem(Code.fetch_docs(Rendro.Format), 4) == :hidden
- elem(Code.fetch_docs(Rendro.Metadata), 4) != :hidden
</verification>

<success_criteria>
- grep "@moduledoc false" lib/rendro/pdf/cid_font.ex lib/rendro/pdf/font_subsetter.ex lib/rendro/text/bidi.ex lib/rendro/text/shaper.ex lib/rendro/audit.ex lib/rendro/format.ex — all six files listed
- grep "@moduledoc false" lib/rendro/metadata.ex — returns empty (no match)
- grep -c "@doc false" lib/rendro/sign.ex — returns 4 (one per redact_* helper)
- grep -c "@doc false" lib/rendro/protect.ex — returns 1 (redact_opts/2)
- mix compile --warnings-as-errors exits 0
- grep -rn "FontSubsetter\.t()\|CidFont\.t()\|Format\.t()\|Bidi\.t()\|Shaper\.t()\|Audit\.t()" lib/ returns empty (no public module references a hidden module's type)
</success_criteria>

<output>
Create `.planning/phases/78-public-api-surface-definition-cleanup/78-01-SUMMARY.md` when done.
</output>
