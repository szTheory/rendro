---
phase: 127-public-example-catalog-quality-ratchet
plan: 03
type: execute
wave: 3
depends_on: [127-02]
files_modified:
  - .github/workflows/ci.yml
  - test/guardrails/required_checks_contract_test.exs
  - test/rendro/catalog_raster_review_test.exs
  - assets/rendro/catalog.json
  - assets/rendro/catalog/
  - priv/quality/rubric_scores.json
  - .planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
autonomous: true
requirements: [CATALOG-01, CATALOG-03, CATALOG-04]
must_haves:
  truths:
    - "Exactly 32 committed page-one PNGs match the canonical IDs/paths, complete-PDF hashes/page counts, native geometry, and pinned PDFium provenance in catalog.json."
    - "Every current cell has exactly one hash-bound disposition before catalog check can pass; before human review all 32 are explicitly reasoned unscored rather than inferred approved."
    - "The twelve D-11 flagships and bounded multi-page proof images are produced at full readable size outside the public catalog tree for human review."
    - "Pinned PDFium generation/check stays in the graph-disconnected advisory job and never enters deterministic required CI."
    - "Generation and review remain separate: the generator writes artifacts/manifest only, and rubric records are edited deliberately after hashes exist."
  artifacts:
    - path: assets/rendro/catalog.json
      provides: "Versioned ordered 32-cell manifest with complete evidence and unscored projection"
    - path: assets/rendro/catalog
      provides: "Exactly 32 committed page-one PNGs in native geometry"
    - path: priv/quality/rubric_scores.json
      provides: "Exactly 32 initial hash-bound reasoned-unscored dispositions"
    - path: test/rendro/catalog_raster_review_test.exs
      provides: "Bounded external full-size flagship and multipage review output"
    - path: .github/workflows/ci.yml
      provides: "Pinned exact-SHA catalog generation/check and review artifact route"
  key_links:
    - from: assets/rendro/catalog.json
      to: assets/rendro/catalog
      via: "png_path/png_sha256 exact file binding"
      pattern: "png_sha256"
    - from: priv/quality/rubric_scores.json
      to: assets/rendro/catalog.json
      via: "32 exact unscored ID/path/PNG/PDF joins"
      pattern: "catalog_dispositions"
    - from: .github/workflows/ci.yml
      to: mix rendro.catalog.check
      via: "graph-disconnected pinned advisory step"
      pattern: "mix rendro.catalog.check"
---

<objective>
Produce and import the exact pinned catalog evidence, then bind every cell to an explicit initial disposition.

Purpose: turn the tested catalog contract into committed artifacts without hiding the circular fact that dispositions need generated hashes and projections need dispositions.
Output: exact-SHA CI provenance, 32 PNGs plus catalog.json, 32 reasoned-unscored records, a green read-only checker, and full-size review inputs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-PATTERNS.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-02-SUMMARY.md
@dev/rendro/catalog.ex
@test/support/edge_fixtures.ex
@.github/workflows/ci.yml
@test/guardrails/required_checks_contract_test.exs
@priv/pdfium_pin.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the pinned advisory generation and bounded review route</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-02-SUMMARY.md
    - .github/workflows/ci.yml
    - test/guardrails/required_checks_contract_test.exs
    - test/support/edge_fixtures.ex
    - test/support/edge_fixtures_test.exs
    - test/rendro/adapters/pdfium_raster_snapshot_test.exs
    - test/rendro/theme/preset_raster_snapshot_test.exs
    - priv/pdfium_pin.json
  </read_first>
  <files>.github/workflows/ci.yml, test/guardrails/required_checks_contract_test.exs, test/rendro/catalog_raster_review_test.exs</files>
  <behavior>
    - "Only an isolated gsd/phase-127-catalog-bless-<12hex> push may run the catalog write/import staging path; normal advisory runs execute read-only check."
    - "The staged payload has catalog.json, exactly 32 public PNGs, exact SHA/run/PDFium provenance, twelve flagship page-one review PNGs, and exactly four Invoice/Statement first/final multipage proof PNGs."
    - "The advisory job remains graph-disconnected and fail-closed for catalog generation, staging, upload, and check."
    - "Required CI contains no PDFium, catalog gen/check, Node, or review path."
  </behavior>
  <action>Write failing workflow/guardrail assertions and the tagged raster-review test first. In `catalog_raster_review_test.exs`, read the twelve exact D-11 IDs from `Rendro.Catalog.catalog_specs/0`, rerender their complete PDFs, rasterize page 1 at the catalog DPI, require equality with manifest/public PNG hashes, and write copies only to `RENDRO_CATALOG_REVIEW_DIR`. Per D-09, add the bounded proof with the repository's exact existing constructors and stress selector: `Rendro.Test.EdgeFixtures.document(:invoice, :line_items_60_plus)` and `Rendro.Test.EdgeFixtures.document(:statement, :line_items_60_plus)`. Assert byte-deterministic complete rendering and independently assert `page_count > 1` for both families before selecting any pages. Rasterize with pinned PDFium and write only the first and final physical page to these four collision-free names under `RENDRO_CATALOG_REVIEW_DIR`: `invoice_line_items_60_plus_page_first.png`, `invoice_line_items_60_plus_page_final.png`, `statement_line_items_60_plus_page_first.png`, and `statement_line_items_60_plus_page_final.png`; no proof image may be written under `assets/rendro/catalog/`. In `ci.yml`, retain the pinned PDFium install and graph-disconnected advisory boundary, add the isolated `gsd/phase-127-catalog-bless-*` push trigger, guard `mix rendro.catalog.gen` to a well-formed isolated ref, stage catalog.json/tree plus the sixteen review PNGs and a manifest containing GITHUB_SHA/GITHUB_RUN_ID/PDFIUM_VERSION/PDFIUM_SHA256/sorted payload paths, upload with `if-no-files-found: error`, and add ordinary `mix rendro.catalog.check` as a blocking advisory step. Extend required-check guards to prove exact guard, payload cardinalities, no `continue-on-error`, no `needs`, and absence from required CI. Do not change the Phase 126 preset evidence steps except where the trigger list must admit the new isolated ref.</action>
  <acceptance_criteria>
    - Guardrails fail if generation runs on main/PR, any payload path/cardinality/provenance line disappears, or catalog steps become non-blocking/required-lane work.
    - The tagged review test proves `page_count > 1` for both `Rendro.Test.EdgeFixtures.document(:invoice, :line_items_60_plus)` and `Rendro.Test.EdgeFixtures.document(:statement, :line_items_60_plus)` and produces only the exact twelve flagships plus four bounded multipage images under a caller-provided temporary directory.
    - Page-one review bytes match committed/public manifest identity; the only multipage outputs are `invoice_line_items_60_plus_page_first.png`, `invoice_line_items_60_plus_page_final.png`, `statement_line_items_60_plus_page_first.png`, and `statement_line_items_60_plus_page_final.png`, representing each fixture's first and final physical pages.
    - No all-page public tree, source PDF, Node build, server, database, or runtime dependency is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs --max-failures 1 &amp;&amp; review_dir="$(mktemp -d)" &amp;&amp; RENDRO_CATALOG_REVIEW_DIR="$review_dir" mix test test/rendro/catalog_raster_review_test.exs --include raster_snapshot --max-failures 1 &amp;&amp; test -f "$review_dir/invoice_line_items_60_plus_page_first.png" &amp;&amp; test -f "$review_dir/invoice_line_items_60_plus_page_final.png" &amp;&amp; test -f "$review_dir/statement_line_items_60_plus_page_first.png" &amp;&amp; test -f "$review_dir/statement_line_items_60_plus_page_final.png"</automated>
  </verify>
  <done>The project has a fail-closed exact-SHA route for public catalog bytes and bounded human-review evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Generate, bind, regenerate, and verify the complete catalog</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-02-SUMMARY.md
    - dev/rendro/catalog.ex
    - assets/rendro/artifacts.json
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - .github/workflows/ci.yml
    - priv/pdfium_pin.json
  </read_first>
  <files>assets/rendro/catalog.json, assets/rendro/catalog/, priv/quality/rubric_scores.json, .planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md</files>
  <precondition>The isolated catalog workflow from Task 1 is committed, the remote supports the existing GitHub Actions push route, and the branch SHA/ref/run/artifact identity can be recorded in the plan summary.</precondition>
  <action>Use two exact-SHA pinned runs to close the artifact/disposition projection cycle without granting generation authority over reviews. First create and push a unique `gsd/phase-127-catalog-bless-<12hex>` ref at the committed Task-1 SHA, wait for the exact advisory job, verify pin/version/SHA and staged manifest cardinalities, download the artifact, and import only `assets/rendro/catalog.json`, the 32 catalog PNGs, and the external review directory. Verify the imported source manifest has all 32 IDs/hashes/page counts and no launch-gallery diff. Then deliberately populate `catalog_dispositions` in canonical registry order with exactly 32 hash-bound `review_status: "unscored"` records: use `Pending Phase 127 full-size flagship review` for the twelve D-11 cells and `Outside the twelve-cell flagship review budget for Phase 127` for the other twenty, each with current date and exact evidence path/PNG/PDF hashes; do not alter the six legacy scores. Commit this binding, run a second isolated exact-SHA generation so `catalog.json` derives all 32 `Not yet scored` projections from the reviewer-owned records, import only the regenerated manifest/PNG payload if byte identity/provenance/cardinality all pass, and run the read-only catalog check on the same pinned binary. Preserve every isolated ref until Plan 05 cleanup, and record each ref, full SHA, run ID/URL, artifact name, pin identity, imported path list, and `retained_ci_ref:` line in `127-03-SUMMARY.md`. Update Validation rows 127-03-01/02 with measured deterministic/advisory commands and observed runtimes, but leave final Nyquist sign-off pending.</action>
  <acceptance_criteria>
    - The committed tree has exactly 32 PNGs and no PDFs/trailing-page PNGs; every file hash/path/order/page count/dimension matches catalog.json.
    - `catalog_dispositions` has exactly 32 unique records in registry order, twelve pending-review reasons and twenty bounded-unscored reasons, all bound to current artifact identity.
    - Final catalog.json has exactly 32 `unscored`/`Not yet scored` projections and the pinned read-only checker returns success without writes.
    - Both exact CI runs and every retained isolated ref are provenance-recorded; existing launch assets and six legacy scores are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>test "$(find assets/rendro/catalog -type f -name '*.png' | wc -l | tr -d ' ')" = "32" &amp;&amp; test "$(jq '.cells | length' assets/rendro/catalog.json)" = "32" &amp;&amp; test "$(jq '.catalog_dispositions | length' priv/quality/rubric_scores.json)" = "32" &amp;&amp; test "$(jq '[.catalog_dispositions[] | select(.reason == "Pending Phase 127 full-size flagship review")] | length' priv/quality/rubric_scores.json)" = "12" &amp;&amp; test "$(jq '[.catalog_dispositions[] | select(.reason == "Outside the twelve-cell flagship review budget for Phase 127")] | length' priv/quality/rubric_scores.json)" = "20" &amp;&amp; mix test test/rendro/catalog_test.exs test/docs_contract/catalog_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>All 32 public artifacts are pinned, hash-bound, explicitly unscored, and ready for bounded human review.</done>
</task>

</tasks>

## Artifacts this phase produces

New files/paths: `.github/workflows/ci.yml` isolated Phase 127 catalog steps; `test/rendro/catalog_raster_review_test.exs`; `assets/rendro/catalog.json`; 32 files under `assets/rendro/catalog/<family>/<brand>/<preset>-<mode>.png`; ephemeral `tmp/rendro_phase127_review/<catalog-id>_page_1.png` plus `invoice_line_items_60_plus_page_first.png`, `invoice_line_items_60_plus_page_final.png`, `statement_line_items_60_plus_page_first.png`, and `statement_line_items_60_plus_page_final.png`; `catalog_dispositions` content in `priv/quality/rubric_scores.json`.

New environment variable: `RENDRO_CATALOG_REVIEW_DIR` for test-only external review output.

New CI surface: `gsd/phase-127-catalog-bless-*` guarded push path and `phase-127-catalog-bless` artifact; blocking advisory `mix rendro.catalog.check` step.

No new CLI flag or public symbol is added in this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| isolated CI artifact → committed catalog | External runner bytes are imported only after exact SHA/pin/payload verification. |
| generated evidence → initial reviewer records | Human-owned JSON is deliberately bound after hashes exist, never generated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-03 | Tampering | pinned raster artifact import | high | mitigate | Exact branch SHA, PDFium version/SHA, job success, sorted bounded payload, and post-import check. |
| T-127-02 | Tampering | initial disposition binding | high | mitigate | Author after import, exact one-to-one hashes, second projection run, and read-only checker. |
| T-127-04 | Repudiation | initial status projection | high | mitigate | All cells explicitly `unscored`; no passing/approval inference before review. |
| T-127-05 | Denial of service | generated payload | medium | mitigate | Workflow and import gates enforce exactly 32 public PNGs plus bounded 16-image review payload. |
</threat_model>

<verification>
Deterministic contracts run locally; pinned generation/check and review bytes run only through the graph-disconnected advisory route with exact provenance.
</verification>

<success_criteria>
- The complete artifact tree, manifest, and 32 initial dispositions agree exactly.
- Human review inputs are full-size, ordered, and bounded.
- No evidence source, launch asset, legacy score, or reviewer judgment is silently mutated.
</success_criteria>

<output>
Create `.planning/phases/127-public-example-catalog-quality-ratchet/127-03-SUMMARY.md` with exact CI/import/ref provenance when done.
</output>
