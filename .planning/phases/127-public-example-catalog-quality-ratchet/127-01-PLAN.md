---
phase: 127-public-example-catalog-quality-ratchet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - dev/rendro/catalog.ex
  - dev/mix/tasks/rendro/catalog/gen.ex
  - dev/mix/tasks/rendro/catalog/check.ex
  - priv/examples/ticket/aurora-live/ticket.json
  - test/rendro/catalog_test.exs
autonomous: true
requirements: [CATALOG-01, CATALOG-02]
must_haves:
  truths:
    - "CATALOG-01 adjacency: catalog IDs and PNG output paths are unique; duplicate IDs or paths fail before generation."
    - "CATALOG-01 empty: the literal registry contains exactly 32 cells including all six default baselines; empty, null, one-cell, 31-cell, or 33-cell membership fails the contract."
    - "CATALOG-01 ordering: literal registry order is canonical and all manifest output preserves it."
    - "CATALOG-02 boundary: 32 is both the exact planned count and hard ceiling; 31 and 33 fail exact-count validation, and every count above 32 also fails the ceiling guard."
    - "CATALOG-02 precision: membership is an integer count over literal entries with no rounding, tie behavior, generated arithmetic, fixture discovery, or Cartesian product."
    - "Each catalog cell renders a complete deterministic source PDF, while only physical page 1 is designated for the public PNG."
  artifacts:
    - path: dev/rendro/catalog.ex
      provides: "Private dev/test-only catalog registry, deterministic render dispatch, generation, and verification seams"
    - path: dev/mix/tasks/rendro/catalog/gen.ex
      provides: "Deliberate catalog write task"
    - path: dev/mix/tasks/rendro/catalog/check.ex
      provides: "Read-only catalog verification task"
    - path: test/rendro/catalog_test.exs
      provides: "Wave 0 registry, boundary, ordering, render, and error contracts"
  key_links:
    - from: dev/rendro/catalog.ex
      to: priv/examples
      via: "literal fixture_ref values passed through Rendro.Examples.load!/1 and Rendro.ExamplesData.transform/2"
      pattern: "Rendro.Examples.load!"
    - from: dev/rendro/catalog.ex
      to: Rendro.Theme.Presets
      via: "preset/2 followed by explicit document-owned register_fonts/2 before deterministic render"
      pattern: "register_fonts"
    - from: dev/mix/tasks/rendro/catalog/check.ex
      to: dev/rendro/catalog.ex
      via: "Rendro.Catalog.check/1 with parsed --pdfium option"
      pattern: "Rendro.Catalog.check"
  prohibitions:
    - statement: "Generation MUST NOT create, delete, reorder as meaning, or change human-authored disposition judgments, and MUST NOT turn passed: false into true without explicit resolution evidence."
      status: unverified
      flagged: true
---

<objective>
Create the deterministic catalog spine and prove one real cell end-to-end before expanding it to the locked 32-cell registry.

Purpose: establish catalog identity, render dispatch, and bounded membership without touching the launch gallery or widening Rendro's runtime package.
Output: dev/test-only `Rendro.Catalog`, conventional gen/check Mix tasks, the complete literal registry, and fast render-free/deterministic contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-PATTERNS.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
@.planning/phases/126-carryover-polish-dark-mode-legibility-hierarchy-decision-gol/126-05-SUMMARY.md
@lib/rendro/launch_artifacts.ex
@lib/rendro/examples.ex
@lib/rendro/examples_data.ex
@lib/rendro/theme/presets.ex
@priv/pdfium_pin.json
</context>

<interfaces>
Use `Rendro.Examples.load!/1` for safe-relative fixture loading, `Rendro.ExamplesData.transform/2` for the six family transforms, `Rendro.Theme.Presets.preset/2` plus `register_fonts/2` for preset documents, `Rendro.render/2` for deterministic complete PDFs, and `Rendro.Adapters.Pdfium.render/2` only for the pinned advisory page-one raster.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the default Invoice cell from literal identity to deterministic PDF</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
    - lib/rendro/launch_artifacts.ex
    - lib/rendro/examples.ex
    - lib/rendro/examples_data.ex
    - lib/rendro/theme/presets.ex
    - test/rendro/launch_artifacts_test.exs
    - mix.exs
  </read_first>
  <files>mix.exs, dev/rendro/catalog.ex, test/rendro/catalog_test.exs</files>
  <behavior>
    - "The canonical default Invoice entry has ID invoice--default--default--light, fixture_ref invoice/acme-phoenix-saas/invoice.json, and truthful null brand/preset/accent fields."
    - "The entry loads through Rendro.Examples, transforms through Rendro.ExamplesData, builds the Invoice recipe with Theme.default/0, and renders a non-empty deterministic PDF twice with identical SHA-256."
    - "Unsafe fixture or PNG paths are rejected before any filesystem read/write."
  </behavior>
  <action>Start Wave 0 by writing the failing tracer tests in `test/rendro/catalog_test.exs`, then add `dev` to `elixirc_paths(:dev)` and `elixirc_paths(:test)` while leaving production compilation on `lib` only, so D-24 keeps `Rendro.Catalog` out of the Hex runtime. Create `dev/rendro/catalog.ex` with `@moduledoc false`, catalog root/manifest constants, a literal first canonical default-Invoice spec, `catalog_specs/0`, `asset_root/0`, `manifest_path/0`, `source_document_for/1`, and `render_source_pdf/1`. Per D-06/D-07/D-18/D-22, wire that one production path through safe fixture loading, family transform, `Rendro.Recipes.Invoice.document(theme: Rendro.Theme.default())`, and `Rendro.render(deterministic: true)`; retain the complete PDF in memory for hash/page-count evidence and designate only page 1 for later rasterization. Validate lowercase stable ID grammar and both fixture/output paths with `Path.safe_relative/1` before touching files. The research analog lives in `lib/rendro/launch_artifacts.ex`; use the dev-only location because locked D-24 requires the catalog machinery to stay outside the runtime package.</action>
  <acceptance_criteria>
    - The tracer test fails before implementation and passes afterward using the real fixture, recipe, theme, and deterministic renderer.
    - Production compilation remains `lib`-only; dev/test compilation loads `dev/rendro/catalog.ex`.
    - The default manifest semantics are exactly brand/preset/accent null, theme `default`, mode `light`, physical page 1.
    - A traversal-shaped fixture or asset path raises/reports a safe-relative error before any filesystem operation.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs --max-failures 1</automated>
  </verify>
  <done>A real default Invoice fixture reaches a byte-stable complete PDF through the permanent catalog path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the literal registry to the exact canonical 32 cells</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-PATTERNS.md
    - dev/rendro/catalog.ex
    - test/rendro/catalog_test.exs
    - lib/rendro/examples.ex
    - lib/rendro/examples_data.ex
    - lib/rendro/theme/presets.ex
    - priv/examples/ticket/aurora-live/ticket.json
  </read_first>
  <files>dev/rendro/catalog.ex, priv/examples/ticket/aurora-live/ticket.json, test/rendro/catalog_test.exs</files>
  <reversibility rating="costly">D-01's IDs, paths, and exact membership feed Phase 128 URLs and require coordinated artifact/disposition changes to undo.</reversibility>
  <behavior>
    - "The registry order is Invoice, Statement, Receipt, Certificate, Payslip, Ticket; each family begins with its default baseline and every curated pair is light immediately before dark."
    - "Exactly 32 unique IDs and output paths exist; 31/33 fail exact-count validation and every count above 32 also reports the ceiling violation."
    - "Preset-backed documents call preset/2 with the fixture's closed accent and mode, then register_fonts/2 before render; default documents do neither."
    - "Certificate and Ticket report one page, native landscape/A6 geometry is retained, and no complete PDF or trailing-page PNG is designated for commit."
  </behavior>
  <action>Write the RED exact-ID/order, duplicate-ID/path, 31/32/33 boundary, family-dispatch, and truthful-default tests first. Expand `@catalog_specs` to the exact D-01/D-02/D-03/D-20 sequence: Invoice `invoice--default--default--light`, `invoice--northline-logistics--swiss--light`, its dark sibling, `invoice--cedar-mutual--corporate-classic--light`, its dark sibling; Statement baseline, Signal Ledger Minimal-Mono light/dark, Aster Research Fund Editorial light/dark; Receipt baseline, Poppy &amp; Grain Humanist light/dark, Circuit Supply Co. Minimal-Mono light/dark; Certificate baseline, Aster Institute Swiss light/dark, Meridian Arts Fellowship Editorial light/dark; Payslip baseline, Northline Logistics Swiss light/dark, Cedar Mutual Corporate-Classic light/dark; Ticket baseline, Field Notes Conference Minimal-Mono light/dark, The Letterpress Hall Editorial light/dark, Aurora Live Brutalist light/dark. Baselines use `<family>--default--default--light`; every PNG is exactly `assets/rendro/catalog/<family>/<brand>/<preset>-<mode>.png`. Do not call `Examples.list/1`, enumerate files, or derive a cross product. Per D-04 and the resolved living-brand palette decision, add Aurora Live's data-only brand metadata under its existing JSON `brand` object with exact `accent: "#C78600"` (`amber-600`), `slug: aurora-live`, `display_name: Aurora Live`, and `recommended_preset: brutalist`, preserving every historical ticket field. Add finite family dispatch through `Rendro.ExamplesData.transform/2` and each existing recipe, apply `Theme.preset/2` with the exact fixture accent/mode, and then call `Rendro.Theme.Presets.register_fonts/2`. Per D-08/D-19/D-23, validate IDs, modes, literal fixture existence, safe paths, unique destinations, expected order, exact count, ceiling, native dimensions, and fixed-layout page counts with errors naming catalog ID, condition, and a narrow next action.</action>
  <acceptance_criteria>
    - `catalog_specs/0` returns exactly the 32 locked IDs in domain-first/light-dark order with no duplicate path.
    - Mutated 31- and 33-row registries fail exact-count checks; 33 also fails the hard ceiling; membership remains literal integer enumeration.
    - All 26 preset cells use explicit font registration and all six baselines retain truthful null/default fields.
    - Aurora metadata is data-only with exact closed-palette accent `#C78600`, and its unthemed fixture transform/render remains unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs --max-failures 1 &amp;&amp; jq -e '.brand.slug == "aurora-live" and .brand.recommended_preset == "brutalist" and .brand.accent == "#C78600"' priv/examples/ticket/aurora-live/ticket.json</automated>
  </verify>
  <done>The only public membership source is a tested, ordered, exact 32-cell literal registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose deliberate generation and read-only verification tasks</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
    - dev/rendro/catalog.ex
    - test/rendro/catalog_test.exs
    - lib/rendro/launch_artifacts.ex
    - lib/mix/tasks/rendro/launch_artifacts/gen.ex
    - lib/mix/tasks/rendro/launch_artifacts/check.ex
    - priv/pdfium_pin.json
  </read_first>
  <files>dev/rendro/catalog.ex, dev/mix/tasks/rendro/catalog/gen.ex, dev/mix/tasks/rendro/catalog/check.ex, test/rendro/catalog_test.exs</files>
  <behavior>
    - "mix rendro.catalog.gen accepts only --pdfium PATH, writes the catalog tree/manifest, and never writes reviewer records."
    - "mix rendro.catalog.check accepts only --pdfium PATH, accumulates cell-addressed errors, and leaves every tracked file byte-identical."
    - "Each entry records complete-PDF SHA/page count and page-one PNG SHA/dimensions under the pinned renderer identity."
  </behavior>
  <action>Write task-option and read-only/static-validation tests first. Implement `generate/1`, `check/1`, `static_contract_errors/0`, `static_contract_errors/1`, `manifest_shape_errors/1`, `read_manifest!/0`, and deterministic ordered JSON encoding in `Rendro.Catalog`, mirroring the launch-artifact `with_pdfium/2`, SHA-256, IHDR, split-results, and all-errors patterns without its manual/README/guide mutations. Generation must create only `assets/rendro/catalog/` PNGs and `assets/rendro/catalog.json`, render the complete source, store complete-PDF hash/page count, rasterize exactly page 1 at 96 DPI, and never open or write `priv/quality/rubric_scores.json`. Check must recompute complete PDFs and page-one PNGs, compare pin/version/hash/dimensions/page-count/order, report all failures, and remain read-only. Add the two dev Mix tasks with `OptionParser.parse(strict: [pdfium: :string])`, no extra positional args, `Mix.Task.run("app.start")`, nonzero failure exit, and messages that distinguish deliberate generation from verification. Keep `Rendro.Catalog` absent from `priv/public_api.json` by construction.</action>
  <acceptance_criteria>
    - The two exact commands `mix rendro.catalog.gen [--pdfium PATH]` and `mix rendro.catalog.check [--pdfium PATH]` resolve in dev/test; unknown flags/arguments fail.
    - Generate's write set is bounded to catalog PNGs/manifest and contains no quality/sign-off mutation.
    - Check aggregates missing/hash/order/count/renderer/page-count failures and a test proves no writes across its read-only path.
    - No launch-gallery spec, `assets/rendro/artifacts.json`, public API manifest, or runtime dependency changes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/catalog_test.exs --max-failures 1 &amp;&amp; mix help rendro.catalog.gen &gt;/dev/null &amp;&amp; mix help rendro.catalog.check &gt;/dev/null</automated>
  </verify>
  <done>Maintainers have conventional, bounded, separate write and read-only catalog operations.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols: `Rendro.Catalog.asset_root/0`, `manifest_path/0`, `catalog_specs/0`, `source_document_for/1`, `render_source_pdf/1`, `generate/1`, `check/1`, `static_contract_errors/0`, `static_contract_errors/1`, `manifest_shape_errors/1`, `read_manifest!/0`; `Mix.Tasks.Rendro.Catalog.Gen.run/1`; `Mix.Tasks.Rendro.Catalog.Check.run/1`.

CLI flags: `mix rendro.catalog.gen --pdfium PATH`; `mix rendro.catalog.check --pdfium PATH`.

New files/paths: `dev/rendro/catalog.ex`, `dev/mix/tasks/rendro/catalog/gen.ex`, `dev/mix/tasks/rendro/catalog/check.ex`, `test/rendro/catalog_test.exs`; declared generated destinations `assets/rendro/catalog.json` and `assets/rendro/catalog/<family>/<brand>/<preset>-<mode>.png`.

New registry fields: `id`, `family`, `brand`, `preset`, `theme`, `accent`, `mode`, `recipe_module`, `fixture_ref`, `png_path`, `alt`, `caption`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| literal registry → fixture/filesystem | Catalog-owned paths select repository fixtures and output destinations. |
| deterministic PDF → pinned PDFium | Complete deterministic evidence crosses into an advisory rasterizer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-01 | Tampering / Information disclosure | fixture and asset paths | high | mitigate | Validate every literal with `Path.safe_relative/1`, constrain outputs below catalog root, and test traversal rejection before I/O. Path traversal/input validation is canonical security scope; retain this breadcrumb for `$gsd-secure-phase` rather than minting a bespoke prohibition. |
| T-127-03 | Tampering | PDFium evidence boundary | high | mitigate | Enforce existing pin version/SHA provenance and compare exact page-one bytes/dimensions. |
| T-127-05 | Denial of service | registry membership | medium | mitigate | Literal membership plus exact 32 and hard-ceiling tests; no discovery/cross-product path. |
</threat_model>

<verification>
Run the focused catalog test after each task; this plan intentionally stops before pinned artifact generation, which belongs to Wave 3's advisory evidence path.
</verification>

<success_criteria>
- The exact registry, deterministic complete-PDF path, task boundaries, and fail-loud validations exist.
- CATALOG-01/02 edge predicates are all mechanically owned by `catalog_test.exs`.
- Runtime/package purity and launch-gallery isolation remain intact.
</success_criteria>

<output>
Create `.planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md` when done.
</output>
