---
phase: 127-public-example-catalog-quality-ratchet
plan: 02
type: execute
wave: 2
depends_on: [127-01]
files_modified:
  - mix.exs
  - dev/rendro/catalog.ex
  - priv/quality/rubric_scores.json
  - priv/schemas/rubric_scores.schema.json
  - test/docs_contract/catalog_manifest_contract_test.exs
  - test/docs_contract/catalog_quality_contract_test.exs
  - test/docs_contract/rubric_manifest_contract_test.exs
autonomous: true
requirements: [CATALOG-03, CATALOG-04]
must_haves:
  truths:
    - "CATALOG-04 adjacency: duplicate dispositions, duplicate catalog IDs, and more than one disposition per catalog cell fail the exact join."
    - "CATALOG-04 empty: a missing, empty, or null disposition for any current cell fails closed; every cell must have exactly one scored or reasoned-unscored record."
    - "CATALOG-04 ordering: the quality projection follows canonical catalog registry order regardless of reviewer-source ordering."
    - "Freshness is derived only from catalog ID, evidence path, PNG SHA-256, and complete-PDF SHA-256; it is never reviewer-entered state."
    - "A scored passed:false disposition is valid evidence and cannot become passed:true without superseded evidence plus a non-empty behavioral resolution reference."
    - "The manifest exposes only Scored — passes current rubric, Scored — needs work, or Not yet scored; missing/stale is a build failure rather than a publishable status."
    - "Per D-10, every cell with page_count > 1 has generated preview_copy exactly `Preview: page 1 of <page_count>` using its integer page count, while every one-page cell has null; the field is never reviewer-authored and never presents a page-one PNG as the complete document."
    - "Per D-25, every dark cell exposes boundary_disclosure exactly as `Screen-oriented; not a print, accessibility, PDF/UA, or WCAG claim.` while every light cell exposes null; this field is derived from mode and remains independent of quality status."
  assumptions:
    - statement: "CATALOG-03 unclassified probe remains flagged: planner interpretation is that D-18 through D-23 manifest/path assertions are the observable contract, pending verifier confirmation that they exhaust the generic unclassified probe."
      status: unverified
      flagged: true
  artifacts:
    - path: test/docs_contract/catalog_manifest_contract_test.exs
      provides: "Wave 0 consumer manifest, D-10 derived preview copy, D-25 mode-derived boundary disclosure, path, package, and sibling-isolation contract"
    - path: test/docs_contract/catalog_quality_contract_test.exs
      provides: "Wave 0 exact disposition/freshness/projection/transition contract"
    - path: priv/schemas/rubric_scores.schema.json
      provides: "Additive scored/unscored catalog disposition union"
    - path: dev/rendro/catalog.ex
      provides: "Static manifest and quality-join validation"
  key_links:
    - from: assets/rendro/catalog.json
      to: priv/quality/rubric_scores.json
      via: "exact ID/path/PNG-hash/PDF-hash join computed by Rendro.Catalog"
      pattern: "catalog_dispositions"
    - from: priv/schemas/rubric_scores.schema.json
      to: test/docs_contract/catalog_quality_contract_test.exs
      via: "JSV validates both scored and unscored branches"
      pattern: "review_status"
    - from: dev/rendro/catalog.ex
      to: assets/rendro/catalog.json
      via: "mode-derived boundary_disclosure is emitted beside, never inside or inferred from, the three-state quality projection"
      pattern: "boundary_disclosure"
    - from: dev/rendro/catalog.ex
      to: assets/rendro/catalog.json
      via: "page-count-derived preview_copy is emitted during manifest generation independently of copy fields, mode disclosure, reviewer data, and quality projection"
      pattern: "preview_copy"
  prohibitions:
    - statement: "Status projection and documentation MUST NOT present unscored or passed: false cells as approved, and MUST NOT imply PDF/UA, WCAG, arbitrary print, all-page viewer, or production-readiness guarantees beyond evidence."
      status: unverified
      flagged: true
---

<objective>
Define the consumer manifest and exact human-disposition relation before any pinned artifact is committed.

Purpose: make catalog metadata and quality freshness deterministic, additive, and fail-closed while preserving legacy rubric scores.
Output: manifest/package contracts, the additive disposition schema, synthetic join tests, and `Rendro.Catalog` validation/projection logic.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-PATTERNS.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
@.planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md
@dev/rendro/catalog.ex
@assets/rendro/artifacts.json
@priv/quality/rubric_scores.json
@priv/schemas/rubric_scores.schema.json
@test/docs_contract/rubric_manifest_contract_test.exs
@mix.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the versioned consumer manifest and package boundary</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-PATTERNS.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md
    - dev/rendro/catalog.ex
    - assets/rendro/artifacts.json
    - priv/public_api.json
    - mix.exs
    - test/docs_contract/public_api_contract_test.exs
  </read_first>
  <files>dev/rendro/catalog.ex, mix.exs, test/docs_contract/catalog_manifest_contract_test.exs</files>
  <reversibility rating="costly">D-21's manifest fields become Phase 128's static consumer contract and coordinated consumers must change if renamed.</reversibility>
  <behavior>
    - "Top-level metadata is versioned and every ordered cell contains D-21's identity, render, geometry, copy, and quality-projection fields."
    - "Default cells carry null brand/preset/accent plus theme default/mode light; preset cells carry real brand/preset/accent, theme preset, and mode."
    - "Catalog paths are safe, domain/brand organized, unique, and exactly match ID metadata."
    - "Per D-10, every generated multi-page cell has preview_copy exactly Preview: page 1 of its integer page_count, every one-page cell has null, and neither mode nor quality status can change it."
    - "Per D-25, dark cells derive the exact screen-oriented boundary disclosure and light cells derive null, independent of scored/unscored status."
    - "The launch gallery and artifacts.json remain byte-isolated; catalog sources/assets are excluded from Hex package contents and public API discovery."
  </behavior>
  <action>Start the CATALOG-03 Wave 0 file with RED synthetic manifest tests. Extend `Rendro.Catalog` ordered encoding and `manifest_shape_errors/1` so `catalog.json` has `schema_version`, `generated_by`, pinned `renderer`, and ordered `cells`; require each cell's `id`, `family`, `brand`, `preset`, `theme`, `accent`, `mode`, `recipe_module`, `fixture_ref`, `png_path`, `png_sha256`, `source_pdf_sha256`, `page`, `page_count`, `dpi`, `width_px`, `height_px`, `renderer_kind`, `renderer_version`, `alt`, `caption`, `preview_copy`, `boundary_disclosure`, and `quality`. Per D-08/D-10/D-19/D-21/D-22/D-23, enforce physical page 1, positive page count, native geometry, concrete non-duplicative alt/caption, exact safe path grammar, truthful nulls, lowercase SHA digests, canonical order, and actionable ID-addressed errors. Derive `preview_copy` during manifest generation only from `page_count`: when `page_count > 1`, emit exactly `Preview: page 1 of <page_count>` using the integer value; when `page_count == 1`, emit JSON null. Keep `preview_copy` distinct from `caption`, `alt`, `boundary_disclosure`, reviewer-authored data, and quality status, and do not allow mode or quality changes to affect it. Add positive contract assertions over every cell plus mutation/negative cases that reject a missing field, null on a multi-page cell, non-null on a one-page cell, a wrong page number/count, altered wording, and wording that presents a multi-page page-one PNG as the complete document; every error names the catalog ID and correction. Per D-25, derive `boundary_disclosure` only from `mode`: dark cells carry the exact value `Screen-oriented; not a print, accessibility, PDF/UA, or WCAG claim.` and light cells carry JSON null. Keep `boundary_disclosure` as a sibling of `quality`, never a quality/approval input or reviewer-authored rubric field. Add focused positive and mutation tests proving every dark cell has the exact sentence, every light cell is null, status changes cannot alter disclosure, and malformed/swapped/missing values fail with the catalog ID and correction action. Represent preset provenance as `theme: "preset"` plus the canonical preset string; do not expose raw fixtures, document structs, font mechanics, rubric justifications, caches, or implementation options. Change `mix.exs` package files from the broad `assets/rendro` directory to the existing launch artifacts only (`assets/rendro/artifacts.json`, `assets/rendro/gallery`, `assets/rendro/manual.pdf`) so D-24 excludes the new catalog tree/manifest while ExDoc's repository asset copy-through remains available in dev. Test that `Rendro.Catalog` is not generated into `priv/public_api.json`, `mix hex.build` excludes all catalog paths, and the eleven launch IDs/paths remain unchanged.</action>
  <acceptance_criteria>
    - Synthetic valid manifests pass; missing/wrong/null/path/order/hash/geometry/copy fields produce precise cell-addressed failures.
    - Manifest output key and cell order is deterministic and canonical.
    - Every multi-page cell's `preview_copy` is exactly `Preview: page 1 of <page_count>`, every one-page cell's value is null, and mutations prove missing/wrong numbers or complete-document wording fail independently of alt, caption, disclosure, mode, and quality.
    - Every dark cell has the exact D-25 `boundary_disclosure`, every light cell has null, and changing `quality.status` never changes or substitutes for the disclosure.
    - `mix hex.build` retains the existing launch assets but excludes `dev/**`, `assets/rendro/catalog.json`, `assets/rendro/catalog/**`, `priv/quality/**`, and `priv/schemas/rubric_scores.schema.json`.
    - The public API manifest and legacy `assets/rendro/artifacts.json` remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/catalog_manifest_contract_test.exs --max-failures 1 &amp;&amp; mix hex.build &gt;/dev/null</automated>
  </verify>
  <done>The catalog has a deterministic, consumer-focused, package-excluded manifest contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the exact scored-or-unscored quality join</name>
  <read_first>
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-CONTEXT.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-RESEARCH.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-VALIDATION.md
    - .planning/phases/127-public-example-catalog-quality-ratchet/127-01-SUMMARY.md
    - dev/rendro/catalog.ex
    - priv/quality/rubric_scores.json
    - priv/schemas/rubric_scores.schema.json
    - test/docs_contract/rubric_manifest_contract_test.exs
  </read_first>
  <files>dev/rendro/catalog.ex, priv/quality/rubric_scores.json, priv/schemas/rubric_scores.schema.json, test/docs_contract/catalog_quality_contract_test.exs, test/docs_contract/rubric_manifest_contract_test.exs</files>
  <reversibility rating="costly">D-12's additive disposition shape and hash identity become durable reviewer and Phase 128 inputs.</reversibility>
  <behavior>
    - "A catalog cell joins to exactly one scored or unscored disposition by ID/path/PNG/PDF identity."
    - "Missing, empty, duplicate, orphan, stale, or null dispositions fail; reviewer source order does not affect canonical manifest projection order."
    - "Scored verdicts recompute hierarchy=5, every other core>=4, and both gates true; passed:false remains valid."
    - "A false-to-true transition fails unless supersedes_evidence_ref and resolution_ref are both concrete and non-empty."
    - "Generation never creates/edits dispositions; the join only reads reviewer-owned data."
  </behavior>
  <action>Write the Wave 0 schema and synthetic join tests before implementation, including missing/empty/null, duplicate cell/disposition, orphan, stale path/PNG/PDF hash, reviewer-order reversal, threshold near misses, valid passed:false, invalid false-to-true, and projection-drift cases. Extend `rubric_scores.schema.json` additively with `catalog_dispositions` and a `oneOf` union keyed by `review_status: scored | unscored`. Require common `catalog_id`, `family`, nullable `brand`/`preset`, `mode`, `evidence_ref`, `png_sha256`, `source_pdf_sha256`, and `recorded_at`; scored requires six existing dimensions, two gates, recomputed `passed`, `signed_off_by`, `signed_off_at`, and bounded `justifications`; unscored requires a non-empty `reason` and no invented dimensions/gates/passed. Add optional `supersedes_evidence_ref` and `resolution_ref` only for evidence transitions. Add an empty `catalog_dispositions` array to the current rubric file without changing any existing `scores` bytes/values, then extend the legacy rubric contract to assert its six existing records and arithmetic remain intact. Implement `quality_contract_errors/2` and ordered quality projection in `Rendro.Catalog`: `passes`/`Scored — passes current rubric`, `needs_work`/`Scored — needs work`, or `unscored`/`Not yet scored`. Missing/stale has no projection and must fail check. Generation may read dispositions solely to derive projection but must never write the quality file.</action>
  <acceptance_criteria>
    - Both schema union branches validate positive fixtures and reject missing branch-specific identity/judgment fields.
    - Synthetic exact joins fail for every missing/duplicate/orphan/stale permutation with catalog ID and next action.
    - Reversing disposition source order yields identical registry-ordered projection.
    - All six legacy scores, thresholds, evidence paths, and passed arithmetic remain unchanged and green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/catalog_quality_contract_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1 &amp;&amp; jq -e '.catalog_dispositions == [] and (.scores | length == 6)' priv/quality/rubric_scores.json</automated>
  </verify>
  <done>The machine contract can distinguish valid human failure, explicit unscored state, and fail-closed missing/stale evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols: `Rendro.Catalog.quality_contract_errors/2` and ordered internal quality-projection/manifest encoders.

New manifest fields: top-level `schema_version`, `generated_by`, `renderer`, `cells`; per-cell `id`, `family`, `brand`, `preset`, `theme`, `accent`, `mode`, `recipe_module`, `fixture_ref`, `png_path`, `png_sha256`, `source_pdf_sha256`, `page`, `page_count`, `dpi`, `width_px`, `height_px`, `renderer_kind`, `renderer_version`, `alt`, `caption`, page-count-derived `preview_copy`, mode-derived `boundary_disclosure`, and independent `quality`; quality `status`, `label`. Multi-page `preview_copy` is exactly `Preview: page 1 of <page_count>` and one-page `preview_copy` is null. Dark `boundary_disclosure` is exactly `Screen-oriented; not a print, accessibility, PDF/UA, or WCAG claim.` and light is null.

New rubric fields: `catalog_dispositions`; common disposition `catalog_id`, `family`, `brand`, `preset`, `mode`, `evidence_ref`, `png_sha256`, `source_pdf_sha256`, `recorded_at`, `review_status`; scored `dimension_scores`, `gate_results`, `passed`, `signed_off_by`, `signed_off_at`, `justifications`, optional `supersedes_evidence_ref`, `resolution_ref`; unscored `reason`.

New files: `test/docs_contract/catalog_manifest_contract_test.exs`, `test/docs_contract/catalog_quality_contract_test.exs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated manifest → reviewer-owned rubric | Artifact identity is joined to human judgment without granting the generator write authority. |
| rubric/manifest → Phase 128 consumer | Only a derived three-state projection may cross into public static metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-127-02 | Tampering | artifact/disposition relation | high | mitigate | Exact one-to-one ID/path/PNG/PDF join, orphan/duplicate rejection, and derived freshness. |
| T-127-04 | Repudiation | quality projection and boundary claims | high | mitigate | Recompute thresholds, preserve passed:false, fixed three-state labels, hash-bound provenance, and exact mode-derived D-25 disclosure tests kept independent from approval state. |
| T-127-01 | Tampering / Information disclosure | manifest paths | high | mitigate | Validate safe-relative grammar before any asset/fixture use; canonical traversal/input validation remains routed to `$gsd-secure-phase`. |
</threat_model>

<verification>
Run both new Wave 0 contract files plus the legacy rubric contract; no PDFium is required in this wave.
</verification>

<success_criteria>
- CATALOG-03 metadata and CATALOG-04 disposition/freshness semantics are executable tests.
- The checked-in rubric remains legacy-compatible and explicitly has no catalog judgments until evidence exists.
- No status can be inferred as approval from absence, staleness, or passed:false.
</success_criteria>

<output>
Create `.planning/phases/127-public-example-catalog-quality-ratchet/127-02-SUMMARY.md` when done.
</output>
