---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/adapters/phoenix.ex
  - lib/rendro/error.ex
  - examples/phoenix_example/mix.exs
  - examples/phoenix_example/config/config.exs
  - examples/phoenix_example/lib/phoenix_example/application.ex
  - examples/phoenix_example/lib/phoenix_example_web.ex
  - examples/phoenix_example/lib/phoenix_example_web/endpoint.ex
  - examples/phoenix_example/lib/phoenix_example_web/router.ex
  - examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex
autonomous: true
requirements: [ADPT-01, ADPT-02, ADPT-03, OBS-03, QUAL-03]
must_haves:
  truths:
    - "Rendro.Adapters.Phoenix functions raise explicit descriptive errors if Plug/Phoenix are missing."
    - "Render pipeline errors in the Phoenix adapter return a formatted structured text response instead of crashing with inspect(reason)."
    - "The examples/phoenix_example application compiles and boots a Bandit web server on port 4000."
    - "Accessing the example app's routes successfully triggers a PDF download/preview."
  artifacts:
    - path: "lib/rendro/adapters/phoenix.ex"
      provides: "Guarded adapter logic for Plug/Phoenix"
    - path: "lib/rendro/error.ex"
      provides: "String.Chars protocol implementation for Rendro.Error"
    - path: "examples/phoenix_example/mix.exs"
      provides: "Dependencies for Bandit and Phoenix"
    - path: "examples/phoenix_example/config/config.exs"
      provides: "Basic Phoenix Endpoint configuration"
    - path: "examples/phoenix_example/lib/phoenix_example_web/router.ex"
      provides: "Routing for the PDF download and preview actions"
  key_links:
    - from: "lib/rendro/adapters/phoenix.ex"
      to: "lib/rendro/error.ex"
      via: "String.Chars to_string/1 conversion for errors"
    - from: "examples/phoenix_example/lib/phoenix_example_web/router.ex"
      to: "examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex"
      via: "GET routes mapped to :download and :preview"
---

<objective>
Harden the Phoenix adapter to make it truly optional with conditional compilation, implement structured text error responses, and complete the Phoenix example app skeleton so it successfully compiles and runs with Bandit.

Purpose: Fixes implicit compilation errors when Plug is missing, improves developer experience by formatting error structs in HTTP responses instead of leaking internal atoms, and provides a working reference implementation for Phoenix users.
Output: Guarded `phoenix.ex`, `String.Chars` implemented for `Rendro.Error`, and a fully bootable `phoenix_example` app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-phoenix-adapter-hardening/07-PATTERNS.md
@.planning/phases/07-phoenix-adapter-hardening/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard Phoenix Adapter and Implement String.Chars for Error</name>
  <files>lib/rendro/adapters/phoenix.ex, lib/rendro/error.ex</files>
  <action>
    1. In `lib/rendro/error.ex`, implement the `String.Chars` protocol for `Rendro.Error` at the bottom of the file (see `07-PATTERNS.md`). The `to_string/1` function must return a nicely formatted multiline string displaying what, where, why, and next.
    2. In `lib/rendro/adapters/phoenix.ex`, conditionally compile the real module implementation using `if Code.ensure_loaded?(Plug.Conn) do ... else ... end`.
    3. In the `else` block, define a stub `Rendro.Adapters.Phoenix` module where `render_pdf/3` and `preview_pdf/2` raise a `RuntimeError` explaining that `:plug` and `:phoenix` are required.
    4. In the `do` block (the real implementation), update `render_pdf` and `preview_pdf` error handling. Instead of `"PDF Rendering Error: #{error.reason}"`, pattern match on `{:error, %Rendro.Error{} = error}`, set the content type to `text/plain`, and `send_resp(conn, 500, to_string(error))`.
  </action>
  <verify>
    <automated>mix test --only adapter_phoenix</automated>
  </verify>
  <done>Phoenix adapter uses Code.ensure_loaded? and formats Rendro.Error nicely using to_string/1.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Example App Base and Dependencies</name>
  <files>examples/phoenix_example/mix.exs, examples/phoenix_example/config/config.exs, examples/phoenix_example/lib/phoenix_example/application.ex</files>
  <action>
    1. Update `examples/phoenix_example/mix.exs` to include `{:phoenix, "~> 1.7"}`, `{:plug, "~> 1.14"}`, `{:bandit, "~> 1.0"}` in deps. Ensure `extra_applications` includes `[:logger]`, and set `mod: {PhoenixExample.Application, []}` in `application/0`.
    2. Create `examples/phoenix_example/config/config.exs`. Configure `PhoenixExampleWeb.Endpoint` with `url: [host: "localhost"]`, `adapter: Bandit.PhoenixAdapter`, and `render_errors: [formats: [json: PhoenixExampleWeb.ErrorJSON], layout: false]`. Configure `:logger` if necessary.
    3. Create `examples/phoenix_example/lib/phoenix_example/application.ex` defining `PhoenixExample.Application` (using `Application`). Its `start/2` function should start a supervisor tree containing `PhoenixExampleWeb.Endpoint`.
  </action>
  <verify>
    <automated>cd examples/phoenix_example && mix deps.get && mix compile --force</automated>
  </verify>
  <done>Example app has correct dependencies, config, and an application module.</done>
</task>

<task type="auto">
  <name>Task 3: Complete Example App Web Skeleton</name>
  <files>examples/phoenix_example/lib/phoenix_example_web.ex, examples/phoenix_example/lib/phoenix_example_web/endpoint.ex, examples/phoenix_example/lib/phoenix_example_web/router.ex, examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex</files>
  <action>
    1. Create `examples/phoenix_example/lib/phoenix_example_web.ex`. It should define a `controller` macro that imports `Plug.Conn` and `Phoenix.Controller`, and a `router` macro that imports `Plug.Conn` and `Phoenix.Router`.
    2. Create `examples/phoenix_example/lib/phoenix_example_web/endpoint.ex`. Define `PhoenixExampleWeb.Endpoint` using `Phoenix.Endpoint, otp_app: :phoenix_example`. Add basic plugs: `Plug.RequestId`, `Plug.Telemetry`, `Plug.Parsers`, and `plug PhoenixExampleWeb.Router`.
    3. Create `examples/phoenix_example/lib/phoenix_example_web/router.ex`. Define `PhoenixExampleWeb.Router` using `PhoenixExampleWeb, :router`. Add a pipeline `:api` with `plug :accepts, ["json"]`. Add routes `get "/download", PDFController, :download` and `get "/preview", PDFController, :preview`.
    4. Ensure `examples/phoenix_example/lib/phoenix_example_web/controllers/pdf_controller.ex` exists and properly returns PDFs (it mostly does, just ensure it compiles with the new setup).
  </action>
  <verify>
    <automated>cd examples/phoenix_example && mix compile</automated>
  </verify>
  <done>The Phoenix web skeleton is fully wired, and the application compiles successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> HTTP Adapter | Phoenix adapter processes HTTP requests and renders dynamic PDF content based on potentially untrusted input documents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `Rendro.Adapters.Phoenix` | mitigate | Ensure error responses returned by `to_string(error)` do not leak sensitive system paths or env vars; return standardized Rendro struct fields only. |
| T-07-02 | Denial of Service | `phoenix_example` App | accept | Example app is for demonstration/local use only. Real environments must set policies (Phase 08) for resource bounds. |
</threat_model>

<verification>
1. Run `mix test` in the root repository to verify `lib/rendro/adapters/phoenix.ex` behavior when plugged.
2. Ensure `examples/phoenix_example` can be compiled and started via `cd examples/phoenix_example && mix deps.get && mix phx.server` without missing module errors.
</verification>

<success_criteria>
- The Phoenix adapter no longer forces compilation of Plug components if they are not in the project deps.
- Structured `%Rendro.Error{}` responses are sent to the client as text, matching `String.Chars` implementation.
- The `examples/phoenix_example` project builds cleanly and serves the download/preview routes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-phoenix-adapter-hardening/07-01-SUMMARY.md`
</output>
