---
phase: 110-test-concurrency-determinism-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/docs_contract.ex
  - test/support/docs_contract_test.exs
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - Docs contracts are statically verified before evaluation to ensure they do not write to the file system.
    - Contract snippets attempting to invoke `System.cmd` or `Mix.Task` raise an error without executing.
    - Contract snippets attempting to invoke `File` mutation functions raise an error without executing.
    - All docs contract tests can be safely run concurrently with `async: true`.
  artifacts:
    - path: test/support/docs_contract.ex
      provides: Safe AST-checked `DocsContract.evaluate!/2`
    - path: test/support/docs_contract_test.exs
      provides: Validation tests ensuring side effects are blocked
  key_links:
    - from: test/support/docs_contract.ex
      to: Code.eval_quoted/3
      via: Macro.prewalk/2 AST validation
      pattern: Macro\.prewalk
---

<objective>
Implement structural safety guarantees in `DocsContract.evaluate!/2` and convert all contract tests to run concurrently.

Purpose: Contract tests evaluate Elixir code from markdown documentation. To maximize concurrency, we must enforce a read-only guarantee so that parallel tests do not introduce filesystem race conditions.
Output: A secured `DocsContract` module, validation tests for the evaluator, and all contract tests converted to `async: true`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-test-concurrency-determinism-cleanup/110-CONTEXT.md
@.planning/phases/110-test-concurrency-determinism-cleanup/110-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Secure DocsContract evaluation with AST checking</name>
  <files>test/support/docs_contract.ex, test/support/docs_contract_test.exs</files>
  <behavior>
    - Test 1: Evaluating standard valid Elixir assertions works normally.
    - Test 2: Evaluating a snippet containing `File.write`, `File.rm`, etc. raises a specific error before execution.
    - Test 3: Evaluating a snippet containing `System.cmd` raises an error before execution.
    - Test 4: Evaluating a snippet containing `Mix.Task.run` raises an error before execution.
  </behavior>
  <action>
    Create `test/support/docs_contract_test.exs` to verify write-blocking behavior. 
    Refactor `DocsContract.evaluate!/2` in `test/support/docs_contract.ex`:
    - Use `Code.string_to_quoted!` to generate an AST.
    - Use `Macro.prewalk/2` to traverse the AST and raise an error if any of the following nodes are detected:
      - `File` operations (`write`, `write!`, `rm`, `rm!`, `rm_rf`, `mkdir`, `mkdir!`, `cp`, `cp!`)
      - `System` operations (`cmd`)
      - `Mix.Task` operations (`run`, `clear`)
    - After traversal, run the AST safely using `Code.eval_quoted/3`.
  </action>
  <verify>
    <automated>mix test test/support/docs_contract_test.exs</automated>
  </verify>
  <done>The DocsContract evaluator statically rejects side-effects via AST checks, allowing concurrent test execution.</done>
</task>

<task type="auto">
  <name>Task 2: Maximize concurrency for docs contract tests</name>
  <files>test/docs_contract/*_test.exs</files>
  <action>
    Now that `DocsContract` guarantees read-only evaluation, recursively search all test files in the `test/docs_contract/` directory and replace any instance of `use ExUnit.Case, async: false` with `use ExUnit.Case, async: true`.
    This includes `IntegrationsContractTest` whose ETS table usage (`:rendro_threadline_calls`) has been validated as PID-keyed and concurrent-safe.
  </action>
  <verify>
    <automated>grep -v 'async: true' test/docs_contract/*_test.exs | grep 'async: false' | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <done>All test files in `test/docs_contract/` declare `async: true`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Test Code → Docs Evaluator | Untrusted AST code strings are loaded from static files and evaluated dynamically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Elevation of Privilege | DocsContract.evaluate!/2 | mitigate | Enforce AST pre-walk to deny OS-level and filesystem writes before triggering `Code.eval_quoted`. |
</threat_model>

<verification>
- `mix test test/docs_contract/` passes cleanly with no deadlocks or race conditions.
</verification>

<success_criteria>
- Docs contract evaluation statically blocks `File`, `System.cmd`, and `Mix.Task` mutations.
- 10+ contract tests are safely parallelized.
</success_criteria>

<output>
Create `.planning/phases/110-test-concurrency-determinism-cleanup/110-01-SUMMARY.md` when done
</output>
