---
phase: 98
plan: 03
type: execute
wave: 3
depends_on:
  - 98-02
files_modified:
  - test/rendro/integration/outlines_integration_test.exs
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
must_haves:
  truths:
    - Zero human intervention required to verify PDF outlines.
    - E2E test verifies hierarchical tree structure in the rendered PDF binary.
    - E2E test verifies UTF-16BE encoding of non-Latin characters in the rendered PDF binary.
    - E2E test verifies outline destinations correctly point to page object references.
  artifacts:
    - path: test/rendro/integration/outlines_integration_test.exs
      provides: e2e binary verification of outlines
  key_links: []
---

<objective>
Automate the visual/human verification of PDF outlines by introducing an end-to-end integration test that programmatically asserts the correctness of the generated PDF binary.

Purpose: Shift-left verification to CI and achieve a 0-human-intervention testing pipeline for Document Outlines.
Output: A new integration test file `test/rendro/integration/outlines_integration_test.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add E2E Outlines Integration Test</name>
  <files>test/rendro/integration/outlines_integration_test.exs</files>
  <behavior>
    - The test constructs a complete document with multiple pages.
    - It uses blocks with `outline: true` or `outline: "Non-Latin ページ"` nested at different levels.
    - It runs the document through `Rendro.render/1` to get the final PDF binary.
    - It performs regex or string inclusion assertions against the binary to ensure `/Outlines`, `/First`, `/Last`, `/Parent`, `/Next` are present and correctly linked.
    - It ensures the UTF-16BE hex string (with BOM) corresponding to the non-Latin text is correctly injected.
    - It ensures the `/Dest` arrays map to valid internal object references (e.g., `[{:ref, X, Y}, :XYZ, ...]`).
  </behavior>
  <action>
    Create `test/rendro/integration/outlines_integration_test.exs`:
    - Setup an `ExUnit.Case`.
    - Build a multi-page document with nested outline headers (including Japanese characters and emoji).
    - Render the document: `{:ok, pdf} = Rendro.render(doc)`.
    - Assert `pdf =~ "/Type /Outlines"`.
    - Verify non-Latin encoding: Compute the UTF-16BE hex string manually in the test to assert against what the engine generated.
    - Assert that the nested items have `/Parent` and `/First`/`/Last` structures.
    - Assert that destination page references correlate to valid allocated `/Page` objects.
  </action>
  <verify>
    <automated>mix test test/rendro/integration/outlines_integration_test.exs</automated>
  </verify>
  <done>The automated E2E test runs successfully and proves structural correctness of the PDF outlines without human intervention.</done>
</task>

</tasks>

<verification>
Ensure `mix test` runs and passes, validating that the test effectively guards against regression in outline serialization, encoding, and navigation targets.
</verification>

<success_criteria>
The human verification steps outlined in `98-VERIFICATION.md` are completely superseded by the new automated tests.
</success_criteria>

<output>
Create `.planning/phases/98-03-SUMMARY.md` when done
</output>
