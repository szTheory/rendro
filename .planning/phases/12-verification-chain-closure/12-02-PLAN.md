---
phase: "12"
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - lib/mix/tasks/verify.ex
  - test/mix/tasks/verify_test.exs
autonomous: true
requirements:
  - QUAL-01
  - QUAL-05
must_haves:
  truths:
    - "`mix verify` runs deterministic and advisory lanes to completion before exiting"
    - "deterministic-lane failures are still surfaced as failures without suppressing advisory-lane output"
    - "verification output ends with a combined pass/fail summary instead of failing fast inside the first lane"
  artifacts:
    - path: lib/mix/tasks/verify.ex
      provides: "Verification-lane sequencing, reporting, and aggregated exit semantics"
    - path: test/mix/tasks/verify_test.exs
      provides: "Regression coverage for lane completion and aggregated failure reporting"
  key_links:
    - from: lib/mix/tasks/verify.ex
      to: test/mix/tasks/verify_test.exs
      via: "lane completion regression coverage"
      pattern: "DETERMINISTIC|ADVISORY|VERIFICATION COMPLETE"
---

<objective>
Refactor `mix verify` so it completes deterministic and advisory verification lanes end-to-end, reports all outcomes clearly, and exits only after the full suite has run.

Purpose: Close `INT-VERIFY-LANES` and make `QUAL-05` truthful at the command boundary.
Output: An updated verification task plus regression tests that prove lane completion and aggregated exit semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-verification-chain-closure/12-RESEARCH.md
@.planning/phases/12-verification-chain-closure/12-VALIDATION.md
@lib/mix/tasks/verify.ex
@mix.exs
@examples/phoenix_example/mix.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor mix verify to accumulate step results instead of exiting from the first failed lane</name>
  <files>lib/mix/tasks/verify.ex</files>
  <read_first>
    lib/mix/tasks/verify.ex
    mix.exs
    examples/phoenix_example/mix.exs
    .planning/phases/12-verification-chain-closure/12-RESEARCH.md
    .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md
  </read_first>
  <action>
    Replace the current fail-fast `run_step/2` pattern in `lib/mix/tasks/verify.ex` with an aggregated suite runner. Keep the visible lane headings `DETERMINISTIC (CORE)` and `ADVISORY (ADAPTERS)`, keep the existing step names `CI`, `Docs Contract`, and `Phoenix Example`, but execute all planned steps even when `mix ci` or docs-contract fails.
    Introduce a result representation that records at least the lane, step name, and `:pass` or `:fail` status. `run_step/2` should return a result tuple or map rather than calling `exit/1`. For subprocess-backed steps, capture non-zero exits and error output, print the step failure inline, and return a failing result instead of halting the VM immediately.
    After running all steps, print a combined verification summary and exit non-zero exactly once if any step failed. Preserve deterministic/advisory separation in the output order. Do not downgrade failures to warnings; the command must still fail overall when any step fails, but only after the advisory lane has had a chance to run.
    Keep `verify_phoenix_example/0` running `mix deps.get` before compile, but make it return a structured failing result on non-zero exit rather than causing an early suite shutdown.
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/verify.ex` contains both `[LANE: DETERMINISTIC (CORE)]` and `[LANE: ADVISORY (ADAPTERS)]`.
    - `lib/mix/tasks/verify.ex` contains `Running CI...`, `Running Docs Contract...`, and `Running Phoenix Example...`.
    - `lib/mix/tasks/verify.ex` contains a summary section after the steps run.
    - `lib/mix/tasks/verify.ex` does not call `exit({:shutdown, code})` from inside the per-step execution branch.
    - `lib/mix/tasks/verify.ex` exits non-zero only after aggregating results for all steps.
    - `lib/mix/tasks/verify.ex` still runs `System.cmd(\"mix\", [\"deps.get\"])` inside the Phoenix example verification path.
  </acceptance_criteria>
  <verify>
    <automated>mix verify</automated>
  </verify>
  <done>`mix verify` reports deterministic and advisory results in one run and exits only after the full suite has completed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for lane completion, output ordering, and aggregated failure exit</name>
  <files>test/mix/tasks/verify_test.exs, lib/mix/tasks/verify.ex</files>
  <read_first>
    lib/mix/tasks/verify.ex
    test/test_helper.exs
    .planning/phases/12-verification-chain-closure/12-VALIDATION.md
  </read_first>
  <action>
    Add `test/mix/tasks/verify_test.exs` and refactor `lib/mix/tasks/verify.ex` only as needed to make the suite testable without invoking the real full verification commands on every assertion. Expose a small internal seam such as a `run_with_steps/1` or equivalent function that accepts injected step functions and returns aggregated results plus final exit status.
    Write one regression that simulates a deterministic-lane failure followed by a passing advisory-lane step and asserts the advisory step still runs and appears after the deterministic heading in captured output. Write a second regression that simulates an advisory-lane failure and asserts the final status is failing only after `VERIFICATION COMPLETE` or the suite summary prints. Keep the tests focused on ordering, aggregation, and exit semantics rather than on the real example compile itself.
  </action>
  <acceptance_criteria>
    - `test/mix/tasks/verify_test.exs` exists.
    - `test/mix/tasks/verify_test.exs` contains the strings `DETERMINISTIC (CORE)` and `ADVISORY (ADAPTERS)`.
    - `test/mix/tasks/verify_test.exs` contains an assertion that the advisory step output is present even when a deterministic step fails.
    - `test/mix/tasks/verify_test.exs` contains an assertion tied to final failing exit semantics after summary output.
    - `mix test test/mix/tasks/verify_test.exs` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/verify_test.exs</automated>
    <automated>mix verify</automated>
  </verify>
  <done>The verification command has focused regression coverage proving that lane separation survives failures without hiding downstream results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix verify` -> downstream commands | The task shells out to `mix ci`, docs-contract checks, and Phoenix example commands whose failures must be reported truthfully. |
| Verification output -> maintainers | Maintainers depend on `mix verify` to distinguish deterministic and advisory outcomes accurately. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | D | `lib/mix/tasks/verify.ex` | mitigate | Replace fail-fast per-step exits with aggregated result handling so one failure cannot suppress later verification evidence. |
| T-12-04 | R | `lib/mix/tasks/verify.ex` | mitigate | Add regression tests that assert advisory output still appears after deterministic failure and that final failure happens only after suite summary output. |
</threat_model>

<verification>
- `mix test test/mix/tasks/verify_test.exs`
- `mix verify`
- Inspect the output to confirm both lane headings and the final summary are present in one invocation.
</verification>

<success_criteria>
1. `mix verify` always reaches the advisory lane and final summary, even when deterministic steps fail.
2. The command still exits non-zero for failures, but only after reporting every planned verification segment.
3. Regression tests pin the lane-ordering and aggregated-exit behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-chain-closure/12-02-SUMMARY.md`
</output>
