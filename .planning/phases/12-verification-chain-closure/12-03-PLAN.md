---
phase: "12"
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-02"
gap_closure: true
files_modified:
  - mix.exs
  - test/mix/tasks/verify_test.exs
  - lib/mix/tasks/verify.ex
autonomous: true
requirements:
  - QUAL-01
  - QUAL-05
must_haves:
  truths:
    - "`mix ci` is the canonical merge-blocking lane and includes format, compile, tests, docs, and package build exactly as QUAL-01 claims"
    - "Hosted CI remains truthful because `.github/workflows/ci.yml` delegates to a `mix ci` alias that now matches the requirement contract"
    - "The public `Mix.Tasks.Verify.run/1` entrypoint exits non-zero only after the final verification summary has printed"
  artifacts:
    - path: mix.exs
      provides: "Canonical `mix ci` alias matching the QUAL-01 contract"
      contains: "format --check-formatted"
    - path: test/mix/tasks/verify_test.exs
      provides: "Regression coverage for the public `run/1` shutdown boundary and summary ordering"
      contains: "catch_exit(Verify.run"
    - path: lib/mix/tasks/verify.ex
      provides: "Any minimal seam needed to exercise `run/1` without real CI subprocesses"
      contains: "def run("
  key_links:
    - from: .github/workflows/ci.yml
      to: mix.exs
      via: "workflow `Run CI` step executes `mix ci`"
      pattern: "run: mix ci"
    - from: lib/mix/tasks/verify.ex
      to: test/mix/tasks/verify_test.exs
      via: "public command-boundary regression coverage"
      pattern: "catch_exit\\(Verify.run"
---

<objective>
Close the remaining Phase 12 verification blocker by making `mix ci` actually satisfy the `QUAL-01` contract, while optionally tightening closure trust with a focused regression on the public `Mix.Tasks.Verify.run/1` shutdown boundary.

Purpose: `12-VERIFICATION.md` is blocked because the hosted workflow truthfully runs `mix ci`, but the alias itself still under-delivers the documented quality lane. This plan repairs the real contract at the source and removes the residual command-boundary blind spot if it fits without widening scope.
Output: Updated `mix ci` alias in `mix.exs`, plus a public-entrypoint regression test for `mix verify` if only a minimal seam is required.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-verification-chain-closure/12-VERIFICATION.md
@.planning/phases/12-verification-chain-closure/12-REVIEW.md
@.planning/phases/12-verification-chain-closure/12-02-SUMMARY.md
@mix.exs
@lib/mix/tasks/verify.ex
@test/mix/tasks/verify_test.exs
@.github/workflows/ci.yml

<interfaces>
From `mix.exs`:
```elixir
def cli do
  [
    preferred_envs: [ci: :test, verify: :test]
  ]
end

defp aliases do
  [
    ci: ["compile --warnings-as-errors", "test", "credo --strict", "dialyzer"]
  ]
end
```

From `lib/mix/tasks/verify.ex`:
```elixir
def run(_args) do
  case run_with_lanes() do
    {:ok, _results} -> :ok
    {:error, _results} -> exit({:shutdown, 1})
  end
end
```

From `test/mix/tasks/verify_test.exs`:
```elixir
{messages, {:error, results}} = capture_shell_messages(fn -> Verify.run_with_lanes(lanes) end)
assert output =~ "VERIFICATION COMPLETE"
assert message_index(output, "VERIFICATION COMPLETE") <
         message_index(output, "Overall: FAIL")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the canonical `mix ci` alias to the full QUAL-01 contract</name>
  <files>mix.exs</files>
  <read_first>
    mix.exs
    .planning/REQUIREMENTS.md
    .planning/phases/12-verification-chain-closure/12-VERIFICATION.md
    .github/workflows/ci.yml
  </read_first>
  <behavior>
    - `mix ci` runs format checking before compile/test verification so formatting drift fails the merge-blocking lane.
    - `mix ci` includes docs generation and package build verification in the same alias rather than relying on separate undocumented commands.
    - The docs verification step is actually runnable from the `ci` lane under the chosen Mix environment instead of assuming `mix docs` exists in `MIX_ENV=test`.
    - The workflow remains truthful because its existing `run: mix ci` step now executes the complete QUAL-01 surface.
  </behavior>
  <action>
    Update `mix.exs` so the `ci` alias matches the requirement named in `REQUIREMENTS.md` and the blocker listed in `12-VERIFICATION.md`. The alias must include, in one canonical lane, `format --check-formatted`, `compile --warnings-as-errors`, `test`, `docs`, and package build verification via `hex.build`. Keep the existing strict quality gates that Phase 12 already uses, so `credo --strict` and `dialyzer` remain in the alias unless they demonstrably conflict with the expanded contract.

    Because `mix ci` currently runs in `MIX_ENV=test` via `preferred_envs: [ci: :test]` while `ex_doc` is only available in `:dev`, explicitly fix that mismatch as part of this task by keeping `ci` in `:test` and widening `ex_doc` availability so `mix docs` is runnable in the same environment. Do not change the `ci` preferred env and do not substitute a different docs-verification command; the point is to make the documented `docs` step executable inside the canonical lane the workflow already runs.

    Keep the change narrow: do not modify `.github/workflows/ci.yml` unless the alias rename or invocation surface changes, which it should not. The point is to fix the command that CI already delegates to, not to duplicate or reshuffle verification responsibilities in YAML.
  </action>
  <acceptance_criteria>
    - `mix.exs` `ci` alias contains `format --check-formatted`.
    - `mix.exs` `ci` alias contains `compile --warnings-as-errors`.
    - `mix.exs` `ci` alias contains `test`.
    - `mix.exs` `ci` alias contains `docs`.
    - `mix.exs` `ci` alias contains `hex.build`.
    - `mix.exs` keeps `preferred_envs: [ci: :test, verify: :test]`.
    - `ex_doc` is available to the `:test` environment so `mix docs` is executable inside `mix ci`.
    - `MIX_ENV=test mix help docs` succeeds without `The task "docs" could not be found`.
    - `.github/workflows/ci.yml` still delegates to `mix ci` without re-encoding the lane contents.
  </acceptance_criteria>
  <verify>
    <automated>mix ci</automated>
    <automated>MIX_ENV=test mix help docs</automated>
    <automated>rg -n 'preferred_envs: \\[ci: :test, verify: :test\\]|format --check-formatted|compile --warnings-as-errors|test|docs|hex.build|\\{:ex_doc, .*only: \\[:dev, :test\\]' mix.exs</automated>
    <automated>rg -n 'run: mix ci' .github/workflows/ci.yml</automated>
  </verify>
  <done>`mix ci` is the single truthful merge-blocking lane for format, compile, tests, docs, and package build, satisfying QUAL-01 instead of only approximating it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the public `Mix.Tasks.Verify.run/1` shutdown boundary after summary output</name>
  <files>test/mix/tasks/verify_test.exs, lib/mix/tasks/verify.ex</files>
  <read_first>
    lib/mix/tasks/verify.ex
    test/mix/tasks/verify_test.exs
    .planning/phases/12-verification-chain-closure/12-REVIEW.md
    .planning/phases/12-verification-chain-closure/12-VERIFICATION.md
  </read_first>
  <behavior>
    - When the verification suite fails, `Verify.run/1` exits with `{:shutdown, 1}` only after printing `VERIFICATION COMPLETE` and the final overall verdict.
    - The regression exercises the public `run/1` entrypoint, not just `run_with_lanes/1`.
    - Any seam added to make the test deterministic stays minimal and does not weaken the production command path.
  </behavior>
  <action>
    Extend `test/mix/tasks/verify_test.exs` with one focused regression covering the warning in `12-REVIEW.md`: call `Verify.run/1`, assert it exits with `{:shutdown, 1}`, and assert the final summary output is printed before shutdown. If the current implementation cannot be exercised without spawning the real `mix ci` and Phoenix example commands, add the smallest possible internal seam in `lib/mix/tasks/verify.ex` to inject test lanes or default steps while preserving `run/1` as the public entrypoint and leaving production behavior unchanged.

    Keep this task opportunistic and narrow. Do not redesign the verification runner or add broad new helper APIs; only introduce the minimum hook required to make the public command boundary testable.
  </action>
  <acceptance_criteria>
    - `test/mix/tasks/verify_test.exs` contains a `catch_exit(Verify.run(` assertion.
    - The new regression proves `VERIFICATION COMPLETE` is emitted before the failing shutdown boundary.
    - `mix test test/mix/tasks/verify_test.exs` passes without invoking the real full verification suite.
    - `mix verify` still preserves the Phase 12 lane-ordering and aggregated-summary behavior after any test seam is added.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/verify_test.exs</automated>
    <automated>mix verify</automated>
  </verify>
  <done>The public `mix verify` command boundary is regression-tested, so future changes cannot silently re-break shutdown ordering while internal helper tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix ci` alias -> maintainers/CI | Maintainers and hosted CI trust the alias as the canonical merge-blocking quality contract. |
| `Mix.Tasks.Verify.run/1` -> shell/automation | Automation relies on the public Mix task to return a truthful final exit after all output is emitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | R | `mix.exs` `ci` alias | mitigate | Encode the full QUAL-01 contract directly in the alias so CI cannot over-report coverage by delegating to an incomplete command. |
| T-12-06 | D | `Mix.Tasks.Verify.run/1` | mitigate | Add a public-entrypoint regression proving shutdown happens only after the final summary, preventing early-exit regressions from hiding results. |
</threat_model>

<verification>
- Run `mix ci` to prove the canonical deterministic lane now includes the missing format/docs/package-build checks.
- Run `mix test test/mix/tasks/verify_test.exs` to prove the public command-boundary regression.
- Run `mix verify` to confirm the existing lane-completion behavior still holds after any minimal seam added for the test.
</verification>

<success_criteria>
1. `12-VERIFICATION.md` blocker for `QUAL-01` is closed because `mix ci` now truly covers format, compile, tests, docs, and package build.
2. Hosted CI remains truthful without workflow expansion because `.github/workflows/ci.yml` still delegates to the corrected `mix ci` alias.
3. The public `Mix.Tasks.Verify.run/1` shutdown boundary is directly covered by regression tests if only a minimal seam is required.
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-chain-closure/12-03-SUMMARY.md`
</output>
