---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QUAL-01
  - QUAL-03
must_haves:
  truths:
    - "`.github/workflows/ci.yml` is tracked and remains the canonical hosted deterministic verification surface"
    - "Hosted CI runs `mix ci` and an explicit Phoenix example proof step from committed workflow content"
    - "Phoenix example proof is exercised as automation, not inferred from local compilation state"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Committed hosted CI verification and example-proof workflow"
  key_links:
    - from: .github/workflows/ci.yml
      to: mix ci
      via: "workflow step command"
      pattern: "run: mix ci"
    - from: .github/workflows/ci.yml
      to: examples/phoenix_example
      via: "workflow step command"
      pattern: "cd examples/phoenix_example"
---

<objective>
Commit a truthful hosted CI workflow that proves the deterministic verification lane and explicitly exercises the Phoenix example adoption path.

Purpose: Close the proof gap called out by `INT-CI-TRACKING` and raise `QUAL-03` from local-only evidence to committed CI-backed evidence.
Output: A tracked `.github/workflows/ci.yml` that runs `mix ci` plus an explicit Phoenix example verification step.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-verification-chain-closure/12-RESEARCH.md
@.planning/phases/12-verification-chain-closure/12-VALIDATION.md
@.planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md
@mix.exs
@.github/workflows/ci.yml
@examples/phoenix_example/mix.exs
@test/rendro/adapters/phoenix_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the hosted CI workflow as committed proof for deterministic lane plus Phoenix example evidence</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    mix.exs
    examples/phoenix_example/mix.exs
    test/rendro/adapters/phoenix_test.exs
    .planning/phases/12-verification-chain-closure/12-RESEARCH.md
    .planning/phases/04-quality-and-release-hardening/04-VERIFICATION.md
  </read_first>
  <action>
    Replace `.github/workflows/ci.yml` with a single truthful workflow that runs on `push` and `pull_request` to `main`, uses `actions/checkout@v4`, uses `erlef/setup-beam@v1` with exactly `otp-version: '28'` and `elixir-version: '1.19.5'`, runs `mix deps.get`, and runs `mix ci` as the canonical deterministic verification lane.
    After the deterministic lane, add a separate named step that proves the Phoenix example path from committed automation. That step must `cd examples/phoenix_example`, run `mix deps.get`, and run `mix compile`. Keep the example proof as an explicit workflow step instead of implying it through root-project tests or comments. Keep the workflow narrow to Phase 12 scope: do not add release-preflight or docs-contract expansion beyond the existing `mix ci` contract.
    Preserve YAML clarity with step names that make the proof chain obvious, including one step named `Run CI` and one step named `Verify Phoenix Example`.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `name: CI`.
    - `.github/workflows/ci.yml` contains both `push:` and `pull_request:` with `main`.
    - `.github/workflows/ci.yml` contains `uses: actions/checkout@v4`.
    - `.github/workflows/ci.yml` contains `uses: erlef/setup-beam@v1`.
    - `.github/workflows/ci.yml` contains `otp-version: '28'`.
    - `.github/workflows/ci.yml` contains `elixir-version: '1.19.5'`.
    - `.github/workflows/ci.yml` contains a step named `Run CI`.
    - `.github/workflows/ci.yml` contains `run: mix ci`.
    - `.github/workflows/ci.yml` contains a step named `Verify Phoenix Example`.
    - `.github/workflows/ci.yml` contains `cd examples/phoenix_example`.
    - `.github/workflows/ci.yml` contains both `mix deps.get` and `mix compile` inside the Phoenix example step.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "name: CI|push:|pull_request:|actions/checkout@v4|erlef/setup-beam@v1|otp-version: '28'|elixir-version: '1.19.5'|Run CI|run: mix ci|Verify Phoenix Example|cd examples/phoenix_example|mix deps.get|mix compile" .github/workflows/ci.yml</automated>
    <automated>cd examples/phoenix_example && mix deps.get && mix compile</automated>
  </verify>
  <done>The committed workflow itself proves the repo's deterministic lane and the Phoenix example adoption path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> repository code | Hosted CI executes repository-defined commands and can over-claim proof if the workflow is incomplete or misleading. |
| Workflow proof -> maintainers | Maintainers rely on committed CI configuration as evidence for `QUAL-01` and `QUAL-03`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | `.github/workflows/ci.yml` | mitigate | Require explicit `mix ci` and Phoenix example steps in committed workflow content so hosted proof matches the documented contract exactly. |
| T-12-02 | R | `.github/workflows/ci.yml` | mitigate | Keep acceptance criteria grep-verifiable and command-verifiable so the workflow cannot claim example proof without a concrete step. |
</threat_model>

<verification>
- Read back `.github/workflows/ci.yml` and confirm the deterministic lane and Phoenix example step are both explicit.
- Run `cd examples/phoenix_example && mix deps.get && mix compile` locally as the same command surface the workflow will host.
- Confirm the file is tracked in git after execution rather than remaining untracked.
</verification>

<success_criteria>
1. `.github/workflows/ci.yml` is a tracked workflow that runs `mix ci` on pushes and pull requests to `main`.
2. The committed workflow contains an explicit Phoenix example proof step using the example app directory.
3. The workflow no longer relies on local-only evidence for `QUAL-03`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-chain-closure/12-01-SUMMARY.md`
</output>
