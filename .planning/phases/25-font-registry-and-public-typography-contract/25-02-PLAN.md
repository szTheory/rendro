---
phase: 25
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/rendro/font_registry.ex
  - lib/rendro/pipeline/build.ex
  - lib/rendro/pipeline/measure.ex
  - lib/rendro/pdf/font.ex
  - lib/rendro/pdf/writer.ex
  - test/rendro/pipeline/measure_test.exs
  - test/rendro/pdf/font_test.exs
  - test/rendro/pdf/writer_test.exs
autonomous: true
requirements: [FONT-01]

must_haves:
  truths:
    - "Measure and Writer resolve authored font references through one shared logical-font contract."
    - "Phase 25 stays honest about scope by supporting registry-backed built-ins now while deferring custom metrics and embedding to Phase 26."
    - "Unknown logical font references fail through explicit validation instead of silently drifting back to Helvetica."
  artifacts:
    - path: "lib/rendro/font_registry.ex"
      provides: "Shared resolution and built-in lookup contract"
      min_lines: 90
    - path: "lib/rendro/pipeline/measure.ex"
      provides: "Registry-backed deterministic measurement path"
      min_lines: 40
    - path: "lib/rendro/pdf/writer.ex"
      provides: "Registry-backed rendering path"
      min_lines: 40
  key_links:
    - from: "lib/rendro/font_registry.ex"
      to: "lib/rendro/pipeline/measure.ex"
      via: "resolved font metrics path"
      pattern: "resolve|default_font|built_in"
    - from: "lib/rendro/font_registry.ex"
      to: "lib/rendro/pdf/writer.ex"
      via: "resolved render font path"
      pattern: "resolve|base_font|resource"
    - from: "lib/rendro/pipeline/build.ex"
      to: "test/rendro/pipeline/measure_test.exs"
      via: "explicit failure for unknown logical font references"
      pattern: "invalid|unknown|font"
---

<objective>
Wire deterministic logical-font resolution into the measurement and rendering stages.

Purpose: after Plan 01 introduces the public registry contract, Phase 25 must make downstream stages consume that contract so authored font selection is real behavior instead of decorative metadata.
Output: a shared built-in resolution path, explicit invalid-font handling, and focused regression proof that Measure and Writer use the same resolved selection without widening scope into custom embedding.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-RESEARCH.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-PATTERNS.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-VALIDATION.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-01-PLAN.md
@lib/rendro/pipeline/build.ex
@lib/rendro/pipeline/measure.ex
@lib/rendro/pdf/font.ex
@lib/rendro/pdf/writer.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one shared built-in font resolution path and explicit unknown-font validation</name>
  <files>lib/rendro/font_registry.ex, lib/rendro/pipeline/build.ex, test/rendro/pdf/font_test.exs</files>
  <read_first>
    - lib/rendro/font_registry.ex
    - lib/rendro/pipeline/build.ex
    - test/rendro/pdf/font_test.exs
    - .planning/phases/25-font-registry-and-public-typography-contract/25-RESEARCH.md
    - .planning/phases/25-font-registry-and-public-typography-contract/25-PATTERNS.md
  </read_first>
  <action>
    Extend `Rendro.FontRegistry` with the shared resolution behavior Phase 25 needs: given a document registry, a text font reference, and the document default font, return one resolved built-in definition containing the data needed by both measurement and rendering. Keep this contract narrow and truthful. It should support the built-in fonts that Rendro explicitly registers in Phase 25 and no more. Do not add external-file loading, glyph fallback chains, or embedding streams yet.

    Add explicit invalid-font handling early in the pipeline so unknown logical names do not silently collapse back to Helvetica. `Build` is the preferred boundary if it can validate authored text and section content before stage work proceeds; otherwise use the earliest deterministic stage that can produce a typed actionable error consistently. Extend `test/rendro/pdf/font_test.exs` to prove built-in registry lookup and unknown-font failure behavior directly.
  </action>
  <acceptance_criteria>
    - `lib/rendro/font_registry.ex` exposes one shared resolution function for stage consumers.
    - Unknown logical font references fail explicitly instead of silently defaulting.
    - `lib/rendro/pipeline/build.ex` or the chosen earliest boundary contains intentional invalid-font handling.
    - `test/rendro/pdf/font_test.exs` contains a regression for built-in lookup and unknown-font failure.
    - No code path loads external font files or claims embedding support.
    - `mix test test/rendro/pdf/font_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pdf/font_test.exs</automated>
  </verify>
  <done>The repo now has one explicit built-in font resolver and a truthful failure path for unknown logical fonts.</done>
</task>

<task type="auto">
  <name>Task 2: Route Measure and Writer through the shared resolver and prove deterministic parity</name>
  <files>lib/rendro/pipeline/measure.ex, lib/rendro/pdf/writer.ex, test/rendro/pipeline/measure_test.exs, test/rendro/pdf/writer_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/measure.ex
    - lib/rendro/pdf/writer.ex
    - test/rendro/pipeline/measure_test.exs
    - test/rendro/pdf/writer_test.exs
    - lib/rendro/font_registry.ex
    - lib/rendro/text.ex
    - .planning/phases/25-font-registry-and-public-typography-contract/25-RESEARCH.md
  </read_first>
  <action>
    Replace the duplicate `Font.helvetica()` stage defaults with shared registry-backed resolution. `Measure` must use the resolved built-in definition for width and wrapping calculations, and `Writer` must use the corresponding resolved built-in definition for PDF font selection and `/Tf` emission. Preserve current default output for documents that rely on the default registry entry so Phase 25 stays a contract-and-plumbing phase, not a user-visible typography expansion.

    Extend measurement and writer tests so they prove authored logical names reach the downstream stages. At minimum, cover one non-default logical font registration backed by a built-in definition, one default-font path, and one compatibility case if `"Helvetica"` inputs remain accepted. Keep the assertions scoped to resolution parity and deterministic behavior; do not add custom-font embedding claims or fallback-chain semantics.
  </action>
  <acceptance_criteria>
    - `lib/rendro/pipeline/measure.ex` no longer hard-codes `Font.helvetica()` as the universal path for all text.
    - `lib/rendro/pdf/writer.ex` no longer hard-codes one universal Helvetica selection path for all text.
    - `test/rendro/pipeline/measure_test.exs` contains a regression proving a registered logical font reaches measurement.
    - `test/rendro/pdf/writer_test.exs` contains a regression proving a registered logical font reaches rendering.
    - Default documents still render deterministically through the built-in default registry entry.
    - `mix test test/rendro/pipeline/measure_test.exs test/rendro/pdf/writer_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Measurement and rendering now consume the same resolved logical-font contract, making authored font selection real behavior without widening scope prematurely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Builder/validation to stage execution | Unknown logical font references must fail explicitly before hidden fallback can distort layout truth. |
| Measure to Writer | Both stages must consume the same resolved font contract or later custom-font work will drift. |
| Current scope to future scope | Built-in registry support now must not overstate custom embedding or Unicode capabilities reserved for later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Repudiation | stage resolution path | mitigate | Route both Measure and Writer through one shared resolver and prove parity with focused tests. |
| T-25-05 | Tampering | invalid-font handling | mitigate | Reject unknown logical font references explicitly instead of silently falling back to Helvetica. |
| T-25-06 | Elevation of privilege | scope claims | mitigate | Keep Phase 25 limited to registry-backed built-ins and defer metrics/embedding/fallback-chain claims to later phases. |
</threat_model>

<verification>
Run `mix test test/rendro/pipeline/measure_test.exs test/rendro/pdf/font_test.exs test/rendro/pdf/writer_test.exs` to prove shared logical-font resolution, explicit unknown-font handling, and deterministic default behavior land together.
</verification>

<success_criteria>
- Unknown logical font references fail explicitly instead of silently reverting to hidden defaults.
- Measure and Writer both consume one shared resolved built-in font contract.
- Phase 25 stays honest about scope while making authored font selection a real deterministic behavior boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/25-font-registry-and-public-typography-contract/25-02-SUMMARY.md`
</output>
