---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/document.ex
  - lib/rendro/text.ex
  - lib/rendro.ex
  - lib/rendro/font_registry.ex
  - test/rendro/document_test.exs
  - test/rendro/text_test.exs
  - test/rendro_builders_test.exs
autonomous: true
requirements: [FONT-01]

must_haves:
  truths:
    - "Font registration is part of the authored document contract, not an implicit writer default."
    - "Public font selection uses logical names and preserves pure-core boundaries by hiding PDF object internals."
    - "Current Helvetica-based documents retain a narrow compatibility path instead of breaking at the boundary."
  artifacts:
    - path: "lib/rendro/font_registry.ex"
      provides: "Pure-core font registry data contract and built-in registration helpers"
      min_lines: 60
    - path: "lib/rendro/document.ex"
      provides: "Document-owned font registry storage and builder API"
      min_lines: 40
    - path: "lib/rendro/text.ex"
      provides: "Truthful logical-font text contract"
      min_lines: 20
  key_links:
    - from: "lib/rendro/document.ex"
      to: "lib/rendro.ex"
      via: "document-owned registration and top-level builder wrappers"
      pattern: "font_registry|default_font|register_font"
    - from: "lib/rendro/text.ex"
      to: "test/rendro/text_test.exs"
      via: "logical-font type/default contract"
      pattern: "font|logical|Helvetica|helvetica"
    - from: "lib/rendro/font_registry.ex"
      to: "test/rendro/document_test.exs"
      via: "registry data shape and built-in registration behavior"
      pattern: "register_font|built_in|default_font"
---

<objective>
Establish the public font registry contract on the document and text authoring surfaces.

Purpose: Phase 25 starts by making fonts explicit authored state instead of hidden runtime defaults.
Output: a pure-core font registry module, document-level registration/default APIs, truthful logical-font docs/types, and compatibility-tested boundary behavior for current built-in inputs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-RESEARCH.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-PATTERNS.md
@.planning/phases/25-font-registry-and-public-typography-contract/25-VALIDATION.md
@lib/rendro/document.ex
@lib/rendro/text.ex
@lib/rendro.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pure-core font registry owned by the document contract</name>
  <files>lib/rendro/font_registry.ex, lib/rendro/document.ex, test/rendro/document_test.exs</files>
  <read_first>
    - lib/rendro/document.ex
    - test/rendro/document_test.exs
    - .planning/phases/25-font-registry-and-public-typography-contract/25-RESEARCH.md
    - .planning/phases/25-font-registry-and-public-typography-contract/25-PATTERNS.md
  </read_first>
  <action>
    Introduce a new pure-core module `Rendro.FontRegistry` that stores document-owned logical font registrations. Keep the contract intentionally narrow for Phase 25: a logical name such as `:body` or `:heading` maps to a supported built-in font descriptor, and the document also tracks a `default_font` logical name used when authored text does not override it. Do not expose PDF object numbers or writer resource names in this public contract.

    Extend `%Rendro.Document{}` with fields for the registry and default logical font, plus builder helpers in `Rendro.Document` that follow the existing pure-function style (`register_font/3`, `put_default_font/2`, or equivalent). The default out-of-the-box state must preserve the current behavior by making Helvetica available through a built-in logical entry and defaulting ordinary documents to it without requiring user setup. Update `test/rendro/document_test.exs` so it proves empty documents get the built-in default, documents can register a second logical name, and builder calls remain deterministic pure data transforms.
  </action>
  <acceptance_criteria>
    - `lib/rendro/font_registry.ex` defines a public pure-data registry module.
    - `lib/rendro/document.ex` contains explicit font-registry and default-font fields on `%Rendro.Document{}`.
    - `lib/rendro/document.ex` exposes pure builder helpers for registration/default selection.
    - A newly created document still resolves to a built-in Helvetica-compatible default path without extra setup.
    - `test/rendro/document_test.exs` contains at least one assertion for registering a logical font and one for default-font behavior.
    - `mix test test/rendro/document_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/document_test.exs</automated>
  </verify>
  <done>The document now owns a truthful font registry contract instead of relying on hidden stage defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the public authoring boundary around logical font names and compatibility aliases</name>
  <files>lib/rendro/text.ex, lib/rendro.ex, test/rendro/text_test.exs, test/rendro_builders_test.exs</files>
  <read_first>
    - lib/rendro/text.ex
    - lib/rendro.ex
    - test/rendro/text_test.exs
    - test/rendro_builders_test.exs
    - lib/rendro/document.ex
    - lib/rendro/font_registry.ex
    - .planning/phases/25-font-registry-and-public-typography-contract/25-RESEARCH.md
  </read_first>
  <action>
    Update the public authoring contract so `%Rendro.Text{font: ...}` is described as a logical font reference rather than an implicit PDF built-in name. Accept atoms for logical names and keep a compatibility path for the current built-in inputs (`\"Helvetica\"`, and any equivalent built-in alias shape you intentionally support), but make the docs, type, and builder behavior recommendation-first around logical names. Do not require authors to know writer resource names, base font strings, or embedding details.

    Extend `Rendro` top-level helpers with wrappers that surface the new document registration/default APIs directly, following the repo’s existing builder ergonomics. Update text and builder tests so they prove: the default authoring path still works, logical names can be authored explicitly, and compatibility inputs stay narrow and intentional rather than becoming a free-form arbitrary PDF-font escape hatch.
  </action>
  <acceptance_criteria>
    - `lib/rendro/text.ex` docs and type surface describe logical font references truthfully.
    - `lib/rendro.ex` exposes top-level wrappers for the document font-registration contract.
    - `test/rendro/text_test.exs` contains an assertion using a logical font name.
    - `test/rendro_builders_test.exs` contains assertions for top-level registration/default-font behavior.
    - Current default text creation remains compatible with the Helvetica-based baseline.
    - `mix test test/rendro/text_test.exs test/rendro_builders_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/text_test.exs test/rendro_builders_test.exs</automated>
  </verify>
  <done>The public authoring surface now speaks in logical font names while preserving a narrow compatibility path for today’s default inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public authoring API | Font registration and selection must be expressed as pure document/text data, not hidden writer knobs. |
| Compatibility behavior | Current Helvetica-based inputs must remain intentional and narrow so the new contract does not become ambiguous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `lib/rendro/document.ex` contract | mitigate | Put font registry ownership on `%Rendro.Document{}` and expose pure builder helpers so font choice is explicit authored state. |
| T-25-02 | Repudiation | `lib/rendro/text.ex` public docs/types | mitigate | Rewrite the public contract around logical names and cover it with focused text/builder tests. |
| T-25-03 | Elevation of privilege | compatibility path | mitigate | Keep compatibility aliases narrow and tested instead of allowing arbitrary PDF-internal escape hatches. |
</threat_model>

<verification>
Run `mix test test/rendro/document_test.exs test/rendro/text_test.exs test/rendro_builders_test.exs` to prove document-owned font registration, logical-name authoring, and compatibility-default behavior land together.
</verification>

<success_criteria>
- Documents can register and default logical fonts through pure-core APIs.
- Text authoring talks about logical font names truthfully.
- Existing Helvetica-based callers retain a tested compatibility path without exposing PDF internals publicly.
</success_criteria>

<output>
After completion, create `.planning/phases/25-font-registry-and-public-typography-contract/25-01-SUMMARY.md`
</output>
