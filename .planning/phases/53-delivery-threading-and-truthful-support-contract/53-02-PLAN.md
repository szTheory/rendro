---
phase: 53
plan: 02
type: execute
wave: 2
depends_on:
  - "53-01"
files_modified:
  - priv/support_matrix.json
  - guides/api_stability.md
  - guides/integrations.md
  - lib/rendro/adapters/mailglass.ex
  - test/docs_contract/protection_claims_test.exs
  - test/docs_contract/integrations_claims_test.exs
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
must_haves:
  truths:
    - The machine-readable support matrix and the human-facing docs tell the same narrow protection-boundary story.
    - Protected delivery is documented as `render_to_artifact -> Protect.password -> store/deliver`, with delivery and storage seams transporting bytes rather than passwords.
    - Protection claims explicitly separate password-to-open support from advisory permissions and unsupported signing, tamper-evidence, compliance, and native in-core encryption narratives.
  artifacts:
    - path: priv/support_matrix.json
      provides: "Compact family-first `protection` contract with explicit boundary leaves"
    - path: guides/api_stability.md
      provides: "Canonical product-facing wording for the `protection` family boundaries"
    - path: guides/integrations.md
      provides: "Canonical protected async and Mailglass workflow guidance with late secret resolution"
    - path: lib/rendro/adapters/mailglass.ex
      provides: "Mailglass moduledoc and function docs that state transport-only protected delivery"
    - path: test/docs_contract/protection_claims_test.exs
      provides: "Executable lock on support-matrix and protection-guide boundary wording"
    - path: test/docs_contract/integrations_claims_test.exs
      provides: "Executable lock on async and Mailglass boundary wording"
  key_links:
    - from: priv/support_matrix.json
      to: guides/api_stability.md
      via: "D-12 through D-15 require the exact same protection boundary story in machine-readable and human-readable contracts"
    - from: guides/integrations.md
      to: lib/rendro/adapters/mailglass.ex
      via: "the protected delivery recipe and Mailglass transport-only wording must match per D-08 through D-11"
    - from: test/docs_contract/integrations_claims_test.exs
      to: guides/integrations.md
      via: "docs-contract tests freeze the identifier-only async args and `attach_artifact/3` protected delivery recipe per D-01 through D-03"
---

<objective>
Close the `protection` family contract so the support matrix, guides, Mailglass docs, and docs-contract tests all publish one compact, truthful boundary story.

Purpose: Satisfy TRUST-01 and TRUST-02 by eliminating wording drift and making the highest-risk protection boundary rules executable.
Output: Updated support matrix, synchronized guides/moduledoc, and docs-contract tests that lock the protection boundary story.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-CONTEXT.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-DISCUSSION-LOG.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-RESEARCH.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-PATTERNS.md
@.planning/phases/50-support-boundary-and-proof-closure/50-01-PLAN.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-02-PLAN.md
@.planning/phases/53-delivery-threading-and-truthful-support-contract/53-01-PLAN.md
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@priv/support_matrix.json
@guides/api_stability.md
@guides/integrations.md
@lib/rendro/adapters/mailglass.ex
@lib/rendro/adapters/oban/render_worker.ex
@test/docs_contract/protection_claims_test.exs
@test/docs_contract/integrations_claims_test.exs

<interfaces>
From `priv/support_matrix.json`:
```json
"protection": {
  "capabilities": {
    "password_to_open": "supported",
    "external_hook_qpdf": "supported",
    "native_encryption": "unsupported"
  },
  "algorithms": {
    "aes_256": "supported"
  },
  "behaviors": {
    "advisory_permissions": "supported",
    "deterministic_output": "unsupported",
    "digital_signatures": "unsupported",
    "pdf_a_compliance": "unsupported",
    "tamper_evidence": "unsupported"
  }
}
```

From `guides/api_stability.md`:
```markdown
Rendro supports password-to-open PDF protection through an external artifact-first boundary.
Advisory permissions are an honor-system PDF flag surface, not a cryptographic enforcement mechanism.
Protection is not compliance, not tamper evidence, and not digital signing.
```

From `guides/integrations.md`:
```markdown
The worker also does **not** accept password or protection fields in job args.
If your workflow needs password-to-open delivery, protect the artifact first and
then hand the protected artifact to `attach_artifact/3`.
That flow keeps protection at the artifact boundary. Mailglass does not need to know the passwords; it just transports the already-protected PDF bytes.
```

From `lib/rendro/adapters/mailglass.ex`:
```elixir
@doc """
Attaches a rendered `Rendro.Artifact` as a PDF to `email_or_message`.

Returns the modified email/message on success, or `{:error, error}` if
the target is invalid.
"""
def attach_artifact(email_or_message, %Rendro.Artifact{binary: binary}, filename \\ @default_filename)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the protection support matrix with compact boundary leaves per D-12 through D-15</name>
  <files>priv/support_matrix.json, test/docs_contract/protection_claims_test.exs</files>
  <behavior>
    - The `protection` family stays family-first and compact, with a small `boundaries` subsection instead of a larger taxonomy rewrite.
    - The new boundary leaves explicitly encode external-hook-only posture, no password material in persisted async job args, and delivery/storage seams transporting protected bytes rather than passwords.
    - Existing viewer rows remain `unverified`, and the matrix continues to distinguish supported password-to-open capability from advisory permission and unsupported trust narratives.
  </behavior>
  <action>
    Update `priv/support_matrix.json` so the existing `protection` family from Phase 52 stays compact per D-12 and D-14: preserve `capabilities`, `algorithms`, `behaviors`, and `viewers`, then add a minimal `boundaries` subsection rather than a new top-level taxonomy. Use compact, policy-level leaves aligned with the locked context, such as external-hook-only posture, passwords not belonging in persisted async job args, and delivery/storage seams transporting protected artifacts rather than password material.
    Keep the matrix product-facing. Do not enumerate every adapter, validator path, or async implementation detail, and do not pull Phase 54 viewer-proof promotion into this plan. The matrix must still say password-to-open is supported through an external hook, advisory permissions are supported but advisory, native in-core encryption is unsupported, and signing/tamper-evidence/compliance narratives remain unsupported.
    Extend `test/docs_contract/protection_claims_test.exs` so it parses or matches the exact new `boundaries` leaves, preserves the existing narrow protection assertions, and refutes scope-widening phrases or missing boundary leaves. Use these tests to lock TRUST-01 directly, not as a generic prose check.
  </action>
  <verify>
    <automated>mix test test/docs_contract/protection_claims_test.exs</automated>
  </verify>
  <done>The support matrix publishes a compact `protection.boundaries` contract that makes the highest-risk boundary rules machine-readable without widening the overall matrix shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize guides, Mailglass docs, and docs-contract wording around one canonical protected-delivery story</name>
  <files>guides/api_stability.md, guides/integrations.md, lib/rendro/adapters/mailglass.ex, test/docs_contract/protection_claims_test.exs, test/docs_contract/integrations_claims_test.exs</files>
  <behavior>
    - `guides/api_stability.md`, `guides/integrations.md`, and Mailglass moduledoc all use one cohesive recommendation set instead of option menus.
    - The canonical protected flow is `render_to_artifact -> Protect.password -> store/deliver`, with identifiers-only async args and late secret resolution inside the application boundary.
    - Mailglass stays transport-only: `attach_pdf/3` remains the plain unprotected convenience path, while protected delivery uses `attach_artifact/3` with a pre-protected artifact.
  </behavior>
  <action>
    Update `guides/api_stability.md` so the human-facing protection contract mirrors the matrix leaves from Task 1 per D-15. Keep the existing narrow statements about password-to-open, `:aes_256`, advisory permissions, and unsupported signing/compliance/tamper-evidence/native encryption narratives, then strengthen the boundary wording so the guide explicitly says delivery/storage seams transport protected artifacts rather than passwords and that Phase 53 does not introduce a first-party protected worker or orchestration API.
    Strengthen `guides/integrations.md` rather than reinventing it. Keep the existing worker section render-only per D-01 through D-03, retain the prohibition on password/protection job args, and tighten the protected-delivery recipe so it is clearly the canonical application-owned flow: identifiers in job args, late secret resolution at execution time, `render_to_artifact`, `Rendro.Protect.password/2`, then downstream `store` or `Rendro.Adapters.Mailglass.attach_artifact/3`. Do not add option menus, `protect:` adapter options, or `attach_protected_pdf/4`.
    Update `lib/rendro/adapters/mailglass.ex` moduledoc and `attach_artifact/3` docs per D-08 through D-11 so the adapter explicitly states that `attach_pdf/3` is the render-and-attach convenience path for unprotected PDFs, while protected delivery must pass an already-protected `%Rendro.Artifact{}` to `attach_artifact/3`. The docs should say Mailglass never accepts, persists, derives, or manages password material.
    Extend `test/docs_contract/protection_claims_test.exs` and `test/docs_contract/integrations_claims_test.exs` to lock that exact wording: identifiers-only async args, late secret resolution, transport-only Mailglass, `attach_artifact/3` as the protected seam, and the same unsupported narratives listed in the support matrix. Keep these docs-contract seams literal and product-facing so wording drift fails fast.
  </action>
  <verify>
    <automated>mix test test/docs_contract/protection_claims_test.exs test/docs_contract/integrations_claims_test.exs</automated>
  </verify>
  <done>The support matrix, guides, and Mailglass docs all publish the same narrow protection-boundary story, and docs-contract tests fail if any one of them drifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal implementation boundary -> public support contract | Matrix values and guide/moduledoc wording become external capability statements here. |
| Application-owned secrets -> documented async recipe | Secret-handling guidance crosses from implementation expectations into user-facing operational instructions here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-05 | Repudiation | `priv/support_matrix.json` + docs-contract tests | mitigate | Encode the highest-risk protection boundary rules in machine-readable leaves and lock them with literal tests so support claims remain auditable per D-12 through D-15. |
| T-53-06 | Information integrity | `guides/api_stability.md` | mitigate | Keep the guide aligned with the matrix on password-to-open, advisory permissions, and unsupported signing/compliance/tamper-evidence/native encryption narratives. |
| T-53-07 | Information disclosure | `guides/integrations.md` + `lib/rendro/adapters/mailglass.ex` | mitigate | State explicitly that persisted async args and delivery adapters must not receive password material, and freeze that rule in docs-contract tests per D-01 through D-03 and D-08 through D-11. |
| T-53-08 | Spoofing / semantic confusion | Mailglass docs | mitigate | Distinguish `attach_pdf/3` from `attach_artifact/3` so callers cannot mistake transport helpers for a first-party protection API. |
</threat_model>

<verification>
mix test test/docs_contract/protection_claims_test.exs
mix test test/docs_contract/protection_claims_test.exs test/docs_contract/integrations_claims_test.exs
</verification>

<success_criteria>
- `priv/support_matrix.json` exposes a compact `protection.boundaries` subsection without redesigning the family-first matrix.
- `guides/api_stability.md`, `guides/integrations.md`, and Mailglass docs all repeat the same canonical protected-delivery boundary story.
- Docs-contract tests fail if support-matrix leaves, guide wording, or Mailglass/integration language drift apart.
- No Phase 54 viewer-proof promotion or release-tail work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/53-delivery-threading-and-truthful-support-contract/53-02-SUMMARY.md`
</output>
