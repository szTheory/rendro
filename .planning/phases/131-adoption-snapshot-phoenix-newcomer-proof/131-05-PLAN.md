---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "05"
type: execute
wave: 5
depends_on: ["131-04"]
files_modified:
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
autonomous: false
requirements: [JOURNEY-01, JOURNEY-02, JOURNEY-04]
must_haves:
  truths:
    - "D-33: pre-approval revalidation reruns the bounded open-silent-FIFO regression, separate 45/15-minute timeout contract, candidate tests, full CI, exact-SHA ref-free preflight/dry-run, package/docs checks, and v1.3.2 absence against an equality-asserted detached HEAD."
    - "The pre-approval proof snapshots all tag refs and proves they are unchanged; it never invokes the legacy synthetic-tag mode or any git tag command."
    - "Fresh blocking approval names the unchanged 40-character candidate SHA and authorizes annotated v1.3.2 tag, protected Hex, and candidate-bound protected HexDocs together; it immediately precedes mutation."
    - "Only a new annotated v1.3.2 tag at the approved SHA triggers the push-tag protected release; HexDocs is dispatched only after exact Hex 1.3.2 is independently verified."
    - "The named verifier binds the approved SHA, v1.3.2 tag object/peeled SHA, exact new protected run identities, Hex archive, and versioned HexDocs source/symbols in one atomic VERIFIED prerequisite."
    - "v1.3.0/run 32513353551 and v1.3.1/run 32539594278 remain untouched failed history with their exact identities and absent Hex/HexDocs releases."
  artifacts:
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
      provides: "Fresh read-only exact-SHA proof presented at the irreversible checkpoint"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      provides: "Approval and new protected workflow run identities tied to the sealed candidate"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
      provides: "Atomic named-verifier proof of exact public 1.3.2 and both preserved incidents"
  key_links:
    - from: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
      to: checkpoint:decision
      via: "fresh proof names the exact unchanged candidate immediately before approval"
    - from: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      to: scripts/release_preflight_proof.exs
      via: "recorded candidate SHA passed to --candidate-sha with detached HEAD equality and unchanged tag refs"
    - from: checkpoint:decision
      to: annotated refs/tags/v1.3.2
      via: "approval explicitly authorizes the same SHA plus tag, Hex, and HexDocs together"
    - from: .github/workflows/release.yml
      to: public Hex 1.3.2
      via: "push-tag-only protected run at the approved peeled SHA"
    - from: scripts/verify_public_release.exs
      to: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
      via: "atomic VERIFIED output after current public identity and both immutable incident checks pass"
  prohibitions:
    - statement: "The executor may not rerun, repush, move, delete, overwrite, recreate, dispatch from, or publish v1.3.0 or v1.3.1."
      status: unverified
      flagged: true
    - statement: "The executor may not mutate v1.3.2 before fresh exact-SHA approval or use local/alternate Hex or HexDocs publication."
      status: unverified
      flagged: true
---

<objective>
Freshly approve the final exact candidate, publish v1.3.2 only through protected paths, and atomically verify every public prerequisite.

Purpose: cross the one-way boundary once, with the fixed regression and timeout policy proven immediately beforehand and both earlier failures preserved.
Output: exact approval record, successful protected release/HexDocs run identities, and a named-verifier public prerequisite for the clean-room proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-04-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
@.planning/debug/release-preflight-theme-hang.md
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@scripts/release_preflight_proof.exs
@scripts/verify_public_release.exs
@test/scripts/public_release_verifier_test.exs
@test/guardrails/required_checks_contract_test.exs
@test/mix/tasks/rendro_gen_theme_test.exs
@test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs
@mix.exs
@CHANGELOG.md

<interfaces>
Plan 131-04 defines one final release-bearing candidate SHA and a later control-plane record. `release.yml` can start only from a pushed version tag. `hexdocs.yml` accepts only exact v1.3.2 release_ref plus candidate SHA equal to the checked-out SHA. `Rendro.PublicReleaseVerifier` is the sole producer of `131-PUBLIC-PREREQUISITE.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the fresh read-only approval packet against the sealed SHA</name>
  <precondition>Plan 131-04 records one exact v1.3.2 candidate SHA, and `git diff --name-only candidate..HEAD` contains only `131-RELEASE-CANDIDATE.md`, `131-04-SUMMARY.md`, and `.planning/STATE.md`; any other path invalidates the candidate.</precondition>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md</files>
  <read_first>131-04-SUMMARY.md and candidate record; D-33; debug causal proof; exact candidate tree; release/HexDocs workflows; focused regression/verifier/preflight tests</read_first>
  <action>Capture the recorded candidate SHA without modifying source and assert the precondition's exact control-plane-only delta. Snapshot all tag refs, create a unique detached worktree directly at that SHA, and assert worktree HEAD equality. In that worktree rerun the bounded open-silent-FIFO theme conflict command under `timeout`/`gtimeout 10`; exit 0 plus the test's internal `Skipped lib/generated_theme.ex` assertion is required, and captured output must contain no interactive overwrite question. Separately rerun release.yml push-tag-only and 45/15-minute timeout/parser contracts, the exact-SHA wrapper/preflight tests, public-verifier tests, full `mix ci.fast`, `release_preflight_proof.exs --candidate-sha "$candidate_commit_sha"` with repeated CI/security gates/Hex dry run, independent package checksum/inventory, and docs build. Confirm `9dee9c8b837510191a1036a642e43e0b5dba2018` ancestry. Remove the worktree, compare the full tag-ref snapshot byte-for-byte, and prove no v1.3.2 ref exists; the legacy synthetic-tag mode and all git-tag commands are forbidden. Perform read-only public v1.3.2 absence and both prior incident checks. Only after success write `131-RELEASE-APPROVAL-PACKET.md` with SHA equality, FIFO result, timeout result, no-tag mode, unchanged refs, commands/results, package identities, both incidents, and deterministic/advisory labels. Stop before approval on any drift.</action>
  <verify>
    <automated>bash -c 'set -euo pipefail; d=$(mktemp -d); fifo="$d/stdin"; out="$d/output"; holder=""; trap '\''test -z "$holder" || kill "$holder" 2&gt;/dev/null || true; rm -rf "$d"'\'' EXIT; mkfifo "$fifo"; (exec 3&gt;"$fifo"; sleep 30) &amp; holder=$!; if command -v gtimeout &gt;/dev/null; then lim=gtimeout; else lim=timeout; fi; "$lim" 10 mix test test/mix/tasks/rendro_gen_theme_test.exs --trace &lt;"$fifo" &gt;"$out" 2&gt;&amp;1; ! grep -F "overwrite? [Yn]" "$out"' &amp;&amp; mix test test/scripts/release_preflight_proof_test.exs test/mix/tasks/release_preflight_test.exs test/scripts/public_release_verifier_test.exs test/guardrails/required_checks_contract_test.exs --max-failures 1 &amp;&amp; rg -q '9dee9c8b837510191a1036a642e43e0b5dba2018' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q 'proof_mode: exact-sha-no-tag' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q 'head_equals_candidate: true' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q 'fifo_regression: passed' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q 'overwrite_prompt_seen: false' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q 'tag_refs_unchanged: true' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q '32513353551' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q '32539594278' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md</automated>
  </verify>
  <done>A fresh, exact, non-mutating proof packet is ready for the human decision; candidate and public state remain unchanged.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve or reject exact v1.3.2 tag, Hex, and HexDocs together</name>
  <what-built>The final v1.3.2 candidate has just passed the exact fixed stdin regression, release timeout contracts, full private preflight/package/docs proof, and read-only public absence/history checks.</what-built>
  <read_first>131-RELEASE-APPROVAL-PACKET.md; 131-RELEASE-CANDIDATE.md; exact candidate commit diff; D-33; release.yml; hexdocs.yml</read_first>
  <decision>Whether the displayed exact candidate SHA may be used for annotated tag v1.3.2, protected tag-driven Hex publication, and candidate-bound protected HexDocs publication together.</decision>
  <context>The next task creates permanent external history. Neither approval for v1.3.0 nor approval/tagging for v1.3.1 transfers to this SHA, and neither older tag is in the mutation set.</context>
  <options>
    <option id="approve-exact-candidate">
      <name>Approve exact v1.3.2</name>
      <pros>Authorizes only the displayed candidate through the existing protected release and docs paths.</pros>
      <cons>Creates permanent tag, Hex package, and HexDocs history for 1.3.2.</cons>
    </option>
    <option id="reject-or-revise">
      <name>Reject or revise</name>
      <pros>Leaves all v1.3.2 public state absent.</pros>
      <cons>The public newcomer proof remains blocked until a new final candidate is proven and freshly approved.</cons>
    </option>
  </options>
  <verify>
    <automated>rg -q '^version: 1\.3\.2$' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q '^candidate_commit_sha: [0-9a-f]{40}$' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; test -f .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md</automated>
    <human-check>Approval must repeat the displayed 40-character SHA and authorize v1.3.2 tag, Hex, and HexDocs together; silence, auto-advance, generic approval, or partial approval stops execution.</human-check>
  </verify>
  <resume-signal>Type `approve exact v1.3.2 candidate &lt;40-char-sha&gt; for tag, Hex, and HexDocs` or `reject` with required changes.</resume-signal>
  <done>A fresh exact-SHA all-surfaces approval exists, or execution stops before mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Publish only v1.3.2 through protected paths and verify public identity</name>
  <precondition>The immediately preceding checkpoint explicitly approves the unchanged recorded candidate SHA for annotated v1.3.2 tag, protected Hex, and candidate-bound protected HexDocs together.</precondition>
  <reversibility rating="one-way">The v1.3.2 tag, Hex package, and HexDocs artifacts become permanent external release history.</reversibility>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json</files>
  <read_first>Literal approval response; fresh approval packet; candidate record; protected workflow definitions; generalized named verifier and tests; D-33 forbidden actions</read_first>
  <action>Immediately compare the literal approved SHA with the sealed candidate and approval packet, assert no repository bytes or tag refs changed since the checkpoint, and recheck local/remote v1.3.2 absence. With no intervening build or candidate work, create and push only a new annotated `refs/tags/v1.3.2` pointing directly at the approved candidate commit. Discover the new protected `Release to Hex` run caused by that tag push, record its exact run ID/event/head SHA, and wait for successful validation/preflight/dry-run/publish; any cancellation, timeout, failure, wrong SHA, or wrong event stops without retrying the tag or using another publisher. Independently verify exact Hex 1.3.2 before dispatching only the existing protected HexDocs workflow with ref/release_ref v1.3.2 and candidate_commit_sha equal to the approved SHA; record the new run ID/inputs/head SHA and wait for success. Invoke `Rendro.PublicReleaseVerifier` with the candidate record, v1.3.2, and the two new run IDs. Require current tag object/peeled identity, run identities, Hex archive checksum/inventory, versioned HexDocs source/symbols, and both historical incident/absence records to agree before atomically creating `131-PUBLIC-PREREQUISITE.json`. Never issue a rerun or mutation for v1.3.0/v1.3.1, and never invoke local Hex or HexDocs publication.</action>
  <verify>
    <automated>mix run scripts/verify_public_release.exs -- --candidate-record .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md --tag v1.3.2 --release-run-id "$RENDRO_RELEASE_RUN_ID" --hexdocs-run-id "$RENDRO_HEXDOCS_RUN_ID" --output .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json --check-existing</automated>
  </verify>
  <done>Exact v1.3.2 is public through only the approved protected paths, its prerequisite is VERIFIED, and both earlier failed tags/runs remain unchanged evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh proof → human approval | Exact read-only evidence becomes authority for a one-way decision. |
| Human approval → tag push | Literal SHA-scoped authorization crosses into permanent mutation. |
| Protected workflows → Hex/HexDocs | Repository secrets publish current package and docs. |
| Public services → prerequisite record | External identities and artifacts become bounded downstream evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-20 | Elevation of Privilege | release authorization | critical | mitigate | Fresh exact-SHA checkpoint immediately precedes the sole mutation task and names tag, Hex, and HexDocs together. |
| T-131-21 | Spoofing | candidate/tag/run identity | critical | mitigate | Revalidate candidate tree, annotated tag object/peeled SHA, workflow events/heads, and exact public version. |
| T-131-22 | Denial of Service | protected jobs | high | mitigate | Pre-tag regression plus 45/15-minute timeout contracts; failures stop without tag rerun or alternate publishing. |
| T-131-23 | Information Disclosure | release credentials | high | mitigate | Credentials remain inside protected environments and are neither read nor retained. |
| T-131-24 | Tampering | prior incidents/current prerequisite | critical | mitigate | Named verifier checks both immutable histories and writes current VERIFIED evidence atomically only after all public facts agree. |
| T-131-32 | Tampering | pre-approval private proof | critical | mitigate | Exact-SHA mode asserts detached HEAD equality, contains no git-tag command, and compares complete tag-ref snapshots before/after. |
</threat_model>

<verification>
The read-only proof completes first, the human checkpoint is the immediately preceding task to mutation, and the named verifier must bind all exact current and historical identities. Public/network results are advisory external evidence; the focused regression and workflow contracts remain deterministic authority.
</verification>

<success_criteria>
The approved final candidate is either rejected before mutation or becomes exact public v1.3.2 through only protected tag/Hex/HexDocs paths, with a VERIFIED prerequisite and both earlier failures untouched.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-05-SUMMARY.md` when done; retain literal approval, new run IDs, and both prior incident identities.
</output>
