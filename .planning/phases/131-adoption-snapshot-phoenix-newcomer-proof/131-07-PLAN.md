---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "07"
type: execute
wave: 7
depends_on: ["131-06"]
files_modified:
  - mix.exs
  - CHANGELOG.md
  - .github/workflows/hexdocs.yml
  - scripts/verify_public_release.exs
  - test/scripts/public_release_verifier_test.exs
  - test/docs_contract/launch_execution_claims_test.exs
autonomous: true
requirements: [JOURNEY-01, JOURNEY-02, JOURNEY-04]
must_haves:
  truths:
    - "D-35: every existing release-bearing exact-version, package/docs, workflow, verifier, and incident surface is committed as 1.3.4 before candidate capture, with bbe75d2 in ancestry."
    - "D-03/D-35: public consumer documentation remains ~> 1.3 while protected release, verifier, and retained journey contracts target exact 1.3.4."
    - "D-05/D-35: complete credential-free release.preflight is the validation gate; HEX_API_KEY exists only on actual protected publish, which is the authorization gate."
    - "D-32 through D-35: v1.3.0 through v1.3.3 retain exact immutable tag/run/job/absence facts and are rejected as release, verifier, dispatch, and journey targets."
  artifacts:
    - path: mix.exs
      provides: "Exact 1.3.4 project/package/ExDoc source identity"
    - path: .github/workflows/hexdocs.yml
      provides: "Candidate-bound exact v1.3.4 protected HexDocs dispatch contract"
    - path: scripts/verify_public_release.exs
      provides: "Exact public 1.3.4 atomic verifier with four immutable incident records"
  key_links:
    - from: bbe75d2bf3f53e5235626974c539500395d2032e
      to: .github/workflows/release.yml
      via: "ancestry and least-privilege guardrail prove complete preflight without the duplicate standalone dry run"
    - from: mix.exs
      to: .github/workflows/hexdocs.yml
      via: "exact 1.3.4 declaration, source ref, and dispatch parity"
    - from: scripts/verify_public_release.exs
      to: test/scripts/public_release_verifier_test.exs
      via: "exact public success plus v1.3.0-v1.3.3 immutable-history fixtures"
---

<objective>
Finish the complete existing exact v1.3.4 release, HexDocs, verifier, and four-incident contract on top of the least-privilege workflow fix.

Purpose: ensure the later candidate SHA includes every existing release-bearing/package/docs/workflow/verifier/incident change, while leaving the nonexistent clean-room harness and evidence to their post-verifier owner in Plan 10.
Output: one committed exact-1.3.4 release tree with four immutable incidents and the validation-versus-authorization boundary enforced; no harness, candidate, tag, publish, dispatch, or verifier output.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RESEARCH.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-05-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-06-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
@.planning/debug/v133-hex-dry-run.md
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@mix.exs
@CHANGELOG.md
@scripts/verify_public_release.exs

<interfaces>
`Mix.Project.config()[:version]` and ExDoc `source_ref` supply exact package/docs identity. `hexdocs.yml` accepts `candidate_commit_sha` plus `release_ref`. `Rendro.PublicReleaseVerifier.run/2` is the sole atomic writer of `131-PUBLIC-PREREQUISITE.json`. Plan 10 later creates the clean-room consumer of that prerequisite. `release.yml` at bbe75d2 runs `mix release.preflight` in credential-free validation and exposes `HEX_API_KEY` only to protected `mix hex.publish --yes`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace exact 1.3.4 through project, protected docs, and least-privilege release contracts</name>
  <files>mix.exs, CHANGELOG.md, .github/workflows/hexdocs.yml, test/docs_contract/launch_execution_claims_test.exs</files>
  <read_first>mix.exs, CHANGELOG.md, .github/workflows/release.yml, .github/workflows/hexdocs.yml, test/guardrails/required_checks_contract_test.exs, test/docs_contract/launch_execution_claims_test.exs, .planning/debug/v133-hex-dry-run.md</read_first>
  <behavior>
    - "Project version, changelog heading, ExDoc source ref, and HexDocs release_ref all equal 1.3.4."
    - "README/public dependency examples remain ~> 1.3."
    - "Candidate ancestry contains bbe75d2 and release validation retains Run Release Preflight without a second workflow dry-run step."
    - "Validation has no protected environment or HEX_API_KEY; actual publish retains the Hex Publish environment, nonempty-key check, and HEX_API_KEY."
  </behavior>
  <action>Write the exact-version and least-privilege assertions first. Then update the project, changelog, ExDoc source identity, and candidate-bound HexDocs dispatch to exact `1.3.4` per D-35 while preserving public `~> 1.3` examples per D-03. Keep `.github/workflows/release.yml` at the bbe75d2 contract: `Run Release Preflight` is the complete credential-free validation path, and only the protected `Publish to Hex` action receives `HEX_API_KEY`. Do not restore a separate workflow dry run because it does not test credential validity and caused the immutable v1.3.3 failure. Require `git merge-base --is-ancestor bbe75d2 HEAD` to pass. Do not capture a candidate or mutate public state.</action>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs test/docs_contract/launch_execution_claims_test.exs --max-failures 1 &amp;&amp; git merge-base --is-ancestor bbe75d2 HEAD &amp;&amp; test "$(mix run -e 'IO.write(Mix.Project.config()[:version])')" = "1.3.4"</automated>
  </verify>
  <acceptance_criteria>All project/docs/protected-dispatch exact versions are 1.3.4; public dependency copy remains ~> 1.3; bbe75d2 is an ancestor; the release guardrail proves complete credential-free preflight and publish-only HEX_API_KEY exposure.</acceptance_criteria>
  <done>The protected release and docs entry points agree on exact 1.3.4 without weakening or duplicating the preflight boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the atomic verifier to exact 1.3.4 and four immutable incidents</name>
  <files>scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs</files>
  <read_first>scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-06-SUMMARY.md, .planning/debug/v133-hex-dry-run.md</read_first>
  <behavior>
    - "Verifier success requires exact 1.3.4 tag object/peeled candidate, protected release success, public Hex archive checksum/inventory, candidate-bound HexDocs success/source/symbols, and no stale target."
    - "Verifier fixtures retain v1.3.3 tag object c96bf205d7216cdcf4846a0f24a312f9c1c75b0f, peeled SHA cfc58a81865e060351ce33d98f5e52de8cd198d9, run 32596108284, validate job 97087204354, publish job 97088652899, failed/skipped conclusions, four public absences, no docs dispatch, and no verifier."
    - "v1.3.0 through v1.3.3 cannot satisfy the success target or be omitted from the atomic prerequisite."
  </behavior>
  <action>Update `Rendro.PublicReleaseVerifier` and its fixtures per D-35 so exact `1.3.4` is the only success target and the v1.3.3 incident joins the three older immutable incidents. Bind the precise tag/run/job/absence/no-dispatch/no-verifier facts from the Plan 06 summary and debug record. Preserve atomic output semantics: no `VERIFIED` file exists unless protected release, public Hex archive, candidate-bound HexDocs, versioned source/symbols, candidate SHA, and all four incidents match together. Do not run the live verifier or write a prerequisite in this plan.</action>
  <verify>
    <automated>mix test test/scripts/public_release_verifier_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>Verifier tests accept only exact public 1.3.4 and require the complete immutable v1.3.0-v1.3.3 incident set before an atomic VERIFIED write.</acceptance_criteria>
  <done>The named verifier is source-complete for exact 1.3.4 and cannot erase or reuse any failed release.</done>
</task>

</tasks>

## Artifacts this phase produces

- Exact `1.3.4` values in `mix.exs`, `CHANGELOG.md`, and `.github/workflows/hexdocs.yml`.
- `Rendro.PublicReleaseVerifier` exact-1.3.4/four-incident behavior.
- No clean-room harness/test/evidence artifact; Plan 10 creates those only after Plan 09 writes the exact public prerequisite.
- No new public API, dependency, tag, package, docs publication, or prerequisite artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical public failures → verifier contracts | External incident facts become fail-closed source requirements. |
| Repository HEAD → future candidate | Every existing release-bearing/package/docs/workflow/verifier/incident surface must be committed before capture. |
| Validation job → protected publish | Credential-free proof crosses into credentialed irreversible authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-52 | Tampering | candidate ancestry | critical | mitigate | Require bbe75d2 ancestry and focused least-privilege guardrails before version commit. |
| T-131-53 | Information Disclosure | HEX_API_KEY | high | mitigate | Assert validation has no key/environment and only actual protected publish receives the secret. |
| T-131-54 | Spoofing | exact version surfaces | high | mitigate | Cross-check project, changelog, ExDoc, HexDocs, verifier, and fixtures at 1.3.4; Plan 10 separately owns the post-verifier harness. |
| T-131-55 | Repudiation | immutable incidents | high | mitigate | Bind exact v1.3.0-v1.3.3 identities and absences in verifier tests. |
</threat_model>

<verification>
Run `mix test test/guardrails/required_checks_contract_test.exs test/docs_contract/launch_execution_claims_test.exs test/scripts/public_release_verifier_test.exs --max-failures 1`, prove bbe75d2 ancestry, then run `mix ci.fast`. Confirm the completed Task 1/2 commits `593a14b`, `33067a6`, `ceec64e`, and `3cb2152` contain the exact release/HexDocs/verifier/four-incident surfaces. Confirm the clean-room harness/test paths remain absent and owned by Plan 10.
</verification>

<success_criteria>
The committed repository is a complete exact-1.3.4 release candidate source tree on top of bbe75d2, public docs remain ~> 1.3, four failures remain immutable, clean-room creation remains deferred until after the verifier, and no public or candidate mutation has occurred.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-07-SUMMARY.md` when done, recording completed commits `593a14b`, `33067a6`, `ceec64e`, and `3cb2152`, the available focused-test plus `mix ci.fast` result, and the explicit Plan-10 ownership of clean-room harness/tests/evidence.
</output>
