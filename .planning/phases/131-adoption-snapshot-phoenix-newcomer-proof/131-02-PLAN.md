---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "02"
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - mix.exs
  - CHANGELOG.md
  - README.md
  - test/docs_contract/preset_fonts_package_contract_test.exs
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
autonomous: false
requirements: [JOURNEY-01, JOURNEY-02]
must_haves:
  truths:
    - "D-01, D-02: The public-only journey is blocked until exact Rendro 1.3.0 is published; context approval alone never authorizes tag push or publication."
    - "D-03: Public installation copy is `{:rendro, \"~> 1.3\"}` while release and retained evidence use exact 1.3.0."
    - "D-04: Hex API, package contents, HexDocs version/source, preset/font/snippet/adapter surfaces must all be public before the clean-room plan can start."
    - "D-05: Version/tag parity, package allowlist, CI, release preflight, dry-run, protected publish environment, and post-publish checks remain the only release path."
    - "D-13, D-14: README starts the install -> select -> customize -> serve -> verify path at Invoice / Swiss / #2C6BED / light."
    - "D-15: README points to or embeds formatter-owned snippet output and never creates a divergent theme snippet."
    - "D-19, D-20: No UI is added; copy uses concrete verbs and verified outcomes and keeps capability limits adjacent."
    - "Automatic execution may complete candidate preparation, then must stop at the decision checkpoint before any tag push or registry mutation."
  artifacts:
    - path: mix.exs
      provides: "Exact 1.3.0 source/version/package contract while preserving optional Phoenix dependency"
    - path: README.md
      provides: "README-first public newcomer route with ~> 1.3 consumer declaration"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      provides: "Exact candidate SHA/version/package checksum/preflight/dry-run/tag-absence facts presented at approval"
  key_links:
    - from: mix.exs
      to: .github/workflows/release.yml
      via: "@version 1.3.0 equals v1.3.0 tag checked by protected workflow"
    - from: README.md
      to: lib/rendro/theme/snippet.ex
      via: "canonical Swiss/light guide/configurator selection"
    - from: checkpoint:decision
      to: tag push task
      via: "explicit approval is a hard precondition"
  prohibitions:
    - statement: "No pre-checkpoint task MUST push v1.3.0 or mutate Hex, HexDocs, GitHub releases, or another public registry."
      status: unverified
      flagged: true
    - statement: "The release MUST NOT introduce a second publication path, hard Phoenix dependency, unsupported claim, or divergent formatter snippet."
      status: unverified
      flagged: true
---

<objective>
Prepare and explicitly authorize the exact 1.3.0 release, publish only through the protected tag path, and fail closed on public-surface verification.

Purpose: make the already-shipped preset/configurator surface genuinely available to a public-only newcomer before the clean-room proof.
Output: verified candidate facts, a blocking one-way decision, protected publication evidence, and a verified public prerequisite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-01-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RESEARCH.md
@mix.exs
@CHANGELOG.md
@README.md
@guides/presets.md
@lib/rendro/theme/snippet.ex
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@scripts/release_preflight_proof.exs
@test/scripts/release_preflight_proof_test.exs
@test/docs_contract/preset_fonts_package_contract_test.exs

<interfaces>
`.github/workflows/release.yml` accepts `v*.*.*`, verifies tag/version equality, runs `mix ci`, `mix release.preflight`, and `mix hex.publish --dry-run`, then publishes via the protected `Hex Publish` environment. `Rendro.Theme.Snippet.usage_snippet/4` and `module_source/4` are the formatter authority; `Rendro.Adapters.Phoenix.render_pdf/3` is the public HTTP boundary.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prepare exact 1.3.0 as a fully verified, still-private release candidate</name>
  <files>mix.exs, CHANGELOG.md, README.md, test/docs_contract/preset_fonts_package_contract_test.exs, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</files>
  <read_first>131-CONTEXT.md D-01 through D-05 and D-13 through D-15/D-19/D-20; current mix.exs version/package/docs/optional deps; CHANGELOG release style; README discovery/preset links; guides/presets.md canonical marked snippet; Theme.Snippet public formatter output; release.yml and hexdocs.yml; release_preflight_proof.exs; preset_fonts_package_contract_test.exs</read_first>
  <behavior>
    - "Project version, ExDoc source_ref, candidate tag, and changelog release are exact 1.3.0."
    - "Consumer docs declare ~> 1.3 and lead through install/select/customize/serve/verify using the Swiss bright-blue light selection."
    - "Hex archive contains every existing required asset plus the adoption sidecar and public discovery/docs surfaces."
    - "Phoenix and Plug remain optional dependencies and no runtime dependency is added."
    - "Candidate preflight and dry-run operate without pushing or publishing any tag/package."
  </behavior>
  <action>Update `@version` and the additive release narrative to exact 1.3.0; add the concise README-first consumer route with `{:rendro, "~> 1.3"}`, the locked Swiss/light selection, links to the canonical preset/configurator formatter seam, app-owned document/controller boundary, adapter call, and response verification. Preserve capability limits and do not duplicate the marked snippet. Extend package contract assertions for every linked evidence/public surface. Run the full candidate `mix ci`, `mix release.preflight`, `mix hex.publish --dry-run`, package inventory inspection, docs build, exact tag/version checks, and remote/local tag-absence checks without creating or pushing a tag. Write `131-RELEASE-CANDIDATE.md` containing the exact candidate commit SHA, clean-tree state, version/tag, package archive SHA-256 and inventory result, CI/preflight/dry-run outputs/statuses, public current-version fact, and explicit `tag_pushed: false` / `registry_mutated: false`. Implement D-01, D-03, D-05, D-13, D-14, D-15, D-19, and D-20. Record assumption-delta `no-change` for primary noun `Rendro core`: the Phoenix adapter remains optional and core purity/identity is unchanged.</action>
  <acceptance_criteria>`mix.exs` is 1.3.0 with optional Phoenix/Plug unchanged; README has the coherent five-step route and range `~> 1.3`; formatter-owned selection remains single-source; the built package has the linked adoption evidence; CI, preflight, dry-run, and docs build pass; the candidate record contains exact facts and proves neither local nor remote v1.3.0 exists and no registry mutation occurred.</acceptance_criteria>
  <verify>
    <automated>mix test test/scripts/release_preflight_proof_test.exs test/docs_contract/preset_fonts_package_contract_test.exs test/docs_contract/configurator_phase_gate_test.exs --max-failures 1 &amp;&amp; mix ci.fast &amp;&amp; mix release.preflight &amp;&amp; mix hex.publish --dry-run &amp;&amp; test "$(mix run -e 'IO.write(Mix.Project.config()[:version])')" = "1.3.0" &amp;&amp; test -z "$(git tag -l v1.3.0)"</automated>
  </verify>
  <done>The exact candidate is locally and deterministically proven, its public consumer story is packaged, and no one-way external action has occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize or reject the one-way v1.3.0 publication</name>
  <read_first>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md; mix.exs; CHANGELOG.md; .github/workflows/release.yml; scripts/release_preflight_proof.exs</read_first>
  <action>Present the exact candidate commit SHA, version 1.3.0/tag v1.3.0, package checksum/inventory, `mix ci`, release-preflight, package dry-run, tag-absence, and no-registry-mutation facts from `131-RELEASE-CANDIDATE.md`. State that approval authorizes the next task to create and push only exact tag v1.3.0, which triggers the existing protected publish workflow; rejection or changed facts halt the plan. Do not accept context/discussion approval, auto-advance, silence, or a generic confirmation as release authorization.</action>
  <decision>Whether to push exact tag v1.3.0 for the recorded candidate and allow the existing protected workflow to publish it.</decision>
  <context>Tag push and Hex publication are externally visible one-way actions. Auto mode must stop here; no subsequent task may begin without explicit approval tied to the displayed candidate facts.</context>
  <options>
    <option id="approve-exact-candidate">
      <name>Approve exact v1.3.0 candidate</name>
      <pros>Unblocks the required public-only newcomer proof using the already-verified additive surface.</pros>
      <cons>Creates an externally visible tag/package version that cannot be erased as if it never happened.</cons>
    </option>
    <option id="reject-or-revise">
      <name>Reject or revise candidate</name>
      <pros>Preserves all public registries and tags unchanged.</pros>
      <cons>The public-Hex newcomer proof remains blocked until a new exact candidate is prepared and approved.</cons>
    </option>
  </options>
  <acceptance_criteria>The human sees exact candidate/version/preflight/dry-run facts and responds specifically `approve exact v1.3.0 candidate <candidate-sha>` or rejects/describes required changes. Any mismatch with the current candidate record halts.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q 'tag_pushed: false' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q 'registry_mutated: false' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</automated>
    <human-check>Confirm or reject the exact candidate using the resume signal; approval is never inferred.</human-check>
  </verify>
  <resume-signal>Type `approve exact v1.3.0 candidate &lt;candidate-sha&gt;` or `reject` with the required changes.</resume-signal>
  <done>Explicit candidate-bound authorization exists, or the plan stops with public state unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Push the approved tag and fail closed on every public prerequisite</name>
  <precondition>The immediately preceding checkpoint contains explicit approval for the unchanged candidate SHA and exact v1.3.0 tag.</precondition>
  <reversibility rating="one-way">Pushing the release tag invokes public Hex publication; undo cannot erase the externally observed package/version history.</reversibility>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</files>
  <read_first>The approved checkpoint response; unchanged 131-RELEASE-CANDIDATE.md; release.yml; hexdocs.yml; mix.exs; Hex/HexDocs capabilities marked INTEGRATE in COVERAGE.md</read_first>
  <action>Revalidate candidate SHA, clean tree, exact version, local/remote tag absence, and all recorded hashes immediately before mutation. Create annotated tag v1.3.0 at that exact SHA and push only `refs/tags/v1.3.0`; do not run a local publish command. Observe the existing release workflow through completion and protected environment, then verify public Hex API exact 1.3.0, fetch/inspect the public package contents, and verify versioned HexDocs source/version plus Theme, presets, explicit font registration, formatter/configurator assets, and `Rendro.Adapters.Phoenix.render_pdf/3`. Record workflow/run URLs, public checks, timestamps, package checksum, and final tag SHA in the candidate record. If any public surface is absent/stale or workflow status is not successful, mark the prerequisite failed and halt before Plan 03.</action>
  <acceptance_criteria>Remote tag v1.3.0 equals the approved candidate SHA; existing protected release workflow succeeds; Hex API latest/releases includes exact 1.3.0; fetched public archive includes all asserted files; HexDocs identifies 1.3.0 and exposes all locked preset/font/snippet/adapter surfaces. No alternate publish command/path was used. Plan 03 has a machine-checkable `public_prerequisite: VERIFIED` fact or remains blocked.</acceptance_criteria>
  <verify>
    <automated>test "$(git ls-remote --tags origin refs/tags/v1.3.0 | cut -f1)" = "$(rg -o '[0-9a-f]{40}' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md | head -1)" &amp;&amp; rg -q 'public_prerequisite: VERIFIED' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</automated>
  </verify>
  <done>Exact 1.3.0 is public through the protected path and every public package/docs surface required by the clean-room journey has been observed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository candidate → public tag/workflow | A local commit becomes an externally visible release trigger. |
| Protected workflow → Hex/HexDocs | Release credentials authorize permanent public publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-05 | Elevation of Privilege | tag/publication authorization | critical | mitigate | Blocking candidate-bound human decision immediately before the only mutating task; auto-advance is invalid. |
| T-131-06 | Spoofing | candidate/tag identity | high | mitigate | Exact SHA/version/tag/package digest revalidation before push and remote tag equality afterward. |
| T-131-07 | Information Disclosure | release credentials | high | mitigate | Credentials remain only in protected GitHub environments; plans/scripts never read, echo, or persist them. |
| T-131-08 | Tampering | public package/docs | high | mitigate | Inspect Hex API, fetched archive, versioned HexDocs, and required public symbols before downstream proof. |
</threat_model>

## Artifacts this phase produces

- Modified release/public docs: `mix.exs` exact 1.3.0, `CHANGELOG.md`, README-first `~> 1.3` journey.
- Extended test: package/public discovery assertions in `test/docs_contract/preset_fonts_package_contract_test.exs`.
- New evidence file: `131-RELEASE-CANDIDATE.md` with exact preflight, approval, workflow, public-package, and HexDocs facts.
- External artifact after explicit approval only: annotated tag `v1.3.0` and protected Hex package publication.
- No new publish CLI, dependency, core API, UI, schema, recipe, preset, or catalog item.

<verification>
Before the checkpoint, all local deterministic candidate gates are green and public state is unchanged. After explicit approval, the public prerequisite is fail-closed and must be VERIFIED before any Plan 03 work.
</verification>

<success_criteria>
The release is either safely paused at the explicit decision with no public mutation, or exact approved 1.3.0 is verifiably public through the existing protected path with all newcomer surfaces present.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-02-SUMMARY.md` when done.
</output>

