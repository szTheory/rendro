---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "02"
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/hexdocs.yml
  - test/guardrails/required_checks_contract_test.exs
  - test/scripts/public_release_verifier_test.exs
  - test/docs_contract/phoenix_newcomer_contract_test.exs
  - test/docs_contract/preset_fonts_package_contract_test.exs
  - scripts/verify_public_release.exs
  - mix.exs
  - CHANGELOG.md
  - README.md
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
autonomous: true
requirements: [JOURNEY-01, JOURNEY-02]
must_haves:
  truths:
    - "D-01 and D-32: the public-only journey remains blocked until exact Rendro 1.3.1 is public; v1.3.0 remains immutable failed-release history and is never retried, deleted, moved, overwritten, or used by an alternate publish path."
    - "Both protected workflow parser consumers select exactly one top-level @version declaration and fail on zero or multiple declarations; deterministic contracts reproduce the declaration-plus-source_ref multiline failure against both workflows."
    - "D-02 through D-05 as superseded by D-32: public installation copy remains `{:rendro, \"~> 1.3\"}`, while private candidate, HexDocs gates, verifier, and retained evidence pin exact 1.3.1 through the existing machinery only."
    - "Protected run 32513353551 is retained with failure conclusion and peeled v1.3.0 commit 3d014b8194782fc29bc685c0d5e84e4adc64b2c3; Hex and HexDocs 1.3.0 absence is re-observed and never represented as release success."
    - "D-13 through D-15, D-19, and D-20: README preserves install -> select -> customize -> serve -> verify, Swiss/light selection, formatter ownership, truthful limits, and the public `~> 1.3` range."
    - "A fully validated private exact 1.3.1 candidate exists with no public mutation and is ready for the separate fresh approval plan."
  artifacts:
    - path: .github/workflows/release.yml
      provides: "Protected tag-driven Hex workflow with exact-one declaration extraction"
    - path: .github/workflows/hexdocs.yml
      provides: "Candidate-bound protected HexDocs workflow using the same exact-one declaration extraction contract"
    - path: test/guardrails/required_checks_contract_test.exs
      provides: "Regression contracts for every protected workflow parser consumer"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      provides: "Fully validated private exact 1.3.1 candidate plus immutable failed v1.3.0 evidence"
    - path: scripts/verify_public_release.exs
      provides: "Fail-closed exact 1.3.1 public verifier retaining failed v1.3.0 run/tag facts"
  key_links:
    - from: test/guardrails/required_checks_contract_test.exs
      to: .github/workflows/release.yml
      via: "original two-line fixture, exact-one extraction, and zero/multiple failure"
    - from: test/guardrails/required_checks_contract_test.exs
      to: .github/workflows/hexdocs.yml
      via: "the same fixture and failure contract applied to the docs workflow parser consumer"
    - from: mix.exs
      to: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      via: "exact 1.3.1 version/source_ref, package checksum, preflight, dry-run, and no-mutation facts"
  prohibitions:
    - statement: "No task may delete, move, overwrite, recreate, or retry v1.3.0, nor publish Hex or HexDocs 1.3.0 through another path."
      status: unverified
      flagged: true
    - statement: "This plan may not create or push v1.3.1, dispatch HexDocs, publish to Hex, or mutate another public registry."
      status: unverified
      flagged: true
    - statement: "The recovery may not introduce a new release framework, local publish route, hard Phoenix dependency, unsupported claim, or divergent formatter snippet."
      status: unverified
      flagged: true
---

<objective>
Repair both protected workflow version parsers and prepare one fully validated, still-private exact v1.3.1 recovery candidate.

Purpose: eliminate the v1.3.0 multiline parser failure everywhere it can recur before asking for new release authorization.
Output: shared parser behavior under regression contract and an exact no-mutation v1.3.1 candidate ready for Plan 131-03 approval.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-01-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RESEARCH.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
@mix.exs
@CHANGELOG.md
@README.md
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@scripts/release_preflight_proof.exs
@scripts/verify_public_release.exs
@test/guardrails/required_checks_contract_test.exs
@test/scripts/public_release_verifier_test.exs
@test/docs_contract/phoenix_newcomer_contract_test.exs
@test/docs_contract/preset_fonts_package_contract_test.exs

<interfaces>
`.github/workflows/release.yml` and `.github/workflows/hexdocs.yml` are both protected workflow consumers of the project version. Each must implement the same observable extraction contract: select only one top-level `@version` string declaration, reject zero/multiple declarations, and compare the one extracted value to its exact release target. `Rendro.PublicReleaseVerifier` remains the sole later producer of `131-PUBLIC-PREREQUISITE.json`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair exact-one version extraction in both protected workflows</name>
  <reversibility rating="reversible">The parser changes and deterministic contracts are repository-local and can be reverted before any external release mutation.</reversibility>
  <files>.github/workflows/release.yml, .github/workflows/hexdocs.yml, test/guardrails/required_checks_contract_test.exs</files>
  <read_first>131-CONTEXT.md D-05/D-32; failed run 32513353551 evidence; every `@version` extraction in release.yml and hexdocs.yml; required_checks_contract_test.exs workflow parsing patterns</read_first>
  <behavior>
    - "release.yml selects only a top-level @version string declaration and fails with a diagnostic when the declaration count is zero or greater than one."
    - "hexdocs.yml uses the same exact-one declaration semantics before exact candidate/version parity and likewise fails on zero or multiple declarations."
    - "For each protected workflow parser consumer, a fixture containing @version 1.3.0 plus source_ref interpolation reproduces the former broad-match multiline result and proves the repaired parser yields only 1.3.0."
    - "Both workflow consumers have zero-declaration and duplicate-declaration failure coverage; no broad token-occurrence search remains as a version parser."
  </behavior>
  <action>Per D-32, keep both existing workflows and replace every broad `grep '@version' mix.exs`-style parser consumer in `.github/workflows/release.yml` and `.github/workflows/hexdocs.yml` with the same deterministic extraction semantics for exactly one top-level `@version` string declaration. Each workflow must count matching declarations, emit a useful error, and exit nonzero unless the count is one before using the extracted version in parity checks. Extend `test/guardrails/required_checks_contract_test.exs` so the exact v1.3.0-era declaration and interpolated `source_ref` lines are fed through the contract separately for release.yml and hexdocs.yml: demonstrate that the former broad occurrence search would return multiple lines, require the repaired consumer to return only `1.3.0`, and assert zero/duplicate declarations fail for both. Parse and inspect both workflow files in the contract so a future workflow-specific regression cannot hide behind coverage of the other file. Do not change release targets or candidate artifacts in this task.</action>
  <acceptance_criteria>Both protected workflows implement identical exact-one declaration behavior, every parser consumer is covered by the original multiline fixture plus zero/multiple cases, and neither workflow can reach parity or publication with an empty or multiline project version.</acceptance_criteria>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The v1.3.0 parser failure is regression-protected in every protected workflow that reads the project version.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare and fully validate the private exact v1.3.1 candidate</name>
  <reversibility rating="reversible">Version/docs/verifier changes and the private candidate remain repository-local until Plan 131-03 obtains fresh approval.</reversibility>
  <files>mix.exs, CHANGELOG.md, README.md, .github/workflows/hexdocs.yml, scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs, test/docs_contract/phoenix_newcomer_contract_test.exs, test/docs_contract/preset_fonts_package_contract_test.exs, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</files>
  <read_first>Completed Task 1 parser/contracts; CONTEXT D-01 through D-05, D-13 through D-15, D-19/D-20, D-32; current version/source_ref/changelog/docs/package declarations; verifier and fixtures; prior candidate/incident record</read_first>
  <behavior>
    - "Project version, ExDoc source_ref, changelog, HexDocs exact gates, verifier target, package/docs contracts, and candidate record agree on 1.3.1; README dependency copy remains ~> 1.3."
    - "The candidate record preserves annotated v1.3.0, peeled commit 3d014b8194782fc29bc685c0d5e84e4adc64b2c3, failed run 32513353551, failure-before-publish conclusion, and observed absence of Hex/HexDocs 1.3.0."
    - "Candidate validation runs focused tests, CI, release preflight, Hex dry-run, package inventory, docs build, local/remote v1.3.1 absence, and read-only v1.3.0 incident checks without external mutation."
  </behavior>
  <action>After both workflow parser contracts pass, bump `mix.exs` `@version` and its ExDoc `source_ref`, `CHANGELOG.md`, `.github/workflows/hexdocs.yml` exact dispatch gates, `scripts/verify_public_release.exs`, its fixtures, package/docs contracts, and candidate schema to exact 1.3.1 while leaving README dependency copy at `~> 1.3`. Preserve the existing README-first Swiss/light handoffs and formatter ownership. Extend the verifier/candidate record to retain `v1.3.0`, peeled commit `3d014b8194782fc29bc685c0d5e84e4adc64b2c3`, workflow run `32513353551`, failed conclusion/stage, and read-only proof that Hex package and HexDocs 1.3.0 are absent; these facts remain incident evidence only. Prepare a fresh candidate SHA after all parser/version/docs/verifier/package changes are committed. Run focused contracts, `mix ci.fast`, release preflight, Hex dry-run, package inventory, docs build, local/remote v1.3.1 tag absence, and remote v1.3.0 peeled identity. Write `131-RELEASE-CANDIDATE.md` with exact 1.3.1 SHA/checksum/workflow inputs plus `tag_pushed: false`, `hexdocs_dispatched: false`, and `registry_mutated: false`. Do not create/push a tag, dispatch a workflow, publish a package, retry v1.3.0, or emit `131-PUBLIC-PREREQUISITE.json` in this plan.</action>
  <acceptance_criteria>Every private candidate surface agrees on 1.3.1, public docs still say `~> 1.3`, optional Phoenix/Plug and deterministic/advisory separation remain unchanged, candidate evidence preserves run 32513353551 as failed with Hex/HexDocs 1.3.0 absent, and all candidate gates pass with no external mutation.</acceptance_criteria>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs test/scripts/release_preflight_proof_test.exs test/scripts/public_release_verifier_test.exs test/docs_contract/phoenix_newcomer_contract_test.exs test/docs_contract/preset_fonts_package_contract_test.exs --max-failures 1 &amp;&amp; mix ci.fast &amp;&amp; mix release.preflight &amp;&amp; mix hex.publish --dry-run &amp;&amp; test "$(mix run -e 'IO.write(Mix.Project.config()[:version])')" = "1.3.1" &amp;&amp; test "$(git ls-remote --tags origin 'refs/tags/v1.3.0^{}' | cut -f1)" = "3d014b8194782fc29bc685c0d5e84e4adc64b2c3" &amp;&amp; test -z "$(git ls-remote --tags origin refs/tags/v1.3.1)"</automated>
  </verify>
  <done>One exact 1.3.1 candidate is fully validated and private, with immutable failed v1.3.0 evidence ready for the fresh approval checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix.exs declaration → protected workflow parsers | Ambiguous extraction could misidentify the release version. |
| Failed public tag/run → recovery candidate | Immutable external history must be consumed as evidence, never rewritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-08 | Tampering | v1.3.0 incident and private v1.3.1 candidate | critical | mitigate | Preserve exact failed run/tag/absence facts, forbid v1.3.0 mutation, and validate all exact v1.3.1 private facts. |
| T-131-14 | Repudiation | release/HexDocs version parsers | high | mitigate | Both workflows reproduce the original multiline input and enforce exactly one declaration with zero/multiple failure. |
| T-131-07 | Information Disclosure | release credentials | high | mitigate | This plan performs dry runs/read-only checks only and never reads protected publication credentials. |
</threat_model>

## Artifacts this phase produces

- Repaired release.yml and hexdocs.yml exact-one parsers plus per-consumer deterministic regression contracts.
- Fully validated private exact v1.3.1 candidate, verifier, package/docs contracts, and public `~> 1.3` discovery route.
- Immutable incident record: v1.3.0 peeled commit `3d014b8194782fc29bc685c0d5e84e4adc64b2c3`, failed run `32513353551`, and Hex/HexDocs 1.3.0 absence.
- No public mutation, new release framework, local publish route, dependency, core API, UI, schema, recipe, preset, or catalog item.

<verification>
Run both-workflow parser regression contracts after Task 1. After Task 2, require all private candidate gates and exact incident/no-mutation facts before Plan 131-03 may present approval.
</verification>

<success_criteria>
Every protected workflow parser rejects ambiguous project versions, and exact v1.3.1 is a fully validated private candidate while v1.3.0 remains untouched failed-release history.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-02-SUMMARY.md` when done; preserve run `32513353551` and its failed conclusion.
</output>
