---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "02"
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - mix.exs
  - CHANGELOG.md
  - README.md
  - .github/workflows/hexdocs.yml
  - scripts/verify_public_release.exs
  - test/scripts/public_release_verifier_test.exs
  - test/docs_contract/phoenix_newcomer_contract_test.exs
  - test/docs_contract/preset_fonts_package_contract_test.exs
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
autonomous: false
requirements: [JOURNEY-01, JOURNEY-02]
must_haves:
  truths:
    - "D-01, D-02: The public-only journey is blocked until exact Rendro 1.3.0 is published; context approval alone never authorizes tag push or publication."
    - "D-03: Public installation copy is `{:rendro, \"~> 1.3\"}` while release and retained evidence use exact 1.3.0."
    - "D-04: A named fail-closed verifier must bind the peeled v1.3.0 tag, successful Hex and HexDocs workflow runs, Hex API/package contents, and versioned HexDocs symbols to the exact candidate before the clean-room plan can start."
    - "D-05: Version/tag parity, package allowlist, CI, release preflight, dry-run, protected publish environment, and post-publish checks remain the only release path."
    - "D-13, D-14: README starts the install -> select -> customize -> serve -> verify path at Invoice / Swiss / #2C6BED / light and explicitly links HexDocs depth, preset/configurator selection, and the runnable Phoenix example."
    - "D-15: README points to or embeds formatter-owned snippet output and never creates a divergent theme snippet."
    - "D-19, D-20: No UI is added; copy uses concrete verbs and verified outcomes and keeps capability limits adjacent."
    - "Automatic execution may complete candidate preparation, then must stop at the decision checkpoint before any tag push or registry mutation."
    - "A main/PR event may verify HexDocs readiness but cannot publish docs; exact 1.3.0 HexDocs publication is possible only through the explicitly approved candidate-bound dispatch after the checkpoint."
  artifacts:
    - path: mix.exs
      provides: "Exact 1.3.0 source/version/package contract while preserving optional Phoenix dependency"
    - path: README.md
      provides: "README-first public newcomer route with ~> 1.3 consumer declaration"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      provides: "Exact candidate SHA/version/package checksum/preflight/dry-run/tag-absence facts presented at approval"
    - path: scripts/verify_public_release.exs
      provides: "Named read-only fail-closed verifier for exact candidate tag, workflows, Hex archive, and versioned HexDocs"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
      provides: "Machine-readable exact-candidate public prerequisite record written only after every public check passes"
  key_links:
    - from: mix.exs
      to: .github/workflows/release.yml
      via: "@version 1.3.0 equals v1.3.0 tag checked by protected workflow"
    - from: README.md
      to: lib/rendro/theme/snippet.ex
      via: "canonical Swiss/light guide/configurator selection"
    - from: checkpoint:decision
      to: tag push and candidate-bound HexDocs workflow dispatch task
      via: "one explicit approval is the hard precondition for every irreversible mutation"
    - from: .github/workflows/hexdocs.yml
      to: exact candidate commit
      via: "workflow_dispatch inputs and checkout/version/SHA parity checks before protected docs publication"
    - from: scripts/verify_public_release.exs
      to: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
      via: "writes VERIFIED only after peeled tag, both workflow runs, Hex archive, and HexDocs symbols validate"
  prohibitions:
    - statement: "No pre-checkpoint task MUST push v1.3.0 or mutate Hex, HexDocs, GitHub releases, or another public registry."
      status: unverified
      flagged: true
    - statement: "The release MUST NOT introduce a second publication path, hard Phoenix dependency, unsupported claim, or divergent formatter snippet."
      status: unverified
      flagged: true
---

<objective>
Prepare and explicitly authorize the exact 1.3.0 release, publish Hex through the protected tag path and HexDocs through the existing candidate-bound workflow, and fail closed on public-surface verification.

Purpose: make the already-shipped preset/configurator surface genuinely available to a public-only newcomer before the clean-room proof.
Output: verified candidate facts, a blocking one-way decision, protected publication evidence, and a verified public prerequisite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-01-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RESEARCH.md
@mix.exs
@CHANGELOG.md
@README.md
@guides/presets.md
@lib/rendro/theme/snippet.ex
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@scripts/release_preflight_proof.exs
@scripts/verify_public_release.exs
@test/scripts/release_preflight_proof_test.exs
@test/scripts/public_release_verifier_test.exs
@test/docs_contract/phoenix_newcomer_contract_test.exs
@test/docs_contract/preset_fonts_package_contract_test.exs

<interfaces>
`.github/workflows/release.yml` accepts `v*.*.*`, verifies tag/version equality, runs `mix ci`, `mix release.preflight`, and `mix hex.publish --dry-run`, then publishes Hex via the protected `Hex Publish` environment. `.github/workflows/hexdocs.yml` currently verifies on main/PR and exposes `workflow_dispatch`, but its publish condition admits only a main push; this plan must extend that existing dispatch route with required `candidate_commit_sha` and `release_ref` inputs, exact checkout/SHA/version parity checks, and the same protected environment before exact 1.3.0 docs publication. `Rendro.Theme.Snippet.usage_snippet/4` and `module_source/4` are the formatter authority; `Rendro.Adapters.Phoenix.render_pdf/3` is the public HTTP boundary.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prepare exact 1.3.0 as a fully verified, still-private release candidate</name>
  <files>mix.exs, CHANGELOG.md, README.md, .github/workflows/hexdocs.yml, scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs, test/docs_contract/phoenix_newcomer_contract_test.exs, test/docs_contract/preset_fonts_package_contract_test.exs, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</files>
  <read_first>131-CONTEXT.md D-01 through D-05 and D-13 through D-15/D-19/D-20; current mix.exs version/package/docs/optional deps; CHANGELOG release style; README discovery/preset links and examples/phoenix_example/README.md; guides/presets.md canonical marked snippet; Theme.Snippet public formatter output; release.yml and hexdocs.yml event conditions/environments; release_preflight_proof.exs; preset_fonts_package_contract_test.exs; existing workflow guardrail tests</read_first>
  <behavior>
    - "Project version, ExDoc source_ref, candidate tag, and changelog release are exact 1.3.0."
    - "Consumer docs declare ~> 1.3 and lead through install/select/customize/serve/verify using the Swiss bright-blue light selection."
    - "README links directly to HexDocs for depth, guides/presets.md and the static configurator for selection, and examples/phoenix_example/README.md as the runnable broader reference; docs contracts fail on any missing handoff."
    - "Hex archive contains every existing required asset plus the adoption sidecar and public discovery/docs surfaces."
    - "Manual HexDocs dispatch requires exact candidate_commit_sha and release_ref inputs, verifies checkout SHA/version parity, and cannot publish on an unapproved or mismatched candidate."
    - "The public-release verifier stays read-only and refuses to write VERIFIED unless the peeled annotated tag, both workflow conclusions/heads, public archive, and exact versioned docs symbols all agree."
    - "Phoenix and Plug remain optional dependencies and no runtime dependency is added."
    - "Candidate preflight and dry-run operate without pushing or publishing any tag/package."
  </behavior>
  <action>Update `@version` and the additive release narrative to exact 1.3.0. Add the concise README-first consumer route with `{:rendro, "~> 1.3"}`, the locked Swiss/light selection, and visibly ordered install/select/customize/serve/verify actions. In that route add explicit links to the versioned HexDocs integration/reference depth, `guides/presets.md`, `assets/rendro/configurator/index.html`, and `examples/phoenix_example/README.md`; state that the example is broader runnable reference rather than clean-room authority. Preserve capability limits and do not duplicate the marked formatter snippet. Create/extend `phoenix_newcomer_contract_test.exs` to assert the five ordered route labels, exact consumer range, all four destinations, Swiss/light selection, formatter ownership, and example role. Extend `hexdocs.yml`'s existing `workflow_dispatch` route with required `candidate_commit_sha` and `release_ref` inputs; for dispatch, checkout the exact SHA, require it to equal the input, require `release_ref` v1.3.0 and `mix.exs` 1.3.0, keep verification before publication, and allow `publish-hexdocs` only for a successful exact-input dispatch under the protected `Hex Publish` environment. Remove main-push publication eligibility while preserving normal main/PR readiness verification, so committing candidate preparation cannot publish docs before approval. Add parsed workflow contract assertions for required inputs, exact parity gates, protected environment, and dispatch-only publication. Implement `Rendro.PublicReleaseVerifier` in `scripts/verify_public_release.exs` with injected read-only runners and tests for peeled-tag identity, GitHub workflow run name/conclusion/head SHA/event, Hex exact version and downloaded archive inventory, HexDocs exact version/source/symbol probes, atomic bounded record writing, and refusal to emit VERIFIED on any absent/mismatched fact. Extend package assertions, then run candidate CI/preflight/dry-run/package/docs/tag-absence checks without pushing or dispatching. Write `131-RELEASE-CANDIDATE.md` with an unambiguous `candidate_commit_sha`, exact version/tag/package/preflight/dry-run facts, and `tag_pushed: false`, `hexdocs_dispatched: false`, `registry_mutated: false`. Implement D-01, D-03 through D-05, D-13 through D-15, D-19, and D-20. Record assumption-delta `no-change` for primary noun `Rendro core`: the Phoenix adapter remains optional and core purity/identity is unchanged.</action>
  <acceptance_criteria>`mix.exs` is 1.3.0 with optional Phoenix/Plug unchanged. README has the coherent ordered five-step route, range `~> 1.3`, explicit HexDocs depth link, preset guide and configurator selection links, and runnable Phoenix example link/role; the docs contract asserts each. The HexDocs workflow's dispatch publish path is exact-SHA/version/input gated and protected; main/PR events verify only, and no publish job can run before approval or for a mismatched candidate. Parsed workflow tests prove these event and parity constraints. Fixture tests prove the named verifier cannot write VERIFIED for tag-object/peeled-tag confusion, wrong workflow head/conclusion, missing archive member, stale HexDocs version/source, or missing symbols. CI, preflight, dry-run, package, and docs build pass; the candidate record names one `candidate_commit_sha` and proves no tag push, HexDocs dispatch, or registry mutation occurred.</acceptance_criteria>
  <verify>
    <automated>mix test test/scripts/release_preflight_proof_test.exs test/scripts/public_release_verifier_test.exs test/docs_contract/phoenix_newcomer_contract_test.exs test/docs_contract/preset_fonts_package_contract_test.exs test/docs_contract/configurator_phase_gate_test.exs --max-failures 1 &amp;&amp; mix ci.fast &amp;&amp; mix release.preflight &amp;&amp; mix hex.publish --dry-run &amp;&amp; test "$(mix run -e 'IO.write(Mix.Project.config()[:version])')" = "1.3.0" &amp;&amp; test -z "$(git tag -l v1.3.0)"</automated>
  </verify>
  <done>The exact candidate is locally and deterministically proven, its public consumer story is packaged, and no one-way external action has occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize or reject the one-way v1.3.0 publication</name>
  <read_first>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md; mix.exs; CHANGELOG.md; .github/workflows/release.yml; .github/workflows/hexdocs.yml; scripts/release_preflight_proof.exs; scripts/verify_public_release.exs</read_first>
  <action>Present the exact `candidate_commit_sha`, version 1.3.0/tag v1.3.0, package checksum/inventory, `mix ci`, release-preflight, package dry-run, tag-absence, HexDocs dispatch inputs, and no-mutation facts from `131-RELEASE-CANDIDATE.md`. State that approval authorizes the immediately following task to (1) create and push only annotated tag v1.3.0 at that candidate, allowing the protected release workflow to publish Hex, and then (2) dispatch the existing HexDocs workflow with that same `candidate_commit_sha` and `release_ref=v1.3.0`, allowing protected exact-version docs publication. Rejection or changed facts halt before either mutation. Do not accept context/discussion approval, auto-advance, silence, partial approval of only one mutation, or a generic confirmation as authorization.</action>
  <decision>Whether to authorize the complete exact-candidate release mutation set: v1.3.0 tag push, protected Hex publication, and protected candidate-bound HexDocs workflow dispatch/publication.</decision>
  <context>The tag, Hex package, and HexDocs publication are externally visible one-way actions. Auto mode must stop here; no release mutation may begin without one explicit approval tied to all displayed candidate facts and both workflow paths.</context>
  <options>
    <option id="approve-exact-candidate">
      <name>Approve exact v1.3.0 candidate</name>
      <pros>Unblocks the required public-only newcomer proof using the already-verified additive surface.</pros>
      <cons>Creates externally visible tag, Hex package, and HexDocs version artifacts that cannot be erased as if they never happened.</cons>
    </option>
    <option id="reject-or-revise">
      <name>Reject or revise candidate</name>
      <pros>Preserves all public registries and tags unchanged.</pros>
      <cons>The public-Hex newcomer proof remains blocked until a new exact candidate is prepared and approved.</cons>
    </option>
  </options>
  <acceptance_criteria>The human sees exact candidate/version/preflight/dry-run and HexDocs dispatch facts, including every authorized mutation, and responds specifically `approve exact v1.3.0 candidate &lt;candidate-sha&gt; for tag, Hex, and HexDocs` or rejects/describes required changes. Any mismatch with the current candidate record or partial authorization halts.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q '^candidate_commit_sha: [0-9a-f]{40}$' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q 'tag_pushed: false' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q 'hexdocs_dispatched: false' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q 'registry_mutated: false' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</automated>
    <human-check>Confirm or reject the exact candidate using the resume signal; approval is never inferred.</human-check>
  </verify>
  <resume-signal>Type `approve exact v1.3.0 candidate &lt;candidate-sha&gt; for tag, Hex, and HexDocs` or `reject` with the required changes.</resume-signal>
  <done>Explicit candidate-bound authorization exists, or the plan stops with public state unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the approved Hex and HexDocs mutations, then run the fail-closed public verifier</name>
  <precondition>The immediately preceding checkpoint explicitly approves the unchanged `candidate_commit_sha` for all three mutations: exact v1.3.0 tag push, protected Hex publication, and protected candidate-bound HexDocs dispatch/publication.</precondition>
  <reversibility rating="one-way">The tag push, Hex package, and HexDocs publication create external version history that cannot be erased as if it never occurred.</reversibility>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json</files>
  <read_first>The approved checkpoint response; unchanged 131-RELEASE-CANDIDATE.md; release.yml; candidate-bound hexdocs.yml; scripts/verify_public_release.exs and its tests; mix.exs; Hex/HexDocs capabilities marked INTEGRATE in COVERAGE.md</read_first>
  <action>Revalidate `candidate_commit_sha`, clean tree, exact version, local/remote tag absence, workflow definitions, and recorded hashes immediately before mutation. Create annotated tag v1.3.0 at exactly that commit and push only `refs/tags/v1.3.0`; do not run a local publish command. Wait for the existing protected release workflow and record its exact successful run ID/head SHA. Only after Hex publication succeeds, dispatch the existing HexDocs workflow as `gh workflow run hexdocs.yml --ref v1.3.0 -f candidate_commit_sha=&lt;exact-sha&gt; -f release_ref=v1.3.0`; wait for its protected publish job and record its exact successful run ID/head SHA. Then invoke only the named read-only `scripts/verify_public_release.exs` command with the candidate SHA, v1.3.0, both run IDs, and output `131-PUBLIC-PREREQUISITE.json`. The verifier must compare the approved commit against the peeled remote ref `refs/tags/v1.3.0^{}`, not the annotated tag-object ref; validate both workflow conclusions/events/head SHAs, Hex API exact version, fetched archive checksum/inventory, and versioned HexDocs version/source plus Theme, presets, explicit font registration, formatter/configurator assets, and `Rendro.Adapters.Phoenix.render_pdf/3`. It writes `public_prerequisite: VERIFIED` only after all facts pass atomically; otherwise it writes no VERIFIED record, records failure in the candidate log, and halts before Plan 03.</action>
  <acceptance_criteria>The peeled remote v1.3.0 commit equals `candidate_commit_sha`; the tag-object SHA is recorded separately and never compared as the commit. Both protected workflow runs conclude success at the exact candidate, Hex exposes exact 1.3.0 and the fetched archive contains all asserted files, and versioned HexDocs identifies 1.3.0/source candidate and exposes all locked symbols/assets. `131-PUBLIC-PREREQUISITE.json` is produced by the named verifier, includes exact run IDs/hashes/checksums/probes, and has `public_prerequisite: VERIFIED`; no grep or manually edited assertion can substitute. No alternate Hex or docs publishing path was used.</acceptance_criteria>
  <verify>
    <automated>mix run scripts/verify_public_release.exs -- --candidate-record .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md --tag v1.3.0 --release-run-id "$RENDRO_RELEASE_RUN_ID" --hexdocs-run-id "$RENDRO_HEXDOCS_RUN_ID" --output .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json --check-existing</automated>
  </verify>
  <done>Exact 1.3.0 is public through the protected path and every public package/docs surface required by the clean-room journey has been observed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository candidate → public tag/workflow | A local commit becomes an externally visible release trigger. |
| Protected workflow → Hex/HexDocs | Release credentials authorize permanent public publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-05 | Elevation of Privilege | tag/Hex/HexDocs authorization | critical | mitigate | One blocking candidate-bound human decision immediately before the sole task containing all release mutations; auto-advance and partial approval are invalid. |
| T-131-06 | Spoofing | candidate/tag/workflow identity | high | mitigate | Exact candidate/version revalidation, peeled annotated-tag comparison, and both workflow head/event/input checks. |
| T-131-07 | Information Disclosure | release credentials | high | mitigate | Credentials remain only in protected GitHub environments; plans/scripts never read, echo, or persist them. |
| T-131-08 | Tampering | public package/docs prerequisite | high | mitigate | Named tested read-only verifier atomically emits VERIFIED only after workflow, Hex archive, and exact-version HexDocs checks pass. |
</threat_model>

## Artifacts this phase produces

- Modified release/public docs: `mix.exs` exact 1.3.0, `CHANGELOG.md`, README-first `~> 1.3` journey.
- Modified release workflow: `.github/workflows/hexdocs.yml` gains exact-candidate/version-gated protected publication for its existing manual dispatch route.
- Extended tests: package assertions plus `phoenix_newcomer_contract_test.exs` README handoffs and `public_release_verifier_test.exs` fail-closed identity/surface cases.
- New verifier: `scripts/verify_public_release.exs` / `Rendro.PublicReleaseVerifier` with `--candidate-record`, `--tag`, both run IDs, `--output`, and `--check-existing` flags.
- New evidence files: `131-RELEASE-CANDIDATE.md` with `candidate_commit_sha` and preflight/approval/mutation facts; `131-PUBLIC-PREREQUISITE.json` with named-verifier workflow/Hex/HexDocs facts.
- External artifacts after explicit approval only: annotated tag `v1.3.0`, protected Hex package publication, and protected exact-candidate HexDocs publication.
- No new publish CLI, dependency, core API, UI, schema, recipe, preset, or catalog item.

<verification>
Before the checkpoint, all local deterministic candidate gates are green and public state is unchanged. After explicit approval, the public prerequisite is fail-closed and must be VERIFIED before any Plan 03 work.
</verification>

<success_criteria>
The release is either safely paused at the explicit decision with no public mutation, or exact approved 1.3.0 is verifiably public through the existing protected path with all newcomer surfaces present.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-02-SUMMARY.md` when done.
</output>
