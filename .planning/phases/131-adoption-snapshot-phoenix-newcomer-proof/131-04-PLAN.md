---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "04"
type: execute
wave: 4
depends_on: ["131-03"]
files_modified:
  - .github/workflows/hexdocs.yml
  - mix.exs
  - CHANGELOG.md
  - lib/mix/tasks/release/preflight.ex
  - scripts/release_preflight_proof.exs
  - scripts/verify_public_release.exs
  - test/scripts/release_preflight_proof_test.exs
  - test/scripts/public_release_verifier_test.exs
  - test/mix/tasks/release_preflight_test.exs
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
autonomous: true
requirements: [JOURNEY-01, JOURNEY-02, JOURNEY-04]
must_haves:
  truths:
    - "D-33: v1.3.0/run 32513353551 and v1.3.1/run 32539594278 remain immutable failed public-release history; neither tag is moved, retried, repushed, dispatched, or published through another path."
    - "D-33: commit 9dee9c8 is an ancestor of the candidate, a bounded open-silent-FIFO regression completes through Mix.Shell.Process with the asserted Skipped result and no overwrite prompt, and release validation/publish jobs retain separate 45/15-minute timeout caps."
    - "D-01 through D-05 as superseded by D-32/D-33: the next exact private candidate is 1.3.2 while all public installation documentation remains ~> 1.3."
    - "The exact candidate SHA is captured only after the final release-bearing commit; every focused/full/ref-free preflight/dry-run/package/docs proof runs in a detached worktree whose HEAD equals that SHA before any control-plane record names it."
    - "The ref-free proof mode never executes git tag, never creates refs/tags/v1.3.2, and proves the complete tag-ref set is byte-identical before and after."
    - "No local/remote v1.3.2 tag, Hex 1.3.2 release, HexDocs 1.3.2 page, protected workflow dispatch, or registry mutation exists when the plan finishes."
  artifacts:
    - path: scripts/verify_public_release.exs
      provides: "Version-general exact-candidate public verifier with two immutable incident checks"
    - path: scripts/release_preflight_proof.exs
      provides: "Exact-SHA detached-worktree proof mode with no tag creation or mutation"
    - path: lib/mix/tasks/release/preflight.ex
      provides: "Candidate-SHA parity mode that preserves tag parity for protected releases"
    - path: test/scripts/public_release_verifier_test.exs
      provides: "Fail-closed verifier tests for 1.3.2 and v1.3.0/v1.3.1 history"
    - path: .github/workflows/hexdocs.yml
      provides: "Exact v1.3.2 candidate-bound protected HexDocs dispatch"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      provides: "Control-plane record naming the sealed candidate SHA and both failed incidents"
  key_links:
    - from: commit 9dee9c8b837510191a1036a642e43e0b5dba2018
      to: exact v1.3.2 candidate commit
      via: "git ancestry plus bounded FIFO regression and independent workflow timeout contracts"
    - from: exact candidate SHA
      to: scripts/release_preflight_proof.exs
      via: "--candidate-sha detached worktree, HEAD equality assertion, and unchanged refs/tags snapshot"
    - from: mix.exs
      to: .github/workflows/hexdocs.yml
      via: "exact 1.3.2 declaration, source_ref v1.3.2, release_ref v1.3.2, and candidate SHA parity"
    - from: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
      to: scripts/verify_public_release.exs
      via: "record-driven exact target and immutable incident schema"
  prohibitions:
    - statement: "No command may create, move, delete, recreate, repush, retry, or publish v1.3.0 or v1.3.1."
      status: unverified
      flagged: true
    - statement: "No command may create or push v1.3.2, dispatch HexDocs, invoke a publisher, or mutate a registry in this plan."
      status: unverified
      flagged: true
---

<objective>
Close both failed-release incidents and seal one exact private v1.3.2 candidate without crossing any public mutation boundary.

Purpose: make the next approval decision rest on the fixed stdin regression, bounded workflow jobs, exact package/docs state, and a final candidate commit rather than either immutable failed tag.
Output: generalized verifier/contracts, exact 1.3.2 release surfaces, exhaustive private proof, and a candidate control record naming the final candidate SHA.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-03-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RESEARCH.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
@.planning/debug/release-preflight-theme-hang.md
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@mix.exs
@CHANGELOG.md
@lib/mix/tasks/release/preflight.ex
@scripts/release_preflight_proof.exs
@scripts/verify_public_release.exs
@test/scripts/release_preflight_proof_test.exs
@test/scripts/public_release_verifier_test.exs
@test/guardrails/required_checks_contract_test.exs
@test/mix/tasks/rendro_gen_theme_test.exs
@test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs
@test/mix/tasks/release_preflight_test.exs

<interfaces>
`Rendro.PublicReleaseVerifier.run/2` consumes a candidate record plus exact release/HexDocs run IDs and writes the public prerequisite only after `validate/1`. `Mix.Tasks.Release.Preflight` currently enforces exact tag parity; the new private-only `--candidate-sha` path must enforce HEAD equality while leaving the protected no-option tag behavior unchanged. The wrapper's legacy synthetic-tag option is not used by this recovery; its new exact-SHA path must never call `git tag`. `release.yml` is push-tag-only with 45/15-minute job caps.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add a ref-free exact-SHA preflight path and bounded FIFO regression</name>
  <files>lib/mix/tasks/release/preflight.ex, test/mix/tasks/release_preflight_test.exs, scripts/release_preflight_proof.exs, test/scripts/release_preflight_proof_test.exs</files>
  <read_first>D-33; debug session causal reproduction; current release preflight tag-parity command; wrapper option parsing, synthetic-tag lifecycle, worktree cleanup, and command injection; existing release-preflight unit/wrapper tests; Mix.Tasks.RendroGenThemeTest conflict assertion; release.yml 45/15-minute caps and push-tag-only trigger</read_first>
  <behavior>
    - "`mix release.preflight --candidate-sha <40-lowercase-hex>` replaces only exact-tag discovery with `git rev-parse HEAD` equality; ordinary protected release invocation still requires exact tag parity."
    - "`release_preflight_proof.exs --candidate-sha <sha> --worktree <path>` adds a detached worktree directly at that SHA, asserts worktree HEAD equality, runs the complete preflight/dry-run path, and removes the worktree without invoking git tag."
    - "Tests reject malformed/mismatched SHA, mutually exclusive synthetic-tag/ref/exact-SHA modes, any command trace containing git tag in exact-SHA mode, and any changed tag-ref snapshot."
    - "A bounded open-silent FIFO supplies stdin to the exact theme conflict test; it exits successfully within 10 seconds because the test's internal assertion observes Skipped, and captured outer output contains no overwrite prompt."
    - "The FIFO regression is independent from the release.yml 45/15-minute timeout contract; both must pass."
  </behavior>
  <action>Write failing tests first. Add a strict `--candidate-sha` option to `Mix.Tasks.Release.Preflight` which is mutually exclusive with tag-derived parity and compares lowercase 40-hex input to `git rev-parse HEAD`; keep the no-option protected behavior byte-for-byte on exact `git describe --tags --exact-match` parity. Add matching wrapper mode `--candidate-sha SHA` that uses `git worktree add --detach PATH SHA`, asserts `git -C PATH rev-parse HEAD` equals SHA before Mix commands, passes `--candidate-sha SHA` into `mix release.preflight`, and records the complete `git show-ref --tags` output before/after to prove no ref mutation. In this mode, no code path may call tag creation/restoration helpers; tests must inspect the injected command trace, not merely final cleanup. Separately run the exact conflict test with an open silent named pipe held by a background descriptor under `timeout`/`gtimeout 10`, capture output, require exit 0 (the test itself asserts `Skipped lib/generated_theme.ex`), and reject any interactive overwrite question; use a trap to kill the holder and remove the temporary directory. Keep the 45/15-minute workflow assertions in `test/guardrails/required_checks_contract_test.exs` as a separate command gate. Do not create any Git ref or public mutation.</action>
  <verify>
    <automated>mix test test/scripts/release_preflight_proof_test.exs test/mix/tasks/release_preflight_test.exs test/guardrails/required_checks_contract_test.exs --max-failures 1 &amp;&amp; bash -c 'set -euo pipefail; d=$(mktemp -d); fifo="$d/stdin"; out="$d/output"; holder=""; trap '\''test -z "$holder" || kill "$holder" 2&gt;/dev/null || true; rm -rf "$d"'\'' EXIT; mkfifo "$fifo"; (exec 3&gt;"$fifo"; sleep 30) &amp; holder=$!; if command -v gtimeout &gt;/dev/null; then lim=gtimeout; else lim=timeout; fi; "$lim" 10 mix test test/mix/tasks/rendro_gen_theme_test.exs --trace &lt;"$fifo" &gt;"$out" 2&gt;&amp;1; ! grep -F "overwrite? [Yn]" "$out"'</automated>
  </verify>
  <done>The private proof can exercise exact candidate parity without touching refs, and the open-stdin regression plus independent workflow timeout contracts fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit every release-bearing v1.3.2 change before candidate proof</name>
  <files>mix.exs, CHANGELOG.md, .github/workflows/hexdocs.yml, scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs</files>
  <read_first>Completed Task 1; D-01 through D-05 and D-32/D-33; both historical incident facts; current verifier option/fixture/allowlist/atomic-write contracts; current HexDocs exact-candidate gate; version/source_ref/package/docs definitions; existing 1.3.1 changelog narrative; public README range contract</read_first>
  <behavior>
    - "Project version, ExDoc source_ref, changelog, HexDocs gate, verifier policy, package metadata, and tests agree on exact 1.3.2 while README installation remains ~> 1.3."
    - "The generalized verifier requires v1.3.0 peeled SHA/run failure/absence and v1.3.1 tag-object/peeled SHA/run cancellation/publish-skipped/absence before current VERIFIED output."
    - "All release-bearing source, tests, workflows, package/docs declarations, and verifier schema changes are committed before the candidate SHA is captured."
  </behavior>
  <action>Write failing exact-version/verifier tests first, then set release-bearing surfaces to 1.3.2 per D-33 and add an honest changelog entry for the release-deadlock repair without portraying failed 1.3.1 as published. Keep README dependency snippets at `~> 1.3` per D-03/D-33. Generalize `Rendro.PublicReleaseVerifier` around the strict candidate record and exact v1.3.2 policy; extend live/fixture validation and bounded output with v1.3.0 peeled SHA `3d014b8194782fc29bc685c0d5e84e4adc64b2c3`/run `32513353551` plus v1.3.1 tag object `b386d1e39b6c9e63af58aa1fa5890d93909d278f`/peeled SHA `7afb1dd056bba234d1bd4ec1c4487f2ea8e308f1`/run `32539594278`, skipped publish, and absent Hex/HexDocs for both. Retarget only the existing protected HexDocs exact-candidate gate to v1.3.2. Run focused unit/contracts sufficient to catch syntax and contract errors, then commit all Task 1 and Task 2 release-bearing changes. Do not create or update the 1.3.2 candidate/evidence record yet; the next task captures this commit SHA first and performs the exhaustive proof against that immutable object.</action>
  <verify>
    <automated>mix test test/scripts/release_preflight_proof_test.exs test/mix/tasks/release_preflight_test.exs test/scripts/public_release_verifier_test.exs test/guardrails/required_checks_contract_test.exs test/mix/tasks/rendro_gen_theme_test.exs test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs --max-failures 1</automated>
  </verify>
  <done>One clean commit contains every release-bearing v1.3.2 change; no candidate SHA/evidence record has been written and no ref/public state changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the captured SHA without refs, then bind only control-plane evidence</name>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md</files>
  <read_first>Clean Task 2 commit; exact-SHA no-tag wrapper/tests; bounded FIFO procedure; current historical candidate record; D-32/D-33; package/docs/verifier contracts</read_first>
  <action>Immediately after Task 2's clean commit, capture `candidate_commit_sha=$(git rev-parse HEAD)` and assert it is 40 lowercase hex plus contains ancestor `9dee9c8b837510191a1036a642e43e0b5dba2018`. Snapshot the complete tag-ref set. Create a unique detached worktree pinned directly to that SHA and assert its `git rev-parse HEAD` equals the captured value. Inside that worktree run: the bounded open-silent-FIFO theme conflict regression with exit 0/internal Skipped assertion/no overwrite prompt; focused no-tag/preflight/verifier/workflow tests; full `mix ci.fast`; the new `release_preflight_proof.exs --candidate-sha "$candidate_commit_sha"` mode with repeated CI, security gates, and Hex dry run; independent `mix hex.build` with checksum/inventory; and `mix docs`. Perform read-only local/remote v1.3.2 tag, Hex, and HexDocs absence checks plus exact prior-incident checks. Remove the worktree and assert the complete tag-ref set is unchanged and no `refs/tags/v1.3.2` exists. Only after every proof succeeds, update `131-RELEASE-CANDIDATE.md` with the captured SHA, version/ref, checksums, exact commands/results, no-public-mutation facts, and both incidents, then commit that record as control-plane-only. Assert `git diff --name-only "$candidate_commit_sha"..HEAD` contains only the candidate record at this point. Any proof failure or release-bearing edit invalidates the SHA and returns execution to Task 2; the candidate never includes its own evidence record.</action>
  <verify>
    <automated>record=.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md &amp;&amp; rg -q '^version: 1\.3\.2$' "$record" &amp;&amp; rg -q '^candidate_commit_sha: [0-9a-f]{40}$' "$record" &amp;&amp; rg -q 'proof_mode: exact-sha-no-tag' "$record" &amp;&amp; rg -q 'tag_refs_unchanged: true' "$record" &amp;&amp; rg -q 'fifo_regression: passed' "$record" &amp;&amp; rg -q '32513353551' "$record" &amp;&amp; rg -q '32539594278' "$record" &amp;&amp; rg -q 'tag_pushed: false' "$record" &amp;&amp; candidate_sha=$(sed -nE 's/^candidate_commit_sha:[[:space:]]*([0-9a-f]{40})$/\1/p' "$record") &amp;&amp; test "$(git rev-parse "$candidate_sha")" = "$candidate_sha" &amp;&amp; unexpected=$({ git diff --name-only "$candidate_sha"..HEAD; git diff --name-only; git diff --cached --name-only; } | sort -u | grep -v '^\.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE\.md$' || true) &amp;&amp; test -z "$unexpected"</automated>
  </verify>
  <done>The exact source commit passed every proof without creating a tag, and only a later control-plane record names it; no one-way action occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical public state → recovery record | Immutable tags, workflow results, and registry absence are external facts imported read-only. |
| Repository source → candidate SHA | Release-bearing changes become the exact object later considered for publication. |
| Candidate commit → later control-plane commit | The record may name the candidate but must not silently alter its released tree. |
| Exact-SHA proof → Git refs | Private validation must observe the candidate without creating or changing any tag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-16 | Tampering | failed tag/run history | critical | mitigate | Bind both tag identities, exact run IDs/conclusions, skipped publish, and public absences; all probes are read-only. |
| T-131-17 | Denial of Service | repeated release preflight | high | mitigate | Require the bounded open-silent-FIFO Process-shell regression and separate release 45/15-minute timeout contracts before candidate sealing. |
| T-131-18 | Spoofing | candidate identity | high | mitigate | Commit first, capture SHA, assert detached-worktree HEAD equality for every proof, then record the full SHA only afterward. |
| T-131-19 | Repudiation | private proof claims | medium | mitigate | Record exact commands, exit results, checksums, tag absence, and deterministic versus advisory classification. |
| T-131-31 | Tampering | private proof tag refs | critical | mitigate | Exact-SHA mode contains no git-tag command and compares complete tag-ref snapshots before/after. |
</threat_model>

<verification>
Commit all release-bearing changes before capturing the SHA. Against an equality-asserted detached worktree at that SHA, run the bounded FIFO regression, focused contracts, full `mix ci.fast`, ref-free repeated-CI preflight/dry-run, package/docs checks, and read-only absence/history probes. Compare complete tag refs before/after; only then write the separate control-plane record.
</verification>

<success_criteria>
One final release-bearing commit is ready for exact-SHA approval as v1.3.2; the open-stdin regression, timeout policy, full ref-free proof, package/docs evidence, SHA equality, and unchanged tag refs are recorded only afterward, both older failures remain exact, and no external mutation occurred.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-04-SUMMARY.md` when done; name the candidate commit separately from later control-plane commits.
</output>
