---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "13"
type: execute
wave: 11
depends_on: ["131-10"]
files_modified:
  - scripts/verify_public_release.exs
  - test/scripts/public_release_verifier_test.exs
autonomous: true
requirements: [JOURNEY-04]
gap_closure: true
must_haves:
  truths:
    - "D-04/D-35: competing valid verifier writers establish exactly one complete VERIFIED prerequisite without replacing an authoritative target."
    - "The public verifier uses one exclusive no-replace commit operation after syncing a unique temporary file and removes every temporary artifact."
    - "Existing --check-existing behavior remains a read-only byte/mtime-preserving revalidation path."
  artifacts:
    - path: scripts/verify_public_release.exs
      provides: "Exclusive, no-replace public prerequisite publication"
    - path: test/scripts/public_release_verifier_test.exs
      provides: "Competing-writer, pre-existing-target, cleanup, and check-existing regressions"
  key_links:
    - from: verified public facts
      to: public prerequisite JSON
      via: "synced exclusive temporary file followed by hard-link/no-replace publication and unconditional cleanup"
    - from: public prerequisite JSON
      to: clean-room journey
      via: "one immutable complete VERIFIED record; check-existing validates without rewrite"
  prohibitions: []
  flagged_assumptions:
    - "JOURNEY-03 was unclassified by the spec-less edge probe; this closure assumes the already-verified ConnCase and loopback PDF response facts remain unchanged because only prerequisite publication is modified."
    - "JOURNEY-04 was unclassified by the spec-less edge probe; this plan resolves the verifier-discovered concurrent no-overwrite edge while retaining exact versions, commands, results, and bounded repair history."
---

<objective>
Make public prerequisite publication exclusive under competing verifier processes.

Purpose: ensure the clean-room journey can trust one authoritative VERIFIED record that no concurrent writer can replace.
Output: a synced no-replace verifier writer and a deterministic competing-writer regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VERIFICATION.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-07-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-09-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-10-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@scripts/verify_public_release.exs
@test/scripts/public_release_verifier_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one VERIFIED prerequisite under competing writers</name>
  <files>scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs</files>
  <read_first>scripts/verify_public_release.exs, test/scripts/public_release_verifier_test.exs, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-09-SUMMARY.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VERIFICATION.md</read_first>
  <behavior>
    - "Two synchronized valid writers targeting one absent path yield exactly one :ok and one output-already-exists error."
    - "The winning target is one complete bounded VERIFIED record and is never replaced by the losing writer."
    - "Pre-existing target bytes remain unchanged; target-specific temporary artifacts are absent after success, loss, or error."
    - "--check-existing revalidates a matching record without changing bytes or mtime."
  </behavior>
  <action>Write the competing-writer test first using a fresh randomized directory, a start barrier, two independently executed valid writes, and assertions over both results, final bytes, and temp-file cleanup. Then replace the `File.exists?/1` plus `File.rename/2` publication authority in `write_verified/2` with a unique exclusive temporary file, checked write/close/sync, and an exclusive no-replace operation such as `:file.make_link/2`. Treat an existing destination as the existing public error contract, never as permission to overwrite, and remove the temp file in an unconditional cleanup path. Preserve validation and `bounded_record/1`; keep `write_or_check_verified/2` semantics so an existing matching record is read-only revalidated and a mismatching record fails without mutation. Do not rewrite the committed prerequisite or perform live Hex/GitHub calls.</action>
  <verify>
    <automated>mix test test/scripts/public_release_verifier_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>The focused suite proves one competing winner, an immutable complete target, no leaked temps, sequential overwrite refusal, and byte/mtime-preserving check-existing behavior.</acceptance_criteria>
  <done>The named public verifier truthfully implements its exclusive single-writer prerequisite contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new production module, external file format, or public API symbol.
- New test `competing writers publish exactly one VERIFIED prerequisite without overwrite` in `Rendro.PublicReleaseVerifierTest`.

<assumption_delta_decision>
result: no-change
signal: ROADMAP uses the phrase "optional Phoenix adapter"
rationale: The verifier continues to authorize the existing singular `Rendro.Adapters.Phoenix` journey prerequisite; changing local file publication does not add or pluralize adapter identities.
</assumption_delta_decision>

<api_coverage_decision>
Preserve `COVERAGE.md` unchanged. The plan modifies only the local final-write primitive after already-bounded verification facts have been collected; it adds no Hex/GitHub endpoint, query, mutation, or polling behavior.
</api_coverage_decision>

<prohibition_recall>
Descriptor-less recall found no bespoke values/safety/ethics prohibition for the no-replace file primitive. Overwrite refusal and cleanup are routine correctness constraints; public-fact spoofing is canonical security and remains in the STRIDE register. No check descriptor is fabricated.
</prohibition_recall>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Verified external facts → local prerequisite | Validated public release identities become an authoritative local record. |
| Competing verifier processes → output path | Multiple processes may attempt the same final publication. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-13-01 | Tampering | `atomic_write/2` | high | mitigate | Synced temp plus exclusive no-replace link; competing-writer test proves the loser cannot replace target bytes. |
| T-131-13-02 | Spoofing | VERIFIED record | high | mitigate | Existing full fact validation and bounded projection remain prerequisites to the exclusive commit point. |
| T-131-13-03 | Denial of Service | temporary outputs | medium | mitigate | Unique exclusive temps and unconditional cleanup on every result. |
| T-131-13-04 | Repudiation | `--check-existing` | medium | mitigate | Fresh facts must reproduce byte-identical bounded output; test asserts no rewrite and stable mtime. |
</threat_model>

## Source Coverage Audit

| Source | ID | Plans | Status | Coverage |
|---|---|---|---|---|
| GOAL | Phase 131 | 11-13 | COVERED | The dated decision and clean-room prerequisite regain authoritative publication guarantees. |
| REQ | JOURNEY-04 | 13 | COVERED | Exact public facts remain bounded and reproducible while their VERIFIED record becomes race-safe. |
| RESEARCH | Atomic public verifier | 13 | COVERED | Preserves named read-only verifier and exclusive publication boundary. |
| CONTEXT | D-04/D-28/D-30/D-35 | 13 | COVERED | Exact verified prerequisite and truthful deterministic/advisory evidence separation remain intact. |
| CONTEXT | D-01..D-35 | 01-13 | COVERED | Plans 01-10 retain all locked phase decisions; Plans 11-13 close only verifier gaps and do not implement deferred or new capability scope. |

<verification>
Run the focused verifier suite, repeat the competing-writer test, confirm the committed prerequisite is untouched, and then run the combined gap-focused contracts from all three plans.
</verification>

<success_criteria>
Competing valid verifier processes produce one immutable complete VERIFIED prerequisite, every loser fails without overwrite, all temporary files are cleaned, and check-existing remains read-only.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-13-SUMMARY.md` when done.
</output>
