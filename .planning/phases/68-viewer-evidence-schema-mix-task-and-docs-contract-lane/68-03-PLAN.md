---
phase: 68
plan: 03
type: execute
wave: 3
depends_on:
  - 68-01
  - 68-02
files_modified:
  - test/docs_contract/viewer_evidence_claims_test.exs
  - scripts/verify_docs.exs
autonomous: true
requirements:
  - RECIPE-04
  - GUARDRAIL-01
  - GUARDRAIL-03
  - GUARDRAIL-04
must_haves:
  truths:
    - Eighth docs-contract lane registered in `scripts/verify_docs.exs`; `mix docs.contract` runs 8/8 lanes.
    - Production matrix and evidence tree pass tier-A lane tests unchanged.
    - Tier-B violation fixtures fail docs-contract tests (supported without evidence, bad deferral vocabulary, secret/path/image in evidence, orphan files, forbidden matrix keys).
    - Existing seven docs-contract lanes still pass.
  artifacts:
    - path: test/docs_contract/viewer_evidence_claims_test.exs
      provides: "Cross-family viewer evidence semantic-claims lane"
    - path: scripts/verify_docs.exs
      provides: "Eighth lane tuple wired into docs contract aggregator"
  key_links:
    - from: test/docs_contract/viewer_evidence_claims_test.exs
      to: lib/rendro/viewer_evidence/validator.ex
      via: "calls Validator — no duplicated lint/schema logic"
    - from: scripts/verify_docs.exs
      to: test/docs_contract/viewer_evidence_claims_test.exs
      via: "Viewer evidence semantic-claims lane tuple"
---

<objective>
Add the eighth docs-contract lane (`viewer_evidence_claims_test.exs`) and wire it into `scripts/verify_docs.exs`, proving RECIPE-04 and GUARDRAIL-01/03/04 enforcement via shared validator + tier-B fixtures.

Purpose: Merge-blocking CI gate for viewer-evidence recording discipline without a new GitHub required check (D-18).
Output: `mix docs.contract` green with 8 lanes; negative fixture subtests prove violation detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-CONTEXT.md
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-RESEARCH.md §8
@scripts/verify_docs.exs
@test/docs_contract/protection_claims_test.exs
@test/docs_contract/embedded_artifact_claims_test.exs
@lib/rendro/viewer_evidence/validator.ex
</context>

<interfaces>
Lane tuple to add (68-PATTERNS.md §2.1):
```elixir
{"Viewer evidence semantic-claims lane", ["test", "test/docs_contract/viewer_evidence_claims_test.exs"]}
```

Lane registration test pattern (protection_claims_test.exs):
```elixir
assert script =~ ~s|{"Protection semantic-claims lane", ["test", "test/docs_contract/protection_claims_test.exs"]}|
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-family docs-contract test module</name>
  <files>test/docs_contract/viewer_evidence_claims_test.exs</files>
  <read_first>
    - test/docs_contract/protection_claims_test.exs
    - test/docs_contract/embedded_artifact_claims_test.exs
    - lib/rendro/viewer_evidence/validator.ex
    - test/support/viewer_evidence/fixtures/
    - 68-RESEARCH.md §8 test categories
  </read_first>
  <acceptance_criteria>
    - Module `Rendro.DocsContract.ViewerEvidenceClaimsTest` with `async: true` where safe.
    - **Lane registration:** asserts eighth tuple present in `scripts/verify_docs.exs`; asserts prior seven lanes still present (embedded-artifact pattern).
    - **Production tier A:** `Validator.validate_matrix_structure!/1` succeeds on `priv/support_matrix.json`; orphan scan on production tree passes (only `_template.md` + `.gitkeep`); `_template.md` validates.
    - **Tier-B violations via fixtures** (call shared Validator, not inline regex):
      - `supported` without `evidence` → error
      - `explicit_deferral` without `evidence_deferred` → error
      - deferral reason `TBD`, `not yet`, `deferred for later`, length &lt; 40 → error (GUARDRAIL-01)
      - evidence body with `![`, `-----BEGIN`, `/Users/jon/`, `passphrase: secret`, `private_key: abc123` → error (GUARDRAIL-04)
      - file byte_size &gt; 65536 → error
      - orphan `priv/viewer_evidence/forms/orphan_test.md` unreferenced → error
      - viewer row key `compliance_tier` → JSV error (GUARDRAIL-03)
    - **Cross-family isolation:** test file does NOT assert family-specific promotion regexes (forms/protection/signing stay in their own tests).
    - **Staleness:** docs-contract does NOT fail on stale `recorded_at` (D-17).
  </acceptance_criteria>
  <action>
    Create `test/docs_contract/viewer_evidence_claims_test.exs`.
    Use `test/support/viewer_evidence/fixtures/` snippets loaded into temp dirs via `Briefly` or `System.tmp_dir!` for orphan and evidence violation cases.
    Import `Rendro.ViewerEvidence.Validator`, `Rendro.ViewerEvidence.Lint`, `Rendro.ViewerEvidence.Matrix` — zero duplicated validation logic.
  </action>
  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs</automated>
  </verify>
  <done>Docs-contract test module passes; negative cases fail as expected.</done>
</task>

<task type="auto">
  <name>Task 2: Register eighth lane in verify_docs.exs</name>
  <files>scripts/verify_docs.exs</files>
  <read_first>
    - scripts/verify_docs.exs
    - test/docs_contract/viewer_evidence_claims_test.exs
  </read_first>
  <acceptance_criteria>
    - `lanes` list in `scripts/verify_docs.exs` includes exactly one new tuple after Protection lane:
      `{"Viewer evidence semantic-claims lane", ["test", "test/docs_contract/viewer_evidence_claims_test.exs"]}`.
    - `mix run scripts/verify_docs.exs` prints 8 lanes and ends with `Docs contract VERIFIED!`.
    - `.github/workflows/ci.yml` unchanged (no new required job).
  </acceptance_criteria>
  <action>
    Append eighth lane tuple to `lanes` in `scripts/verify_docs.exs` using same shape as existing seven entries.
    Do not modify family `*_claims_test.exs` files.
  </action>
  <verify>
    <automated>mix run scripts/verify_docs.exs</automated>
  </verify>
  <done>Eighth lane registered; full docs contract passes.</done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — prior lanes and phase boundary</name>
  <files></files>
  <read_first>
    - mix.exs (:ci alias)
    - priv/support_matrix.json
    - .planning/ROADMAP.md Phase 68 success criteria
  </read_first>
  <acceptance_criteria>
    - `mix docs.contract` exits 0; stdout contains `Docs contract VERIFIED!`.
    - `scripts/verify_docs.exs` contains exactly 8 lane tuples (grep `^\s+\{` count == 8) and exactly 6 `semantic-claims lane` entries (README + Integration contract lanes excluded).
    - `mix test test/docs_contract/forms_claims_test.exs test/docs_contract/protection_claims_test.exs test/docs_contract/signing_claims_test.exs test/docs_contract/embedded_artifact_claims_test.exs` passes (no regression).
    - `git diff priv/support_matrix.json` empty.
    - `.github/workflows/ci.yml` does NOT contain substring `viewer-evidence`.
    - `mix rendro.viewer_evidence missing` exits 1 (21 cells); `mix rendro.viewer_evidence list` stdout contains `"total": 26` when run with `--json`.
  </acceptance_criteria>
  <action>
    Run full docs contract and targeted prior-lane tests.
    Confirm matrix file untouched and CI workflow unchanged.
    Document expected operator smoke in plan summary (list 26 rows, missing exit 1, validate exit 0).
  </action>
  <verify>
    <automated>mix docs.contract && mix test test/docs_contract/forms_claims_test.exs test/docs_contract/protection_claims_test.exs test/docs_contract/signing_claims_test.exs test/docs_contract/embedded_artifact_claims_test.exs</automated>
  </verify>
  <done>Phase 68 docs-contract lane complete; no regressions.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Docs-Contract Enforcement Lane

| Boundary | Description |
|----------|-------------|
| CI test runner → Validator | Reads fixtures and production `priv/` artifacts only. |
| Violation reports → CI logs | Error messages cite rule id + path; must not leak secret fixture content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03-01 | Tampering | support_matrix.json | mitigate | GUARDRAIL-03: JSV rejects non-additive viewer-row keys in tier-B fixture tests; production matrix unchanged in Phase 68. |
| T-68-03-02 | Tampering | Evidence files | mitigate | GUARDRAIL-04: body lint blocks PEM, images, home paths, operational secrets before merge. |
| T-68-03-03 | Repudiation | Deferral reasons | mitigate | GUARDRAIL-01: forbidden vocabulary (`TBD`, `not yet`, `deferred for later`, empty, &lt;40 chars) fails docs-contract. |
| T-68-03-04 | Information Disclosure | Fixture tests | mitigate | Use synthetic violation strings in `test/support/viewer_evidence/fixtures/`; never commit real credentials. |
| T-68-03-05 | Elevation of Privilege | CI surface | mitigate | Lane folds into existing `test` job via `verify_docs.exs`; no new GitHub required check (D-18). |
</threat_model>

<success_criteria>
- `mix test test/docs_contract/viewer_evidence_claims_test.exs` passes.
- `mix docs.contract` → `Docs contract VERIFIED!` with 8 lanes.
- Tier-B negative fixtures fail when validated.
- `priv/support_matrix.json` unchanged.
- All requirement IDs covered: RECIPE-04, GUARDRAIL-01, GUARDRAIL-03, GUARDRAIL-04.
</success_criteria>

<output>
After completion, create `.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-03-SUMMARY.md` via orchestration.
Update ROADMAP.md Phase 68 Plans to `3/3` when all summaries land.
</output>
