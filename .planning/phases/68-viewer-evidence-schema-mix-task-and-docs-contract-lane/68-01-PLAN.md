---
phase: 68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - priv/schemas/support_matrix.schema.json
  - priv/schemas/viewer_evidence.schema.json
  - priv/viewer_evidence/.gitkeep
  - priv/viewer_evidence/_template.md
  - lib/rendro/viewer_evidence/matrix.ex
  - lib/rendro/viewer_evidence/frontmatter.ex
  - lib/rendro/viewer_evidence/lint.ex
  - lib/rendro/viewer_evidence/validator.ex
  - test/rendro/viewer_evidence/validator_test.exs
  - test/support/viewer_evidence/fixtures/
autonomous: true
requirements:
  - MATRIX-01
  - MATRIX-02
  - MATRIX-03
  - GUARDRAIL-03
must_haves:
  truths:
    - Production `priv/support_matrix.json` passes Tier-A JSV validation unchanged (5 legacy supported, 21 unverified, 0 explicit_deferral, 26 cells).
    - JSON Schema documents `explicit_deferral` as a third status with required `evidence_deferred` and forbidden promotion keys.
    - Shared validator module enforces Tier-B promotion-complete rules via fixture subtests without breaking production matrix.
  artifacts:
    - path: priv/schemas/support_matrix.schema.json
      provides: "Draft 2020-12 matrix contract with $defs/viewer_row"
    - path: priv/schemas/viewer_evidence.schema.json
      provides: "Frontmatter schema_version 1 contract"
    - path: lib/rendro/viewer_evidence/validator.ex
      provides: "JSV tier-A + Elixir tier-B orchestration shared by Mix task and docs-contract"
  key_links:
    - from: lib/rendro/viewer_evidence/validator.ex
      to: priv/schemas/support_matrix.schema.json
      via: "JSV.build!/2 + validate/2 on decoded matrix"
    - from: lib/rendro/viewer_evidence/matrix.ex
      to: priv/support_matrix.json
      via: "enumerate_viewer_cells/1 walks all 8 viewer maps"
---

<objective>
Land JSON Schema contracts, dev/test-only validation dependencies, shared `Rendro.ViewerEvidence.*` modules, and `priv/viewer_evidence/` scaffolding.

Purpose: Phase 68 infrastructure foundation — additive matrix vocabulary (`explicit_deferral`), two-tier validation (Tier A structural on production matrix; Tier B promotion-complete in Elixir + fixtures), and a single validator module that downstream Mix task and docs-contract lane share.
Output: `mix test test/rendro/viewer_evidence/` green; production matrix unchanged and Tier-A valid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-CONTEXT.md
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-RESEARCH.md
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-PATTERNS.md
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-VALIDATION.md
@priv/support_matrix.json
</context>

<interfaces>
From mix.exs (dev/test dep pattern):
```elixir
{:req, "~> 0.5", only: [:dev, :test]}
```

From lib/rendro/pipeline/validate.ex (orchestration pattern):
```elixir
case walk(doc, doc, rules) do
  [] -> {:ok, doc}
  errors -> {:error, Rendro.Error.from_stage(:validate, :structural_corruption, %{details: %{errors: errors}})}
end
```

JSV usage (68-RESEARCH.md §5):
```elixir
schema = "priv/schemas/support_matrix.schema.json" |> File.read!() |> JSON.decode!()
root = JSV.build!(schema, base_uri: "file://" <> Path.absname("priv/schemas/"))
JSV.validate(matrix_map, root)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dev/test deps and JSON Schema contracts</name>
  <files>mix.exs, priv/schemas/support_matrix.schema.json, priv/schemas/viewer_evidence.schema.json</files>
  <read_first>
    - mix.exs (defp deps/0)
    - .planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-RESEARCH.md §5–§6
    - priv/support_matrix.json (viewer row shapes)
  </read_first>
  <acceptance_criteria>
    - `mix.exs` adds `{:jsv, "~> 0.18", only: [:dev, :test], runtime: false}` and `{:yaml_elixir, "~> 2.12", only: [:dev, :test], runtime: false}` (NOT ymlr for decode).
    - `support_matrix.schema.json` uses Draft 2020-12, `$defs/viewer_row` with `status` enum `["supported", "unverified", "explicit_deferral"]`, `additionalProperties: false` on viewer rows.
    - `explicit_deferral` branch requires `evidence_deferred` (minLength 40) and forbids `evidence`, `recorded_at`, `viewer_kind`.
    - `unverified` branch forbids `evidence`, `recorded_at`, `viewer_kind`, `evidence_deferred`.
    - Tier-A carve-out: do NOT require `evidence`/`recorded_at`/`viewer_kind` on `supported` in JSON Schema (Tier B in Elixir until Phase 70).
    - `viewer_evidence.schema.json` requires `schema_version: 1`, `surface`, `viewer`, `viewer_version`, `platform`, `recorded_at`, `behaviors[]`; fixture via exactly one of `fixture` or `fixture_sha256` (`^sha256:[a-f0-9]{64}$`); forbids `status`, `viewer_kind`, `checks`, `date_checked`.
    - Schema descriptions document byte budget **65536**.
  </acceptance_criteria>
  <action>
    Add jsv and yaml_elixir deps to mix.exs.
    Create `priv/schemas/support_matrix.schema.json` referencing `$defs/viewer_row` on all 8 viewer maps (`forms.viewers`, `forms.signature_widget_viewers`, `signing_preparation.viewers`, `signing.viewers`, `signing.long_lived.viewers`, `embedded_files.viewers`, `links.viewers`, `protection.viewers`).
    Create `priv/schemas/viewer_evidence.schema.json` per D-07–D-09 with `behaviors[].result` enum `pass|fail|skip|na` and `behaviors[].note` minLength 1.
  </action>
  <verify>
    <automated>mix deps.get && mix test test/rendro/viewer_evidence/validator_test.exs --only schema_contract</automated>
  </verify>
  <done>Schemas exist; deps compile; schema contract tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Matrix walker and surface mapping</name>
  <files>lib/rendro/viewer_evidence/matrix.ex, test/rendro/viewer_evidence/validator_test.exs</files>
  <read_first>
    - priv/support_matrix.json (nested viewer maps)
    - lib/rendro/viewer_evidence/matrix.ex (create)
    - 68-RESEARCH.md §6 surface mapping table
  </read_first>
  <acceptance_criteria>
    - `Matrix.load!/0` reads and JSON-decodes `priv/support_matrix.json`.
    - `Matrix.enumerate_viewer_cells/1` returns exactly **26** cells sorted by `surface` then `viewer`.
    - Each cell includes `{surface, viewer, status, matrix_path, proof}` where `surface` uses evidence-path segments (`signed_artifact` not `signing`, `long_lived_signed_artifact` not `long_lived`, `signature_widget` for `forms.signature_widget_viewers.*`).
    - Walker includes all 8 viewer maps per D-23.
  </acceptance_criteria>
  <action>
    Implement `Rendro.ViewerEvidence.Matrix` with `@moduledoc false`.
    Add `surface/2` mapping table: `forms.viewers` → `forms`, `forms.signature_widget_viewers` → `signature_widget`, `signing_preparation.viewers` → `signing_preparation`, `signing.viewers` → `signed_artifact`, `signing.long_lived.viewers` → `long_lived_signed_artifact`, `embedded_files.viewers` → `embedded_files`, `links.viewers` → `links`, `protection.viewers` → `protection`.
    Add unit test asserting cell count 26 and expected status split (5 supported, 21 unverified, 0 explicit_deferral) against production matrix.
  </action>
  <verify>
    <automated>mix test test/rendro/viewer_evidence/validator_test.exs --only matrix_walker</automated>
  </verify>
  <done>26 cells enumerated with correct surface segments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontmatter parser and lint module</name>
  <files>lib/rendro/viewer_evidence/frontmatter.ex, lib/rendro/viewer_evidence/lint.ex, test/rendro/viewer_evidence/validator_test.exs, test/support/viewer_evidence/fixtures/</files>
  <read_first>
    - 68-CONTEXT.md D-06–D-16
    - lib/rendro/viewer_evidence/lint.ex (create)
    - test/docs_contract/protection_claims_test.exs (lint style reference only)
  </read_first>
  <acceptance_criteria>
    - `Frontmatter.parse/1` splits `---` fences, uses `YamlElixir.read_from_string/1`, returns `{frontmatter_map, body}`.
    - `Frontmatter.path_alignment/2` errors when frontmatter `surface`/`viewer` ≠ path segments under `priv/viewer_evidence/<surface>/<viewer>.md`.
    - `Lint.evidence_body/1` rejects body containing `-----BEGIN`, `![`, `<img`, `data:image/`, `/Users/`, `/home/`, `C:\Users\`, assignment `passphrase:`, assignment `private_key:` (allow negated prose).
    - `Lint.deferral_reason/1` rejects empty/whitespace, whole-reason `TBD`/`not yet`/`deferred for later` (case-insensitive), `\bTBD\b`, leading `deferred for later`, trimmed length &lt; 40; allows `does not yet implement`, `not yet implemented`, `not yet available in`.
    - `Lint.byte_budget/1` rejects files with `byte_size > 65536`.
    - Fixture snippets under `test/support/viewer_evidence/fixtures/` cover each rejection case.
  </acceptance_criteria>
  <action>
    Implement `Rendro.ViewerEvidence.Frontmatter` and `Rendro.ViewerEvidence.Lint` with `@moduledoc false`.
    Return `{:ok, :clean}` or `{:error, reason_string}` from lint functions.
    Create minimal fixture files: `invalid_deferral_tbd.json`, `invalid_evidence_image.md`, `invalid_evidence_secret.md`, `invalid_evidence_home_path.md`, `oversized_evidence.md` (stub content describing violation).
  </action>
  <verify>
    <automated>mix test test/rendro/viewer_evidence/validator_test.exs --only lint</automated>
  </verify>
  <done>Lint and frontmatter modules pass fixture-driven tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Validator core (Tier A + Tier B)</name>
  <files>lib/rendro/viewer_evidence/validator.ex, test/rendro/viewer_evidence/validator_test.exs, test/support/viewer_evidence/fixtures/</files>
  <read_first>
    - lib/rendro/viewer_evidence/validator.ex (create)
    - 68-RESEARCH.md §1 two-tier table
    - priv/support_matrix.json
  </read_first>
  <acceptance_criteria>
    - `Validator.validate_matrix_structure/1` (Tier A): JSV pass on production matrix; rejects viewer row with forbidden key e.g. `compliance_tier`.
    - `Validator.validate_promotion_complete/2` (Tier B): `supported` requires resolvable `evidence` matching `^priv/viewer_evidence/[a-z0-9_]+/[a-z0-9_]+\.md$`, plus `recorded_at` and `viewer_kind` in `manual|pdfium-cli|pdfjs-dist`; `explicit_deferral` requires lint-clean `evidence_deferred`; rejects conflicting keys.
    - `Validator.validate_evidence_file/3` runs JSV on frontmatter, path alignment, behavior IDs ⊆ matrix `proof[]`, body lint, byte budget.
    - `Validator.list_orphan_evidence/1` flags `.md` files under `priv/viewer_evidence/` except `_template.md` and `.gitkeep` without matrix reference.
    - `Validator.run_full/3` orchestrates Tier A + Tier B + evidence files + orphan scan + staleness warnings; returns `{:ok, warnings}` on production matrix (5 legacy-supported warnings, exit 0 semantics) or `{:error, violations}` on structural failure.
    - Production matrix call via `run_full/3` returns `{:ok, warnings}` with 5 legacy-supported warnings (missing evidence), not `{:error, _}`.
    - Tier-B negative fixtures fail in isolated tests (supported without evidence, deferral without reason, forbidden matrix key).
  </acceptance_criteria>
  <action>
    Implement `Rendro.ViewerEvidence.Validator` orchestrating JSV + Matrix + Frontmatter + Lint.
    Expose `validate_matrix_structure!/1`, `validate_promotion_complete/2`, `validate_evidence_file/3`, `list_orphan_evidence/1`, `staleness_warnings/1` (180-day advisory from matrix `recorded_at`, warnings only), and `run_full/3` as the single Mix-task entry point.
    `run_full/3` signature: `run_full(matrix_path \\ "priv/support_matrix.json", evidence_root \\ "priv/viewer_evidence", opts \\ [])` — runs (1) Tier-A JSV, (2) Tier-B promotion scan (warnings only on legacy supported), (3) validate every referenced evidence file + `_template.md`, (4) orphan scan, (5) staleness warnings.
    Add `@moduledoc false`; no public runtime API (D-25).
  </action>
  <verify>
    <automated>mix test test/rendro/viewer_evidence/</automated>
  </verify>
  <done>Full validator test suite green; production matrix Tier-A passes.</done>
</task>

<task type="auto">
  <name>Task 5: Evidence directory scaffolding</name>
  <files>priv/viewer_evidence/.gitkeep, priv/viewer_evidence/_template.md</files>
  <read_first>
    - priv/schemas/viewer_evidence.schema.json
    - 68-CONTEXT.md D-13
  </read_first>
  <acceptance_criteria>
    - `priv/viewer_evidence/.gitkeep` exists.
    - `_template.md` has valid YAML frontmatter (`schema_version: 1`) with example `behaviors[]` rows (`behavior`, `result`, `note` all populated).
    - `_template.md` validates via `Validator.validate_evidence_file/3` when paired with a representative matrix cell proof list.
    - `_template.md` excluded from orphan scan.
  </acceptance_criteria>
  <action>
    Create `.gitkeep` and `_template.md` with placeholder values: `surface: forms`, `viewer: example_viewer`, `viewer_version: "0.0.0"`, `platform: "macOS 15 (example)"`, `recorded_at: "2026-01-01"`, `fixture: "test/fixtures/example.pdf"`, and sample behaviors matching forms proof IDs.
    Body: short prose explaining template purpose (no secrets, no images).
  </action>
  <verify>
    <automated>mix test test/rendro/viewer_evidence/validator_test.exs --only template</automated>
  </verify>
  <done>Scaffolding shipped; template validates.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Validation Infrastructure

| Boundary | Description |
|----------|-------------|
| Repo artifacts → Validator | Reads `priv/support_matrix.json`, `priv/viewer_evidence/**/*.md`, JSON schemas only. |
| Validator → Operator output | Error strings must not echo file contents beyond path + rule id. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | Tampering | JSON Schema | mitigate | JSV validates matrix structure; `additionalProperties: false` on viewer rows blocks smuggled compliance/trust keys (GUARDRAIL-03). |
| T-68-01-02 | Information Disclosure | Evidence lint | mitigate | `Lint.evidence_body/1` rejects PEM blocks, home paths, operational `passphrase:` assignments before merge (GUARDRAIL-04 prep). |
| T-68-01-03 | Denial of Service | Validator | accept | Offline file reads only; byte budget 65536 enforced per evidence file. |
| T-68-01-04 | Elevation of Privilege | Runtime API | mitigate | `@moduledoc false` modules; no `Rendro.Support.ViewerEvidence`; dev/test deps only (D-25). |
</threat_model>

<success_criteria>
- `mix test test/rendro/viewer_evidence/` passes.
- `git diff priv/support_matrix.json` is empty.
- JSV validates unchanged production matrix (Tier A).
- Tier-B violation fixtures fail in isolated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-01-SUMMARY.md` via orchestration.
</output>
