---
phase: 68
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - lib/mix/tasks/rendro/viewer_evidence.ex
  - test/mix/tasks/viewer_evidence_task_test.exs
autonomous: true
requirements:
  - RECIPE-02
must_haves:
  truths:
    - Operator runs `mix rendro.viewer_evidence list|validate|missing [--json]` against unchanged matrix.
    - `missing` exits 1 with 21 unverified cells; `list` exits 0 with 26-row table; `validate` exits 0 with legacy-supported warnings only.
    - Task is NOT added to `mix.exs` `:ci` alias.
  artifacts:
    - path: lib/mix/tasks/rendro/viewer_evidence.ex
      provides: "Mix.Tasks.Rendro.ViewerEvidence with list/validate/missing"
    - path: test/mix/tasks/viewer_evidence_task_test.exs
      provides: "Exit code and --json contract tests"
  key_links:
    - from: lib/mix/tasks/rendro/viewer_evidence.ex
      to: lib/rendro/viewer_evidence/validator.ex
      via: "validate subcommand calls Validator.run_full/3"
    - from: lib/mix/tasks/rendro/viewer_evidence.ex
      to: lib/rendro/viewer_evidence/matrix.ex
      via: "list/missing enumerate cells via Matrix.enumerate_viewer_cells/1"
---

<objective>
Ship `Mix.Tasks.Rendro.ViewerEvidence` operator tooling with `list`, `validate`, and `missing` subcommands, human-readable tables, optional `--json`, and D-22 exit codes.

Purpose: Operators audit viewer-evidence coverage gaps before recording promotions (Phase 69–71).
Output: `mix test test/mix/tasks/viewer_evidence_task_test.exs` green; manual smoke matches 26/5/21/0 split on production matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-CONTEXT.md
@.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-RESEARCH.md §7
@lib/mix/tasks/rendro.visual_uat.ex
@lib/rendro/viewer_evidence/validator.ex
</context>

<interfaces>
From lib/mix/tasks/rendro.visual_uat.ex (exit pattern):
```elixir
if verdict.overall_pass do
  :ok
else
  Mix.shell().error("Visual UAT failed (overall_pass: false). See notes above.")
  exit({:shutdown, 1})
end
```

Expected production matrix metrics (68-RESEARCH.md §7):
| Metric | Value |
|--------|-------|
| Total cells | 26 |
| supported (legacy, no evidence) | 5 |
| unverified | 21 |
| explicit_deferral | 0 |
| missing exit | 1 |
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mix task subcommands and output formatting</name>
  <files>lib/mix/tasks/rendro/viewer_evidence.ex</files>
  <read_first>
    - lib/mix/tasks/rendro.visual_uat.ex (@moduledoc, Mix.shell, exit codes)
    - lib/rendro/viewer_evidence/matrix.ex
    - lib/rendro/viewer_evidence/validator.ex
    - 68-CONTEXT.md D-19–D-24
  </read_first>
  <acceptance_criteria>
    - Module at `lib/mix/tasks/rendro/viewer_evidence.ex` compiles as `Mix.Tasks.Rendro.ViewerEvidence`.
    - `@shortdoc` present; `@moduledoc` documents three states (`supported`, `unverified`, `explicit_deferral`), subcommands, exit codes, 65536-byte budget, CI truth (`mix docs.contract` + lane path), forward link to future `guides/viewer_evidence.md`.
    - `run/1` parses argv: `list`, `validate`, `missing`, optional `--json`; calls `Mix.Task.run("app.start")`.
    - `list`: prints summary counts + fixed-width table columns `surface`, `viewer`, `status`, `notes` sorted surface→viewer; legacy supported without `evidence:` shows note e.g. `legacy: missing evidence pointer`; exit 0 on parse success.
    - `list --json`: stdout ONLY `{"summary": {"total": 26, "supported": 5, "unverified": 21, "explicit_deferral": 0, ...}, "cells": [...]}`; errors on stderr.
    - `missing`: filters `status == "unverified"` only (21 rows today); exit 1 iff any match; `--json` same shape filtered.
    - `validate`: calls `Validator.run_full/3` only (no ad-hoc orchestration); Tier-A errors → exit 1; Tier-B legacy warnings printed to stderr, exit 0; evidence-file + orphan scan failures → exit 1; staleness warnings (180 days) to stderr, exit 0 (D-17).
    - Task NOT registered in `mix.exs` `:ci` alias.
  </acceptance_criteria>
  <action>
    Implement `Mix.Tasks.Rendro.ViewerEvidence` using `Rendro.ViewerEvidence.Matrix` and `Rendro.ViewerEvidence.Validator`.
    Parse argv with strict unknown-flag rejection.
    For `validate`, call `Validator.run_full/3("priv/support_matrix.json", "priv/viewer_evidence", [])` — single entry point covering Tier-A JSV, Tier-B promotion scan (warnings for 5 legacy supported rows), `_template.md` + referenced evidence validation, orphan scan, and staleness warnings.
    Implement table printer without TTY detection (D-20).
  </action>
  <verify>
    <automated>mix test test/mix/tasks/viewer_evidence_task_test.exs --only subcommands</automated>
  </verify>
  <done>Task runs all three subcommands with documented exit semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mix task integration tests</name>
  <files>test/mix/tasks/viewer_evidence_task_test.exs</files>
  <read_first>
    - test/mix/tasks/docs_contract_task_test.exs (capture_shell_messages, catch_exit)
    - lib/mix/tasks/rendro/viewer_evidence.ex
    - priv/support_matrix.json
  </read_first>
  <acceptance_criteria>
    - Test invokes task against production matrix (no temp matrix mutation).
    - `list` returns `:ok` (exit 0).
    - `missing` triggers `exit({:shutdown, 1})` via `catch_exit`.
    - `validate` returns `:ok` on unchanged matrix (warnings allowed).
    - `--json` output parses as JSON with stable keys `summary`, `cells`; each cell has `surface`, `viewer`, `status`.
    - Assert `Mix.Tasks.Rendro.ViewerEvidence` module doc mentions `mix docs.contract`.
    - Assert `mix.exs` `:ci` alias does NOT contain `rendro.viewer_evidence`.
  </acceptance_criteria>
  <action>
    Create `test/mix/tasks/viewer_evidence_task_test.exs` using `ExUnit.CaptureIO` / `Mix.Tasks.Rendro.ViewerEvidence.run/1` with production artifacts.
    Add tests for human and JSON modes; read mix.exs source for negative assertion on `:ci` alias.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/viewer_evidence_task_test.exs</automated>
  </verify>
  <done>Integration tests green; exit codes match D-22.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 — Operator Mix Task

| Boundary | Description |
|----------|-------------|
| Operator shell → Mix task | Reads repo files only; no network; no env secrets required. |
| Mix task → stdout/stderr | Human and JSON output must not print evidence file bodies or matrix secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-01 | Information Disclosure | JSON output | mitigate | Emit only cell metadata (surface, viewer, status, notes); never dump full evidence file contents. |
| T-68-02-02 | Tampering | argv parsing | accept | Read-only audit tool; no writes to matrix or evidence files in Phase 68. |
| T-68-02-03 | Denial of Service | Table rendering | accept | Fixed 26-row output; no unbounded loops. |
| T-68-02-04 | Repudiation | Exit codes | mitigate | Documented D-22 contract tested in ExUnit; `missing` exit 1 auditable in CI scripts. |
</threat_model>

<success_criteria>
- `mix rendro.viewer_evidence list` shows 26 rows (5 supported, 21 unverified, 0 explicit_deferral).
- `mix rendro.viewer_evidence missing; echo $?` → 1.
- `mix rendro.viewer_evidence validate; echo $?` → 0.
- `mix test test/mix/tasks/viewer_evidence_task_test.exs` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/68-viewer-evidence-schema-mix-task-and-docs-contract-lane/68-02-SUMMARY.md` via orchestration.
</output>
