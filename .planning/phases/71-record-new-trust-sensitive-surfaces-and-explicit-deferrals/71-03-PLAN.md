---
phase: 71
plan: 03
type: execute
wave: 3
depends_on:
  - 71-02
files_modified:
  - priv/support_matrix.json
  - guides/api_stability.md
  - guides/viewer_evidence.md
  - CHANGELOG.md
  - test/docs_contract/viewer_evidence_claims_test.exs
  - test/docs_contract/signing_claims_test.exs
autonomous: true
requirements:
  - VIEWER-02
  - VIEWER-03
  - VIEWER-04
  - VIEWER-05
  - VIEWER-06
  - VIEWER-07
must_haves:
  truths:
    - All 20 trust-sensitive cells terminal: `supported` with promotion keys OR `explicit_deferral` with `evidence_deferred` — zero bare `unverified`.
    - `mix rendro.viewer_evidence missing` output is empty.
    - Signing-prep equivalence section in api_stability documents Acrobat exception + non-Acrobat inheritance (D-15, D-17).
    - Inherited signing_prep rows point at signature_widget evidence (same status, recorded_at, viewer_kind).
    - CHANGELOG has one bullet per (surface × viewer) under `[0.3.0]` Viewer Evidence (v2.3) — Added vs Changed per D-03.
    - `mix docs.contract` passes 8/8 lanes.
    - Merge unit (D-01): executes in same PR as all 71-02 evidence files.
  artifacts:
    - path: priv/support_matrix.json
      provides: "20 terminal cell states"
    - path: guides/api_stability.md
      provides: "Equivalence note + STACK mirrors + deferral prose"
    - path: guides/viewer_evidence.md
      provides: "Appendix B deferral templates (UPSTREAM_ISSUE, NO_SIG_VALIDATION, NO_LTV_INDICATORS, SURFACE_EQUIVALENCE)"
  key_links:
    - from: priv/support_matrix.json
      to: priv/viewer_evidence/
      via: "evidence: pointers on supported rows"
    - from: guides/api_stability.md
      to: .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-DEFERRAL-DRAFTS.md
      via: "explicit_deferral mirror prose"
---

<objective>
Close VIEWER-02 through VIEWER-07 in one atomic public-contract wave — matrix terminal states, api_stability equivalence + mirrors, Appendix B deferral templates, CHANGELOG, docs-contract asserts, empty missing list.

Purpose: Deliver D-01 single-PR closure — ~20 cell states publish atomically like browserslist snapshot. Evidence from 71-02 becomes contractually binding only when matrix + prose + CHANGELOG land together.
Output: Zero silent unverified; docs-contract green; operator `mix rendro.viewer_evidence missing` empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-CONTEXT.md
@.planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-RESEARCH.md
@.planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-PATTERNS.md
@.planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VALIDATION.md
@.planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-DEFERRAL-DRAFTS.md
@guides/api_stability.md
@CHANGELOG.md
@priv/support_matrix.json
@test/docs_contract/viewer_evidence_claims_test.exs
@test/docs_contract/signing_claims_test.exs
@.planning/research/STACK.md
</context>

<interfaces>
Supported promotion shape (add proof[], evidence, recorded_at, viewer_kind):

```json
"adobe_acrobat_reader": {
  "status": "supported",
  "proof": ["open", "default_state_visible", "edit_or_toggle", "save"],
  "evidence": "priv/viewer_evidence/forms/adobe_acrobat_reader.md",
  "recorded_at": "YYYY-MM-DD",
  "viewer_kind": "manual"
}
```

Explicit deferral shape (no evidence file):

```json
"pdfjs": {
  "status": "explicit_deferral",
  "evidence_deferred": "{lint-valid reason from 71-DEFERRAL-DRAFTS.md}"
}
```

Signing-prep inheritance (non-Acrobat, D-15):

```json
"apple_preview": {
  "status": "supported",
  "proof": ["prepared_artifact_opens_cleanly", "widget_renders_as_unsigned_placeholder", "viewer_does_not_silently_re_sign_or_corrupt", "byte_range_layout_intact_after_save_as"],
  "evidence": "priv/viewer_evidence/signature_widget/apple_preview.md",
  "recorded_at": "{same as signature_widget apple_preview}",
  "viewer_kind": "manual"
}
```

STACK mirror template (`.planning/research/STACK.md` line 218):
```markdown
{Viewer} is `supported` for `{surface}` based on the recorded viewer checklist for version **{viewer_version}** on **{platform}** (`priv/viewer_evidence/{surface}/{viewer}.md`). That proof confirms `{proof_ids...}` for the representative {surface} fixture.
```

Equivalence note target (ROADMAP SC#4):
```markdown
For viewers other than Adobe Acrobat Reader, signing_preparation and signature_widget cells are behaviorally indistinguishable — record signature_widget evidence once and inherit the same status, recorded_at, viewer_kind, and evidence pointer for signing_preparation. Adobe Acrobat Reader requires independent signing_preparation evidence because byte-range layout is viewer-discriminable.
```

Twenty cells to close — see 71-RESEARCH.md §2.1 disposition table.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update priv/support_matrix.json — all 20 cells terminal</name>
  <files>priv/support_matrix.json</files>
  <read_first>
    - priv/support_matrix.json (current 20 unverified cells)
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-RESEARCH.md §2.1
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-DEFERRAL-DRAFTS.md
    - All priv/viewer_evidence/ files from 71-02
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-CONTEXT.md D-11, D-14, D-15, D-23
  </read_first>
  <acceptance_criteria>
    - `grep -c '"status": "unverified"' priv/support_matrix.json` returns 0.
    - Every promoted row has `status: supported`, `proof[]`, `evidence`, `recorded_at`, `viewer_kind`.
    - Every deferred row has `status: explicit_deferral`, `evidence_deferred` (no evidence key).
    - signing_preparation non-Acrobat rows inherit signature_widget evidence pointers (D-15).
    - signed_artifact × chrome_pdfium is `supported` not deferred (D-14).
    - `mix rendro.viewer_evidence validate` exits 0.
  </acceptance_criteria>
  <action>
    For each of 20 cells per 71-RESEARCH §2.1 disposition: promote with proof[] IDs from REQUIREMENTS.md or set explicit_deferral with prose from 71-DEFERRAL-DRAFTS.md.
    Add proof[] arrays to surfaces that lacked them (signature_widget, signing_preparation, signed_artifact, long_lived).
    Match recorded_at to evidence frontmatter dates.
    Set viewer_kind: manual for GUI rows, pdfium-cli for chrome_pdfium rows.
  </action>
  <verify>
    <automated>mix rendro.viewer_evidence validate && mix rendro.viewer_evidence missing | grep -q . && exit 1 || exit 0</automated>
  </verify>
  <done>Matrix shows zero bare unverified; missing list empty.</done>
</task>

<task type="auto">
  <name>Task 2: Add signing-prep equivalence section + STACK mirrors to api_stability.md</name>
  <files>guides/api_stability.md</files>
  <read_first>
    - guides/api_stability.md (Signing Preparation section insertion point)
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-CONTEXT.md D-17
    - .planning/research/STACK.md line 218
    - All new priv/viewer_evidence/ files (frontmatter viewer_version, platform, proof IDs)
  </read_first>
  <acceptance_criteria>
    - api_stability.md contains signing-preparation × signature-widget equivalence note verbatim intent (D-17).
    - Every newly supported row has STACK mirror sentence with evidence path.
    - Every explicit_deferral row has named-reason prose in api_stability (not silent unverified).
    - Forms Acrobat unverified sentence replaced with supported mirror.
    - Protection Acrobat unverified sentence replaced with supported mirror.
    - `mix test test/docs_contract/signing_claims_test.exs` passes.
  </acceptance_criteria>
  <action>
    Insert equivalence section in Signing Preparation area before per-viewer mirrors.
    Replace Acrobat forms/protection unverified placeholders with STACK mirrors sourcing evidence frontmatter.
    Add mirrors for signature_widget, signing_preparation (Acrobat separate + inherited note for others), signed_artifact, long_lived supported rows.
    Add deferral prose for pdfjs sig widget, Preview/pdfjs signed, three long_lived deferrals, conditional deferrals.
  </action>
  <verify>
    <automated>mix test test/docs_contract/signing_claims_test.exs test/docs_contract/viewer_evidence_claims_test.exs</automated>
  </verify>
  <done>api_stability mirrors every Phase 71 cell state.</done>
</task>

<task type="auto">
  <name>Task 3: Add Appendix B deferral templates to viewer_evidence.md</name>
  <files>guides/viewer_evidence.md</files>
  <read_first>
    - guides/viewer_evidence.md (existing structure)
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-CONTEXT.md D-22
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-DEFERRAL-DRAFTS.md (worked examples)
  </read_first>
  <acceptance_criteria>
    - guides/viewer_evidence.md contains `## Appendix B` with four skeleton templates: UPSTREAM_ISSUE, NO_SIG_VALIDATION, NO_LTV_INDICATORS, SURFACE_EQUIVALENCE.
    - Each template shows matrix JSON shape and example clause.
    - Lint rules referenced (≥40 chars, forbidden vocabulary list).
  </acceptance_criteria>
  <action>
    Append Appendix B after existing appendices with hybrid template skeletons from D-22.
    Include mozilla/pdf.js#4202 as UPSTREAM_ISSUE example.
    Include Preview append-save invalidation as NO_SIG_VALIDATION example.
  </action>
  <verify>
    <automated>grep -q "Appendix B" guides/viewer_evidence.md && grep -q "UPSTREAM_ISSUE" guides/viewer_evidence.md</automated>
  </verify>
  <done>Operator deferral template reference published.</done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG Viewer Evidence (v2.3) bullets</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md `[0.3.0] - Unreleased` section
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-CONTEXT.md D-03
    - priv/support_matrix.json (final cell states)
  </read_first>
  <acceptance_criteria>
    - CHANGELOG contains `#### Viewer Evidence (v2.3)` under `[0.3.0] - Unreleased`.
    - One bullet per (surface × viewer) row changed in Phase 71 (~20 bullets).
    - `Added` used for unverified → supported promotions.
    - `Changed` used for new explicit_deferral rows and equivalence note.
  </acceptance_criteria>
  <action>
    Add Viewer Evidence subsection with per-row bullets naming surface, viewer, and outcome (supported/deferred).
    Follow Phase 70 CHANGELOG discipline (one bullet per matrix row).
  </action>
  <verify>
    <automated>grep -c "Viewer Evidence" CHANGELOG.md | test "$(cat)" -ge 1</automated>
  </verify>
  <done>CHANGELOG documents atomic Phase 71 closure.</done>
</task>

<task type="auto">
  <name>Task 5: Extend docs-contract lane for signing surfaces and deferrals</name>
  <files>test/docs_contract/viewer_evidence_claims_test.exs, test/docs_contract/signing_claims_test.exs</files>
  <read_first>
    - test/docs_contract/viewer_evidence_claims_test.exs (existing lane 8 patterns)
    - test/docs_contract/signing_claims_test.exs
    - priv/support_matrix.json (terminal states)
    - guides/api_stability.md (new mirrors)
  </read_first>
  <acceptance_criteria>
    - viewer_evidence_claims_test.exs asserts zero bare unverified in trust-sensitive surfaces.
    - Test asserts explicit_deferral rows pass forbidden-vocabulary lint.
    - Test asserts every supported row evidence path resolves to existing file.
    - Test asserts signing_preparation inherited rows point at valid evidence.
    - signing_claims_test.exs asserts equivalence note presence.
    - `mix docs.contract` passes 8/8 lanes.
  </acceptance_criteria>
  <action>
    Add test cases for: no unverified count, deferral lint on each explicit_deferral row, evidence path existence for new signing surface files, api_stability equivalence substring.
    Run full docs.contract and fix any mirror drift.
  </action>
  <verify>
    <automated>mix docs.contract</automated>
  </verify>
  <done>Merge-blocking docs-contract lane covers Phase 71 closure.</done>
</task>

<task type="auto">
  <name>Task 6: Final closure verification</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md Phase 71 success criteria (5 items)
    - .planning/phases/71-record-new-trust-sensitive-surfaces-and-explicit-deferrals/71-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `mix rendro.viewer_evidence list` shows Acrobat rows supported for forms, protection, signature_widget, signing_preparation, signed_artifact, long_lived.
    - `mix rendro.viewer_evidence missing` produces no output.
    - `grep -c '"status": "unverified"' priv/support_matrix.json` equals 0.
    - `mix docs.contract` exit 0.
    - `mix test test/docs_contract/viewer_evidence_claims_test.exs` exit 0.
  </acceptance_criteria>
  <action>
    Run closure command battery and capture output for SUMMARY.md / UAT.
  </action>
  <verify>
    <automated>mix rendro.viewer_evidence missing; test $? -eq 0; mix docs.contract; mix test test/docs_contract/viewer_evidence_claims_test.exs</automated>
  </verify>
  <done>Phase 71 public contract closed — ready for Phase 72 audit.</done>
</task>

</tasks>

<verification>
1. `mix rendro.viewer_evidence missing` — empty.
2. `mix docs.contract` — 8/8 green.
3. `grep '"status": "unverified"' priv/support_matrix.json` — no matches.
4. All VIEWER-02–07 success criteria from ROADMAP satisfied.
</verification>

<success_criteria>
- VIEWER-02 through VIEWER-07 complete.
- Twenty cells terminal; zero silent unverified.
- Single atomic PR discipline satisfied (71-02 + 71-03 together).
</success_criteria>

<threat_model>
ASVS L1 — public documentation contract.
- T-71-03-01: Deferral prose must not use forbidden vocabulary that bypasses honest-failure lint.
- T-71-03-02: Matrix must not claim supported without resolvable evidence file (orphan scan).
Block on: high — docs-contract is merge gate; failures block ship.
</threat_model>

## PLANNING COMPLETE
