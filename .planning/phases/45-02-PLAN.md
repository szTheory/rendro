---
phase: 45
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - lib/rendro/pdf/writer.ex
autonomous: true
requirements:
  - M4-FORMS
must_haves:
  truths:
    - Rendered PDF contains an AcroForm dictionary in its Catalog.
    - Pages with form fields contain an Annots array linking to the fields.
    - Form fields generate a normal Appearance Stream (/N) drawing the text value.
  artifacts:
    - path: lib/rendro/pdf/writer.ex
      provides: "AcroForm and Widget annotation serialization"
  key_links:
    - from: lib/rendro/pdf/writer.ex
      to: "PDF Catalog"
      via: "Injection of /AcroForm dictionary"
    - from: lib/rendro/pdf/writer.ex
      to: "PDF Page"
      via: "Injection of /Annots array with Widget references"
---

<objective>
Implement PDF serialization for AcroForm text fields, translating the `Rendro.FormField` block into valid PDF Catalog, Page Annotations, and Appearance Streams.

Purpose: Bridges the layout-aware form fields into a functional interactive PDF by creating the necessary data structures defined by the PDF specification.
Output: A valid PDF with fillable, pre-populated text fields that are editable in standard viewers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-CONTEXT.md
@.planning/phases/45-PATTERNS.md
@.planning/phases/45-RESEARCH.md
</context>

<interfaces>
From lib/rendro/pdf/writer.ex (Analog):
```elixir
defp build_objects(doc, fonts, images, opts) do
  # ... existing collection and allocation pattern
  with {:ok, fonts} <- collect_fonts(doc),
       {:ok, images} <- collect_images(doc) do
    # ...
  end
end

defp build_page_objects(doc, page_obj_nums, pages_num, font_allocations, image_allocations, font_map, image_map, opts) do
  # ... page dict assembly
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Object Collection & AcroForm Catalog Dictionary</name>
  <files>lib/rendro/pdf/writer.ex</files>
  <behavior>
    - `build_objects/4` gathers form fields from the document tree.
    - `allocate_form_field_nums/2` assigns PDF object numbers to form fields.
    - `catalog_dict` includes an `/AcroForm` dictionary if fields are present.
    - `/AcroForm` contains `/Fields` (array of refs), `/DA` (default appearance string), and `/DR` (default resources with a base font like Helvetica).
  </behavior>
  <action>
    Implement `collect_form_fields/1` by traversing the document blocks.
    Implement `allocate_form_field_nums/2`.
    Update `build_objects/4` to use these and inject `AcroForm` conditionally into the `catalog_dict`.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>AcroForm dictionary is correctly serialized in the Catalog object when fields exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widget Annotations & Appearance Streams</name>
  <files>lib/rendro/pdf/writer.ex</files>
  <behavior>
    - `build_page_objects` injects `/Annots` array into the `Page` dictionary for pages containing fields.
    - Each field generates a `Widget` annotation object containing `/Rect` (absolute coordinates), `/T` (name), `/V` (value).
    - Each field generates an Appearance Stream (`/AP` dict with `/N` stream) rendering the text using the base font.
  </behavior>
  <action>
    Update `build_page_objects/8` to calculate absolute layout bounding boxes for each form field.
    Generate the `Widget` annotation dictionary for each field. Ensure you use `escape_pdf_string/1` for `/V` and `/T` values to prevent injection.
    Generate the corresponding `XObject Form` Appearance Stream using basic PDF drawing operators (e.g. `BT`, `Tj`, `ET`).
    Add `/Annots` array to the `Page` dictionary referencing the field objects.
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>Form fields appear on the page with correct coordinates and visible pre-filled text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User API -> Core | Developer defines form field names and values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Injection | `Writer` | mitigate | Ensure form field values (`/V`) and names (`/T`) are passed through `escape_pdf_string/1` to prevent PDF syntax injection within the text stream or dictionary. |
</threat_model>

<success_criteria>
- End-to-end rendering produces a valid PDF file.
- Acrobat/Preview correctly interpret the AcroForm dictionary and display editable text fields.
</success_criteria>

<output>
After completion, create `.planning/phases/45-02-PLAN.md` summary context logic if needed, actually just proceed via orchestration.
</output>