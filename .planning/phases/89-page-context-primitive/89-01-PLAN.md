---
phase: 89
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/section.ex
  - lib/rendro/pipeline/compose.ex
  - lib/rendro/pipeline/paginate.ex
  - test/rendro_builders_test.exs
  - test/rendro/pipeline/compose_test.exs
  - test/rendro/pipeline/paginate_test.exs
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-03

must_haves:
  truths:
    - "Rendro.section(page_numbering: [restart: true]) builds a Section struct without using the catch-all options map"
    - "Compose preserves page_numbering on body section entries in layout.entries"
    - "A page_numbering restart body section starts on a new physical page unless the current page is already empty"
    - "{{section_page_number}} and {{section_total_pages}} are replaced in PAGE/running-region text"
    - "Section totals are computed from physical page ranges after pagination and are decimal strings"
    - "Existing {{page_number}}, {{total_pages}}, suppress_on, and RunningContent fn {page, total} behavior stays backward-compatible"
    - "No hidden fields are added to Rendro.Block"
  artifacts:
    - path: "lib/rendro/section.ex"
      provides: "Section page_numbering field and type"
      contains: "page_numbering"
    - path: "lib/rendro/pipeline/compose.ex"
      provides: "Normalized entries carrying page_numbering"
      contains: "page_numbering: section.page_numbering"
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Internal page context and section token substitution"
      contains: "section_page_number"
  key_links:
    - from: "lib/rendro/pipeline/compose.ex"
      to: "lib/rendro/pipeline/paginate.ex"
      via: "layout.entries page_numbering metadata"
      pattern: "entries"
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "test/rendro/pipeline/paginate_test.exs"
      via: "section-local token replacement"
      pattern: "section_total_pages"
---

<objective>
Implement Phase 89 Page Context Primitive: extend sections with `page_numbering: [restart: true]`, preserve that metadata through compose, compute internal page context during pagination, and add `{{section_page_number}}` / `{{section_total_pages}}` substitution while preserving all existing PAGE primitive behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/89-page-context-primitive/89-CONTEXT.md
</context>

<interfaces>
From `lib/rendro/section.ex`:
```elixir
defstruct name: nil,
          region: :body,
          content: [],
          suppress_on: nil,
          page_template: nil,
          options: %{}
```

From `lib/rendro/pipeline/compose.ex`:
```elixir
layout = %{
  region_blocks: region_blocks,
  region_suppress_on: region_suppress_on,
  entries: entries
}
```

From `lib/rendro/pipeline/paginate.ex`:
```elixir
replace_page_numbers(blocks, page_num, total)
evaluate_fn_blocks(blocks, page_num, total)
apply_page_template(page, idx, layout, total)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add section page_numbering API and compose metadata</name>
  <files>lib/rendro/section.ex, lib/rendro/pipeline/compose.ex, test/rendro_builders_test.exs, test/rendro/pipeline/compose_test.exs</files>
  <read_first>
    - lib/rendro/section.ex
    - lib/rendro/pipeline/compose.ex
    - test/rendro_builders_test.exs
    - test/rendro/pipeline/compose_test.exs
  </read_first>
  <action>
    Add `page_numbering: []` to `%Rendro.Section{}` and add a narrow type for `[] | [restart: true]`. Update the section type to include the field. Update `Compose.normalize_section/2` so normalized entries carry `page_numbering: section.page_numbering`. Keep flattened `region_blocks`, `content`, `header`, and `footer` behavior unchanged.

    Add builder and compose tests proving `Rendro.section(page_numbering: [restart: true])` builds a struct and `Compose.run/1` preserves page_numbering in `result.options.layout.entries`.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs test/rendro/pipeline/compose_test.exs</automated>
  </verify>
  <done>Section and compose metadata tests pass; existing unknown-key struct tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Compute internal page context and section token substitution</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/paginate.ex
    - test/rendro/pipeline/paginate_test.exs
  </read_first>
  <action>
    Change flow pagination to paginate body entries when `layout.entries` exists. For each body entry with `page_numbering: [restart: true]`, force a page boundary unless the current page is empty and record the physical start page for that section. After pagination, compute a page-context map for each physical page. If no restart applies, treat the whole document as one implicit section so section tokens fall back to physical page numbering.

    Update `apply_page_template/4` and `replace_page_numbers/3` to use page context. Preserve `evaluate_fn_blocks/3` callback arguments exactly as `{page_number, total_pages}`. Do not re-measure `MeasuredText` runs after substitution.

    Add pagination tests proving a restarting section starts on a new page, section tokens render as `1..N` per section, and existing page tokens and running-content callbacks still see physical page numbers and total pages.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Paginate tests cover CTX-01, CTX-02, and CTX-03 and pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused regression and write summary</name>
  <files>.planning/phases/89-page-context-primitive/89-01-SUMMARY.md</files>
  <read_first>
    - /Users/jon/.codex/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Run focused tests for builders, compose, paginate, and existing flow page-number behavior. If they pass, create `89-01-SUMMARY.md` with commits, files changed, verification evidence, deviations, and self-check.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs test/rendro/pipeline/compose_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>Summary exists with Self-Check: PASSED and focused regression command results.</done>
</task>

</tasks>

<verification>
1. `mix test test/rendro_builders_test.exs test/rendro/pipeline/compose_test.exs test/rendro/pipeline/paginate_test.exs`
2. `mix test test/rendro/flow_test.exs`
3. `mix format --check-formatted`
</verification>

<success_criteria>
- CTX-01: restarting body section starts on a new physical page.
- CTX-02: `{{section_page_number}}` and `{{section_total_pages}}` substitute deterministic decimal values.
- CTX-03: existing page tokens, `suppress_on`, and `RunningContent` callback behavior remain backward-compatible.
</success_criteria>
