---
phase: 30-visually-correct-png-image-rendering
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified: []
autonomous: false
requirements: ["ASSET-04"]
must_haves:
  truths:
    - "Logo correctly renders visibly on the PDF"
    - "Visual UAT passes according to Phase 30 criteria"
  artifacts: []
  key_links: []
---

<objective>
Execute the visual UAT to verify that the branded preview renders correctly with the logo, closing the visual regression gap detected in Phase 29.

Purpose: To prove that the fixes applied in previous plans actually resolve the issue observed in Phase 29 and that Rendro now correctly renders PNG assets.
Output: Human verification of the branded preview.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-visually-correct-png-image-rendering/30-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run Phase 29 Visual UAT</name>
  <files>N/A</files>
  <action>
    <what-built>Visual rendering fixes for PNG images (RGB, RGBA, Indexed).</what-built>
    <how-to-verify>
      1. Run the command: `mix rendro.visual_uat 29`.
      2. Open the generated `29-branded-preview.png` output.
      3. Verify that the `rendro-logo.png` image now paints correctly and visibly on the page.
      4. Verify that the layout remains deterministic.
    </how-to-verify>
    <resume-signal>Type "approved" or describe issues</resume-signal>
  </action>
  <verify>Human visual confirmation of the logo on the page.</verify>
  <done>Logo is visible in the branded preview PDF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A - purely visual verification.

## STRIDE Threat Register
N/A
</threat_model>
