---
phase: 30-visually-correct-png-image-rendering
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified: ["lib/rendro/pdf/writer.ex", "test/rendro/pdf/writer_test.exs", "test/rendro/pipeline/render_test.exs", ".github/workflows/ci.yml"]
autonomous: true
requirements: ["ASSET-04"]
must_haves:
  truths:
    - "PDF writer uses the refined PNG contract and correctly sets ColorSpace (per Gap 3)"
    - "CI environment has poppler-utils installed for pdftoppm support (per Gap 2)"
    - "Test suite validates actual rasterized rendering using pdftoppm"
  artifacts:
    - path: "lib/rendro/pdf/writer.ex"
      provides: "PDF Image XObject construction using refined contracts"
    - path: ".github/workflows/ci.yml"
      provides: "CI environment with poppler-utils"
    - path: "test/rendro/pipeline/render_test.exs"
      provides: "pdftoppm integration check"
  key_links:
    - from: "lib/rendro/pdf/writer.ex"
      to: "lib/rendro/pdf/png.ex"
      via: "image payload processing"
      pattern: "Rendro.PDF.PNG.process_for_pdf"
---

<objective>
Update the CI environment, integrate the refined PNG utility into the PDF Writer, and add rasterization tests to close the visual regression gap.

Purpose: To ensure the generated PDF actually paints correctly in viewers and that this is verified automatically in CI. Addresses the missing dependency on CI and the updated data contract for PNGs.
Output: Updated CI config, updated PDF Writer, and a rasterized regression test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci.yml
@lib/rendro/pdf/writer.ex
@test/rendro/pipeline/render_test.exs

<interfaces>
From lib/rendro/pdf/png.ex (refined in 30-01):
```elixir
defmodule Rendro.PDF.PNG do
  @spec process_for_pdf(binary()) ::
    {:pass_through, binary(), keyword(), term()} |
    {:split, binary(), binary(), term()} |
    {:error, term()}
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI and install poppler-utils</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Modify `.github/workflows/ci.yml` to install `poppler-utils` in the CI job (per Gap 2). 
    Use `sudo apt-get update && sudo apt-get install -y poppler-utils`. 
    This is required for `pdftoppm` which is used in the visual regression tests.
  </action>
  <verify>
    <automated>grep "poppler-utils" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI environment updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update PNG XObject generation in PDF Writer</name>
  <files>lib/rendro/pdf/writer.ex, test/rendro/pdf/writer_test.exs</files>
  <action>
    Modify `build_image_objects/2` in `lib/rendro/pdf/writer.ex`. 
    Use `Rendro.PDF.PNG.process_for_pdf(image.binary)`.
    - If `{:pass_through, idat, parms, color_space}`, use the returned `color_space` directly in the Image dictionary.
    - If `{:split, color, alpha, color_space}`, use the returned `color_space` for the main image and `/DeviceGray` for the SMask.
    - Handle `{:error, :interlaced}` by returning a clear error or using a placeholder (but ideally, Rendro should fail gracefully with a specific error message).
  </action>
  <verify>
    <automated>mix test test/rendro/pdf/writer_test.exs</automated>
  </verify>
  <done>PDF Writer integrated with refined PNG utility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add rasterization visual regression test</name>
  <files>test/rendro/pipeline/render_test.exs</files>
  <behavior>
    - Test 1: Given a PDF with an embedded PNG, `pdftoppm` successfully rasterizes it.
    - Test 2: The rasterized image contains non-white pixel data (proving it painted).
  </behavior>
  <action>
    Add a test to `render_test.exs` that generates a PDF containing a PNG. 
    Use `System.cmd("pdftoppm", ["-png", pdf_path, "output_prefix"])` to rasterize it. 
    Verify that the command succeeds. 
    Note: If `pdftoppm` is missing locally, the test should probably skip or warn, but it MUST pass in CI.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/render_test.exs</automated>
  </verify>
  <done>Visual regression test ensures images paint correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Output -> PDF Reader | Generated PDF dictates viewer rendering behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Denial of Service | `Rendro.PDF.Writer` | mitigate | Predictor configurations are passed accurately; incorrect lengths in `IDAT` are checked via `:zlib` during parsing. |
</threat_model>
