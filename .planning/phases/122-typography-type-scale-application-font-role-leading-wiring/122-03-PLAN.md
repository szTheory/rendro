---
phase: 122-typography-type-scale-application-font-role-leading-wiring
plan: 03
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - lib/rendro/recipes/branded_invoice.ex
  - lib/rendro/recipes/certificate.ex
  - lib/rendro/recipes/ticket.ex
  - test/rendro/recipes/branded_invoice_opts_threading_test.exs
  - test/rendro/recipes/certificate_opts_threading_test.exs
  - test/rendro/recipes/ticket_opts_threading_test.exs
  - test/rendro/recipes/ticket_typography_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, TYPE-03]
must_haves:
  truths:
    - "BrandedInvoice binds display to the brand name (18) as its sole anchor (Q1); its two brand runs keep font: font_name on BOTH paths (Q2, the sole non-:default literal-default exception), only Date/thank-you get role seams"
    - "Certificate threads the resolved size into BOTH the %Text{} AND the centering math (line_h/1, text_width/3, centered_line/5); the size: 1 spacer is NOT seamed; recipient name (34) is the sole display anchor"
    - "Ticket exempts @present_code_size (6) and @caption_size (7) from the scale seam (font-only mono seam), scale-seams the remaining ≤6 sizes with reference code (8) as the SOLE display anchor via non-monotone assignment (Q3)"
    - "An unregistered font role raises {:unknown_text_font, _} via Build.run/1 on a representative recipe (Ticket); all 3 byte-identity goldens render byte-identically with ZERO re-bless"
  artifacts:
    - lib/rendro/recipes/branded_invoice.ex (defp typography/1, brand-font exception)
    - lib/rendro/recipes/certificate.ex (defp typography/1, measurement-coupled seam)
    - lib/rendro/recipes/ticket.ex (defp typography/1, 2 exempted mono micro-sizes)
    - test/rendro/recipes/ticket_typography_test.exs (TYPE-02 raise-path)
  key_links:
    - "BrandedInvoice brand runs: font: font_name preserved on themed path (brand ⊥ theme)"
    - "Certificate: one resolved size -> both text run and centering math (or themed centering drifts)"
    - "Ticket: exempted sizes stay literal @attrs (variable reads, not inline literals) so byte-identity holds and the teeth test does not trip"
---

<objective>
Replicate the proven `typography/1` tracer onto the three RISK recipes, resolving the three RESEARCH open questions explicitly: BrandedInvoice (Q1 anchor + Q2 brand font), Certificate (measurement-math coupling), and Ticket (Q3 7-vs-6 sizes).

Purpose: Seam the recipes with structural risk while preserving byte-identity and honoring the exactly-one-display-anchor rule, using the proven tracer mechanism.
Output: `defp typography/1` + seamed call sites on BrandedInvoice/Certificate/Ticket with each open question resolved in-place, threading-test extensions, and a representative TYPE-02 raise-path test.

## Artifacts this plan produces
- `lib/rendro/recipes/branded_invoice.ex`, `certificate.ex`, `ticket.ex` — each gains a `defp typography(opts)` seam + per-recipe literal-default map (BrandedInvoice's carries the brand-font exception; Ticket's exempts two mono micro-sizes).
- `test/rendro/recipes/ticket_typography_test.exs` — NEW: representative TYPE-02 raise-path proof for this expansion group.
- `test/rendro/recipes/{branded_invoice,certificate,ticket}_opts_threading_test.exs` — extended with typography no-op + themed-differs assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-CONTEXT.md
@.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-RESEARCH.md
@.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-PATTERNS.md

# The proven tracer this plan replicates:
@.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seam BrandedInvoice — resolve Q1 (brand-name anchor) + Q2 (brand font wins)</name>
  <files>lib/rendro/recipes/branded_invoice.ex, test/rendro/recipes/branded_invoice_opts_threading_test.exs</files>
  <read_first>
    - lib/rendro/recipes/branded_invoice.ex — `defp palette(opts)` sibling (~L263); the two brand runs `font: font_name` (~L213 `Rendro, Inc.` size 18, ~L214 `Invoice ##{id}` size 12); `Date:` ~L215 (10); thank-you ~L246 (10); brand font registration ~L174-177; the `Rendro.table/2` totals ~L226-230 (NO %Text — do NOT promote)
    - .planning/phases/122-.../122-RESEARCH.md §"2. BrandedInvoice" mapping table + §"Open Questions Q1 & Q2" + §"Pitfall 3"
    - .planning/phases/122-.../122-PATTERNS.md §"branded_invoice.ex"
    - .planning/phases/122-.../122-01-SUMMARY.md — the proven Invoice seam shape
    - test/rendro/recipes/branded_invoice_byte_identity_test.exs — the frozen golden (do NOT re-bless)
  </read_first>
  <action>
    RESOLVE Q1: bind `display` to the brand name run (`Rendro, Inc.`, 18) — BrandedInvoice's de-facto "one key fact"; document the deviation from the literal "Total Due" wording (its totals live inside `Rendro.table/2`, not a `%Text`). Do NOT add a new total `%Text` run (byte-risk). This makes the brand name the SOLE display anchor.
    RESOLVE Q2: the two brand runs (`Rendro, Inc.` and `Invoice ##{id}`) keep `font: font_name` on BOTH the no-theme AND themed paths — brand ⊥ theme. Their literal-default font is `font_name` (the data-driven embedded brand font, registered ~L174-177) — the SOLE exception to D-02's "all `:default`." Do NOT seam those two runs to `type.fonts.heading`.
    Add `defp typography(opts)` (twin of `palette/1`). Literal-default scale per RESEARCH §2: display 18, title 12, body 10 (subtitle/small/caption unused — pick any). Thread: brand name → `size: type.scale.display`, KEEP `font: font_name`; invoice id → `size: type.scale.title`, KEEP `font: font_name`; `Date:` → `size: type.scale.body` + `font: type.fonts.body`; thank-you → `size: type.scale.body` + `font: type.fonts.body`. Add `line_height/widows/orphans` from the seam to ALL four runs (including the two brand runs). Retire/adjust any BrandedInvoice header freeze comment. Extend `branded_invoice_opts_threading_test.exs` with the typography no-op + themed-differs assertions.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/recipes/branded_invoice_opts_threading_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `branded_invoice.ex` defines `defp typography(`; the two brand runs still pass `font: font_name` (grep confirms `font: font_name` retained on both)
    - EXACTLY ONE BrandedInvoice `%Text{}` binds `size: type.scale.display` (the brand name)
    - `mix test test/rendro/recipes/branded_invoice_byte_identity_test.exs` passes with ZERO re-bless
    - `branded_invoice_opts_threading_test.exs` asserts the typography no-op + themed-differs
  </acceptance_criteria>
  <reversibility rating="reversible">Private per-recipe seam; anchor/brand-font decisions tunable without public-contract change.</reversibility>
  <done>BrandedInvoice seamed; brand name is the display anchor; brand font preserved on both paths; golden byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Seam Certificate — thread resolved size into text run AND centering math</name>
  <files>lib/rendro/recipes/certificate.ex, test/rendro/recipes/certificate_opts_threading_test.exs</files>
  <read_first>
    - lib/rendro/recipes/certificate.ex — `defp palette(opts)` sibling (~L403); `@*_size` attrs ~L301-305 (`@title_size` 20, `@subtitle_size` 12, `@recipient_size` 34, `@body_size` 11, `@meta_size` 10); the `centered_line/5` / `centered_paragraph` call sites ~L347-356; `line_h/1` ~L366 and `text_width/3` ~L338 centering math; the `Rendro.text("", size: 1)` spacer ~L347; brand font registered-but-unapplied ~L274-282,322-326
    - .planning/phases/122-.../122-RESEARCH.md §"5. Certificate" mapping table + §"Pitfall 2" (measurement coupling)
    - .planning/phases/122-.../122-PATTERNS.md §"certificate.ex" (measurement-coupled note)
    - .planning/phases/122-.../122-01-SUMMARY.md — the proven Invoice seam shape
    - test/rendro/recipes/certificate_byte_identity_test.exs — the frozen golden (do NOT re-bless)
  </read_first>
  <action>
    Add `defp typography(opts)` (twin of `palette/1`). Literal-default scale per RESEARCH §5: display 34, title 20, subtitle 12, body 11, small 10 (caption unused). RESOLVE the measurement-coupling: for each element, resolve its size ONCE from the seam and pass that SAME resolved value into BOTH the `%Text{}`/`centered_line` text run AND the centering math (`line_h/1`, `text_width/3`) — never let the text run read the seam while the measurement still reads the old `@*_size` attr, or themed renders de-center (RESEARCH Pitfall 2). Role bindings: recipient name (34) → `scale.display` (SOLE display anchor) + `fonts.heading` (name-anchor → heading); title (20) → `scale.title` + `fonts.heading`; "This certifies that" (12) → `scale.subtitle` + `fonts.body`; body paragraph (11) → `scale.body` + `fonts.body`; date/seal meta (10) → `scale.small` + `fonts.body`. Add `line_height/widows/orphans` from the seam to the seamed text runs. Do NOT seam the `Rendro.text("", size: 1)` spacer — keep its literal `size: 1` (it is a layout hack, not typography). Keep the brand font registered-but-unapplied (deferred per CONTEXT). Extend `certificate_opts_threading_test.exs` with the typography no-op + themed-differs assertions.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/certificate_byte_identity_test.exs test/rendro/recipes/certificate_opts_threading_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `certificate.ex` defines `defp typography(`; the resolved size for each element feeds both the text run and the centering measurement call
    - EXACTLY ONE Certificate element binds `size: type.scale.display` (recipient name)
    - the `Rendro.text("", size: 1)` spacer still uses literal `size: 1`
    - `mix test test/rendro/recipes/certificate_byte_identity_test.exs` passes with ZERO re-bless
    - `certificate_opts_threading_test.exs` asserts the typography no-op + themed-differs
  </acceptance_criteria>
  <reversibility rating="reversible">Private per-recipe seam.</reversibility>
  <done>Certificate seamed with centering math threaded from the same resolved size; spacer untouched; golden byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Seam Ticket — resolve Q3 (exempt 2 mono micro-sizes) + representative TYPE-02 raise-path</name>
  <files>lib/rendro/recipes/ticket.ex, test/rendro/recipes/ticket_opts_threading_test.exs, test/rendro/recipes/ticket_typography_test.exs</files>
  <read_first>
    - lib/rendro/recipes/ticket.ex — `defp palette(opts)` sibling (~L540); `@placement_value_size` 26 (~L298, used L321); ticket title ~L348 (16); subtitle ~L337 (10); issuer ~L345 (9); placement label/terms ~L316,487 (8); `@reference_size` 8 (~L376, used L459); `@caption_size` 7 (~L377, used L455); `@present_code_size` 6 (~L378, used L468)
    - .planning/phases/122-.../122-RESEARCH.md §"7. Ticket" mapping table + §"Open Question Q3" + §"Pitfall 4"
    - .planning/phases/122-.../122-PATTERNS.md §"ticket.ex"
    - .planning/phases/122-.../122-01-SUMMARY.md — the proven Invoice seam shape
    - test/rendro/recipes/ticket_byte_identity_test.exs — the frozen golden (do NOT re-bless)
    - test/rendro/recipes/invoice_typography_test.exs — the raise-path template
  </read_first>
  <action>
    RESOLVE Q3: Ticket has 7 distinct sizes {26,16,10,9,8,7,6} vs 6 scale roles. EXEMPT the two machine/label micro-sizes `@caption_size` (7) and `@present_code_size` (6) from the scale seam — keep them as literal module attrs and seam ONLY their FONT to `mono` (`Rendro.text(x, size: @present_code_size, font: type.fonts.mono, ...)` — the `size:` stays a variable read of the attr, NOT an inline literal, so byte-identity holds and the Wave-3 teeth test does not trip). Scale-seam the remaining 5 distinct sizes across the 6 roles with reference code (8) as the SOLE `display` anchor via non-monotone assignment. Literal-default scale: display 8 (reference code, D-01 anchor), title 26 (placement value), subtitle 16 (ticket title), body 10 (subtitle text), small 9 (issuer), caption 8 (placement label/terms — same literal 8, distinct role key, so byte-identical and NOT a second display binding). Font roles per RESEARCH §7: reference code → `fonts.mono`; placement value → `fonts.body`; ticket title → `fonts.heading`; subtitle/issuer/placement-label/terms → `fonts.body`. Add `defp typography(opts)` (twin of `palette/1`) and add `line_height/widows/orphans` from the seam to seamed text runs. Extend `ticket_opts_threading_test.exs` with the typography no-op + themed-differs assertions. Create `ticket_typography_test.exs` (mirror `invoice_typography_test.exs`): unregistered `typography.fonts.mono` atom → `Build.run(doc)` returns `{:error, {:unknown_text_font, :no_such_font}}`.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/ticket_byte_identity_test.exs test/rendro/recipes/ticket_opts_threading_test.exs test/rendro/recipes/ticket_typography_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `ticket.ex` defines `defp typography(`; `@caption_size` and `@present_code_size` remain literal module attrs (grep confirms the two `@*_size 7`/`@*_size 6` attr definitions still present) and their call sites seam font only
    - EXACTLY ONE Ticket `%Text{}` binds `size: type.scale.display` (reference code)
    - `ticket_typography_test.exs` asserts `{:error, {:unknown_text_font, :no_such_font}} = Build.run(doc)`
    - `mix test test/rendro/recipes/ticket_byte_identity_test.exs` passes with ZERO re-bless
  </acceptance_criteria>
  <reversibility rating="reversible">Private per-recipe seam; the exemption set is tunable without public-contract change.</reversibility>
  <done>Ticket seamed with two mono micro-sizes exempted from the scale; reference code is the display anchor; golden byte-identical; raise-path proven for this group.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer `theme:` opt → recipe | Only external input is a developer theme, validated by the frozen Phase-119 `%Theme{}` contract. No network/auth/session/persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-03 | Tampering | unregistered font role in `theme.typography.fonts` (BrandedInvoice/Certificate/Ticket) | low | mitigate | TYPE-02 typed `{:unknown_text_font, _}` raise-path — proven on the representative Ticket recipe (Task 3); identical resolution path across recipes |

No high/critical threats: pure deterministic in-library typography seam, no untrusted input beyond the frozen-contract theme, fail-loud on unregistered fonts. BrandedInvoice's embedded brand font is developer-supplied and already registered; brand ⊥ theme is preserved. No package installs.
</threat_model>

<verification>
- `mix test test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/recipes/certificate_byte_identity_test.exs test/rendro/recipes/ticket_byte_identity_test.exs` — all byte-identical, zero re-bless (TYPE-01/TYPE-03)
- `mix test test/rendro/edge_matrix_test.exs` — leading no-op (TYPE-03)
- `mix test test/rendro/recipes/ticket_typography_test.exs` — TYPE-02 raise-path
- `mix test test/rendro/recipes/*_opts_threading_test.exs` (the 3) — typography no-op + live seam (TYPE-01)
</verification>

<success_criteria>
BrandedInvoice, Certificate, and Ticket are fully seamed across TYPE-01/02/03 with Q1/Q2/Q3 and the Certificate measurement coupling resolved in-place; all three byte-identity goldens and the edge matrix render byte-identically with zero re-bless; the seam is live on the themed path; TYPE-02 fails loud on the representative recipe.
</success_criteria>

<output>
Create `.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-03-SUMMARY.md` when done
</output>
