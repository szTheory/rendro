---
phase: 44
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/adapters/poppler.ex
  - test/rendro/adapters/poppler_test.exs
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
must_haves:
  truths:
    - User can validate structural integrity of a PDF
    - Missing pdfinfo executable is handled gracefully without crashing
    - Corrupt or invalid PDFs are detected and reported correctly
  artifacts:
    - path: lib/rendro/adapters/poppler.ex
      provides: External binary adapter for Poppler pdfinfo
      contains: validate/1
    - path: test/rendro/adapters/poppler_test.exs
      provides: Tests for Poppler adapter
  key_links:
    - from: lib/rendro/adapters/poppler.ex
      to: System
      via: System.find_executable/1 and System.cmd/3
---

<objective>
Implement the Poppler adapter for external PDF structural validation.

Purpose: Strengthen the evidence and support surface around produced PDFs by verifying them against an established validator (`pdfinfo`), proving basic validity while handling missing dependencies gracefully.
Output: `Rendro.Adapters.Poppler` module and its test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-validator-backed-trust-surfaces/44-CONTEXT.md
@.planning/phases/44-validator-backed-trust-surfaces/44-PATTERNS.md
@.planning/phases/44-validator-backed-trust-surfaces/44-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Poppler Adapter and Tests</name>
  <files>lib/rendro/adapters/poppler.ex, test/rendro/adapters/poppler_test.exs</files>
  <behavior>
    - Test 1: Condition on `System.find_executable("pdfinfo")`. If nil, it asserts the validation returns `{:error, {:missing_executable, "pdfinfo"}}`.
    - Test 2: If `pdfinfo` is available, create a dummy text file (invalid PDF) and assert `{:error, {:invalid_pdf, reason}}`.
    - Test 3: If `pdfinfo` is available, validate a valid PDF fixture (or minimal valid PDF written to a temp file) and assert `{:ok, metadata_map}`.
  </behavior>
  <action>
    Implement `Rendro.Adapters.Poppler` containing a `validate/1` function. 
    1. Check for `pdfinfo` using `System.find_executable("pdfinfo")`. If missing, return `{:error, {:missing_executable, "pdfinfo"}}`. (Reference pattern from `lib/mix/tasks/rendro.visual_uat.ex` ensuring no :enoent crashes).
    2. If found, use `System.cmd/3` with arguments `[file_path]` and `stderr_to_stdout: true`. (Reference pattern from `lib/mix/tasks/verify.ex` as noted in PATTERNS.md).
    3. On exit code 0, parse the output lines (split by ":" and trimmed) into a metadata map and return `{:ok, metadata_map}`.
    4. On non-zero exit code, return `{:error, {:invalid_pdf, String.trim(output)}}`.

    Create the corresponding tests in `test/rendro/adapters/poppler_test.exs`. In the tests, cleanly wrap calls around `case System.find_executable("pdfinfo") do` as seen in the analog `test/rendro/pipeline/render_test.exs`, ensuring the test suite gracefully handles missing executables on CI environments.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/poppler_test.exs</automated>
  </verify>
  <done>Adapter correctly parses metadata on success, returns invalid_pdf tuple on corrupt files, handles missing executable without :enoent crashes, and test cases utilize proper analog patterns.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File path to OS | Untrusted or dynamically generated file paths being passed to shell commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Rendro.Adapters.Poppler.validate/1 | mitigate | System.cmd is used instead of os:cmd, which bypasses shell expansion. Pass the file path as an explicit argument in the list `[file_path]` to prevent command injection. |
| T-44-02 | Info Disclosure | Rendro.Adapters.Poppler | accept | Errors from stderr are returned in the error tuple, which may expose local file paths. This is acceptable for a library used in backend environments. |
</threat_model>

<verification>
mix test test/rendro/adapters/poppler_test.exs
</verification>

<success_criteria>
`Rendro.Adapters.Poppler` is fully implemented and tested.
Valid PDFs yield `{:ok, map}`.
Invalid PDFs yield `{:error, {:invalid_pdf, _}}`.
Missing `pdfinfo` yields `{:error, {:missing_executable, "pdfinfo"}}`.
The test suite passes reliably regardless of whether `pdfinfo` is installed on the host machine.
</success_criteria>

<output>
After completion, create `.planning/phases/44-validator-backed-trust-surfaces/44-01-SUMMARY.md`
</output>
