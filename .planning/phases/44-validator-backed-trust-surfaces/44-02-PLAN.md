---
phase: 44
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - priv/support_matrix.json
autonomous: true
requirements:
  - REQ-04
must_haves:
  truths:
    - User can programmatically check which features and claims are validated
    - Machine-readable matrix outlines the supported validator tools
  artifacts:
    - path: priv/support_matrix.json
      provides: Machine-readable document for validating capabilities
      contains: "pdfinfo"
  key_links: []
---

<objective>
Provide a machine-readable support matrix for validated, experimental, and unsupported surfaces.

Purpose: To fulfill the phase goal of providing a machine-readable support matrix for these validation claims, which allows automated tools or scripts to read the supported boundaries.
Output: A `priv/support_matrix.json` document.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-validator-backed-trust-surfaces/44-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create the Support Matrix</name>
  <files>priv/support_matrix.json</files>
  <action>
    Create a JSON file at `priv/support_matrix.json` that outlines the support matrix for Rendro validations.
    
    Structure it with top-level keys for `"validators"`, `"surfaces"`, and `"unsupported"`.
    - `"validators"` should list `"pdfinfo"` with its version range (`"22+"`) and the claims it validates (`"structural_integrity"`).
    - `"surfaces"` should map out experimental and validated surfaces.
    - `"unsupported"` should explicitly list things like `"full_pdf_compliance"`.
  </action>
  <verify>
    <automated>jq . priv/support_matrix.json</automated>
  </verify>
  <done>The JSON file is properly formatted and accurately reflects the validation capabilities implemented.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Output JSON file is static and stored in `priv/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Information Disclosure | priv/support_matrix.json | accept | The matrix contains public capability information, no sensitive data. |
</threat_model>

<verification>
jq . priv/support_matrix.json > /dev/null
</verification>

<success_criteria>
`priv/support_matrix.json` is successfully created and parses as valid JSON. It accurately reflects the claims from the context.
</success_criteria>

<output>
After completion, create `.planning/phases/44-validator-backed-trust-surfaces/44-02-SUMMARY.md`
</output>
