---
phase: 52
plan: 01
type: execute
wave: 1
depends_on: ["51-02"]
files_modified:
  - lib/rendro/protect.ex
  - lib/rendro/adapters/qpdf.ex
  - lib/rendro/error.ex
  - test/rendro/protect_test.exs
  - test/rendro/adapters/qpdf_test.exs
autonomous: true
requirements:
  - ADAPT-01
must_haves:
  truths:
    - The first-party qpdf adapter remains a runtime-optional executable boundary rather than a Mix dependency or hidden global service.
    - Rendro's public advisory permission contract excludes `:extract_for_accessibility` and exposes only the six truthful high-signal permission atoms.
    - qpdf execution failures stay typed and redacted, and hermetic tests prove temp-dir cleanup and permission mapping without requiring host tools.
  artifacts:
    - path: lib/rendro/protect.ex
      provides: "Contracted public protection permission whitelist and normalized option surface"
    - path: lib/rendro/adapters/qpdf.ex
      provides: "qpdf executable seam with contracted permission mapping and cleanup-safe execution"
    - path: test/rendro/adapters/qpdf_test.exs
      provides: "Hermetic regression coverage for argfile behavior, cleanup, and redacted failure paths"
  key_links:
    - from: lib/rendro/protect.ex
      to: lib/rendro/adapters/qpdf.ex
      via: "normalized advisory permissions cross into qpdf arg generation only after public boundary validation"
    - from: lib/rendro/protect.ex
      to: lib/rendro/error.ex
      via: "unknown-permission and adapter-failure messages remain truthful after permission-surface contraction"
    - from: test/rendro/adapters/qpdf_test.exs
      to: lib/rendro/adapters/qpdf.ex
      via: "injected finder/runner tests prove runtime-optional execution and temp-dir cleanup without real qpdf"
---

<objective>
Harden the qpdf adapter boundary and contract the truthful public permission surface.

Purpose: satisfy ADAPT-01 by keeping qpdf optional at runtime while removing misleading permission semantics and proving the adapter stays safe, narrow, and hermetic by default.
Output: contracted permission contract, qpdf mapping updates, and fast regression coverage for adapter/protect behavior.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-CONTEXT.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-RESEARCH.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-PATTERNS.md
@.planning/phases/51-protection-api-contract-and-validation/51-01-PLAN.md
@.planning/phases/51-protection-api-contract-and-validation/51-CONTEXT.md
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/rendro/protect.ex
@lib/rendro/adapters/qpdf.ex
@lib/rendro/error.ex
@test/rendro/protect_test.exs
@test/rendro/adapters/qpdf_test.exs
@guides/api_stability.md

<interfaces>
From `lib/rendro/protect.ex`:
```elixir
@supported_permissions [
  :annotate,
  :assemble,
  :copy,
  :extract_for_accessibility,
  :fill_forms,
  :modify,
  :print
]

@spec supported_permissions() :: [permission()]
def supported_permissions, do: @supported_permissions
```

From `lib/rendro/adapters/qpdf.ex`:
```elixir
@spec protect(Rendro.Artifact.t(), map()) :: {:ok, binary()} | {:error, term()}
def protect(%Rendro.Artifact{} = artifact, opts) when is_map(opts) do
  with {:ok, executable} <- find_executable(),
       {:ok, tmp_dir} <- make_tmp_dir() do
    try do
      protect_with_tmp_dir(executable, tmp_dir, artifact.binary, opts)
    after
      File.rm_rf(tmp_dir)
    end
  end
end
```

From `lib/rendro/error.ex`:
```elixir
defp why({:adapter_failure, adapter, {:qpdf_failed, exit_code}}),
  do: "Protection adapter #{inspect(adapter)} failed: qpdf exited with status #{exit_code}"

defp next_step(:protect, {:unknown_permissions, _permissions}) do
  "Use only supported advisory permissions: :print, :copy, :modify, :annotate, :fill_forms, :assemble, :extract_for_accessibility."
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract the public advisory-permissions surface to the truthful six-atom set</name>
  <files>lib/rendro/protect.ex, lib/rendro/error.ex, test/rendro/protect_test.exs</files>
  <behavior>
    - `Rendro.Protect.supported_permissions/0` exposes only `:print`, `:copy`, `:modify`, `:annotate`, `:fill_forms`, and `:assemble`.
    - Passing `:extract_for_accessibility` now fails at the public boundary with typed `:protect` errors rather than silently mapping through.
    - Error guidance and protect-facing assertions stay truthful after the permission contraction.
  </behavior>
  <action>
    Update `Rendro.Protect` and the protect-stage guidance in `Rendro.Error` to remove `:extract_for_accessibility` from the stable public contract per D-05, D-06, and D-07. Keep the rest of the public whitelist narrow and Phoenix-friendly, and do not introduce qpdf-shaped parity controls, raw flag passthrough, or new permission tiers per D-08 and D-17.
    Limit boundary changes to what the contraction requires: adjust the permission type/whitelist, unknown-permission messaging, and any protect regression assertions that currently treat `:extract_for_accessibility` as supported. This is not Phase 53 docs/support-matrix closure work; only update public boundary code and tests that must change for truthful semantics.
    Extend `test/rendro/protect_test.exs` so the contracted whitelist is explicit, invalid permissions still fail before adapter invocation, and the redacted protect error details continue exposing only safe booleans/permission atoms rather than secret material.
  </action>
  <verify>
    <automated>mix test test/rendro/protect_test.exs</automated>
  </verify>
  <done>The public protection contract advertises only the six truthful permission atoms, and attempts to use `:extract_for_accessibility` are rejected before qpdf execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the stale qpdf accessibility mapping and harden hermetic adapter regressions</name>
  <files>lib/rendro/adapters/qpdf.ex, test/rendro/adapters/qpdf_test.exs</files>
  <behavior>
    - qpdf arg generation no longer emits `--accessibility` while preserving the remaining curated mappings.
    - Missing-executable, non-zero-exit, and runner-crash paths remain typed and redacted.
    - Hermetic tests prove temp-dir cleanup, argfile permissions, and permission mapping without requiring a real qpdf binary.
  </behavior>
  <action>
    Update `Rendro.Adapters.Qpdf` so its permission mapping matches the contracted public surface from Task 1: keep `:print`, `:copy`, `:modify`, `:annotate`, `:fill_forms`, and `:assemble`, and remove the stale `--accessibility` branch per D-06 through D-08. Preserve the existing runtime-injected executable finder/command runner, argfile secret passing, and `after` cleanup behavior so ADAPT-01 remains runtime-optional and host-tool-light.
    Strengthen `test/rendro/adapters/qpdf_test.exs` around the actual executable boundary per D-09, D-13, and D-16: assert the contracted argfile contents, assert that password values never appear in returned tuples, and keep cleanup coverage for both qpdf non-zero exits and runner exceptions. Do not add live-tool coverage here; keep this plan hermetic and fast, with the real-tool proof lane deferred to 52-02 per D-10.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/qpdf_test.exs test/rendro/protect_test.exs</automated>
  </verify>
  <done>The qpdf adapter matches the contracted public permission contract and retains fast, hermetic regression coverage for the executable boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> `Rendro.Protect` | Untrusted authored permissions and passwords enter the public protection contract here. |
| `Rendro.Protect` -> qpdf executable | Validated options cross from Elixir into an external process and temp-file boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `lib/rendro/protect.ex` | mitigate | Reject unsupported permission atoms at the public boundary so callers cannot smuggle misleading or out-of-scope semantics into qpdf per D-05 through D-08. |
| T-52-02 | Information disclosure | `lib/rendro/adapters/qpdf.ex` | mitigate | Keep password material in private argfiles, preserve redacted typed failures, and assert no raw stderr/password text leaks in returned tuples per D-13 and D-16. |
| T-52-03 | Denial of service / residue | `lib/rendro/adapters/qpdf.ex` | mitigate | Preserve `after` cleanup of temp directories and cover failure-path cleanup with hermetic regression tests so repeated adapter failures do not accumulate scratch files. |
</threat_model>

<verification>
mix test test/rendro/protect_test.exs test/rendro/adapters/qpdf_test.exs
</verification>

<success_criteria>
- ADAPT-01 remains satisfied through an optional runtime qpdf seam with no Mix dependency changes.
- `:extract_for_accessibility` is no longer part of the truthful public permission contract or qpdf mapping.
- Default test coverage for the qpdf seam stays fast, hermetic, and secret-safe.
- No Phase 53 delivery/support-contract scope is pulled into this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/52-qpdf-adapter-and-structural-validation/52-01-SUMMARY.md`
</output>
