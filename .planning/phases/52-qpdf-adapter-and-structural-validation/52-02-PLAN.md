---
phase: 52
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - lib/rendro/adapters/poppler.ex
  - test/rendro/adapters/poppler_test.exs
  - test/rendro/adapters/protected_validation_live_test.exs
  - test/test_helper.exs
  - test/docs_contract/protection_claims_test.exs
  - guides/api_stability.md
autonomous: true
requirements:
  - ADAPT-02
must_haves:
  truths:
    - Poppler structural validation uses exactly one password path per call: `open_password` first, otherwise `owner_password`, never both.
    - Protected-PDF validation failures are normalized into a small redacted reason set instead of returning raw `pdfinfo` stderr.
    - A real `qpdf` plus `pdfinfo` proof lane exists, is explicit and skipped by default, and proves both password requirement and structural readability of generated protected PDFs.
  artifacts:
    - path: lib/rendro/adapters/poppler.ex
      provides: "Password-aware Poppler validation with normalized redacted failure reasons"
    - path: test/rendro/adapters/poppler_test.exs
      provides: "Hermetic regression coverage for password precedence and failure normalization"
    - path: test/rendro/adapters/protected_validation_live_test.exs
      provides: "Opt-in live qpdf/pdfinfo proof lane that generates protected PDFs at runtime"
  key_links:
    - from: lib/rendro/adapters/poppler.ex
      to: lib/rendro/protect.ex
      via: "validation terminology and password-role semantics stay aligned with the public protection contract"
    - from: test/rendro/adapters/protected_validation_live_test.exs
      to: lib/rendro/adapters/poppler.ex
      via: "real protected fixtures validate only through the intended password path"
    - from: guides/api_stability.md
      to: lib/rendro/adapters/poppler.ex
      via: "public wording distinguishes structural decryptability proof from viewer proof and owner-password fallback semantics"
---

<objective>
Make Poppler validation password-aware, redacted, and proof-backed for protected PDFs.

Purpose: satisfy ADAPT-02 by ensuring Poppler proves the intended structural decryptability path without leaking vendor text and without making live host-tool checks part of ordinary `mix test`.
Output: normalized Poppler validation semantics, explicit password-path tests, and an opt-in real-tool proof lane.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-CONTEXT.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-RESEARCH.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-PATTERNS.md
@.planning/phases/51-protection-api-contract-and-validation/51-CONTEXT.md
@.planning/phases/52-qpdf-adapter-and-structural-validation/52-01-PLAN.md
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/rendro/adapters/poppler.ex
@lib/rendro/adapters/qpdf.ex
@lib/rendro/protect.ex
@test/rendro/adapters/poppler_test.exs
@test/rendro/adapters/qpdf_test.exs
@guides/api_stability.md
@priv/support_matrix.json

<interfaces>
From `lib/rendro/adapters/poppler.ex`:
```elixir
def validate(file_path, opts \\ []) do
  case System.find_executable("pdfinfo") do
    nil ->
      {:error, {:missing_executable, "pdfinfo"}}

    executable ->
      case System.cmd(executable, pdfinfo_args(file_path, opts), stderr_to_stdout: true) do
        {output, 0} ->
          {:ok, parse_output(output)}

        {output, _exit_code} ->
          {:error, {:invalid_pdf, String.trim(output)}}
      end
  end
end
```

From `lib/rendro/protect.ex`:
```elixir
@type option ::
        {:adapter, module()}
        | {:algorithm, :aes_256}
        | {:open_password, String.t()}
        | {:owner_password, String.t()}
        | {:advisory_permissions, [permission()]}
```

From `guides/api_stability.md`:
```markdown
Structural validation through `pdfinfo`/Poppler proves that a protected PDF remains structurally readable when a password is supplied to the validator. It does not prove viewer behavior.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize Poppler password precedence and redact validation failures</name>
  <files>lib/rendro/adapters/poppler.ex, test/rendro/adapters/poppler_test.exs, guides/api_stability.md</files>
  <behavior>
    - `open_password` takes precedence and results in only `-upw`; `owner_password` is used only when `open_password` is absent or blank.
    - Validation never passes both password flags in a single `pdfinfo` invocation and never silently retries with the owner password after an open-password failure.
    - Non-success validation preserves the outer `{:invalid_pdf, reason}` tuple shape while normalizing `reason` to a small stable redacted set such as `:password_required`, `:incorrect_password`, `:structural_invalidity`, `:missing_executable`, or `:tool_failure`.
  </behavior>
  <action>
    Rework `Rendro.Adapters.Poppler.validate/2` per D-01 through D-04 so password semantics mirror the public protection contract: prefer `open_password` and use only `-upw` when present, otherwise fall back to `owner_password` with only `-opw`, and never pass both or perform a second-chance retry. Keep `password:` out of scope and preserve the current public terminology from `Rendro.Protect`.
    Replace raw `{:invalid_pdf, String.trim(output)}` shaping with the resolved public contract `{:invalid_pdf, stable_reason}` per D-13 through D-16. The implementation may inspect exit codes and tool text internally to classify reasons, but the stable tuple returned to callers and asserted in tests must not expose raw `pdfinfo` stderr, passwords, temp paths, or command lines.
    Extend `test/rendro/adapters/poppler_test.exs` with hermetic runner/finder coverage for password precedence, missing-executable behavior, wrong-password vs password-required normalization, corrupt-file normalization, and success-path metadata parsing. Update `guides/api_stability.md` only as narrowly needed to state that owner-password-only validation proves structural decryptability fallback, not the normative password-to-open recipient path, and extend `test/docs_contract/protection_claims_test.exs` so the wording remains locked by the existing docs-contract lane.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/poppler_test.exs test/docs_contract/protection_claims_test.exs</automated>
  </verify>
  <done>Poppler validation proves a single intended password path, preserves `{:invalid_pdf, stable_reason}` as the public tuple shape, returns redacted stable reasons, and the guide wording stays truthful about what validation does and does not prove.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an opt-in live qpdf plus pdfinfo proof lane for protected structural validation</name>
  <files>test/test_helper.exs, test/rendro/adapters/protected_validation_live_test.exs, test/rendro/adapters/poppler_test.exs</files>
  <behavior>
    - The live lane is explicit, tagged, and skipped by default under ordinary `mix test`.
    - The proof generates or writes an unprotected seed PDF at runtime, protects it with real qpdf using AES-256, proves the result is encrypted and password-gated, and then validates it with `pdfinfo` through the intended password path.
    - The live lane freezes the real-tool negative cases needed for classifier truthfulness: no password maps to `{:invalid_pdf, :password_required}` and a wrong open password maps to `{:invalid_pdf, :incorrect_password}`.
    - The lane skips cleanly when `qpdf` or `pdfinfo` are unavailable and does not commit protected fixture binaries.
  </behavior>
  <action>
    Add a narrow live-tool proof file per D-09 through D-12, using real `qpdf` and `pdfinfo` only in an explicit tagged lane such as `:live_pdf_tools`. Wire that lane to stay opt-in by default in `test/test_helper.exs` via an explicit ExUnit exclude, and make the opt-in command explicit in test tags or module docs so ordinary `mix test` does not run host-tool proofs on machines without qpdf.
    In the live proof, build or write an unprotected seed PDF during execution, call the real protection boundary to produce a protected artifact, confirm the resulting file is encrypted and actually requires a password, and then prove structural readability with `Poppler.validate/2` using the intended password path. Freeze the two real-tool negative cases the classifier depends on: `(a) no password -> {:invalid_pdf, :password_required}` and `(b) wrong open password -> {:invalid_pdf, :incorrect_password}`. Include one owner-password fallback assertion only when `open_password` is intentionally absent; do not create any proof that treats a successful owner-password bypass as equivalent to the normative open-password path.
    Reuse runtime-generated fixtures only; do not add checked-in protected PDFs or widen the proof into Phase 53 support-matrix closure or Phase 54 viewer/release work.
  </action>
  <verify>
    <automated>mix test test/rendro/adapters/poppler_test.exs test/docs_contract/protection_claims_test.exs</automated>
    <automated>mix test --include live_pdf_tools test/rendro/adapters/protected_validation_live_test.exs</automated>
  </verify>
  <done>An explicit opt-in real-tool proof lane exists for protected structural validation, ordinary default tests remain fast and independent of host qpdf availability, and the real-tool negative cases pin the normalized password-failure classifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> `Rendro.Adapters.Poppler` | Untrusted file paths and password options cross into the structural validation seam here. |
| `Rendro.Adapters.Poppler` -> `pdfinfo` executable | Validation crosses from Elixir into an external CLI boundary whose raw output must not become public API. |
| Live proof lane -> host tooling | Opt-in tests depend on real host executables and runtime-generated protected artifacts here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Spoofing / semantic confusion | `lib/rendro/adapters/poppler.ex` | mitigate | Enforce one-password-path semantics so a caller cannot mistake owner-password bypass success for proof of the open-password path per D-01 through D-04. |
| T-52-05 | Information disclosure | `lib/rendro/adapters/poppler.ex` | mitigate | Normalize failures into a small stable reason set and keep raw `pdfinfo` stderr, passwords, temp paths, and command lines out of public tuples and guide wording per D-13 through D-16. |
| T-52-06 | Denial of service / workflow friction | `test/rendro/adapters/protected_validation_live_test.exs` | mitigate | Tag and skip live host-tool proof by default so ordinary `mix test` remains contributor-friendly and only explicit proof lanes require qpdf/pdfinfo per D-09 through D-12. |
</threat_model>

<verification>
mix test test/rendro/adapters/poppler_test.exs test/docs_contract/protection_claims_test.exs
mix test --include live_pdf_tools test/rendro/adapters/protected_validation_live_test.exs
</verification>

<success_criteria>
- ADAPT-02 is covered by single-path password semantics and redacted Poppler failure normalization.
- No stable public tuple or guide wording exposes raw `pdfinfo` stderr or conflates owner-password fallback with the normative open-password path.
- A real qpdf plus pdfinfo proof lane exists, is explicit, and is excluded from ordinary default tests.
- No Phase 53 support-matrix closure or Phase 54 viewer/release work is added here.
</success_criteria>

<output>
After completion, create `.planning/phases/52-qpdf-adapter-and-structural-validation/52-02-SUMMARY.md`
</output>
