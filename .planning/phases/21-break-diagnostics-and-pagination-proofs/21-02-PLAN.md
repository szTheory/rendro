---
phase: "21-break-diagnostics-and-pagination-proofs"
plan: 2
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - lib/rendro/inspector.ex
  - test/rendro/inspector_test.exs
  - README.md
autonomous: true
requirements:
  - QUAL-06
must_haves:
  truths:
    - Developers can visualize the layout output as an ASCII tree instead of generating PDFs.
    - Snapshot tests accurately prove placement bounds, preventing regressions.
    - Diagnostics are visible at the bottom of the tree output.
    - Public documentation demonstrates how to debug pagination using `Rendro.Inspector`.
  artifacts:
    - path: "lib/rendro/inspector.ex"
      provides: "ASCII layout tree serialization"
      contains: "def inspect(%Rendro.Document{}"
    - path: "test/rendro/inspector_test.exs"
      provides: "deterministic multiline snapshot tests"
      contains: "assert Rendro.Inspector.inspect(doc) =="
    - path: "README.md"
      provides: "diagnostics debugging guide"
      contains: "Rendro.Inspector"
  key_links:
    - from: "lib/rendro/inspector.ex"
      to: "lib/rendro/document.ex"
      via: "walk doc.pages and doc.diagnostics"
      pattern: "walk_blocks"
---

<objective>
Implement a deterministic layout inspector for plain-text snapshot testing and update documentation on debugging layout behavior.

Purpose: Provide a bulletproof ExUnit testing mechanism for verifying structural pagination behavior without comparing binary PDF hashes. Introduce operators to the new diagnostic capabilities.
Output: `Rendro.Inspector` utility, test suite, and updated README.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-break-diagnostics-and-pagination-proofs/21-CONTEXT.md
@.planning/phases/21-break-diagnostics-and-pagination-proofs/21-RESEARCH.md
@.planning/phases/21-break-diagnostics-and-pagination-proofs/21-PATTERNS.md
@lib/rendro/inspector.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ASCII Layout Tree Inspector</name>
  <files>lib/rendro/inspector.ex, test/rendro/inspector_test.exs</files>
  <behavior>
    - `Rendro.Inspector.inspect(doc)` returns a formatted multiline string.
    - Includes `Page X (WxH)` followed by a tree of blocks with their `(x, y, w, h)`.
    - Identifies block content (e.g., `Text`, `Table`, `Image`).
    - Appends an `\nDiagnostics:\n- [info] table_split on page 2...` section if `doc.diagnostics` is not empty.
  </behavior>
  <action>
    Create `lib/rendro/inspector.ex` with a recursive tree walker.
    - Format regions/blocks: `├── Block: Table (x: 36, y: 36, w: 540, h: 600)`
    - Iterate `doc.pages`, `page.blocks`. Since blocks are flattened in pages, iterate and print them.
    - Append `Diagnostics:\n` at the bottom, mapping each diagnostic to a `- [level] type...` string.

    Per PATTERNS.md analog `lib/rendro/error.ex` - String formatting pattern (lines 80-92): Follow this exact pattern which uses multi-line string interpolation (`"""`) to build text representations. Implement an equivalent ASCII text tree serializer for `%Rendro.Document{}` that includes `diagnostics`, bounding boxes, and structural types (QUAL-06).

    Create `test/rendro/inspector_test.exs` and construct a mock layout document to assert the output exactly via `assert Rendro.Inspector.inspect(doc) == """..."""`.

    Per PATTERNS.md analog `test/rendro/error_test.exs` - Test structure pattern (lines 1-19): Adapt this exact test structure (setting up with `use ExUnit.Case, async: true`) to construct a `%Rendro.Document{}` with diagnostics and assert its exact string representation using an ExUnit snapshot via `Rendro.Inspector`.
  </action>
  <verify>
    <automated>mix test test/rendro/inspector_test.exs</automated>
  </verify>
  <done>Layouts can be proven deterministically in tests via text snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Document Diagnostics and Inspector</name>
  <files>README.md</files>
  <action>
    Update `README.md`.
    - Under the "Features" or "Production-Ready" section, add a clear example of checking `doc.diagnostics` after `Rendro.render(doc)`.
    - Introduce `Rendro.Inspector.inspect(doc)` as a quick way for developers to debug layout anomalies in the terminal without opening the PDF.
  </action>
  <verify>
    <automated>mix cmd grep Inspector README.md</automated>
  </verify>
  <done>Public documentation establishes the correct way to debug pagination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tool -> Test | Text output representation of in-memory data structures |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Denial of Service | `lib/rendro/inspector.ex` | accept | Utility is primarily intended for test runs and local dev iteration, not untrusted unbounded parsing in prod. |
</threat_model>

<verification>
`mix test test/rendro/inspector_test.exs` passes.
</verification>

<success_criteria>
`Rendro.Inspector` accurately outputs bounding boxes and diagnostics, allowing pure string match logic for regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/21-break-diagnostics-and-pagination-proofs/21-02-SUMMARY.md`
</output>
