---
phase: 45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/form_field.ex
  - lib/rendro/rules/check_form_fields.ex
  - lib/rendro/pipeline/validate.ex
  - lib/rendro.ex
  - lib/rendro/pipeline/measure.ex
autonomous: true
requirements:
  - M4-FORMS
must_haves:
  truths:
    - Developer can create interactive text fields via Elixir DSL.
    - Missing names on form fields yield a structural validation error.
    - Form fields participate in layout measurement with explicit or fallback dimensions.
  artifacts:
    - path: lib/rendro/form_field.ex
      provides: "Rendro.FormField struct and typespec"
    - path: lib/rendro/rules/check_form_fields.ex
      provides: "Validation rule for form field integrity"
  key_links:
    - from: lib/rendro.ex
      to: lib/rendro/form_field.ex
      via: "form_field/3 DSL constructs struct"
    - from: lib/rendro/pipeline/validate.ex
      to: lib/rendro/rules/check_form_fields.ex
      via: "inclusion in @default_rules list"
---

<objective>
Implement the core domain data structure, public DSL, validation, and layout measurement logic for Interactive Form Fields (AcroForm Text Fields).

Purpose: Provides the foundation for interactive PDFs by allowing developers to define fields using `Rendro.form_field/3` which are then validated and sized correctly within the layout flow.
Output: Validated `Rendro.FormField` components in the document tree with resolved dimensions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-CONTEXT.md
@.planning/phases/45-PATTERNS.md
@.planning/phases/45-RESEARCH.md
</context>

<interfaces>
From lib/rendro/text.ex (Analog):
```elixir
defmodule Rendro.Text do
  @enforce_keys [:content]
  defstruct [:content, font: "Helvetica", size: 12, color: {0, 0, 0}]
end
```

From lib/rendro/rules/check_required_keys.ex (Analog):
```elixir
defmodule Rendro.Rules.CheckRequiredKeys do
  def check(%Rendro.Document{pages: pages}, _doc) when is_list(pages), do: :ok
  def check(%Rendro.Block{content: nil}, _doc), do: {:error, {:missing_required_key, :content}}
  def check(_, _doc), do: :ok
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Form Field Domain and Validation</name>
  <files>lib/rendro/form_field.ex, lib/rendro/rules/check_form_fields.ex, lib/rendro/pipeline/validate.ex, test/rendro/rules/check_form_fields_test.exs</files>
  <behavior>
    - `Rendro.FormField` is a struct requiring `:name` and providing defaults for `:value`, `:font`, and `:size`.
    - `Rendro.Rules.CheckFormFields` returns `:ok` if a FormField has a non-nil, non-empty name.
    - `Rendro.Rules.CheckFormFields` returns `{:error, {:missing_required_key, :name}}` if name is missing or empty.
  </behavior>
  <action>
    Create `Rendro.FormField` following `Rendro.Text` pattern.
    Create `Rendro.Rules.CheckFormFields` following `CheckRequiredKeys` pattern.
    Update `Rendro.Pipeline.Validate` to include `CheckFormFields` in `@default_rules`.
  </action>
  <verify>
    <automated>mix test test/rendro/rules/check_form_fields_test.exs</automated>
  </verify>
  <done>Struct exists, rule validates correctly, pipeline includes rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public DSL and Measurement Pipeline</name>
  <files>lib/rendro.ex, lib/rendro/pipeline/measure.ex, test/rendro_builders_test.exs</files>
  <behavior>
    - `Rendro.form_field/3` builds a `Rendro.Block` containing a `Rendro.FormField`.
    - Measurement pipeline assigns fallback `width` (150.0) and `height` (20.0) if missing on the block.
    - Measurement preserves explicit block sizing if provided by user.
  </behavior>
  <action>
    Implement `Rendro.form_field(name, value \\ "", attrs \\ [])` based on `text/2` builder pattern.
    Update `Rendro.Pipeline.Measure.measure_block/3` to match `%Rendro.Block{content: %Rendro.FormField{}}` and inject default dimensions if missing.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>
  <done>DSL creates fields, measure assigns bounding box.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User API -> Core | Developer defines form field names and values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `FormField` | accept | Rendro produces deterministic byte output. Values are developer-supplied at render time. Values will be escaped in Plan 02. |
</threat_model>

<success_criteria>
- Developer can invoke `Rendro.form_field` without error.
- Block structure contains `Rendro.FormField` with default values and dimensions.
</success_criteria>

<output>
After completion, create `.planning/phases/45-01-PLAN.md` summary context logic if needed, actually just proceed via orchestration.
</output>