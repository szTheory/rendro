---
phase: 86-self-proving-launch-artifacts
plan: 04
type: execute
wave: 3
depends_on:
  - 86-01
  - 86-02
  - 86-03
files_modified:
  - lib/rendro/launch_artifacts.ex
  - README.md
  - guides/recipes.md
  - test/docs_contract/launch_artifacts_claims_test.exs
autonomous: true
requirements:
  - GAL-01
  - GAL-02
  - GAL-03

must_haves:
  truths:
    - "README first screen uses `Native PDF layout for Elixir.` and the locked open-source, Elixir-native first mention."
    - "Generated README and recipes-guide blocks call outputs curated deterministic recipe fixtures and distinguish required source/manual proof from advisory pdfium-render PNG proof."
    - "guides/recipes.md no longer says Rendro ships only four canonical visual examples when the gallery presents five entries."
    - "manual.pdf source remains an 8-12 page compact proof artifact with cover, fit/boundary page, five recipe pages, determinism proof, path proof, and running page footer."
    - "Decision coverage: D-06, D-07, D-08, D-13, D-14, D-15, and D-16 are reflected in generator copy, hand-authored docs copy, and docs-contract assertions."
  artifacts:
    - path: "README.md"
      provides: "Brand-aligned first screen and concise proof placement"
      contains: "Native PDF layout for Elixir."
    - path: "guides/recipes.md"
      provides: "HexDocs recipe gallery introduction that matches the five-entry gallery"
      contains: "five rendered gallery entries"
    - path: "lib/rendro/launch_artifacts.ex"
      provides: "Source-of-truth generated README, recipes guide, and manual copy"
      contains: "curated deterministic recipe fixtures"
    - path: "test/docs_contract/launch_artifacts_claims_test.exs"
      provides: "Copy and accessibility guards for Phase 86 public presentation"
      contains: "Native PDF layout for Elixir."
---

<objective>
Bring the public presentation, generated docs blocks, and compact self-rendered manual into the Rendro brand contract. This plan is copy and presentation source work; Plan 86-05 performs the final pdfium-backed regeneration and visual review.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md
@.planning/phases/86-self-proving-launch-artifacts/86-RESEARCH.md
@.planning/phases/86-self-proving-launch-artifacts/86-PATTERNS.md
@.planning/phases/86-self-proving-launch-artifacts/86-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align README and guide hand-authored copy with the brand contract</name>
  <files>README.md, guides/recipes.md</files>
  <read_first>
    - README.md - file being modified; first screen, feature bullets, generated launch block placement, and canonical recipe section
    - guides/recipes.md - file being modified; opening copy and generated gallery block placement
    - .planning/phases/86-self-proving-launch-artifacts/86-UI-SPEC.md - README first-screen order, copywriting contract, anti-patterns
    - .planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md - decisions D-13 through D-16
    - .planning/phases/86-self-proving-launch-artifacts/86-RESEARCH.md - README opening and stale guide count gaps
  </read_first>
  <action>
    Edit `README.md`.

    Keep the first screen order:
    1. `# Rendro`
    2. existing CI/Hex/HexDocs badges
    3. tagline line exactly `Native PDF layout for Elixir.`
    4. first-mention sentence with this exact meaning: `Rendro is an open-source, Elixir-native PDF layout library for Phoenix teams that need reliable PDFs without Chrome.`
    5. concise feature bullets
    6. generated launch artifact block

    Replace the current tagline `Pure-Elixir PDF generation with deterministic layout and pagination.` with the locked tagline and first-mention sentence.

    Keep feature bullets short and truthful. They may mention pure Elixir core, deterministic output, flow/pagination, opt-in table polish, Phoenix fit, telemetry/diagnostics, and proof-backed boundaries. Do not claim arbitrary HTML/CSS, PDF/A, PDF/UA, GUI-viewer compatibility, every-viewer support, or default launch fixture styling.

    Do not add a README hero composite, custom marketing banner, browser-window artwork, printer/prepress cue, JavaScript, lightbox, or remote image.

    Edit `guides/recipes.md`.

    Replace the stale opening sentence that says Rendro ships four canonical recipes with wording that says Rendro ships canonical recipes and five rendered gallery entries, including a branded invoice variant. Keep the guide's support-matrix boundary paragraph.

    Do not hand-author new gallery metadata outside the generated block.
  </action>
  <acceptance_criteria>
    - `README.md` contains `Native PDF layout for Elixir.` before `## Features`.
    - `README.md` contains `Rendro is an open-source, Elixir-native PDF layout library for Phoenix teams that need reliable PDFs without Chrome.`
    - `README.md` keeps the generated launch block before `## Guides`.
    - `guides/recipes.md` contains `five rendered gallery entries`.
    - `guides/recipes.md` does not contain the phrase `four canonical recipes`.
    - Neither file contains `PDF/A compliant`, `PDF/UA compliant`, `works in every viewer`, `full HTML/CSS rendering`, `browserless viewer`, `prepress SDK`, or `pixel-perfect HTML-to-PDF`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update generated README and recipes-guide block copy</name>
  <files>lib/rendro/launch_artifacts.ex, test/docs_contract/launch_artifacts_claims_test.exs</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex - file being modified; readme_block/1 and recipes_block/1 are source of truth for generated docs blocks
    - test/docs_contract/launch_artifacts_claims_test.exs - file being modified; public copy and generated block assertions
    - assets/rendro/artifacts.json - current manifest values used by block renderer
    - README.md - generated block markers and first-screen placement
    - guides/recipes.md - generated block markers and richer gallery presentation
    - .planning/phases/86-self-proving-launch-artifacts/86-UI-SPEC.md - locked generated copy phrases and accessibility contract
    - .planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md - decisions D-09 through D-16
  </read_first>
  <action>
    Edit `lib/rendro/launch_artifacts.ex`.

    Update `readme_block/1` so the generated block includes these exact proof concepts:
    - `These previews are rendered by Rendro from curated deterministic recipe fixtures.`
    - `Source PDFs and the self-rendered manual are byte-checked by the required docs contract.`
    - `PNG rasters are regenerated and hash-checked in the pinned pdfium-render advisory lane.`
    - `pdfium-render rasters are render proof, not GUI-viewer proof.`
    - `Launch fixtures may use opt-in table polish; canonical recipe defaults remain unchanged.`

    Keep README thumbnail width exactly `150`.

    Keep every thumbnail as an anchor whose `href` equals the nested image `src`.

    Update `recipes_block/1` so the guide block includes:
    - `## Rendered Gallery`
    - the same required/advisory proof split, with source PDFs/manual separated from PNG raster regeneration
    - five entries in manifest order
    - image width exactly `320`
    - each caption from the manifest
    - each `Source PDF SHA-256` and `PNG SHA-256`
    - `## Self-Rendered Manual`
    - `manual.pdf` link and SHA-256

    Edit `test/docs_contract/launch_artifacts_claims_test.exs` so public copy tests assert the positive phrases above. The test must allow the required boundary phrase `not GUI-viewer proof` while still rejecting positive GUI-viewer claims such as `is GUI-viewer proof` and `are GUI-viewer proof`.

    Do not change generated hashes by hand in this plan. Plan 86-05 regenerates assets and docs blocks from the generator.
  </action>
  <acceptance_criteria>
    - `mix run -e 'm = Rendro.LaunchArtifacts.read_manifest!(); IO.puts(Rendro.LaunchArtifacts.readme_block(m))'` prints `curated deterministic recipe fixtures`.
    - `mix run -e 'm = Rendro.LaunchArtifacts.read_manifest!(); IO.puts(Rendro.LaunchArtifacts.readme_block(m))'` prints `not GUI-viewer proof`.
    - `mix run -e 'm = Rendro.LaunchArtifacts.read_manifest!(); IO.puts(Rendro.LaunchArtifacts.recipes_block(m))'` prints `Source PDF SHA-256`.
    - `test/docs_contract/launch_artifacts_claims_test.exs` contains a positive assertion for `canonical recipe defaults remain unchanged`.
    - `test/docs_contract/launch_artifacts_claims_test.exs` does not reject the substring `not GUI-viewer proof`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Tighten compact manual source copy and structure</name>
  <files>lib/rendro/launch_artifacts.ex</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex - file being modified; manual_document/0, manual_blocks/0, recipe_page/1, path_rule/2, path_diagram/0
    - assets/rendro/gallery/*.png - current manual image inputs registered by source path
    - .planning/phases/86-self-proving-launch-artifacts/86-UI-SPEC.md - manual PDF page count, section hierarchy, visual tone, footer contract
    - .planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md - decisions D-06 through D-08
    - .planning/phases/86-self-proving-launch-artifacts/86-PATTERNS.md - manual PDF patterns
  </read_first>
  <action>
    Edit `lib/rendro/launch_artifacts.ex` manual source functions.

    Keep the manual compact at 8-12 pages. Preserve the current one-page-per-gallery-entry shape.

    Ensure the manual source has these pages or page groups:
    - cover page with `Native PDF layout for Elixir`, generated-by-Rendro statement, external SHA note, and HexDocs pointer.
    - fit check and boundaries page naming good fits, HTML-to-PDF boundary, complex-script boundary, no PDF/A/PDF/UA claim, and `pdfium-render` not GUI-viewer proof.
    - one page per gallery entry with rendered image, job-to-be-done caption, module name, and manifest pointer.
    - determinism/path proof page naming manifest, source PDF hashes, PNG hashes, renderer metadata, manual hash, `mix rendro.launch_artifacts.check`, Path primitive proof, and advisory raster lane.

    Keep the footer format exactly `Rendro manual - Page {{page_number}} of {{total_pages}}`.

    Keep A4 portrait manual dimensions and 54pt margin baseline unless a deliberate change is recorded in the summary.

    Do not imply PDF-in-PDF composition support. It is acceptable for the manual to embed generated PNG previews because source PDFs and PNGs are separately hash-checked.
  </action>
  <acceptance_criteria>
    - `lib/rendro/launch_artifacts.ex` manual source contains `Native PDF layout for Elixir`.
    - `lib/rendro/launch_artifacts.ex` manual source contains `No arbitrary HTML/CSS rendering`.
    - `lib/rendro/launch_artifacts.ex` manual source contains `No blanket PDF/A or PDF/UA claim`.
    - `lib/rendro/launch_artifacts.ex` manual source contains `pdfium-render` or `Pdfium` and `not GUI-viewer proof`.
    - `lib/rendro/launch_artifacts.ex` manual source contains `mix rendro.launch_artifacts.check`.
    - `Rendro.LaunchArtifacts.render_manual_pdf()` returns `{:ok, pdf}` with `%PDF-` header.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Public README and HexDocs copy becomes launch-facing evidence and must stay bounded to checked artifacts.
- Generated docs blocks are trusted only when produced from `Rendro.LaunchArtifacts` and the manifest.
- Manual PDF source is a proof artifact, not an API reference or compliance certificate.
</trust_boundaries>

<threats>
- T-86-16 stale/hash-drift artifacts: mitigate by keeping docs blocks generated from manifest; final hash regeneration happens in Plan 86-05.
- T-86-17 accidental public overclaims: mitigate with locked brand/copy phrases and docs-contract guards rejecting GUI-viewer, PDF/A, PDF/UA, HTML/CSS, browser/prepress, and default-styling overclaims.
- T-86-18 package-content mistakes: accepted here; Plan 86-02 package proof and Plan 86-05 final verification cover package contents.
- T-86-19 required CI depending on external pdfium tooling: mitigate by making this plan source/copy-only; no pdfium generation required.
- T-86-20 generated asset/tarball worktree noise: mitigate by not running `mix rendro.launch_artifacts.gen` or `mix hex.build` in this plan unless a targeted test already cleans up.
</threats>

<high_severity_blockers>
None expected. Stop if brand alignment requires a README hero, remote image dependency, JavaScript gallery, or a claim beyond current support matrix proof.
</high_severity_blockers>
</threat_model>

<verification>
Required source checks after this plan:
- `grep -n "Native PDF layout for Elixir." README.md`
- `grep -n "five rendered gallery entries" guides/recipes.md`
- `mix run -e 'm = Rendro.LaunchArtifacts.read_manifest!(); IO.puts(Rendro.LaunchArtifacts.readme_block(m))'`
- `mix run -e 'm = Rendro.LaunchArtifacts.read_manifest!(); IO.puts(Rendro.LaunchArtifacts.recipes_block(m))'`
- `mix run -e 'case Rendro.LaunchArtifacts.render_manual_pdf() do {:ok, <<"%PDF-", _::binary>>} -> :ok; other -> raise inspect(other) end'`

Expected state:
- Full docs-contract may still report source PDF, manual, or generated block drift until Plan 86-05 regenerates artifacts from the final source.
</verification>

<success_criteria>
Plan 86-04 is complete when README, HexDocs block generators, manual source, and copy guards all express the Phase 86 brand and proof contract without overclaiming or changing the required/advisory verification split.
</success_criteria>
