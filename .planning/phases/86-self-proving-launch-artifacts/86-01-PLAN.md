---
phase: 86-self-proving-launch-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/guardrails/required_status_checks.json
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
requirements:
  - GAL-01
  - GAL-02

must_haves:
  truths:
    - "raster-advisory remains advisory-only: it is present in advisory_contexts and absent from required_contexts."
    - "priv/guardrails/required_status_checks.json documents both raster snapshot testing and mix rendro.launch_artifacts.check in the raster-advisory command."
    - "The test GitHub Actions job still runs only mix ci; required mix ci does not install pdfium-cli, curl a pdfium binary, or run mix rendro.launch_artifacts.check."
    - "raster-advisory is graph-disconnected and continue-on-error: true, so pdfium download/check failures cannot block required engine merges."
    - "Decision coverage: D-09, D-10, D-11, and D-12 are protected by executable guardrail tests."
  artifacts:
    - path: "priv/guardrails/required_status_checks.json"
      provides: "Required/advisory status context contract updated for Phase 86 launch artifact checking"
      contains: "mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs && mix rendro.launch_artifacts.check"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Negative proof that required CI remains pdfium-free and launch artifact PNG regeneration remains advisory"
      contains: "rendro.launch_artifacts.check"
---

<objective>
Lock the Phase 85 advisory boundary before launch artifact work adds more generated proof. This plan updates the guardrail registry and tests so Phase 86 cannot accidentally make required CI depend on external pdfium tooling or raster regeneration.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md
@.planning/phases/86-self-proving-launch-artifacts/86-RESEARCH.md
@.planning/phases/86-self-proving-launch-artifacts/86-PATTERNS.md
@.planning/phases/85-deterministic-raster-lane/85-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the advisory status-check registry for launch artifacts</name>
  <files>priv/guardrails/required_status_checks.json</files>
  <read_first>
    - priv/guardrails/required_status_checks.json - file being modified; current required_contexts, contexts, advisory_contexts shape
    - .github/workflows/ci.yml - source of truth for actual raster-advisory CI commands
    - .planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md - decisions D-09 through D-12 about required/static versus advisory/raster verification
    - .planning/phases/85-deterministic-raster-lane/85-VERIFICATION.md - verified advisory raster lane boundary to preserve
  </read_first>
  <action>
    Edit `priv/guardrails/required_status_checks.json`.

    Locate the advisory context object with `"name": "raster-advisory"`.

    Change its `"command"` value to exactly:
    `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs && mix rendro.launch_artifacts.check`

    Update the `"notes"` value so it names both Phase 85 deterministic raster snapshots and Phase 86 launch artifact PNG/hash checks, and explicitly says the context is not required on main and pdfium download/check failures must never block engine merges.

    Do not add `raster-advisory` to `"required_contexts"`.

    Do not change the four required contexts: `long-lived-live-proof`, `release-proof`, `signing-live-proof`, and `test`.
  </action>
  <acceptance_criteria>
    - `priv/guardrails/required_status_checks.json` still parses as JSON.
    - `required_contexts` is exactly `["long-lived-live-proof", "release-proof", "signing-live-proof", "test"]`.
    - `advisory_contexts` contains a `raster-advisory` object whose command contains both `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs` and `mix rendro.launch_artifacts.check`.
    - `advisory_contexts` `raster-advisory.notes` contains `Phase 86` and `not required`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add negative guardrail tests for required/advisory separation</name>
  <files>test/guardrails/required_checks_contract_test.exs</files>
  <read_first>
    - test/guardrails/required_checks_contract_test.exs - file being modified; existing baseline JSON, ci.yml, and mix ci alias checks
    - priv/guardrails/required_status_checks.json - updated registry values from Task 1
    - .github/workflows/ci.yml - actual GitHub Actions job blocks; required `test` job and advisory `raster-advisory` job
    - mix.exs - source of truth for the `ci` alias exact command list
    - .planning/phases/86-self-proving-launch-artifacts/86-PATTERNS.md - CI advisory lane and guardrails patterns
  </read_first>
  <action>
    Edit `test/guardrails/required_checks_contract_test.exs`.

    Add coverage that:
    - `raster-advisory` is present in `baseline["advisory_contexts"]`.
    - `raster-advisory` is absent from `baseline["required_contexts"]`.
    - the registry command for `raster-advisory` includes `mix rendro.launch_artifacts.check`.
    - `.github/workflows/ci.yml` contains a `raster-advisory:` job with `continue-on-error: true`.
    - the `raster-advisory` job block has no `needs:` key.
    - the required `test:` job block still runs `mix ci`.
    - the required `test:` job block does not contain `pdfium-cli`, `curl -fsSL`, `rendro.launch_artifacts.check`, or `Rendro.Adapters.Pdfium.render`.
    - the `ci` alias in `mix.exs` remains the exact deterministic required list: `format --check-formatted`, `hex.build`, `compile --warnings-as-errors`, `test`, `docs`, `credo --strict`, `dialyzer`.

    When extracting job blocks from `.github/workflows/ci.yml`, scope the check to the specific job block instead of scanning the whole file, because other advisory/live-proof jobs legitimately install pdfium.
  </action>
  <acceptance_criteria>
    - `mix test test/guardrails/required_checks_contract_test.exs` exits 0.
    - If `mix rendro.launch_artifacts.check` is added to the required `test:` job block, `mix test test/guardrails/required_checks_contract_test.exs` fails.
    - If `raster-advisory` is added to `required_contexts`, `mix test test/guardrails/required_checks_contract_test.exs` fails.
    - If `continue-on-error: true` is removed from the `raster-advisory` job block, `mix test test/guardrails/required_checks_contract_test.exs` fails.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Required CI (`mix ci`) must stay deterministic/static and must not depend on external pdfium tooling.
- Advisory raster CI may install the pinned pdfium binary, regenerate PNGs, and fail without blocking required engine contexts.
- Guardrail JSON is the durable contract used to detect status-context drift.
</trust_boundaries>

<threats>
- T-86-01 stale/hash-drift artifacts: mitigated by documenting `mix rendro.launch_artifacts.check` in advisory registry; required static proof is handled by later plans.
- T-86-02 accidental public overclaims: mitigated indirectly by preserving the static/advisory split so `pdfium-render` cannot become required GUI-viewer proof.
- T-86-03 package-content mistakes: accepted here; package inclusion proof is handled by Plan 86-02.
- T-86-04 required CI depending on external pdfium tooling: block if introduced. Guardrail tests must fail if `test:` or `mix ci` runs pdfium install/download or `mix rendro.launch_artifacts.check`.
- T-86-05 generated asset/tarball worktree noise: accepted here; no generated assets or package tarballs are written by this plan.
</threats>

<high_severity_blockers>
None expected. If required `mix ci` must download or execute pdfium to make this plan pass, stop and return `## PLANNING INCONCLUSIVE` because that violates D-11 and Phase 85's verified advisory boundary.
</high_severity_blockers>
</threat_model>

<verification>
Required commands after this plan:
- `mix test test/guardrails/required_checks_contract_test.exs`
- `mix test test/docs_contract/raster_claims_test.exs`

This plan must not require:
- `mix rendro.launch_artifacts.check`
- `mix test --include raster_snapshot test/rendro/adapters/pdfium_raster_snapshot_test.exs`
- any pdfium binary download
</verification>

<success_criteria>
Plan 86-01 is complete when the guardrail registry and tests prove the launch artifact PNG regeneration path is advisory-only, while required CI remains the existing deterministic `mix ci` lane.
</success_criteria>
