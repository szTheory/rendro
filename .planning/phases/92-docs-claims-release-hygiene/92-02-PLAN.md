---
phase: 92
plan: 02
type: execute
wave: 2
depends_on:
  - 92-01
files_modified:
  - mix.exs
  - guides/upgrading_to_1.0.md
  - CHANGELOG.md
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - test/docs_contract/launch_artifacts_claims_test.exs
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "`ADOPTION.md` is included in the Hex package and ExDoc extras so public README links resolve in package/docs contexts"
    - "`guides/upgrading_to_1.0.md` links to `CHANGELOG.md` in a way ExDoc can resolve"
    - "`ci.yml` and `release.yml` set top-level `permissions: contents: read` without changing required/advisory job semantics"
    - "Changelog records the docs/support/release hygiene posture without implying a published Hex release already contains it"
  artifacts:
    - path: "mix.exs"
      provides: "Hex package and ExDoc inclusion for public linked docs"
      contains: "ADOPTION.md"
    - path: ".github/workflows/ci.yml"
      provides: "Least-privilege CI token posture"
      contains: "permissions:"
    - path: ".github/workflows/release.yml"
      provides: "Least-privilege release token posture"
      contains: "contents: read"
    - path: "test/docs_contract/launch_artifacts_claims_test.exs"
      provides: "Hex tarball public-doc inclusion proof"
      contains: "ADOPTION.md"
    - path: "test/guardrails/required_checks_contract_test.exs"
      provides: "Workflow permission guardrails"
      contains: "contents"
  key_links:
    - from: "mix.exs"
      to: "test/docs_contract/launch_artifacts_claims_test.exs"
      via: "Hex package file inclusion"
      pattern: "ADOPTION.md"
    - from: ".github/workflows/ci.yml"
      to: "test/guardrails/required_checks_contract_test.exs"
      via: "YAML permissions parsing"
      pattern: "contents"
---

# Plan 92-02: Package, HexDocs, and Workflow Hygiene

<objective>
Fix package/docs inclusion for public linked docs and harden CI/release workflow token permissions without adding release automation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-docs-claims-release-hygiene/92-CONTEXT.md
@.planning/phases/92-docs-claims-release-hygiene/92-RESEARCH.md
@.planning/phases/92-docs-claims-release-hygiene/92-PATTERNS.md
@.planning/phases/92-docs-claims-release-hygiene/92-VALIDATION.md
</context>

<decisions>
- Include `ADOPTION.md` in the Hex package and ExDoc extras because packaged README/comparison docs link to it.
- Add read-only top-level workflow permissions to CI and release, matching the existing HexDocs workflow posture.
- Keep release-please, tag automation, and GitHub write permissions out of scope.
</decisions>

<threat_model>
| Threat | Risk | Mitigation |
|--------|------|------------|
| Public docs link to files excluded from Hex package/HexDocs | Medium | Include `ADOPTION.md` in `package.files` and ExDoc extras, and assert package contents. |
| Release hardening accidentally broadens automation | Medium | Only add read-only token permissions; do not add release-please, tag creation, or write permissions. |
| Docs warnings hide broken links | Medium | Run `mix docs` after link/package changes and fix narrow reference warnings in scope. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fix Hex package and ExDoc public-doc inclusion</name>
  <files>mix.exs, guides/upgrading_to_1.0.md, test/docs_contract/launch_artifacts_claims_test.exs</files>
  <read_first>
    - mix.exs
    - guides/upgrading_to_1.0.md
    - test/docs_contract/launch_artifacts_claims_test.exs
    - README.md
    - ADOPTION.md
  </read_first>
  <action>
    Add `ADOPTION.md` to Hex package files and ExDoc extras, grouped with public policy/reference docs. Fix the `guides/upgrading_to_1.0.md` changelog link so ExDoc can resolve it from the docs context. Extend package docs-contract tests so `ADOPTION.md` is expected in the Hex tarball.
  </action>
  <verify>
    <automated>mix test test/docs_contract/launch_artifacts_claims_test.exs</automated>
    <automated>rm -rf /tmp/rendro_hex_phase92 && mix hex.build --unpack /tmp/rendro_hex_phase92</automated>
    <automated>mix docs</automated>
  </verify>
  <done>README-linked public adoption docs are included in package/docs contexts and ExDoc no longer warns about the targeted missing ADOPTION/CHANGELOG links.</done>
</task>

<task type="auto">
  <name>Task 2: Add least-privilege workflow permissions and changelog entry</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, test/guardrails/required_checks_contract_test.exs, CHANGELOG.md</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/hexdocs.yml
    - .github/workflows/release.yml
    - test/guardrails/required_checks_contract_test.exs
    - CHANGELOG.md
  </read_first>
  <action>
    Add top-level `permissions: contents: read` to `.github/workflows/ci.yml` and `.github/workflows/release.yml`, preserving existing jobs, advisory separation, tag-only release trigger, and Hex publish behavior. Extend guardrail tests to parse CI/release/HexDocs workflows and require read-only contents permissions where applicable.

    Add a concise `[Unreleased]` changelog entry for docs/support-matrix updates, ADOPTION package inclusion, and read-only workflow permissions.
  </action>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs</automated>
  </verify>
  <done>DOC-02 workflow permissions are guarded and changelog documents the posture change.</done>
</task>

</tasks>

<verification>
1. `mix test test/docs_contract/launch_artifacts_claims_test.exs test/guardrails/required_checks_contract_test.exs`
2. `rm -rf /tmp/rendro_hex_phase92 && mix hex.build --unpack /tmp/rendro_hex_phase92`
3. `mix docs`
</verification>

<success_criteria>
- DOC-02: Package/docs and workflow guardrails prevent broken public docs links and keep CI/release token permissions least-privilege where practical.
</success_criteria>
