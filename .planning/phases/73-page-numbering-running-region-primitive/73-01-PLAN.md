---
phase: 73-page-numbering-running-region-primitive
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/rendro/deterministic_test.exs
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/pipeline/measure_test.exs
  - test/rendro/flow_test.exs
  - test/rendro_builders_test.exs
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "All Wave 0 test scaffolding exists and fails (RED) before any production code changes"
    - "`mix test` reports only the newly-added failing tests — existing suite remains green"
    - "Every requirement from PAGE-01..04 has at least one failing test stub covering it"
  artifacts:
    - path: "test/rendro/deterministic_test.exs"
      provides: "D-11 (a)-(d) describe block with four pending/failing assertions"
      contains: "running-region determinism (D-11)"
    - path: "test/rendro/pipeline/paginate_test.exs"
      provides: "fn-evaluation, suppression, flow_layout fallback test stubs"
      contains: "fn {pn, tp}"
    - path: "test/rendro/pipeline/measure_test.exs"
      provides: "body_capacity D-04 fix regression test stub"
      contains: "body_h - header_h - footer_h"
    - path: "test/rendro/flow_test.exs"
      provides: "suppress_on:first integration test stub; body-no-overlap integration test stub"
      contains: "suppress_on"
    - path: "test/rendro_builders_test.exs"
      provides: "page_number/1 builder test stub"
      contains: "page_number"
  key_links:
    - from: "test/rendro/deterministic_test.exs"
      to: "lib/rendro/pipeline/paginate.ex"
      via: "D-11(d) geometry guard — asserts replace_page_numbers/2 does not mutate run widths"
      pattern: "MeasuredText.*lines"
---

<objective>
Write all Wave 0 failing test stubs (RED state) before touching production code. Every new test file addition from 73-VALIDATION.md is written here as a pending or failing assertion. Existing tests continue to pass; only newly-inserted tests fail.

Purpose: Establishes the Nyquist sampling contract for Phase 73. Executors in subsequent waves have concrete test commands to run after each change, and the RED→GREEN cycle is verifiable.

Output: Five test files extended with failing/pending stubs covering PAGE-01..04. `mix test` exits non-zero (new stubs fail) but existing tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md
@.planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md
@.planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md
@.planning/phases/73-page-numbering-running-region-primitive/73-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write D-11 determinism test stubs and body_capacity/page_number builder stubs (RED)</name>

  <read_first>
    - test/rendro/deterministic_test.exs — read in full; copy the `describe "property: deterministic byte-identity"` block (lines 12–39) and embedded-font pipeline chain (lines 122–188) as structural analogs; note existing alias block at line 7
    - test/rendro_builders_test.exs — read in full; copy existing `text/2 builds a Text struct` pattern (lines 22–44) as the analog for the new `page_number/1` builder test; note existing alias block (lines 1–16)
    - test/rendro/pipeline/measure_test.exs — read lines 70–124 only; the existing `body_capacity == 540` test is the analog; note the describe block name and `assert_in_delta` usage
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"test/rendro/deterministic_test.exs" and §"test/rendro_builders_test.exs" excerpts
    - .planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md — D-11(a)-(d) descriptions
  </read_first>

  <behavior>
    - `deterministic_test.exs`: A new `describe "running-region determinism (D-11)"` block with four `test` cases asserting (a) byte-identical two-render, (b) body_capacity identical across 9-page vs 100-page, (c) page count + body-block assignment identical with `{{total_pages}}` vs static placeholder, (d) `replace_page_numbers` leaves MeasuredText geometry unchanged. All four tests use `flunk "not yet implemented"` or a minimal stub that will FAIL.
    - `rendro_builders_test.exs`: A new test `"page_number/1 builds a Block containing a Text with page-number tokens"` using `assert %Block{content: %Text{content: content}} = Rendro.page_number(); assert content =~ "{{page_number}}"; assert content =~ "{{total_pages}}"`. Fails because `Rendro.page_number/1` does not exist yet.
    - `measure_test.exs`: A new companion test in the existing `body_capacity` describe block asserting `layout.body_capacity == 504` for a template with `header_region.height: 0`, `footer_region.height: 36`, `body_region.height: 540`. Fails because current `body_capacity/1` ignores footer height.
  </behavior>

  <action>
    In `test/rendro/deterministic_test.exs`: append a `describe "running-region determinism (D-11)"` block after the last existing `describe` block. Test (a) name: `"(a) two deterministic renders with running footer are byte-identical"` — call a `running_footer_doc/1` helper (define as a `defp` at bottom) that builds a `Rendro.flow/2` document with `footer: [Rendro.block(Rendro.text("Page {{page_number}} of {{total_pages}}"))]` and 50 body lines; render twice with `deterministic: true`; assert `pdf1 == pdf2`. Test fails until PAGE-01 ships. Test (b) name: `"(b) body_capacity is identical for 9-page vs 100-page document"` — define helper `measure_body_capacity/1` that runs `Build.run/1 → Compose.run/1 → Measure.run/1` and returns `result.options.layout.body_capacity`; assert capacity for a 9-line doc equals capacity for a 500-line doc, both using a template with `footer: height 30`. Stub: `flunk "not yet implemented"` until PAGE-03 ships. Test (c) name: `"(c) page count and body-block assignment identical with total_pages vs static placeholder"` — build two docs differing only in footer text (`"{{total_pages}}"` vs `"999"`), run `Build → Compose → Measure → Paginate`, assert `length(token_pages) == length(static_pages)` and body-block-count-per-page lists are equal. Stub. Test (d) name: `"(d) replace_page_numbers does not change MeasuredText geometry"` — run full pipeline on a doc with a measured footer block; capture `block.content.lines` before substitution (extract from `paginated.pages`); apply `replace_page_numbers` call (or use post-paginated form); assert all per-run `width` values and block `height` unchanged. Stub. All four stubs use `flunk "not yet implemented"` so they FAIL on first run.

    In `test/rendro_builders_test.exs`: append one `test "page_number/1 builds a Block containing a Text with page-number tokens"` inside the existing `describe` block (or at module level if no describe block exists). Assert `%Block{content: %Text{content: content}} = Rendro.page_number()`, then `assert content =~ "{{page_number}}"` and `assert content =~ "{{total_pages}}"`. This test will raise `UndefinedFunctionError` until Plan 04 ships.

    In `test/rendro/pipeline/measure_test.exs`: append a second test inside the existing `body_capacity` describe block (after the `body_capacity == 540` test). Test name: `"subtracts header and footer region heights from body region height"`. Build a doc using a `%PageTemplate{}` with `footer: %Region{height: 36}`, `header: %Region{height: 0}`, `body: %Region{height: 540}`. Run `Compose.run/1 → Measure.run/1`. Assert `result.options.layout.body_capacity == 504`. Uses `assert_in_delta` consistent with line 123 pattern. This test FAILS until Plan 02 ships.
  </action>

  <verify>
    <automated>mix test test/rendro/deterministic_test.exs test/rendro_builders_test.exs test/rendro/pipeline/measure_test.exs 2>&1 | grep -E "(failure|error|flunk|UndefinedFunction)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/deterministic_test.exs` reports exactly 4 new failures (one per D-11 property) and all pre-existing tests pass
    - `mix test test/rendro_builders_test.exs` reports exactly 1 new failure (`page_number/1` undefined) and all pre-existing tests pass
    - `mix test test/rendro/pipeline/measure_test.exs` reports exactly 1 new failure (`body_capacity == 504` assertion fails with actual 540) and all pre-existing tests pass
    - No test file in the existing suite that does NOT include Wave 0 stubs breaks: `mix test --exclude phase73_wave0` is not needed — the new tests are additive
    - Each new test name matches exactly the strings specified in the action section (tooling reads test names for traceability)
  </acceptance_criteria>

  <done>Five test files contain all Wave 0 stubs. All new stubs fail. All pre-existing tests pass. `mix test` exits non-zero only because of the new stubs.</done>
</task>

<task type="tdd">
  <name>Task 2: Write fn-evaluation, suppression, overlap, and flow_layout test stubs in paginate_test and flow_test (RED)</name>

  <read_first>
    - test/rendro/pipeline/paginate_test.exs — read lines 1–144 in full; the `describe "run/1"` block with explicit pipeline chain (lines 69–144) is the structural analog for all new stubs; note alias block at lines 1–8
    - test/rendro/flow_test.exs — read lines 102–237 in full; `"headers, footers and page numbers"` (lines 102–122) is the analog for new integration tests; `"explicit page template"` (lines 124–237) is the analog for y-coordinate assertions
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"test/rendro/pipeline/paginate_test.exs" and §"test/rendro/flow_test.exs" excerpts
    - .planning/phases/73-page-numbering-running-region-primitive/73-VALIDATION.md — Wave 0 gaps list
  </read_first>

  <behavior>
    - `paginate_test.exs` gains 3 new test stubs: (i) `fn {pn, tp} -> content end` evaluation stub, (ii) suppressed-page-keeps-body_capacity stub, (iii) flow_layout fallback body_capacity stub.
    - `flow_test.exs` gains 2 new test stubs: (iv) `suppress_on: :first` integration stub, (v) body-does-not-overlap-footer stub.
    - All five stubs use `flunk "not yet implemented"` and FAIL on first run.
  </behavior>

  <action>
    In `test/rendro/pipeline/paginate_test.exs`: append three new `test` cases inside (or after) the existing `describe "run/1"` block.

    (i) Test name: `"evaluates fn {page_number, total_pages} block per page with correct arguments"`. Build a doc with a footer section whose content is a single block carrying a function `fn {pn, tp} -> [Rendro.block(Rendro.text("p#{pn}/#{tp}"))] end` (or use a `%Rendro.RunningContent{fun: ...}` wrapper — whichever the implementation chooses in Plan 04). Run `Build → Compose → Measure → Paginate`. For each page, find the footer block and assert its rendered text contains `"p<idx>/<total>"`. Stub with `flunk "not yet implemented"`.

    (ii) Test name: `"suppressed page retains same body_capacity as non-suppressed page"`. Build a multi-page doc with `suppress_on: :first` on the footer section. After paginating, capture `layout.body_capacity` (from `doc.options.layout`) and assert it equals a second doc's capacity without suppression. Stub with `flunk "not yet implemented"`.

    (iii) Test name: `"flow_layout/1 fallback subtracts footer height from body_capacity"`. Build a `%Rendro.Document{}` directly (no `Rendro.flow/2` call) with `footer: [Rendro.block(Rendro.text("footer"))]` and a template with footer `height: 30`. Call `Paginate.run(doc)` directly (skipping `Build/Compose/Measure`) to force the `flow_layout/1` branch. Assert the resulting `layout.body_capacity` equals `body_region_height - 30`. Because `flow_layout/1` is private, verify via the paginated output: ensure body blocks fit within `body_h - 30` total vertical space. Stub with `flunk "not yet implemented"`.

    In `test/rendro/flow_test.exs`: append two new `test` cases after the existing `"headers, footers and page numbers"` test.

    (iv) Test name: `"suppress_on: :first suppresses footer on first page only"`. Build a multi-page doc with a footer section using `suppress_on: :first`. Render via `Rendro.render/1`. Assert the rendered PDF bytes do NOT contain the footer text on page 1 (use `~r` search on the binary), but DO contain footer text on page 2. Stub with `flunk "not yet implemented"`.

    (v) Test name: `"body blocks do not overlap footer region (y + height <= footer.y)"`. Use the explicit-template pattern (lines 124–237) with a template having a non-zero footer height. Run `Build → Compose → Measure → Paginate`. For each page, find all body blocks and assert `block.y + block.height <= footer_region.y` for all of them. Stub with `flunk "not yet implemented"`.
  </action>

  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs 2>&1 | grep -E "(failure|error|flunk)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/pipeline/paginate_test.exs` reports exactly 3 new failures (stubs i, ii, iii) and all pre-existing tests pass
    - `mix test test/rendro/flow_test.exs` reports exactly 2 new failures (stubs iv, v) and all pre-existing tests pass
    - Test names match exactly as specified (executor in Plan 02–05 uses these names to confirm GREEN transitions)
    - `mix test` (full suite) exits non-zero only due to the 10 total Wave 0 stubs; no regressions in existing tests
  </acceptance_criteria>

  <done>All Wave 0 test stubs exist and are RED. Full suite exits non-zero only on the 10 new stubs. Existing 100% pass rate preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production code | Tests call production functions; no external input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new Hex dependencies confirmed by RESEARCH.md Package Legitimacy Audit |
| T-73-01 | Denial of Service | per-page fn evaluation (future, Plan 04) | mitigate | Function evaluation must be wrapped so a raising fn does not crash the whole render; reuse `maybe_validate_region_fit` error-surfacing pattern; plan explicitly encodes this in Plan 04 action |

No new external input surface. Token substitution operates on author-controlled strings only. No network I/O, filesystem access, deserialization, or authentication paths introduced.
</threat_model>

<verification>
Wave 1 complete when `mix test` exits non-zero ONLY due to the 10 new Wave 0 stubs. Zero regressions in previously passing tests.

Run: `mix test 2>&1 | tail -5`

Expected: "X tests, 10 failures" where all failures are the new Wave 0 stubs.
</verification>

<success_criteria>
- All 10 Wave 0 test stubs created and failing (RED state confirmed)
- Every requirement PAGE-01..04 has at least one failing stub
- Pre-existing suite 100% green
- `mix test` exit code 1 (non-zero from new failures only)
</success_criteria>

<output>
Create `.planning/phases/73-page-numbering-running-region-primitive/73-01-SUMMARY.md` when done
</output>
