---
phase: 73-page-numbering-running-region-primitive
plan: 03
type: execute
wave: 3
depends_on:
  - "73-02"
files_modified:
  - lib/rendro/pipeline/paginate.ex
autonomous: true
requirements:
  - PAGE-01

must_haves:
  truths:
    - "`{{total_pages}}` is substituted with the real page count on every page in a single pipeline pass"
    - "`total = length(pages)` is bound ONCE before the Enum.with_index(1) map — not inside the lambda"
    - "`replace_page_numbers/3` only rewrites `.text` strings; run.width, block.height, and all geometry fields are untouched"
    - "Existing `{{page_number}}` substitution continues to work as before"
  artifacts:
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Extended replace_page_numbers/3 (page_num, total); total threaded through apply_page_template/4; total bound before Enum.with_index(1)"
      contains: "total_pages"
  key_links:
    - from: "lib/rendro/pipeline/paginate.ex Enum.with_index(1) map (lines ~33-41)"
      to: "lib/rendro/pipeline/paginate.ex replace_page_numbers/3"
      via: "total bound once before map; passed through apply_page_template/4"
      pattern: "total = length"
---

<objective>
Extend `replace_page_numbers/2` to `replace_page_numbers/3` (adding `total` parameter) and substitute `{{total_pages}}` alongside `{{page_number}}`. Thread `total = length(pages)` from before the `Enum.with_index(1)` map through `apply_page_template/4`. This is the PAGE-01 single-pass implementation.

Purpose: Closes the `{{total_pages}}` gap. `total` is already known at this site (it is `length(pages)` of the already-paginated list) so no second render pass is needed. This is the ReportLab NumberedCanvas deferred-stamp model (D-10).

Output: `replace_page_numbers/3` in `paginate.ex`. The Wave 0 PAGE-01 integration stub in `flow_test.exs` turns GREEN. D-11(c) and D-11(d) stubs become partially verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md
@.planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md
@.planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend replace_page_numbers/2 → /3 and add {{total_pages}} substitution (D-01, D-10, PAGE-01)</name>

  <read_first>
    - lib/rendro/pipeline/paginate.ex — read lines 33–50 (Enum.with_index map), lines 397–455 (apply_page_template/3 and replace_page_numbers/2) in full; these are both modified in this task
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"lib/rendro/pipeline/paginate.ex — extend replace_page_numbers/2" — full current implementation verbatim (copy as baseline)
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"replace_page_numbers/2 — paginate.ex (ACTUAL: lines 414–455)" with extension pattern; §"Pitfall 2: Substituting run widths" (critical: never touch run.width); §"Wiring total through paginate_flow/1" code example
    - .planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md — D-10 (text-only substitution invariant)
  </read_first>

  <behavior>
    - `replace_page_numbers(blocks, 3, 10)` substitutes `"{{total_pages}}"` → `"10"` and `"{{page_number}}"` → `"3"` in both `Rendro.Text{content:}` and `Rendro.Pipeline.MeasuredText{source.content:}` and all per-run `.text` fields
    - No `run.width`, `block.height`, `block.width`, `measured.width`, or `measured.height` field is modified in any code path through `replace_page_numbers/3`
    - `replace_page_numbers(blocks, page_num, total)` called from `apply_page_template/4`; `total` is bound as `total = length(pages)` before the `Enum.with_index(1)` map in `paginate_flow/1`
    - A document footer containing `"Page {{page_number}} of {{total_pages}}"` rendered via `Rendro.render/1` produces bytes containing `"Page 1 of N"` on page 1, `"Page N of N"` on the last page
  </behavior>

  <action>
    Three coordinated changes inside `lib/rendro/pipeline/paginate.ex`. Make all three in one edit pass to keep the file consistent.

    Change 1 — bind total and thread to `apply_page_template`: In the `paginate_flow/1` function, locate the `pages |> Enum.reverse() |> Enum.with_index(1) |> Enum.map(...)` block (lines 33–41). Insert `total = length(pages)` immediately after `pages = pages |> Enum.reverse()` and before the `Enum.with_index(1)` call. In the `Enum.map` lambda, change `apply_page_template(idx, layout)` to `apply_page_template(idx, layout, total)`. (The `pages` variable at this point is the reversed finalized list — `length/1` on it gives the correct total.)

    Change 2 — add `total` parameter to `apply_page_template/3 → /4`: Change the function signature to `defp apply_page_template(%Page{} = page, idx, layout, total)`. Inside, change `replace_page_numbers(idx)` to `replace_page_numbers(idx, total)`. All other logic in `apply_page_template` remains identical. (The suppression insertion point for Plan 04 goes between `Map.get(region.name, [])` and `replace_page_numbers` — leave a comment `# [Plan 04: fn evaluation and suppression inserted here]` as a guide.)

    Change 3 — extend `replace_page_numbers/2 → /3`: Change function signature to `defp replace_page_numbers(blocks, page_num, total)`. In the `Rendro.Text` branch: chain a second `String.replace(text, "{{total_pages}}", Integer.to_string(total))` immediately after the existing `page_number` replace — use a pipe chain or intermediate binding (not a nested call). In the `Rendro.Pipeline.MeasuredText` branch: chain the same second `String.replace` on `replaced` (for `source.content`), and in the per-run `Enum.map`, chain a second `String.replace(run.text, "{{total_pages}}", Integer.to_string(total))` after the existing `page_number` replace. CRITICAL: only the `run.text` string field is touched — `run.width` and all geometry fields are NOT modified. The `_ -> block` fallback clause is unchanged.
  </action>

  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/flow_test.exs` exits 0 — the existing `"headers, footers and page numbers"` test passes (no regression); the new PAGE-01 integration test covering `{{total_pages}}` is GREEN (if one was added in Plan 01 Task 1 that only tests token substitution, not fn/suppression)
    - `mix test test/rendro/pipeline/paginate_test.exs` exits with the Wave 0 fn/suppression/flow_layout stubs still RED but no new failures
    - `grep -c "{{total_pages}}" lib/rendro/pipeline/paginate.ex` returns 4 or more (two String.replace calls in Text branch, two in MeasuredText branch)
    - `grep -v "total_pages\|page_number" lib/rendro/pipeline/paginate.ex | grep "run\.width\|block\.height\|block\.width\|measured\.width\|measured\.height"` returns 0 results in the `replace_page_numbers` function body (D-10 geometry guard)
    - `mix compile` exits 0
    - `mix test` full suite: only Wave 0 fn/suppression/D-11 stubs remain RED; no regressions
  </acceptance_criteria>

  <done>`replace_page_numbers/3` substitutes both tokens. total bound once before Enum.with_index. run.width untouched. PAGE-01 integration test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-controlled token strings → String.replace | No external input; author writes footer content at document definition time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
| T-73-03 | Tampering | run.width modification after substitution | mitigate | D-10 explicitly forbids touching geometry fields; acceptance criteria include a grep gate asserting no geometry field writes inside replace_page_numbers; D-11(d) test in Plan 05 is a regression guard |

No new external input surface. `String.replace` on author-controlled strings — no injection risk.
</threat_model>

<verification>
`mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs`

Expected: PAGE-01 related tests GREEN; Wave 0 fn/suppression stubs still RED; no regressions.

`mix test` (full suite): only Wave 0 fn/suppression/D-11 stubs remain RED.
</verification>

<success_criteria>
- `replace_page_numbers/3` substitutes `{{page_number}}` and `{{total_pages}}` in both Text and MeasuredText variants
- `total = length(pages)` bound once before map — O(1) per page, not O(N)
- No geometry fields modified — verified by grep gate in acceptance_criteria
- PAGE-01 integration flow test GREEN
</success_criteria>

<output>
Create `.planning/phases/73-page-numbering-running-region-primitive/73-03-SUMMARY.md` when done
</output>
