---
phase: 73-page-numbering-running-region-primitive
plan: 04
type: execute
wave: 4
depends_on:
  - "73-03"
files_modified:
  - lib/rendro/running_content.ex
  - lib/rendro/section.ex
  - lib/rendro.ex
  - lib/rendro/pipeline/paginate.ex
autonomous: true
requirements:
  - PAGE-02

must_haves:
  truths:
    - "A running region block can carry a `fn {page_number, total_pages} -> [Block.t()] end` function — the primitive per D-01/D-03"
    - "`Rendro.page_number/1` helper builds a Block whose Text content contains `{{page_number}}` and `{{total_pages}}` tokens (sugar per D-01)"
    - "`suppress_on: :first` on a Section suppresses footer rendering on page 1 but NOT on page 2+"
    - "A suppressed page has the same `body_capacity` as a non-suppressed page — suppression never reclaims height (D-08)"
    - "Suppression selector is resolved before function evaluation; `suppress_on: :first` takes precedence over any content function"
    - "A function block raising an exception is caught and surfaced as a structured error, not a crash"
  artifacts:
    - path: "lib/rendro/running_content.ex"
      provides: "Rendro.RunningContent struct with enforce_keys [:fun]; @type t with fun field typespec"
      contains: "RunningContent"
    - path: "lib/rendro/section.ex"
      provides: "suppress_on field added to defstruct and @type t; content typespec widened"
      contains: "suppress_on"
    - path: "lib/rendro.ex"
      provides: "page_number/1 helper returning Block with Text token content"
      contains: "page_number"
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "evaluate_fn_block/3 and apply_suppression/3 helpers; apply_page_template/4 wired to call both before replace_page_numbers/3"
      contains: "evaluate_fn_block"
  key_links:
    - from: "lib/rendro.ex page_number/1"
      to: "lib/rendro/pipeline/paginate.ex replace_page_numbers/3"
      via: "page_number/1 produces a Block with Text{content: 'Page {{page_number}} of {{total_pages}}'}; replace_page_numbers/3 substitutes tokens at render time"
      pattern: "page_number.*total_pages"
    - from: "lib/rendro/section.ex suppress_on"
      to: "lib/rendro/pipeline/paginate.ex apply_page_template/4"
      via: "layout.region_blocks values carry section suppress_on metadata; apply_page_template reads it per page"
      pattern: "suppress_on"
    - from: "lib/rendro/running_content.ex"
      to: "lib/rendro/pipeline/paginate.ex evaluate_fn_block/3"
      via: "pattern match on %Rendro.RunningContent{fun: fun} in evaluate_fn_block"
      pattern: "RunningContent"
---

<objective>
Implement the per-page function primitive, the `page_number/1` helper, and the `suppress_on` declarative selector. This completes the PAGE-02 authoring surface and turns the Wave 0 fn-evaluation and suppression stubs GREEN.

Purpose: These are the public API pieces that recipes 74–76 inherit. The `fn {pn, tp} -> content end` primitive, `page_number/1` sugar, and `suppress_on: :first` selector are all that Phase 73 needs to expose for downstream consumption.

Output: `Rendro.RunningContent` struct, `Section.suppress_on` field, `Rendro.page_number/1` helper, `evaluate_fn_block/3` and suppression logic in paginate.ex. All PAGE-02 Wave 0 stubs GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md
@.planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md
@.planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RunningContent struct, widen Section, add page_number/1 helper (D-01, D-03, D-07)</name>

  <read_first>
    - lib/rendro/section.ex — read in full (20 lines); this is the target for suppress_on and content typespec widening
    - lib/rendro/region.ex — read in full (lines 1–27); copy the `@enforce_keys []` + `defstruct` + `@type role` + `@type t` pattern for the new RunningContent struct
    - lib/rendro.ex — read lines 195–230 only; `region/1` and `section/1` at ~199–207, `block/2` and `text/2` at ~209–220 are the pattern analogs for `page_number/1`; note aliases already in scope (Block, Text, Section, Region at lines ~15–22)
    - lib/rendro/block.ex — read lines 1–35; confirms `content: term()` catch-all in @type t; confirms `@enforce_keys` state
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"lib/rendro/section.ex", §"lib/rendro/block.ex", §"lib/rendro.ex — add page_number/1 helper"
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"Rendro.Section struct", §"Open Question 1 (RunningContent wrapper vs bare fn)" — use option (b) dedicated struct; §"Open Question 2 (suppress_on field placement)" — top-level field
  </read_first>

  <behavior>
    - `Rendro.page_number()` returns `%Rendro.Block{content: %Rendro.Text{content: "Page {{page_number}} of {{total_pages}}", font: "Helvetica", size: 12}}`; the default format is `"Page {{page_number}} of {{total_pages}}"`
    - `Rendro.page_number(format: "{{page_number}}/{{total_pages}}")` returns a block with the custom format string
    - `%Rendro.RunningContent{fun: fn {pn, tp} -> [Rendro.block(Rendro.text("#{pn}/#{tp}"))] end}` is valid and `@enforce_keys [:fun]` prevents creation without a fun
    - `Rendro.section(region: :footer, content: [...], suppress_on: :first)` creates a `%Rendro.Section{suppress_on: :first}` without error
    - `Rendro.section(region: :footer, content: [...])` creates a `%Rendro.Section{suppress_on: nil}` (default nil — no suppression)
    - `@type suppress_on :: nil | :first | {:pages, [pos_integer()]}` covers the three specified selector shapes (D-07)
  </behavior>

  <action>
    Step 1 — Create `lib/rendro/running_content.ex`. Follow the `lib/rendro/region.ex` struct pattern exactly: `defmodule Rendro.RunningContent`, `@moduledoc "..."`, `@enforce_keys [:fun]`, `defstruct fun: nil`, `@type t :: %__MODULE__{fun: (({pos_integer(), pos_integer()} -> [Rendro.Block.t()]))}`. The `fun` field holds a function of arity 1 accepting a 2-tuple `{page_number, total_pages}` and returning a list of `Rendro.Block.t()` (or `nil`/`[]` for suppression via function). `@enforce_keys [:fun]` prevents accidental struct creation without a function.

    Step 2 — Modify `lib/rendro/section.ex`. Add `suppress_on: nil` to `defstruct` (after `content: []`, before `page_template: nil`). Add `@type suppress_on :: nil | :first | {:pages, [pos_integer()]}` before `@type t`. Widen `content:` in `@type t` to `[Rendro.Block.t() | Rendro.RunningContent.t()]`. Add `suppress_on: suppress_on()` to `@type t`. Do NOT add `@enforce_keys` entry — `suppress_on: nil` is the correct default.

    Step 3 — Add `page_number/1` to `lib/rendro.ex`. Insert after `section/1` at approximately line 207, before `block/2`. Use the `block/2` + `text/2` builder idiom: `@spec page_number(keyword()) :: Block.t()`. Body: read `format = Keyword.get(opts, :format, "Page {{page_number}} of {{total_pages}}")` and `text_opts = Keyword.drop(opts, [:format])`, then call `block(text(format, text_opts))`. The `Text` and `Block` aliases are already in scope. This produces a `%Block{content: %Text{content: "<format>", ...}}` — the token strings are sugar that lowers to `replace_page_numbers/3` substitution at render time (D-01).

    No changes to `lib/rendro/block.ex` required — the `| term()` catch-all already accommodates `%RunningContent{}`.
  </action>

  <verify>
    <automated>mix test test/rendro_builders_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro_builders_test.exs` exits 0 — the Wave 0 `page_number/1` builder stub is GREEN
    - `%Rendro.RunningContent{fun: fn {_, _} -> [] end}` in IEx compiles without error; `%Rendro.RunningContent{}` raises `KeyError` (enforce_keys [:fun] working)
    - `Rendro.section(region: :footer, content: [], suppress_on: :first)` compiles without error in IEx; `suppress_on: :first` stored on the struct
    - `mix compile` exits 0 with no warnings
    - `grep -n "suppress_on" lib/rendro/section.ex` shows both the defstruct field and @type suppress_on entries
    - `grep -n "page_number" lib/rendro.ex` shows the new `def page_number` and `@spec page_number`
  </acceptance_criteria>

  <done>RunningContent struct created. Section has suppress_on field. page_number/1 helper exists in Rendro. Builders test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fn evaluation and suppression in paginate.ex apply_page_template/4 (D-02, D-07, D-08)</name>

  <read_first>
    - lib/rendro/pipeline/paginate.ex — read lines 397–415 (apply_page_template/4 as modified in Plan 03) and lines 590–605 (maybe_validate_region_fit/5); note the comment `# [Plan 04: fn evaluation and suppression inserted here]` left by Plan 03
    - lib/rendro/pipeline/paginate.ex — read lines 679–685 (bounded_region?/1) to understand the geometry-check pattern reused for suppression
    - lib/rendro/running_content.ex — just created in Task 1; confirms struct fields
    - lib/rendro/section.ex — just modified in Task 1; confirms suppress_on field
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"Suppression insertion point" in apply_page_template; §"maybe_validate_region_fit/5 reuse"
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"Pattern 1: Extending replace_page_numbers/2 for total_pages and fn evaluation" (evaluate_fn_block idiom); §"Pitfall 4: Suppression reclaims height" (iron rule — D-08); §"Anti-Patterns to Avoid"
    - .planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md — D-07 (selector precedence), D-08 (suppression never reclaims height)
  </read_first>

  <behavior>
    - A footer section with `suppress_on: :first` on page 1: `apply_page_template/4` produces EMPTY anchored blocks for the footer (nothing rendered), but the region's geometry is still passed to `maybe_validate_region_fit` with the region's declared height — suppression is purely visual
    - A footer section with `suppress_on: :first` on page 2+: function is evaluated and blocks are rendered normally
    - A footer section carrying a `%Rendro.RunningContent{fun: fun}` block: `fun.({page_num, total})` is called; result replaces the block's position in the anchored block list; if fun returns `nil` or `[]`, the block is omitted (visual suppression via function — not a selector path)
    - A function that raises an exception: the error is caught and surfaced as `{:error, {:running_content_error, reason, page_num}}` — the render DOES NOT crash; reuse `maybe_validate_region_fit` error surfacing pattern
    - Suppression selector precedence: `suppress_on: :first` is resolved BEFORE function evaluation — a suppressed page never calls the content function
    - `body_capacity` is NOT recomputed during suppression — confirmed by the scalar already frozen in layout from Plan 02
  </behavior>

  <action>
    In `lib/rendro/pipeline/paginate.ex`, make the following changes:

    Change 1 — Add private helper `evaluate_fn_blocks/3`. Signature: `defp evaluate_fn_blocks(blocks, page_num, total)`. Uses `Enum.flat_map` to iterate blocks. For each block: pattern match on `%Rendro.Block{content: %Rendro.RunningContent{fun: fun}}` → call `fun.({page_num, total})` inside a `try/rescue` block; on success, return the evaluated blocks (list); on raise, return `[{:error, {:running_content_error, inspect(reason), page_num}}]` (surface error without crashing). For all other block shapes: return `[block]` (pass-through). After `Enum.flat_map`, filter out error tuples and log them (or raise structured error — follow the existing `validate_region_fit!/5` pattern for raising a descriptive `Rendro.Error` instead of returning a bare tuple; look at error.ex for the correct struct pattern before writing).

    Change 2 — Add private helper `apply_suppression/3`. Signature: `defp apply_suppression(blocks, section_suppress_on, page_idx)`. Pattern match on `suppress_on`:
    - `nil` → return `blocks` unchanged
    - `:first` → if `page_idx == 1`, return `[]`; else return `blocks`
    - `{:pages, page_list}` when `is_list(page_list)` → if `page_idx in page_list`, return `[]`; else return `blocks`
    - `_` → return `blocks` (unknown selector shape — pass-through, no crash)
    This function returns an empty list for suppressed pages (visual suppression only — geometry lives on the region struct, not the block list).

    Change 3 — Wire both helpers into `apply_page_template/4`. At the comment left by Plan 03 (`# [Plan 04: fn evaluation and suppression inserted here]`), between `Map.get(region.name, [])` and `replace_page_numbers(idx, total)`, insert the pipeline:
    1. `|> apply_suppression(region_suppress_on(layout, region.name), idx)` — gets the suppress_on value from layout.region_blocks metadata or from the section stored in layout (see note below)
    2. `|> evaluate_fn_blocks(idx, total)`
    Then `|> replace_page_numbers(idx, total)` as before.

    For `region_suppress_on/2`: sections' `suppress_on` values must be accessible at the paginate stage. The cleanest approach is: in `Build.run/1` (if sections are stored on the document) or in `Compose.run/1`, carry the `suppress_on` value onto the region or its block list metadata. However, since `layout.region_blocks` is a map of `region_name => [Block.t()]`, the suppress_on cannot be inferred from blocks alone. The recommended approach (Claude's Discretion): add a `region_suppress_on` key to the layout map in `Compose.run/1` — a `%{region_name => suppress_on_value}` map built from the sections. Then `apply_page_template/4` reads `Map.get(layout.region_suppress_on || %{}, region.name)`. Implement the `region_suppress_on` map in `compose.ex` alongside `region_blocks`.

    Note: this requires reading `lib/rendro/pipeline/compose.ex` lines 80–100 before implementing. The `region_blocks` is built from sections in compose.ex. Add a parallel `region_suppress_on` map using the same `Enum.reduce` or `Map.new` pattern.

    The `maybe_validate_region_fit/5` call remains in place after the pipeline — it uses the region struct's declared `height` (D-08), not the block list length.
  </action>

  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0 — Wave 0 stubs (i) fn-evaluation and (ii) suppression-body_capacity and (iii) flow_layout fallback are all GREEN
    - `mix test test/rendro/flow_test.exs` exits 0 — Wave 0 stub (iv) suppress_on:first integration test and (v) body-no-overlap integration test are GREEN
    - A manually-constructed `%Rendro.RunningContent{fun: fn _ -> raise "boom" end}` block in a doc does NOT crash `Rendro.render/1` — it returns `{:error, _}` (verified by a targeted test or IEx call)
    - `grep -n "suppress_on" lib/rendro/pipeline/paginate.ex` shows the new helper and its call site in apply_page_template
    - `mix compile` exits 0
    - `mix test` (full suite) exits non-zero ONLY due to the 4 D-11 stubs in deterministic_test.exs; no other failures
  </acceptance_criteria>

  <done>fn evaluation and suppression wired. All PAGE-02 Wave 0 stubs GREEN. Only D-11 determinism stubs remain RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-supplied per-page function → paginate stage | The fn is author-controlled; however a buggy or malicious fn could raise, loop, or return unexpected shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No new Hex dependencies |
| T-73-04 | Denial of Service | per-page fn raises exception or infinite loop | mitigate | `evaluate_fn_blocks/3` wraps each fn call in `try/rescue`; raises are caught and surfaced as structured `Rendro.Error` (reuse existing error module); infinite loops are NOT defended against (Elixir has no timeout primitive without Task) — document this limitation in the module doc: "functions must be pure and terminating"; Task-based timeout is a future ergonomic |
| T-73-05 | Tampering | suppress_on unknown selector shape | accept | `apply_suppression/3` has a pass-through `_` clause — unknown selectors silently render all blocks; this is intentional forward-compat behavior; documented in typespec as an open type |

Token substitution and fn evaluation operate on author-controlled content only. No network I/O, filesystem, or deserialization surface introduced.
</threat_model>

<verification>
`mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs test/rendro_builders_test.exs`

Expected: exits 0. All PAGE-02 and PAGE-01 related stubs GREEN.

`mix test` (full suite): only the 4 D-11 determinism stubs remain RED.
</verification>

<success_criteria>
- `Rendro.RunningContent` struct exists with `@enforce_keys [:fun]`
- `Rendro.Section` has `suppress_on` field with `@type suppress_on :: nil | :first | {:pages, [pos_integer()]}`
- `Rendro.page_number/1` helper produces Block with Text containing `{{page_number}}` and `{{total_pages}}`
- fn evaluation happens per-page with correct `{page_num, total}` arguments
- Suppression hides rendering but does NOT reduce body_capacity (D-08 iron rule)
- Selector precedence: suppress_on checked before fn called
- Raising fn surfaced as structured error — render does not crash
- All PAGE-02 Wave 0 stubs GREEN
</success_criteria>

<output>
Create `.planning/phases/73-page-numbering-running-region-primitive/73-04-SUMMARY.md` when done
</output>
