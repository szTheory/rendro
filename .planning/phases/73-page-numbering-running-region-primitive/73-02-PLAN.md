---
phase: 73-page-numbering-running-region-primitive
plan: 02
type: execute
wave: 2
depends_on:
  - "73-01"
files_modified:
  - lib/rendro/pipeline/measure.ex
  - lib/rendro/pipeline/paginate.ex
autonomous: true
requirements:
  - PAGE-03

must_haves:
  truths:
    - "`body_capacity` equals `body_h - header_h - footer_h` at both fix sites (measure.ex and paginate.ex)"
    - "Default template (header_height: 0, footer_height: 0) produces identical body_capacity as before — no regression"
    - "A template with non-zero footer height correctly reduces body_capacity so body blocks never overlap the footer"
  artifacts:
    - path: "lib/rendro/pipeline/measure.ex"
      provides: "Fixed body_capacity/1 — three-key destructure subtracting header + footer heights"
      contains: "header_h"
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Fixed flow_layout/1 — derives header/footer heights from template.regions list"
      contains: "flow_layout"
  key_links:
    - from: "lib/rendro/pipeline/measure.ex"
      to: "lib/rendro/pipeline/compose.ex"
      via: "layout map — header_region/footer_region keys populated by Compose at compose.ex:94-95"
      pattern: "header_region.*footer_region"
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "lib/rendro/page_template.ex"
      via: "flow_layout/1 reads template.regions to find :header/:footer heights"
      pattern: "template\\.regions"
---

<objective>
Fix `body_capacity` at both verified fix sites so non-body region heights are subtracted from the body height. This closes the PAGE-03 prerequisite overlap bug and turns the Wave 0 `body_capacity == 504` stub GREEN.

Purpose: Without this fix, recipes with non-zero footer/header heights produce documents where body content overlaps the running footer. This is a hard exit criterion for Phase 73 (STATE.md "Blockers/Concerns").

Output: `measure.ex:442` and `paginate.ex:flow_layout/1` both implement `body_h - header_h - footer_h`. Existing body_capacity tests still green. New `body_capacity == 504` test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md
@.planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md
@.planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix body_capacity/1 in measure.ex (D-04, primary Compose-path site)</name>

  <read_first>
    - lib/rendro/pipeline/measure.ex — read lines 396–450 in full; the two-clause `body_capacity/1` at lines 442–443 is the target; `measure_layout/2` at lines 396–418 shows how body_capacity feeds the `<= 0` guard
    - lib/rendro/pipeline/compose.ex — read lines 90–100 only; confirms `header_region` and `footer_region` keys exist on the layout map passed to `body_capacity/1`
    - lib/rendro/region.ex — read lines 1–27; confirms `height` field defaults to `nil`
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"lib/rendro/pipeline/measure.ex" for fix pattern and nil-region guard idiom
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"body_capacity/1 — measure.ex (ACTUAL: lines 442–443)" and fix code pattern
  </read_first>

  <behavior>
    - `body_capacity/1` with a layout that has `body_region.height: 540`, `header_region: nil`, `footer_region: %Region{height: 36}` returns `504.0`
    - `body_capacity/1` with a layout that has `body_region.height: 697.89`, `header_region: %Region{height: 0}`, `footer_region: %Region{height: 0}` returns `697.89` (zero regression)
    - `body_capacity/1` with `header_region: nil`, `footer_region: nil` returns `body_region.height` unchanged (default template regression)
    - `body_capacity(_layout), do: 0` fallback clause unchanged
  </behavior>

  <action>
    Replace the two-clause `body_capacity/1` at measure.ex:442–443 with a new implementation. The replacement has TWO clauses:

    Clause 1: pattern-match on `%{body_region: %Region{height: body_h}, header_region: header_region, footer_region: footer_region}` with a guard `when is_number(body_h)`. Inside the body: `header_h = if header_region, do: header_region.height || 0, else: 0` and `footer_h = if footer_region, do: footer_region.height || 0, else: 0`, then return `body_h - header_h - footer_h`. This is the idiomatic nil-region guard from PATTERNS.md §"if region, do: region.height || 0, else: 0".

    Clause 2: fallback `defp body_capacity(_layout), do: 0` — unchanged from current.

    Do NOT touch any other lines in `measure.ex`. Do NOT touch `measure_layout/2`, `measure_region_blocks/2`, or any other function. The `Region` alias is already in scope at the top of the file (verify before editing).
  </action>

  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/pipeline/measure_test.exs` exits 0 — all tests including the new Wave 0 `body_capacity == 504` stub are now GREEN
    - The existing `body_capacity == 540` test (line ~80 of measure_test.exs) also stays green — confirming zero regression on the default-template path
    - `grep -n "body_capacity" lib/rendro/pipeline/measure.ex` shows the replacement at the correct line; no other `body_capacity` occurrences in measure.ex are changed
    - `mix compile` exits 0 (no typespec warnings)
  </acceptance_criteria>

  <done>measure.ex body_capacity/1 returns body_h - header_h - footer_h. measure_test.exs fully green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix flow_layout/1 body_capacity in paginate.ex (D-04, fallback no-Compose site)</name>

  <read_first>
    - lib/rendro/pipeline/paginate.ex — read lines 478–510 in full; `flow_layout/1` at lines 478–501 is the target; the `body_capacity: body_region.height` line (approx 494) is the bug
    - lib/rendro/page_template.ex — read lines 18–46; confirms default `%PageTemplate{}` header and footer regions both have `height: 0`; confirms `template.regions` is a list of `%Region{}` structs
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"lib/rendro/pipeline/paginate.ex — fix flow_layout/1 body_capacity"; §"bounded_region?/1" guard at line 679 for `is_number(h) and h > 0` idiom
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"flow_layout/1 fallback — paginate.ex (ACTUAL: lines 478–501)" and §"Pitfall 1: Two body_capacity sites"
  </read_first>

  <behavior>
    - `flow_layout/1` with a `%PageTemplate{}` whose `:footer` region has `height: 30` produces a map where `body_capacity == body_region.height - 30`
    - `flow_layout/1` with the default `%PageTemplate{}` (footer `height: 0`, header `height: 0`) produces `body_capacity == body_region.height` (no regression)
    - `flow_layout/1` where `template.regions` has no `:header` entry produces `body_capacity == body_region.height - footer_h` (missing region treated as 0 height)
  </behavior>

  <action>
    In `paginate.ex`, find the `flow_layout/1` function body (lines 478–501). Locate the line `body_capacity: body_region.height`. Replace it with a computed value derived from the template's regions.

    Before the `%{...}` map literal, bind two local variables using `Enum.find(template.regions, &(&1.name == :header))` and `Enum.find(template.regions, &(&1.name == :footer))` (these return `%Region{}` or `nil`). Apply the same nil-region guard as in Task 1: `if region, do: region.height || 0, else: 0`. Then compute `body_capacity: body_region.height - header_h - footer_h`.

    This approach reads from `template.regions` (NOT from a `layout.header_region` key — that key does not exist on the fallback path). The `bounded_region?/1` guard idiom from line 679 (`is_number(h) and h > 0`) can be used as an alternative guard to `|| 0` if cleaner. Whichever guard is used must be consistent with the measure.ex fix.

    Do NOT change the `region_blocks` map, the body_region calculation, or the first `flow_layout/1` clause (`defp flow_layout(%Document{options: %{layout: layout}}), do: layout`). Do NOT call `body_capacity/1` from this site — inline the computation directly since `body_capacity/1` is private to `measure.ex`.
  </action>

  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/pipeline/measure_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/pipeline/paginate_test.exs` exits 0 — the Wave 0 flow_layout fallback stub (stub iii from Plan 01) is now GREEN; all pre-existing paginate tests pass
    - `mix test test/rendro/pipeline/measure_test.exs` exits 0 — no regression from paginate.ex change
    - `grep -n "body_capacity" lib/rendro/pipeline/paginate.ex` shows the new computed value in `flow_layout/1` and the `replace_page_numbers` call site remains unchanged (still uses `idx` only at this stage — total threading happens in Plan 03)
    - `mix compile` exits 0
    - `mix test` (full suite) exits non-zero ONLY due to remaining Wave 0 stubs (stubs i, ii, iv, v from Plan 01 + D-11 stubs); no new failures
  </acceptance_criteria>

  <done>Both body_capacity fix sites implemented. PAGE-03 Wave 0 stubs GREEN. Full suite clean of regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored template geometry → body_capacity computation | Author-controlled Region.height values; no external input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero new Hex dependencies |
| T-73-02 | Elevation of Privilege | negative body_capacity from oversized footer | accept | Existing `{:error, :no_body_capacity}` guard in measure_layout/2 (line ~403) already catches this and returns a structured error; no change needed; document the invariant in code comment |

No new external input surface introduced by this plan.
</threat_model>

<verification>
After both tasks:

`mix test test/rendro/pipeline/measure_test.exs test/rendro/pipeline/paginate_test.exs`

Expected: exits 0. The Wave 0 PAGE-03 stubs (body_capacity == 504 in measure_test; flow_layout fallback in paginate_test) are GREEN. Suppression, fn-eval, and D-11 stubs still RED (implementation in later plans).
</verification>

<success_criteria>
- `measure.ex body_capacity/1` returns `body_h - header_h - footer_h`; default-template value unchanged
- `paginate.ex flow_layout/1` computes `body_capacity` by subtracting header/footer heights from template.regions
- Both fix sites changed in same commit (Pitfall 1 prevention)
- PAGE-03 Wave 0 stubs GREEN; no regressions in pre-existing suite
</success_criteria>

<output>
Create `.planning/phases/73-page-numbering-running-region-primitive/73-02-SUMMARY.md` when done
</output>
