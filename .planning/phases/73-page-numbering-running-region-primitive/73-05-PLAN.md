---
phase: 73-page-numbering-running-region-primitive
plan: 05
type: tdd
wave: 5
depends_on:
  - "73-04"
files_modified:
  - test/rendro/deterministic_test.exs
autonomous: true
requirements:
  - PAGE-04

must_haves:
  truths:
    - "Two deterministic renders of the same running-footer document produce byte-identical PDF bytes"
    - "`body_capacity` is geometry-only: identical for a 9-page vs 100+-page document with the same non-zero footer height"
    - "Page count and per-page body-block assignment are identical whether the footer contains `{{total_pages}}` or a static wide placeholder"
    - "`replace_page_numbers/3` leaves `MeasuredText.lines` run widths and block height unchanged before vs. after substitution"
    - "Full `mix test` suite exits 0 — Phase 73 complete"
  artifacts:
    - path: "test/rendro/deterministic_test.exs"
      provides: "Four passing D-11 property/unit tests in 'running-region determinism (D-11)' describe block"
      contains: "running-region determinism (D-11)"
  key_links:
    - from: "test/rendro/deterministic_test.exs D-11(d)"
      to: "lib/rendro/pipeline/paginate.ex replace_page_numbers/3"
      via: "Asserts MeasuredText run widths unchanged after substitution — regression guard against future re-measure-on-substitute"
      pattern: "MeasuredText.*lines.*width"
---

<objective>
Implement the four D-11 determinism assertions (stubs from Plan 01) against the now-complete production code. Turn all four stubs GREEN and confirm `mix test` (full suite) exits 0.

Purpose: PAGE-04 is the determinism contract proof. It is the machine-verifiable evidence that the single-pass model holds, geometry is frozen post-measure, and two renders with `deterministic: true` are byte-identical. Without this plan, the implementation is unproven on the four properties that distinguish Rendro from non-deterministic PDF libraries.

Output: Four passing tests in `deterministic_test.exs`. Full suite green. Phase 73 implementation complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md
@.planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md
@.planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Implement D-11(a) and D-11(b) determinism assertions (byte-identity and body_capacity invariance)</name>

  <read_first>
    - test/rendro/deterministic_test.exs — read in full; locate the `describe "running-region determinism (D-11)"` block added in Plan 01; read the stub implementations of tests (a) and (b); also read the existing `describe "property: deterministic byte-identity"` (lines 12–39) and helper `defp simple_doc` (lines 190–195) as structural analogs
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"D-11 Determinism Test Skeleton" for the full reference implementation of tests (a) and (b)
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"test/rendro/deterministic_test.exs" full excerpt; §"Shared Patterns: Build → Compose → Measure → Paginate pipeline chain"
    - .planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md — D-09 (body_capacity is geometry-only), D-10 (substitution is text-only), D-11(a) and D-11(b) exact assertions
  </read_first>

  <behavior>
    - D-11(a): `Rendro.render(doc, deterministic: true)` called twice on the same running-footer document returns `pdf1 == pdf2` (byte-identical binaries). The footer must contain `{{page_number}}` and `{{total_pages}}` to exercise the new substitution path.
    - D-11(b): A helper `measure_body_capacity/1` that runs `Build.run/1 → Compose.run/1 → Measure.run/1` and returns `result.options.layout.body_capacity`. For a template with `footer_height: 30`, `measure_body_capacity` is identical for a 9-line body doc and a 500-line body doc (or any doc long enough to paginate to 100+ pages). This directly asserts that `body_capacity` is a pure function of declared geometry, not of page count or content volume.
  </behavior>

  <action>
    Replace the `flunk "not yet implemented"` stubs in tests (a) and (b) with real assertions.

    Test (a) — `"(a) two deterministic renders with running footer are byte-identical"`: Replace the stub body with: call `running_footer_doc("Page {{page_number}} of {{total_pages}}")` (defined as a `defp` at the bottom of the file per Plan 01 Task 1); call `Rendro.render(doc, deterministic: true)` twice, binding `{:ok, pdf1}` and `{:ok, pdf2}`; `assert pdf1 == pdf2`. The `running_footer_doc/1` helper must build a `Rendro.flow/2` document with `footer: [Rendro.block(Rendro.text(footer_text))]` and sufficient body content to produce at least 2 pages (use 50–100 `Rendro.block(Rendro.text("Line #{i}"))` entries). The helper must use the default page template (no custom geometry needed for this test).

    Test (b) — `"(b) body_capacity is identical for 9-page vs 100+-page document"`: Replace the stub with: define (or inline) a `measure_body_capacity/1` helper that accepts a doc, runs `Build.run/1 → Compose.run/1 → Measure.run/1`, and returns `elem({:ok, result} = result_tuple, 1).options.layout.body_capacity` (pattern match on `{:ok, measured}` at each stage). Build two documents using a named template with `footer: %Region{height: 30}` — one with 9 body lines and one with 500 body lines (enough to fill 100+ pages at default font size). Call `measure_body_capacity` on both. `assert cap_9 == cap_500`. Use `assert_in_delta` if float precision varies. The key: both must produce identical `body_capacity` values, proving that content volume does not affect geometry.

    Both tests share the `running_footer_doc/1` and `measure_body_capacity/1` helpers. If `running_footer_doc/1` was declared as a stub body in Plan 01, replace the stub body now with the real implementation. If helpers do not yet exist, add them as `defp` at the bottom of the test module (per lines 190–195 pattern).
  </action>

  <verify>
    <automated>mix test test/rendro/deterministic_test.exs --only "running-region determinism"</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/rendro/deterministic_test.exs` exits 0 with 2 of the 4 D-11 stubs now GREEN (tests a and b); tests (c) and (d) remain as stubs (still RED or skipped until Task 2)
    - D-11(a) test explicitly passes two `deterministic: true` renders through the footer substitution path — confirmed by examining the returned PDF bytes for `"Page 1 of"` substring
    - D-11(b) test proves `body_capacity` is identical for 9-line vs 500-line document — verified by `assert cap_9 == cap_500` (exact equality or `assert_in_delta` within 1.0e-9)
    - `mix compile` exits 0
  </acceptance_criteria>

  <done>D-11(a) and D-11(b) GREEN. Byte-identity and geometry-independence proven.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement D-11(c) and D-11(d) assertions; confirm full suite green (PAGE-04 closed)</name>

  <read_first>
    - test/rendro/deterministic_test.exs — read the current state after Task 1; locate stubs (c) and (d) still using flunk; also read lines 139–188 (embedded font parity pipeline chain) as the structural analog for full-pipeline-chain tests; read lines 147–160 for the block-finder pattern `Enum.find(page.blocks, &match?(...))` and per-run text access
    - .planning/phases/73-page-numbering-running-region-primitive/73-RESEARCH.md — §"D-11 Determinism Test Skeleton" for tests (c) and (d) reference implementations; §"Pitfall 2: Substituting run widths" for what D-11(d) must guard
    - .planning/phases/73-page-numbering-running-region-primitive/73-PATTERNS.md — §"D-11(d) geometry guard" excerpt showing how to read MeasuredText.lines from a footer block post-paginate
    - .planning/phases/73-page-numbering-running-region-primitive/73-CONTEXT.md — D-11(c) and D-11(d) exact assertion descriptions; D-10 (text-only substitution invariant)
  </read_first>

  <behavior>
    - D-11(c): Two documents differing only in footer text — one with `"Page {{page_number}} of {{total_pages}}"` and one with `"Page {{page_number}} of 999"` (static wide placeholder) — produce the same `length(pages)` after `Build → Compose → Measure → Paginate` AND the same per-page body-block count list. This proves `{{total_pages}}` token presence does not affect pagination geometry.
    - D-11(d): A document with a measured footer block: after running through `Build → Compose → Measure → Paginate`, the per-run `width` values and block `height` in the footer's `MeasuredText` are identical to the pre-substitution values. Concretely: capture the footer block from the first page post-paginate; capture all run widths and the block height; access the substituted text via `run.text`; assert that `run.width` values for each run are unchanged from their measured values (i.e., the width of `"Page 1 of 10"` run is the same as the width of the `"Page {{page_number}} of {{total_pages}}"` run that was measured at layout time). This is the regression guard against a future re-measure-on-substitute.
  </behavior>

  <action>
    Replace the `flunk "not yet implemented"` stubs in tests (c) and (d).

    Test (c) — `"(c) page count and body-block assignment identical with total_pages vs static placeholder"`: Replace stub with a full pipeline run. Build two documents: `doc_token = running_footer_doc("Page {{page_number}} of {{total_pages}}")` and `doc_static = running_footer_doc("Page {{page_number}} of 999")`. Run `Build → Compose → Measure → Paginate` on both. Assert `length(paginated_token.pages) == length(paginated_static.pages)`. Define a helper `body_block_counts/1` that maps over pages and counts blocks with `role: :body` or matching the body_region anchor (use the `%Region{role: :body}` pattern from measure.ex or simply count non-footer, non-header blocks — look at how `stack_body_blocks` identifies body blocks in paginate.ex). Assert `body_block_counts(paginated_token) == body_block_counts(paginated_static)`.

    Test (d) — `"(d) replace_page_numbers does not change MeasuredText geometry"`: Replace stub with: build a doc with footer `[Rendro.page_number()]` (which produces `"Page {{page_number}} of {{total_pages}}"` text). Run `Build → Compose → Measure → Paginate`. Find the footer block on the first page using `Enum.find(page.blocks, &match?(%{content: %Rendro.Pipeline.MeasuredText{}}, &1))`. Extract `block.content.lines` and collect all `{run.text, run.width}` pairs and `block.content.height`. The `run.text` values will have been substituted (e.g., `"Page 1 of N"`). Assert that if we replace the substituted text back with the original tokens, the `run.width` values are the same as measured — BUT this is hard to do directly. The practical test: compare the `run.width` values of the footer block on page 1 vs page 2 (they should be identical, because the width was measured for the token-length string, not the substituted string). `assert Enum.map(page1_footer_runs, & &1.width) == Enum.map(page2_footer_runs, & &1.width)`. Additionally, assert that `block.content.height` is the same on page 1 and page 2. This proves geometry is frozen and not recomputed per substitution.

    After both test implementations are in place, run the full suite to confirm all Phase 73 Wave 0 stubs are now GREEN.
  </action>

  <verify>
    <automated>mix test</automated>
  </verify>

  <acceptance_criteria>
    - `mix test` exits 0 — full suite green
    - `mix test test/rendro/deterministic_test.exs` exits 0 — all 4 D-11 tests pass
    - D-11(c): `assert length(paginated_token.pages) == length(paginated_static.pages)` passes — confirmed by test output showing page count match
    - D-11(d): `assert page1_run_widths == page2_run_widths` passes — confirmed by test output showing geometry invariance
    - `grep -c "flunk" test/rendro/deterministic_test.exs` returns 0 — no remaining stubs
    - `grep -c "flunk" test/rendro/pipeline/paginate_test.exs` returns 0
    - `grep -c "flunk" test/rendro/flow_test.exs` returns 0
    - `grep -c "flunk" test/rendro_builders_test.exs` returns 0
    - `mix test --cover` (if configured) or `mix test` shows no regressions in any pre-existing test
  </acceptance_criteria>

  <done>All 4 D-11 assertions GREEN. Full suite exits 0. Phase 73 PAGE-04 closed. All PAGE-01..04 requirements satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → deterministic render pipeline | Author-controlled inputs only; no external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
| T-73-06 | Information Disclosure | non-deterministic PDF bytes (timestamps, object IDs) | mitigate | D-11(a) test with `deterministic: true` directly asserts byte-identity; the existing `deterministic: true` path from prior milestones seeds timestamps/IDs deterministically; this plan's test exercises the running-footer substitution path through that same flag |

No new external input surface. This plan is test-only — no production code changes.
</threat_model>

<verification>
`mix test`

Expected: exits 0. Full suite green. Zero flunk stubs remaining.

Cross-check:
```
grep -rn "flunk" test/rendro/deterministic_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs test/rendro_builders_test.exs
```
Expected: no output (zero flunk occurrences).
</verification>

<success_criteria>
- All four D-11 determinism assertions pass
- Full `mix test` suite exits 0
- Zero `flunk` stubs remaining across all Phase 73 test files
- PAGE-04 requirement satisfied
- All PAGE-01..04 requirements covered by GREEN tests
</success_criteria>

<output>
Create `.planning/phases/73-page-numbering-running-region-primitive/73-05-SUMMARY.md` when done
</output>
