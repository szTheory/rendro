---
phase: 97-location-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/block.ex
  - lib/rendro/metadata.ex
  - lib/rendro/rules/check_ids.ex
  - lib/rendro/pipeline/validate.ex
  - test/rendro/rules/check_ids_test.exs
autonomous: true
requirements:
  - ANC-01
  - ANC-03
must_haves:
  truths:
    - "Developer can assign an `id` to any block"
    - "Engine traverses the document to validate unique IDs"
    - "Duplicate IDs trigger a clear failure before layout processing completes"
  artifacts:
    - path: "lib/rendro/block.ex"
      provides: "id field"
    - path: "lib/rendro/metadata.ex"
      provides: "anchors map"
    - path: "lib/rendro/rules/check_ids.ex"
      provides: "pre-layout structural duplicate validation"
  key_links:
    - from: "lib/rendro/pipeline/validate.ex"
      to: "lib/rendro/rules/check_ids.ex"
      via: "@default_rules"
---

<objective>
Introduce the core Location Tracking primitives (`id` and `anchors`) and pre-layout duplicate ID validation.

Purpose: Provide the structural foundation for bookmarks and cross-references without mutating layout.
Output: Core models updated and a new pipeline validation rule for deterministic duplicate checking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/rendro/block.ex
@lib/rendro/metadata.ex
@lib/rendro/pipeline/validate.ex
@lib/rendro/rules/check_form_fields.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Primitives</name>
  <files>lib/rendro/block.ex, lib/rendro/metadata.ex</files>
  <action>Add `id: nil` to the `Rendro.Block` struct and `@type t` (typed as `id: String.t() | nil`). Add `anchors: %{}` to the `Rendro.Metadata` struct and its `@type t` if it exists. These form the explicit primitive boundaries.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Models compile and correctly define the new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Duplicate ID Validation Rule</name>
  <files>lib/rendro/rules/check_ids.ex, lib/rendro/pipeline/validate.ex, test/rendro/rules/check_ids_test.exs</files>
  <behavior>
    - Test 1: Unique IDs pass check/2 returning :ok.
    - Test 2: Duplicate IDs return {:errors, [{:duplicate_id, "id_name"}]}.
  </behavior>
  <action>Create `lib/rendro/rules/check_ids.ex` implementing a document tree traversal to find blocks with string `id`s, returning duplicate ID tuples. Register `CheckIds` at the end of `@default_rules` in `lib/rendro/pipeline/validate.ex`. Use `CheckFormFields` as a structural analog for traversal and module structure.</action>
  <verify>
    <automated>mix test test/rendro/rules/check_ids_test.exs</automated>
  </verify>
  <done>CheckIds is registered in the pipeline and catches duplicate IDs deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline input | User provides IDs that must be validated as non-colliding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | CheckIds rule | mitigate | Enforce absolute uniqueness at the structural level via check_ids.ex |
</threat_model>

<verification>
mix test test/rendro/rules/check_ids_test.exs
</verification>

<success_criteria>
Blocks can carry explicit `id` attributes and duplicates fail deterministically.
</success_criteria>

<output>
Create `.planning/phases/97-location-tracking/97-01-SUMMARY.md` when done
</output>