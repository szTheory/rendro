---
phase: 97-location-tracking
plan: 02
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - lib/rendro/pipeline/paginate.ex
  - lib/rendro/fragmentable.ex
  - lib/rendro/error.ex
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/fragmentable_test.exs
autonomous: true
requirements:
  - ANC-02
  - ANC-03
must_haves:
  truths:
    - "Document metadata contains an exact `[page, :XYZ, x, y, nil]` entry for each `id`"
    - "Fragments of a block (due to page breaks) do not trigger duplicate IDs"
    - "Pagination crashes deterministically if an invalid duplicate is encountered during layout"
  artifacts:
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "anchor accumulation loop"
    - path: "lib/rendro/fragmentable.ex"
      provides: "ID clearing on fragments"
    - path: "lib/rendro/error.ex"
      provides: "duplicate_anchor_id error mappings"
  key_links:
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "Rendro.Error"
      via: "from_stage(:paginate, :duplicate_anchor_id)"
---

<objective>
Harvest absolute physical locations of ID-bearing blocks during pagination without causing multi-pass layout oscillations.

Purpose: Generate precise `XYZ` jump targets that preserve user zoom level in PDF viewers.
Output: `doc.metadata.anchors` is accurately populated and safe fragment splitting is guaranteed.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/rendro/pipeline/paginate.ex
@lib/rendro/fragmentable.ex
@lib/rendro/error.ex

<interfaces>
From lib/rendro/block.ex (Plan 01):
```elixir
%Rendro.Block{
  id: String.t() | nil,
  # ... x, y, content ...
}
```

From lib/rendro/metadata.ex (Plan 01):
```elixir
%Rendro.Metadata{
  anchors: %{},
  # ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fragment ID Isolation</name>
  <files>lib/rendro/fragmentable.ex, test/rendro/fragmentable_test.exs</files>
  <behavior>
    - Test 1: Splitting an anchored Block returns a remaining fragment with `id: nil`.
  </behavior>
  <action>Modify `Rendro.Fragmentable.split/2` for `Rendro.Block`. Explicitly set `id: nil` on the `rem_block` inside the split implementation to ensure multi-page block splits do not artificially trigger duplicate ID errors during accumulation.</action>
  <verify>
    <automated>mix test test/rendro/fragmentable_test.exs</automated>
  </verify>
  <done>Fragmentable correctly drops the `id` on trailing splits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anchor Accumulation</name>
  <files>lib/rendro/pipeline/paginate.ex, lib/rendro/error.ex, test/rendro/pipeline/paginate_test.exs</files>
  <behavior>
    - Test 1: Paginate accumulates `[page_idx, :XYZ, x, y, nil]` for ID-bearing blocks.
    - Test 2: Paginate translates duplicate IDs into a structured `Rendro.Error`.
  </behavior>
  <action>Update `run/1` in `Rendro.Pipeline.Paginate` to traverse `paginated_doc.pages` after layout. Build a `collect_anchors/1` function that aggregates `%{id => [page_idx, :XYZ, x || 0, y || 0, nil]}` recursively (including nested blocks like Table contents). Explicitly assign the accumulated anchors map to `paginated_doc.metadata.anchors` on the success path and return the updated document. If a duplicate is detected during accumulation, throw `{:error, :duplicate_anchor_id, id}`. Catch this in `run/1` and return `Rendro.Error.from_stage(:paginate, :duplicate_anchor_id, %{id: id})`. Finally, add the corresponding `:duplicate_anchor_id` mapping to `Rendro.Error` (`what/2`, `why/2`, `next_step/2`).</action>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Metadata anchors map is correctly hydrated with XYZ destinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pagination Loop | Block layout properties are finalized and fixed into absolute coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02 | Information Disclosure | Metadata | accept | Anchors are purely structural and do not leak user-sensitive metadata beyond explicitly provided IDs |
</threat_model>

<verification>
mix test test/rendro/pipeline/paginate_test.exs
</verification>

<success_criteria>
`paginate` correctly aggregates XYZ anchors in `metadata.anchors` and `fragmentable` securely isolates split blocks.
</success_criteria>

<output>
Create `.planning/phases/97-location-tracking/97-02-SUMMARY.md` when done
</output>
