---
phase: 125-foundation-curated-fonts-style-genre-presets-brand-fixtures
plan: 02
type: execute
wave: 2
depends_on: [125-01]
files_modified:
  - lib/rendro/theme/presets.ex
  - lib/rendro/recipes/certificate.ex
  - test/rendro/theme/presets_test.exs
  - test/docs_contract/theme_industry_guard_test.exs
  - test/rendro/recipes/certificate_typography_test.exs
  - test/rendro/theme/preset_render_matrix_test.exs
  - test/rendro/theme/preset_raster_snapshot_test.exs
  - priv/raster_refs/presets/swiss/light.sha256
  - priv/raster_refs/presets/swiss/dark.sha256
  - priv/raster_refs/presets/humanist/light.sha256
  - priv/raster_refs/presets/humanist/dark.sha256
  - priv/raster_refs/presets/editorial/light.sha256
  - priv/raster_refs/presets/editorial/dark.sha256
  - priv/raster_refs/presets/corporate_classic/light.sha256
  - priv/raster_refs/presets/corporate_classic/dark.sha256
  - priv/raster_refs/presets/minimal_mono/light.sha256
  - priv/raster_refs/presets/minimal_mono/dark.sha256
  - priv/raster_refs/presets/brutalist/light.sha256
  - priv/raster_refs/presets/brutalist/dark.sha256
autonomous: true
requirements: [PRESET-01, PRESET-02, PRESET-03, PRESET-04, PRESET-05, PRESET-06, FONT-04]
must_haves:
  truths:
    - "All five required canonical presets and the complete Brutalist preset resolve to the exact D-10 structural token contracts; no aliases or recipe branches exist."
    - "EDGE PRESET-02 adjacency: each canonical preset has a distinct immediate-neighbor signature and differs on at least three material axes."
    - "EDGE PRESET-02 empty/null/single: the supported-set contract contains exactly the five required atoms plus the fully proven Brutalist atom, with no nil/empty/duplicate row."
    - "EDGE PRESET-02 stable ordering: enumerating signatures repeatedly yields the same canonical order and values."
    - "EDGE PRESET-03 adjacency: the allowed `theme.ex` delegation is isolated before the unchanged forbidden vocabulary scan, so adjacent docs/helpers cannot hide genre data."
    - "EDGE PRESET-03 empty/null/single: exactly one narrow `preset/2` delegation exists; zero or multiple delegations fail the guard."
    - "EDGE PRESET-03 stable ordering: source-guard behavior is independent of function placement and checks the remaining source after removing the one delegation block."
    - "EDGE PRESET-04 adjacency: both `:light` and `:dark` boundaries are exercised for every preset and preserve the caller accent."
    - "EDGE PRESET-04 empty/null/single: absent theme/default recipe calls remain byte-identical, while one valid preset is sufficient for rendering."
    - "EDGE PRESET-04 stable ordering: equivalent option order and repeat renders produce equal themes/PDF bytes without per-draw preset arithmetic."
  artifacts:
    - path: lib/rendro/theme/presets.ex
      provides: "Complete D-10 preset table and role mapping"
    - path: lib/rendro/recipes/certificate.ex
      provides: "Curated-role metric compatibility without changing caller order"
    - path: test/rendro/theme/preset_render_matrix_test.exs
      provides: "Deterministic 7-recipe/light-dark/font-embedding matrix"
    - path: test/rendro/theme/preset_raster_snapshot_test.exs
      provides: "Pinned-PDFium advisory snapshot producer/comparator"
  key_links:
    - from: lib/rendro/theme/presets.ex
      to: lib/rendro/theme.ex
      via: "`Theme.resolve/1` followed by `Theme.dark/1` for dark mode"
      pattern: "Theme.(resolve|dark)"
    - from: lib/rendro/recipes/certificate.ex
      to: lib/rendro/theme/presets.ex
      via: "curated logical role resolves to exact embedded PDF metrics before centering"
      pattern: "metric_font!"
    - from: test/rendro/theme/preset_render_matrix_test.exs
      to: lib/rendro/recipes
      via: "bounded matrix covers all required genres/modes, all recipes, and all four embedded faces"
      pattern: "register_fonts"
  flagged_assumptions:
    - "EDGE PRESET-05 unclassified manual-review item: the existing typed missing-font error is considered actionable only if the final deterministic output names the logical role and explicit `Presets.register_fonts/2` next action; Plan 04 reviews the emitted evidence."
    - "EDGE PRESET-06 unclassified manual-review item: Brutalist distinctness and business-document legibility require the same pinned-PDFium human review as every other genre; token inequality alone is insufficient."
  prohibitions:
    - statement: "Presets must not be represented or reviewed as a guarantee of design quality, accessibility, WCAG/PDF-UA conformance, dark-mode print safety, or suitability for every recipe pairing."
      status: unresolved
      verification: judgment
---

<objective>
Expand the proven tracer to the complete structural genre system, remove Certificate's curated-font incompatibility, and create the bounded deterministic and advisory proof matrix.

Purpose: make every required preset and every existing recipe genuinely usable through the same public pipeline, while preserving byte identity and keeping visual judgment honest.
Output: five required presets plus complete Brutalist, strengthened source guard, Certificate exact-metric seam, render/embedding matrix, and pinned raster references.

## Artifacts this phase produces

- Expanded symbols/data: D-10 token rows and role sets for `:humanist`, `:editorial`, `:corporate_classic`, `:minimal_mono`, and complete `:brutalist`; `Rendro.Theme.Presets.metric_font!/1` consumed by Certificate.
- New test files: `test/rendro/theme/preset_render_matrix_test.exs`, `test/rendro/theme/preset_raster_snapshot_test.exs`.
- New raster paths: `priv/raster_refs/presets/{swiss,humanist,editorial,corporate_classic,minimal_mono,brutalist}/{light,dark}.sha256` (12 exact files).
- Modified contracts: `test/docs_contract/theme_industry_guard_test.exs` permits/asserts only the narrow delegation; `test/rendro/recipes/certificate_typography_test.exs` proves metric/render coupling.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-RESEARCH.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-PATTERNS.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-01-SUMMARY.md
@.planning/milestones/v2.11-phases/122-typography-type-scale-application-font-role-leading-wiring/122-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Materialize and guard the complete genre grammar</name>
  <reversibility rating="costly">Canonical genre atoms and quantified public token behavior will be copied into later catalog/configurator surfaces (D-01, D-10); the locked choices are recorded without re-asking them.</reversibility>
  <read_first>
    - .planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
    - lib/rendro/theme/presets.ex
    - lib/rendro/theme.ex
    - test/rendro/theme/presets_test.exs
    - test/docs_contract/theme_industry_guard_test.exs
    - test/rendro/theme_test.exs
  </read_first>
  <files>lib/rendro/theme/presets.ex, test/rendro/theme/presets_test.exs, test/docs_contract/theme_industry_guard_test.exs</files>
  <behavior>
    - "The D-10 values for all five required genres resolve exactly; Minimal-Mono defaults to comfortable/1.25 and explicit compact uses existing 1.1 semantics (D-08..D-11)."
    - "Brutalist ships only as a complete sixth canonical atom with its D-10 token row, full role registration, validation, matrix, determinism, and raster coverage (D-12)."
    - "Every pair of required preset signatures is unequal and nearest neighbors differ on at least three named axes (D-25)."
    - "The unchanged forbidden vocabulary is scanned over `theme.ex` after removing exactly one statically asserted, readable delegation block; no token/dispatch/path data leaks (D-07)."
  </behavior>
  <action>Write failing table-driven tests first, then materialize every D-10 literal in `Presets`: fonts, scale, leading, spacing, rules, radius, density, and neutral directions. Preserve D-08 structural differences, D-09 semantic-role-only composition, D-10 ordering, and D-11 comfortable/1.25 Minimal-Mono behavior. Fully implement Brutalist after the five required rows pass, including its canonical atom and role set; because it is included in this plan, it must meet the identical validation/render/determinism/raster bar with no partial dispatch or stub (D-12). Ensure strict option validation and light-first/accent/on-accent/dark-last semantics remain uniform for every row (D-01..D-04). Strengthen `theme_industry_guard_test.exs` per D-07: retain the forbidden list byte-for-byte, positively assert exactly one narrow readable `preset/2` delegation to `Rendro.Theme.Presets`, remove only that documented function block from the scan subject, and scan the remaining source so `theme.ex` cannot contain genre tables, font paths, catalog logic, or dynamic/generated concealment. Add named token-signature helpers in the test only and prove every required nearest neighbor differs on at least three of font mapping, display/body ratio, leading, spacing, rules, radius, and neutrals (D-25). Do not add new `%Theme{}` fields or recipe branches (D-09, D-26).</action>
  <verify>
    <automated>mix test test/rendro/theme/presets_test.exs test/docs_contract/theme_industry_guard_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert every literal in the D-10 matrix and every D-15 role set, including complete Brutalist.
    - Invalid genre/options/color/mode/density cases name the bad value and canonical alternatives.
    - Every required nearest-neighbor signature differs on at least three named axes.
    - The source guard retains its original forbidden word list and proves one delegation plus zero residual forbidden occurrences.
  </acceptance_criteria>
  <done>The complete six-row grammar is strict, structurally distinct, deterministic, and confined to `Presets`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Certificate metric-compatible and prove every render path</name>
  <read_first>
    - .planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
    - lib/rendro/theme/presets.ex
    - lib/rendro/recipes/certificate.ex
    - lib/rendro/font_registry.ex
    - lib/rendro/pdf/font.ex
    - test/rendro/recipes/certificate_typography_test.exs
    - test/rendro/recipes/themed_render_smoke_test.exs
    - test/rendro/adapters/pdfium_raster_snapshot_test.exs
    - priv/pdfium_pin.json
  </read_first>
  <files>lib/rendro/recipes/certificate.ex, test/rendro/recipes/certificate_typography_test.exs, test/rendro/theme/preset_render_matrix_test.exs, test/rendro/theme/preset_raster_snapshot_test.exs, priv/raster_refs/presets/swiss/light.sha256, priv/raster_refs/presets/swiss/dark.sha256, priv/raster_refs/presets/humanist/light.sha256, priv/raster_refs/presets/humanist/dark.sha256, priv/raster_refs/presets/editorial/light.sha256, priv/raster_refs/presets/editorial/dark.sha256, priv/raster_refs/presets/corporate_classic/light.sha256, priv/raster_refs/presets/corporate_classic/dark.sha256, priv/raster_refs/presets/minimal_mono/light.sha256, priv/raster_refs/presets/minimal_mono/dark.sha256, priv/raster_refs/presets/brutalist/light.sha256, priv/raster_refs/presets/brutalist/dark.sha256</files>
  <behavior>
    - "Certificate measures centered lines with the exact curated face metrics corresponding to the emitted logical role before the caller's post-`document/2` registration, while the public call order remains unchanged (D-05, D-06)."
    - "Certificate no-theme/default bytes stay equal to committed goldens, and an unknown non-curated role retains an actionable failure instead of Helvetica measurement (D-06, D-26)."
    - "A bounded 12-row matrix covers each of six presets in light/dark; its required subset covers all seven existing recipes and embeds all four curated faces at least once (D-12, D-24)."
    - "Repeated matrix renders are byte-identical and raster snapshots use the pinned PDFium lane separately from deterministic ExUnit defaults (D-24..D-27)."
  </behavior>
  <action>Replace Certificate's curated-role incompatibility without changing `Recipe.document(theme: theme) |> Presets.register_fonts(genre)` (D-05/D-06). Implement the narrow internal seam chosen by research: `Presets.metric_font!/1` recognizes only the four Rendro-owned curated atoms, constructs/preflights the exact same descriptor used by registration through existing `FontRegistry` primitives, and returns its parsed `%Rendro.PDF.Font{}`; Certificate's centering and body-line estimates use it for curated roles while preserving `:default`/`\"Helvetica\"` behavior and bytes. Unknown non-curated roles keep an actionable error and never measure with Helvetica. Add a deterministic render matrix with these required rows: Swiss Invoice light + Certificate dark; Humanist Receipt light + Payslip dark; Editorial Certificate light + Ticket dark; Corporate-Classic BrandedInvoice light + Invoice dark; Minimal-Mono Statement light + Ticket dark. Add Brutalist light/dark rows through suitable business recipes so its all-or-nothing bar matches D-12. Assert all seven recipes occur, each required genre has both modes, all four curated logical roles become embedded at least once, unregistered counterparts return typed errors, and two renders per row are byte-identical (D-24/D-26). Add a separate `:raster_snapshot`-tagged pinned-PDFium test producing/comparing the 12 exact reference hashes; it may emit review PNGs to `RENDRO_PRESET_RASTER_REVIEW_DIR`, but remains excluded from deterministic required tests. Describe evidence only as bounded plumbing/genre-distinct starting-point review per D-25/D-27.</action>
  <verify>
    <automated>mix test test/rendro/recipes/certificate_typography_test.exs test/rendro/theme/preset_render_matrix_test.exs test/rendro/recipes/*_byte_identity_test.exs --max-failures 1 &amp;&amp; git diff --exit-code -- priv/goldens &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - A curated serif Certificate renders and its measured widths come from the same face descriptor later registered for rendering.
    - Existing Certificate and all recipe byte-identity tests pass with no changes under `priv/goldens`.
    - The matrix test proves 12 rows, six genres × two modes, seven recipes covered, four roles embedded, typed omission failures, and repeat-byte equality.
    - Raster snapshot tests are tagged/excluded from default tests, use `priv/pdfium_pin.json`, and have one exact SHA-256 reference per row.
  </acceptance_criteria>
  <done>Every preset and recipe path composes with exact font metrics, deterministic bytes, and a separately reviewable pinned raster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| preset token table → recipes | Shared semantic roles and geometry values affect all recipe measurement/render paths. |
| curated role → Certificate metric resolver | A logical atom selects a packaged binary whose metrics determine geometry before registration. |
| PDF output → PDFium | Deterministic generated bytes cross into an advisory external rasterizer. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-06 | Tampering | preset dispatch/token table | medium | mitigate | Exact D-10 literal assertions, stable signature order, three-axis distinctness, and source-confinement guard. |
| T-125-07 | Denial of service | Certificate curated metric preflight | high | mitigate | Resolve only four vendored/preflighted roles; reuse FontRegistry parser and fail closed on any other role. |
| T-125-08 | Tampering | role-to-metric link | high | mitigate | Metric resolver and registration share the exact curated descriptor; integration test compares measured/emitted face identity. |
| T-125-09 | Repudiation | visual distinctness evidence | medium | mitigate | Fixed matrix identifiers, pinned PDFium version, committed hashes, and an explicit human checkpoint in Plan 04. |
| T-125-10 | Information disclosure | raster review outputs | low | accept | Inputs are synthetic examples only; review files stay in a bounded temporary directory and are not packaged. |
</threat_model>

<verification>
Deterministic lane: preset literals/signatures, guard confinement, exact metric coupling, 12-row render/embedding coverage, typed errors, repeated PDF bytes, and unchanged legacy goldens. Advisory lane: only `mix test --include raster_snapshot test/rendro/theme/preset_raster_snapshot_test.exs` uses pinned PDFium and feeds Plan 04 human judgment; it never gates deterministic CI or supports accessibility/print-safety claims.
</verification>

<success_criteria>
- Five required genres and complete Brutalist pass identical API/font/render checks.
- Every existing recipe renders at least one required preset; every required genre renders light/dark; every face embeds.
- Certificate uses exact curated metrics without changing caller order or default bytes.
- Genre distinctness has both mechanical three-axis evidence and separately routed pinned-raster evidence.
</success_criteria>

<output>
Create `.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-02-SUMMARY.md` when done.
</output>
