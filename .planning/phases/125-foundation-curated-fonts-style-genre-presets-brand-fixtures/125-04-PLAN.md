---
phase: 125-foundation-curated-fonts-style-genre-presets-brand-fixtures
plan: 04
type: execute
wave: 4
depends_on: [125-03]
files_modified:
  - lib/rendro/theme/presets.ex
  - lib/rendro/recipes/certificate.ex
  - test/rendro/recipes/certificate_typography_test.exs
  - test/rendro/theme/preset_render_matrix_test.exs
autonomous: true
requirements: [PRESET-04, PRESET-05, PRESET-06, FONT-04]
must_haves:
  truths:
    - "Certificate measures curated text with the exact face descriptor later registered, while public caller order stays unchanged (D-05, D-06)."
    - "Default/Helvetica Certificate bytes remain unchanged and every unknown non-curated role fails rather than borrowing Helvetica metrics (D-06, D-26)."
    - "A deterministic 12-row matrix covers six genres in light/dark, all seven recipes, all four embedded faces, typed omission failures, and repeat-byte equality (D-12, D-24, D-26)."
  artifacts:
    - path: lib/rendro/recipes/certificate.ex
      provides: "Private exact curated-metric compatibility"
    - path: test/rendro/theme/preset_render_matrix_test.exs
      provides: "Bounded deterministic recipe/mode/font proof matrix"
  key_links:
    - from: lib/rendro/recipes/certificate.ex
      to: lib/rendro/theme/presets.ex
      via: "private metric_font!/1 uses the same curated descriptor as registration"
      pattern: "metric_font!"
    - from: test/rendro/theme/preset_render_matrix_test.exs
      to: lib/rendro/recipes
      via: "stable 12-row construction and explicit register_fonts call"
      pattern: "register_fonts"
---

<objective>
Remove Certificate's curated-font incompatibility and prove every deterministic preset/render path without coupling it to advisory raster assets.

Purpose: ensure all recipes compose with the public bridge and exact font metrics while the deterministic lane remains fast, complete, and PDFium-independent.
Output: private Certificate metric seam, regression coverage, and the fixed 12-row deterministic matrix.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-RESEARCH.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-03-SUMMARY.md
@.planning/milestones/v2.11-phases/122-typography-type-scale-application-font-role-leading-wiring/122-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Certificate use the exact curated descriptor for metrics</name>
  <read_first>
    - .planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-RESEARCH.md
    - lib/rendro/theme/presets.ex
    - lib/rendro/recipes/certificate.ex
    - lib/rendro/font_registry.ex
    - lib/rendro/pdf/font.ex
    - test/rendro/recipes/certificate_typography_test.exs
  </read_first>
  <files>lib/rendro/theme/presets.ex, lib/rendro/recipes/certificate.ex, test/rendro/recipes/certificate_typography_test.exs</files>
  <behavior>
    - "The resolved research choice is private `Presets.metric_font!/1`, limited to the four D-15 roles and returning the parsed exact registration descriptor (D-06)."
    - "Certificate centering, widths, line count, and vertical estimates use that exact face before post-document registration; public call order is unchanged (D-05, D-06)."
    - "Default/Helvetica bytes remain equal and unknown non-curated roles retain an actionable failure (D-06, D-26)."
  </behavior>
  <action>Write curated-role centering and default-byte failures first. Implement the resolved research contract: Certificate calls the private `Presets.metric_font!/1` for only the four Rendro-owned atoms; that seam constructs and preflights the exact same descriptor used by `register_fonts/2` through existing FontRegistry parsing and returns `%Rendro.PDF.Font{}`. Use it consistently for centered width, body-line count, and vertical estimates before registration. Preserve the existing `:default`/`"Helvetica"` behavior and bytes, reject all unknown non-curated roles actionably, and keep the public pipeline `Recipe.document(theme: theme) |> Presets.register_fonts(genre)` unchanged (D-05, D-06, D-26). Do not expose metric lookup through `Rendro.Theme` or widen any public API.</action>
  <verify>
    <automated>mix test test/rendro/recipes/certificate_typography_test.exs test/rendro/recipes/certificate_byte_identity_test.exs --max-failures 1 &amp;&amp; git diff --exit-code -- priv/goldens &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Certificate measures and renders curated roles with one exact descriptor while all default bytes and public call shapes remain stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the fixed deterministic preset/render matrix</name>
  <read_first>
    - test/rendro/recipes/themed_render_smoke_test.exs
    - test/rendro/recipes/invoice_byte_identity_test.exs
    - lib/rendro/recipes/invoice.ex
    - lib/rendro/recipes/ticket.ex
  </read_first>
  <files>test/rendro/theme/preset_render_matrix_test.exs</files>
  <behavior>
    - "The stable matrix has 12 rows: each of six genres in light/dark, all seven recipes represented, and all four curated roles embedded at least once (D-12, D-24)."
    - "Every unregistered counterpart returns the typed role error and every registered row renders byte-identically twice (D-24, D-26)."
    - "No default recipe golden changes and this deterministic test never invokes PDFium (D-24, D-26, D-27)."
  </behavior>
  <action>Create the deterministic matrix with these locked required rows: Swiss Invoice light + Certificate dark; Humanist Receipt light + Payslip dark; Editorial Certificate light + Ticket dark; Corporate-Classic BrandedInvoice light + Invoice dark; Minimal-Mono Statement light + Ticket dark; Brutalist light/dark through suitable business recipes. Assign stable row IDs shared later by raster tests. Assert exactly 12 rows, six genres × two modes, all seven recipes, all four embedded roles, typed omission failures, and two-render byte equality (D-12, D-24, D-26). Keep PDFium, PNGs, and visual claims out of this file and leave all legacy goldens unchanged.</action>
  <verify>
    <automated>mix test test/rendro/theme/preset_render_matrix_test.exs test/rendro/recipes/*_byte_identity_test.exs --max-failures 1 &amp;&amp; git diff --exit-code -- priv/goldens &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Every genre/mode/recipe/font path has deterministic, typed, repeatable PDF proof independent of the advisory raster lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| curated role → Certificate metric resolver | A logical role selects binary metrics before document registration. |
| preset table → all recipes | Shared values affect measurement and layout across seven recipes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-07 | Denial of service | Certificate metric preflight | high | mitigate | Resolve only four vendored/preflighted roles and fail closed on every other role. |
| T-125-08 | Tampering | metric/render identity | high | mitigate | Metric lookup and registration share the exact descriptor and tests compare measured/emitted identity. |
| T-125-22 | Repudiation | render coverage | medium | mitigate | Stable row IDs and explicit cardinality/recipe/role assertions prevent vacuous matrix claims. |
</threat_model>

<verification>
Focused deterministic tests prove exact metric/render coupling, unchanged defaults/goldens, all required rows, typed omissions, embedding, and repeated bytes without invoking PDFium.
</verification>

<success_criteria>
- Certificate supports every curated role with exact metrics and no public API expansion.
- The 12-row matrix covers six modes pairs, seven recipes, and four faces.
- Deterministic tests remain separate from visual evidence and legacy bytes remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-04-SUMMARY.md` when done.
</output>
