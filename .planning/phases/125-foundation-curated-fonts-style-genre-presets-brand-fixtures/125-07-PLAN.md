---
phase: 125-foundation-curated-fonts-style-genre-presets-brand-fixtures
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/schemas/examples.schema.json
  - test/docs_contract/examples_schema_contract_test.exs
  - priv/examples/invoice/northline-logistics/invoice.json
  - priv/examples/invoice/northline-logistics/logo.svg
  - priv/examples/invoice/cedar-mutual/invoice.json
  - priv/examples/invoice/cedar-mutual/logo.svg
  - priv/examples/payslip/northline-logistics/payslip.json
  - priv/examples/payslip/northline-logistics/logo.svg
  - priv/examples/payslip/cedar-mutual/payslip.json
  - priv/examples/payslip/cedar-mutual/logo.svg
autonomous: true
requirements: [CATALOG-05]
must_haves:
  truths:
    - "The generic schema validates and preserves stable brand identity, strict accent, canonical preset, and safe-local logo fields without creating a brand model (D-19, D-20)."
    - "Invoice and Payslip each gain exactly Northline Logistics and Cedar Mutual with consistent cross-domain identity/marks and exact domain arithmetic (D-19, D-22)."
    - "All four SVGs satisfy the resolved deterministic geometry-only contract and all fixture paths remain local (D-21)."
  artifacts:
    - path: priv/schemas/examples.schema.json
      provides: "Generic data-only brand metadata contract"
    - path: test/docs_contract/examples_schema_contract_test.exs
      provides: "Progressive count, path, SVG, identity, arithmetic, and old-byte controls"
  key_links:
    - from: priv/examples/invoice
      to: priv/schemas/examples.schema.json
      via: "generic brand block and existing Invoice discriminator"
      pattern: "recommended_preset"
    - from: priv/examples/payslip
      to: lib/rendro/examples.ex
      via: "existing safe-relative generic loader"
      pattern: "Path.safe_relative"
---

<objective>
Introduce the generic brand-data contract through two operational domains and the locked cross-domain identities.

Purpose: prove brands remain generic tuples before expanding the other four domains.
Output: schema/contract progression plus two additional Invoice and two additional Payslip brands.

<assumption_delta_decision>
Primary noun: example-brand fixture tuple identified by domain + slug. Decision: no-change — D-19/D-20 multiply data tuples and do not create a brand model; tests enforce unique `(domain, slug)` identity.
</assumption_delta_decision>
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-RESEARCH.md
@.planning/milestones/v2.11-phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the generic contract and add Invoice brands</name>
  <read_first>
    - priv/schemas/examples.schema.json
    - lib/rendro/examples.ex
    - test/docs_contract/examples_schema_contract_test.exs
    - priv/examples/invoice/acme-phoenix-saas/invoice.json
    - priv/examples/invoice/DOMAIN.md
  </read_first>
  <files>priv/schemas/examples.schema.json, test/docs_contract/examples_schema_contract_test.exs, priv/examples/invoice/northline-logistics/invoice.json, priv/examples/invoice/northline-logistics/logo.svg, priv/examples/invoice/cedar-mutual/invoice.json, priv/examples/invoice/cedar-mutual/logo.svg</files>
  <behavior>
    - "The generic brand object accepts the five D-20 fields and rejects bad slug/accent/preset or unsafe/non-local logo paths while retaining family conditionals."
    - "Invoice adds Northline Logistics/#2C6BED/Swiss and Cedar Mutual/#1F4FB8/Corporate-Classic exactly, with realistic synthetic totals reconciling through Decimal (D-22)."
    - "Both local marks use the resolved text-free, one-color, simple-geometry SVG subset (D-21)."
  </behavior>
  <action>Write failing generic schema/path/SVG/arithmetic tests first. Extend only the existing generic brand object with required `slug`, `display_name`, strict six-digit `accent`, canonical `recommended_preset`, and safe-relative local `logo`; retain `additionalProperties: true` and all existing family conditionals (D-20). Add the exact D-22 Northline and Cedar Invoice directories, identities, visual directions, synthetic data, decimal-string money, and exact reconciliation. Their SVGs must be text-free one-color simple geometry, contain no script/external reference/image/gradient/filter, and resolve inside the fixture directory under the resolved research contract (D-21). Preserve the original Invoice fixture bytes; add no module, recipe branch, remote asset, or custom font (D-20, D-23, D-26).</action>
  <verify>
    <automated>mix test test/docs_contract/examples_schema_contract_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>The generic brand contract is live and Invoice has exactly two additional locked, safe, reconciling data brands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse Northline and Cedar consistently in Payslip</name>
  <read_first>
    - test/docs_contract/examples_schema_contract_test.exs
    - priv/examples/payslip/aurora-live/payslip.json
    - priv/examples/payslip/DOMAIN.md
  </read_first>
  <files>test/docs_contract/examples_schema_contract_test.exs, priv/examples/payslip/northline-logistics/payslip.json, priv/examples/payslip/northline-logistics/logo.svg, priv/examples/payslip/cedar-mutual/payslip.json, priv/examples/payslip/cedar-mutual/logo.svg</files>
  <behavior>
    - "Payslip adds the same locked Northline and Cedar identity/accent/preset tuples and consistent marks (D-22)."
    - "Gross, deductions, net, and YTD data reconcile exactly as decimal strings; synthetic data contains no real employee/account/credential/payment data (D-19, D-23)."
  </behavior>
  <action>Add the exact D-22 Northline and Cedar Payslip directories, reusing their Invoice identity tuple and recognizable geometry consistently. Author realistic invented payroll content satisfying all existing schema limits and exact gross/deduction/net/YTD invariants; keep every money field a decimal string. Extend the progressive contract to assert cross-domain identity/mark consistency, unique `(domain, slug)`, exactly three fixtures in Invoice and Payslip, safe local SVGs, and unchanged original fixture hashes (D-19..D-23). Add no brand code or recipe logic.</action>
  <verify>
    <automated>mix test test/docs_contract/examples_schema_contract_test.exs test/rendro/examples_data_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Invoice and Payslip each expose exactly three data fixtures with the two locked cross-domain brands consistent and mechanically valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JSON/SVG fixture → schema/loader | Package data is decoded through generic paths. |
| logo reference → package filesystem | Fixture data selects a local asset path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-11 | Elevation of privilege | logo paths | high | mitigate | Safe-relative validation plus containment inside each fixture directory. |
| T-125-12 | Tampering | cross-domain identity | medium | mitigate | Exact D-22 tuple and mark consistency assertions plus unique domain/slug pairs. |
| T-125-13 | Information disclosure | fixture content | high | mitigate | Invented organizations/persons/account references and credential/payment-pattern checks. |
</threat_model>

<verification>
Schema/fixture tests prove generic identity fields, safe local SVGs, exact counts, cross-domain reuse, Decimal reconciliation, and unchanged original data.
</verification>

<success_criteria>
- Invoice and Payslip each contain exactly two new locked brands.
- Brands remain generic data and all paths/arithmetic/old-byte controls pass.
</success_criteria>

<output>
Create `.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-07-SUMMARY.md` when done.
</output>
