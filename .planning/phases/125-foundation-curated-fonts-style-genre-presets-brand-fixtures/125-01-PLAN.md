---
phase: 125-foundation-curated-fonts-style-genre-presets-brand-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/theme.ex
  - lib/rendro/theme/presets.ex
  - priv/fonts/inter/Inter-Regular.ttf
  - priv/fonts/jetbrains-mono/JetBrainsMono-Regular.ttf
  - test/rendro/theme/presets_test.exs
autonomous: true
requirements: [PRESET-01, PRESET-04, PRESET-05, FONT-04]
must_haves:
  truths:
    - "TRACER: `Theme.preset(:swiss, accent: \"#2C6BED\") |> Invoice.document/2 |> Presets.register_fonts(:swiss) |> Rendro.render/1` succeeds with real Inter and JetBrains Mono faces embedded (D-01..D-05, D-15)."
    - "Only canonical atoms and the strict keyword contract are accepted; malformed inputs raise actionable errors and option ordering does not change the resolved theme (D-01..D-04)."
    - "The theme is registry-inert; omission retains the typed missing-role error, equal registration is idempotent, unequal collisions do not overwrite, and document registries remain isolated (D-05, D-26)."
  artifacts:
    - path: lib/rendro/theme/presets.ex
      provides: "Strict Swiss preset resolver and explicit document-first font bridge"
    - path: lib/rendro/theme.ex
      provides: "Exactly one public preset/2 delegation"
    - path: test/rendro/theme/presets_test.exs
      provides: "End-to-end Swiss render and registry contracts"
  key_links:
    - from: lib/rendro/theme.ex
      to: lib/rendro/theme/presets.ex
      via: "single readable delegation"
      pattern: "Rendro.Theme.Presets.preset"
    - from: lib/rendro/theme/presets.ex
      to: priv/fonts
      via: "Application.app_dir descriptors registered on the caller document"
      pattern: "rendro_preset_(grotesque|mono)"
---

<objective>
Prove the production architecture through one Swiss invoice before expanding the phase.

Purpose: establish the strict public constructor, pure theme result, explicit registry ownership, real packaged font bytes, typed omission failure, and final PDF render in the thinnest complete path.
Output: `Theme.preset/2`, the private Presets bridge, two tracer font binaries, and a runnable end-to-end contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-RESEARCH.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-PATTERNS.md
@.planning/milestones/v2.11-phases/119-rendro-theme-core-module-the-one-way-door/119-02-SUMMARY.md
@.planning/milestones/v2.11-phases/122-typography-type-scale-application-font-role-leading-wiring/122-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render one Swiss invoice through the real explicit-font path</name>
  <reversibility rating="costly">The canonical preset/2 and document-first register_fonts/2 shapes are public contracts locked by D-01 and D-05.</reversibility>
  <read_first>
    - .planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
    - lib/rendro/theme.ex
    - lib/rendro/document.ex
    - lib/rendro/font_registry.ex
    - lib/rendro/recipes/invoice.ex
    - test/rendro/theme_test.exs
    - test/rendro/recipes/invoice_byte_identity_test.exs
  </read_first>
  <files>lib/rendro/theme.ex, lib/rendro/theme/presets.ex, priv/fonts/inter/Inter-Regular.ttf, priv/fonts/jetbrains-mono/JetBrainsMono-Regular.ttf, test/rendro/theme/presets_test.exs</files>
  <behavior>
    - "Strict Swiss construction returns the exact D-10 light theme, then applies existing dark semantics last; equivalent option orderings compare equal (D-01..D-03, D-10)."
    - "Missing/malformed accent, non-keyword opts, strings/aliases, unknown keys, modes, and densities raise errors naming the invalid value and canonical choices (D-01, D-02, D-04)."
    - "The unregistered invoice returns the existing typed missing-role error; explicit Swiss registration embeds grotesque and mono and renders non-empty deterministic PDF bytes (D-05, D-15, D-24, D-26)."
    - "Repeated identical registration is idempotent, unequal caller-owned collisions are preserved and rejected, and two documents remain isolated (D-05)."
  </behavior>
  <action>Write the failing integration tests first. Add `lib/rendro/theme/presets.ex` and exactly one readable `Rendro.Theme.preset/2` delegation. Implement the D-01 canonical-atom boundary with no `/1`, D-02 keyword/color validation, D-03 light-first `Theme.resolve/1` then `Theme.dark/1`, D-04 actionable errors, the exact D-10 Swiss literals, and the D-15 grotesque/mono roles. Vendor the official unmodified Inter 4.1 and JetBrains Mono 2.304 static Regular TTF bytes at the locked paths after verifying release/member/hash data. Implement `register_fonts(document, :swiss)` through existing registry/document APIs: register only the two required descriptors, accept exact descriptor equality idempotently, and reject unequal collisions without overwrite. Exercise the exact caller order `Theme.preset/2` → `Invoice.document/2` → `Presets.register_fonts/2` → `Rendro.render/1`; keep the pre-registration typed error and no-theme bytes unchanged (D-05, D-24, D-26). Keep token tables, paths, and registry behavior out of `theme.ex` per D-07 and leave `priv/public_api.json` for Phase 129.</action>
  <verify>
    <automated>mix test test/rendro/theme/presets_test.exs test/rendro/recipes/invoice_byte_identity_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `Rendro.Theme.__info__(:functions)` exposes `preset: 2` and no `preset: 1`.
    - The tracer asserts the typed pre-registration error and a non-empty post-registration PDF with both curated roles embedded.
    - Idempotence, collision preservation, and independent-document registry assertions pass.
    - `theme.ex` contains only the narrow delegation and no genre table, font path, or registry mutation.
  </acceptance_criteria>
  <done>The Swiss path is production-usable end to end with strict inputs, explicit isolated font ownership, real embedded fonts, and unchanged legacy behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller configuration → preset resolver | Programmer-supplied values cross the public API. |
| vendored bytes → parser/renderer | Upstream binaries become local parser input and rendered resources. |
| caller document → curated registry bridge | Caller-owned and Rendro-owned roles share one namespace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-01 | Denial of service | tracer TTF preflight | high | mitigate | Accept only the two locked static TrueType faces and require parser/preflight success before registration. |
| T-125-03 | Tampering | register_fonts/2 | high | mitigate | Exact descriptor equality for idempotence; reject every unequal collision without overwrite. |
| T-125-05 | Information disclosure | public errors | low | accept | Show supplied values and canonical next action, never loader internals or secrets. |
</threat_model>

<verification>
Focused ExUnit proves the real end-to-end Swiss path, strict API shape, registry behavior, typed omission failure, embedding, repeat bytes, and unchanged Invoice golden behavior.
</verification>

<success_criteria>
- The tracer renders only after explicit font registration and uses the locked public call shapes.
- Invalid inputs and collisions fail actionably without mutation or fallback.
- No-theme Invoice bytes remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-01-SUMMARY.md` when done.
</output>
