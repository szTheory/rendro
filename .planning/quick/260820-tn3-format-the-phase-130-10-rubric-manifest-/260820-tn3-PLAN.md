---
type: quick-task
quick_id: 260820-tn3
slug: format-the-phase-130-10-rubric-manifest-
title: Format the Phase 130-10 rubric manifest contract test
repo: /Users/jon/projects/rendro
created: 2026-08-20
autonomous: true
scope:
  - Format only test/docs_contract/rubric_manifest_contract_test.exs
  - Preserve the Phase 130-10 contract behavior and assertions unchanged
files:
  - test/docs_contract/rubric_manifest_contract_test.exs
verification:
  - mix format --check-formatted test/docs_contract/rubric_manifest_contract_test.exs
  - mix test test/docs_contract/rubric_manifest_contract_test.exs
---

# Quick Task Plan

## Objective

Remove the formatter-only failure introduced in the Phase 130-10 rubric manifest contract test so the Phase 130-11 `mix ci.fast` gate can proceed without changing contract behavior.

## Constraints

- Keep production code, rubric data, schemas, assertions, and Phase 130 planning artifacts unchanged.
- Do not absorb or revert concurrent work; inspect the diff and stage only the scoped test file.
- Treat any verification failure beyond formatting as a blocker to report, not permission to broaden this quick task.

## Execution

### Task 1: Format, verify, and commit the rubric manifest contract test

Run `mix format test/docs_contract/rubric_manifest_contract_test.exs` against exactly the scoped file. Inspect `git diff -- test/docs_contract/rubric_manifest_contract_test.exs` and confirm the formatter changed layout only, with no assertion, test data, or contract semantics altered. Then run:

```bash
mix format --check-formatted test/docs_contract/rubric_manifest_contract_test.exs
mix test test/docs_contract/rubric_manifest_contract_test.exs
```

After both commands pass, stage only `test/docs_contract/rubric_manifest_contract_test.exs`, verify the staged diff remains formatting-only, and create one atomic code commit such as `style(130): format rubric manifest contract test`. Leave unrelated tracked and untracked changes untouched; the quick workflow may manage its own plan, summary, and state artifacts separately.

## Done When

- The scoped file passes `mix format --check-formatted`.
- The focused rubric manifest contract test passes.
- The source diff contains only formatter-produced layout changes in the scoped test file.
- The scoped code change is recorded in one atomic commit without staging unrelated work.
