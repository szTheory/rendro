---
type: quick-task
mode: quick-full
phase: 260820-u73-reconcile-all-twelve-phase-130-catalog-r
plan: 01
quick_id: 260820-u73
slug: reconcile-all-twelve-phase-130-catalog-r
title: Reconcile all twelve Phase 130 catalog review records and ratchet evidence contracts
repo: /Users/jon/projects/rendro
created: 2026-08-20
wave: 1
depends_on: []
autonomous: true
requirements: [CATALOG-07, CATALOG-09]
files_modified:
  - priv/schemas/rubric_scores.schema.json
  - dev/rendro/catalog.ex
  - test/docs_contract/rubric_manifest_contract_test.exs
  - test/docs_contract/catalog_quality_contract_test.exs
  - test/fixtures/quality/rubric_scores_phase130_non_prose.json
  - priv/quality/rubric_scores.json
  - priv/quality/SIGN-OFF.md
must_haves:
  truths:
    - "All twelve scored catalog records retain their exact Phase 130 IDs, hashes, scores, gates, passed values, reviewer/date, evidence references, resolution references, and canonical order while their 72 justification strings exactly transcribe the approved 130-05 full-size observations."
    - "The current Phase 130 catalog sign-off binds the reconciliation authority, candidate/source identity, route/ref/run identity, pinned PDFium identity, full artifact hashes, frozen threshold arithmetic, dark screen-only boundary, and bounded multipage observations, while Phase 127 is visibly historical and superseded."
    - "Schema, runtime validation, and contract tests fail closed when any scored catalog row—including passed:false rows—lacks a signer, date, any of the exact six justification keys, a resolution reference, or a superseded-evidence reference."
    - "A checked-in pre-fix snapshot independently preserves the complete rubric manifest after removing only the twelve scored `justifications` maps: immutable top-level fields, legacy `scores[]`, all 32 ordered catalog dispositions, all twenty exact unscored rows, and every scored non-prose field cannot drift."
    - "The fixed catalog and every generated catalog artifact remain byte-for-byte untouched; this task reconciles reviewer-owned evidence only and never runs catalog generation."
  artifacts:
    - path: "priv/quality/rubric_scores.json"
      provides: "Twelve current Phase 130 reviewer records with exact approved dimension prose"
    - path: "priv/quality/SIGN-OFF.md"
      provides: "Current Phase 130 catalog sign-off plus explicitly historical Phase 127 evidence"
    - path: "priv/schemas/rubric_scores.schema.json"
      provides: "Exact scored-disposition completeness and six-key justification schema"
    - path: "dev/rendro/catalog.ex"
      provides: "Runtime fail-closed validation for every scored disposition"
    - path: "test/docs_contract/rubric_manifest_contract_test.exs"
      provides: "Literal pinning of all twelve records and current sign-off evidence"
    - path: "test/docs_contract/catalog_quality_contract_test.exs"
      provides: "Runtime mutation tests for incomplete failed and passed scored rows"
    - path: "test/fixtures/quality/rubric_scores_phase130_non_prose.json"
      provides: "Independent pre-fix baseline for the complete manifest surface with only scored justification maps omitted"
  key_links:
    - from: ".planning/phases/130-catalog-quality-evidence-ratchet/130-05-SUMMARY.md"
      to: "priv/quality/rubric_scores.json"
      via: "Exact per-cell IA/CH/DF/RA/TC/RC transcription from the approved full-size review"
    - from: "priv/schemas/rubric_scores.schema.json"
      to: "dev/rendro/catalog.ex"
      via: "Identical all-scored completeness contract and exact six justification keys"
    - from: "priv/quality/rubric_scores.json"
      to: "priv/quality/SIGN-OFF.md"
      via: "Canonical twelve-ID order, full current hashes, verdicts, and dark boundaries"
    - from: "test/docs_contract/rubric_manifest_contract_test.exs"
      to: "priv/quality/rubric_scores.json"
      via: "Literal expected records rather than self-derived assertions"
    - from: "test/fixtures/quality/rubric_scores_phase130_non_prose.json"
      to: "priv/quality/rubric_scores.json"
      via: "Whole-manifest equality after deleting exactly the twelve scored justification maps"
---

<objective>
Reconcile the canonical catalog reviewer evidence to Jon's already approved Phase 130 full-size review, then make incomplete or stale scored evidence fail closed in both schema and runtime validation.

Purpose: Close the Phase 130 verification contradiction without re-reviewing, regenerating, or changing the fixed catalog. The authority is the locked approval in `130-05-SUMMARY.md`; this plan transcribes it exactly and prevents the same Phase 127 prose drift from recurring.

Output: Twelve truthful reviewer records, a current Phase 130 SIGN-OFF index, and TDD contracts that pin their identities, narratives, ordering, and completeness.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-VERIFICATION.md
@.planning/phases/130-catalog-quality-evidence-ratchet/130-05-SUMMARY.md
@priv/quality/rubric_scores.json
@priv/quality/SIGN-OFF.md
@priv/schemas/rubric_scores.schema.json
@dev/rendro/catalog.ex
@test/docs_contract/rubric_manifest_contract_test.exs
@test/docs_contract/catalog_quality_contract_test.exs
</context>

<constraints>
- Treat `130-05-SUMMARY.md` as the approved human-review authority. Do not reinterpret scores, prose, verdicts, or visual findings and do not perform a new review.
- Do not run `mix rendro.catalog.gen`, edit `assets/rendro/catalog.json`, edit any file under `assets/rendro/catalog/`, or change catalog generation/projection behavior.
- In `rubric_scores.json`, change only the 72 justification string values across the twelve scored records. Preserve every other byte-significant data value: immutable top-level fields, 32-record order, IDs, identity fields, evidence paths, both full hashes, recorded/signed dates, reviewer, scores, gates, passed values, resolution references, superseded references, the twenty exact unscored rows, and all legacy `scores[]` data. Prove this against a separate pre-fix fixture; never derive the expected preservation value from the post-fix manifest.
- Preserve the deterministic/advisory boundary. Human prose and SIGN-OFF are evidence contracts, never deterministic catalog authority; dark output remains screen-oriented, `print_safety: false`, and non-print-safe.
- Preserve concurrent work and the untracked `130-VERIFICATION.md`. Stage only files owned by the current atomic task and never absorb unrelated changes.
</constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make every scored catalog evidence record fail closed</name>
  <files>priv/schemas/rubric_scores.schema.json, dev/rendro/catalog.ex, test/docs_contract/rubric_manifest_contract_test.exs, test/docs_contract/catalog_quality_contract_test.exs, test/fixtures/quality/rubric_scores_phase130_non_prose.json</files>
  <behavior>
    - Schema accepts `justifications` only when it contains exactly the six dimension keys `information_architecture`, `content_hierarchy`, `domain_fit`, `reader_affordances`, `typographic_craft`, and `restraint_cohesion`, each mapped to a non-empty string; missing, blank, or extra keys fail.
    - Every `review_status: scored` row requires non-empty `signed_off_by`, a valid ISO calendar date in `signed_off_at`, complete exact-key justifications, non-empty `resolution_ref`, and non-empty `supersedes_evidence_ref`, regardless of `passed` being true or false.
    - `Catalog.quality_contract_errors/2` enforces the same completeness contract at runtime and reports the affected catalog ID for each mutation.
    - The valid checked-in manifest continues to pass schema and runtime validation with unchanged threshold arithmetic and dark boundaries.
    - After removing only each scored row's `justifications` map, the entire live manifest exactly equals a separate checked-in snapshot created from the pre-fix baseline, including ordered all-32 dispositions, twenty exact unscored records, legacy `scores[]`, and immutable top-level fields.
  </behavior>
  <action>Before editing `rubric_scores.json`, create `test/fixtures/quality/rubric_scores_phase130_non_prose.json` from the current pre-fix manifest by deleting exactly the `justifications` map from each of the twelve scored dispositions and nothing else. Check in the resulting full manifest snapshot; do not generate it during tests and do not update it after evidence transcription. Add a contract that performs the same twelve-path deletion on the live manifest and compares the complete decoded result to this independent fixture. This equality must cover every immutable top-level field, legacy `scores[]`, the exact ordered 32 `catalog_dispositions`, all twenty exact unscored rows, and every scored non-prose field, so only the twelve justification maps are permitted to differ.

Start RED by extending both contract suites with table-driven mutations applied separately to one real `passed: true` Phase 130 row and one real `passed: false` Phase 130 row. For each row: delete and blank each required evidence field (`signed_off_by`, `signed_off_at`, `resolution_ref`, `supersedes_evidence_ref`, and the `justifications` object); set `signed_off_at` to a non-date and an impossible ISO calendar date; delete each of the six justification keys one at a time; blank each of the six justification values one at a time; and add one extra key. Assert every mutation fails schema validation and `Catalog.quality_contract_errors/2`, with every runtime error containing the mutated catalog ID. Use the actual complete rows as fixtures so pass arithmetic, hashes, and identity binding remain realistic. Then implement GREEN in `rubric_scores.schema.json`: define an exact justification object with the six required non-empty string properties and `additionalProperties: false`, reference it from catalog dispositions, and require signer/date/justifications/resolution/supersedes for every scored row instead of only promotions. Mirror the full contract in `Catalog.review_status_errors/2`, including real ISO calendar-date validation, exact justification key equality, nonblank values, and closure references for both verdicts. Do not weaken hash, threshold, evidence-binding, or unscored behavior. Keep validation pure and additive—no catalog generation or reviewer-data mutation. Refactor shared mutation helpers only after RED and GREEN are demonstrated.</action>
  <verify>
    <automated>mix format --check-formatted dev/rendro/catalog.ex test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs &amp;&amp; mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The exact six-key justification schema and complete signer/date/justifications/resolution/supersedes contract reject every missing, blank, malformed-date, deleted-key, blank-value, and extra-key mutation for both a real passing and failing scored row in schema and runtime validation with ID-bearing errors; the independent pre-fix fixture proves the remainder of the complete manifest is immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transcribe all twelve approved records and publish the current Phase 130 sign-off</name>
  <files>priv/quality/rubric_scores.json, priv/quality/SIGN-OFF.md, test/docs_contract/rubric_manifest_contract_test.exs</files>
  <behavior>
    - The twelve scored rows occur in the exact canonical order Invoice light/dark, Statement light/dark, Receipt light/dark, Certificate light/dark, Payslip light/dark, Ticket light/dark and match every identity/hash/score/gate/verdict/reviewer/date/ref plus all 72 approved justification strings from `130-05-SUMMARY.md`.
    - Shorthand in the approved summary resolves deterministically: Certificate dark copies all six Certificate light dimension strings; Payslip dark copies Payslip light IA/CH/DF and uses its own explicit RA/TC/RC strings; Ticket dark copies Ticket light IA/CH/DF and uses its own explicit RA/TC/RC strings. Boundary sentences remain gate/SIGN-OFF evidence rather than a seventh justification key.
    - The Humanist dark justifications state that the prior contrast deficit is visibly repaired and that its only remaining threshold boundary is `print_safety: false`; stale Phase 127 defect prose cannot satisfy the contract.
    - The current Phase 130 SIGN-OFF section contains all twelve IDs and full PNG/source-PDF SHA-256 values, four light passes, eight `needs_work` results, all six dark `print_safety: false` boundaries, and frozen threshold arithmetic.
    - SIGN-OFF identifies reconciliation SHA `1646eeb8875cc67d7d452d3f28bc2b0d6503f943a2a6775f7e256a3e51bb3f22`, candidate/source SHA `411cdcafa5d3090f3d0ec144c0cba59d991ba99f`, route/ref `30657d92cf8be49f30094c57aaf163b76bd0ad9c` / `gsd/phase130-candidate-route-411cdcafa5d3090f3d0ec144c0cba59d991ba99f`, run `32417257428` attempt `1` job `candidate-evidence`, `pdfium-render`, `pdfium-cli v0.11.0`, and executable SHA `b1e7f3dd8d6c77e0eb8e67c6a33de4efa5de9f38d87263c151acb88994ae160a`.
    - The bounded multipage note records Statement closure at `$7,500` and states that supplied Invoice endpoints show lines 1–40 and 42–65, so line 41 is neither asserted nor denied and remains deterministic-test evidence only.
    - The Phase 127 section remains present but is explicitly labeled historical and superseded by the Phase 130 section; it cannot read as the current catalog disposition.
  </behavior>
  <action>Start RED by replacing the broad Phase 130 test map with a literal expected record set sourced from `130-05-SUMMARY.md`. Resolve all shorthand before forming that expected map: (1) Certificate dark copies the exact six Certificate light IA/CH/DF/RA/TC/RC strings because the approved record says all six observations hold on screen; (2) Payslip dark copies the exact Payslip light IA, CH, and DF strings, then uses the three explicit dark RA, TC, and RC sentences; (3) Ticket dark copies the exact Ticket light IA, CH, and DF strings, then uses the three explicit dark RA, TC, and RC sentences. Do not append the dark `print_safety` sentence to any justification value and do not invent a seventh key; preserve it in `gate_results` and SIGN-OFF. Every other row maps each labeled IA/CH/DF/RA/TC/RC sentence directly to its long-form key. Materialize all 72 resolved strings as literal expected values in the test—the test must not read or parse `130-05-SUMMARY.md` at runtime and must not derive expected prose from `rubric_scores.json`.

Pin every scored record's canonical position, ID, family/brand/preset/mode, evidence path, full PNG/PDF hashes, recorded/reviewer dates, reviewer, six scores, two gates, computed `passed`, resolution and superseded references, and the literal resolved justifications. In addition to the Task 1 whole-manifest fixture equality, add SIGN-OFF assertions that locate the current Phase 130 heading before the historical Phase 127 heading; pin all twelve table rows in canonical order with full hashes, provenance identities, frozen thresholds, four-pass/eight-needs-work arithmetic, six dark boundaries, and the bounded `$7,500`/Invoice line-41 language. Ensure these tests fail against the stale Phase 127 prose and absent current section.

Then change only the twelve `justifications` objects in `rubric_scores.json`, using the same resolved 72-string map. Run the independent full-manifest preservation test immediately after transcription; any mismatch outside those twelve maps is a blocker and must be reverted, not blessed into the fixture. In `SIGN-OFF.md`, insert a current Phase 130 catalog review section using the complete identities and full hashes from summary lines 51–178, with the twelve rows in canonical order. State the exact pass formula, four light promotions, eight retained `needs_work` rows, all dark screen/non-print boundaries, and the bounded multipage observation. Relabel the existing Phase 127 catalog flagship section as historical and superseded without deleting or rewriting its historical records. Do not copy stale Phase 127 findings into the current section and do not duplicate the rubric JSON as a second editable prose source beyond the concise bounded findings needed for the sign-off index.</action>
  <verify>
    <automated>mix format --check-formatted test/docs_contract/rubric_manifest_contract_test.exs &amp;&amp; mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1 &amp;&amp; mix rendro.catalog.check &amp;&amp; git diff --exit-code -- assets/rendro/catalog.json assets/rendro/catalog</automated>
  </verify>
  <done>All twelve canonical rows and the deterministic shorthand expansion are literally pinned to the approved Phase 130 identities and 72 resolved narratives; the independent fixture proves every other manifest value unchanged; SIGN-OFF makes Phase 130 current and Phase 127 historical; the Humanist dark contradiction is gone; and no catalog artifact changed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the evidence ratchet through the full deterministic gate</name>
  <files>priv/schemas/rubric_scores.schema.json, dev/rendro/catalog.ex, test/docs_contract/rubric_manifest_contract_test.exs, test/docs_contract/catalog_quality_contract_test.exs, test/fixtures/quality/rubric_scores_phase130_non_prose.json, priv/quality/rubric_scores.json, priv/quality/SIGN-OFF.md</files>
  <action>Inspect the complete scoped diff. Confirm JSON changes are confined to the twelve justification objects; the independent fixture was captured from the pre-fix manifest with only those maps deleted and was not refreshed after transcription; SIGN-OFF adds the current Phase 130 section and marks Phase 127 historical; schema/runtime changes only strengthen validation; and tests use a literal 72-string map rather than deriving expected values from the files under test. Confirm both real verdict classes receive the complete table-driven mutation matrix with ID-bearing runtime assertions. Confirm there is no diff under `assets/rendro/catalog.json` or `assets/rendro/catalog/`, no generated file, no new dependency, and no unrelated Phase 130 artifact staged. Run the focused suites first, then the catalog checker, then `mix ci.fast`. If a gate fails, fix only an issue caused by these seven files; report an unrelated failure rather than broadening scope or regenerating catalog assets. Commit the contract ratchet and evidence reconciliation atomically according to the quick workflow, preserving all concurrent changes.</action>
  <verify>
    <automated>mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1 &amp;&amp; mix rendro.catalog.check &amp;&amp; mix ci.fast &amp;&amp; git diff --exit-code -- assets/rendro/catalog.json assets/rendro/catalog</automated>
  </verify>
  <done>Focused evidence contracts, full-manifest preservation, the symmetric pass/fail mutation matrix, catalog verification, and `mix ci.fast` pass; the final diff contains only the seven planned files and leaves catalog outputs unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved review summary to canonical evidence | Human-approved prose is transcribed into machine-checked reviewer records and public sign-off documentation. |
| Reviewer evidence to deterministic catalog projection | Advisory reviewer fields influence status projection but must not mutate or replace deterministic catalog identity/hash checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260820-u73-01 | Tampering | `rubric_scores.json` non-prose fields | high | mitigate | Literal twelve-record contract pins IDs, full hashes, scores, gates, verdicts, reviewer/date, refs, and order while the task changes justification values only. |
| T-260820-u73-02 | Repudiation | `SIGN-OFF.md` current authority | high | mitigate | Record full reconciliation, route/ref/run, renderer, reviewer/date, and artifact identities; label Phase 127 historical/superseded. |
| T-260820-u73-03 | Information disclosure / overclaim | Dark and multipage evidence | medium | mitigate | Preserve screen-only/non-print-safe language and the bounded Invoice line-41 limitation; make no accessibility, PDF/UA, WCAG, arbitrary-print, or universal pagination claim. |
| T-260820-u73-04 | Tampering | Catalog artifacts | high | mitigate | Forbid generation and require an automated zero-diff gate over the canonical catalog manifest and raster tree. |
</threat_model>

<source_audit>
| Source | ID | Required outcome | Task | Status |
|---|---|---|---|---|
| GOAL | Phase 130 | Truthful current dispositions while fixed catalog and advisory boundary remain intact | 1–3 | COVERED |
| REQ | CATALOG-06 | Preserve the already repaired exact twelve cells without catalog expansion | 3 | COVERED |
| REQ | CATALOG-07 | Humanist dark repair evidence is truthful and retains `print_safety: false` | 2 | COVERED |
| REQ | CATALOG-08 | Preserve deterministic 32-cell catalog integrity without regeneration or artifact changes | 2–3 | COVERED |
| REQ | CATALOG-09 | All twelve full-size review dispositions bind exact current artifacts and threshold misses remain `needs_work` | 2–3 | COVERED |
| RESEARCH | — | No research required; established schema/runtime/test patterns are already present | 1 | COVERED |
| CONTEXT | 130-05 approval | Exact approved twelve-record prose, identities, thresholds, dark boundary, and bounded multipage findings are locked | 2 | COVERED |
</source_audit>

<verification>
1. `mix format --check-formatted dev/rendro/catalog.ex test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs`
2. `mix test test/docs_contract/rubric_manifest_contract_test.exs test/docs_contract/catalog_quality_contract_test.exs --max-failures 1`
3. `mix rendro.catalog.check`
4. `mix ci.fast`
5. `git diff --exit-code -- assets/rendro/catalog.json assets/rendro/catalog`
</verification>

<success_criteria>
- All 72 stale Phase 127 justification strings are replaced by the exact approved Phase 130 dimension observations, with every non-prose record field unchanged.
- Certificate, Payslip, and Ticket dark shorthand is expanded by the explicit copy-forward rules and pinned as a literal per-ID/per-key 72-string expected map.
- SIGN-OFF contains a complete current Phase 130 catalog section with full identities, hashes, canonical order, thresholds, boundaries, and bounded multipage limitations; Phase 127 remains present as historical/superseded evidence.
- Schema and runtime validation require complete signer/valid-date/exact-six-key justifications/resolution/supersedes fields for every scored row; table-driven mutations cover missing, blank, malformed-date, deleted-key, blank-value, and extra-key failures for both one real passed and failed row, with runtime errors naming the ID.
- A separate pre-fix fixture proves exact equality for the complete manifest after removing only the twelve scored justification maps, including all immutable top-level fields, legacy `scores[]`, ordered all-32 dispositions, and twenty exact unscored rows.
- Literal tests pin all twelve records and the current SIGN-OFF section so stale prose, truncated hashes, reordered rows, omitted boundaries, or incomplete evidence fail closed.
- Focused tests, catalog check, and `mix ci.fast` pass with no catalog regeneration or artifact change.
</success_criteria>

<output>
Create `.planning/quick/260820-u73-reconcile-all-twelve-phase-130-catalog-r/260820-u73-SUMMARY.md` when done.
</output>
