---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "09"
type: execute
wave: 9
depends_on: ["131-08"]
files_modified:
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
  - .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
autonomous: false
requirements: [JOURNEY-01, JOURNEY-02, JOURNEY-04]
must_haves:
  truths:
    - "D-35: the exact 1.3.4 candidate is completely re-proven with no tag immediately before a fresh exact-SHA blocking-human decision."
    - "D-02/D-35: approval names the exact SHA and jointly authorizes annotated v1.3.4, protected release/Hex, and candidate-bound protected HexDocs; no prior approval transfers."
    - "D-05/D-35: protected tag-driven release and actual HEX_API_KEY publish succeed before candidate-bound HexDocs dispatch."
    - "D-04/D-35: the named verifier atomically writes VERIFIED only after exact tag/run/job/archive/HexDocs/source identities and all four immutable incidents match."
    - "D-30: public workflow/registry/docs observations remain advisory and separately labeled from deterministic preflight."
  artifacts:
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
      provides: "Fresh proof, literal exact-SHA decision, and protected public identities"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json
      provides: "Atomic exact-public-1.3.4 VERIFIED prerequisite"
    - path: .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
      provides: "Actual protected/advisory evidence mapped without lane conflation"
  key_links:
    - from: 131-RELEASE-CANDIDATE.md
      to: blocking-human approval
      via: "fresh same-SHA complete no-tag proof and control-only delta"
    - from: annotated v1.3.4 tag
      to: protected release workflow
      via: "push-tag trigger at the approved peeled SHA"
    - from: protected Hex success
      to: candidate-bound HexDocs dispatch
      via: "after package success, gh workflow run hexdocs.yml --ref v1.3.4 passes the exact candidate_commit_sha and release_ref inputs"
    - from: scripts/verify_public_release.exs
      to: 131-PUBLIC-PREREQUISITE.json
      via: "literal candidate-record/tag/release-run/hexdocs-run/output/--check-existing invocation writes atomically after exact public and immutable-history checks"
---

<objective>
Freshly approve and publish the exact v1.3.4 candidate through protected paths, then atomically verify its public package and docs.

Purpose: make the one-way release decision explicit and give the clean-room journey one exact, auditable public prerequisite.
Output: literal approval, protected release/Hex and HexDocs identities, exact-public-1.3.4 VERIFIED prerequisite, and updated advisory validation—or a fail-closed immutable incident stop.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-08-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md
@.planning/debug/v133-hex-dry-run.md
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@scripts/release_preflight_proof.exs
@scripts/verify_public_release.exs

<interfaces>
The candidate record supplies one exact 1.3.4 SHA and checksum. `release.yml` accepts a pushed `v*.*.*` tag, validates via complete credential-free preflight, and exposes `HEX_API_KEY` only to protected actual publish. `hexdocs.yml` dispatch inputs are `candidate_commit_sha` and `release_ref`. `Rendro.PublicReleaseVerifier` consumes the candidate, exact tag, release run ID, HexDocs run ID, output path, and `--check-existing`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Repeat complete exact-SHA no-tag proof immediately before approval</name>
  <precondition>Plan 131-08 sealed one exact private 1.3.4 candidate containing bbe75d2, and no local/remote v1.3.4 tag or public 1.3.4 surface exists.</precondition>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md</files>
  <read_first>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-08-SUMMARY.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md, .planning/debug/v133-hex-dry-run.md, scripts/release_preflight_proof.exs, .github/workflows/release.yml</read_first>
  <action>Immediately before approval, reassert a clean release-bearing tree, exact candidate identity, and bbe75d2 ancestry. Snapshot every local/remote tag ref and prove v1.3.4 plus public Hex/HexDocs 1.3.4 absent. Re-run the detached exact-SHA proof with focused regressions, open-silent FIFO, `mix ci.fast`, docs contract, isolated tutorial, package checksum/inventory/unpack, both audits, and complete candidate-SHA preflight. Confirm the workflow still uses preflight as its credential-free dry-run validation and reserves `HEX_API_KEY` for actual publish. Remove the worktree, prove refs unchanged, verify `candidate..HEAD` is control-only, and refresh the packet with current evidence and `pending_blocking_human`. Stop on any difference and do not create even a temporary tag.</action>
  <verify>
    <automated>mix test test/scripts/release_preflight_proof_test.exs test/mix/tasks/release_preflight_test.exs test/guardrails/required_checks_contract_test.exs test/scripts/public_release_verifier_test.exs test/mix/tasks/rendro_livebook_check_test.exs --max-failures 1 &amp;&amp; mix ci.fast &amp;&amp; mix docs.contract &amp;&amp; mix rendro.livebook.check &amp;&amp; mix hex.build &amp;&amp; mix hex.audit &amp;&amp; mix deps.audit &amp;&amp; rg -q '^version: 1\.3\.4$' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md &amp;&amp; rg -q '^approval_status: pending_blocking_human$' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md</automated>
  </verify>
  <acceptance_criteria>The packet is current, exact-SHA bound, bbe75d2-derived, complete-proof green, ref-invariant, public-absence checked, control-only after capture, and pending a new human decision.</acceptance_criteria>
  <done>The exact candidate is freshly revalidated and immediately ready for a one-way decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve or reject exact v1.3.4 one-way publication</name>
  <decision>Whether to authorize annotated tag v1.3.4 at the exact candidate SHA in the freshly generated packet, protected tag-driven release and Hex publication, and exact-candidate protected HexDocs publication as one indivisible decision.</decision>
  <context>D-02 and D-35 classify public tag/package/docs mutation as one-way. v1.3.0 through v1.3.3 are immutable failed history. Their approvals, generic assent, silence, partial approval, or auto-advance cannot authorize v1.3.4.</context>
  <options>
    <option id="approve-exact-1.3.4">
      <name>Approve exact candidate</name>
      <pros>Allows the existing protected paths to publish the completely revalidated candidate and unblock clean-room proof.</pros>
      <cons>Creates irreversible public tag/package/docs state; any protected failure becomes new immutable history.</cons>
    </option>
    <option id="reject-or-hold">
      <name>Reject or hold</name>
      <pros>Leaves public state unchanged while the candidate or proof is investigated.</pros>
      <cons>JOURNEY-01 through JOURNEY-04 remain blocked.</cons>
    </option>
  </options>
  <resume-signal>Type `approve exact v1.3.4 candidate &lt;40-character SHA&gt; for annotated tag, protected release and Hex, and candidate-bound HexDocs` or `reject` with the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Publish in protected order and atomically verify exact public 1.3.4</name>
  <precondition>The immediately preceding blocking-human response exactly approves the same candidate SHA recorded by Task 1 for annotated v1.3.4, protected release/Hex, and candidate-bound HexDocs together.</precondition>
  <reversibility rating="one-way">The annotated tag and public Hex/HexDocs artifacts cannot be erased as though publication never occurred.</reversibility>
  <files>.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-VALIDATION.md</files>
  <read_first>literal Task 2 response, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-APPROVAL-PACKET.md, .github/workflows/release.yml, .github/workflows/hexdocs.yml, scripts/verify_public_release.exs, .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-06-SUMMARY.md</read_first>
  <action>Record the literal approval and prove it names the packet SHA before mutation. Create one annotated `v1.3.4` tag at that SHA and push only that tag. Capture the unique protected release run as `release_run_id` plus its validate/publish jobs; require exact candidate `head_sha`, push event, workflow identity, successful version match, CI, complete preflight, and actual protected Hex publish. Do not expect or add a standalone validation dry run; authorization is tested only by the actual protected publish using `HEX_API_KEY`. If any step fails or skips unexpectedly, stop and retain the new tag/run as immutable evidence without retry, ref mutation, alternate publisher, or docs dispatch. After exact Hex 1.3.4 and archive identity are public, set `candidate_sha` from the sealed candidate and execute exactly `gh workflow run hexdocs.yml --ref v1.3.4 -f candidate_commit_sha="$candidate_sha" -f release_ref=v1.3.4`. Identify the unique new HexDocs run created by that command as `hexdocs_run_id`; require `head_sha == candidate_sha`, event `workflow_dispatch`, workflow `hexdocs.yml`, exact `v1.3.4` ref and `candidate_commit_sha`/`release_ref` inputs wherever GitHub exposes them, and successful `publish-hexdocs` completion. Then execute exactly `mix run scripts/verify_public_release.exs -- --candidate-record .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RELEASE-CANDIDATE.md --tag v1.3.4 --release-run-id "$release_run_id" --hexdocs-run-id "$hexdocs_run_id" --output .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json --check-existing`. The verifier may atomically write the prerequisite only after tag object/peeled SHA, release/HexDocs runs, Hex version/archive checksum/inventory, versioned docs source/symbols, and all four older incidents match. Update validation with actual identities and retain deterministic/advisory labels.</action>
  <verify>
    <automated>mix test test/scripts/public_release_verifier_test.exs test/guardrails/required_checks_contract_test.exs test/docs_contract/phoenix_newcomer_contract_test.exs --max-failures 1 &amp;&amp; test -f .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json &amp;&amp; rg -q '"public_prerequisite":"VERIFIED"' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json &amp;&amp; rg -q '"version":"1.3.4"' .planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-PUBLIC-PREREQUISITE.json</automated>
  </verify>
  <acceptance_criteria>Fresh approval precedes one exact tag; protected release and actual Hex publish succeed before the literal candidate-bound HexDocs dispatch; the unique workflow_dispatch run has candidate head/ref/input identity and publish-hexdocs success; the literal verifier command atomically records exact public 1.3.4 plus all four immutable failures.</acceptance_criteria>
  <done>Exact public 1.3.4 is VERIFIED through approved protected paths, or execution stopped fail-closed with no retry or alternate route.</done>
</task>

</tasks>

## Artifacts this phase produces

- One annotated `v1.3.4` tag only after exact approval, if approval and protected execution succeed.
- Exact protected release and HexDocs run/job identities.
- Atomic `131-PUBLIC-PREREQUISITE.json` schema instance for exact public 1.3.4.
- No new publishing path, dependency, public API, or mutation of v1.3.0-v1.3.3.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Private candidate → human decision | Deterministic evidence becomes one-way authorization. |
| Approved SHA → public tag | Exact commit becomes immutable public identity. |
| Protected jobs → Hex/HexDocs | Credentials mutate external registries. |
| Public services → prerequisite | Advisory external facts enter bounded atomic evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-56 | Elevation of Privilege | approval gate | critical | mitigate | Exact phrase binds SHA and all mutations; auto mode and prior approvals cannot advance. |
| T-131-57 | Tampering | pre-approval state | critical | mitigate | Repeat complete no-tag proof, full ref snapshots, public absence, and control-only delta. |
| T-131-58 | Information Disclosure | HEX_API_KEY | critical | mitigate | Protected environment supplies key only to actual publish; validation remains credential-free. |
| T-131-59 | Spoofing | public identities | critical | mitigate | Verify workflow/event/head/inputs, tag object/peeled SHA, archive/docs source, and exact IDs atomically. |
</threat_model>

<verification>
Fresh complete proof and literal approval precede mutation. Protected release/Hex precedes HexDocs, which precedes one atomic verifier. Any mismatch stops without retry or alternate publication.
</verification>

<success_criteria>
The approved exact candidate either stays private after rejection/failure-before-tag or becomes exact public 1.3.4 with a VERIFIED prerequisite; v1.3.0-v1.3.3 remain untouched immutable failures.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-09-SUMMARY.md` when done with literal approval and exact public identities.
</output>
