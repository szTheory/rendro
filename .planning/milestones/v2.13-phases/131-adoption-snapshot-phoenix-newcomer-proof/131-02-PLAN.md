---
phase: 131-adoption-snapshot-phoenix-newcomer-proof
plan: "02"
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/hexdocs.yml
  - test/guardrails/required_checks_contract_test.exs
autonomous: true
requirements: [JOURNEY-01, JOURNEY-02]
must_haves:
  truths:
    - "D-05 and D-32: both existing protected workflow parser consumers select exactly one top-level @version declaration and fail on zero or multiple declarations."
    - "Deterministic contracts reproduce the v1.3.0 declaration-plus-source_ref multiline failure independently against release.yml and hexdocs.yml."
    - "No release target, tag, package, docs publication, candidate record, or external state changes in this parser-only plan."
  artifacts:
    - path: .github/workflows/release.yml
      provides: "Protected tag-driven Hex workflow with exact-one declaration extraction"
    - path: .github/workflows/hexdocs.yml
      provides: "Candidate-bound protected HexDocs workflow with the same extraction contract"
    - path: test/guardrails/required_checks_contract_test.exs
      provides: "Per-workflow multiline, zero-match, and duplicate-declaration regression contracts"
  key_links:
    - from: test/guardrails/required_checks_contract_test.exs
      to: .github/workflows/release.yml
      via: "original two-line fixture plus zero/multiple failure cases"
    - from: test/guardrails/required_checks_contract_test.exs
      to: .github/workflows/hexdocs.yml
      via: "the same behavior asserted independently for the docs workflow consumer"
  prohibitions:
    - statement: "This plan may not create, delete, move, overwrite, or retry any tag, nor publish or dispatch Hex/HexDocs."
      status: unverified
      flagged: true
    - statement: "The repair may not introduce a new release framework or alternate publication path."
      status: unverified
      flagged: true
---

<objective>
Repair exact-one project-version extraction in both protected workflows and lock the v1.3.0 multiline incident into deterministic regression contracts.

Purpose: remove the parser failure from every protected workflow consumer before exact v1.3.1 candidate preparation begins.
Output: repaired release/HexDocs workflow parsers and focused deterministic guardrails only.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-01-SUMMARY.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-CONTEXT.md
@.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-RESEARCH.md
@.github/workflows/release.yml
@.github/workflows/hexdocs.yml
@test/guardrails/required_checks_contract_test.exs

<interfaces>
`.github/workflows/release.yml` and `.github/workflows/hexdocs.yml` both consume the project version. Each must select only one top-level `@version` string declaration, reject zero/multiple declarations, and expose the one extracted value to its existing parity gate.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair and regression-test both protected workflow parsers</name>
  <reversibility rating="reversible">The parser changes and tests are repository-local and precede every release candidate or external mutation.</reversibility>
  <files>.github/workflows/release.yml, .github/workflows/hexdocs.yml, test/guardrails/required_checks_contract_test.exs</files>
  <read_first>131-CONTEXT.md D-05/D-32; failed run 32513353551 evidence; every project-version extraction in release.yml and hexdocs.yml; workflow parsing patterns in required_checks_contract_test.exs</read_first>
  <behavior>
    - "release.yml selects only a top-level @version string declaration and fails diagnostically when the count is zero or greater than one."
    - "hexdocs.yml implements the same exact-one semantics before candidate/version parity and likewise fails on zero or multiple declarations."
    - "For each workflow, a fixture containing @version 1.3.0 plus source_ref interpolation reproduces the former broad-match multiline value and proves the repaired parser returns only 1.3.0."
    - "Both workflow consumers have independent zero-declaration and duplicate-declaration failure coverage."
  </behavior>
  <action>Per D-32, keep both workflows and replace every broad token-occurrence version parser in `.github/workflows/release.yml` and `.github/workflows/hexdocs.yml` with identical observable semantics for exactly one top-level `@version` string declaration. Each consumer must count declarations, emit a useful diagnostic, and exit nonzero unless the count is one before using the extracted value in existing parity checks. Extend `test/guardrails/required_checks_contract_test.exs` so the exact v1.3.0-era declaration and interpolated `source_ref` lines are exercised separately against release.yml and hexdocs.yml: prove the prior consumer would return multiple lines, require the repaired consumer to return only `1.3.0`, and require zero/duplicate declarations to fail for both. Parse both workflow files so coverage of one cannot mask a regression in the other. Do not change exact release targets, project/docs/package/verifier/candidate files, or external state.</action>
  <acceptance_criteria>Both protected workflows implement the same exact-one declaration behavior, every parser consumer is covered by the original multiline fixture plus zero/multiple cases, and neither workflow can reach parity or publication with an empty or multiline project version.</acceptance_criteria>
  <verify>
    <automated>mix test test/guardrails/required_checks_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The v1.3.0 parser failure is regression-protected in every protected workflow that reads the project version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix.exs declaration → protected workflow parsers | Ambiguous extraction could misidentify a release or docs version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-14 | Repudiation | release/HexDocs version parsers | high | mitigate | Both workflows reproduce the original multiline input and enforce exactly one declaration with zero/multiple failure. |
| T-131-08 | Tampering | protected workflow definitions | high | mitigate | Parsed tests inspect both workflow consumers and preserve their existing protected publication paths. |
</threat_model>

<verification>
Run the focused guardrail contract and confirm both workflow consumers independently satisfy the multiline, zero, duplicate, and one-declaration cases.
</verification>

<success_criteria>
Every protected workflow parser rejects ambiguous project versions without changing any release candidate or external state.
</success_criteria>

<output>
Create `.planning/phases/131-adoption-snapshot-phoenix-newcomer-proof/131-02-SUMMARY.md` when done; retain run `32513353551` as the failed regression source.
</output>
