---
phase: 125-foundation-curated-fonts-style-genre-presets-brand-fixtures
plan: 02
type: execute
wave: 2
depends_on: [125-01]
files_modified:
  - lib/rendro/theme/presets.ex
  - lib/rendro/pdf/font_subsetter.ex
  - priv/fonts/source-sans-3/SourceSans3-Regular.ttf
  - priv/fonts/source-serif-4/SourceSerif4-Regular.ttf
  - NOTICE
  - mix.exs
  - test/rendro/theme/preset_fonts_test.exs
  - test/rendro/pdf/font_subsetter_test.exs
  - test/docs_contract/preset_fonts_package_contract_test.exs
autonomous: true
requirements: [FONT-01, FONT-02, FONT-03, FONT-04, FONT-05]
must_haves:
  truths:
    - "All four locked unmodified static Regular TTFs pass fail-closed parser/preflight checks and map to stable D-15 role descriptors (D-13..D-17)."
    - "Four delimited NOTICE blocks bind exact upstream identity and SHA-256 to the four package paths, all of which appear in the Hex archive (D-16, D-17)."
    - "Empty and permuted/duplicated equal glyph sets produce parseable byte-identical subsets for every face (D-18)."
  artifacts:
    - path: test/rendro/theme/preset_fonts_test.exs
      provides: "Four-face parser, embedding, descriptor, and provenance contract"
    - path: test/docs_contract/preset_fonts_package_contract_test.exs
      provides: "Positive Hex archive proof"
    - path: lib/rendro/pdf/font_subsetter.ex
      provides: "Defensive sorted/deduplicated glyph boundary"
  key_links:
    - from: lib/rendro/theme/presets.ex
      to: priv/fonts
      via: "four Application.app_dir descriptors"
      pattern: "rendro_preset_(grotesque|humanist_sans|text_serif|mono)"
    - from: lib/rendro/pdf/font_subsetter.ex
      to: test/rendro/pdf/font_subsetter_test.exs
      via: "equal-set byte determinism"
      pattern: "Enum.(uniq|sort)"
---

<objective>
Complete the trusted four-face package and deterministic subsetting foundation after the tracer proves the architecture.

Purpose: make font provenance, parser compatibility, package delivery, role mapping, and subset equality independently auditable.
Output: two remaining binaries, four-face descriptors/provenance/package proof, and normalized subset behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-RESEARCH.md
@.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete four-face provenance, descriptors, and Hex packaging</name>
  <read_first>
    - .planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-CONTEXT.md
    - lib/rendro/theme/presets.ex
    - lib/rendro/pdf/font_parser.ex
    - lib/rendro/font_registry.ex
    - test/docs_contract/examples_schema_contract_test.exs
    - mix.exs
    - NOTICE
  </read_first>
  <files>lib/rendro/theme/presets.ex, priv/fonts/source-sans-3/SourceSans3-Regular.ttf, priv/fonts/source-serif-4/SourceSerif4-Regular.ttf, NOTICE, mix.exs, test/rendro/theme/preset_fonts_test.exs, test/docs_contract/preset_fonts_package_contract_test.exs</files>
  <behavior>
    - "The four D-13 files are non-empty unmodified static TrueType faces with required tables, cmap/metrics, and embedding permission; unsupported inputs fail closed (D-13, D-14, D-17)."
    - "Each computed SHA-256 matches its own delimited NOTICE block containing every D-16 field."
    - "Hex archive inspection finds all four exact paths and NOTICE (D-17/FONT-03)."
  </behavior>
  <action>Write failing four-face and archive contracts first. Vendor the locked Source Sans 3 3.052R and Source Serif 4 4.005R static Regular TTFs, then audit them together with the tracer faces. Record official project/foundry, copyright, OFL 1.1 text/reference, exact tag, commit, immutable URL, archive member/original filename, computed SHA-256, unmodified-byte statement, and retained RFN in four stable delimited NOTICE blocks (D-13..D-17). Reject variable-only, WOFF/WOFF2, TTC, CFF/OTTO, restricted-embedding, missing-table, and parser/preflight-failing candidates. Add `priv/fonts` to `mix.exs` package files and inspect the built tarball for all exact paths and NOTICE. Complete all four role descriptors and genre role sets in `Presets` without automatic registration (D-14, D-15).</action>
  <verify>
    <automated>mix test test/rendro/theme/preset_fonts_test.exs test/docs_contract/preset_fonts_package_contract_test.exs --max-failures 1 &amp;&amp; mix hex.build &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>All four exact faces are preflighted, provenance-bound, role-mapped, and positively present in the Hex package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize glyph sets at the subsetter boundary</name>
  <read_first>
    - lib/rendro/pdf/font_subsetter.ex
    - test/rendro/pdf/font_subsetter_test.exs
    - test/rendro/theme/preset_fonts_test.exs
  </read_first>
  <files>lib/rendro/pdf/font_subsetter.ex, test/rendro/pdf/font_subsetter_test.exs</files>
  <behavior>
    - "For every curated face, duplicates and permutations of an equal glyph-ID set produce identical bytes and parse successfully (D-18)."
    - "Empty input deterministically retains the required `.notdef` closure and repeated runs contain no timestamp variance (D-18)."
  </behavior>
  <action>Extend four-face subset tests first, then sort and deduplicate `used_glyphs` inside `FontSubsetter.subset/2` before dependency traversal. Prove permutations, duplicates, empty input, parser validity, and two-run byte equality for each face; retain the existing parser/subsetter architecture and never transform the vendored source bytes (D-14, D-18).</action>
  <verify>
    <automated>mix test test/rendro/pdf/font_subsetter_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Equal glyph sets serialize identically for all four curated fonts at the public subset boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| official release → vendored binary | External bytes enter the shipped package. |
| package allowlist → Hex archive | Checkout files may be omitted from consumer artifacts. |
| glyph list → subsetter | Caller ordering could perturb serialized output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-02 | Tampering | fonts/NOTICE | high | mitigate | Immutable official identity plus computed SHA-256 and face-specific test binding. |
| T-125-04 | Repudiation | Hex artifact | medium | mitigate | Positive tarball inspection for every font and NOTICE. |
| T-125-20 | Denial of service | FontSubsetter | high | mitigate | Vendored preflighted inputs and deterministic normalized glyph lists with bounded tests. |
</threat_model>

<verification>
Focused tests prove static-TTF compatibility, provenance, role descriptors, archive contents, and subset equality; no PDFium or new dependency is involved.
</verification>

<success_criteria>
- Exactly four locked static TTFs and four complete NOTICE records ship.
- All four descriptors are available to the explicit bridge.
- Equivalent glyph sets produce parseable byte-identical subsets.
</success_criteria>

<output>
Create `.planning/phases/125-foundation-curated-fonts-style-genre-presets-brand-fixtures/125-02-SUMMARY.md` when done.
</output>
