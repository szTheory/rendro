---
phase: 128-static-configurator-theme-codegen-livebook
plan: 03
type: execute
wave: 2
depends_on:
  - 128-01
files_modified:
  - assets/rendro/configurator/index.html
  - assets/rendro/configurator/configurator.css
  - assets/rendro/configurator/configurator.js
  - test/docs_contract/configurator_static_contract_test.exs
  - test/docs_contract/configurator_resolver_contract_test.exs
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - CONFIG-04
must_haves:
  truths:
    - "The configurator is static HTML/CSS/vanilla JavaScript under existing ExDoc asset copy-through, with no server, database, Node/npm, framework, form submission, persistence, or live PDF generation (D-06, D-21)."
    - "Canonical requested state is exactly family/preset/accent/mode; preview resolves exact, then first same-family+preset+mode representative by manifest order, then none, without mutating requested code or crossing dimensions (D-01, D-02)."
    - "Absent, partial, duplicate, malformed, or unknown queries fall back atomically to the first selectable manifest record; the URL is rewritten to exactly four canonical keys with strict uppercase percent-encoded #RRGGBB (D-03, D-04)."
    - "The visible and copied source is the exact Plan-01 index string; JavaScript never assembles Elixir, and valid no-preview selections remain copyable (D-05, D-07, D-10)."
    - "Native labeled selects, 44px targets, existing tokens, non-color focus/selection, natural raster aspect, manifest alt/caption/disclosure, polite live status, error alerts, responsive 320px/two-column-to-stacked layout, reduced motion, full code overflow, and light/dark/system-safe chrome satisfy the locked UI-SPEC (D-06)."
    - "FLAGGED UNRESOLVED EDGE ASSUMPTION CONFIG-04/empty: empty or partial URL input resolves atomically to Invoice/Swiss/#2C6BED/light per D-03; no partial value survives."
    - "FLAGGED UNRESOLVED EDGE ASSUMPTION CONFIG-04/encoding: URL equality is strict canonical ASCII slug equality plus uppercase six-hex-digit code-unit equality encoded by URLSearchParams per D-04; no Unicode normalization or grapheme comparison is inferred."
    - "FLAGGED UNRESOLVED EDGE ASSUMPTIONS CONFIG-01, CONFIG-02, CONFIG-03/unclassified: the probe supplied no narrower category; static topology, resolver-state, exact-source, clipboard, accessibility-state, and error contracts are the explicit accounting response."
  artifacts:
    - path: "assets/rendro/configurator/index.html"
      provides: "Semantic static configurator shell"
      contains: "Copy Elixir snippet"
    - path: "assets/rendro/configurator/configurator.css"
      provides: "Token-based responsive and state styling"
    - path: "assets/rendro/configurator/configurator.js"
      provides: "Closed-enum state, URL, preview, DOM, and clipboard controller"
    - path: "test/docs_contract/configurator_static_contract_test.exs"
      provides: "Static packaging, semantics, safe DOM, token, and scope contract"
    - path: "test/docs_contract/configurator_resolver_contract_test.exs"
      provides: "Exact/representative/none, atomic fallback, URL, and copy provenance contract"
  key_links:
    - from: "assets/rendro/configurator/configurator.js"
      to: "assets/rendro/configurator/index.json"
      via: "closed option state and exact snippet lookup"
      pattern: "index.json"
    - from: "assets/rendro/configurator/configurator.js"
      to: "assets/rendro/catalog.json"
      via: "preview-only exact/representative/none scan in manifest order"
      pattern: "catalog.json"
    - from: "assets/rendro/configurator/index.html"
      to: "brand/tokens/tokens.css"
      via: "existing ExDoc-relative stylesheet plus configurator token consumers"
      pattern: "tokens.css"
    - from: "copy button"
      to: "selected index record snippet"
      via: "await navigator.clipboard.writeText(visibleCodeText)"
      pattern: "clipboard.writeText"
  prohibitions:
    - statement: "The configurator must not silently mutate requested code values to match a representative raster."
      status: unresolved
      verification: null
      flagged: true
    - statement: "The configurator must not imply representative or absent previews received exact visual review."
      status: unresolved
      verification: null
      flagged: true
    - statement: "The configurator must not become a server, database, account, persistence, framework, or Node-build surface."
      status: unresolved
      verification: null
      flagged: true
    - statement: "Preset, dark-mode, copy, and preview text must not imply design-quality, accessibility, PDF/UA, WCAG, print-safety, or universal production guarantees."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Deliver the truthful, zero-server catalog configurator as a strict static consumer of the committed snippet index and catalog evidence.

Purpose: Let developers share a requested configuration, understand its exact/representative/absent preview relationship, and copy the exact formatter-owned Elixir without overstating visual evidence.
Output: Static HTML/CSS/JS and deterministic ExUnit source/behavior contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-RESEARCH.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-PATTERNS.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the static semantic shell, token layout, and safe loading states</name>
  <read_first>
    - assets/rendro/configurator/index.json — Plan 01 schema, option values/labels, and exact formatter-owned source records.
    - assets/rendro/catalog.json — ordered preview paths, dimensions, alt/caption, mode, accent, and boundary disclosure.
    - brand/tokens/tokens.css — exact custom-property names, OS/explicit theme cascade, focus, spacing, grid, typography, and reduced-motion tokens.
    - mix.exs — existing ExDoc `assets: %{"assets" => "assets"}` copy-through and package boundary; modify only if direct inspection disproves it.
    - test/docs_contract/catalog_manifest_contract_test.exs — parsed manifest, safe path, disclosure, and exact-copy assertions.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md — all approved design, responsive, copy, and state locks.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md — D-01, D-03, D-05, D-06, D-21.
  </read_first>
  <files>assets/rendro/configurator/index.html, assets/rendro/configurator/configurator.css, assets/rendro/configurator/configurator.js, test/docs_contract/configurator_static_contract_test.exs</files>
  <behavior>
    - The page starts in `Loading catalog previews…` with four native labeled selects and copy disabled, then enables only after both local JSON inputs pass schema/enum/path validation.
    - Manifest/index/image failures show the exact actionable error copy, keep unavailable actions disabled, and never fabricate a preview.
    - Chrome follows OS preference unless an explicit root data-theme override exists; document mode never changes chrome theme.
    - Layout is 320px control column + primary preview/output at >=900px and configuration/preview/disclosure/snippet stacked below 900px; all controls are at least 44px.
  </behavior>
  <action>
Write the missing static-contract test first, then create the semantic shell with a form containing four visible `<label>` + `<select>` controls, a primary preview/output region, factual notice/disclosure regions, visible selectable `<pre><code>`, `Copy Elixir snippet` button, polite status region, and alert-only actionable error region. Link existing tokens and a local CSS/JS file through ExDoc-relative paths. Style exclusively with existing `--rendro-*` semantic tokens and the locked UI-SPEC scale: centered max-1280 page with 24px gutters, 320px/two-column layout at 900px, locked stacked order below it, 44px native controls/button, 400/600 weights, mono 14px/1.57 code, natural-aspect scroll-free preview, wrapped full notices, horizontal code overflow, visible non-color focus/selection, and reduced-motion handling. Implement load/schema validation for the local index and catalog; allow only closed values and safe relative raster paths. Build nodes and set text/attributes through safe DOM APIs (T-128-01). Do not change `mix.exs` unless inspection proves the existing asset mapping false (D-06, D-21).
  </action>
  <acceptance_criteria>
    - All five new Phase-128 static/test files are served by existing ExDoc asset copy-through without `mix.exs`, package, dependency, server, or build changes.
    - HTML contains four native labeled selects, visible code, one text-labeled copy button, polite status, actionable alert, and no form submission/persistence destination.
    - CSS consumes existing token variables and implements the exact 900px layout transition, 320px control column, 24px gutters, 44px targets, locked typography weights/sizes, natural image sizing, full wrapping/overflow, focus, and reduced-motion rules.
    - Loading/error/disabled and manifest/image-failure behavior use the approved exact copy and do not expose an unsafe path or fabricated preview.
    - Source-contract tests reject framework/build/server markers, unsafe HTML sinks, arbitrary color input, live rendering, and document-mode-to-chrome coupling.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/configurator_static_contract_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
    <human-check>Open the packaged asset at wide and narrow viewports with OS light/dark and reduced motion; confirm focal order, natural raster sizing, code overflow, labels/focus, disabled loading, and actionable error rendering against 128-UI-SPEC.md.</human-check>
  </verify>
  <done>The configurator has a static, accessible, token-native shell with safe fail-closed loading and no new runtime/build surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic URL state, three-state preview resolution, and truthful clipboard feedback</name>
  <read_first>
    - assets/rendro/configurator/configurator.js — Task 1 safe load/state/DOM seams.
    - assets/rendro/configurator/index.json — exact 504 requested-state/source records and first selectable option contract.
    - assets/rendro/catalog.json — manifest order and bounded raster evidence fields.
    - test/docs_contract/configurator_static_contract_test.exs — Task 1 sink/topology/state assertions.
    - test/docs_contract/catalog_quality_contract_test.exs — exact/representative/none evidence and no-overclaim language boundaries.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md — D-01 through D-05, D-07, D-10, D-20.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md — CONFIG-02/03/04 resolver, URL, clipboard, and safe-DOM verification map.
  </read_first>
  <files>assets/rendro/configurator/configurator.js, assets/rendro/configurator/index.html, assets/rendro/configurator/configurator.css, test/docs_contract/configurator_static_contract_test.exs, test/docs_contract/configurator_resolver_contract_test.exs</files>
  <behavior>
    - Valid four-key state restores exactly; absent, partial, duplicate, malformed, or unknown values atomically become Invoice/Swiss/#2C6BED/light and never retain a partial untrusted value.
    - URL output has exactly family/preset/accent/mode, lowercase slugs, uppercase strict color encoded by URLSearchParams, and no serialized preview identity.
    - Resolver returns exact, first manifest-ordered same-family+preset+mode different-accent representative, or none; it never crosses those dimensions or performs fuzzy distance.
    - Exact/representative/none render the D-05 factual copy, valid none remains copyable, and dark selected rows display boundary_disclosure verbatim.
    - Copy awaits the Clipboard promise, writes exactly the visible selected index string, shows `Snippet copied` for two seconds without moving focus, and retains source plus retry/actionable error on rejection.
  </behavior>
  <action>
Add the missing resolver contract with synthetic ordered manifests before implementation. Model requested state as exactly four validated strings. Parse each key with `URLSearchParams.getAll`; require exactly one closed value per key, use strict ASCII slug comparison and uppercase six-digit accent equality, and apply the D-03 fallback atomically on any invalidity. Canonicalize with `history.replaceState` and exactly four keys; encode the accent through `URLSearchParams` (D-04 and the surfaced empty/encoding assumptions). Implement a pure three-state resolver in manifest order: exact on all four, representative only when family/preset/mode agree and accent differs, otherwise none (D-01, D-02). Never let preview choice rewrite state. Retrieve visible/copyable source only from the selected Plan-01 index record; JavaScript must not assemble Elixir or atoms (T-128-02). Render all data via constructed nodes, `textContent`, and validated safe attributes. Use exact D-05 copy, verbatim dark disclosure, and manifest alt/caption/dimensions; valid none keeps snippet/copy enabled. Await clipboard completion before success, hold `Snippet copied` for two seconds without focus movement, and expose retry/actionable error on failure (T-128-05). Cover keyboard/focus/live-status, image pending/error, long content, and chrome independence per D-06/D-20.
  </action>
  <acceptance_criteria>
    - Synthetic contracts prove exact, first representative, none, manifest-order tie-break, and no cross-family/preset/mode or distance path.
    - Every invalid/partial/duplicate/empty query atomically selects Invoice/Swiss/#2C6BED/light; a valid reload restores identical requested state.
    - Canonical URLs contain exactly four keys with lowercase slugs and percent-encoded uppercase strict #RRGGBB; preview identity is absent.
    - Exact/representative/none and dark disclosure text equal D-05/UI-SPEC copy, and none keeps valid source selectable/copyable.
    - Clipboard assertions bind visible text and copied text to the exact selected `index.json` string, cover success delay/focus and rejection/retry, and never claim success before the promise resolves.
    - No code path assembles Elixir, mutates requested state from a preview, preloads all 32 images, or implies representative/none received visual review.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs_contract/configurator_static_contract_test.exs test/docs_contract/configurator_resolver_contract_test.exs test/rendro/theme/snippet_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted</automated>
    <human-check>In current Chrome, Firefox, and Safari, exercise keyboard-only exact/representative/none, valid reload, malformed URL fallback, copy success/failure recovery, image failure, screen-reader status, long code, and document-dark/chrome-theme independence.</human-check>
  </verify>
  <done>Every valid selection has a canonical URL and exact formatter-owned copy source, while its preview relationship and clipboard result remain explicit and truthful.</done>
</task>

</tasks>

## Artifacts this phase produces

- New file `assets/rendro/configurator/index.html` defining the durable public static URL surface under ExDoc assets and semantic IDs/hooks for four controls, preview, disclosure, snippet, copy, status, loading, empty, and error states.
- New file `assets/rendro/configurator/configurator.css` defining token-consumer layout/state rules; no new design tokens.
- New file `assets/rendro/configurator/configurator.js` defining canonical requested state keys `family`, `preset`, `accent`, `mode`; URL contract of exactly those four keys; pure `exact | representative | none` resolution; safe DOM projection; and Clipboard behavior.
- New file `test/docs_contract/configurator_static_contract_test.exs` for static/semantic/security/scope/UI contract coverage.
- New file `test/docs_contract/configurator_resolver_contract_test.exs` for synthetic resolver, query, source-provenance, and clipboard contracts.
- No `package.json`, build config, server route, database/schema, external-service coverage matrix, `mix.exs` change, or new dependency is planned; inspection already confirms existing ExDoc asset copy-through.

## Assumption delta decision

Primary noun: canonical requested configuration/source model (`family`, `preset`, `accent`, `mode`). Decision: no-change. The browser serializes this identity while preview is derived evidence; the Mix task and Livebook project the same formatter-owned identity.

## Flagged edge assumptions

- CONFIG-01, CONFIG-02, CONFIG-03 `unclassified — review manually`: all remain explicit unresolved probe rows; they are accounted for by topology/state/resolver/source/copy tests rather than silently dismissed.
- CONFIG-04 empty/null/single/partial input: unresolved probe row with D-03's locked answer—any absent or incomplete four-key state falls back atomically to the first selectable Invoice/Swiss/#2C6BED/light record.
- CONFIG-04 encoding/equality: unresolved probe row with D-04's locked answer—lowercase ASCII catalog slugs and uppercase strict six-hex-digit strings are compared exactly and serialized by URLSearchParams; no byte/codepoint/grapheme normalization beyond that closed ASCII vocabulary is claimed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/local JSON → browser state and DOM | Query and committed manifests are treated as untrusted until schema, enum, multiplicity, and path validation. |
| requested state → preview evidence | Resolver may select only exact or same-family+preset+mode representative evidence. |
| index source → clipboard primitive | Exact visible committed source crosses into an asynchronous browser capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation / owner | Automated verification | Residual risk |
|---|---|---|---|---|---|---|---|
| T-128-01 | Tampering / information disclosure | URL/manifest parsing and DOM projection | high | mitigate | Tasks 1-2 enforce closed enums, duplicate rejection, atomic fallback, safe relative raster paths, constructed nodes, textContent/safe attributes, and no unsafe HTML sink. | both configurator contract suites | Browser-native URL/path semantics vary at edges; closed committed values and source contracts bound the surface. |
| T-128-02 | Elevation of privilege / DoS | selected source lookup | high | mitigate | Task 2 retrieves exact committed formatter strings and performs no JavaScript source/atom construction. | resolver + snippet provenance tests | Committed index compromise is a repository integrity concern; drift checking binds it to Elixir. |
| T-128-05 | Repudiation | preview provenance and clipboard status | high | mitigate | Task 2 distinguishes exact/representative/none, preserves disclosure, awaits clipboard completion, and reports rejection/retry without visual-review overclaim. | resolver/copy source contracts | Browser permission UX differs; failure remains explicit and source remains selectable. |
</threat_model>

<verification>
Run the mapped static suite after Task 1 and both static/resolver suites plus snippet provenance after Task 2. After Wave 2, run `mix test`; final phase closure adds `mix ci.fast`, `mix rendro.livebook.check`, and browser smoke/manual checks from 128-VALIDATION.md.
</verification>

<success_criteria>
- Static configurator loads through existing ExDoc assets with no new build/runtime surface.
- Four-key requested state, fallback, URL, and resolver semantics exactly implement D-01..D-05.
- Visible/copied source is exactly formatter-owned and success/failure feedback is truthful.
- All UI-SPEC responsive, semantic, accessibility-state, token, copy, and boundary locks are executable acceptance criteria.
</success_criteria>

## Source Coverage Audit (Plan 03 subset)

| Source | ID | Feature / constraint | Status |
|---|---|---|---|
| GOAL | — | Browse, pick, truthful preview, copy from zero-server page | COVERED |
| REQ | CONFIG-01 | Static ExDoc configurator | COVERED |
| REQ | CONFIG-02 | Exact/representative/none preview relationship | COVERED |
| REQ | CONFIG-03 | One-click exact source copy and feedback | COVERED (intentionally shares source ownership with Plan 01) |
| REQ | CONFIG-04 | Safe deterministic share URL | COVERED |
| RESEARCH | — | Two-manifest ownership, static browser APIs, safe resolver | COVERED |
| CONTEXT | D-01..D-07, D-10, D-20, D-21 | Requested identity, resolver, URL, UI, exact source, proof and bounded scope | COVERED |
| UI-SPEC | — | Design, responsive, copywriting, state, accessibility and registry locks | COVERED |

<output>
Create `.planning/phases/128-static-configurator-theme-codegen-livebook/128-03-SUMMARY.md` when done.
</output>
