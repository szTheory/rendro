---
phase: 128-static-configurator-theme-codegen-livebook
plan: 06
type: execute
wave: 4
depends_on:
  - 128-05
files_modified:
  - scripts/configurator_e2e/tests/configurator.spec.mjs
  - assets/rendro/configurator/configurator.js
  - test/docs_contract/dx_local_reproducibility_claims_test.exs
  - .planning/phases/128-static-configurator-theme-codegen-livebook/128-05-PLAN.md
autonomous: true
gap_closure: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - CONFIG-05
must_haves:
  truths:
    - "Pinned Chromium executes clipboard rejection followed by a successful retry, with the exact visible snippet retained and failure/success feedback exposed through the locked polite-live and actionable-alert semantics (D-05, D-06, D-20)."
    - "Pinned Chromium executes a selected preview-image failure followed by documented reload recovery, proving the alert, disabled failure state, cleared fabricated content, restored image, and restored controls (D-05, D-06, D-20)."
    - "Pinned Chromium proves browser chrome color scheme and selected document mode are independent in both cross-combinations: dark chrome with a light document and light chrome with a dark document (D-01, D-20)."
    - "The Phase-128 docs-contract file is formatter-clean and the complete deterministic `mix ci.fast` gate passes."
    - "Plan 05's `files_modified` frontmatter truthfully inventories its actual implementation footprint without changing its completed task contract."
  artifacts:
    - path: "scripts/configurator_e2e/tests/configurator.spec.mjs"
      provides: "Executable rejection/retry, image failure/reload recovery, failure semantics, and chrome/document-mode independence evidence"
    - path: "assets/rendro/configurator/configurator.js"
      provides: "Successful clipboard retry clears the no-longer-actionable failure alert if the new red test exposes the existing stale-error defect"
    - path: "test/docs_contract/dx_local_reproducibility_claims_test.exs"
      provides: "Formatter-compliant deterministic CI wiring contract"
    - path: ".planning/phases/128-static-configurator-theme-codegen-livebook/128-05-PLAN.md"
      provides: "Accurate historical execution-file inventory for the completed browser/fresh-consumer gate"
  key_links:
    - from: "scripts/configurator_e2e/tests/configurator.spec.mjs"
      to: "assets/rendro/configurator/configurator.js"
      via: "Playwright-controlled Clipboard promise rejection/retry and selected PNG request failure/reload exercise the shipped handlers"
      pattern: "clipboard|route|reload"
    - from: "browser chrome media state"
      to: "document mode query, control, snippet, and disclosure"
      via: "opposite `emulateMedia({ colorScheme })` and canonical `mode` selections are asserted simultaneously"
      pattern: "colorScheme|mode"
    - from: "test/docs_contract/dx_local_reproducibility_claims_test.exs"
      to: "mix ci.fast"
      via: "the formatted CI wiring contract no longer stops the terminal gate before its test, docs, Credo, and Dialyzer stages"
      pattern: "format --check-formatted"
---

<objective>
Close the two actionable Phase-128 verification gaps without revisiting completed product scope.

Purpose: Make the zero-human pinned-Chromium claim executable for every missing failure/recovery and theme-independence case, then restore the deterministic terminal CI gate.
Output: Expanded authoritative Chromium evidence, one narrowly test-proven clipboard recovery repair if required, formatter-clean ExUnit source, and corrected Plan-05 metadata.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-VALIDATION.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-VERIFICATION.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-PLAN.md
@.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md

@scripts/configurator_e2e/tests/configurator.spec.mjs
@scripts/configurator_e2e/playwright.config.mjs
@scripts/configurator_e2e/README.md
@assets/rendro/configurator/index.html
@assets/rendro/configurator/configurator.js
@assets/rendro/configurator/configurator.css
@brand/tokens/tokens.css
@test/docs_contract/dx_local_reproducibility_claims_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the pinned-Chromium matrix and deterministic CI gaps</name>
  <read_first>
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-VERIFICATION.md — authoritative two-gap statement; implement only its `gaps` entries.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-CONTEXT.md — D-01 chrome/document independence, D-05 truthful states, D-06 live/alert and recovery semantics, and D-20 browser proof bar.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-UI-SPEC.md — locked clipboard failure/retry, manifest/image failure/reload recovery, and automated-evidence boundaries.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-05-PLAN.md — completed acceptance contract whose `files_modified` inventory drift must be repaired without rewriting its tasks.
    - .planning/phases/128-static-configurator-theme-codegen-livebook/128-05-SUMMARY.md — actual Phase-128 browser, CI, CSS/JS deviation, and fresh-consumer execution record.
    - scripts/configurator_e2e/tests/configurator.spec.mjs — existing nine-test pinned browser suite and helpers to extend, not replace.
    - scripts/configurator_e2e/playwright.config.mjs — authoritative Chromium-only, single-worker, fixed-environment configuration.
    - scripts/configurator_e2e/README.md — exact pinned-container command and explicit no-claim boundary.
    - assets/rendro/configurator/index.html — `#status` polite live region, `#error` alert, preview, snippet, and copy controls under assertion.
    - assets/rendro/configurator/configurator.js — clipboard promise handling, image load/error handling, `fail()` state, and reload recovery behavior.
    - brand/tokens/tokens.css — OS chrome theme and explicit theme-override token behavior used to distinguish chrome from document mode.
    - test/docs_contract/dx_local_reproducibility_claims_test.exs — exact formatter blocker reported at lines 44-47.
  </read_first>
  <files>scripts/configurator_e2e/tests/configurator.spec.mjs, assets/rendro/configurator/configurator.js, test/docs_contract/dx_local_reproducibility_claims_test.exs, .planning/phases/128-static-configurator-theme-codegen-livebook/128-05-PLAN.md</files>
  <behavior>
    - Clipboard rejection: deterministically replace or intercept `navigator.clipboard.writeText` before application execution so the first attempt rejects; assert the exact visible snippet is retained, the copy control remains usable, `#error[role=alert]` becomes visible with the documented actionable copy, and `#status[aria-live=polite]` announces the copy failure (D-05, D-06, D-20).
    - Clipboard retry: allow the next write attempt to resolve, click the same copy control again, and assert `Snippet copied`, exact copied-source identity, retained focus/usability, and removal of the stale actionable failure alert. Start with this red assertion; change `configurator.js` only if the shipped handler fails it, clearing/hiding the prior clipboard error on successful retry without changing canonical source or preview behavior.
    - Image failure: intercept the selected catalog PNG request itself, not either JSON manifest, and fail exactly that request; assert the image error handler exposes the documented alert, announces unavailable previews through the polite status region, removes the failed preview and snippet rather than fabricating content, and disables all four controls plus copy (D-06, D-20).
    - Image recovery: remove the PNG interception and reload the same canonical URL; assert the selected image loads, the alert is hidden, controls/copy are enabled, the exact snippet returns, and status reports the restored exact preview. This is the locked reload recovery action, not a new in-page recovery surface.
    - Theme independence: cover both dark OS chrome with `mode=light` and light OS chrome with `mode=dark`. For each case assert the emulated media state and resolved chrome background, the opposite selected `#mode` value, canonical URL mode, formatter-owned snippet mode, and matching preview/disclosure remain simultaneously correct. Do not satisfy this by pairing like-with-like screenshots (D-01, D-20).
    - Preserve `noExternal`, deterministic request counts, the existing scoped axe/ARIA evidence, Chromium-only scope, fixed workers/retries/environment, seven committed visual baselines, and the no-Firefox/Safari/AT/WCAG/aesthetic-claim boundary. Add no dependency, snapshot, product build, server feature, or new public behavior beyond the test-proven stale clipboard-alert repair.
  </behavior>
  <action>
Extend `scripts/configurator_e2e/tests/configurator.spec.mjs` with focused deterministic tests for the four missing areas above. Install all route or browser-global controls before navigation, count the intended rejected/failed request so a non-matching interception cannot yield a false pass, await the precise state transition rather than using delays, and remove each route before the recovery attempt. Keep the tests within the existing pinned Chromium harness and reuse its helpers where their preconditions fit; do not fold negative paths into a broad test name that hides which contract executed.

Run the new clipboard retry assertion before altering product code. If it exposes the current stale actionable alert after a successful retry, make the narrow recovery fix in `assets/rendro/configurator/configurator.js` by clearing and hiding the prior clipboard error only after `writeText` resolves; do not change copy source, timeout, URL, resolver, image, or manifest behavior. If the red test does not expose that defect, leave the product file byte-identical and record the result in the SUMMARY.

Format only `test/docs_contract/dx_local_reproducibility_claims_test.exs` with the repository formatter. In `128-05-PLAN.md`, replace only frontmatter `files_modified` with the truthful completed execution inventory: `.github/workflows/ci.yml`; `assets/rendro/configurator/configurator.css`; `assets/rendro/configurator/configurator.js`; `scripts/configurator_e2e/.gitignore`; `scripts/configurator_e2e/README.md`; `scripts/configurator_e2e/package.json`; `scripts/configurator_e2e/package-lock.json`; `scripts/configurator_e2e/playwright.config.mjs`; `scripts/configurator_e2e/static-server.mjs`; `scripts/configurator_e2e/tests/configurator.spec.mjs`; `scripts/configurator_e2e/tests/configurator.spec.mjs-snapshots/`; `test/docs_contract/configurator_phase_gate_test.exs`; `test/docs_contract/dx_local_reproducibility_claims_test.exs`; and `test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs`. Do not revise Plan 05's tasks, decisions, acceptance contract, or historical dependency/wave metadata. Perform this metadata edit before the task commit: the automated gate compares the working-tree file with `HEAD` after removing only the complete `files_modified` YAML block from both versions, so any other Plan-05 byte change fails. It separately extracts the new list and diffs it against the exact ordered fourteen-entry inventory; visual inspection or merely checking that the key exists is insufficient.
  </action>
  <acceptance_criteria>
    - The authoritative pinned-container run executes and passes explicit clipboard rejection/retry assertions, including retained exact source, live failure feedback, visible actionable alert, successful second copy, and no stale failure alert after recovery.
    - The authoritative pinned-container run executes and passes an intercepted selected-PNG failure followed by same-URL reload recovery, including error/live semantics, disabled failure state, no fabricated preview/source, restored image/source, hidden alert, and re-enabled controls.
    - The authoritative pinned-container run executes and passes dark-chrome/light-document and light-chrome/dark-document cases, proving chrome appearance follows the emulated OS scheme while query, select, snippet, preview, and disclosure follow document mode.
    - Existing URL, resolver, source identity, accessibility, responsive, reduced-motion, external-request, and seven visual-baseline tests remain green; no package or snapshot is added or updated.
    - `test/docs_contract/dx_local_reproducibility_claims_test.exs` passes its focused ExUnit test and `mix format --check-formatted`.
    - An automated exact-list diff proves `128-05-PLAN.md` contains the ordered fourteen-entry `files_modified` inventory named in the action.
    - An automated normalized byte diff against the committed pre-task `128-05-PLAN.md` proves the `files_modified` block is the only Plan-05 content changed; the gate runs before the task commit so `HEAD` remains the immutable baseline.
    - `mix ci.fast` exits zero after the focused browser, ExUnit, and formatting gates pass.
  </acceptance_criteria>
  <verify>
    <automated>sh -c 'set -eu; plan=.planning/phases/128-static-configurator-theme-codegen-livebook/128-05-PLAN.md; expected=$(mktemp); actual=$(mktemp); before=$(mktemp); after=$(mktemp); trap '\''rm -f "$expected" "$actual" "$before" "$after"'\'' EXIT; printf "%s\n" ".github/workflows/ci.yml" "assets/rendro/configurator/configurator.css" "assets/rendro/configurator/configurator.js" "scripts/configurator_e2e/.gitignore" "scripts/configurator_e2e/README.md" "scripts/configurator_e2e/package.json" "scripts/configurator_e2e/package-lock.json" "scripts/configurator_e2e/playwright.config.mjs" "scripts/configurator_e2e/static-server.mjs" "scripts/configurator_e2e/tests/configurator.spec.mjs" "scripts/configurator_e2e/tests/configurator.spec.mjs-snapshots/" "test/docs_contract/configurator_phase_gate_test.exs" "test/docs_contract/dx_local_reproducibility_claims_test.exs" "test/mix/tasks/rendro_gen_theme_fresh_consumer_test.exs" &gt; "$expected"; awk '\''/^files_modified:$/ {inside=1; next} inside &amp;&amp; /^[^ ]/ {exit} inside {line=$0; sub(/^  - /, "", line); print line}'\'' "$plan" &gt; "$actual"; diff -u "$expected" "$actual"; git show "HEAD:$plan" | awk '\''/^files_modified:$/ {inside=1; next} inside &amp;&amp; /^[^ ]/ {inside=0} !inside {print}'\'' &gt; "$before"; awk '\''/^files_modified:$/ {inside=1; next} inside &amp;&amp; /^[^ ]/ {inside=0} !inside {print}'\'' "$plan" &gt; "$after"; diff -u "$before" "$after"' &amp;&amp; npm run test:container --prefix scripts/configurator_e2e &amp;&amp; mix test test/docs_contract/dx_local_reproducibility_claims_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted test/docs_contract/dx_local_reproducibility_claims_test.exs &amp;&amp; mix ci.fast</automated>
  </verify>
  <done>Both gaps in 128-VERIFICATION.md are closed by executable evidence: the complete pinned-Chromium failure/recovery/theme-independence matrix is green, the formatter blocker is gone, Plan 05 metadata is truthful, and the full deterministic CI gate passes.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| Playwright controls → shipped browser handlers | Test-injected Clipboard behavior and local request failures must execute the real configurator recovery paths rather than duplicate them. |
| Pinned Chromium evidence → public acceptance claim | Passing tests establish only enumerated Chromium behavior inside the exact pinned container. |
| Completed execution history → planning metadata | Plan 05's file inventory must describe the committed implementation without rewriting its historical contract. |

## STRIDE Threat Register (OWASP ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation / owner | Automated verification | Residual risk |
|---|---|---|---|---|---|---|---|
| T-128-09 | Repudiation | Clipboard and PNG failure interception | high | mitigate | Task 1 installs controls before navigation, counts the intended failed call/request, and asserts failure plus recovery states against shipped DOM handlers. | Pinned-container Playwright tests | Browser implementation outside pinned Chromium remains deliberately unclaimed. |
| T-128-10 | Tampering / Repudiation | Chrome/document-mode independence | medium | mitigate | Task 1 asserts both opposite combinations across media state, computed chrome, query/control, formatter source, and preview disclosure. | Pinned-container Playwright tests | OS/browser theme rendering outside the pinned environment remains outside evidence. |
| T-128-11 | Repudiation | Terminal CI and Plan-05 history | medium | mitigate | Task 1 formats the named blocker, runs focused ExUnit and full `mix ci.fast`, and repairs only Plan 05's execution-file inventory. | Focused ExUnit, format check, `mix ci.fast`, plan-structure validation | Remote branch-protection settings remain outside local proof. |
</threat_model>

<verification>
Run the authoritative pinned-container browser suite first. Then run the focused docs-contract ExUnit test and targeted format check. Run `mix ci.fast` only after those focused gates pass. Preserve Chromium-only/no-certification claim boundaries and do not update visual snapshots.
</verification>

<success_criteria>
- Every missing executable Chromium case named in 128-VERIFICATION.md runs and passes in the exact pinned container.
- Failure paths prove the locked polite-live and actionable-alert semantics, and both recovery paths prove usable restored state.
- Both cross-combinations prove chrome theme does not follow document mode.
- The named ExUnit file is formatter-clean and `mix ci.fast` is green.
- Plan 05's frontmatter accurately records its actual implementation files.
- No deferred idea, dependency, snapshot, server/build surface, public API, or unrelated product behavior is added.
</success_criteria>

## Gap-Closure Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Zero-human acceptance for the shipped configurator/codegen/Livebook phase | 06 | COVERED | Closes only the two verification failures; completed product surfaces remain owned by Plans 01-05. |
| REQ | CONFIG-01 | Pinned test-only Chromium coverage for the delivered static surface | 06 | COVERED | Adds the omitted browser failure and theme-independence executions. |
| REQ | CONFIG-02 | Truthful catalog-preview failure and recovery | 06 | COVERED | Fails the selected PNG, proves alert/live/disabled state, then reloads successfully. |
| REQ | CONFIG-03 | Clipboard rejection, retry, and exact source identity | 06 | COVERED | Rejects first write and proves successful exact-source retry. |
| REQ | CONFIG-05 | Green terminal deterministic CI gate | 06 | COVERED | Formats the blocking docs-contract file and reruns `mix ci.fast`. |
| RESEARCH | — | Existing isolated pinned Playwright 1.62.0 harness remains test-only and graph-disconnected | 06 | COVERED | No dependency, product Node path, or CI topology change. |
| CONTEXT | D-01 | Configurator chrome theme remains independent from document mode | 06 | COVERED | Both opposite combinations execute in Chromium. |
| CONTEXT | D-05 | Failure copy remains factual and valid code identity is preserved | 06 | COVERED | Clipboard failure retains source; image failure fabricates neither source nor preview. |
| CONTEXT | D-06 | Polite live status, actionable alert, image loading/error, and recovery semantics | 06 | COVERED | Assertions target the shipped semantic nodes and documented retry/reload actions. |
| CONTEXT | D-20 | Static UI tests cover copy failure, theme independence, status semantics, and error states | 06 | COVERED | Missing executable cases become required pinned-container tests. |
| VERIFICATION | Gap 1 | Missing Chromium rejection/recovery, failure semantics, and independent theme cases | 06 | COVERED | One focused browser expansion owns all named missing cases. |
| VERIFICATION | Gap 2 | Formatter-red docs-contract file makes `mix ci.fast` fail | 06 | COVERED | Targeted formatting plus focused and terminal gates. |

CONFIG-04 and CONFIG-06 are already verified and receive no new work. CONTEXT decisions D-02..D-04 and D-07..D-19/D-21 remain covered by completed Plans 01-05 and are not replanned. Deferred ideas remain excluded: arbitrary colors/fuzzy or cross-dimension matching, live PDF/server/accounts/persistence, catalog expansion, interactive/all-preset Livebook work, and broader browser/AT/WCAG/aesthetic claims.

<output>
Create `.planning/phases/128-static-configurator-theme-codegen-livebook/128-06-SUMMARY.md` when done.
</output>
