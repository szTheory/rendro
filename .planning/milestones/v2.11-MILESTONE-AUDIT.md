---
milestone: v2.11
milestone_name: Document Theming & Design-Token System
audited: 2026-07-28
re_audited: 2026-07-29  # 3rd pass — after Phase 124 EXECUTED+VERIFIED; ci-blocking debt trio CLOSED; live re-confirmed
re_audited_4: 2026-07-28  # 4th pass — live re-confirm (format exit 0; stale-113 3/0); integration re-WIRED indep; Nyquist advanced (121 now COMPLIANT); SUMMARY frontmatter now populated (doc debt resolved)
re_audited_5: 2026-07-28  # 5th pass — full workflow re-run; integration independently re-WIRED (0 orphaned/0 missing); Nyquist advanced again (122 now COMPLIANT → 119/120/121/122); only 123/124 remain draft
re_audited_6: 2026-07-29  # 6th pass — full workflow re-run; integration independently re-WIRED (1697 tests/0 failures); Nyquist now FULLY COMPLIANT (123 & 124 reconciled to validated+compliant, commits 79b655c/cf3fc8d) → all 6 phases COMPLIANT, overall COMPLIANT
status: tech_debt
scores:
  requirements: 21/21
  phases: 6/6   # 119-124 all verified passed (124 = tech-debt remediation phase)
  integration: WIRED (0 orphaned, 0 missing)
  flows: 4/4
gaps:
  requirements: []   # none unsatisfied — all 21 verified + WIRED
  integration: []
  flows: []
tech_debt:
  - phase: 124-CLOSED  # Phase 124 executed & verified passed 2026-07-29 — the ci-blocking trio is resolved
    items:
      - "RESOLVED (D-01): stale dx_local_reproducibility_claims_test.exs fixed — 2 stale cases reading deleted .planning/phases/113-* deleted; now 3 tests, 0 failures. Live re-confirmed this audit."
      - "RESOLVED (D-02): formatter drift on 7 files cleared — mix format --check-formatted exits 0. Live re-confirmed this audit."
      - "RESOLVED (D-03): Background.emit?/1 @spec widened (single-file) — mix dialyzer 0 errors (was 133). mix ci.fast green end-to-end (1697 tests, credo clean, dialyzer 0). Byte-identity guard held (27/27)."
  - phase: 123-theming-deviations (WINDOWS open, deferred to Milestone C / follow-up)
    items:
      - "WINDOWS id 1 (open): invoice_dark table body cells render as bare strings → inherit default black, illegible on dark bg. Fix risks the frozen INV-01 byte-identity golden; deferred. Affects dark-mode polish, not a v2.11 requirement gate."
      - "WINDOWS id 2 (open, LOCKED): ticket/ticket_dark uniform type scale inverts intended hierarchy (display anchor 21pt > title role 16.5pt). Locked Phase-122 design outcome; re-mapping is an architectural call. Honestly scored passed:false — NOT flattened, NOT the DEFAULT-02 SHOW-01 gate (invoice demo, passed)."
      - "WINDOWS id 3 (open): payslip themed numeric cells wrap mid-number ($4,200.0 / 0) at 10.5pt/1.35 leading. Column-width retuning deferred."
      - "WINDOWS id 7 (open): pdfium-cli v0.11.0 not on PATH → mix rendro.launch_artifacts.check (advisory lane) fails. Environment/tooling gap, not a code defect."
  - phase: nyquist-coverage-RESOLVED  # 6th pass: all 6 phases now validated + nyquist_compliant
    items:
      - "RESOLVED: all 6 phases (119-124) reconciled to status: validated + nyquist_compliant: true — 123 (79b655c) & 124 (cf3fc8d) advanced since 5th pass. Overall Nyquist now COMPLIANT (was PARTIAL). No validate-phase TODO remains."
  - phase: test-coverage-warnings (integration checker, non-blocking)
    items:
      - "No byte-level regression golden renders a from_brand-derived accent op and asserts it in output bytes. Wiring confirmed LIVE this audit (from_brand accent {200,10,90} → invoice.ex:540 → '0.7843 0.0392 0.3529 rg' present; absent on no-theme). A one-line accent-op golden would close the gap. Affects COLOR-02, DEFAULT-01..03."
      - "Dedicated *_typography_test.exs exists for only 3 of 7 recipes (invoice, statement, certificate); other 4 rely on byte-identity + smoke coverage. Threading wired for all 7 — coverage depth, not a break."
  - phase: documentation-RESOLVED  # 4th pass: SUMMARY frontmatter now populated
    items:
      - "RESOLVED: SUMMARY.md requirements-completed frontmatter is now populated across all plan summaries (119-01/02, 121-01..04, 122-01..05, 123-01..05, 124-01) — the 3-source cross-check (VERIFICATION + SUMMARY frontmatter + traceability) now fully aligns. (120 has none because PLUMB reqs are recorded on its plan bodies; verified via VERIFICATION + traceability.) Prior 'all null' bookkeeping gap is closed."
nyquist:
  compliant_phases: [119, 120, 121, 122, 123, 124]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: COMPLIANT  # all 6 phases validated + nyquist_compliant: true (123/124 reconciled 6th pass)
remediation_phase:
  phase: 124
  status: executed_verified_passed  # 3 commits (d16ebdf, f7beecb, 98310da) + f2e19a9; VERIFICATION 5/5 truths, 5/5 prohibitions
  targets_closed: ["stale 113 docs-contract test (D-01)", "formatter drift (D-02)", "ticket dialyzer/Background.emit? contract (D-03)"]
  outcome: "mix ci.fast green end-to-end; byte-identity golden guard preserved (27/27); ticket.ex untouched (WINDOWS id 2 stays locked); WINDOWS ids 4/5/6 marked fixed."
integration_warnings:
  - "Seam 2 (from_brand E2E): from_brand/2 → resolve/1 → render is WIRED and confirmed LIVE this audit (accent rg op present in output bytes, absent on no-theme). Residual: no committed byte-golden guards it against silent regression. Non-blocking. Affects COLOR-02, DEFAULT-01..03."
---

# Milestone v2.11 — Document Theming & Design-Token System — Audit Report

**Audited:** 2026-07-28
**Status:** ⚡ tech_debt (all requirements met, no blockers, accumulated debt needs review)
**Scope:** Phases 119–123 (Milestone B / `SEED-003` of the Happy-Path Home Runs program)

## Milestone Definition of Done

> Give Rendro a public, deterministic PDF theming contract so every recipe is fully themable (brand
> colors + typography), gets light/dark for free, and ships a strong unbranded default that clears
> the Milestone-A reader-quality rubric — without widening the deterministic core or the
> family-not-industry boundary.

The single irreversible act — shipping the public `Rendro.Theme` value on the adapter/Evolving tier —
is done and registered in `priv/public_api.json`. The central regression guard — an un-themed call
reproducing v2.10 bytes exactly for all 7 recipes — holds after phases 120/121/122 composed together
(verified by the frozen-SHA golden suite passing as a set).

## Requirements Coverage — 21/21 satisfied

3-source cross-reference: **VERIFICATION.md per-requirement evidence tables** (authoritative) +
**REQUIREMENTS.md traceability** (all `[x]`) + **integration checker** (all WIRED). SUMMARY
frontmatter `requirements_completed` was empty across all plans (see tech debt) — VERIFICATION
evidence tables were used as the primary source instead.

| REQ-ID | Phase | Verification | Integration | Final |
|--------|-------|--------------|-------------|-------|
| THEME-01 | 119 | passed | WIRED | satisfied |
| THEME-02 | 119 | passed | WIRED | satisfied |
| THEME-03 | 119 | passed | WIRED | satisfied |
| THEME-04 | 119 | passed | WIRED | satisfied |
| COLOR-01 | 119 | passed | WIRED | satisfied |
| COLOR-02 | 119 | passed | WIRED | satisfied |
| CONTRACT-01 | 119 | passed | WIRED | satisfied |
| CONTRACT-03 | 119 | passed | WIRED | satisfied |
| PLUMB-01 | 120 | passed | WIRED | satisfied |
| PLUMB-02 | 120 | passed | WIRED | satisfied |
| PLUMB-03 | 120 | passed | WIRED | satisfied |
| MODE-01 | 121 | passed | WIRED | satisfied |
| MODE-02 | 121 | passed | WIRED | satisfied |
| MODE-03 | 121 | passed | WIRED | satisfied |
| TYPE-01 | 122 | passed | WIRED | satisfied |
| TYPE-02 | 122 | passed | WIRED | satisfied |
| TYPE-03 | 122 | passed | WIRED | satisfied |
| DEFAULT-01 | 123 | passed | WIRED | satisfied |
| DEFAULT-02 | 123 | passed | WIRED | satisfied |
| DEFAULT-03 | 123 | passed | WIRED | satisfied |
| CONTRACT-02 | 123 | passed | WIRED | satisfied |

**Orphan detection:** none. Every REQ-ID in the traceability table is verified by its assigned
phase's VERIFICATION.md.

## Phase Verifications — 6/6 passed

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 119 `Rendro.Theme` core (one-way door) | passed | 10/10 truths, 12/12 prohibitions, 8/8 reqs | Zero recipe change; 62 goldens byte-identical |
| 120 S1 retrofit + `theme:` swap (7 recipes) | passed | 8/8 | Retrofit committed separately from swap; no-theme byte-identity holds |
| 121 Light/dark background-fill (7 recipes) | passed | 10/10 | Dark paints every page incl. overflow (fill count == page count); light emits no rect |
| 122 Typography type-scale + font-role/leading | passed | 6/6 (re-verified after 122-05 gap closure) | CR-01 blocker (themed Payslip crash) closed; metric no-op preserved |
| 123 `from_brand/2` E2E + honest rubric + docs | passed | 5/5 | Honest 3-commit order (DATA→theme→re-score); Ticket honestly passed:false; 11 gallery rows |
| 124 Address v2.11 tech debt (remediation) | passed | 5/5 truths, 5/5 prohibitions | ci-blocking trio cleared (D-01/02/03); `mix ci.fast` green; byte-identity held (27/27); ticket.ex untouched |

## Cross-Phase Integration — WIRED

The integration checker confirmed a single resolved `%Theme{}` threads coherently through all 3
rungs across all 7 recipes: `palette/1` (colors), `typography/1` (type scale/fonts/leading), and
`Background.emit?/region/section` (dark fill) all derive from the same idempotent
`Rendro.Theme.resolve/1` output — they cannot diverge. Zero orphaned exports, zero missing consumers.

**E2E flows — all complete:**
1. `from_brand/2` → themed recipe render → gallery artifact (`launch_artifacts.ex:518` →
   `assets/rendro/artifacts.json`) — real chain 119→120/122→123.
2. `dark/1` → background paints every page incl. paginate overflow (golden proves fill-op count ==
   page count).
3. No-theme byte-identity after 120+121+122 composed — targeted golden set (49 tests + 4 properties)
   passes together, not just per-phase claims.
4. Themed happy path — all 7 recipes return `{:ok, _}` under `Theme.default()`.

## Tech Debt by Area

**ci-blocking trio — RESOLVED by Phase 124** *(executed & verified passed 2026-07-29; live re-confirmed this audit)*
- ~~`dx_local_reproducibility_claims_test.exs` — 2 failures reading deleted `.planning/phases/113-*`~~
  → **FIXED (D-01):** 2 stale cases removed; now **3 tests, 0 failures** (live re-confirmed).
- ~~`mix format --check-formatted` fails on 7 files (formatter-version drift)~~
  → **FIXED (D-02):** bounded reformat of exactly the 7 known files; `mix format --check-formatted`
  now **exits 0** (live re-confirmed).
- ~~`mix dialyzer` fails on `Background.emit?/1` contract mismatch + `ticket.ex` errors (133 total)~~
  → **FIXED (D-03):** single-file `@spec` widening on `Background.emit?/1`; `mix dialyzer` **0 errors**;
  `mix ci.fast` green end-to-end (1697 tests, credo clean). Byte-identity golden guard held (27/27);
  `ticket.ex` untouched (WINDOWS id 2 stays locked). WINDOWS ids 4/5/6 marked `fixed`.

**Phase 123 theming deviations — WINDOWS ledger still open (deferred, not v2.11 requirement gates)**
- **id 1 (open):** `invoice_dark` table body cells render as bare strings → inherit default black,
  illegible on dark background. Fix risks the frozen INV-01 byte-identity golden → deferred. Dark-mode
  polish; Milestone-C / follow-up scope.
- **id 2 (open, LOCKED):** `ticket`/`ticket_dark` uniform type scale inverts intended hierarchy.
  Locked Phase-122 design outcome; re-mapping is an architectural call. Honestly scored `passed:false`
  — **not** flattened, **not** the DEFAULT-02 SHOW-01 gate (invoice demo, passed). Explicitly out of
  Phase-124 scope (untouched, confirmed).
- **id 3 (open):** `payslip` themed numeric cells wrap mid-number at 10.5pt/1.35 leading. Column-width
  retuning deferred.
- **id 7 (open):** `pdfium-cli` v0.11.0 not on PATH → `mix rendro.launch_artifacts.check` (advisory
  lane) fails. Environment/tooling gap, not a code defect.

> `WINDOWS.md`: `open_count: 4` (ids 1/2/3/7), `fixed_count: 3` (ids 4/5/6). `/gsd-ship` blocks while
> `open_count > 0` — these 4 must be fixed or waived before a ship, but none is a v2.11 requirement gap.

**Test-coverage warnings** *(integration checker, non-blocking)*
- No committed byte-level golden renders a `from_brand`-derived accent op and asserts it in output
  bytes. Wiring confirmed **LIVE this audit** (accent `{200,10,90}` → `invoice.ex:540` →
  `0.7843 0.0392 0.3529 rg` present; absent on no-theme). A one-line accent-op golden would close it.
  Affects COLOR-02, DEFAULT-01..03.
- Dedicated `*_typography_test.exs` for only 3 of 7 recipes; other 4 rely on byte-identity + smoke.
  Coverage depth, not a break.

**Nyquist coverage** *(RESOLVED this pass)*
- **All 6 phases (119–124)** now reconciled to `status: validated` + `nyquist_compliant: true` →
  **COMPLIANT**. 123 (`79b655c`) and 124 (`cf3fc8d`) advanced since the 5th pass. Overall: **COMPLIANT**
  — no `validate-phase` TODO remains.

**Documentation** *(RESOLVED this pass)*
- SUMMARY frontmatter `requirements_completed` is now **populated** across all plan summaries — the
  3-source cross-check fully aligns. Prior "empty frontmatter" bookkeeping gap is closed.

### Nyquist Coverage *(6th pass — live frontmatter re-read)*

| Phase | VALIDATION.md | status | nyquist_compliant | Verdict | Action |
|-------|---------------|--------|-------------------|---------|--------|
| 119 | exists | validated | true | **COMPLIANT** | — (reconciled, 0 gaps) |
| 120 | exists | validated | true | **COMPLIANT** | — (reconciled, 0 gaps) |
| 121 | exists | validated | true | **COMPLIANT** | — (reconciled, 0 gaps) |
| 122 | exists | validated | true | **COMPLIANT** | — (reconciled 4th pass, `bde76d4`) |
| 123 | exists | validated | true | **COMPLIANT** | — (reconciled 6th pass, `79b655c`) |
| 124 | exists | validated | true | **COMPLIANT** | — (reconciled 6th pass, `cf3fc8d`) |

Overall: **COMPLIANT** (6 compliant / 0 not-validated). All milestone phases have been reconciled by
validate-phase (`status: validated`) with `nyquist_compliant: true` — the Nyquist coverage TODO is
fully closed.

## Remediation Phase 124 — EXECUTED & VERIFIED PASSED

Phase **124 "Address v2.11 tech debt"** was planned, executed, and verified since the last audit pass.
Commits `d16ebdf` (dialyzer `@spec`), `f7beecb` (7-file formatter), `98310da` (stale-113 test) +
`f2e19a9` (tracking docs). VERIFICATION: **5/5 truths, 5/5 prohibitions**, no gaps.

**Re-confirmed live this audit:**
- `mix format --check-formatted` → **exit 0** (was red).
- `test/docs_contract/dx_local_reproducibility_claims_test.exs` → **3 tests, 0 failures** (was 5/2).
- (Per 124 VERIFICATION, run live in that session) `mix dialyzer` 0 errors; `mix ci.fast` green
  end-to-end; byte-identity guard 27/27; `ticket.ex` untouched.

The trio that drove the prior `tech_debt` verdict is closed. What remains is deferred quality debt
(WINDOWS ids 1/2/3/7) and two non-blocking test-coverage warnings — none a requirement gap. (Nyquist
reconciliation is now fully closed as of the 6th pass — all 6 phases COMPLIANT.)

## Verdict

All **21/21 requirements satisfied**, all **6/6 phases verified passed**, cross-phase integration
fully **WIRED**, all 4 E2E flows complete. The one-way-door `Rendro.Theme` contract shipped and the
v2.10 byte-identity regression guard holds under the composed theming stack. The milestone
Definition of Done is met. **No critical blockers.**

Status stays `tech_debt` (not `passed`) because of accumulated non-blocking debt worth a decision
before archiving — now materially smaller than the prior pass:
- **4 open WINDOWS deviations** (ids 1/2/3/7) — deferred dark-mode/typography polish + pdfium tooling;
  id 2 is a **locked, accepted** design outcome. These gate `/gsd-ship` (open_count > 0) until fixed
  or waived, but none invalidates a v2.11 requirement.
- ~~**Nyquist** validation un-run on phases 123-124~~ → **RESOLVED:** all 6 phases now COMPLIANT.
- Two **test-coverage warnings** (from_brand accent byte-golden; typography 3/7 recipes).

The ci-blocking trio is **resolved**. The remaining items are either accepted design outcomes,
follow-up/Milestone-C polish, environmental tooling, or coverage TODOs — all safe to accept-and-carry
or close via targeted follow-up.

**Re-audit note (2026-07-29):** Third audit pass, run after Phase 124 was executed and verified
`passed`. The ci-blocking debt trio (stale-113 test, formatter drift, dialyzer contract) is now
CLOSED and live-re-confirmed (format exit 0; stale-113 3/0). Integration independently re-confirmed
**WIRED** with the from_brand accent op verified live in output bytes. Nyquist unchanged (119/120
COMPLIANT; 121-124 draft). Verdict remains `tech_debt` but with the concrete CI blocker gone — the
milestone is now cleanly completable (accept remaining deferred debt) or shippable after the 4 open
WINDOWS are fixed/waived.

**Re-audit note (4th pass):** Live re-run of the full audit workflow. Deltas since 3rd pass, all
favorable: (1) `mix format --check-formatted` → **exit 0** and stale-113 test → **3 tests / 0
failures** both re-confirmed live; (2) an **independent** integration checker re-derived all 4 seams
**WIRED** from live evidence (single `%Theme{}` cannot diverge across palette/typography/background;
`from_brand(accent: {200,10,90})` → `0.7843 0.0392 0.3529 rg` present in bytes, absent no-theme; 7
frozen-SHA byte-identity goldens; themed happy path `{:ok,_}`) — and **CONFIRMED** (not refuted) the
lone residual: no committed byte-golden guards the from_brand accent op; (3) **Nyquist advanced** —
121 is now `validated` + `nyquist_compliant: true` (COMPLIANT), so 119/120/121 COMPLIANT and only
122/123/124 remain draft; (4) SUMMARY `requirements-completed` frontmatter is now **populated**,
closing the prior 3-source-crosscheck bookkeeping gap. 21/21 requirements satisfied, 6/6 phases
passed, 0 orphaned/0 missing. Verdict remains `tech_debt` — no blockers; the milestone is cleanly
completable (accept remaining deferred debt) or shippable after the 4 open WINDOWS are fixed/waived.

**Re-audit note (5th pass, 2026-07-28):** Full audit workflow re-run. All prior findings hold. One
favorable delta: **Phase 122 has been reconciled to `status: validated` + `nyquist_compliant: true`
(commit `bde76d4`) since the 4th pass**, so Nyquist COMPLIANT is now **119/120/121/122** and only
**123/124** remain `draft` (NOT-VALIDATED — coverage TODO). An independent integration checker again
re-derived all 6 cross-phase seams **WIRED** (0 orphaned / 0 missing) against live `lib/` code and a
green theme suite (66 theme tests, 0 failures), CONFIRMING the two residual test-coverage warnings
(from_brand render-through not byte-golden'd; dark E2E byte-asserted for 2/7 recipes) as
non-blocking. 3-source requirements cross-reference (VERIFICATION + SUMMARY frontmatter +
traceability) fully aligns: **21/21 satisfied, 0 unsatisfied, 0 orphaned**. WINDOWS unchanged
(`open_count: 4` — ids 1/2/3/7). Verdict remains `tech_debt` — no requirement gaps or critical
blockers; milestone Definition of Done met.

**Re-audit note (6th pass, 2026-07-29):** Full audit workflow re-run. All prior findings hold. One
favorable delta closes the last coverage TODO: **Nyquist is now FULLY COMPLIANT** — phases **123**
(`79b655c`) and **124** (`cf3fc8d`) have been reconciled to `status: validated` +
`nyquist_compliant: true` since the 5th pass, so **all 6 phases (119–124) are COMPLIANT** and overall
Nyquist advances **PARTIAL → COMPLIANT** (no `/gsd-validate-phase` TODO remains). An independent
integration checker again re-derived all cross-phase seams **WIRED** against a green full suite
(**1697 tests, 0 failures**), re-CONFIRMING the two non-blocking test-coverage warnings (from_brand
render-through not byte-golden'd; dark E2E byte-asserted for a subset of recipes). 3-source
requirements cross-reference fully aligns: **21/21 satisfied, 0 unsatisfied, 0 orphaned**. WINDOWS
unchanged (`open_count: 4` — ids 1/2/3/7; id 2 locked/accepted). Verdict remains `tech_debt` solely on
the 4 open WINDOWS deviations — no requirement gaps, no critical blockers, Nyquist fully closed, and
the milestone is cleanly completable (accept remaining deferred debt) or shippable once the 4 open
WINDOWS are fixed or waived.
