---
phase: 121-light-dark-background-fill-mechanism-all-7-recipes
plan: 03
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - lib/rendro/recipes/payslip.ex
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes/receipt.ex
  - lib/rendro/recipes/branded_invoice.ex
  - lib/rendro/recipes/ticket.ex
autonomous: true
requirements: [MODE-02]
must_haves:
  truths:
    - "All 5 verify-only recipes (Payslip, Invoice, Receipt, BrandedInvoice, Ticket) wire the shared Rendro.Recipes.Background region+section into page_template/1 and sections/2, gated on Background.emit?/1, using each recipe's OWN resolved page dims (Pitfall 6: verify-only is scoped to the text seam — the region is wired on ALL 7 recipes)."
    - "Under a dark theme each of the 5 gets its full-page :background fill for free (existing colored elements ride the dark/1 swap — D-05); under no theme each emits ZERO background ops."
    - "Each recipe's no-theme render stays byte-identical to v2.10 (its *_byte_identity_test.exs stays green with no re-bless)."
  artifacts: []
  key_links:
    - "Each recipe passes its own resolved dims: Payslip/Ticket via geometry(opts) g.pw/g.ph; Invoice/Receipt via @page_width/@page_height constants; BrandedInvoice via its A4 page dims (595.28 x 841.89 default)."
    - "Text seams are VERIFY-ONLY here — these 5 already pass an explicit swappable color: on every Rendro.text; their palette/1 nil-branches already carry background (verified: branded_invoice:238, invoice:477, payslip:689, receipt:483, ticket:521)."
  prohibitions:
    - "MUST NOT edit any of the 5 recipes' text draw-sites or palette/1 (text seams already correct — D-02 verify-only)."
    - "MUST NOT re-bless any *_byte_identity_test.exs or edge_matrix golden (D-03)."
    - "MUST NOT read theme.typography.* (Phase 122)."
---

<objective>
Complete the `:background` region wiring across the remaining 5 recipes so every one of the 7 recipes gets dark for free.

These 5 (Payslip, Invoice, Receipt, BrandedInvoice, Ticket) are text-seam VERIFY-ONLY (D-02) — they already pass explicit swappable `color:` roles on every `Rendro.text` and already carry `background` in their `palette/1` nil-branch. This plan adds ONLY the shared `Background` region+section wiring (Pitfall 6: the region is added to all 7 recipes even though only Statement + Certificate needed text edits), gated on `Background.emit?/1`, using each recipe's own resolved page dims.

Purpose: extend the tracer-proven mechanism to the full recipe family with zero bespoke dark code (D-05) and zero light-path byte drift.
Output: all 7 recipes background-wired; the 5 verify-only recipes' light goldens stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-CONTEXT.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-RESEARCH.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-PATTERNS.md
@.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire background region+section into Payslip, Invoice, Receipt</name>
  <files>lib/rendro/recipes/payslip.ex, lib/rendro/recipes/invoice.ex, lib/rendro/recipes/receipt.ex</files>
  <read_first>
    - lib/rendro/recipes/background.ex (helper from 121-01 — emit?/1, region/2, section/3)
    - lib/rendro/recipes/payslip.ex:102-179 (page_template/1 `geometry(opts)` → g.pw/g.ph; sections/2 prepend point), payslip.ex:680-699 (nil-branch already carries background — verify only)
    - lib/rendro/recipes/invoice.ex:34-35 (@page_width/@page_height constants), invoice.ex:94-165 (page_template/1 + sections/2), invoice.ex:468-490 (palette nil-branch — verify)
    - lib/rendro/recipes/receipt.ex:84-85 (@page_width/@page_height), receipt.ex:138-220 (page_template/1 + sections/2), receipt.ex:474-490 (palette nil-branch — verify)
    - .planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-PATTERNS.md (the 5-recipe wiring shape)
  </read_first>
  <action>
    For each of Payslip, Invoice, Receipt, apply the uniform wiring shape (do NOT touch text draw-sites or `palette/1`):
    - In `page_template/1`: compute `colors = palette(opts)`; prepend `Background.region(pw, ph)` as the FIRST element of the `regions:` list iff `Background.emit?(colors)`, else leave the list unchanged. Ensure the `:regions` key remains inside any `Keyword.take` whitelist so it threads to the struct.
    - In `sections/2`: prepend `Background.section(colors, pw, ph)` as the FIRST section iff `Background.emit?(colors)`. Gate BOTH on the same `palette(opts)` (Pitfall 3).
    - Resolve dims per recipe: Payslip via `geometry(opts)` → `g.pw`, `g.ph`; Invoice via `@page_width`, `@page_height`; Receipt via `@page_width`, `@page_height`. Never hardcode dims inside the helper (Pitfall 4).
    Verify (do not edit) that each recipe's `palette/1` nil-branch already carries `background` and every `Rendro.text` already passes a swappable `color:` role (D-02 verify-only).
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/payslip_byte_identity_test.exs test/rendro/recipes/invoice_byte_identity_test.exs test/rendro/recipes/receipt_byte_identity_test.exs test/rendro/recipes/no_inline_color_literals_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The three named `*_byte_identity_test.exs` pass with frozen sha256 values UNCHANGED (no re-bless) — no-theme paths byte-identical (MODE-02 light).
    - `mix test test/rendro/recipes/no_inline_color_literals_test.exs` passes.
    - `git diff --stat lib/rendro/recipes/{payslip,invoice,receipt}.ex` shows edits only in `page_template/1` and `sections/2` (no text draw-site or `palette/1` change).
    - Each recipe's background wiring gates region AND section on `Background.emit?(palette(opts))` and passes that recipe's own resolved `pw`/`ph`.
  </acceptance_criteria>
  <done>Payslip, Invoice, Receipt are background-wired on their own dims with region+section both gated on emit?; light byte-identity holds with no bless; no text/palette edits.</done>
</task>

<task type="auto">
  <name>Task 2: Wire background region+section into BrandedInvoice and Ticket</name>
  <files>lib/rendro/recipes/branded_invoice.ex, lib/rendro/recipes/ticket.ex</files>
  <read_first>
    - lib/rendro/recipes/background.ex (helper from 121-01)
    - lib/rendro/recipes/branded_invoice.ex:49-115 (page_template/1 — A4 default page, content width 451.28 = 595.28 - 2*72; sections/2), branded_invoice.ex:229-245 (palette nil-branch — carries background at :238, verify only)
    - lib/rendro/recipes/ticket.ex:133-244 (page_template/1 `geometry(opts)` → g.pw/g.ph; sections/2), ticket.ex:512-525 (palette nil-branch — carries background at :521, verify only)
    - .planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-PATTERNS.md
  </read_first>
  <action>
    Apply the same uniform wiring shape (region + section prepend, both gated on `Background.emit?(palette(opts))`, dims from each recipe) to BrandedInvoice and Ticket, without touching text draw-sites or `palette/1`:
    - Ticket: resolve dims via `geometry(opts)` → `g.pw`, `g.ph`.
    - BrandedInvoice: it has no `@page_width`/`@page_height` constants — it uses the A4 default page (595.28 x 841.89). Confirm the template's actual page width/height and pass those to `Background.region`/`Background.section`; do not invent smaller content-box dims (the fill must cover the full page, not the 451.28 content column).
    Verify (do not edit) that both nil-branches already carry `background` and every `Rendro.text` already passes a swappable `color:` role.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/recipes/ticket_byte_identity_test.exs test/rendro/recipes/no_inline_color_literals_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both named `*_byte_identity_test.exs` pass with frozen sha256 values UNCHANGED (no re-bless).
    - `mix test test/rendro/recipes/no_inline_color_literals_test.exs` passes.
    - BrandedInvoice background region/section use the full A4 page dims (595.28 x 841.89), not the 451.28 content-column width.
    - `git diff --stat lib/rendro/recipes/{branded_invoice,ticket}.ex` shows edits only in `page_template/1` and `sections/2`.
  </acceptance_criteria>
  <done>BrandedInvoice and Ticket are background-wired on their full page dims; light byte-identity holds with no bless; all 7 recipes now carry the region.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → recipe `theme:`/`:palette` opts | Only untrusted input; roles pre-validated by `Rendro.Color.validate/1` in `Theme.resolve/1`. No new external-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-01 | Tampering | `Background.section/3` fill op across 5 recipes | low | accept | Numeric-only fill via `Rendro.Color.rg/1`; shared control from 121-01, no per-recipe code. |
| T-121-02 | Repudiation | light-path byte determinism of the 5 recipes | low | mitigate | The 5 frozen `*_byte_identity_test.exs` goldens gate any drift (must stay green, no bless). |

No package installs; zero new deps — no `T-121-SC` applies.
</threat_model>

<verification>
- `mix test test/rendro/recipes/` green (all 5 byte-identity goldens + no-inline-literal guard).
- No existing `*.sha256` under `priv/goldens/` modified; no `@toy_golden_sha256` literal changed.
</verification>

<success_criteria>
- All 7 recipes carry the shared `:background` region+section, gated value-driven on `emit?/1`.
- The 5 verify-only recipes' light renders are byte-identical; dark works for free via the shared helper + `dark/1` swap.
</success_criteria>

<output>
Create `.planning/phases/121-light-dark-background-fill-mechanism-all-7-recipes/121-03-SUMMARY.md` when done.
</output>
