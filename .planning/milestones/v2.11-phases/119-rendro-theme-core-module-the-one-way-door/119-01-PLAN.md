---
phase: 119
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/theme.ex
  - test/rendro/theme_test.exs
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04, COLOR-01, COLOR-02]
must_haves:
  truths:
    - "%Rendro.Theme{} exposes the full frozen field set (9 color roles + typography/spacing/rules/radius/density/mode); a bare %Rendro.Theme{} equals default/0 (no half-nil trap)."
    - "resolve/1 is idempotent (resolve(resolve(x)) == resolve(x)), deep-merges partial keyword|map|%Theme{} input without KeyError, validates every color role via Rendro.Color.validate/1, and raises an instructive ArgumentError on a bad token (THEME-02)."
    - "from_brand/2 derives on_accent deterministically from a single accent: seed via WCAG max-contrast (D-04 threshold 0.179), returns an integer tuple (one of the theme's own poles), respects an explicit on_accent: override, emits tokens only, and registers NO asset."
    - "dark/1 swaps pre-resolved integer role tuples to the D-05 dark targets (mode: :dark), keeping accent unchanged and on_accent white per R2 — no transcendental color math at draw time."
    - "Every shipped color value is an integer {r,g,b}; type-scale values are integers or single-decimals (D-03 ramp 21/16.5/13/10.5/9/8); no float reaches the stored Theme value."
    - "typography defaults (leading 1.2, widows 2, orphans 2) are metric-identical to %Text{} defaults so later application (Phase 122) is a proven no-op."
  prohibitions:
    - "MUST NOT tint background away from {255,255,255} (sheet-000) — warm character lives in surface (paper-100) + rule (line-400), never the page (D-05, Pitfall 4)."
    - "MUST NOT register an asset in from_brand/2 — no calls to FontRegistry or AssetRegistry; brand (who) stays orthogonal to theme (how) (COLOR-02)."
    - "MUST NOT String.to_atom any caller-supplied string (atom-exhaustion DoS); store compile-time atoms only, use String.to_existing_atom/whitelist if string keys are ever accepted."
    - "MUST NOT use :math.pow or any runtime formula for the type scale — materialize explicit points (TYPE-01/D-03)."
    - "MUST NOT add any web-concept field (shadow/elevation, z-index, motion, focus/hover, opacity/gradient, raw color scales, numeric weight axis, letter-spacing, wide-gamut) to %Theme{} (THEME-04/D-02)."
    - "MUST NOT hand-roll a color validator — reuse Rendro.Color.validate/1 verbatim (its message is already instructive)."
    - "MUST NOT name any industry, recipe family, or named brand in lib/rendro/theme.ex (CONTRACT-03; exact forbidden list at 119-PATTERNS.md:165-173)."
  artifacts:
    - lib/rendro/theme.ex
    - test/rendro/theme_test.exs
  key_links:
    - "resolve/1 -> Rendro.Color.validate/1 (color.ex:67) on every role"
    - "@default_typography leading/widows/orphans -> %Text{} defaults (text.ex:18-22) metric parity"
    - "@default_colors integer tuples -> mined from brand/tokens/tokens.json (D-05), hex->tuple via Base.decode16! at authoring boundary only"
---

<objective>
Create `lib/rendro/theme.ex` — the pure, inert `Rendro.Theme` value: the full frozen token shape (9 color roles + typography + spacing + rules + radius + density + mode) with shared-module-attribute defaults, `@enforce_keys []`, per-group `@type`s, and `@moduledoc tags: [:adapter]`, constructed only via `default/0` · `resolve/1` · `dark/1` · `from_brand/2` with all derivation helpers `defp`/`@doc false`. Create `test/rendro/theme_test.exs` — pure example + `stream_data` property tests proving the shape, idempotence, deep-merge, color validation, `on_accent` derivation, byte-reproducibility, and web-concept exclusion.

Purpose: This is the milestone's one-way door. Once `%Theme{}` ships, its field set and nesting shape become an observable public contract. This plan builds the module and its behavioral proof; Plan 02 registers it on the adapter tier and reconciles the manifest.

Output: `lib/rendro/theme.ex` (compiles clean, `@spec` on every public fn) + `test/rendro/theme_test.exs` (green).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-CONTEXT.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-RESEARCH.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New public symbols introduced by this phase (Plan 01 creates the module + tests; Plan 02 registers it):

- **Module:** `Rendro.Theme` (adapter/Evolving tier).
- **Struct fields:** `colors`, `typography`, `spacing`, `rules`, `radius`, `density`, `mode` (`@enforce_keys []`; all defaulted).
- **Public `@type`s:** `t`, `rgb`, `colors`, `font_role`, `type_step`, `typography`, `spacing`, `rules`, `radius`.
- **Public functions (each `@spec`'d):** `default/0`, `resolve/1`, `dark/1`, `from_brand/2`.
- **Private helpers (`defp`/`@doc false`, never manifested):** `on_accent_for`, `luminance`, `contrast_ratio`, `linearize`, `hex_to_rgb`, `deep_merge`, `normalize` (exact names planner-discretion per D-06/CONTEXT §Claude's Discretion).
- **New test files:** `test/rendro/theme_test.exs` (this plan), `test/docs_contract/theme_industry_guard_test.exs` (Plan 02).
- **Manifest entry (Plan 02):** `"Elixir.Rendro.Theme"` in `priv/public_api.json` (`"tier": "adapter"`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/rendro/theme.ex — struct, defaults, constructors, private derivation helpers</name>
  <files>lib/rendro/theme.ex</files>
  <read_first>
    - lib/rendro/format.ex:1-34,61,83,100,108 — adapter-tier precedent: @moduledoc tags: [:adapter], shared @labels module attr consumed by public fn, @spec above every public fn. Mirror this shape exactly.
    - lib/rendro/recipes/invoice.ex:466-481 — the S1 palette/1 seam: the bare 7-role {r,g,b} map the colors group must be a drop-in superset of, plus the Map.merge(defaults, overrides) deep-merge idiom resolve/1 extends.
    - lib/rendro/text.ex:14-37 — defstruct-with-defaults + @type shape; the metric no-op target (line_height 1.2, widows 2, orphans 2) typography defaults must match.
    - lib/rendro/color.ex:67-138 — validate/1 (:ok | {:error, String.t()}) the resolver reuses verbatim; format_num float boundary (draw-time only) confirming stored values stay integer tuples.
    - lib/rendro/font_registry.ex:11-16 — :default built-in always registered; fonts default is three logical atoms, no resolution this phase.
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-CONTEXT.md — D-01 (frozen field set), D-03 (type scale + leading values), D-04 (on_accent derivation + threshold 0.179 + override rule), D-05 (mined-Swiss palette table + dark swap targets), R2 (dark on_accent stays white).
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-PATTERNS.md:25-120 — copy-ready excerpts for the module header, shared-attr defaults, per-group @type, @spec block, Color.validate call site, hex_to_rgb, branch-only on_accent derivation.
  </read_first>
  <behavior>
    - `%Rendro.Theme{} == Rendro.Theme.default()` (bare struct equals light default; no half-nil).
    - `Map.keys(default().colors)` sorts to `[:accent, :background, :ink, :muted, :negative, :on_accent, :positive, :rule, :surface]` (9 roles), each an integer `{r,g,b}`.
    - `default().typography.scale` == `%{display: 21, title: 16.5, subtitle: 13, body: 10.5, small: 9, caption: 8}`; `leading: 1.2`, `widows: 2`, `orphans: 2`; `fonts: %{heading: :default, body: :default, mono: :default}`.
    - `default().colors.background == {255,255,255}`; `default().colors.surface == {247,243,234}`; `default().colors.rule == {196,188,169}`; `default().colors.ink == {16,24,39}`; `default().colors.accent == {44,107,237}`.
    - `resolve(resolve(x)) == resolve(x)` for keyword|map|%Theme{} input; partial input never KeyErrors and returns a full `%Theme{}`.
    - `resolve(colors: %{ink: "#000"})` raises `ArgumentError` whose message matches `~r/hex/`.
    - `dark(default())` returns mode `:dark` with `ink {242,236,224}`, `background {27,23,19}`, `surface {35,32,25}`, `rule {74,68,57}`, `accent` unchanged `{44,107,237}`, `on_accent` white `{255,255,255}` (R2).
    - `from_brand(accent: {44,107,237}).colors.on_accent in [background, ink]` and is an integer tuple; `from_brand(accent: {44,107,237}, on_accent: {1,2,3}).colors.on_accent == {1,2,3}` (override respected, never recomputed).
  </behavior>
  <action>
Create `lib/rendro/theme.ex` following the Format adapter-tier idiom. Header: a `@moduledoc` carrying the honest stability note ("The field shape is stable; token values and rendered bytes may evolve across minor versions. Express elevation flatly via `surface` tint + `rule` hairline.") plus flat-elevation guidance, and the mandatory `@moduledoc tags: [:adapter]` line (load-bearing — omitting it fails Plan 02's manifest tier assertions).

Define token-group defaults as module attributes shared by both `defstruct` and `default/0` (the Format `@labels` idiom) so a bare `%Rendro.Theme{}` already equals the light default (D-01, no half-nil):
- `@default_colors` — the 9 integer tuples from D-05 (119-CONTEXT.md:132-142 light column): ink {16,24,39}, muted {91,101,115}, accent {44,107,237}, on_accent {255,255,255}, background {255,255,255}, surface {247,243,234}, rule {196,188,169}, positive {20,122,75}, negative {194,65,50}. All 9 ALWAYS present, never nil.
- `@default_typography` — fonts %{heading: :default, body: :default, mono: :default}; scale %{display: 21, title: 16.5, subtitle: 13, body: 10.5, small: 9, caption: 8} (D-03, explicit points, NO :math.pow); leading 1.2, widows 2, orphans 2 (metric-identical to %Text{}).
- `@default_spacing`, `@default_rules`, `@default_radius` — point-valued semantic named steps for the frozen shape (spacing: unit/tight/normal/loose/section; rules: hairline/thin/thick stroke widths; radius: none/sm/md corner radii). These are Evolving values (not golden-gated this phase); pick restrained clean numbers (e.g. spacing {unit:6,tight:4,normal:8,loose:12,section:24}, rules {hairline:0.5,thin:1,thick:2}, radius {none:0,sm:2,md:4}) — the SHAPE is frozen, the numbers refine later.
- `defstruct` referencing all group attrs plus `density: :comfortable`, `mode: :light`; state `@enforce_keys []` explicitly (D-01 — every future field is a non-breaking addition).

Add `@type` per group per D-01/THEME-03: `rgb :: {0..255,0..255,0..255}`, `font_role :: atom()`, `type_step :: number()`, closed `%{required(k) => v}` map types for colors/typography/spacing/rules/radius, and `t :: %__MODULE__{...}` with `density: :comfortable | :compact`, `mode: :light | :dark`. Use closed required-key maps so the frozen shape is machine-checkable (RESEARCH Open Q1).

Public functions, each with a `@spec` immediately above it:
- `default/0 :: t()` — returns `%__MODULE__{}` (the shared-attr defaults).
- `resolve/1 :: (t() | map() | keyword()) -> t()` — normalize input to a map, `deep_merge/2` it onto the default groups (extend the invoice.ex Map.merge idiom to nested groups; partial input never KeyErrors), then validate EVERY color role by calling `Rendro.Color.validate/1` and `raise ArgumentError, reason` on `{:error, reason}` (reuse the instructive message verbatim). Idempotent: `resolve(resolve(x)) == resolve(x)`. Honor `density: :compact` shallowly (a pure leading/spacing nudge, no new field — planner discretion per CONTEXT §Claude's Discretion).
- `dark/1 :: (t()) -> t()` — resolve, then swap the pre-resolved integer role tuples to the D-05 dark column (119-CONTEXT.md:132-142): ink {242,236,224}, muted {150,143,126}, background {27,23,19}, surface {35,32,25}, rule {74,68,57}, positive {63,179,127}, negative {224,113,95}; accent unchanged; on_accent stays white per R2; set mode :dark. No draw-time float tint math — swap resolved tuples only.
- `from_brand/2 :: (keyword(), keyword()) -> t()` — signature `from_brand(brand_tokens, opts \\ [])` where brand_tokens carries the `accent:` seed (+ optional `on_accent:` override + optional brand color tokens) and opts carries mode/density. Derive on_accent via the private WCAG max-contrast helper unless explicitly overridden; return a fully resolved `%Theme{}`. Emit tokens ONLY — never call FontRegistry or AssetRegistry (COLOR-02). The `\\ []` default is intentional; do not hand-tune the resulting arities — Plan 02 regenerates the manifest to reflect the real surface.

Private helpers, ALL `defp`/`@doc false` so they never enter the manifest (D-06): `on_accent_for` (WCAG max-contrast: compute relative luminance of accent, compare to threshold 0.179, return whichever of the theme's own poles — background vs ink — has greater contrast; the float picks a BRANCH only, output is always an integer tuple per D-04), `luminance`, `contrast_ratio`, `linearize`, `hex_to_rgb` (Base.decode16! + binary match — authoring convenience only; stored attrs hold already-resolved integer tuples), `deep_merge`, `normalize`. Exact names are planner-discretion.

The file must contain NO industry, recipe-family, or named-brand identifier (CONTRACT-03 guard in Plan 02 greps for these; exact forbidden list at 119-PATTERNS.md:165-173).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/rendro/theme.ex` compiles with `--warnings-as-errors` (no unused/undefined warnings).
    - Source contains `@moduledoc tags: [:adapter]` and a `@spec` line immediately above each of `default`, `resolve`, `dark`, `from_brand`.
    - `iex -S mix` (or a `mix run -e` one-liner): `Rendro.Theme.default().colors` has exactly the 9 role keys, all integer tuples; `background == {255,255,255}`.
    - No `:math.pow` in the file; `default().typography.scale` values are the exact D-03 numbers.
    - `on_accent_for`, `hex_to_rgb`, `deep_merge`, `luminance`, `contrast_ratio`, `linearize`, `normalize` are all defined with `defp`.
    - No industry/recipe-family/named-brand token present in the source (verified green by Plan 02's guard).
  </acceptance_criteria>
  <done>lib/rendro/theme.ex exists, compiles warnings-as-errors, exposes the full frozen shape via default/0/resolve/1/dark/1/from_brand/2 with @spec on each, all derivation helpers private, matching D-01/D-03/D-04/D-05/R2 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create test/rendro/theme_test.exs — example + property unit tests</name>
  <files>test/rendro/theme_test.exs</files>
  <read_first>
    - lib/rendro/theme.ex — the module under test (created in Task 1); confirm the exact public surface and default values.
    - test/support/generators.ex:5,11-23 — stream_data usage: `use ExUnitProperties`, `gen all`, `integer(0..255)` rgb component generators to model rgb_gen()/partial_map()/theme_or_partial().
    - lib/rendro/color.ex:67 — Color.validate/1 for the per-role integer assertion.
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-VALIDATION.md:42-63 — the authoritative Requirement -> Test map with exact assertions and 1-arg from_brand call forms.
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-PATTERNS.md:124-145 — coverage checklist + property-generator idiom.
  </read_first>
  <action>
Create `test/rendro/theme_test.exs` with `use ExUnit.Case, async: true` and `use ExUnitProperties`. Implement inline property generators `rgb_gen()` (three `integer(0..255)` -> tuple), `partial_map()` (a subset of theme keys), and `theme_or_partial()` (keyword | map | `%Theme{}`) — or extend test/support/generators.ex if cleaner.

Cover, per 119-VALIDATION.md:42-63 (exact assertions):
- THEME-01: `assert %Rendro.Theme{} == Rendro.Theme.default()`; `Map.keys(t.colors)` sorted == the 9 roles; `t.typography.scale` has 6 steps; `:spacing/:rules/:radius/:density/:mode` all present.
- THEME-02 (property): `check all t <- theme_or_partial(), do: assert Theme.resolve(Theme.resolve(t)) == Theme.resolve(t)` (idempotence); `check all part <- partial_map(), do: assert match?(%Theme{}, Theme.resolve(part))` (deep-merge never KeyErrors); every color role `assert Color.validate(role_value) == :ok`; `assert_raise ArgumentError, ~r/hex/, fn -> Theme.resolve(colors: %{ink: "#000"}) end`.
- COLOR-01: exact key set `[:accent,:background,:ink,:muted,:negative,:on_accent,:positive,:rule,:surface]`.
- COLOR-02: `from_brand(accent: {44,107,237}).colors.accent == {44,107,237}`; `on_accent in [background, ink]`; override `from_brand(accent: {44,107,237}, on_accent: {1,2,3}).colors.on_accent == {1,2,3}`; property `check all rgb <- rgb_gen()` -> `is_integer(elem(from_brand(accent: rgb).colors.on_accent, 0))` and stable across two calls (branch determinism); assert `from_brand/2` is a pure return (no FontRegistry/AssetRegistry side effect — the struct carries only logical font atoms).
- dark/1: `dark(default())` role tuples match the D-05 dark column; accent unchanged; on_accent white (R2); mode :dark.
- THEME-04: recurse every group map and assert no `:shadow/:elevation/:z_index/:opacity/:gradient/:motion/:letter_spacing` key exists.
- THEME-03 (spec presence): a `Code.Typespec.fetch_specs/1` check asserting `default/0, resolve/1, dark/1, from_brand/2` each have a spec.
- Byte-repro: `for {_role, {r,g,b}} <- Theme.default().colors, do: assert is_integer(r) and is_integer(g) and is_integer(b)` (and same for `Theme.dark(Theme.default())`); `for {_step, v} <- Theme.default().typography.scale, do: assert v == Float.round(v, 1)` (rejects :math.pow irrationals).

No fenced implementations here beyond ExUnit test bodies; keep tests pure (no rendering, no I/O).
  </action>
  <verify>
    <automated>mix test test/rendro/theme_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rendro/theme_test.exs` is green with 0 failures.
    - The idempotence and deep-merge assertions run as `property` blocks (stream_data), not single examples.
    - The invalid-token test asserts `ArgumentError` matching `~r/hex/`.
    - Byte-repro assertions cover both `default()` and `dark(default())` colors and the type scale.
    - The `Code.Typespec` check confirms all four public fns are `@spec`'d.
  </acceptance_criteria>
  <done>test/rendro/theme_test.exs is green and covers THEME-01/02/04, COLOR-01/02, dark/1 swap, spec presence, and byte-reproducibility with both example and property tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → resolve/1, from_brand/2 | Authoring-time input (keyword/map). Not runtime deserialization of untrusted data; the deterministic pipeline never sees %Theme{} this phase. |
| brand/tokens/tokens.json → module attrs | Values transcribed to integer-tuple literals at authoring time (hex→tuple via Base.decode16!), NOT read at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-01 | Denial of Service | from_brand/2 / resolve/1 accepting caller keys | low | mitigate | Store font roles as compile-time atoms from default/0; do NOT String.to_atom any caller-supplied string. If string keys are ever accepted, use String.to_existing_atom or a fixed keyword whitelist. |
| T-119-02 | Tampering | malformed {r,g,b} reaching the render pipeline | low | mitigate | resolve/1 calls Rendro.Color.validate/1 on every color role and raises an instructive ArgumentError, rejecting non-integer/out-of-range tuples before they can produce a malformed PDF operator downstream. |

No high-severity threats — pure, inert value module with no I/O, network, filesystem, secrets, or runtime deserialization. Does not block (ASVS L1).
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/rendro/theme_test.exs` green (example + property).
- Zero-recipe-change guard (phase-wide, confirmed at Plan 02 gate): this plan touches no recipe file — `git status priv/goldens` stays clean; do NOT set MIX_GOLDEN_BLESS.
</verification>

<success_criteria>
- `%Rendro.Theme{}` exposes the full frozen field set and equals `default/0`.
- `resolve/1` idempotent, deep-merges, validates every color, raises instructively on bad tokens.
- `from_brand/2` derives on_accent deterministically (integer out, override respected), emits tokens only, registers no asset.
- `dark/1` swaps to pre-resolved integer dark tuples (R2 on_accent white).
- All shipped values integer/single-decimal; web concepts absent by construction.
</success_criteria>

<output>
Create `.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-01-SUMMARY.md` when done.
</output>
