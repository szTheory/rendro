---
phase: 119
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - test/docs_contract/theme_industry_guard_test.exs
  - lib/mix/tasks/rendro/api.gen.ex
  - priv/public_api.json
autonomous: true
requirements: [CONTRACT-01, CONTRACT-03, THEME-03]
must_haves:
  truths:
    - "Rendro.Theme appears in priv/public_api.json on the adapter tier with the \"field shape stable; token values and rendered bytes may evolve\" doc note; all derivation helpers (on_accent_for/luminance/contrast_ratio/linearize/hex_to_rgb/deep_merge/normalize) stay out of the manifest functions list (THEME-03/D-06)."
    - "BOTH manifest byte-equality tests reconcile green together after `mix rendro.api.gen` + commit — RG-1 test/docs_contract/public_api_contract_test.exs:72 AND RG-2 test/rendro/public_api/manifest_test.exs:98 (CONTRACT-01/D-06; the Phase-115 duplicate-assertion lesson)."
    - "An industry/brand source-grep guard on lib/rendro/theme.ex fails if any industry, recipe-family, or named-brand term (or preset/catalog/configurator) appears in the source (CONTRACT-03/D-02)."
    - "Full `mix test` is green with zero golden re-bless and `git status priv/goldens` clean — every one of the 62 committed v2.10 `.sha256` goldens stays byte-identical."
  prohibitions:
    - "MUST NOT set MIX_GOLDEN_BLESS or modify any priv/goldens/**/*.sha256 — a changed hash is a defect, not a refresh."
    - "MUST NOT touch any recipe file (lib/rendro/recipes/**) — zero recipe change this phase."
    - "MUST NOT reconcile only one manifest byte-equality test — both RG-1 (public_api_contract_test.exs:72) and RG-2 (manifest_test.exs:98) must be addressed."
    - "MUST NOT expose any derivation helper (on_accent_for/luminance/contrast_ratio/linearize/hex_to_rgb/deep_merge/normalize) in the manifest functions list for Elixir.Rendro.Theme."
    - "MUST NOT hand-edit priv/public_api.json — regenerate via `mix rendro.api.gen` (the byte-equality tests reject hand edits)."
  artifacts:
    - test/docs_contract/theme_industry_guard_test.exs
    - lib/mix/tasks/rendro/api.gen.ex
    - priv/public_api.json
  key_links:
    - "lib/mix/tasks/rendro/api.gen.ex @public_modules (adapter block ~L44-104, next to Rendro.Format at :86) -> Rendro.Theme entry -> tier derived from @moduledoc tags: [:adapter] via Code.fetch_docs/1 (api.gen.ex:116-121)"
    - "mix rendro.api.gen -> encode_manifest/1 (api.gen.ex:140-158) -> priv/public_api.json \"Elixir.Rendro.Theme\" adapter entry -> RG-1 (public_api_contract_test.exs:72) + RG-2 (manifest_test.exs:98) byte-equality"
    - "test/docs_contract/theme_industry_guard_test.exs -> File.read!(\"lib/rendro/theme.ex\") + refute source =~ term (mirror integrations_claims_test.exs:37-42)"
---

<objective>
Register `Rendro.Theme` on the adapter/Evolving tier and lock the two industry/regression guards. Three coordinated touches after Plan 01's `lib/rendro/theme.ex` exists: (1) create the CONTRACT-03 source-grep tripwire `test/docs_contract/theme_industry_guard_test.exs`; (2) add `Rendro.Theme` to `@public_modules` in `lib/mix/tasks/rendro/api.gen.ex` (one line, adapter block); (3) regenerate `priv/public_api.json` via `mix rendro.api.gen` — the pre-declared planned red→green step that turns BOTH manifest byte-equality assertions (RG-1 + RG-2) from red to green together. Finally, prove the phase-wide zero-recipe-change gate: full `mix test` green with no golden re-bless and `git status priv/goldens` clean.

Purpose: This is the manifest side of the milestone's one-way door. Plan 01 built the inert `%Theme{}` value + its behavioral proof; Plan 02 makes the field shape an observable public contract on the adapter tier, fences `theme.ex` against industry/brand leakage (CONTRACT-03), and byte-proves that adding a new module changed zero rendered output (every v2.10 golden untouched).

Output: `test/docs_contract/theme_industry_guard_test.exs` (green), one new line in `lib/mix/tasks/rendro/api.gen.ex`, a regenerated + committed `priv/public_api.json` carrying the `"Elixir.Rendro.Theme"` `"tier": "adapter"` entry, with RG-1 + RG-2 green and `priv/goldens` untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-CONTEXT.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-RESEARCH.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-PATTERNS.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-VALIDATION.md
@.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-01-PLAN.md
</context>

<artifacts_this_phase_produces>
New public symbols / artifacts introduced by this plan (Plan 01 created the module + unit tests; Plan 02 registers and fences it):

- **New test file:** `test/docs_contract/theme_industry_guard_test.exs` — the CONTRACT-03 source-grep tripwire (two tests: no-industry/brand terms; no preset/catalog/configurator machinery), sibling of `branding_contract_test.exs` / `integrations_claims_test.exs`.
- **Registry edit:** one new `Rendro.Theme` line in the `@public_modules` adapter block of `lib/mix/tasks/rendro/api.gen.ex` (~L44-104, adjacent to `Rendro.Format` at :86). Tier derived from Plan 01's `@moduledoc tags: [:adapter]`.
- **Manifest entry:** `"Elixir.Rendro.Theme"` in `priv/public_api.json` with `"tier": "adapter"`, `functions: ["dark/1","default/0","from_brand/2","resolve/1"]` (alphabetical), and per-group `types` — generated by `mix rendro.api.gen`, never hand-edited. Derivation helpers absent from `functions`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create test/docs_contract/theme_industry_guard_test.exs — CONTRACT-03 industry/brand source-grep tripwire</name>
  <files>test/docs_contract/theme_industry_guard_test.exs</files>
  <read_first>
    - test/docs_contract/integrations_claims_test.exs:37-42 — the exact working `source = File.read!(path)` + `refute source =~ term` tripwire pattern to mirror (inverted from the `assert source =~` claim form to `refute` for a guard).
    - test/docs_contract/branding_contract_test.exs:1-4 — sibling placement + `use ExUnit.Case, async: true` convention for `test/docs_contract/` guards.
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-PATTERNS.md:148-176 — the copy-ready guard shape (forbidden `~w(...)` list, `refute String.contains?` equivalent variant, assert-the-positive note).
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-CONTEXT.md — D-02 (excluded-by-construction + industry-agnostic guard) and CONTRACT-03 (one theme + from_brand/2, no genre/preset machinery).
  </read_first>
  <action>
Create `test/docs_contract/theme_industry_guard_test.exs` as a sibling of `branding_contract_test.exs`, `use ExUnit.Case, async: true`, module name `Rendro.DocsContract.ThemeIndustryGuardTest`. Mirror the `File.read!` + `refute source =~ term` shape from `integrations_claims_test.exs:37-42`, inverted to a tripwire.

Test 1 ("theme.ex names no industry, recipe family, or named brand"): read `source = File.read!("lib/rendro/theme.ex")`, then `for term <- forbidden, do: refute source =~ term` over the exact forbidden list at 119-PATTERNS.md:165-173 (the recipe-family / industry terms `invoice payslip ticket certificate statement receipt medical legal restaurant retail` plus any named brands from that list). Use the forbidden-term identifiers verbatim from 119-PATTERNS.md:165-173 as the source of truth for the list — do not invent additional terms.

Test 2 ("theme.ex ships one theme + from_brand/2 only — no genre/preset machinery"): read the same source and `for term <- ~w(preset catalog configurator genre), do: refute source =~ term` (CONTRACT-03 / D-02 — B ships exactly `default/0` + `from_brand/2`, no genre presets, catalog, or configurator).

Assert the positive too (CONTRACT-03 "one theme + from_brand/2 only"): assert the source contains the `from_brand` and `default` public-function definitions (a lightweight `=~` on the def names) so the guard proves the intended surface exists, not merely that forbidden terms are absent.

Keep the guard pure (static `File.read!` + regex only — no compilation, no rendering, no I/O beyond reading the source file). Do NOT place forbidden literal terms anywhere except inside the `refute` list data — the guard's own list is the allowlisted home for those tokens.
  </action>
  <verify>
    <automated>mix test test/docs_contract/theme_industry_guard_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/docs_contract/theme_industry_guard_test.exs` is green (both tests pass against the Plan 01 `theme.ex`, which is already industry-agnostic).
    - The guard reads `lib/rendro/theme.ex` via `File.read!` and uses `refute source =~ term` (or the equivalent `refute String.contains?`) over the forbidden list.
    - The forbidden list matches 119-PATTERNS.md:165-173 (industry/recipe-family/named-brand terms) AND includes `preset`/`catalog`/`configurator`/`genre`.
    - `grep -c "File.read!(\"lib/rendro/theme.ex\")" test/docs_contract/theme_industry_guard_test.exs` >= 1.
    - The test asserts the positive `from_brand`/`default` surface exists.
    - No recipe file and no other lib/ file is touched by this task.
  </acceptance_criteria>
  <done>test/docs_contract/theme_industry_guard_test.exs exists, is green, greps lib/rendro/theme.ex for the industry/recipe-family/named-brand + preset/catalog/configurator forbidden terms via refute, and asserts the one-theme + from_brand/2 positive surface (CONTRACT-03 / D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Register Rendro.Theme in @public_modules + regenerate priv/public_api.json — the planned red→green reconcile (RG-1 + RG-2)</name>
  <files>lib/mix/tasks/rendro/api.gen.ex, priv/public_api.json</files>
  <read_first>
    - lib/mix/tasks/rendro/api.gen.ex:44-104 — the `@public_modules` list (adapter-tier block, comment at :77); `Rendro.Format` at :86 is the exact adjacency target. Add ONE sibling line `Rendro.Theme`.
    - lib/mix/tasks/rendro/api.gen.ex:116-121 — tier derived from `@moduledoc tags: [:adapter]` via `Code.fetch_docs/1` at gen time (Plan 01 already put the tag on the module; source-list position is cosmetic).
    - lib/mix/tasks/rendro/api.gen.ex:140-158 — `encode_manifest/1` sorts functions/types alphabetically and emits pretty JSON + trailing `"\n"` (api.gen.ex:125). Do NOT hand-edit; regenerate.
    - priv/public_api.json:247-255 — the `Elixir.Rendro.Format` adapter entry shape the new `Elixir.Rendro.Theme` entry mirrors (`functions`, `"tier": "adapter"`, `types`).
    - test/docs_contract/public_api_contract_test.exs:72 — RG-1: `assert fresh_json == checked_in` (byte-equality); goes red the instant Theme enters @public_modules, green after gen+commit.
    - test/rendro/public_api/manifest_test.exs:98 — RG-2: the plan-unlisted duplicate `assert fresh_json == checked_in` (Phase-115 lesson / D-06). Both reconcile together.
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-RESEARCH.md:116-146 — full enumerated red→green list (RG-1/RG-2 red; G-1..G-7 stay green) + the reconcile procedure + grep receipts.
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-PATTERNS.md:180-218 — the exact one-line api.gen edit + the expected regenerated JSON entry shape.
  </read_first>
  <action>
Two coordinated edits, then regenerate.

Step 1 — MODIFY `lib/mix/tasks/rendro/api.gen.ex`: add exactly one line, `Rendro.Theme`, to the adapter-tier block of `@public_modules` (~L44-104), placed logically next to `Rendro.Format` (:86). The list is a plain registry (not source-sorted); position is cosmetic because `encode_manifest/1` sorts the output. Tier comes from Plan 01's `@moduledoc tags: [:adapter]` — no tier argument here. Change NOTHING else in this file.

Step 2 — REGENERATE `priv/public_api.json`: run `mix rendro.api.gen`. This is the pre-declared planned red→green step. The regenerated manifest gains the `"Elixir.Rendro.Theme"` entry with `"tier": "adapter"`, `functions: ["dark/1","default/0","from_brand/2","resolve/1"]` (alphabetical), and the per-group `types` (rgb, colors, typography, spacing, rules, radius, font_role, type_step, t — alphabetized). Confirm the derivation helpers (`on_accent_for`, `luminance`, `contrast_ratio`, `linearize`, `hex_to_rgb`, `deep_merge`, `normalize`) are ABSENT from `functions` (they are `defp`/`@doc false` per Plan 01, so RG byte-equality plus G-1/G-4 hidden-modules assertions stay green).

Step 3 — RECONCILE BOTH byte-equality assertions (do NOT list only one — the Phase-115 duplicate trap, D-06): after regen, run `mix test test/docs_contract/public_api_contract_test.exs test/rendro/public_api/manifest_test.exs`. RG-1 (`public_api_contract_test.exs:72`) AND RG-2 (`manifest_test.exs:98`) both go green together. Use the completeness check `grep -rn "fresh_json == checked_in" test/` (must return exactly 2 hits — RG-1 and RG-2) to confirm no third byte-compare was missed. Also confirm the green-stay assertions survive: `manifest_test.exs:57` (no untagged entries — passes because the module is `adapter`-tagged) and the hidden-modules lists (G-1 `public_api_contract_test.exs:85`, G-4 `manifest_test.exs:29`) — Theme is public, not hidden, so those are unaffected.

Do NOT hand-edit `priv/public_api.json` — the byte-equality tests reject any manual edit; the generator is the only writer.
  </action>
  <verify>
    <automated>mix rendro.api.gen && mix test test/docs_contract/public_api_contract_test.exs test/rendro/public_api/manifest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"Elixir.Rendro.Theme"' priv/public_api.json` == 1, with `"tier": "adapter"` on that entry.
    - The `Elixir.Rendro.Theme` `functions` list is exactly `["dark/1","default/0","from_brand/2","resolve/1"]` — no `on_accent_for`/`luminance`/`contrast_ratio`/`linearize`/`hex_to_rgb`/`deep_merge`/`normalize`.
    - RG-1 + RG-2 both green: `mix test test/docs_contract/public_api_contract_test.exs test/rendro/public_api/manifest_test.exs` passes with 0 failures.
    - `grep -rn "fresh_json == checked_in" test/` returns exactly 2 hits (RG-1 public_api_contract_test.exs:72, RG-2 manifest_test.exs:98) — completeness proof.
    - `lib/mix/tasks/rendro/api.gen.ex` changed by exactly one added line (`Rendro.Theme` in the adapter block); no other diff.
    - `priv/public_api.json` was produced by `mix rendro.api.gen` (trailing newline preserved), not hand-edited.
  </acceptance_criteria>
  <done>Rendro.Theme is registered in @public_modules (one line, adapter block), priv/public_api.json regenerated with the "Elixir.Rendro.Theme" adapter entry (helpers absent from functions), and BOTH byte-equality assertions RG-1 + RG-2 reconcile green (CONTRACT-01 / THEME-03 / D-06).</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full mix test green with zero golden re-bless (zero-recipe-change regression proof)</name>
  <files>priv/public_api.json</files>
  <read_first>
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-VALIDATION.md:55-63 — the zero-recipe-change regression row + the `git status priv/goldens` clean requirement (62 committed `.sha256` unchanged).
    - .planning/phases/119-rendro-theme-core-module-the-one-way-door/119-RESEARCH.md:355-364 — the central zero-recipe-change guard: run existing recipe suites unchanged WITHOUT MIX_GOLDEN_BLESS; a changed hash is a defect, not a refresh; add an explicit `git status priv/goldens` check.
    - test/rendro/recipes/invoice_byte_identity_test.exs:12 — the frozen-sha regression pattern the recipe goldens enforce (referenced; do not modify).
  </read_first>
  <action>
Run the phase-wide regression gate proving that adding `Rendro.Theme` (a new module imported by nothing this phase) changed zero rendered output. Run the full suite with NO `MIX_GOLDEN_BLESS` set in the environment: `mix test`. All recipe byte-identity suites (`invoice_byte_identity_test.exs`, `payslip_byte_identity_test.exs`, `ticket_byte_identity_test.exs`) plus every recipe example test must stay green against the frozen goldens.

Then run the explicit committed-golden integrity check: `git status --porcelain priv/goldens` must return empty output — the 62 committed `priv/goldens/**/*.sha256` files across certificate/invoice/payslip/receipt/statement/ticket are unmodified. If any `.sha256` shows as modified, STOP and treat it as a defect (a changed hash is a regression, not a refresh) — do NOT set `MIX_GOLDEN_BLESS` to "fix" it. Also confirm no `lib/rendro/recipes/**` file was touched this phase (`git status --porcelain lib/rendro/recipes` empty).

This task adds no new source; it is the acceptance gate. The only working-tree changes across the phase are the new module + tests (Plan 01), the new guard test + the one api.gen line + the regenerated `priv/public_api.json` (Plan 02) — never a golden and never a recipe.
  </action>
  <verify>
    <automated>mix test && git status --porcelain priv/goldens lib/rendro/recipes</automated>
  </verify>
  <acceptance_criteria>
    - Full `mix test` is green (0 failures) with `MIX_GOLDEN_BLESS` UNSET.
    - `git status --porcelain priv/goldens` prints nothing (all 62 committed `.sha256` byte-identical).
    - `git status --porcelain lib/rendro/recipes` prints nothing (zero recipe change this phase).
    - No `MIX_GOLDEN_BLESS` was exported at any point during the run.
  </acceptance_criteria>
  <done>Full mix test green with no golden re-bless; `git status priv/goldens` and `git status lib/rendro/recipes` both clean — every v2.10 golden stays byte-identical, proving the new Theme module changed zero rendered output (phase success criterion 4 + zero-recipe-change gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `@public_modules` list → `mix rendro.api.gen` → `priv/public_api.json` | Compile-time registry drives a generated, committed artifact. No runtime/untrusted input; the generator reads first-party module docs only. |
| `File.read!("lib/rendro/theme.ex")` (guard test) | Reads a first-party source file at test time; static regex, no execution of the read content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-03 | Tampering | hand-edited `priv/public_api.json` diverging from source | low | mitigate | Never hand-edit — regenerate via `mix rendro.api.gen`; RG-1 + RG-2 byte-equality tests reject any manual edit. |
| T-119-04 | Information Disclosure | industry/brand term leaking into the public `theme.ex` surface | low | mitigate | `theme_industry_guard_test.exs` greps the source and fails on any forbidden industry/recipe-family/named-brand or preset/catalog/configurator term (CONTRACT-03). |

No new runtime attack surface — this plan adds a mix-task registry line + two static tests + a regenerated manifest. No I/O, network, secrets, or runtime deserialization. Does not block (ASVS L1).
</threat_model>

<verification>
- `mix test test/docs_contract/theme_industry_guard_test.exs` green (CONTRACT-03 guard).
- `mix rendro.api.gen` regenerates `priv/public_api.json` with the `"Elixir.Rendro.Theme"` `"tier": "adapter"` entry; helpers absent from `functions`.
- `mix test test/docs_contract/public_api_contract_test.exs test/rendro/public_api/manifest_test.exs` green — RG-1 (public_api_contract_test.exs:72) AND RG-2 (manifest_test.exs:98) reconciled together.
- `grep -rn "fresh_json == checked_in" test/` returns exactly 2 hits (completeness proof — no missed byte-compare).
- Full `mix test` green with `MIX_GOLDEN_BLESS` unset; `git status --porcelain priv/goldens lib/rendro/recipes` empty (zero-recipe-change gate).
</verification>

<success_criteria>
- `Rendro.Theme` ships on the adapter/Evolving tier in a regenerated `priv/public_api.json` with the "output may evolve" doc note; derivation helpers private/absent from the manifest (Success Criterion 4 / THEME-03).
- The pre-declared planned red→green reconcile turns BOTH RG-1 and RG-2 green together after gen+commit (Success Criterion 4 / CONTRACT-01 / D-06 duplicate-assertion lesson).
- The industry-agnostic guard fails if `theme.ex` names any industry/recipe-family/named-brand or preset/catalog/configurator term (Success Criterion 5 / CONTRACT-03).
- Full `mix test` green with zero golden re-bless and clean `priv/goldens`/`lib/rendro/recipes` — every v2.10 golden byte-identical (zero-recipe-change gate).
</success_criteria>

<output>
Create `.planning/phases/119-rendro-theme-core-module-the-one-way-door/119-02-SUMMARY.md` when done.
</output>
