---
phase: 120
plan: 03
type: execute
wave: 2
depends_on: [120-01, 120-02]
files_modified:
  - lib/rendro/recipes/statement.ex
  - lib/rendro/recipes/certificate.ex
  - lib/rendro/recipes/receipt.ex
  - lib/rendro/recipes/branded_invoice.ex
  - test/rendro/recipes/statement_opts_threading_test.exs
  - test/rendro/recipes/certificate_opts_threading_test.exs
  - test/rendro/recipes/receipt_opts_threading_test.exs
  - test/rendro/recipes/branded_invoice_opts_threading_test.exs
autonomous: true
requirements: [PLUMB-02, PLUMB-03]
must_haves:
  truths:
    - "All 4 retrofitted recipes (Statement, Certificate, Receipt, BrandedInvoice) extend their palette/1 base to read Rendro.Theme.resolve(theme).colors when a :theme opt is present, keeping the literal-defaults map as the nil branch; Map.merge(base, :palette-override) order is preserved so an explicit :palette still wins (D-01, PLUMB-02)."
    - "This is the theme SWAP — committed SEPARATELY from the Plan 01/02 retrofit commits (split-commit discipline, research line 48). The retrofit already moved every inline literal into a role read; the swap adds ONLY the `case opts[:theme]` branch to each palette/1."
    - "A supplied :theme changes the rendered colors (a themed render differs from the no-theme render) and threads without KeyError through document/2 → page_template/1 → sections/2 for all 4 recipes (PLUMB-02)."
    - "The no-theme path stays byte-identical: document(data) with no :theme reproduces the frozen sha256 baselines from Plan 01/02 and keeps edge_matrix green (PLUMB-03 — the central regression guard)."
    - "Each recipe's *_opts_threading_test.exs asserts: :theme threads through page_template/1 without KeyError; a themed render differs from no-theme; :palette override wins over :theme (D-01); and no-theme sections equal empty-opts sections (byte-identity, PLUMB-03)."
  prohibitions:
    - "MUST NOT combine this swap with the retrofit in one commit — this plan is a separate commit set (research line 48)."
    - "MUST NOT re-bless any byte-identity or edge_matrix golden — the no-theme path MUST still match the Plan 01/02 frozen sha256s; a drift is a defect (golden.ex L84)."
    - "MUST NOT add :theme/:palette to any page_template Keyword.take list — they must remain dropped so they thread to palette/1."
    - "MUST NOT hand-roll color resolution or reorder the merge — use Rendro.Theme.resolve/1 and the exact Map.merge(base, Keyword.get(opts, :palette, %{})) order (D-01)."
    - "MUST NOT read any theme.typography.* field — Phase 120 reads colors ONLY (D-04, Phase 122 scope)."
  artifacts:
    - "lib/rendro/recipes/statement.ex :: palette/1 gains `case opts[:theme]` theme branch"
    - "lib/rendro/recipes/certificate.ex :: palette/1 gains theme branch (theme rule overrides {34,34,34} frame default)"
    - "lib/rendro/recipes/receipt.ex :: palette/1 gains theme branch (theme ink recolors primary text)"
    - "lib/rendro/recipes/branded_invoice.ex :: palette/1 gains theme branch"
    - "test/rendro/recipes/statement_opts_threading_test.exs (new)"
    - "test/rendro/recipes/certificate_opts_threading_test.exs (new)"
    - "test/rendro/recipes/receipt_opts_threading_test.exs (new)"
    - "test/rendro/recipes/branded_invoice_opts_threading_test.exs (extend existing with :theme + override-wins cases)"
  key_links:
    - "each palette/1 -> Rendro.Theme.resolve(theme).colors (9 integer-{r,g,b} roles) when opts[:theme] present; nil branch = Plan 01/02 literal defaults"
    - "Map.merge(base, Keyword.get(opts, :palette, %{})) — :palette override is the FINAL layer (D-01 precedence)"
    - "no-theme render -> Plan 01/02 frozen @toy_golden_sha256 (unchanged) + edge_matrix green (PLUMB-03)"
---

<objective>
Swap the 4 retrofitted recipes to read `theme.colors.*`: extend each `palette/1` base to `Rendro.Theme.resolve(theme).colors` when a `:theme` opt is present, keeping the literal-defaults map as the no-theme branch and the `:palette` override as the final merge layer (D-01). Add per-recipe `*_opts_threading_test.exs` proving `:theme` threads without `KeyError`, recolors output, respects `:palette` override precedence, and leaves the no-theme path byte-identical. This is Commit-set 2 (swap) — a separate commit from the Plan 01/02 retrofit.

Purpose: The retrofit (Plans 01/02) proved these recipes byte-identical with literal defaults. This plan makes them actually themable by adding the single `case opts[:theme]` branch to each `palette/1`. Because the retrofit already moved every literal into a role read, the swap is a minimal, mechanical, per-recipe one-branch change — and the no-theme byte-identity guard (PLUMB-03) is preserved by construction.

Output: theme-aware `palette/1` in all 4 retrofitted recipes; 4 `*_opts_threading_test.exs` files (branded extends the existing one).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-RESEARCH.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-PATTERNS.md

# Retrofit context (this plan's dependency):
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-01-SUMMARY.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-02-SUMMARY.md

# Reference swap transform + reference threading test shape:
@lib/rendro/recipes/invoice.ex
@test/rendro/recipes/invoice_opts_threading_test.exs
@lib/rendro/theme.ex
</context>

<artifacts_this_plan_produces>
- Modified: `palette/1` in statement.ex, certificate.ex, receipt.ex, branded_invoice.ex — each gains a `case opts[:theme]` branch reading `Rendro.Theme.resolve(theme).colors`.
- New files: `statement_opts_threading_test.exs`, `certificate_opts_threading_test.exs`, `receipt_opts_threading_test.exs`.
- Extended: `branded_invoice_opts_threading_test.exs` — add `:theme` + `:palette`-override-wins cases.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Swap Statement + Certificate palette/1 to theme.colors + threading tests</name>
  <files>lib/rendro/recipes/statement.ex, lib/rendro/recipes/certificate.ex, test/rendro/recipes/statement_opts_threading_test.exs, test/rendro/recipes/certificate_opts_threading_test.exs</files>
  <read_first>
    - 120-PATTERNS.md § "Shared Pattern A — Commit 2 (swap) transform" (the exact `case opts[:theme]` shape).
    - lib/rendro/recipes/invoice.ex palette/1 (the target swap shape reference) and test/rendro/recipes/invoice_opts_threading_test.exs (reference threading assertions).
    - The Plan 01 SUMMARY for the seam helper names + literal-default maps established in statement.ex / certificate.ex.
  </read_first>
  <action>
    In statement.ex `palette/1`, wrap the existing literal-defaults map (surface {245,245,245} / rule {0,0,0}) in a `case opts[:theme]` where the `nil` branch returns that exact literal map and the `theme ->` branch returns `Rendro.Theme.resolve(theme).colors`; keep the final `Map.merge(base, Keyword.get(opts, :palette, %{}))` line unchanged so `:palette` still wins (D-01). Apply the identical transform to certificate.ex `palette/1`, whose `nil` branch keeps `rule {34,34,34}` and whose `theme ->` branch lets the theme's `rule` recolor the frame. Do NOT edit any section builder (the retrofit already routed all colors through `palette/1`), do NOT touch the whitelists, and do NOT read any `theme.typography.*` field. Then create statement_opts_threading_test.exs and certificate_opts_threading_test.exs mirroring invoice_opts_threading_test.exs with these assertions per recipe: (a) `:theme` threads through `page_template/1` without KeyError (`assert %Rendro.PageTemplate{} = <Recipe>.page_template(theme: Rendro.Theme.default())`); (b) a themed render differs from the no-theme render (theme actually recolors); (c) `:palette` override wins over `:theme` (D-01) — a themed render with a `:palette` role override differs from the plain themed render; (d) `<Recipe>.sections(data) == <Recipe>.sections(data, [])` (no-theme byte-identity, PLUMB-03). For certificate, exercise the frame recolor with a `:theme` and confirm the no-theme + no-border case stays `{34,34,34}`.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/statement_opts_threading_test.exs test/rendro/recipes/certificate_opts_threading_test.exs test/rendro/recipes/statement_byte_identity_test.exs test/rendro/recipes/certificate_byte_identity_test.exs test/rendro/edge_matrix_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Statement + Certificate `palette/1` read `Rendro.Theme.resolve(theme).colors` when `:theme` present; nil branch = literal defaults; `:palette` override wins.
    - Both threading tests pass (theme threads, recolors, override-wins, no-theme byte-identity).
    - Plan 01 byte-identity goldens + Statement/Certificate edge_matrix cells stay green with NO re-bless (no-theme path unchanged).
    - No `theme.typography` read introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Swap Receipt + BrandedInvoice palette/1 to theme.colors + threading tests</name>
  <files>lib/rendro/recipes/receipt.ex, lib/rendro/recipes/branded_invoice.ex, test/rendro/recipes/receipt_opts_threading_test.exs, test/rendro/recipes/branded_invoice_opts_threading_test.exs</files>
  <read_first>
    - 120-PATTERNS.md § "Shared Pattern A — Commit 2 (swap) transform".
    - lib/rendro/recipes/invoice.ex palette/1 + test/rendro/recipes/invoice_opts_threading_test.exs (reference shapes).
    - test/rendro/recipes/branded_invoice_opts_threading_test.exs (existing — extend, do not overwrite its current assertions).
    - The Plan 02 SUMMARY for the seam names + whitelist fixes established in receipt.ex / branded_invoice.ex.
  </read_first>
  <action>
    In receipt.ex `palette/1`, wrap the `ink {0,0,0}` literal-defaults map in a `case opts[:theme]` (`nil ->` literal defaults; `theme -> Rendro.Theme.resolve(theme).colors`), preserving the final `Map.merge(base, Keyword.get(opts, :palette, %{}))`. Apply the identical transform to branded_invoice.ex `palette/1`. A themed `ink` now recolors the primary text seamed in Plan 02. Do NOT edit section builders or whitelists; do NOT read `theme.typography.*`. Then create receipt_opts_threading_test.exs mirroring invoice_opts_threading_test.exs with the four assertions: (a) `:theme` threads through `page_template/1` without KeyError (this guards the Plan 02 whitelist fix); (b) themed render differs from no-theme; (c) `:palette` override (e.g. `palette: %{ink: {200,0,0}}`) wins over `:theme` (D-01); (d) `Receipt.sections(data) == Receipt.sections(data, [])` (byte-identity). Extend the existing branded_invoice_opts_threading_test.exs with the same four assertions (keep its current tests green) — it now also has the whitelist fix from Plan 02 to guard.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/receipt_opts_threading_test.exs test/rendro/recipes/branded_invoice_opts_threading_test.exs test/rendro/recipes/receipt_byte_identity_test.exs test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/edge_matrix_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Receipt + BrandedInvoice `palette/1` read `Rendro.Theme.resolve(theme).colors` when `:theme` present; nil branch = `ink {0,0,0}`; `:palette` override wins.
    - Both threading tests pass, including `:theme` through `page_template/1` without KeyError (whitelist fix guarded).
    - Plan 02 byte-identity goldens + Receipt edge_matrix cells stay green with NO re-bless; branded_invoice net-new golden stays green.
    - No `theme.typography` read introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → recipe `:theme`/`:palette` opts | Caller-supplied `%Theme{}` (or partial theme) and `:palette` override map cross into `palette/1`. No network/session/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-06 | Tampering | `:theme`/`:palette` color tuples reaching `palette/1` | medium | mitigate | `Rendro.Theme.resolve/1` deep-merges and validates every role via `Rendro.Color.validate/1`, raising an instructive `ArgumentError` on a malformed token before any draw; `:palette` overrides pass through the same downstream validation. |
| T-120-07 | Repudiation | no-theme byte-determinism after the swap | high | mitigate | Plan 01/02 frozen `*_byte_identity_test.exs` + edge_matrix re-run on every swap task; the no-theme path MUST still match the frozen sha256 (a drift hard-flunks as a defect). |
</threat_model>

<verification>
- `mix test test/rendro/recipes/{statement,certificate,receipt,branded_invoice}_opts_threading_test.exs` — all green (theme threads, recolors, override-wins, byte-identity).
- `mix test test/rendro/recipes/{statement,certificate,receipt,branded_invoice}_byte_identity_test.exs test/rendro/edge_matrix_test.exs` — no-theme path still matches frozen sha256s (PLUMB-03), no re-bless.
- Colors-only guard: `grep -rn "typography" lib/rendro/recipes/statement.ex lib/rendro/recipes/certificate.ex lib/rendro/recipes/receipt.ex lib/rendro/recipes/branded_invoice.ex` returns no matches (D-04).
- Merge-order guard: each swapped `palette/1` ends with `Map.merge(base, Keyword.get(opts, :palette, %{}))` (`:palette` is the final layer).
</verification>

<success_criteria>
- All 4 retrofitted recipes read `theme.colors.*` via a `case opts[:theme]` branch; no-theme path byte-identical to the frozen baselines (PLUMB-03).
- `:theme` threads all 3 rungs without KeyError; `:palette` override wins over `:theme` (D-01).
- Swap committed separately from retrofit; no golden re-blessed; no typography read.
</success_criteria>

<output>
Create `.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-03-SUMMARY.md` when done.
</output>
