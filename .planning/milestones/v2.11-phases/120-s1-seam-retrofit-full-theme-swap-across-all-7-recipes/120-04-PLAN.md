---
phase: 120
plan: 04
type: execute
wave: 2
depends_on: [120-01, 120-02]
files_modified:
  - lib/rendro/recipes/invoice.ex
  - lib/rendro/recipes/payslip.ex
  - lib/rendro/recipes/ticket.ex
  - test/rendro/recipes/invoice_opts_threading_test.exs
  - test/rendro/recipes/payslip_opts_threading_test.exs
  - test/rendro/recipes/ticket_opts_threading_test.exs
  - test/rendro/recipes/no_inline_color_literals_test.exs
autonomous: true
requirements: [PLUMB-02, PLUMB-03]
must_haves:
  truths:
    - "The 3 already-seamed recipes (Invoice, Payslip, Ticket) extend their existing palette/1 base to read Rendro.Theme.resolve(theme).colors when a :theme opt is present, keeping their current literal-defaults map as the nil branch and Map.merge(base, :palette-override) as the final layer (D-01, PLUMB-02). No retrofit, no section-builder edit, no whitelist edit — swap-only."
    - "The ONE legacy :palette dependent — invoice_opts_threading_test.exs — stays green: the merge-on-top order guarantees a :palette override still changes only the footer color, unaffected by the new :theme branch (D-01)."
    - "A phase-wide source-scan ExUnit test asserts no inline {r,g,b} color literal remains in any recipe section builder (roles only), EXCLUDING each recipe's palette/1 default map and non-color numerics (e.g. Statement's width: 0.75, geometry constants) (PLUMB-02)."
    - "This is a theme SWAP commit — separate from any retrofit; the no-theme path for all 3 recipes stays byte-identical (edge_matrix + invoice/payslip/ticket byte-identity goldens green, no re-bless, PLUMB-03)."
    - "No theme.typography.* read is introduced anywhere in the recipe layer — Phase 120 reads colors ONLY (D-04); a static check confirms the recipe layer is typography-free."
  prohibitions:
    - "MUST NOT combine this swap with any retrofit in one commit (research line 48)."
    - "MUST NOT re-bless any edge_matrix or byte-identity golden — the no-theme path MUST match existing sha256s (golden.ex L84)."
    - "MUST NOT add :theme/:palette to any page_template Keyword.take list."
    - "MUST NOT read any theme.typography.* field in Phase 120 (D-04, Phase 122 scope) — the source-scan/static check enforces this."
    - "MUST NOT flag palette/1 default maps or non-color numerics (width: 0.75, geometry) as violations in the source-scan test."
  artifacts:
    - "lib/rendro/recipes/invoice.ex :: palette/1 gains `case opts[:theme]` theme branch (swap-only)"
    - "lib/rendro/recipes/payslip.ex :: palette/1 gains theme branch (swap-only)"
    - "lib/rendro/recipes/ticket.ex :: palette/1 gains theme branch (swap-only)"
    - "test/rendro/recipes/invoice_opts_threading_test.exs (extend with :theme cases; keep the :palette-footer test green)"
    - "test/rendro/recipes/payslip_opts_threading_test.exs (new)"
    - "test/rendro/recipes/ticket_opts_threading_test.exs (new)"
    - "test/rendro/recipes/no_inline_color_literals_test.exs (new phase-wide source-scan)"
  key_links:
    - "invoice/payslip/ticket palette/1 -> Rendro.Theme.resolve(theme).colors; nil branch = existing literal defaults (all {0,0,0}/{255,255,255})"
    - "invoice_opts_threading_test.exs :palette-footer invariant -> preserved by Map.merge(base, palette-override) order (D-01)"
    - "no_inline_color_literals_test.exs -> scans lib/rendro/recipes/*.ex section builders, excludes palette/1 maps + non-color numerics"
---

<objective>
Complete the theme swap on the 3 already-seamed recipes (Invoice, Payslip, Ticket) — a swap-only, single-branch change to each existing `palette/1` — and add the phase-wide guard test proving no inline `{r,g,b}` color literal remains in any recipe section builder. Extend/add the three `*_opts_threading_test.exs` files, keeping the ONE legacy `:palette` dependent (invoice) green. This is Commit-set 2 (swap), separate from any retrofit.

Purpose: Invoice, Payslip, and Ticket already carry the full `palette/1` seam + `Keyword.take` whitelist (verified). Their swap is purely the same `case opts[:theme]` branch added to Plan 03's recipes — no literals to move, no whitelist to fix. Bundled here is the phase's PLUMB-02 completeness proof: a source-scan test that fails if any recipe still inlines a color literal outside its `palette/1` default map. This plan closes the phase: all 7 recipes themable, no-theme byte-identity intact, colors-only (no typography).

Output: theme-aware `palette/1` in Invoice/Payslip/Ticket; 3 threading tests (invoice extended); one phase-wide source-scan test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-RESEARCH.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-PATTERNS.md

# Reference swap transform + the ONE :palette dependent to keep green:
@lib/rendro/recipes/invoice.ex
@lib/rendro/recipes/payslip.ex
@lib/rendro/recipes/ticket.ex
@test/rendro/recipes/invoice_opts_threading_test.exs
@lib/rendro/theme.ex
</context>

<artifacts_this_plan_produces>
- Modified: `palette/1` in invoice.ex (L466), payslip.ex (L677), ticket.ex (L509) — each gains a `case opts[:theme]` branch; existing literal defaults become the `nil` branch verbatim.
- Extended: `invoice_opts_threading_test.exs` — add `:theme` cases; the existing `:palette`-changes-only-the-footer test (D-01 invariant) stays green.
- New files: `payslip_opts_threading_test.exs`, `ticket_opts_threading_test.exs`.
- New file: `no_inline_color_literals_test.exs` — phase-wide source-scan (PLUMB-02).
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Swap Invoice + Payslip + Ticket palette/1 to theme.colors + threading tests</name>
  <files>lib/rendro/recipes/invoice.ex, lib/rendro/recipes/payslip.ex, lib/rendro/recipes/ticket.ex, test/rendro/recipes/invoice_opts_threading_test.exs, test/rendro/recipes/payslip_opts_threading_test.exs, test/rendro/recipes/ticket_opts_threading_test.exs</files>
  <read_first>
    - lib/rendro/recipes/invoice.ex palette/1 (L466-481) and payslip.ex (L677-692) and ticket.ex (L509-524) — all three have the identical existing seam shape.
    - 120-PATTERNS.md § "Shared Pattern A — Commit 2 (swap) transform".
    - test/rendro/recipes/invoice_opts_threading_test.exs — note the existing "a :palette override changes only the footer section's color" test (the D-01 invariant that MUST stay green).
  </read_first>
  <action>
    In each of invoice.ex, payslip.ex, ticket.ex `palette/1`, wrap the existing literal-defaults map in a `case opts[:theme]` where the `nil` branch returns that existing map verbatim and the `theme ->` branch returns `Rendro.Theme.resolve(theme).colors`; leave the trailing `Map.merge(base, Keyword.get(opts, :palette, %{}))` unchanged so `:palette` remains the final override layer (D-01). These are swap-only: do NOT edit any section builder, do NOT touch the `Keyword.take` whitelist, do NOT read `theme.typography.*`. Then extend invoice_opts_threading_test.exs with `:theme` cases (`:theme` threads through `page_template/1` without KeyError; themed render differs from no-theme; `:palette` override wins over `:theme`) while leaving the existing `:palette`-footer invariant test untouched and green. Create payslip_opts_threading_test.exs and ticket_opts_threading_test.exs mirroring invoice_opts_threading_test.exs with the four standard assertions per recipe: (a) `:theme` threads through `page_template/1` without KeyError; (b) themed render differs from no-theme; (c) `:palette` override wins over `:theme` (D-01); (d) `<Recipe>.sections(data) == <Recipe>.sections(data, [])` (no-theme byte-identity, PLUMB-03).
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/invoice_opts_threading_test.exs test/rendro/recipes/payslip_opts_threading_test.exs test/rendro/recipes/ticket_opts_threading_test.exs test/rendro/recipes/invoice_byte_identity_test.exs test/rendro/edge_matrix_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Invoice/Payslip/Ticket `palette/1` read `Rendro.Theme.resolve(theme).colors` when `:theme` present; nil branch = existing literal defaults; `:palette` override wins.
    - invoice_opts_threading_test.exs stays green including the pre-existing `:palette`-footer invariant (D-01); payslip + ticket threading tests pass.
    - Invoice/Payslip/Ticket byte-identity + edge_matrix goldens stay green with NO re-bless (no-theme path unchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add phase-wide source-scan test — no inline color literal in any recipe section builder</name>
  <files>test/rendro/recipes/no_inline_color_literals_test.exs</files>
  <read_first>
    - 120-PATTERNS.md § "No Analog Found" (source-scan guidance: scan section builders, EXCLUDE palette/1 default maps + non-color numerics).
    - 120-RESEARCH.md § Validation Architecture (PLUMB-02 static/source-scan row).
    - All 7 recipe files' `palette/1` functions (so the scan can reliably exclude their default maps).
  </read_first>
  <action>
    Create test/rendro/recipes/no_inline_color_literals_test.exs — a pure ExUnit static-source test (no rendering) that reads each `lib/rendro/recipes/*.ex` file, isolates the section-builder region (everything EXCEPT the `defp palette(opts)` body), and asserts no inline integer `{r,g,b}` color triple remains there. Detect a color triple with a regex matching a brace-wrapped triple of 0-255 integers; scope the search to color contexts (e.g. `fill:`, `stroke:`, `color:` assignments) so non-color numeric tuples and geometry are not false-positives. Explicitly EXCLUDE from the scan: (1) each recipe's `palette/1` default map (legitimately holds the literal role defaults), and (2) non-color numerics such as a path stroke `width: 0.75` and coordinate/geometry constants. The test asserts the violation list is empty and, on failure, reports the offending file + line so a regression is actionable. Additionally assert the recipe layer is typography-free in Phase 120: no recipe file references `theme.typography` (D-04 colors-only boundary) — read each recipe file and assert the substring is absent (comment lines excluded).
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/no_inline_color_literals_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - no_inline_color_literals_test.exs passes: no inline `{r,g,b}` color literal in any recipe section builder (palette/1 default maps + non-color numerics excluded).
    - The typography-free assertion passes — no `theme.typography` reference in any recipe file (D-04).
    - The test fails loudly (with file + line) if a future edit reintroduces an inline color literal into a section builder.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → recipe `:theme`/`:palette` opts | Caller-supplied theme + palette override cross into `palette/1` for the 3 seamed recipes. No network/session/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-08 | Tampering | `:theme`/`:palette` color tuples reaching `palette/1` | medium | mitigate | `Rendro.Theme.resolve/1` → `Rendro.Color.validate/1` validates every role and raises `ArgumentError` on a malformed token before draw (existing control, unchanged). |
| T-120-09 | Repudiation | no-theme byte-determinism + inline-literal regression | high | mitigate | Existing byte-identity + edge_matrix goldens re-run on the swap (no re-bless); the new source-scan test permanently guards against a section builder reacquiring an inline color literal (PLUMB-02). |
</threat_model>

<verification>
- `mix test test/rendro/recipes/{invoice,payslip,ticket}_opts_threading_test.exs` — all green; invoice's legacy `:palette`-footer invariant preserved (D-01).
- `mix test test/rendro/recipes/no_inline_color_literals_test.exs` — no inline color literal in section builders; recipe layer typography-free (D-04).
- `mix test test/rendro/edge_matrix_test.exs test/rendro/recipes/*_byte_identity_test.exs` — full no-theme byte-identity across all 7 recipes (PLUMB-03), no re-bless.
- Phase gate: full `mix test` green before `/gsd-verify-work`.
</verification>

<success_criteria>
- All 3 seamed recipes read `theme.colors.*` via a `case opts[:theme]` branch; no-theme path byte-identical (PLUMB-03).
- The ONE legacy `:palette` dependent stays green (D-01); `:palette` override wins over `:theme` for all 3.
- Source-scan test proves no inline color literal remains in any recipe section builder and the recipe layer is typography-free (PLUMB-02, D-04).
- Swap committed separately from retrofit; no golden re-blessed.
</success_criteria>

<output>
Create `.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-04-SUMMARY.md` when done.
</output>
