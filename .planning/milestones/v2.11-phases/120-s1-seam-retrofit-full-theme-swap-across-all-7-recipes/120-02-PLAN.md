---
phase: 120
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/recipes/receipt.ex
  - lib/rendro/recipes/branded_invoice.ex
  - test/rendro/recipes/receipt_byte_identity_test.exs
  - test/rendro/recipes/branded_invoice_byte_identity_test.exs
autonomous: true
requirements: [PLUMB-01, PLUMB-02, PLUMB-03]
must_haves:
  truths:
    - "Receipt gains a private palette/1 seam with retrofit default ink {0,0,0}; its primary text runs (title, customer, date, merchant, minor total, dominant total) get an explicit color: colors.ink read — byte-identical because color: {0,0,0} renders identically to no color arg (D-03, live-verified)."
    - "BrandedInvoice gains a private palette/1 seam with retrofit default ink {0,0,0}; its header (brand, id, date) + footer text get an explicit color: colors.ink read — byte-identical (D-03)."
    - "Receipt's page_template/1 whitelist is fixed: the L178 Keyword.delete(opts, :header_height) forwarding is replaced with a Keyword.take struct-key whitelist (copy invoice.ex L137-149) so :palette/:theme no longer reach struct!/2 and raise KeyError (live-verified gotcha, PLUMB-02 prep)."
    - "BrandedInvoice's page_template/1 whitelist is fixed: the L92 Keyword.merge(defaults, opts) forwarding is replaced with a Keyword.take struct-key whitelist so :palette/:theme no longer raise KeyError (live-verified gotcha, PLUMB-02 prep)."
    - "This plan is a byte-identity RETROFIT ONLY (no theme reads); committed SEPARATELY from any swap (split-commit discipline, research line 48). BrandedInvoice gets a NET-NEW byte-identity golden — it is absent from edge_matrix and has no priv/goldens/branded_invoice/ dir (PLUMB-01/03 blind spot)."
  prohibitions:
    - "MUST NOT read theme.colors.* or call Rendro.Theme.resolve/1 in this plan — that is the swap (Plan 03), a separate commit (research line 48)."
    - "MUST NOT add :theme or :palette to either recipe's page_template Keyword.take list — the whitelist DROPS them so they thread to palette/1; adding them would send them to struct!/2 and re-raise KeyError."
    - "MUST NOT re-bless any existing golden on the no-theme path — a drift is a defect (golden.ex L84)."
    - "MUST NOT introduce any theme.typography.* read (Phase 122 scope, D-04)."
  artifacts:
    - "lib/rendro/recipes/receipt.ex :: defp palette(opts) (new seam, default ink {0,0,0}) + Keyword.take whitelist replacing L178"
    - "lib/rendro/recipes/branded_invoice.ex :: defp palette(opts) (new seam, default ink {0,0,0}) + Keyword.take whitelist replacing L92"
    - test/rendro/recipes/receipt_byte_identity_test.exs
    - test/rendro/recipes/branded_invoice_byte_identity_test.exs (net-new golden)
  key_links:
    - "receipt.ex page_template/1 L178 Keyword.delete -> Keyword.take([:name,:width,:height,:margin_*,:regions]) (copy invoice.ex L137-149)"
    - "branded_invoice.ex page_template/1 L92 Keyword.merge(defaults, opts) -> Keyword.take struct-key whitelist"
    - "receipt/branded primary text %Text{} -> color: colors.ink (default {0,0,0}, byte-identical per live probe)"
---

<objective>
Retrofit the two colorless recipes (Receipt, BrandedInvoice) with a byte-identical `palette/1` seam, seam their primary text to `colors.ink` (D-03), and fix the two live-verified `KeyError` whitelist gotchas so a `:palette`/`:theme` opt can thread without raising. Freeze a fresh sha256 byte-identity golden for each — BrandedInvoice's is net-new (it has no existing golden and is absent from edge_matrix). This is Commit-set 1 (retrofit) of the split-commit discipline, separate from any swap.

Purpose: Receipt and BrandedInvoice are the remaining 2 of 4 un-seamed recipes. They carry no color literals, so D-03 seams their primary text to `theme.colors.ink` NOW (default `{0,0,0}`, proven byte-identical) so Phase 121 dark mode "just works." Both also raise `KeyError` today when passed `:palette`/`:theme` — the whitelist fix is mandatory before any theme can thread. BrandedInvoice's missing golden is a regression blind spot that this plan closes.

Output: `palette/1` seams + `colors.ink` text reads + `Keyword.take` whitelists in both recipes; two new `*_byte_identity_test.exs` files (BrandedInvoice's a net-new baseline).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-RESEARCH.md
@.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-PATTERNS.md

# Reference seam + whitelist + reference test shape (copy these):
@lib/rendro/recipes/invoice.ex
@test/rendro/recipes/invoice_byte_identity_test.exs
</context>

<artifacts_this_plan_produces>
- New symbol: `Rendro.Recipes.Receipt.palette/1` (defp) — default `ink {0,0,0}`.
- New symbol: `Rendro.Recipes.BrandedInvoice.palette/1` (defp) — default `ink {0,0,0}`.
- Modified: Receipt `page_template/1` — `Keyword.take` struct-key whitelist (replaces L178 `Keyword.delete`).
- Modified: BrandedInvoice `page_template/1` — `Keyword.take` struct-key whitelist (replaces L92 `Keyword.merge(defaults, opts)`).
- New file: `test/rendro/recipes/receipt_byte_identity_test.exs`.
- New file: `test/rendro/recipes/branded_invoice_byte_identity_test.exs` (net-new golden; `priv/goldens/branded_invoice/` created).
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit Receipt — whitelist fix + palette/1 ink seam + frozen golden</name>
  <files>lib/rendro/recipes/receipt.ex, test/rendro/recipes/receipt_byte_identity_test.exs</files>
  <read_first>
    - lib/rendro/recipes/receipt.ex `page_template/1` around L176-178 (the `Keyword.merge(defaults, Keyword.delete(opts, :header_height))` that raises KeyError on `:palette`/`:theme`) and the section builders around L262-264/L288/L388/L395 (primary text runs: title, customer, date, merchant, minor total, dominant total).
    - lib/rendro/recipes/invoice.ex L137-149 (the `Keyword.take` whitelist to copy) and L466-481 (palette/1 shape).
    - test/rendro/recipes/invoice_byte_identity_test.exs (reference test shape).
  </read_first>
  <action>
    First fix the whitelist: replace the `page_template/1` forwarding at L178 (currently `Rendro.page_template(Keyword.merge(defaults, Keyword.delete(opts, :header_height)))`) with the invoice.ex pattern — build `template_opts = Keyword.take(opts, [:name, :width, :height, :margin_top, :margin_right, :margin_bottom, :margin_left, :regions])` then `Rendro.page_template(Keyword.merge(defaults, template_opts))`. The `Keyword.take` naturally drops `:header_height`, `:palette`, and `:theme`, so `:header_height` stays consumed locally as before and `:palette`/`:theme` thread to `sections/2` instead of hitting `struct!/2`. Do NOT add `:palette`/`:theme` to the take list. Next add a `defp palette(opts)` copying the invoice.ex retrofit seam shape with default `ink {0,0,0}` (D-03). Thread `colors = palette(opts)` into the section builder(s), and add an explicit `color: colors.ink` to each primary text run (title, customer, date, merchant, minor total, dominant total). This is byte-identical because an explicit `{0,0,0}` color renders identically to no color arg (live-verified). Do NOT read `theme.colors.*` or call `Rendro.Theme.resolve/1` (that is Plan 03). Then create test/rendro/recipes/receipt_byte_identity_test.exs mirroring invoice_byte_identity_test.exs; bless `@toy_golden_sha256` from the un-seamed HEAD render and confirm the post-seam render sha256 is unchanged.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/receipt_byte_identity_test.exs test/rendro/edge_matrix_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Receipt.page_template(palette: %{})` and `Receipt.page_template(theme: Rendro.Theme.default())` no longer raise `KeyError` (whitelist drops them).
    - `Receipt.palette/1` exists (default `ink {0,0,0}`); primary text runs read `color: colors.ink`.
    - receipt_byte_identity_test.exs passes; post-seam sha256 equals pre-seam sha256; existing Receipt edge_matrix goldens stay green (no re-bless).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Retrofit BrandedInvoice — whitelist fix + palette/1 ink seam + NET-NEW golden</name>
  <files>lib/rendro/recipes/branded_invoice.ex, test/rendro/recipes/branded_invoice_byte_identity_test.exs</files>
  <read_first>
    - lib/rendro/recipes/branded_invoice.ex `page_template/1` around L92 (the `Keyword.merge(defaults, opts)` that forwards everything to struct!/2 and raises KeyError) and the header/footer text builders around L160-162 (brand, id, date) + L191 (footer).
    - lib/rendro/recipes/invoice.ex L137-149 (whitelist) and L466-481 (palette/1 shape).
    - test/rendro/recipes/invoice_byte_identity_test.exs (reference test shape). Note: BrandedInvoice has NO existing golden and is NOT in edge_matrix — this test is a net-new baseline.
  </read_first>
  <action>
    First fix the whitelist: replace the `page_template/1` forwarding at L92 (currently `Rendro.page_template(Keyword.merge(defaults, opts))`) with `template_opts = Keyword.take(opts, [:name, :width, :height, :margin_top, :margin_right, :margin_bottom, :margin_left, :regions])` then `Rendro.page_template(Keyword.merge(defaults, template_opts))`. Do NOT add `:palette`/`:theme` to the take list — they must be dropped here to thread to `sections/2`. Next add a `defp palette(opts)` copying the invoice.ex retrofit seam shape with default `ink {0,0,0}` (D-03). Thread `colors = palette(opts)` into the section builders and add an explicit `color: colors.ink` to the header text (brand, id, date) and the footer text. Byte-identical per the live `{0,0,0}` probe. Do NOT read `theme.colors.*` or call `Rendro.Theme.resolve/1` (Plan 03). Then create test/rendro/recipes/branded_invoice_byte_identity_test.exs mirroring invoice_byte_identity_test.exs — this is a NET-NEW golden (nothing pre-existing to keep green): render the toy data, freeze the sha256 as `@toy_golden_sha256`, and assert two deterministic renders are byte-identical. Because there is no prior baseline, freeze the sha256 from the render AFTER the retrofit (the seam is byte-identical by construction: default `ink {0,0,0}` == no color); optionally cross-check against a pre-seam render sha256 to document the no-op.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/branded_invoice_byte_identity_test.exs test/rendro/recipes/branded_invoice_opts_threading_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `BrandedInvoice.page_template(palette: %{})` and `BrandedInvoice.page_template(theme: Rendro.Theme.default())` no longer raise `KeyError`.
    - `BrandedInvoice.palette/1` exists (default `ink {0,0,0}`); header + footer text read `color: colors.ink`.
    - branded_invoice_byte_identity_test.exs passes with a frozen `@toy_golden_sha256` (net-new baseline); the pre-existing branded_invoice_opts_threading_test.exs stays green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → recipe `document/2`/`page_template/1` opts | Caller-supplied `:palette`/`:theme` opts cross into the recipe. Before this plan, Receipt/BrandedInvoice `page_template/1` forwarded them to `struct!/2` and crashed; the whitelist fix contains that boundary. No network/session/auth surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-03 | Denial of Service | Receipt/BrandedInvoice `page_template/1` unfiltered opts forwarding | high | mitigate | Replace `Keyword.delete`/`Keyword.merge(defaults, opts)` with a `Keyword.take` struct-key whitelist so recipe-level opts (`:palette`, `:theme`, `:header_height`) are dropped before `struct!/2` — eliminating the live-verified `KeyError` crash path. |
| T-120-04 | Tampering | recipe `palette/1` `:palette` override map | low | accept | Malformed color tuples validated downstream by `Rendro.Color.validate/1` at draw time (existing control). No new untrusted surface. |
| T-120-05 | Repudiation | byte-determinism of no-theme render | medium | mitigate | Two new `*_byte_identity_test.exs` render twice and freeze a human-blessed sha256; BrandedInvoice's net-new golden closes its regression blind spot. |
</threat_model>

<verification>
- `mix test test/rendro/recipes/receipt_byte_identity_test.exs test/rendro/recipes/branded_invoice_byte_identity_test.exs` — both green.
- `mix test test/rendro/recipes/branded_invoice_opts_threading_test.exs` — pre-existing test stays green.
- `mix test test/rendro/edge_matrix_test.exs` — Receipt matrix cells stay green (no re-bless).
- No theme wiring yet: `grep -n "Theme" lib/rendro/recipes/receipt.ex lib/rendro/recipes/branded_invoice.ex` returns nothing (swap is Plan 03).
</verification>

<success_criteria>
- Both recipes have a byte-identical `palette/1` seam (default `ink {0,0,0}`) and primary text reading `colors.ink`.
- Both `page_template/1` whitelists fixed — `:palette`/`:theme` thread without `KeyError`.
- Two frozen sha256 byte-identity tests pass; BrandedInvoice's net-new golden closes the blind spot; existing goldens stay green.
- Zero theme reads introduced (retrofit-only commit).
</success_criteria>

<output>
Create `.planning/phases/120-s1-seam-retrofit-full-theme-swap-across-all-7-recipes/120-02-SUMMARY.md` when done.
</output>
