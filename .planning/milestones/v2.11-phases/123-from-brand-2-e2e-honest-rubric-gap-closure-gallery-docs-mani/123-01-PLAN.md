---
phase: 123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rendro/examples_data_test.exs
autonomous: true
requirements: [DEFAULT-02]
must_haves:
  truths:
    - "transform_invoice(load!(invoice fixture)) yields non-nil :issuer, :customer, and :totals.total (the Phase-115 DATA fix survived) — asserted by a committed test (DEFAULT-02, honest order Commit 1)."
    - "Commit 1 changes NO theme code and NO rubric scores — its diff touches only test/ (git-provable start of the D-05 honest order)."
  artifacts:
    - test/rendro/examples_data_test.exs
  key_links:
    - "priv/examples/invoice/acme-phoenix-saas/invoice.json (issuer L5 / customer L13 / totals L34) -> ExamplesData.transform_invoice/1 (put_optional L58-62) -> the demo the rubric re-scores in Commit 3."
---

<objective>
D-05 Commit 1 — DATA verify/attest. The honest order begins by proving the demo DATA is
present BEFORE any theme is applied or any score is touched. Research GT-1 confirmed this is
a verify-only no-op: `Rendro.ExamplesData.transform_invoice/1` already `put_optional`s
`:issuer` (L58), `:customer` (L59), `:totals` (L62), and the fixture
`priv/examples/invoice/acme-phoenix-saas/invoice.json` carries all three (issuer L5, customer
L13, totals L34 with total 696.60). This plan locks a test asserting that survival so the
SHOW-01 root-cause fix (DATA, not color) can never silently regress.

This is the leading honest slice of the phase (the D-05 three-commit order is the binding
spine per the planning directive; it replaces a cross-layer tracer here by explicit
instruction — a tracer that also re-scored would violate Commit-1 isolation).

Purpose: make "the DATA is present" a machine-checked fact and the first, isolated, git-provable commit of the honest order.
Output: a new assertion in test/rendro/examples_data_test.exs; Commit 1 with a test-only diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the invoice DATA survived (issuer/customer/totals present)</name>
  <files>test/rendro/examples_data_test.exs</files>
  <read_first>
    - test/rendro/examples_data_test.exs (existing transform_invoice test structure + aliases)
    - lib/rendro/examples_data.ex (transform_invoice/1 L36-63; put_optional of :issuer L58, :customer L59, :totals L62)
    - priv/examples/invoice/acme-phoenix-saas/invoice.json (issuer L5, customer L13, totals L34: subtotal 645.00 / tax 51.60 / total 696.60)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§Ground-Truth GT-1)
  </read_first>
  <action>
    Add a test to test/rendro/examples_data_test.exs named after SHOW-01 data survival that loads
    the invoice fixture via Rendro.Examples.load!("invoice/acme-phoenix-saas/invoice.json"), pipes
    through Rendro.ExamplesData.transform_invoice/1, and asserts the result map has a non-nil
    :issuer, a non-nil :customer, and a :totals whose :total is present (the fixture total is the
    Decimal 696.60 — assert :total is non-nil rather than pinning the numeric to avoid coupling to
    money formatting). This is verify-only per GT-1: DO NOT modify transform_invoice/1 or the
    fixture. The assertion is the Commit-1 deliverable and the anchor of the honest order.
  </action>
  <acceptance_criteria>
    - `mix test test/rendro/examples_data_test.exs` exits 0
    - The new test asserts transform_invoice(load!(...)) yields non-nil :issuer AND non-nil :customer AND non-nil :totals.total
    - `git diff --stat` for this task shows ONLY test/rendro/examples_data_test.exs (no lib/ change — verify-only)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/examples_data_test.exs</automated>
  </verify>
  <done>The DATA-survival test is green and asserts issuer, customer, and totals.total are all non-nil.</done>
  <reversibility rating="reversible">Additive test only; no product code touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Establish the pre-theme green baseline for Commit 1</name>
  <files>test/rendro/examples_data_test.exs</files>
  <read_first>
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-VALIDATION.md (sampling rate + full-suite command)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md (D-05 Commit 1: no theme, no scores)
  </read_first>
  <action>
    Run the recipe golden + examples-data suite to record a clean pre-theme baseline before the
    `leading:1.35` change lands in Plan 02. This proves Commit 1 stands on a green tree and that no
    theme/score change has leaked in. Confirm the working tree at Commit 1 contains no edit to
    lib/rendro/theme.ex, priv/quality/rubric_scores.json, priv/schemas/, or assets/rendro/ — Commit
    1 must be a test-only diff per D-05.
  </action>
  <acceptance_criteria>
    - `mix test test/rendro/recipes` exits 0 (no-theme byte-identity goldens green on the literal path)
    - `mix test test/rendro/examples_data_test.exs` exits 0
    - `git show --stat HEAD` for the Commit-1 commit lists ONLY paths under test/ (zero lib/, zero priv/quality/, zero priv/schemas/, zero assets/)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes test/rendro/examples_data_test.exs</automated>
  </verify>
  <done>Recipe goldens + data-survival test green; Commit 1 is a test-only diff (no theme, no scores).</done>
  <reversibility rating="reversible">Baseline gate only; no product code touched.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (Commit 1) produces:
- **New test** in `test/rendro/examples_data_test.exs`: a SHOW-01 data-survival assertion (issuer/customer/totals.total non-nil).
No new files, no new symbols, no lib/ change. (The full phase also produces — across later plans:
`guides/theming.md`, `priv/quality/SIGN-OFF.md`, `test/docs_contract/theming_contract_test.exs`,
4 new gallery PNGs, 4 new `@gallery_specs` entries + a `readme_hero` key, `signed_off_by`/
`signed_off_at`/`evidence_ref` schema fields, new `build_source_document/1` `defp` clauses for
`invoice_dark`/`certificate_dark`/`ticket_dark`/`invoice_brand`, and new `# docs-contract:` marker
IDs `theming-accent-only`/`theming-accent-contrast-both-ways`/`theming-brand-orthogonal`.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none | Pure-code offline PDF-library phase; no auth, session, network, DB, or user-facing input surface. |

## STRIDE Threat Register (ASVS L1, block-on-high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-NA | N/A | Test-only assertion over in-repo fixtures | low | accept | No applicable vector: no external input, no package install, no network/DB. Fixture + transform are compile/author-time data. Honest N/A per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `mix test test/rendro/examples_data_test.exs` green.
- `mix test test/rendro/recipes` green (no-theme goldens untouched).
- Commit 1 diff is test-only (git-provable start of the honest order).
</verification>

<success_criteria>
The invoice demo DATA (issuer/customer/totals.total) is proven present by a committed test, and
Commit 1 is an isolated, theme-free, score-free, git-provable commit.
</success_criteria>

<output>
Create `.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-01-SUMMARY.md` when done.
</output>
