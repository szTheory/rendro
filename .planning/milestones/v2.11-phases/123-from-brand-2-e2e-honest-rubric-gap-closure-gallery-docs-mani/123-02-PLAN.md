---
phase: 123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani
plan: 02
type: execute
wave: 2
depends_on: [123-01]
files_modified:
  - lib/rendro/theme.ex
autonomous: true
requirements: [DEFAULT-01]
must_haves:
  truths:
    - "Rendro.Theme.default().typography.leading == 1.35 (the single deferred D-01 value change; D-01)."
    - "default/0 colours are byte-unchanged — @default_colors stays the exact 9-role map (ink {16,24,39}, accent {44,107,237} blue-600, ...); the only edit in this commit is the leading literal (D-01, GT-4)."
    - "All 7 no-theme recipe byte-identity goldens stay green — document(data) with no theme: never reads default/0, so the literal 1.2 path is untouched (PLUMB-03, GT-3)."
    - "The themed Certificate body citation still fits single-page A4-landscape at leading 1.35 (fit-CHECK on the rendered themed raster, NOT a validate_body!/1 change — GT-3)."
  artifacts:
    - lib/rendro/theme.ex
  key_links:
    - "theme.ex @default_typography.leading (L79) -> every recipe %Text{line_height} (uniform Phase-122 seam) -> multi-line prose reflow on the themed path -> gallery re-bless in Plan 03."
---

<objective>
D-05 Commit 2 (part a) — realize the strong `default/0` by making the single deferred value
change: `@default_typography` `leading: 1.2 → 1.35` in lib/rendro/theme.ex (L79). Research GT-2/GT-4
confirmed: the palette is already mined zero-delta (no colour edit), `leading` is a one-line literal,
the Phase-122 seam wires `type.leading` uniformly to every `%Text{line_height}`, and single-line runs
are inert (line_height is a no-op on one line) — only wrapped multi-line **prose** on the themed path
re-flows (Certificate body citation ~L433, Invoice/BrandedInvoice "Terms:", Ticket terms).

Critically, the 7 no-theme byte-identity goldens must stay green: `document(data)` with no `theme:`
renders on each recipe's literal 1.2 path and never reads `default/0` (GT-3; certificate.ex L527-528
warns against reading `Theme.default().typography` on the no-theme path).

Purpose: land the strong-default value change in isolation, on a green no-theme baseline, before the gallery re-bless.
Output: the `leading:1.35` edit; a themed-path leading assertion; a Certificate single-page fit-check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md
@.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change @default_typography leading 1.2 -> 1.35 (colours untouched)</name>
  <files>lib/rendro/theme.ex</files>
  <read_first>
    - lib/rendro/theme.ex (@default_colors L48-58 — LOCK, no edit; @default_typography L75-81 — the `leading: 1.2` literal is at L79; default/0 L180)
    - brand/tokens/tokens.json (semantic.light aliases — confirms the palette is already mined; no colour change)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-CONTEXT.md (D-01: leading is the ONLY value change; Brand Book §9 Swiss print-prose band 1.3-1.45)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§GT-2, §GT-4)
  </read_first>
  <action>
    In lib/rendro/theme.ex @default_typography (L75-81), change the leading value from 1.2 to 1.35
    on L79. Change nothing else: @default_colors (L48-58) stays byte-identical (accent stays
    blue-600 {44,107,237}, NOT blue-700, per D-01), the scale map stays {display:21, title:16.5,
    subtitle:13, body:10.5, small:9, caption:8}, widows/orphans stay 2. This is the sole value edit
    for DEFAULT-01. Add or extend a Theme unit assertion (in the existing theme test) that
    `Rendro.Theme.default().typography.leading == 1.35` and that resolve/1 keeps every colour role
    equal to its @default_colors tuple (accent == {44,107,237}) — proving the colour surface did not
    move when leading did.
  </action>
  <acceptance_criteria>
    - `lib/rendro/theme.ex` @default_typography leading line reads `leading: 1.35`
    - `git diff lib/rendro/theme.ex` shows a single changed value line inside @default_typography and ZERO change under @default_colors
    - A theme unit test asserts `Rendro.Theme.default().typography.leading == 1.35`
    - A theme unit test asserts `Rendro.Theme.resolve(Rendro.Theme.default()).colors.accent == {44, 107, 237}`
    - `mix test test/rendro/theme_test.exs` (or the file that hosts Theme unit tests) exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/theme_test.exs</automated>
  </verify>
  <done>default/0 leading is 1.35, colours are provably unchanged, and the theme unit tests are green.</done>
  <reversibility rating="reversible">Evolving-tier VALUE change under a frozen field shape; reverts by restoring the literal.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove no-theme byte-identity holds + Certificate themed single-page fit-check</name>
  <files>lib/rendro/theme.ex</files>
  <read_first>
    - lib/rendro/recipes/certificate.ex (@line_height 1.2 L298; content_height_estimate L360-362; centered_paragraph L433/L441; validate_body!/1 L717-731 — a 2000-byte guard, insensitive to leading; no-theme branch warning L527-528)
    - priv/examples/certificate/summit-training-institute/certificate.json (body is 176 bytes -> ~3-4 wrapped lines)
    - .planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-RESEARCH.md (§GT-3: fit-check is a VISUAL confirmation of the themed raster; risk LOW; themed display 21<34 so smaller; do NOT narrow measure_w — counterproductive)
  </read_first>
  <action>
    Two proofs. (1) No-theme byte-identity: run the full recipe golden suite and confirm every
    document(data) golden is byte-identical to v2.10 — the leading change must NOT touch the literal
    path (PLUMB-03 central regression guard). (2) Certificate themed fit-check: render the Certificate
    with theme: Rendro.Theme.default() (the themed path that reads leading 1.35) and confirm the
    document paginates to a SINGLE page on A4-landscape. Per GT-3 this is a fit-CHECK (single-page
    assertion on the themed render), NOT a validate_body!/1 change and NOT a measure_w change; the
    body is 176 bytes and the themed display size shrinks (34->21), so overflow risk is LOW. If a
    single-page assertion ever fails, the honest lever is the vertical-centering estimate, never
    narrowing measure_w. Record the single-page result; do not modify certificate.ex geometry unless
    the fit-check actually fails.
  </action>
  <acceptance_criteria>
    - `mix test test/rendro/recipes` exits 0 (all 7 no-theme byte-identity goldens green — literal path unchanged)
    - A check renders `Rendro.Recipes.Certificate.document(data, theme: Rendro.Theme.default(), border: true)` and asserts the resulting document has exactly one page (single-page A4-landscape at leading 1.35)
    - No edit to `certificate.ex` `validate_body!/1` and no `measure_w` narrowing (GT-3)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rendro/recipes</automated>
  </verify>
  <done>All no-theme goldens are byte-identical and the themed Certificate fits a single A4-landscape page at leading 1.35.</done>
  <reversibility rating="reversible">Verification-only; certificate.ex geometry untouched unless the fit-check fails.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (Commit 2a) produces:
- **Modified** `lib/rendro/theme.ex`: `@default_typography.leading` = 1.35 (the single DEFAULT-01 value change).
- **New/extended assertions** in the Theme unit test: `default().typography.leading == 1.35` and colour-surface unchanged; a Certificate themed single-page fit-check.
No new files. (Gallery re-bless, from_brand guide, and the honest re-score are later plans — see 123-01's phase-wide artifact list.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none | Pure-code offline PDF-library phase; theme values are compile/author-time data with no injection sink. |

## STRIDE Threat Register (ASVS L1, block-on-high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-NA | N/A | @default_typography literal value edit | low | accept | No applicable vector: a one-line token-value edit in a static library; no external input, no package install, no network/DB. Honest N/A per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `mix test test/rendro/theme_test.exs` green (leading 1.35; accent unchanged).
- `mix test test/rendro/recipes` green (no-theme byte-identity holds — PLUMB-03).
- Themed Certificate renders a single A4-landscape page at leading 1.35.
</verification>

<success_criteria>
`default/0` leading is 1.35 with the colour surface provably unchanged, the no-theme byte-identity
guard holds, and the themed Certificate fits one page — the strong-default value change lands in
isolation on a green baseline.
</success_criteria>

<output>
Create `.planning/phases/123-from-brand-2-e2e-honest-rubric-gap-closure-gallery-docs-mani/123-02-SUMMARY.md` when done.
</output>
