---
phase: 122-typography-type-scale-application-font-role-leading-wiring
plan: 04
type: execute
wave: 3
depends_on: ["122-01", "122-02", "122-03"]
files_modified:
  - test/rendro/recipes/no_inline_size_literals_test.exs
autonomous: true
requirements: [TYPE-01]
must_haves:
  truths:
    - "A source-scan teeth test guards against re-introducing inline size: literals on seamed call sites across all 7 recipes, excluding the typography/1 body, @*_size attr definitions, and the Certificate size:1 layout spacer"
    - "The full suite (mix test) is green: all 7 byte-identity goldens, edge_matrix, the 7 opts-threading tests, the raise-path tests, and both static-scan guards"
  artifacts:
    - test/rendro/recipes/no_inline_size_literals_test.exs (TYPE-01 teeth test)
  key_links:
    - "teeth test excludes size: <var> reads (@attr) and only flags size: <number-literal> in section builders"
---

<objective>
Consolidation + phase gate: add the TYPE-01 source-scan teeth test (now green because all 7 recipes are seamed) and confirm the full suite is green before verification.

Purpose: Lock the "no inline `size:` literal on seamed call sites" invariant with teeth (mirroring the existing `no_inline_color_literals_test.exs`) and prove the whole phase preserves byte-identity across every golden.
Output: `test/rendro/recipes/no_inline_size_literals_test.exs` and a green full suite.

## Artifacts this plan produces
- `test/rendro/recipes/no_inline_size_literals_test.exs` — NEW: TYPE-01 static-scan teeth test guarding against re-introduced inline `size:` literals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-RESEARCH.md
@.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the no-inline-size-literals teeth test (TYPE-01 guard)</name>
  <files>test/rendro/recipes/no_inline_size_literals_test.exs</files>
  <read_first>
    - test/rendro/recipes/no_inline_color_literals_test.exs — the whole file (the exact template to copy: `@color_literal` regex L30, `palette_body_indices/1` L41-53, `comment_line?/1` L55, the `Enum.flat_map` violation collector L57-79)
    - .planning/phases/122-.../122-RESEARCH.md §"Wave 0 Gaps" and §"State of the Art" (the teeth-test intent)
    - .planning/phases/122-.../122-PATTERNS.md §"NEW no_inline_size_literals_test.exs"
    - lib/rendro/recipes/certificate.ex ~L347 — the `Rendro.text("", size: 1)` layout spacer that must be allowlisted
    - lib/rendro/recipes/ticket.ex ~L376-378 — the exempted `@caption_size`/`@present_code_size` attr definitions (these are `size: @attr` variable reads at call sites, NOT inline literals, so the regex must only match a numeric literal after `size:`)
  </read_first>
  <action>
    Create `test/rendro/recipes/no_inline_size_literals_test.exs` by copying `no_inline_color_literals_test.exs` structure verbatim and adapting: replace the `@color_literal` regex with one matching `size:` followed by an integer/float NUMERIC literal (e.g. `~r/\bsize:\s*\d+(\.\d+)?\b/`) — this deliberately does NOT match `size: @attr` variable reads (Ticket's exempted micro-sizes) or `size: type.scale.<role>`. Generalize the body-exclusion helper to skip BOTH the `defp palette(` body AND the `defp typography(` body (where literal role-default maps legitimately live) AND `@*_size` module-attr definition lines (Certificate/Ticket define sizes as attrs — the legit literal home). Allowlist the single legitimate remaining inline literal: Certificate's `Rendro.text("", size: 1)` layout spacer (exclude by an explicit spacer guard, e.g. skip lines containing `size: 1)` on a `Rendro.text("",` spacer, or a narrow allowlist). Reuse `comment_line?/1` and the `Enum.flat_map` violation collector as-is. The test asserts `violations == []` across all 7 recipe files.
  </action>
  <verify>
    <automated>mix test test/rendro/recipes/no_inline_size_literals_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `no_inline_size_literals_test.exs` exists and asserts no inline numeric `size:` literal in any recipe section builder
    - the test excludes the `typography/1` body, `@*_size` attr definitions, and the Certificate `size: 1` spacer
    - `mix test test/rendro/recipes/no_inline_size_literals_test.exs` passes (green — all 7 recipes seamed)
  </acceptance_criteria>
  <done>The TYPE-01 teeth test is green, guarding against re-introduced inline `size:` literals.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite phase gate</name>
  <files>test/rendro/recipes/no_inline_size_literals_test.exs</files>
  <read_first>
    - .planning/phases/122-.../122-RESEARCH.md §"Validation Architecture" (§Sampling Rate: phase gate = full `mix test` green)
    - mix.exs — the `ci` / `test.all` aliases (context only)
  </read_first>
  <action>
    Run the full suite and confirm it is green: all 7 `*_byte_identity_test.exs` (zero re-bless), `edge_matrix_test.exs` (leading no-op), the 7 `*_opts_threading_test.exs` (typography live seam + no-op), the raise-path tests (`invoice_typography_test.exs`, `statement_typography_test.exs`, `ticket_typography_test.exs`), and both static-scan guards (`no_inline_color_literals_test.exs` with the D-04 assertion relaxed, `no_inline_size_literals_test.exs`). This is a verification-only task — no source changes; if any golden drifts, the drift is a real regression to fix in the owning recipe plan, NOT a re-bless.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` passes with zero failures
    - no golden fixture was re-blessed during this phase (all `*_byte_identity_test.exs` frozen sha256 unchanged)
  </acceptance_criteria>
  <done>The full suite is green with zero re-bless; the phase is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan — no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-04 | Tampering | re-introduced inline `size:` literal bypassing the seam | low | mitigate | the `no_inline_size_literals_test.exs` static-scan teeth test (Task 1) fails the build on any inline `size:` literal in a section builder |

No high/critical threats: a static-scan test and a full-suite run — no runtime, network, auth, persistence, or dependency surface. No package installs.
</threat_model>

<verification>
- `mix test test/rendro/recipes/no_inline_size_literals_test.exs` — TYPE-01 teeth green
- `mix test` — full suite green, zero re-bless
</verification>

<success_criteria>
The TYPE-01 teeth test is green and the full suite passes with zero re-bless — every byte-identity golden, the edge matrix, all threading and raise-path tests, and both static-scan guards. The phase is complete and ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/122-typography-type-scale-application-font-role-leading-wiring/122-04-SUMMARY.md` when done
</output>
