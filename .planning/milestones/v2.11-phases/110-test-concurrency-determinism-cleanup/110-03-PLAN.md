---
phase: 110-test-concurrency-determinism-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["110-02"]
files_modified:
  - test/support/hex_build_cache.ex
  - test/support/hex_build_cache_test.exs
  - test/test_helper.exs
  - test/docs_contract/branding_claims_test.exs
  - test/docs_contract/launch_artifacts_claims_test.exs
  - test/docs_contract/comparison_claims_test.exs
autonomous: true
requirements: [TEST-01, TEST-05]
must_haves:
  truths:
    - Shared `hex.build` test setup costs are mitigated via a singleton caching agent.
    - Claims tests run concurrently (`async: true`) because they no longer duplicate shared `_build` folder mutation.
  artifacts:
    - path: test/support/hex_build_cache.ex
      provides: Agent-based cache for `mix hex.build` execution
    - path: test/support/hex_build_cache_test.exs
      provides: Functional tests for the HexBuildCache logic
  key_links:
    - from: test/test_helper.exs
      to: test/support/hex_build_cache.ex
      via: Agent.start_link
      pattern: Rendro.Test.HexBuildCache.start_link
---

<objective>
Address shared `hex.build` setup costs and unlock `async: true` for claims tests.

Purpose: Phase 108 identified docs tests (claims tests) as the slowest, primarily due to redundant `System.cmd("mix", ["hex.build"])` calls which prevented `async: true` execution due to shared filesystem mutations. By extracting this into a cached singleton `Agent` initialized in `test_helper.exs`, we solve the performance bottleneck (TEST-05) and allow these tests to safely run concurrently (TEST-01).
Output: A new `HexBuildCache` module, updated `test_helper.exs` to start it, and refactored claims tests running `async: true`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-test-concurrency-determinism-cleanup/110-CONTEXT.md
@.planning/phases/110-test-concurrency-determinism-cleanup/110-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create singleton cache for hex.build test setups</name>
  <files>test/support/hex_build_cache.ex, test/support/hex_build_cache_test.exs</files>
  <behavior>
    - Test 1: Calling `get_build_output/0` runs the build, returns `{output, exit_code}` and caches the result.
    - Test 2: Subsequent calls return the cached result without executing the command again.
  </behavior>
  <action>
    Create `test/support/hex_build_cache_test.exs` to functionally verify the `HexBuildCache` logic.
    Create `Rendro.Test.HexBuildCache` in `test/support/hex_build_cache.ex`. Implement a singleton `Agent` that runs `System.cmd("mix", ["hex.build"], stderr_to_stdout: true)` exactly once per test run and caches the result `{output, exit_code}`.
    It should provide a `get_build_output/0` function that either returns the cached result or executes the command and caches it.
    This solves the shared `hex.build` test setup costs mentioned in RESEARCH.md (TEST-05) and avoids redundant IO operations that prevent test concurrency.
  </action>
  <verify>
    <automated>mix test test/support/hex_build_cache_test.exs</automated>
  </verify>
  <done>HexBuildCache module created with Agent state and functionally tested.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor claims tests to use cache and unlock async: true</name>
  <files>
    test/docs_contract/branding_claims_test.exs,
    test/docs_contract/launch_artifacts_claims_test.exs,
    test/docs_contract/comparison_claims_test.exs,
    test/test_helper.exs
  </files>
  <action>
    - Ensure `Rendro.Test.HexBuildCache.start_link()` (or similar init function) is called in `test/test_helper.exs` before tests run.
    - Update `branding_claims_test.exs`, `launch_artifacts_claims_test.exs`, and `comparison_claims_test.exs` to call `Rendro.Test.HexBuildCache.get_build_output()` instead of `System.cmd("mix", ["hex.build"], ...)`.
    - Change `@moduletag async: false` to `@moduletag async: true` (or `use ExUnit.Case, async: true`) in these three test files since they no longer mutate the shared `_build` folder.
  </action>
  <verify>
    <automated>grep -q HexBuildCache test/docs_contract/branding_claims_test.exs</automated>
  </verify>
  <done>Claims tests share a single hex build and run concurrently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process state | Isolation of test setup caching via Agent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Tampering | Test execution | mitigate | HexBuildCache runs exactly once, caching results to prevent concurrent `_build` folder mutation during test suite execution. |
</threat_model>

<verification>
- `mix test` successfully runs and reports fewer slow tests, with improved overall test execution time.
</verification>

<success_criteria>
- Shared `hex.build` setup costs are completely eliminated.
- Claims tests run `async: true`.
</success_criteria>

<output>
Create `.planning/phases/110-test-concurrency-determinism-cleanup/110-03-SUMMARY.md` when done
</output>