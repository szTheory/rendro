---
phase: 85-deterministic-raster-lane
plan: 03
type: execute
wave: 2
depends_on:
  - 85-01
files_modified:
  - priv/schemas/support_matrix.schema.json
  - lib/rendro/viewer_evidence/validator.ex
  - priv/support_matrix.json
autonomous: true
requirements:
  - RAST-03

must_haves:
  truths:
    - "viewer_kind: 'pdfium-render' is accepted by JSV schema validation (priv/schemas/support_matrix.schema.json enum extended)"
    - "@viewer_kinds in lib/rendro/viewer_evidence/validator.ex contains 'pdfium-render' in sync with the schema enum"
    - "priv/support_matrix.json has a top-level 'raster' key with renderer, capabilities, boundaries, and evidence sub-sections"
    - "The raster section evidence.viewer_kind is 'pdfium-render', NOT 'pdfium-cli'"
    - "The raster section is NOT placed inside forms.viewers, signing.viewers, or any existing viewer_map path"
    - "mix test passes (JSV validation now accepts pdfium-render, no orphan evidence triggered, no viewer_map promotion failures)"
    - "RAST-03b: viewer_evidence_claims_test.exs JSV-validation test passes after schema update"
  artifacts:
    - path: "priv/schemas/support_matrix.schema.json"
      provides: "viewer_kind enum extended to include pdfium-render"
      contains: "pdfium-render"
    - path: "lib/rendro/viewer_evidence/validator.ex"
      provides: "@viewer_kinds sigil extended to include pdfium-render"
      contains: "pdfium-render"
    - path: "priv/support_matrix.json"
      provides: "raster top-level section with boundary declarations and pdfium-render evidence"
      contains: "\"raster\""
  key_links:
    - from: "priv/schemas/support_matrix.schema.json viewer_kind enum"
      to: "lib/rendro/viewer_evidence/validator.ex @viewer_kinds"
      via: "atomic sync — both must list the same values or mix test fails"
      pattern: "pdfium-render"
    - from: "priv/support_matrix.json raster section"
      to: "test/docs_contract/raster_claims_test.exs test 1"
      via: "File.read! assertion on raster key presence"
      pattern: "\"raster\""
---

<objective>
Atomic dual-schema sync: extend `viewer_kind` enum in `priv/schemas/support_matrix.schema.json` AND `@viewer_kinds` in `lib/rendro/viewer_evidence/validator.ex` to include `"pdfium-render"` in the same commit — these two edits are inseparable (Pitfall 1: one without the other fails `mix test`). Then add the `"raster"` top-level section to `priv/support_matrix.json` as a new root key (NOT inside any viewer_map) with boundary declarations and `viewer_kind: "pdfium-render"` evidence.

Purpose: RAST-03 evidence vocabulary — `pdfium-render` becomes a valid and distinct viewer_kind, and the support matrix records raster capabilities and GUI-viewer claim boundaries.
Output: Three files modified atomically; `mix test` passes; `raster_claims_test.exs` tests 1 and 5 both green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-deterministic-raster-lane/85-RESEARCH.md
@.planning/phases/85-deterministic-raster-lane/85-PATTERNS.md
@.planning/phases/85-deterministic-raster-lane/85-VALIDATION.md
@.planning/phases/85-deterministic-raster-lane/85-01-SUMMARY.md

<interfaces>
From priv/schemas/support_matrix.schema.json (lines 113-116 — current state):
```json
"viewer_kind": {
  "type": "string",
  "enum": ["manual", "pdfium-cli", "pdfjs-dist"]
},
```
Target after edit:
```json
"viewer_kind": {
  "type": "string",
  "enum": ["manual", "pdfium-cli", "pdfjs-dist", "pdfium-render"]
},
```

From lib/rendro/viewer_evidence/validator.ex (line 15 — current state):
```elixir
@viewer_kinds ~w(manual pdfium-cli pdfjs-dist)
```
Target after edit:
```elixir
@viewer_kinds ~w(manual pdfium-cli pdfjs-dist pdfium-render)
```

From priv/support_matrix.json (top-level structure — the "validators" key as shape reference for a non-viewer top-level section; root has "additionalProperties": true in schema so new top-level keys are allowed):
New section to add as a root-level key "raster" (from RESEARCH.md Pattern 4 and PATTERNS.md section):
```json
"raster": {
  "renderer": "pdfium-render",
  "capabilities": {
    "pdf_to_png": "supported",
    "dpi_configurable": "supported",
    "page_range": "supported",
    "byte_deterministic_on_pinned_container": "supported"
  },
  "boundaries": {
    "gui_viewer_equivalence": "unsupported",
    "adobe_acrobat_visual_fidelity_claim": "unsupported",
    "apple_preview_visual_fidelity_claim": "unsupported"
  },
  "evidence": {
    "renderer_version": "v0.11.0",
    "viewer_kind": "pdfium-render",
    "platform": "Linux (x86_64) — CI container only",
    "dpi": 150,
    "notes": "pdfium-render evidence records what the pdfium engine renders. It does not claim GUI-viewer visual fidelity for Adobe Acrobat, Apple Preview, or Chrome PDF viewer."
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic dual-schema sync — add pdfium-render to schema enum and @viewer_kinds</name>
  <files>priv/schemas/support_matrix.schema.json, lib/rendro/viewer_evidence/validator.ex</files>
  <read_first>
    - priv/schemas/support_matrix.schema.json — read lines 110-120 to confirm the current enum shape; verify "additionalProperties": false on viewer_row (line 96 area) — the enum must be updated here, not elsewhere
    - lib/rendro/viewer_evidence/validator.ex — read lines 1-20 to confirm current @viewer_kinds sigil and its exact whitespace/formatting so the edit is a clean one-word append
    - .planning/phases/85-deterministic-raster-lane/85-PATTERNS.md — "priv/schemas/support_matrix.schema.json (one-line enum edit)" section and "lib/rendro/viewer_evidence/validator.ex (one-line constant edit)" section — exact before/after shown
    - .planning/phases/85-deterministic-raster-lane/85-RESEARCH.md — Pitfall 1 (schema sync atomic requirement) and Common Pitfalls section; Pattern 4 vocabulary section (pdfium-cli vs pdfium-render distinction)
  </read_first>
  <action>
    Edit priv/schemas/support_matrix.schema.json: find the line containing "enum": ["manual", "pdfium-cli", "pdfjs-dist"] under the viewer_kind property definition. Append "pdfium-render" to make the array ["manual", "pdfium-cli", "pdfjs-dist", "pdfium-render"]. Change only this one line (or the array span if it is multi-line). Do not touch any other part of the schema.

    Edit lib/rendro/viewer_evidence/validator.ex: find the @viewer_kinds ~w(...) line (line 15). Change ~w(manual pdfium-cli pdfjs-dist) to ~w(manual pdfium-cli pdfjs-dist pdfium-render). One word appended. Do not touch any other part of the file.

    These two edits must land in the same commit. After both edits, run mix test to confirm JSV schema validation still passes (the viewer_evidence_claims_test.exs structural test will now accept pdfium-render as a valid viewer_kind).

    CRITICAL: Do NOT add pdfium-render under any existing viewer_map section (forms.viewers, signing.viewers, etc.). The schema enum update only unlocks the value for future use — it does not place it anywhere in the matrix.
  </action>
  <verify>
    <automated>mix test test/docs_contract/viewer_evidence_claims_test.exs</automated>
  </verify>
  <done>
    - grep confirms "pdfium-render" in priv/schemas/support_matrix.schema.json enum array
    - grep confirms "pdfium-render" in lib/rendro/viewer_evidence/validator.ex @viewer_kinds
    - mix test test/docs_contract/viewer_evidence_claims_test.exs exits 0 (RAST-03b green)
    - mix test (full suite) exits 0 (no JSV validation regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "raster" top-level section to priv/support_matrix.json</name>
  <files>priv/support_matrix.json</files>
  <read_first>
    - priv/support_matrix.json — read the full file to understand current root-level structure; identify the closing brace location and the last key before it (so the new "raster" key can be appended correctly with a comma on the preceding key's closing brace)
    - .planning/phases/85-deterministic-raster-lane/85-PATTERNS.md — "priv/support_matrix.json (add top-level 'raster' key)" section — the "validators" key shape as reference for non-viewer top-level sections; exact JSON shape for the new "raster" section
    - .planning/phases/85-deterministic-raster-lane/85-RESEARCH.md — Pitfall 4 (raster section must be top-level, NOT inside a viewer_map); Pitfall 5 (do NOT create priv/viewer_evidence/raster/ — use priv/raster_refs/ only); Pattern 4 (full JSON shape for the raster section including capabilities, boundaries, evidence sub-keys)
  </read_first>
  <action>
    Edit priv/support_matrix.json to add a new top-level key "raster" at the root level of the JSON object — not nested inside any existing section. Place it after the last existing top-level key (before the closing brace of the root object). Ensure valid JSON by adding a comma after the preceding key's closing brace if needed.

    The "raster" section shape must match RESEARCH.md Pattern 4 exactly:
    - Top-level keys under "raster": "renderer" (value: "pdfium-render"), "capabilities" (object), "boundaries" (object), "evidence" (object)
    - capabilities keys: pdf_to_png, dpi_configurable, page_range, byte_deterministic_on_pinned_container — all with value "supported"
    - boundaries keys: gui_viewer_equivalence, adobe_acrobat_visual_fidelity_claim, apple_preview_visual_fidelity_claim — all with value "unsupported"
    - evidence keys: renderer_version ("v0.11.0"), viewer_kind ("pdfium-render"), platform ("Linux (x86_64) — CI container only"), dpi (150, as number not string), notes (boundary disclaimer string)

    Do NOT place the raster section under "forms", "signing", "page_primitive", "path_primitive", or any other existing key.
    Do NOT create any file under priv/viewer_evidence/ for raster evidence (Pitfall 5).

    After editing, validate JSON syntax: mix run -e "File.read!(\"priv/support_matrix.json\") |> JSON.decode!" must not raise.
  </action>
  <verify>
    <automated>mix test test/docs_contract/raster_claims_test.exs test/docs_contract/viewer_evidence_claims_test.exs</automated>
  </verify>
  <done>
    - mix test test/docs_contract/raster_claims_test.exs exits 0 with tests 1 and 5 passing (raster section present, GUI-viewer boundary assertion passes)
    - mix test test/docs_contract/viewer_evidence_claims_test.exs exits 0 (no promotion-complete violations, no orphan evidence)
    - mix test (full suite) exits 0
    - mix run -e "File.read!(\"priv/support_matrix.json\") |> JSON.decode!()" exits 0 (valid JSON)
    - grep confirms top-level "raster" key is in support_matrix.json (not nested)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| priv/support_matrix.json → JSV validator | Schema enforces structural correctness; new raster section at wrong location would bypass viewer_map validation silently |
| viewer_kind enum (schema) ↔ @viewer_kinds (validator.ex) | These two lists must be identical; divergence causes test failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-08 | Info Disclosure | "pdfium-render" viewer_kind on GUI-viewer rows | mitigate | raster_claims_test.exs test 5 asserts GUI-viewer rows (forms section) do not carry viewer_kind pdfium-render (RAST-03c); docs-contract gates this in CI |
| T-85-09 | Tampering | support_matrix.json raster section misclassified inside a viewer_map | mitigate | raster section placed at root level per schema "additionalProperties": true; viewer_evidence_claims_test.exs promotion-complete test would catch wrong placement; no priv/viewer_evidence/raster/ directory created (Pitfall 5) |
| T-85-10 | Tampering | schema enum and @viewer_kinds sigil diverging over time | mitigate | both files updated in same commit; viewer_evidence_claims_test.exs JSV test validates schema; annotation in validator.ex @viewer_kinds comment: "must sync with priv/schemas/support_matrix.schema.json viewer_kind enum" |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No new packages; config files only |
</threat_model>

<verification>
After both tasks complete:
- `mix test test/docs_contract/raster_claims_test.exs` — tests 1 and 5 pass (RAST-03d, RAST-03c); tests 4 and 6 still fail (guardrails and verify_docs — fixed in Plan 04)
- `mix test test/docs_contract/viewer_evidence_claims_test.exs` exits 0 (RAST-03b green; no orphan evidence; no promotion-complete violations)
- `mix test` (full suite) exits 0
- `grep -c '"pdfium-render"' priv/schemas/support_matrix.schema.json` returns at least 1
- `grep -c 'pdfium-render' lib/rendro/viewer_evidence/validator.ex` returns at least 1
- `grep -c '"raster"' priv/support_matrix.json` returns at least 1
</verification>

<success_criteria>
Plan 03 is complete when:
1. "pdfium-render" is present in both the schema enum and @viewer_kinds (atomic sync confirmed by grep)
2. `priv/support_matrix.json` has a top-level "raster" section with boundary declarations
3. `viewer_evidence_claims_test.exs` and partial `raster_claims_test.exs` (tests 1 and 5) pass
4. `mix test` (full suite) exits 0 — no regressions in viewer evidence validation
</success_criteria>

<output>
Create `.planning/phases/85-deterministic-raster-lane/85-03-SUMMARY.md` when done.
</output>
