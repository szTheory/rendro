---
phase: 84-drawn-path-primitive-visible-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 84-02
files_modified:
  - lib/rendro/table.ex
  - lib/rendro.ex
  - lib/rendro/pdf/writer.ex
autonomous: true
requirements:
  - PATH-02

must_haves:
  truths:
    - "A table with borders: :all renders visible border rules (re + S ops) in the PDF content stream"
    - "A table with no borders field (default :none) produces byte-identical output to the pre-Phase-84 baseline"
    - "A table with header_fill: {r,g,b} emits rg + re + f ops for the header band"
    - "Interior edges are drawn once — a 2x2 bordered table with :grid has exactly one shared segment per interior edge, not two"
    - "The borders set-list is canonicalized at construction: [:all, :outer] and [:outer, :all] produce identical struct values and identical PDF bytes"
    - "table_decoration returns empty string when borders is :none and header_fill is nil — no stray newline"
  artifacts:
    - path: "lib/rendro/table.ex"
      provides: "Three new flat fields: borders: :none, border_style: nil, header_fill: nil; @type t updated"
    - path: "lib/rendro.ex"
      provides: "normalize_table_attrs extended: borders canonicalization + border_style/header_fill color validation"
    - path: "lib/rendro/pdf/writer.ex"
      provides: "table_decoration/3 private helper + guarded prepend in render_block for %Rendro.Table{}"
  key_links:
    - from: "lib/rendro.ex"
      to: "lib/rendro/color.ex"
      via: "Rendro.Color.validate/1 called for header_fill and border_style.color in normalize_table_attrs"
      pattern: "Rendro\\.Color\\.validate"
    - from: "lib/rendro/pdf/writer.ex"
      to: "lib/rendro/pipeline/measure.ex"
      via: "table_decoration uses table._grid_layout (populated by measure.ex _grid_layout pass)"
      pattern: "_grid_layout"
    - from: "lib/rendro/pdf/writer.ex"
      to: "lib/rendro/color.ex"
      via: "table_decoration calls Rendro.Color.rg_stroke/1 and Rendro.Color.rg/1 for border color emission"
      pattern: "Rendro\\.Color\\.rg"
---

<objective>
Implement opt-in table borders, row/column rules, and header-band shading on `%Rendro.Table{}`. Three mutually reinforcing changes: add flat fields to the struct, extend normalize_table_attrs with set-canonicalization and color validation, and implement the `table_decoration/3` helper in the writer with a draw-once collapse model.

Purpose: After this plan, `Rendro.table(rows, borders: :all)` produces an invoice/statement with crisp cell rules, and the default (no borders field) output is byte-identical to the pre-Phase-84 baseline — the byte-identity regression guard passes. PATH-02 tests turn GREEN.

Output: table.ex (new fields), rendro.ex (extended normalize), writer.ex (table_decoration + guarded prepend).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rendro/.planning/PROJECT.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-CONTEXT.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-PATTERNS.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-02-SUMMARY.md

<interfaces>
<!-- Existing interfaces the executor must match. -->

From lib/rendro/table.ex:1-38 (existing struct — add three fields after decoration_break):
```elixir
defmodule Rendro.Table do
  @moduledoc tags: [:stable]
  @enforce_keys [:rows]
  defstruct [
    :rows, header: nil, columns: nil, split_policy: :row_atomic,
    repeat_header: true, decoration_break: :slice,
    column_widths: nil, row_heights: nil, header_height: nil, _grid_layout: nil
  ]
end
```
Add after decoration_break: `borders: :none, border_style: nil, header_fill: nil`

From lib/rendro/pipeline/measure.ex:278-285 (_grid_layout shape):
```elixir
# grid_layout[r][c] is a map:
%{is_continuation: boolean(), cell: measured_cell | nil, ref_r: integer(), ref_c: integer()}
# colspan at {r,c}: grid_layout[r][c].ref_c != c
# rowspan at {r,c}: grid_layout[r][c].ref_r != r
```

From lib/rendro/pdf/writer.ex:511-535 (existing Table render_block — add decoration prepend):
```elixir
defp render_block(doc, %Rendro.Block{content: %Rendro.Table{} = table}, page, font_map, image_map) do
  header_ops = ...
  rows_ops = ...
  [header_ops | rows_ops] |> List.flatten() |> Enum.join("\n")
end
```
Modify to: compute `decoration = table_decoration(table, page, block)`; then
`if decoration == "", do: cells_content, else: decoration <> "\n" <> cells_content`

From lib/rendro/pdf/writer.ex:1282-1340 (iodata ops assembly pattern — table_decoration must follow this):
```elixir
IO.iodata_to_binary(["q\n", ..., "Q\n"])
```

From .planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md:
- gridline position derivation via cumulative scan of column_widths and row_heights
- span suppression: for vertical line at boundary c/c+1, check each row r — if ref_c != c skip that segment
- borders atom vocabulary: :none, :outer, :rows, :columns, :grid (=rows+columns), :all (=outer+rows+columns)
- canonical expansion: :all → [:outer, :rows, :columns]; :grid → [:rows, :columns]
- draw order: header fill FIRST → strokes → cell content (text always on top)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: table.ex new fields + rendro.ex normalize_table_attrs extension</name>
  <files>lib/rendro/table.ex, lib/rendro.ex</files>
  <behavior>
    - struct!(Rendro.Table, rows: []) succeeds with borders: :none, border_style: nil, header_fill: nil as defaults
    - Rendro.table([["cell"]], borders: :all) sets borders to the canonical [:columns, :outer, :rows] (sorted atoms)
    - Rendro.table([["cell"]], borders: [:all, :outer]) produces same struct as borders: :all (canonicalized set)
    - Rendro.table([["cell"]], borders: [:outer, :rows]) sets borders to [:outer, :rows] (sorted)
    - Rendro.table([["cell"]], borders: :grid) sets borders to [:columns, :rows] (grid = rows+columns, sorted)
    - Rendro.table([["cell"]], header_fill: "#abc") raises ArgumentError mentioning "hex"
    - Rendro.table([["cell"]], border_style: %{color: "#000"}) raises ArgumentError mentioning "hex"
    - Rendro.table([["cell"]], border_style: %{color: {0,0,0}}) succeeds
    - Rendro.table([["cell"]], borders: :unknown_atom) raises ArgumentError with valid atoms listed
  </behavior>
  <read_first>
    - lib/rendro/table.ex (full file — locate defstruct, @type t fields to extend)
    - lib/rendro.ex lines 295-375 (table/2, normalize_table_attrs/1 — extend this function)
    - lib/rendro/color.ex (validate/1 interface — call for header_fill and border_style.color)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-CONTEXT.md D-13, D-14 (borders vocabulary, border_style inert subset, header_fill)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-PATTERNS.md table.ex section and rendro.ex section
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md Pitfall 2 (non-canonical borders set causes non-deterministic bytes)
  </read_first>
  <action>
    In lib/rendro/table.ex: append three fields to defstruct after `decoration_break: :slice`:
    - `borders: :none` (default inert atom per D-13)
    - `border_style: nil` (default nil per D-13)
    - `header_fill: nil` (default nil per D-13)
    Update `@type t` to add: `borders: :none | :outer | :rows | :columns | :grid | :all | [atom()]`, `border_style: nil | map()`, `header_fill: nil | {non_neg_integer(), non_neg_integer(), non_neg_integer()}`. Do NOT change any existing fields or defaults — this must be purely additive.

    In lib/rendro.ex: extend `normalize_table_attrs/1` to handle the three new fields. Add a pipeline of checks AFTER the existing split_policy validation:

    1. Borders canonicalization (call from a `normalize_borders/1` private helper):
       - Accept a single atom or a list. Valid single atoms: `:none`, `:outer`, `:rows`, `:columns`, `:grid`, `:all`.
       - Expand shorthand: `:all` → `[:outer, :rows, :columns]`; `:grid` → `[:rows, :columns]`; `:none` → `[]` (the canonical empty set).
       - For a list input: flatten any shorthands (`:all` or `:grid` in list expands), deduplicate with `Enum.uniq`, sort with `Enum.sort` (gives canonical form regardless of author order per D-14 "normalize to a canonical form at construction"). The stored value for the empty set is `[]` (not `:none` — the writer checks `borders in [:none, [], nil]` for the inert guard).
       - Invalid atom: raise `ArgumentError` with What/Where/Why/Next format listing valid atoms per D-04 pattern. Example: "Unknown borders atom: :foo. Valid atoms: :none, :outer, :rows, :columns, :grid, :all."

    2. border_style color validation: if `border_style` is a map and has a `:color` key, call `Rendro.Color.validate(color_val)` — on `{:error, msg}` raise `ArgumentError, msg`.

    3. header_fill color validation: if `header_fill` is not nil, call `Rendro.Color.validate(header_fill)` — on `{:error, msg}` raise `ArgumentError, msg`.

    Return the updated attrs keyword list. Use `Keyword.update/4` or pattern-match to replace/set the `:borders` key.
  </action>
  <verify>
    <automated>mix test test/rendro/table_borders_test.exs -x 2>&1 | grep -E "P02a|P02b|P02c|failure|error" | head -20</automated>
  </verify>
  <done>
    - table.ex compiles; three new fields present with correct defaults
    - normalize_table_attrs canonicalizes :all to [:columns, :outer, :rows] (sorted)
    - normalize_table_attrs validates header_fill and border_style.color via Rendro.Color
    - mix test test/rendro/table_test.exs -x exits 0 (no regressions to existing table tests)
    - mix test test/rendro/deterministic_test.exs -x exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: writer.ex table_decoration helper + guarded prepend</name>
  <files>lib/rendro/pdf/writer.ex</files>
  <behavior>
    - table with borders: :none → table_decoration/3 returns "" → content stream unchanged from baseline
    - table with borders: [:outer] → content stream contains "re\nS" and outer rect at block dims
    - table with borders: [:rows, :columns] (grid) → horizontal rules between rows + vertical rules between columns in content stream
    - table with header_fill: {0,102,204} → content stream contains "rg" op followed by "re" and "f" before cell content
    - 2x2 table with borders: [:columns, :rows] → exactly 1 vertical interior line + 1 horizontal interior line (draw-once, not 2+2)
    - colspan cell: vertical rule at spanned column boundary suppressed for the merged cell's row
    - rowspan cell: horizontal rule at spanned row boundary suppressed for the merged cell's column
    - no stray newline when borders is inert (Pitfall 3 from RESEARCH.md)
    - all coordinates emitted via format_num (no raw Float.to_string)
  </behavior>
  <read_first>
    - lib/rendro/pdf/writer.ex lines 507-580 (render_block for Table — full table clause to modify)
    - lib/rendro/pdf/writer.ex lines 1280-1340 (iodata ops pattern — q/Q, RG/rg/w/re/S/f)
    - lib/rendro/pdf/writer.ex lines 1755-1765 (format_num — all new coords must use this)
    - lib/rendro/pipeline/measure.ex lines 275-345 (_grid_layout structure)
    - lib/rendro/color.ex (rg/1, rg_stroke/1 interfaces)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md sections 4 (gridline derivation + span suppression) and 3 (draw-once collapse model Pattern 3)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-PATTERNS.md writer.ex Analog E (table decoration guard pattern)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md Pitfalls 2, 3 (non-canonical set, stray newline)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-CONTEXT.md D-15, D-16
  </read_first>
  <action>
    In lib/rendro/pdf/writer.ex, add the `table_decoration/3` private helper and modify the existing Table render_block clause.

    `table_decoration(table, page, block)` guard:
    - Early exit: `if table.borders in [:none, [], nil] and is_nil(table.header_fill), do: "", else: do_table_decoration(table, page, block)`

    `do_table_decoration(table, page, block)`:
    1. Compute block origin in PDF space: `bx = block.x + page.margin_left`; `by = page.height - (block.y + block.height) - page.margin_top`. (Same Y-flip formula as Path and Image.)
    2. Compute total dims: `total_w = Enum.sum(table.column_widths)`, `total_h = (table.header_height || 0) + Enum.sum(table.row_heights || [])`.
    3. Expand borders to a canonical set (call `expand_borders/1` private helper: `:all → [:outer,:rows,:columns]`, `:grid → [:rows,:columns]`, list is used as-is; the normalized list from construction is already canonical).
    4. Header fill (emit first for draw order: fill under strokes, strokes under cell content):
       - If `table.header_fill` is not nil and `table.header_height` is not nil and > 0: emit `rg` + `re f` for the header band. Band rect: `x=0, y_bottom_pdf=total_h-table.header_height` (block-relative Y-up), `w=total_w, h=table.header_height`. Using `bx`/`by` as PDF origin: `"#{Rendro.Color.rg(table.header_fill)}#{format_num(bx)} #{format_num(by + (total_h - table.header_height))} #{format_num(total_w)} #{format_num(table.header_height)} re\nf\n"`.
    5. Stroke setup (emit once before all stroke segments):
       - Default hairline: `%{color: {0,0,0}, width: 0.5, dash: nil}` (per D-13 "nil → built-in hairline").
       - Merge with `table.border_style` if set.
       - Emit: `Rendro.Color.rg_stroke(color)` + (if width != 0.5 → `"#{format_num(width)} w\n"` else `"0.5000 w\n"`) + (if dash → render dash pattern).
    6. Outer border (if `:outer` in borders): four segments — left, right, top, bottom edges of the full table rect. Use `m`/`l` pairs: `"#{format_num(bx)} #{format_num(by)} m\n#{format_num(bx+total_w)} #{format_num(by)} l\n..."` etc. Emit one `S` after all path segments.
    7. Horizontal interior rules (if `:rows` in borders): compute cumulative y positions from `table.header_height` and `table.row_heights`. For each interior horizontal line at y_offset (row boundary): for each column `c`, check if the row above AND row below have `grid_layout[r][c].ref_r == r` (no rowspan spans this boundary at column c). Group consecutive unspanned columns into one `m/l` segment per continuous run. Emit one `S` at end.
    8. Vertical interior rules (if `:columns` in borders): compute cumulative x positions from `table.column_widths`. For each interior vertical line at x_offset (column boundary `c/c+1`): for each row `r`, check if `grid_layout[r][c].ref_c == c` (no colspan spans this boundary). Group consecutive unspanned rows into `m/l` segments. Emit one `S` at end.
    9. Wrap everything in `["q\n", fill_ops, "0.5000 w\n" <stroke_color>, outer_ops, h_rule_ops, v_rule_ops, "Q\n"]` → `IO.iodata_to_binary/1`. Adjust: emit stroke color ONCE before outer+rules (not repeated).

    Modify the Table render_block clause to prepend decoration per Pitfall 3 guard:
    ```
    decoration = table_decoration(table, page, block)
    cells_content = ... existing join ...
    if decoration == "", do: cells_content, else: decoration <> "\n" <> cells_content
    ```
    The `block` is already in scope as the matched function argument.

    CRITICAL: table._grid_layout may be nil for tables that were not measured (e.g., empty tables or edge cases). Add a nil guard: if `_grid_layout` is nil, skip span-suppression and draw all interior lines unconditionally.
  </action>
  <verify>
    <automated>mix test test/rendro/table_borders_test.exs test/rendro/deterministic_test.exs -x 2>&1 | tail -15</automated>
  </verify>
  <done>
    - P02a (borders: :all → re+S), P02b (no borders → byte-identity), P02c (borders: :none → byte-identity), P02d ([:outer,:rows] → no vertical rules), P02e (draw-once), P02f (header_fill → rg+f) — all GREEN
    - mix test test/rendro/deterministic_test.exs -x exits 0 (no regressions)
    - mix test test/rendro/table_test.exs -x exits 0
    - mix test -x full suite green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Rendro.table/2 borders/border_style/header_fill | Developer-supplied values; validated in normalize_table_attrs |
| _grid_layout → table_decoration | Internal pipeline data; nil guard prevents crash on uninitialized tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-07 | Tampering | normalize_table_attrs borders validation | mitigate | Closed allowlist of valid atoms; unknown atoms raise ArgumentError with What/Where/Why/Next; invalid colors delegated to Rendro.Color.validate/1 |
| T-84-08 | Denial of Service | table_decoration with large table | accept | In-process developer data; gridline computation is O(rows*cols); no untrusted table data |
| T-84-09 | Tampering | _grid_layout nil in writer | mitigate | Nil guard added — if _grid_layout is nil, skip span suppression and render unconditionally (conservative, no crash) |
| T-84-SC | Tampering | npm/pip/cargo installs | accept | No external package installs in this phase |
</threat_model>

<verification>
After both tasks complete:

1. `mix test test/rendro/table_borders_test.exs -x` exits 0 — all P02a-P02f GREEN
2. `mix test test/rendro/deterministic_test.exs -x` exits 0
3. `mix test test/rendro/table_test.exs -x` exits 0
4. `mix test -x` full suite green
5. Byte-identity regression: render a table without borders twice; assert equal bytes — `assert pdf1 == pdf2`
6. Content-stream check: render `Rendro.table([["a","b"],["c","d"]], borders: :all)` → PDF contains "re" and "S" and "RG"
</verification>

<success_criteria>
- table.ex: borders/border_style/header_fill fields added with inert defaults; @type t updated
- rendro.ex: normalize_table_attrs canonicalizes borders set, validates colors, raises ArgumentError on invalid input
- writer.ex: table_decoration/3 with draw-once collapse model; guarded prepend in render_block; no stray newline when inert
- mix test test/rendro/table_borders_test.exs -x exits 0
- Borderless table output byte-identical to baseline (P02b, P02c GREEN)
</success_criteria>

<output>
Create `.planning/phases/84-drawn-path-primitive-visible-polish/84-03-SUMMARY.md` when done.
</output>
