---
phase: 84-drawn-path-primitive-visible-polish
plan: 05
type: execute
wave: 4
depends_on:
  - 84-03
  - 84-04
files_modified:
  - priv/support_matrix.json
  - lib/mix/tasks/rendro/api.gen.ex
  - priv/public_api.json
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PATH-04
  - PATH-01
  - PATH-02
  - PATH-03

must_haves:
  truths:
    - "priv/support_matrix.json has a top-level path_primitive section with capabilities, behaviors, and explicit_deferrals sub-objects"
    - "transforms_cm, clipping_W, and gradients each have status: explicit_deferral in the path_primitive.explicit_deferrals object — D-23: transforms/clipping/gradients/blend/even-odd DEFERRED with explicit support-matrix rows; design must not preclude but must not implement them"
    - "evidence_deferred strings are >= 40 characters (schema minLength requirement)"
    - "priv/public_api.json contains Rendro.Path in the stable tier and Rendro.path/2 as a function"
    - "mix test test/docs_contract/path_claims_test.exs -x exits 0 — all assertions GREEN"
    - "mix test test/docs_contract/public_api_contract_test.exs -x exits 0 — manifest not stale"
    - "ROADMAP.md Phase-84 success criterion 1 uses {0,0,0} instead of '#000' (D-05 correction)"
    - "ROADMAP.md Phase 84 Plans section updated with count and plan file list"
  artifacts:
    - path: "priv/support_matrix.json"
      provides: "path_primitive section with capabilities, behaviors, and three explicit_deferral entries"
      contains: "path_primitive"
    - path: "priv/public_api.json"
      provides: "Rendro.Path in stable tier; Rendro.path/2 function entry"
      contains: "Rendro.Path"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 84 success criterion 1 corrected from '#000' to {0,0,0}; plan list updated"
  key_links:
    - from: "test/docs_contract/path_claims_test.exs"
      to: "priv/support_matrix.json"
      via: "File.read! + regex assertions for path_primitive + deferrals"
      pattern: "path_primitive"
    - from: "test/docs_contract/public_api_contract_test.exs"
      to: "priv/public_api.json"
      via: "Assertion 2: generated manifest must match committed manifest (run mix rendro.api.gen)"
      pattern: "Rendro\\.Path"
---

<objective>
Complete Phase 84's docs-contract and manifest layer: add the `path_primitive` section to `priv/support_matrix.json` with three explicit deferral rows, regenerate `priv/public_api.json` to include `Rendro.Path`, and correct the ROADMAP's `"#000"` hex-string example to `{0,0,0}` per D-05. Also update the ROADMAP Phase 84 plan list.

Purpose: PATH-04 requires terminal support_matrix rows and byte-determinism golden tests. The byte-determinism tests already pass from Plans 01-04. This plan closes the docs-contract assertions and the manifest. All five plans' tests must be GREEN before this plan commits the manifests.

Output: priv/support_matrix.json (new section), priv/public_api.json (regenerated), api.gen.ex (@public_modules entry), ROADMAP.md (D-05 fix + plan list).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/rendro/.planning/PROJECT.md
@/Users/jon/projects/rendro/.planning/ROADMAP.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-CONTEXT.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-03-SUMMARY.md
@/Users/jon/projects/rendro/.planning/phases/84-drawn-path-primitive-visible-polish/84-04-SUMMARY.md

<interfaces>
<!-- Contracts to match exactly. -->

From priv/support_matrix.json (schema: additionalProperties: true at top level; text_shaping section as analog):
The existing text_shaping section (added in Phase 83) uses:
```json
"text_shaping": {
  "arabic": {
    "status": "explicit_deferral",
    "evidence_deferred": "Arabic right-to-left text shaping deferred — requires Unicode Bidi Algorithm + HarfBuzz shaping; Phase 84 defers with named reason..."
  },
  ...
}
```
PATH-04 path_primitive section structure (from RESEARCH.md section 6):
```json
"path_primitive": {
  "capabilities": {
    "move_line_curve_rect_rounded_rect": "supported",
    "stroke_color_width_dash_cap_join": "supported",
    "fill": "supported",
    "deterministic_output": "supported"
  },
  "behaviors": {
    "block_relative_y_down_coords": "supported",
    "ops_extent_bbox_fallback": "supported",
    "format_num_determinism": "supported"
  },
  "explicit_deferrals": {
    "transforms_cm": {
      "status": "explicit_deferral",
      "evidence_deferred": "Affine transforms (cm operator) deferred — adds viewer-compat surface; v1 path surface is move/line/curve/rect/rounded-rect + stroke/fill only. Tracked in support_matrix.json deferral rows per PATH-04."
    },
    "clipping_W": {
      "status": "explicit_deferral",
      "evidence_deferred": "Clipping paths (W/W* operators) deferred — adds viewer-compat surface requiring cross-viewer testing; v1 path surface deferred per PATH-04."
    },
    "gradients": {
      "status": "explicit_deferral",
      "evidence_deferred": "Gradients (PDF shading dictionaries) deferred — adds significant viewer-compat surface; v1 path surface is stroke/fill only per PATH-04."
    }
  }
}
```

From lib/mix/tasks/rendro/api.gen.ex:43-98 (@public_modules list — insert Rendro.Path in stable tier):
```elixir
@public_modules [
  # Stable tier — core document model and facades
  Rendro,
  ...
  Rendro.Image,
  Rendro.Link,
  Rendro.Metadata,
  Rendro.Page,
  Rendro.PageTemplate,   # ← insert Rendro.Path between PageTemplate and ... (alphabetical)
  ...
  Rendro.Table,
  Rendro.Text,
  ...
]
```
Alphabetical position: Rendro.Path goes after Rendro.PageTemplate and before Rendro.Protect.

From test/docs_contract/public_api_contract_test.exs (Assertion 2 — the test that will fail until manifest is regenerated):
```elixir
# Assertion 2: committed manifest matches generated manifest
test "committed public_api.json matches what mix rendro.api.gen would generate" do
  generated = Rendro.PublicApi.generate_manifest()
  committed = File.read!("priv/public_api.json") |> Jason.decode!()
  assert generated == committed
end
```
This means the executor MUST run `mix rendro.api.gen` to regenerate the committed file after adding Rendro.Path to @public_modules.

From .planning/ROADMAP.md Phase 84 success criterion 1 (contains "#000" — D-05 correction target):
```
A caller can declare `%Rendro.Path{ops: [{:rect, x, y, w, h}], stroke: %{color: "#000", width: 1.0}}`
```
Must become:
```
A caller can declare `%Rendro.Path{ops: [{:rect, x, y, w, h}], stroke: %{color: {0, 0, 0}, width: 1.0}}`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: support_matrix.json path_primitive section + api.gen.ex @public_modules + manifest regen</name>
  <files>priv/support_matrix.json, lib/mix/tasks/rendro/api.gen.ex, priv/public_api.json</files>
  <read_first>
    - priv/support_matrix.json (full file — locate the text_shaping section added in Phase 83 as the structural analog; find the correct insertion point for path_primitive; verify additionalProperties: true at top level)
    - priv/schemas/support_matrix.schema.json (verify schema allows new top-level key; check minLength on evidence_deferred)
    - test/docs_contract/path_claims_test.exs (the test stubs from Plan 01 — know exactly what assertions must pass)
    - lib/mix/tasks/rendro/api.gen.ex lines 43-100 (@public_modules list — find alphabetical insertion point for Rendro.Path)
    - test/docs_contract/public_api_contract_test.exs lines 1-60 (Assertion 2 — understand what mix rendro.api.gen does)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-RESEARCH.md section 6 (path_primitive row structure + evidence_deferred minLength requirement)
  </read_first>
  <action>
    Step 1 — support_matrix.json: Edit priv/support_matrix.json to add the `"path_primitive"` top-level key. Insert it in alphabetical order among top-level keys (check existing key ordering). Use the exact JSON structure from the interfaces section above. Verify each `evidence_deferred` string is >= 40 characters (schema requirement — count characters, pad if needed). Do NOT break JSON validity (use a JSON-aware edit — read the full file first, edit carefully, validate mentally that braces/commas are correct).

    Step 2 — api.gen.ex: Add `Rendro.Path` to the `@public_modules` list in alphabetical order within the stable tier. The alphabetical position is: after `Rendro.PageTemplate` and before `Rendro.Protect`. This is a one-line addition. Do NOT add `Rendro.Color` — it is `@moduledoc false` and intentionally excluded per D-02.

    Step 3 — Regenerate manifest: Run `mix rendro.api.gen` to regenerate `priv/public_api.json`. This command reads `@public_modules`, introspects `Code.fetch_docs/1` for each module's tags, and writes the sorted JSON manifest. The committed file must match the generated one to satisfy `public_api_contract_test.exs` Assertion 2.

    Step 4 — Verify both docs-contract tests pass:
    - `mix test test/docs_contract/path_claims_test.exs -x` — path_primitive + deferral assertions GREEN
    - `mix test test/docs_contract/public_api_contract_test.exs -x` — manifest not stale GREEN

    If `mix rendro.api.gen` fails because `Rendro.Path` does not have a compiled BEAM file (i.e., it was just added and not compiled), run `mix compile` first then re-run `mix rendro.api.gen`.
  </action>
  <verify>
    <automated>mix test test/docs_contract/path_claims_test.exs test/docs_contract/public_api_contract_test.exs -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - path_claims_test.exs: all assertions GREEN (path_primitive section present, three deferrals with status explicit_deferral, evidence_deferred >= 40 chars)
    - public_api_contract_test.exs: Assertion 2 GREEN (committed manifest matches generated)
    - priv/public_api.json contains "Rendro.Path" in stable tier
    - priv/support_matrix.json is valid JSON (no parse errors)
    - mix compile --no-deps-check exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md D-05 correction + Phase 84 plan list + full suite gate</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 79-92 (Phase 84 section — locate the "#000" example in success criterion 1 and the "Plans: TBD" line to update)
    - .planning/phases/84-drawn-path-primitive-visible-polish/84-CONTEXT.md D-05 (exact correction mandate)
  </read_first>
  <action>
    In .planning/ROADMAP.md, make two edits to the Phase 84 section:

    Edit 1 — D-05 correction: In the Phase 84 success criteria section (around line 86), find the text:
    `stroke: %{color: "#000", width: 1.0}`
    Replace with:
    `stroke: %{color: {0, 0, 0}, width: 1.0}`
    (The hex string "#000" is the exact unvetted example D-05 mandates correcting. This is a documentation fix only — the API already uses {r,g,b} tuples.)

    Edit 2 — Plan list update: Find the `**Plans**: TBD` line under Phase 84. Replace with:
    ```
    **Plans**: 5 plans

    Plans:
    **Wave 1**
    - [ ] 84-01-PLAN.md — Rendro.Color helper + %Rendro.Path{} struct + Wave 0 test stubs

    **Wave 2** *(blocked on Wave 1 completion)*
    - [ ] 84-02-PLAN.md — Path pipeline dispatch: measure + writer render_block + path/2 builder + D-03 retrofit

    **Wave 3** *(blocked on Wave 2 completion)*
    - [ ] 84-03-PLAN.md — Table borders: borders/border_style/header_fill fields + table_decoration draw-once
    - [ ] 84-04-PLAN.md — Certificate border frame: validate_border! + :frame region + sections + document

    **Wave 4** *(blocked on Wave 3 completion)*
    - [ ] 84-05-PLAN.md — Manifests: support_matrix path_primitive rows + public_api regen + ROADMAP D-05 fix
    ```

    After editing ROADMAP.md, run the full test suite to confirm all plans' tests are GREEN:
    `mix test` — must exit 0.

    This is the phase-gate verification: all PATH-01..04 tests must be green before this plan is considered complete.
  </action>
  <verify>
    <automated>mix test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 84 success criterion 1: "#000" replaced with {0, 0, 0}
    - ROADMAP.md Phase 84: "Plans: 5 plans" with wave structure and five plan entries
    - mix test exits 0 — full suite green (all PATH-01..04 tests GREEN, no regressions)
    - mix test test/rendro/path_test.exs test/rendro/table_borders_test.exs test/rendro/recipes/certificate_test.exs test/rendro/deterministic_test.exs -x exits 0 (quick run command from VALIDATION.md)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| priv/support_matrix.json → docs-contract test | File is committed to repo; schema-validated by existing docs-contract lane |
| priv/public_api.json → public_api_contract_test | Generated by mix rendro.api.gen; committed copy must match generated copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-13 | Tampering | priv/public_api.json manifest drift | mitigate | public_api_contract_test.exs Assertion 2 regenerates and compares — any drift fails CI; manifest regen is part of this plan's task |
| T-84-14 | Information Disclosure | support_matrix.json explicit_deferral | accept | Deferral rows are intentional public disclosure of roadmap scope; no sensitive information |
| T-84-15 | Repudiation | ROADMAP "#000" → {0,0,0} correction | accept | The correction is tracked via git history; D-05 decision is recorded in CONTEXT.md |
| T-84-SC | Tampering | npm/pip/cargo installs | accept | No external package installs in this phase |
</threat_model>

<verification>
After both tasks complete:

1. `mix test test/docs_contract/path_claims_test.exs -x` exits 0
2. `mix test test/docs_contract/public_api_contract_test.exs -x` exits 0
3. `grep '"path_primitive"' /Users/jon/projects/rendro/priv/support_matrix.json` returns a match
4. `grep '"Rendro.Path"' /Users/jon/projects/rendro/priv/public_api.json` returns a match
5. `grep '0, 0, 0' /Users/jon/projects/rendro/.planning/ROADMAP.md | grep "stroke"` returns the corrected line
6. `grep '"#000"' /Users/jon/projects/rendro/.planning/ROADMAP.md` returns NO match (hex removed)
7. `mix test` exits 0 — full suite green, all 21 PATH test cases GREEN (P01a-P01f, P02a-P02f, C15-C20, PATH-04 docs-contract)
</verification>

<success_criteria>
- priv/support_matrix.json: path_primitive section with three explicit_deferral entries; evidence_deferred >= 40 chars each; valid JSON
- lib/mix/tasks/rendro/api.gen.ex: Rendro.Path in @public_modules stable tier (alphabetical); Rendro.Color absent
- priv/public_api.json: regenerated via mix rendro.api.gen; contains Rendro.Path stable tier + Rendro.path/2 function; committed copy matches generated copy
- .planning/ROADMAP.md: "#000" corrected to {0,0,0} in Phase 84 success criterion 1; plan list updated with 5 plans + wave structure
- mix test exits 0 — full suite green, all PATH-01..04 requirements satisfied
</success_criteria>

<output>
Create `.planning/phases/84-drawn-path-primitive-visible-polish/84-05-SUMMARY.md` when done.
</output>
