---
phase: 87-comparison-page-livebook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/comparison.ex
  - test/rendro/comparison_test.exs
  - test/docs_contract/comparison_claims_test.exs
  - scripts/verify_docs.exs
  - bench/results/comparison.json
  - bench/results/raw/.gitkeep
  - bench/results/raw/plan01-static-fixture.json
autonomous: true
requirements:
  - CMP-01
  - CMP-02

must_haves:
  truths:
    - "A new Rendro.Comparison module owns the Phase 87 benchmark manifest, generated block helpers, raw-artifact hash validation, and required/static contract errors."
    - "Required static checks are external-tool free: no Chrome, wkhtmltopdf, Typst, Livebook, Kino, Docker, network access, or benchmark rerun is required for the new docs-contract lane."
    - "bench/results/comparison.json has schema_version, generated_by, run metadata, scenario metadata, comparator metadata, result rows, and public claim records."
    - "The initial manifest includes comparator IDs for rendro, chromic_pdf, chromic_pdf_warm_pool, pdf_generator, and typst_cli, preserving D-08 and D-10 through D-16."
    - "Docs-contract scaffolding registers a Comparison claims lane and fails unresolved [bench:CMP-*] citations."
    - "Decision coverage: D-01 through D-16 are represented in the manifest/static-proof scaffold, with Plan 02 filling measured results and raw artifacts."
  artifacts:
    - path: "lib/rendro/comparison.ex"
      provides: "Static comparison manifest contract and generated-block helper surface"
      contains: "static_contract_errors"
    - path: "test/docs_contract/comparison_claims_test.exs"
      provides: "Required docs-contract lane for benchmark claim binding"
      contains: "Comparison claims lane"
---

<objective>
Create the static comparison proof scaffold before benchmark or guide content is
expanded. This plan establishes `Rendro.Comparison`, an initial manifest shape,
and required docs-contract tests so later plans cannot add public comparison
claims without executable claim binding.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-comparison-page-livebook/87-CONTEXT.md
@.planning/phases/87-comparison-page-livebook/87-RESEARCH.md
@.planning/phases/87-comparison-page-livebook/87-PATTERNS.md
@.planning/phases/87-comparison-page-livebook/87-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the comparison manifest module and static contract helpers</name>
  <files>lib/rendro/comparison.ex, bench/results/comparison.json, bench/results/raw/.gitkeep, bench/results/raw/plan01-static-fixture.json</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex - closest analog for manifest paths, marker helpers, static_contract_errors/0, raw hash helpers, generated block helpers, and actionable drift messages
    - assets/rendro/artifacts.json - current manifest shape and stable JSON conventions
    - .planning/phases/87-comparison-page-livebook/87-CONTEXT.md - D-01 through D-16 benchmark fairness and claim-binding decisions
    - .planning/phases/87-comparison-page-livebook/87-PATTERNS.md - manifest pattern, generated block pattern, docs-contract pattern
  </read_first>
  <action>
    Create `lib/rendro/comparison.ex` as `@moduledoc false`.

    Define module attributes for:
    - `@manifest_path "bench/results/comparison.json"`
    - `@guide_path "guides/comparison.md"`
    - generated block markers:
      - `<!-- rendro-comparison-fit-start -->` / `<!-- rendro-comparison-fit-end -->`
      - `<!-- rendro-comparison-results-start -->` / `<!-- rendro-comparison-results-end -->`
      - `<!-- rendro-comparison-evidence-start -->` / `<!-- rendro-comparison-evidence-end -->`
    - `@schema_version 1`
    - `@generated_by "mix rendro.comparison.gen"`
    - required comparator IDs: `rendro`, `chromic_pdf`, `chromic_pdf_warm_pool`, `pdf_generator`, `typst_cli`
    - required metric IDs: `cold_start_ms`, `rss_mb`, `container_image_mb`, `dependency_count`

    Implement public helpers:
    - `manifest_path/0`
    - `guide_path/0`
    - `fit_markers/0`
    - `results_markers/0`
    - `evidence_markers/0`
    - `read_manifest!/0`
    - `static_contract_errors/0`
    - `static_contract_errors/1`
    - `fit_block/1`
    - `results_block/1`
    - `evidence_block/1`

    `static_contract_errors/1` must validate only committed/static data:
    - `schema_version == 1`
    - `generated_by == "mix rendro.comparison.gen"`
    - run metadata has `id`, `recorded_at`, `git_sha`, `host`, `container`, `commands`, `repetitions`
    - scenario metadata has `id` and `fixture`
    - comparator IDs include the required five IDs
    - results include all required metric IDs
    - every result has `comparator`, `metric`, `median`, `p95`, `samples`, `unit`, `raw_artifact`, and `raw_sha256`
    - every raw artifact exists and hashes to the declared lowercase SHA-256
    - every public claim has `id`, `text`, `scope`, and `evidence`
    - claim IDs match `CMP-[A-Z0-9-]+`

    Do not call external benchmark commands, Docker, Chrome, wkhtmltopdf, Typst, Livebook, Kino, network APIs, or shell out from `static_contract_errors/0`.

    Create `bench/results/raw/.gitkeep`.

    Create `bench/results/raw/plan01-static-fixture.json` as a tiny deterministic raw artifact for the scaffold. It must be clearly non-public static-fixture data, not public benchmark evidence.

    Create an initial `bench/results/comparison.json` with the complete schema, required comparator IDs, required metric IDs, and raw artifact references that all point at existing raw files with correct SHA-256 values. Public claim records may be empty or marked `"public": false` in this scaffold. Do not use strings `TODO`, `TBD`, `placeholder`, or `sample-only`. Plan 02 must replace scaffold metrics with real committed benchmark artifacts before guide publication.
  </action>
  <acceptance_criteria>
    - `lib/rendro/comparison.ex` compiles without requiring external runtime deps.
    - `Rendro.Comparison.static_contract_errors/0` contains no call to `System.cmd/3`, Docker, Chrome, wkhtmltopdf, Typst, Livebook, Kino, or network clients.
    - `Rendro.Comparison.read_manifest!/0` reads `bench/results/comparison.json`.
    - Mutating a raw artifact SHA-256 in the manifest causes `static_contract_errors/1` to return an error naming the raw artifact.
    - Missing comparator `chromic_pdf_warm_pool` causes `static_contract_errors/1` to return an error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add unit and docs-contract tests for static comparison proof</name>
  <files>test/rendro/comparison_test.exs, test/docs_contract/comparison_claims_test.exs, scripts/verify_docs.exs</files>
  <read_first>
    - test/rendro/launch_artifacts_test.exs - pure unit tests for manifest-backed launch helpers
    - test/docs_contract/launch_artifacts_claims_test.exs - static docs-contract pattern, generated-block equality, package inclusion, overclaim guards, tarball cleanup
    - test/docs_contract/raster_claims_test.exs - support-matrix/static-boundary docs-contract style
    - scripts/verify_docs.exs - docs-contract lane registry format
    - .planning/phases/87-comparison-page-livebook/87-VALIDATION.md - Wave 0 and per-task verification map
  </read_first>
  <action>
    Create `test/rendro/comparison_test.exs`.

    Cover:
    - `read_manifest!/0` returns a map.
    - `static_contract_errors/1` catches missing `schema_version`, missing comparator IDs, missing metric IDs, invalid claim IDs, missing raw artifacts, and raw SHA-256 drift using in-memory mutated manifests and temporary raw files.
    - `fit_block/1`, `results_block/1`, and `evidence_block/1` include the expected marker names and `bench:CMP-` citations when the manifest has public claims.

    Create `test/docs_contract/comparison_claims_test.exs`.

    Cover:
    - `Rendro.Comparison.static_contract_errors() == []` against the Plan 01 scaffold manifest and tiny raw fixture.
    - manifest comparator IDs contain `rendro`, `chromic_pdf`, `chromic_pdf_warm_pool`, `pdf_generator`, and `typst_cli`.
    - public claim IDs are unique and resolve to `[bench:CMP-*]` citations.
    - no uncited comparative phrase appears in `guides/comparison.md` when the guide exists.
    - the docs verification script includes a lane exactly named `Comparison claims lane`.

    Edit `scripts/verify_docs.exs` to add:
    `{"Comparison claims lane", ["test", "test/docs_contract/comparison_claims_test.exs"]}`

    Update any lane-count assertion in `test/guardrails/required_checks_contract_test.exs` only if it currently pins the exact docs-contract lane count and fails after this registration.
  </action>
  <acceptance_criteria>
    - `mix test test/rendro/comparison_test.exs` exits 0.
    - `mix test test/docs_contract/comparison_claims_test.exs` exits 0.
    - `mix run scripts/verify_docs.exs` reaches the comparison lane; if later lanes fail for unrelated pre-existing reasons, the comparison lane itself must pass.
    - The new required docs-contract lane does not require Chrome, wkhtmltopdf, Typst, Livebook, Kino, Docker, network access, or benchmark reruns.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Required docs-contract tests trust only committed manifest/raw/docs files.
- External benchmark tools remain outside this plan and are introduced only as advisory work in later plans.
- Public guide claims are untrusted until bound to manifest claim IDs and citations.
</trust_boundaries>

<threats>
- T-87-01 benchmark-result tampering: mitigate with raw artifact SHA-256 validation in `Rendro.Comparison.static_contract_errors/0`.
- T-87-02 uncited public overclaim: mitigate with citation resolution and forbidden comparative phrase scaffolding in the docs-contract test.
- T-87-03 required CI contamination: mitigate by keeping Plan 01 static checks free of external tools and network calls.
- T-87-04 stale generated blocks: accepted until Plan 03 creates the comparison guide; Plan 01 only creates helper and test scaffolding.
- T-87-05 placeholder benchmark data reaching public docs: block in Plan 03 and Plan 06; any placeholder claim must be marked non-public and absent from generated public guide blocks.
</threats>

<high_severity_blockers>
Stop if passing the Plan 01 docs-contract tests requires shelling out to Chrome,
wkhtmltopdf, Typst, Docker, Livebook, or Kino. That would violate D-15 and D-16.
</high_severity_blockers>
</threat_model>

<verification>
Required commands after this plan:
- `mix test test/rendro/comparison_test.exs`
- `mix test test/docs_contract/comparison_claims_test.exs`
- `mix test test/guardrails/required_checks_contract_test.exs`

Optional sanity command:
- `mix run -e "IO.inspect(Rendro.Comparison.static_contract_errors())"`
</verification>

<success_criteria>
Plan 87-01 is complete when the repository has a static comparison manifest
contract, a required docs-contract lane, and unit tests that make benchmark
truth explicit before any public comparison guide is expanded.
</success_criteria>
