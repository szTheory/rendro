---
phase: 87-comparison-page-livebook
plan: 05
type: execute
wave: 4
depends_on:
  - 87-03
  - 87-04
files_modified:
  - mix.exs
  - README.md
  - .github/workflows/ci.yml
  - priv/guardrails/required_status_checks.json
  - test/guardrails/required_checks_contract_test.exs
  - test/docs_contract/comparison_claims_test.exs
autonomous: true
requirements:
  - CMP-02
  - CMP-03

must_haves:
  truths:
    - "mix.exs registers guides/comparison.md and guides/livebook/first_invoice.livemd as ExDoc extras, with intentional grouping."
    - "The Hex package includes guides/comparison.md, guides/livebook/first_invoice.livemd, bench/results/comparison.json, and every raw artifact cited by the manifest."
    - "README links to the comparison guide and Livebook tutorial in the Guides area without adding a marketing hero."
    - "comparison-advisory and livebook-advisory CI jobs are graph-disconnected advisory contexts and are absent from required_contexts."
    - "The required test job still runs only mix ci and does not install Chrome, wkhtmltopdf, Typst, Livebook server tooling, Kino, Docker, or run benchmark/livebook advisory tasks."
    - "Decision coverage: D-15, D-16, D-23, D-29, and D-30 are enforced through ExDoc, package, README, docs-contract, and guardrail tests."
    - "D-15: docs-contract checks validate manifest shape, raw hashes, generated block freshness, citation resolution, forbidden overclaims, pins, environment metadata, caveats, ExDoc registration, package inclusion, and required/advisory CI separation."
    - "D-16: benchmark reruns, Chrome/wkhtmltopdf/Typst downloads, Livebook execution, Kino, and pdfium-style external tooling stay advisory and out of required mix ci."
    - "D-23: README and guide surfaces expose Try the invoice workflow in Livebook and Reproduce the benchmark results affordances."
    - "D-29: ExDoc extras register the .livemd so HexDocs owns the Run in Livebook affordance, while README gets a compact guide link."
    - "D-30: docs-contract and guardrail tests prove notebook package inclusion, README/comparison links, advisory CI, and absence from required status checks."
  artifacts:
    - path: "mix.exs"
      provides: "ExDoc extras and package inclusion for comparison and Livebook assets"
      contains: "guides/livebook/first_invoice.livemd"
    - path: "priv/guardrails/required_status_checks.json"
      provides: "Advisory CI context contract"
      contains: "livebook-advisory"
---

<objective>
Wire the comparison guide and Livebook tutorial into public documentation,
package contents, README discoverability, and advisory CI while proving required
CI remains deterministic and external-tool free.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-comparison-page-livebook/87-CONTEXT.md
@.planning/phases/87-comparison-page-livebook/87-RESEARCH.md
@.planning/phases/87-comparison-page-livebook/87-PATTERNS.md
@.planning/phases/87-comparison-page-livebook/87-UI-SPEC.md
@.planning/phases/87-comparison-page-livebook/87-03-PLAN.md
@.planning/phases/87-comparison-page-livebook/87-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register guide/notebook in ExDoc and package contents</name>
  <files>mix.exs, test/docs_contract/comparison_claims_test.exs</files>
  <read_first>
    - mix.exs - docs extras/groups, package allowlist, dev/test dependency placement
    - test/docs_contract/launch_artifacts_claims_test.exs - Hex package inclusion test and tarball cleanup pattern
    - guides/comparison.md - guide to register
    - guides/livebook/first_invoice.livemd - notebook to register
    - bench/results/comparison.json - benchmark manifest to include
  </read_first>
  <action>
    Edit `mix.exs`.

    In `docs/0`:
    - add `guides/comparison.md` to `extras`;
    - add `guides/livebook/first_invoice.livemd` to `extras`;
    - add a `Evaluation` group containing both paths, or extend an existing guide group if that is cleaner.

    In `package/0`:
    - ensure `guides` remains included;
    - add `bench/results` so `bench/results/comparison.json` and raw artifacts cited by the public guide ship with the Hex package.

    Update `test/docs_contract/comparison_claims_test.exs` so it asserts:
    - docs extras contain `guides/comparison.md`;
    - docs extras contain `guides/livebook/first_invoice.livemd`;
    - package contents include `guides/comparison.md`;
    - package contents include `guides/livebook/first_invoice.livemd`;
    - package contents include `bench/results/comparison.json`;
    - package contents include every raw artifact path listed in the manifest.

    Use the tarball cleanup pattern from `test/docs_contract/launch_artifacts_claims_test.exs`: compute `rendro-#{Mix.Project.config()[:version]}.tar`, remove before build, register `on_exit(fn -> File.rm(tarball) end)`, and assert it is absent after the test command when verifying manually.
  </action>
  <acceptance_criteria>
    - `mix test test/docs_contract/comparison_claims_test.exs` exits 0.
    - `mix hex.build` package contents include `guides/comparison.md`, `guides/livebook/first_invoice.livemd`, and `bench/results/comparison.json`.
    - After package tests, `test ! -f rendro-1.0.0.tar` exits 0.
    - `mix docs` includes both new extras without undefined-reference failures.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add README guide and Livebook affordances</name>
  <files>README.md, test/docs_contract/comparison_claims_test.exs</files>
  <read_first>
    - README.md - current first-screen and Guides section
    - .planning/phases/87-comparison-page-livebook/87-UI-SPEC.md - README placement rules and anti-hero constraint
    - guides/comparison.md - target comparison guide path
    - guides/livebook/first_invoice.livemd - target notebook path
  </read_first>
  <action>
    Edit `README.md`.

    In the `## Guides` list, add:
    - `[Generating PDFs in Elixir without Chrome](guides/comparison.md)` with a short description as an evidence-backed comparison guide.
    - `[First Invoice Livebook](guides/livebook/first_invoice.livemd)` with a short description as a zero-friction tutorial.

    If adding a Livebook badge/link, keep it compact and adjacent to the guide list. Do not add a large hero, screenshot composite, or marketing banner.

    Update `test/docs_contract/comparison_claims_test.exs` to assert README contains both paths.
  </action>
  <acceptance_criteria>
    - `README.md` contains `guides/comparison.md`.
    - `README.md` contains `guides/livebook/first_invoice.livemd`.
    - `README.md` does not contain a new hero block before the rendered recipe gallery.
    - `mix test test/docs_contract/comparison_claims_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add advisory CI jobs and required/advisory guardrails</name>
  <files>.github/workflows/ci.yml, priv/guardrails/required_status_checks.json, test/guardrails/required_checks_contract_test.exs</files>
  <read_first>
    - .github/workflows/ci.yml - existing required and advisory job blocks; raster-advisory graph-disconnected pattern
    - priv/guardrails/required_status_checks.json - required_contexts and advisory_contexts shape
    - test/guardrails/required_checks_contract_test.exs - scoped CI job-block helper and negative proof patterns
    - .planning/phases/87-comparison-page-livebook/87-CONTEXT.md - D-15, D-16, D-28, D-30 advisory-lane decisions
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml`.

    Add `comparison-advisory`:
    - `runs-on: ubuntu-latest`
    - `continue-on-error: true`
    - no `needs:` key
    - checkout, setup Beam OTP 28 / Elixir 1.19.5, `mix deps.get`, then `mix rendro.comparison.check`
    - do not rerun the full external benchmark in this job unless external tools are explicitly installed in a future refinement; this advisory check validates committed benchmark truth.

    Add `livebook-advisory`:
    - `runs-on: ubuntu-latest`
    - `continue-on-error: true`
    - no `needs:` key
    - checkout, setup Beam OTP 28 / Elixir 1.19.5, `mix deps.get`, then `mix rendro.livebook.check`

    Edit `priv/guardrails/required_status_checks.json`.

    Add advisory context objects:
    - `comparison-advisory` with semantic_class `comparison_static_advisory`, ci_job `comparison-advisory`, command `mix rendro.comparison.check`, notes naming Phase 87 and saying not required.
    - `livebook-advisory` with semantic_class `livebook_execution`, ci_job `livebook-advisory`, command `mix rendro.livebook.check`, notes naming Phase 87 and saying not required.

    Do not add either context to `required_contexts`.

    Edit `test/guardrails/required_checks_contract_test.exs`.

    Add assertions:
    - both advisory contexts exist in `advisory_contexts`;
    - neither appears in `required_contexts`;
    - CI contains both job blocks;
    - each advisory job has `continue-on-error: true`;
    - each advisory job block has no `needs:` key;
    - the required `test` job block still runs `mix ci`;
    - the required `test` job block does not contain `rendro.comparison.check`, `rendro.livebook.check`, `chrome`, `wkhtmltopdf`, `typst`, `Livebook`, `Kino`, or `docker`.

    If the test pins the exact docs-contract lane count, update it for the comparison lane added in Plan 01.
  </action>
  <acceptance_criteria>
    - `mix test test/guardrails/required_checks_contract_test.exs` exits 0.
    - `priv/guardrails/required_status_checks.json` parses as JSON.
    - `.github/workflows/ci.yml` parses as YAML.
    - `comparison-advisory` and `livebook-advisory` are absent from `required_contexts`.
    - Removing `continue-on-error: true` from either advisory job causes the guardrail test to fail.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Required CI remains `mix ci`; advisory comparison/notebook checks are separate status contexts.
- Hex package contents are trusted only after tarball inspection.
- README links are discovery aids, not proof; docs-contract tests still own proof.
</trust_boundaries>

<threats>
- T-87-21 missing public docs/package artifacts: mitigate with ExDoc extras and Hex package assertions.
- T-87-22 required CI contamination: mitigate with scoped CI job-block negative assertions.
- T-87-23 advisory job false-blocking: mitigate with no `needs:` and `continue-on-error: true`.
- T-87-24 README marketing drift: mitigate by adding compact guide links only, no hero.
- T-87-25 package tarball worktree noise: mitigate with immediate cleanup in package tests.
</threats>

<high_severity_blockers>
Stop if adding the comparison guide or notebook to HexDocs requires moving
benchmark reruns, Chrome/wkhtmltopdf/Typst installs, or Livebook execution into
the required `test` job or `mix ci`.
</high_severity_blockers>
</threat_model>

<verification>
Required commands after this plan:
- `mix test test/docs_contract/comparison_claims_test.exs`
- `mix test test/guardrails/required_checks_contract_test.exs`
- `mix docs`
- `mix hex.build`
- `test ! -f rendro-1.0.0.tar`
</verification>

<success_criteria>
Plan 87-05 is complete when the comparison guide and Livebook tutorial are
discoverable in README/HexDocs/package output and their advisory checks are
registered without becoming required CI.
</success_criteria>
