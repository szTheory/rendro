---
phase: 87-comparison-page-livebook
plan: 04
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - guides/livebook/first_invoice.livemd
  - lib/mix/tasks/rendro/livebook/check.ex
  - test/mix/tasks/rendro_livebook_check_test.exs
  - mix.exs
autonomous: true
requirements:
  - CMP-03

must_haves:
  truths:
    - "guides/livebook/first_invoice.livemd renders a deterministic invoice PDF, asserts %PDF-, displays byte size and SHA-256, previews via Kino.HTML.new/1, and downloads via Kino.Download.new/2."
    - "The notebook has a published-use Mix.install mode and a RENDRO_LIVEBOOK_LOCAL=1 checkout mode for advisory CI."
    - "The notebook keeps Phoenix code schematic and does not start a Phoenix app or make Phoenix a runtime requirement."
    - "mix rendro.livebook.check converts the .livemd through Livebook.live_markdown_to_elixir/1 and executes it without starting a Livebook server."
    - "Livebook is added only as a dev/test/runtime-false dependency if needed for the advisory check; Kino remains inside notebook Mix.install and is not a Rendro runtime dependency."
    - "Decision coverage: D-24 through D-30 are implemented by the notebook shape, advisory execution helper, and static assertions."
    - "D-24: ship one canonical guides/livebook/first_invoice.livemd notebook with no multi-recipe gallery notebook and no benchmark cells."
    - "D-25: notebook flow uses Mix.install, fixed invoice data, Rendro.Recipes.Invoice.document/1, deterministic render, %PDF- assertion, byte size, SHA-256, Kino HTML preview, Kino download, Phoenix handoff, and links."
    - "D-26: Phoenix code stays schematic and is not executed in Livebook."
    - "D-27: Kino is used only inside notebook Mix.install; project livebook dependency may be dev/test runtime-false for advisory conversion."
    - "D-28: CI executes notebook code cells without starting a Livebook server by converting .livemd with Livebook.live_markdown_to_elixir/1."
    - "D-29: ExDoc owns the main Livebook affordance by listing the .livemd in extras; README gets a compact guide/badge link."
    - "D-30: static docs-contract tests verify ExDoc extras, Hex package inclusion, README/comparison guide links, advisory guardrails, required-status absence, and advisory CI execution."
  artifacts:
    - path: "guides/livebook/first_invoice.livemd"
      provides: "Zero-friction first invoice tutorial"
      contains: "Kino.Download.new"
    - path: "lib/mix/tasks/rendro/livebook/check.ex"
      provides: "Advisory notebook execution task"
      contains: "live_markdown_to_elixir"
---

<objective>
Create the canonical Livebook tutorial and a CI-friendly advisory check that
executes the notebook code cells without launching a Livebook server. This plan
prevents notebook rot while preserving Rendro core's optional dependency
boundary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-comparison-page-livebook/87-CONTEXT.md
@.planning/phases/87-comparison-page-livebook/87-RESEARCH.md
@.planning/phases/87-comparison-page-livebook/87-PATTERNS.md
@.planning/phases/87-comparison-page-livebook/87-UI-SPEC.md
</context>

<decision_traceability>
- D-24: Ship one canonical notebook at `guides/livebook/first_invoice.livemd`; no multi-recipe gallery notebook and no benchmark cells.
- D-25: Notebook flow uses Mix.install, fixed invoice data, `Rendro.Recipes.Invoice.document/1`, deterministic render, `%PDF-` assertion, byte size, SHA-256, Kino HTML preview, Kino download, Phoenix handoff, and links.
- D-26: Phoenix code stays schematic and is not executed in Livebook.
- D-27: Kino is used only inside notebook `Mix.install`; project `:livebook` may be dev/test runtime-false for the advisory conversion API.
- D-28: CI executes notebook code cells without starting a Livebook server by converting `.livemd` with `Livebook.live_markdown_to_elixir/1`.
- D-29: ExDoc owns the main Livebook affordance by listing the `.livemd` in extras; README gets a compact guide/badge link.
- D-30: Static docs-contract tests verify ExDoc extras, Hex package inclusion, README/comparison guide links, advisory guardrails, and required-status absence; advisory CI verifies execution.
</decision_traceability>

<tasks>

<task type="auto">
  <name>Task 1: Add the first-invoice Livebook tutorial</name>
  <files>guides/livebook/first_invoice.livemd</files>
  <read_first>
    - .planning/phases/87-comparison-page-livebook/87-CONTEXT.md - D-24 through D-30 Livebook decisions
    - .planning/phases/87-comparison-page-livebook/87-UI-SPEC.md - notebook flow and UX requirements
    - lib/rendro/recipes/invoice.ex - invoice data contract and `document/2` API
    - README.md - current getting-started code style
    - guides/recipes.md - recipe guide links and tone
  </read_first>
  <action>
    Create `guides/livebook/first_invoice.livemd`.

    The notebook must contain cells that:
    - install Rendro from Hex by default;
    - switch to the local checkout when `System.get_env("RENDRO_LIVEBOOK_LOCAL") == "1"`;
    - install Kino inside the notebook `Mix.install` dependency list;
    - define fixed invoice data with at least three line items;
    - build `Rendro.Recipes.Invoice.document(data)`;
    - call `Rendro.render(doc, deterministic: true)`;
    - assert `binary_part(pdf, 0, 5) == "%PDF-"`;
    - compute and display byte size and SHA-256;
    - preview the PDF with `Kino.HTML.new/1` using a base64 `data:application/pdf` iframe or embed;
    - provide `Kino.Download.new(fn -> pdf end, filename: "rendro-invoice.pdf")`;
    - include a schematic Phoenix controller handoff snippet that is not executed;
    - link to `guides/recipes.md`, `guides/comparison.md`, and `assets/rendro/manual.pdf`.

    Keep the notebook simple enough for `Livebook.live_markdown_to_elixir/1`:
    - no macros split across cells;
    - no branching sections required for the success path;
    - no benchmark cells;
    - no Phoenix runtime startup.
  </action>
  <acceptance_criteria>
    - `guides/livebook/first_invoice.livemd` contains `RENDRO_LIVEBOOK_LOCAL`.
    - `guides/livebook/first_invoice.livemd` contains `Rendro.Recipes.Invoice.document`.
    - `guides/livebook/first_invoice.livemd` contains `Kino.HTML.new`.
    - `guides/livebook/first_invoice.livemd` contains `Kino.Download.new`.
    - `guides/livebook/first_invoice.livemd` contains `%PDF-`.
    - `guides/livebook/first_invoice.livemd` does not contain benchmark execution cells.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the no-server Livebook execution task</name>
  <files>lib/mix/tasks/rendro/livebook/check.ex, mix.exs</files>
  <read_first>
    - lib/mix/tasks/docs.contract.ex - canonical thin Mix task and injected runner pattern
    - lib/mix/tasks/verify.ex - command runner and final summary patterns
    - test/mix/tasks/docs_contract_task_test.exs - task test seam pattern
    - mix.exs - dev/test dependency placement and optional dependency conventions
    - .planning/phases/87-comparison-page-livebook/87-RESEARCH.md - current Livebook API facts and conversion limitations
  </read_first>
  <action>
    Add `{:livebook, "~> 0.19", only: [:dev, :test], runtime: false}` to `mix.exs` only if no existing dependency already provides `Livebook.live_markdown_to_elixir/1`.

    Do not add `:kino` to `mix.exs` unless a test proves it is required outside the notebook. Kino should normally live only in the notebook `Mix.install` call.

    Create `lib/mix/tasks/rendro/livebook/check.ex` with:
    - `use Mix.Task`;
    - `@moduledoc tags: [:adapter]`;
    - `@compile {:no_warn_undefined, Livebook}`;
    - default notebook path `guides/livebook/first_invoice.livemd`;
    - injected converter env key `:livebook_converter`, defaulting to a private function that calls `Livebook.live_markdown_to_elixir/1` after `Code.ensure_loaded?(Livebook)`;
    - injected command runner env key `:livebook_command_runner`, defaulting to `&System.cmd/3`;
    - temp script writing and cleanup;
    - `RENDRO_LIVEBOOK_LOCAL=1` in the command environment;
    - clear non-zero exit and error output if conversion or execution fails.

    The task must not start a Livebook server.
  </action>
  <acceptance_criteria>
    - `mix deps.get` succeeds after dependency update.
    - `mix compile --warnings-as-errors` does not warn about missing `Livebook` in normal test/dev use.
    - `mix.exs` has no runtime dependency on `:livebook` or `:kino`.
    - `lib/mix/tasks/rendro/livebook/check.ex` contains `Livebook.live_markdown_to_elixir`.
    - The task can be invoked as `mix rendro.livebook.check`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add task tests and run the notebook check</name>
  <files>test/mix/tasks/rendro_livebook_check_test.exs</files>
  <read_first>
    - test/mix/tasks/docs_contract_task_test.exs - Mix shell capture and injected runner pattern
    - test/mix/tasks/verify_test.exs - command failure and summary handling
    - lib/mix/tasks/rendro/livebook/check.ex - task under test
    - guides/livebook/first_invoice.livemd - notebook under test
  </read_first>
  <action>
    Create `test/mix/tasks/rendro_livebook_check_test.exs`.

    Cover:
    - the task reads the configured notebook path and calls the injected converter;
    - the task runs the injected command runner with `RENDRO_LIVEBOOK_LOCAL=1`;
    - non-zero command runner exit causes the task to exit non-zero and print command output;
    - converter failure or missing Livebook dependency causes a clear error;
    - the production notebook contains `Kino.HTML.new`, `Kino.Download.new`, `%PDF-`, and `RENDRO_LIVEBOOK_LOCAL`.

    After tests pass, run `mix rendro.livebook.check` against the real notebook.
  </action>
  <acceptance_criteria>
    - `mix test test/mix/tasks/rendro_livebook_check_test.exs` exits 0.
    - `mix rendro.livebook.check` exits 0.
    - Failure output from a fake command runner is surfaced in test assertions.
    - No Livebook server process is started by the task.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Notebook UX dependencies are isolated to `Mix.install` and advisory execution.
- Livebook conversion/execution is advisory and must not become required `mix ci` until explicitly wired in Plan 05 as advisory-only.
- Phoenix code in the notebook is schematic and must not pull Phoenix into the tutorial runtime.
</trust_boundaries>

<threats>
- T-87-16 Livebook rot: mitigate with `mix rendro.livebook.check` converting and executing the notebook code cells.
- T-87-17 runtime dependency leak: mitigate with `only: [:dev, :test], runtime: false` for Livebook and no project-level Kino dep.
- T-87-18 server exposure: mitigate by never starting a Livebook server in CI; use conversion to an Elixir script.
- T-87-19 notebook conversion failure: mitigate by avoiding cross-cell macros and branching sections required for success.
- T-87-20 Phoenix boundary drift: mitigate by keeping Phoenix handoff code schematic and non-executed.
</threats>

<high_severity_blockers>
Stop if the notebook can only execute by adding Kino or Phoenix as Rendro
runtime dependencies, or by starting a Livebook server in CI.
</high_severity_blockers>
</threat_model>

<verification>
Required commands after this plan:
- `mix deps.get`
- `mix compile --warnings-as-errors`
- `mix test test/mix/tasks/rendro_livebook_check_test.exs`
- `mix rendro.livebook.check`
</verification>

<success_criteria>
Plan 87-04 is complete when the Livebook tutorial gives a first-invoice render,
preview, and download flow, and the advisory check proves the notebook cannot rot
without contaminating Rendro's runtime dependency boundary.
</success_criteria>
