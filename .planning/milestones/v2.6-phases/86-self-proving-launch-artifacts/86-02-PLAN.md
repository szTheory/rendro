---
phase: 86-self-proving-launch-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/launch_artifacts.ex
  - test/docs_contract/launch_artifacts_claims_test.exs
autonomous: true
requirements:
  - GAL-01
  - GAL-02
  - GAL-03

must_haves:
  truths:
    - "Rendro.LaunchArtifacts.static_contract_errors/0 remains pdfium-free and safe for required mix test/mix ci."
    - "The static contract detects manifest shape drift, exact five gallery IDs, committed PNG/manual hash drift, regenerated source-PDF hash drift, regenerated manual hash drift, stale generated docs blocks, missing alt text, package omissions, and public overclaims."
    - "The Hex package check covers assets/rendro/artifacts.json, assets/rendro/manual.pdf, and all five gallery PNG files."
    - "The package assertion removes the generated rendro-1.0.0.tar tarball after the test."
    - "Decision coverage: D-09, D-11, D-12, D-15, and D-16 are enforced without raster regeneration."
  artifacts:
    - path: "lib/rendro/launch_artifacts.ex"
      provides: "Static launch artifact docs-contract proof surface"
      contains: "static_contract_errors"
    - path: "test/docs_contract/launch_artifacts_claims_test.exs"
      provides: "Required docs-contract assertions for gallery/manual/package/copy proof"
      contains: "hex package includes every public launch asset"
---

<objective>
Strengthen the required/static proof for the Phase 86 gallery and manual before changing fixture styling or regenerating assets. This plan makes the docs-contract lane authoritative for deterministic proof while keeping pdfium-only PNG regeneration out of required CI.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md
@.planning/phases/86-self-proving-launch-artifacts/86-RESEARCH.md
@.planning/phases/86-self-proving-launch-artifacts/86-PATTERNS.md
@.planning/phases/86-self-proving-launch-artifacts/86-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand static manifest and drift validation without pdfium</name>
  <files>lib/rendro/launch_artifacts.ex</files>
  <read_first>
    - lib/rendro/launch_artifacts.ex - file being modified; current manifest shape, static_contract_errors/0, raster_contract_errors/2, file hash, source PDF, manual, and docs block checks
    - assets/rendro/artifacts.json - current generated manifest shape and field values
    - priv/pdfium_pin.json - source of truth for renderer pin_version and pin_sha256
    - .planning/phases/86-self-proving-launch-artifacts/86-CONTEXT.md - decisions D-09 through D-12
    - .planning/phases/86-self-proving-launch-artifacts/86-PATTERNS.md - manifest pattern, static vs raster checks, error message pattern
  </read_first>
  <action>
    Edit `lib/rendro/launch_artifacts.ex`.

    Keep `static_contract_errors/0` and `static_contract_errors/1` free of `Rendro.Adapters.Pdfium.render/2`, `Rendro.Adapters.Pdfium.version/0`, executable lookup, network access, and file writes.

    Extend `collect_manifest_shape_errors/2` or split it into private helpers so static validation checks all of these source assertions:
    - `schema_version == 1`.
    - `generated_by == "mix rendro.launch_artifacts.gen"`.
    - `renderer.kind == "pdfium-render"`.
    - `renderer.dpi == 96`.
    - `renderer.pin_path == "priv/pdfium_pin.json"`.
    - `renderer.pin_version` equals `priv/pdfium_pin.json` `"version"`.
    - `renderer.pin_sha256` equals `priv/pdfium_pin.json` `"sha256"` and is a lowercase 64-character SHA-256 hex digest.
    - `manual.path == "assets/rendro/manual.pdf"`.
    - `manual.source == "Rendro.LaunchArtifacts.render_manual_pdf/0"`.
    - `manual.sha256` is a lowercase 64-character SHA-256 hex digest.
    - gallery IDs are exactly `invoice`, `branded_invoice`, `statement`, `receipt_report`, `certificate` in that order.
    - every gallery entry has the required keys already listed in the scaffold.
    - every gallery `png_sha256` and `source_pdf_sha256` is a lowercase 64-character SHA-256 hex digest.
    - every gallery `renderer_kind == "pdfium-render"`.
    - every gallery `dpi == 96`.
    - every gallery `page == 1`.
    - every gallery `alt` and `caption` is a non-empty trimmed string.
    - portrait gallery entries retain dimensions `794x1123` and the certificate retains `1123x794` unless this plan also updates tests and generated manifest intentionally.

    Keep raster-only checks in `raster_contract_errors/2`: regenerated PNG hash, PNG dimensions, and renderer-version drift.

    Make new static drift errors actionable. At least one error message for stale docs or hash drift must include `run mix rendro.launch_artifacts.gen`.
  </action>
  <acceptance_criteria>
    - `Rendro.LaunchArtifacts.static_contract_errors/0` contains no call path to `Rendro.Adapters.Pdfium.render/2`.
    - `Rendro.LaunchArtifacts.static_contract_errors/0` returns `[]` for the current manifest after this plan's test updates and before fixture regeneration.
    - If `assets/rendro/artifacts.json` `renderer.pin_sha256` differs from `priv/pdfium_pin.json`, `static_contract_errors/0` returns an error.
    - If a gallery `alt` value is set to an empty string, `static_contract_errors/0` returns an error.
    - If `assets/rendro/manual.pdf` bytes drift from `manifest["manual"]["sha256"]`, `static_contract_errors/0` returns an error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Harden launch artifact docs-contract tests and package inclusion</name>
  <files>test/docs_contract/launch_artifacts_claims_test.exs</files>
  <read_first>
    - test/docs_contract/launch_artifacts_claims_test.exs - file being modified; current static, gallery, copy, CI, and package tests
    - lib/rendro/launch_artifacts.ex - generator/checker APIs and readme_block/1, recipes_block/1 helpers under test
    - mix.exs - package files allowlist and ExDoc asset mapping source of truth
    - README.md - generated launch block markers and public copy surface
    - guides/recipes.md - generated gallery block markers and public copy surface
    - scripts/verify_docs.exs - required docs-contract lane registry
    - .planning/phases/86-self-proving-launch-artifacts/86-UI-SPEC.md - copy, accessibility, and package surface requirements
  </read_first>
  <action>
    Edit `test/docs_contract/launch_artifacts_claims_test.exs`.

    Keep the existing `Rendro.LaunchArtifacts.static_contract_errors() == []` test.

    Add or strengthen tests so the required docs-contract lane asserts:
    - exact five gallery IDs in manifest order.
    - README generated block equals `Rendro.LaunchArtifacts.readme_block(manifest)`.
    - recipes guide generated block equals `Rendro.LaunchArtifacts.recipes_block(manifest)`.
    - README and recipes guide include every gallery `png_path`.
    - every image has non-empty manifest alt text and both generated surfaces emit that exact alt text.
    - every README image link `href` equals the image `src`.
    - recipes guide includes every source PDF SHA-256 and PNG SHA-256 from the manifest.
    - README and recipes guide include `assets/rendro/manual.pdf` and the manifest manual SHA-256.
    - public copy includes `pdfium-render`.
    - public copy does not include positive GUI-viewer overclaims such as `is GUI-viewer proof` or `are GUI-viewer proof`, and does not include `works in every viewer`, `PDF/A compliant`, `PDF/UA compliant`, `full HTML/CSS rendering`, `browserless viewer`, `prepress SDK`, `pixel-perfect HTML-to-PDF`, or `launch gallery shows default recipe styling`.
    - `scripts/verify_docs.exs` still registers `Launch artifacts claims lane`.

    Replace the package assertion so it checks all public launch assets:
    - `assets/rendro/artifacts.json`
    - `assets/rendro/manual.pdf`
    - `assets/rendro/gallery/invoice.png`
    - `assets/rendro/gallery/branded_invoice.png`
    - `assets/rendro/gallery/statement.png`
    - `assets/rendro/gallery/receipt_report.png`
    - `assets/rendro/gallery/certificate.png`

    Register `on_exit(fn -> File.rm(tarball) end)` immediately after computing and removing the `rendro-#{Mix.Project.config()[:version]}.tar` path. The test must leave no Hex tarball behind whether it passes or fails after the build.

    Do not add any test that calls `mix rendro.launch_artifacts.check`, `Rendro.Adapters.Pdfium.render/2`, or downloads pdfium. This file belongs to required static docs-contract proof.
  </action>
  <acceptance_criteria>
    - `mix test test/docs_contract/launch_artifacts_claims_test.exs` exits 0 without a pdfium executable on PATH.
    - After `mix test test/docs_contract/launch_artifacts_claims_test.exs`, `test ! -f rendro-1.0.0.tar` exits 0.
    - If any one of the five gallery PNGs is absent from the `mix hex.build` package contents, the package inclusion test fails.
    - Removing a gallery image from the README generated block causes the docs block equality test to fail.
    - Adding `PDF/A compliant` to README or `guides/recipes.md` causes the public copy test to fail.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS level: 1

<trust_boundaries>
- Static docs-contract proof runs in required CI and must not call pdfium or network.
- Manifest and generated docs blocks are trusted only when regenerated source PDFs/manual and committed asset hashes match.
- Hex package contents are trusted only through `mix hex.build` inspection that cleans up its generated tarball.
</trust_boundaries>

<threats>
- T-86-06 stale/hash-drift artifacts: mitigate by comparing committed manual/PNG hashes, regenerated source-PDF hashes, regenerated manual hash, and generated README/guide blocks against the manifest.
- T-86-07 accidental public overclaims: mitigate with public copy negative assertions for positive GUI-viewer claims, PDF/A, PDF/UA, full HTML/CSS, browser/prepress, pixel-perfect HTML-to-PDF, and default-styling claims while still allowing the required boundary phrase `not GUI-viewer proof`.
- T-86-08 package-content mistakes: mitigate by inspecting the Hex tarball for manifest, manual, and all five gallery PNGs.
- T-86-09 required CI depending on external pdfium tooling: block if any docs-contract test in this plan needs pdfium; static checks must remain renderer-free.
- T-86-10 generated asset/tarball worktree noise: mitigate with `on_exit` tarball cleanup in the package test.
</threats>

<high_severity_blockers>
None expected. Stop if the only way to make static contract tests pass is to call pdfium, regenerate PNGs, or weaken manifest/hash checks.
</high_severity_blockers>
</threat_model>

<verification>
Required commands after this plan:
- `mix test test/docs_contract/launch_artifacts_claims_test.exs`
- `mix test test/docs_contract/launch_artifacts_claims_test.exs --trace`
- `test ! -f rendro-1.0.0.tar`

Optional sanity command:
- `mix run -e "IO.inspect(Rendro.LaunchArtifacts.static_contract_errors())"`
</verification>

<success_criteria>
Plan 86-02 is complete when required docs-contract tests prove all five gallery entries and the self-rendered manual are statically current, publicly bounded, package-included, and free of pdfium/runtime-tool requirements.
</success_criteria>
