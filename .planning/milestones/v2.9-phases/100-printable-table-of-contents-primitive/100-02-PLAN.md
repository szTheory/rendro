---
phase: 100
plan: 02
type: execute
wave: 2
depends_on:
  - 100-01
files_modified:
  - lib/rendro/pipeline/paginate.ex
  - test/rendro/pipeline/paginate_test.exs
autonomous: true
requirements:
  - TOC-03
must_haves:
  truths:
    - Paginate phase performs post-layout substitution of exact page numbers.
    - Multi-page document generation remains strictly single-pass and deterministic.
  artifacts:
    - path: lib/rendro/pipeline/paginate.ex
      provides: post-pagination token substitution
  key_links: []
---

<objective>
Substitute `{{anchor_page:id}}` tokens with exact resolved page numbers in a post-pagination pass to ensure single-pass deterministic document generation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Perform Post-Pagination Token Substitution</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs</files>
  <behavior>
    - After `collect_anchors` identifies the physical pages of all `id` attributes.
    - A new step `resolve_toc_tokens` replaces `{{anchor_page:id}}` in text content with the actual `page_number` from `doc.metadata.anchors[id]`.
    - Substitution happens directly in the measured text blocks.
  </behavior>
  <action>
    - In `lib/rendro/pipeline/paginate.ex`, add a `resolve_toc_tokens/1` step at the end of the `paginate_flow`.
    - Walk through all `pages` and their `blocks`.
    - For text blocks containing `{{anchor_page:id}}`, map the `id` against `doc.metadata.anchors`. Replace the token with the resolved `page_idx`.
    - Write an integration test proving that an anchor placed on page 5 will correctly render as "5" on page 1 without throwing overflow errors or altering text boundaries.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs</automated>
  </verify>
  <done>Tokens are correctly substituted with exact page numbers.</done>
</task>
</tasks>

<verification>
Ensure `mix test` passes.
</verification>

<success_criteria>
Final document text correctly substitutes the exact page numbers into the text blocks without oscillating layout.
</success_criteria>

<output>
Create `.planning/phases/100-printable-table-of-contents-primitive/100-02-SUMMARY.md` when done
</output>