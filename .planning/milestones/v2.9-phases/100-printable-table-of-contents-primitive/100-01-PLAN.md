---
phase: 100
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/pipeline/measure.ex
  - test/rendro/pipeline/measure_test.exs
autonomous: true
requirements:
  - TOC-01
  - TOC-02
must_haves:
  truths:
    - `Rendro.Pipeline.Measure` detects `{{anchor_page:id}}` tokens inside text blocks.
    - Token widths are measured as fixed bounding boxes (e.g., equivalent to "8888") to prevent layout oscillation.
  artifacts:
    - path: lib/rendro/pipeline/measure.ex
      provides: deterministic bounding box sizing for unresolved page substitution tokens
  key_links: []
---

<objective>
Introduce the `{{anchor_page:id}}` substitution token and ensure the Measure pipeline reserves a fixed-width bounding box for it to prevent infinite layout oscillations when real page numbers are later injected.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Token Measurement Primitive</name>
  <files>lib/rendro/pipeline/measure.ex, test/rendro/pipeline/measure_test.exs</files>
  <behavior>
    - Text containing `{{anchor_page:id}}` is parsed.
    - For the purpose of layout wrapping, the token is treated as having the width of "8888" in the block's current font and size.
  </behavior>
  <action>
    - In `lib/rendro/pipeline/measure.ex`, intercept text strings before measurement.
    - Temporarily replace `{{anchor_page:.*}}` with a fixed string like `"8888"` when calculating the text dimensions.
    - Write unit tests ensuring that `Rendro.text("Page {{anchor_page:intro}}")` measures exactly the same width as `Rendro.text("Page 8888")`.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/measure_test.exs</automated>
  </verify>
  <done>Tokens correctly reserve deterministic layout space.</done>
</task>
</tasks>

<verification>
Ensure `mix test` passes.
</verification>

<success_criteria>
Tokens measure at a fixed width and do not cause text block dimensions to shift.
</success_criteria>

<output>
Create `.planning/phases/100-printable-table-of-contents-primitive/100-01-SUMMARY.md` when done
</output>