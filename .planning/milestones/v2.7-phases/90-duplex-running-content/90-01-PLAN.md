---
phase: 90
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rendro/section.ex
  - lib/rendro/pipeline/compose.ex
  - lib/rendro/pipeline/paginate.ex
  - test/rendro_builders_test.exs
  - test/rendro/pipeline/compose_test.exs
  - test/rendro/pipeline/paginate_test.exs
  - test/rendro/flow_test.exs
  - test/rendro/public_api/manifest_test.exs
  - test/docs_contract/public_api_contract_test.exs
  - priv/public_api.json
  - .planning/REQUIREMENTS.md
  - .planning/phases/90-duplex-running-content/90-01-SUMMARY.md
autonomous: true
requirements:
  - DUP-01
  - DUP-02
  - DUP-03

must_haves:
  truths:
    - "Rendro.section(only_on: :odd) and Rendro.section(only_on: :even) build Section structs without using the catch-all options map"
    - "Compose validates only_on and page_numbering before normalized section metadata can render"
    - "Compose preserves suppress_on and only_on on per-section layout entries so odd and even variants can coexist in the same running region"
    - "Paginate evaluates only_on against physical page parity, never section-local page numbers"
    - "Paginate applies suppress_on and only_on before RunningContent callback evaluation and PAGE-token substitution"
    - "Odd/even running content composes with section-local {{section_page_number}} and {{section_total_pages}} tokens after a section restart"
    - "Existing {{page_number}}, {{total_pages}}, suppress_on, and RunningContent fn {page, total} behavior stays backward-compatible"
    - "No public Rendro.PageContext API, recto/verso aliases, blank-page insertion, or docs support-matrix claim broadening is introduced"
  artifacts:
    - path: "lib/rendro/section.ex"
      provides: "Section only_on field and type"
      contains: "only_on"
    - path: "lib/rendro/pipeline/compose.ex"
      provides: "Section option validation and per-entry running-region metadata"
      contains: "only_on: section.only_on"
    - path: "lib/rendro/pipeline/paginate.ex"
      provides: "Physical parity filtering for running-region entries"
      contains: "only_on"
    - path: "test/rendro/pipeline/paginate_test.exs"
      provides: "DUP-01 and DUP-02 behavioral regression coverage"
      contains: "section_page_number"
    - path: "priv/public_api.json"
      provides: "Regenerated public API manifest"
      contains: "only_on/0"
  key_links:
    - from: "lib/rendro/section.ex"
      to: "lib/rendro/pipeline/compose.ex"
      via: "Section.only_on normalized into layout entries"
      pattern: "only_on"
    - from: "lib/rendro/pipeline/compose.ex"
      to: "lib/rendro/pipeline/paginate.ex"
      via: "layout.entries running-region metadata"
      pattern: "entries"
    - from: "lib/rendro/pipeline/paginate.ex"
      to: "test/rendro/pipeline/paginate_test.exs"
      via: "physical odd/even filtering with section-local tokens"
      pattern: "section_page_number"
    - from: "lib/rendro/section.ex"
      to: "priv/public_api.json"
      via: "mix rendro.api.gen"
      pattern: "only_on/0"
---

# Plan 90-01: Physical Odd/Even Running Content

<objective>
Add narrow duplex running-content support through `Rendro.section(only_on: :odd | :even)`, evaluated against physical page parity, with per-section running-region metadata, validation for malformed `only_on` and `page_numbering` options, section-token compatibility, focused tests, and regenerated public API manifest.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-duplex-running-content/90-CONTEXT.md
@.planning/phases/90-duplex-running-content/90-RESEARCH.md
@.planning/phases/90-duplex-running-content/90-PATTERNS.md
@.planning/phases/90-duplex-running-content/90-VALIDATION.md
</context>

<threat_model>
| Threat | Risk | Mitigation |
|--------|------|------------|
| Misleading output from invalid options | Medium | Validate all `%Rendro.Section{}` options in `Compose.run/1` before section metadata is normalized or rendered. |
| Wrong parity after section restart | Medium | Compute parity from physical `idx` passed to `apply_page_template/5`, never from `section_page_number`. |
| Odd/even variants collapse into one footer/header | Medium | Carry `suppress_on` and `only_on` on each normalized entry and render running entries per region. |
| Layout drift from token substitution | Low | Keep Phase 89 replacement path that mutates text/runs without remeasuring widths. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Section.only_on and compose-time validation</name>
  <files>lib/rendro/section.ex, lib/rendro/pipeline/compose.ex, test/rendro_builders_test.exs, test/rendro/pipeline/compose_test.exs</files>
  <read_first>
    - lib/rendro/section.ex
    - lib/rendro/pipeline/compose.ex
    - test/rendro_builders_test.exs
    - test/rendro/pipeline/compose_test.exs
    - .planning/phases/90-duplex-running-content/90-CONTEXT.md
    - .planning/phases/90-duplex-running-content/90-RESEARCH.md
  </read_first>
  <action>
    Update `%Rendro.Section{}` with `only_on: nil`, add `@type only_on :: nil | :odd | :even`, and include `only_on: only_on()` in `t/0`. Keep `Rendro.section/1` as a thin `struct!(Section, attrs)` builder.

    In `Rendro.Pipeline.Compose`, validate every section before normalized entries are trusted. Allow `only_on` only when it is `nil`, `:odd`, or `:even`; allow `page_numbering` only when it is `[]` or `[restart: true]`. Raise `ArgumentError` with messages that include `Invalid only_on` or `Invalid page_numbering` and the inspected invalid value. Extend normalized entries to include `suppress_on` and `only_on`, including the synthetic `:content` entry with `suppress_on: nil` and `only_on: nil`.

    Add builder and compose tests proving `Rendro.section(only_on: :odd)` builds a struct, `Compose.run/1` preserves `only_on` in `result.options.layout.entries`, and invalid `only_on` / `page_numbering` values raise before rendering.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs test/rendro/pipeline/compose_test.exs</automated>
  </verify>
  <done>Section API and compose validation tests pass; existing unknown-key struct and suppress_on conflict tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply physical parity filters per running-region entry</name>
  <files>lib/rendro/pipeline/paginate.ex, test/rendro/pipeline/paginate_test.exs, test/rendro/flow_test.exs</files>
  <read_first>
    - lib/rendro/pipeline/compose.ex
    - lib/rendro/pipeline/paginate.ex
    - test/rendro/pipeline/paginate_test.exs
    - test/rendro/flow_test.exs
    - .planning/phases/89-page-context-primitive/89-01-SUMMARY.md
    - .planning/phases/90-duplex-running-content/90-PATTERNS.md
  </read_first>
  <action>
    Replace the page-template running-region rendering path so it groups normalized layout entries by non-body region and evaluates each entry independently. For each physical page and region, select entries whose `region` equals the template region name; for every entry, apply `suppress_on` and `only_on` before `evaluate_fn_blocks/3`, `replace_page_numbers/2`, `anchor_region_blocks/3`, and `maybe_validate_region_fit/5`.

    Implement `only_on` matching from physical page index: odd when `rem(page_idx, 2) == 1`, even when `rem(page_idx, 2) == 0`. Preserve legacy `doc.header`, `doc.footer`, and `layout.region_blocks` fallback behavior for documents without entries. Do not remeasure PAGE text and do not change `RunningContent` callback arguments.

    Add tests proving odd/even footer sections can coexist, `suppress_on: :first` plus `only_on: :odd` suppresses physical page 1 before callback evaluation, and a restarted body section on an even physical page still renders `only_on: :even` with `{{section_page_number}}` / `{{section_total_pages}}` tokens from section-local context.
  </action>
  <verify>
    <automated>mix test test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
  </verify>
  <done>Paginate and flow tests prove DUP-01 and DUP-02, including combined duplex plus section-local token behavior after restart.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate API manifest, verify, and summarize</name>
  <files>priv/public_api.json, test/rendro/public_api/manifest_test.exs, test/docs_contract/public_api_contract_test.exs, .planning/REQUIREMENTS.md, .planning/phases/90-duplex-running-content/90-01-SUMMARY.md</files>
  <read_first>
    - priv/public_api.json
    - lib/mix/tasks/rendro/api.gen.ex
    - test/rendro/public_api/manifest_test.exs
    - test/docs_contract/public_api_contract_test.exs
    - /Users/jon/.codex/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Regenerate `priv/public_api.json` with `mix rendro.api.gen` after adding the public `Rendro.Section.only_on/0` type. Run focused tests, public API contract tests, full `mix test`, and `mix format --check-formatted`. If verification passes, update `.planning/REQUIREMENTS.md` to mark DUP-01, DUP-02, and DUP-03 complete and create `90-01-SUMMARY.md` with implementation commits, verification output, deviations, and self-check.
  </action>
  <verify>
    <automated>mix test test/rendro_builders_test.exs test/rendro/pipeline/compose_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs</automated>
    <automated>mix test test/rendro/public_api/manifest_test.exs test/docs_contract/public_api_contract_test.exs</automated>
    <automated>mix test</automated>
    <automated>mix format --check-formatted</automated>
  </verify>
  <done>Public API manifest includes `only_on/0`; all focused, contract, full, and format checks pass; summary exists with Self-Check: PASSED.</done>
</task>

</tasks>

<verification>
1. `mix test test/rendro_builders_test.exs test/rendro/pipeline/compose_test.exs test/rendro/pipeline/paginate_test.exs test/rendro/flow_test.exs`
2. `mix test test/rendro/public_api/manifest_test.exs test/docs_contract/public_api_contract_test.exs`
3. `mix test`
4. `mix format --check-formatted`
</verification>

<success_criteria>
- DUP-01: Header/footer/running-region sections support `only_on: :odd | :even` on physical page parity.
- DUP-02: Duplex filtering works with section-local PAGE tokens in the same running content path after a section restart and no second render pass.
- DUP-03: Invalid `only_on` and `page_numbering` values raise instructive errors before rendering.
- Existing PAGE, suppression, running-content callback, flow, and public API contract tests pass.
</success_criteria>
