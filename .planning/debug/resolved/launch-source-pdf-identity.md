---
status: resolved
trigger: "Plan 130-06 launch publication preflight reports ten source-PDF hash drifts between the accepted exact-SHA launch artifact and PDFs generated from the current checkout"
created: 2026-08-20
updated: 2026-08-20
---

# Debug Session: Launch Source-PDF Identity

## Symptoms

### Expected behavior

Plan 130-06 should verify the accepted exact-SHA staged launch family against the current checkout, pass the pinned PDFium launch check and focused contracts, and publish exactly the reviewed 19-path patch atomically.

### Actual behavior

Staging HEAD/provenance, both authorized golden targets, the exact ten-image gallery inventory, the byte-stable branded control, and all six approved light-review records pass preflight. All ten source-PDF hashes in the staged launch manifest differ from PDFs generated by the current checkout, so publication stops before any edit.

### Error messages

`mix rendro.launch_artifacts.check --pdfium /tmp/rendro-phase127-pdfium/pdfium-cli` reports ten source-PDF hash drifts and exits 8. The focused launch/docs/rubric test set fails its static launch contract for the same ten drifts. `mix compile --force` does not change the result.

### Timeline

Observed on 2026-08-20 when executing Plan 130-06 after Plans 130-01 through 130-05 and 130-07 through 130-09 completed. Plan 130-08 accepted the launch artifact from its authorized exact-SHA CI run into detached staging; later candidate/review routing and evidence work completed before publication.

### Reproduction

From the canonical checkout, run the Plan 130-06 launch preflight and `mix rendro.launch_artifacts.check` with the verified pinned PDFium binary, then compare all ten manifest `source_pdf_sha256` values with the PDFs generated from the current checkout.

## Scope Constraints

- Preserve the exact 19-path publication boundary until root cause is proven.
- Do not regenerate or publish launch artifacts, alter recipe/public API/dependency/catalog membership, rewrite approvals, or weaken hash checks merely to make the gate pass.
- Distinguish stale compilation/build state, environment nondeterminism, source-SHA mismatch, and invalid artifact/base pairing using exact hashes and commits.
- Prefer the narrowest fail-closed repair that restores a truthful one-to-one artifact/source identity contract.
- User has granted standing permission for recommended in-scope non-destructive fixes; no routine command authorization is needed.

## Current Focus

hypothesis: Confirmed — the pre-copy canonical checker was comparing current renders with the previous manifest; the corrected plan pairs the staged family before invoking that checker.
test: Validate the plan’s explicit staging-identity -> no-canonical-check -> copy -> launch/contracts-check ordering, and run the isolated launch-artifact unit suite.
expecting: The ordering assertion and isolated unit suite pass; the unpaired canonical static-contract check remains expected to fail until the executor overlays the staged family.
next_action: Executor: from a clean canonical checkout, execute Plan 130-06 exactly as written — validate staging provenance/identity, assemble only the explicit 19-path uncommitted batch, run the launch/contracts gate on that paired batch (using the authorized exact-SHA CI PDFium proof for advisory raster validation), enforce the inventory/fences, then create its sole publication commit.
bug_class: Bohrbug (all ten hashes drift deterministically after a forced compilation)
reasoning_checkpoint:
  hypothesis: "Plan 130-06 executes the launch checker before copying the exact staged launch family, causing an intentional old manifest to be compared to current source PDFs; the resulting ten hash drifts are valid and block the necessary overlay."
  confirming_evidence:
    - "Every one of the checker’s ten actual source-PDF SHA-256 values exactly matches the staged manifest value for that ID."
    - "The staging worktree is pinned to producer SHA 2463749… and no launch render-closure path differs through canonical HEAD; only the ten staged identities differ from the old canonical manifest."
  falsification_test: "If a checker actual hash differed from its staged manifest value, or a launch render-closure path differed from the producer commit, then copying staging first would not restore a truthful source/artifact identity."
  fix_rationale: "Make staging identity checks the pre-copy gate, copy only the explicit generator-owned paths and goldens into the uncommitted canonical batch, then run the existing checker/contracts against the now-paired source and manifest before the one permitted commit."
  blind_spots: "The pinned Linux PDFium binary cannot execute on this macOS host, so local advisory raster validation remains unavailable; accepted exact-SHA CI provides that independent renderer evidence."
  candidate_causes:
    - "code: the Plan 130-06 instruction orders/phrases the checker as a pre-copy condition despite its manifest being among the staged files to copy."
    - "environment: host cannot execute the pinned Linux PDFium binary, but this occurs after the static source-PDF comparisons and cannot create their deterministic values."
    - "data: canonical artifacts.json contains the prior artifact identity while staging contains the accepted current identity."
  and_gate: "no — the plan-ordering defect alone fully explains the static failure; the host PDFium incompatibility is independent and only limits local advisory verification."
tdd_checkpoint:

## Evidence

- timestamp: 2026-08-20
  checked: Current debug-session symptoms and initial codebase index
  found: The committed launch manifest contains source_pdf_sha256 values, while the launch checker emits one source-PDF drift per gallery spec. The failure persists after `mix compile --force` and uses the pinned PDFium CLI.
  implication: The shared, repeatable ten-item failure is not explained by a stale BEAM build alone or by PDFium rasterization; it is a deterministic mismatch between manifest source PDFs and PDFs rendered from the present source tree.

- timestamp: 2026-08-20
  checked: Launch-artifact implementation and phase-130 provenance summary
  found: `Rendro.LaunchArtifacts.check/1` invokes `static_contract_errors/1`, which rerenders all source PDFs and compares their SHA-256 values directly to `assets/rendro/artifacts.json`; the accepted artifact records rendered source commit `2463749d2b877c4761f2b7bf3a25dd1e8673a9bd` and the pinned PDFium binary identity separately.
  implication: PDFium is irrelevant to the observed source-PDF-hash failures. The decisive comparison is the effective Elixir source/compile identity used by commit `2463749…` against the current checkout.

- timestamp: 2026-08-20
  checked: Canonical and staged `assets/rendro/artifacts.json` source-PDF identities, staging git state, and a fresh canonical checker run
  found: The checker actual hashes for invoice, statement, receipt_report, certificate, payslip, ticket, invoice_dark, certificate_dark, ticket_dark, and invoice_brand exactly equal the corresponding staged manifest values. The staged worktree is at `2463749d2b877c4761f2b7bf3a25dd1e8673a9bd`, has the intended 17 tracked launch/golden modifications, and no `lib`, `priv/examples`, or `assets/rendro` render-closure file differs between that commit and HEAD. `branded_invoice` is unchanged in both manifests.
  implication: This refutes both a source-SHA mismatch and an Elixir build-state explanation. The check failure is the expected consequence of checking the old canonical manifest before copying the accepted staged artifact family; the source render identity is already correct.

- timestamp: 2026-08-20
  checked: Pinned PDFium invocation
  found: `/tmp/rendro-phase127-pdfium/pdfium-cli` is an ELF binary that cannot execute on this macOS host (`exec format error`). After static source-PDF checks run, the full checker therefore also reports a PDFium CLI failure.
  implication: The host cannot independently execute the advisory raster phase, but this is separate from and cannot explain the ten static source-PDF drifts; the accepted CI route remains the renderer evidence.

- timestamp: 2026-08-20
  checked: Corrected Plan 130-06 action sequencing and focused verification
  found: The plan now requires staged-manifest source-PDF identity before copying, explicitly excludes the canonical launch check at that point, copies the exact staged generator-owned family, and only then requires launch/contracts checks on the paired batch. A structural ordering assertion and `mix test test/rendro/launch_artifacts_test.exs --max-failures 1` pass. The broader contract command fails pre-overlay with exactly the known ten drifts in `launch_artifacts_claims_test`, as expected because canonical `artifacts.json` is intentionally still prior identity.
  implication: The plan repair addresses the root cause without weakening checks. Publication remains an executor-owned operation, and its post-copy gate is the correct point for the full focused contract suite.

- timestamp: 2026-08-20
  checked: Human verification authorization
  found: The user authorized accepted exact-SHA CI pinned-Linux-PDFium proof as advisory renderer evidence and explicitly declined a manual local renderer run.
  implication: The macOS/Linux binary incompatibility no longer blocks closure of this debugging session; it remains an executor instruction to retain the accepted CI evidence during publication.

## Eliminated

- hypothesis: the accepted artifact was generated from a different launch-source closure than the current checkout
  evidence: Current checker actual hashes equal all ten staged-manifest hashes, and no render-closure file differs from producer commit `2463749…` through HEAD.
  timestamp: 2026-08-20

- hypothesis: stale BEAM compilation or PDFium behavior caused the source-PDF drift
  evidence: `mix compile --force` did not change the deterministic values; source-PDF validation runs before PDFium rasterization, and the current render values match staged manifest values exactly.
  timestamp: 2026-08-20

## Resolution

root_cause: Plan 130-06 treats the current canonical `artifacts.json` as if it were already the accepted staged launch artifact and runs the source-PDF checker before copying the staged family. The check correctly rejects the prior ten manifest hashes; current renders already equal the staged manifest hashes.
fix: Clarify Plan 130-06’s preflight ordering: validate staging provenance and source-PDF identity against staging first, copy only the explicit staged launch/golden files into the uncommitted canonical batch, then run existing launch/contracts checks before the single publication commit.
verification:
  target_test: {result: pass, command: "awk ordering assertion on .planning/phases/130-catalog-quality-evidence-ratchet/130-06-PLAN.md", oracle_type: derived, detail: "the staged identity preflight, explicit pre-copy canonical-check exclusion, staging copy, and paired-batch launch/contracts gate occur in that strict order"}
  mutation_check: {result: skipped, reason_if_skipped: "No mutation tool is configured for planning-document ordering changes."}
  no_op_deletion: {result: pass, deletion_justified_by_rca: false, detail: "The one-line plan change preserves every provenance, inventory, contract, and rollback gate while moving the checker after the required overlay."}
  adjacent_tests: {result: pass, suites_run: ["mix test test/rendro/launch_artifacts_test.exs --max-failures 1", "git diff --check -- .planning/phases/130-catalog-quality-evidence-ratchet/130-06-PLAN.md"], detail: "isolated launch-artifact suite passed; plan diff has no whitespace errors"}
  revert_and_reconfirm: {result: skipped, reason_if_skipped: "The corrective change is execution-plan ordering. Reverting it would require executing the prohibited pre-copy publication flow; the direct counterfactual was instead observed in the original pre-copy checker failure and the staged-identity assertion."}
  pre_overlay_contract: {result: expected_fail, command: "mix test test/rendro/launch_artifacts_test.exs test/docs_contract/launch_artifacts_claims_test.exs test/docs_contract/theming_claims_test.exs test/docs_contract/rubric_manifest_contract_test.exs --max-failures 1", detail: "launch_artifacts_claims_test reports exactly the ten known source-PDF drifts because the canonical manifest remains deliberately unpaired; this is the condition the executor’s copied-batch gate must remove"}
  advisory_renderer_proof: {result: accepted, detail: "User authorized the accepted exact-SHA CI pinned-Linux-PDFium proof as advisory renderer verification; local execution is unavailable because the binary is Linux ELF on macOS."}
  guardrail_verdict: accepted
oracle_type: derived
files_changed:
  - .planning/phases/130-catalog-quality-evidence-ratchet/130-06-PLAN.md
